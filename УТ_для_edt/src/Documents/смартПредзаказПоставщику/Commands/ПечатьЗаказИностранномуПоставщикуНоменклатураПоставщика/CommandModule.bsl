&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	//Если УправлениеПечатьюКлиент.ПроверитьДокументыПроведены(ПараметрКоманды, ПараметрыВыполненияКоманды.Источник) Тогда
	
		УправлениеПечатьюКлиент.ВыполнитьКомандуПечати(
			"Документ.смартПредзаказПоставщику",
			"ПредзаказИностранномуПоставщикуНоменклатураПоставщика",
			ПараметрКоманды,
			Неопределено,
			Новый Структура("Тип", "ПредзаказИностранномуПоставщикуНоменклатураПоставщика")
		);
	
	//КонецЕсли;
	
КонецПроцедуры
