// BSLLS:MissingVariablesDescription-off
// BSLLS:UnusedLocalVariable-off
Перем Кэш Экспорт;

//******************************************
//{		ПОДКЛЮЧЕНИЕ В DDPro

	Перем ОсновнойМодуль Экспорт;

//}		ПОДКЛЮЧЕНИЕ В DDPro

//******************************************
//{		ПОДКЛЮЧЕНИЕ В DDStandart

Перем Манифест 				Экспорт;
Перем ОбработкаОбъект 		Экспорт;
Перем ПараметрыКлиентСервер Экспорт;
Перем НомерИтерацииВызоваМодуля;
// BSLLS:UnusedLocalVariable-on
// BSLLS:MissingVariablesDescription-on

Процедура Инициализировать(ИмяМодуля) Экспорт
	
	Если НомерИтерацииВызоваМодуля = Неопределено Тогда
		НомерИтерацииВызоваМодуля = 0;
	КонецЕсли;
	
	НомерИтерацииВызоваМодуля = НомерИтерацииВызоваМодуля + 1;
	
	Если Манифест = Неопределено Тогда
		ЗаполнитьМанифест(ЭтотОбъект, ИмяМодуля);
	КонецЕсли;
	
КонецПроцедуры

Процедура ОчиститьПеременные() Экспорт
	
	// Разрывается циклическая ссылка, для того чтобы исключить утечку памяти.
	// Модуль может быть вызван повторно во вложенных методах,
	// поэтому очищаем переменные, ТОЛЬКО если это начальная итерация вызова модуля.
	
	НомерИтерацииВызоваМодуля = НомерИтерацииВызоваМодуля - 1;
	
	Если НомерИтерацииВызоваМодуля = 0 Тогда
		ОбработкаОбъект			 = Неопределено;
		ПараметрыКлиентСервер	 = Неопределено;
	КонецЕсли;
	
КонецПроцедуры

// Может пригодиться при отладке в режиме управляемых форм, когда точки остановки в открытом модуле не срабатывают.
// Достаточно вставить вызов данной процедуры в нужном месте и включить остановку по ошибке.
Процедура ПриглашениеОтладки() Экспорт
	
	// Удалите комментарий напротив оператора "ВызватьИсключение", чтобы получить приглашение для начала отладки модуля.
	// ВызватьИсключение "Нажмите кнопку ""Подробно"", а затем ""Конфигуратор"", чтобы начать отладку модуля!"; 
	
КонецПроцедуры

Процедура ЗаполнитьМанифест(Модуль, ИмяМодуля) Экспорт
	
	ФункцииМодуля = ФункцииМодуля();
	
	Манифест = Новый Структура;
	Манифест.Вставить("Функции", ФункцииМодуля);
	
КонецПроцедуры

Функция ФункцииМодуля()
	
	Результат = Новый Структура;
	
	Результат.Вставить("ВерсияОбработки");
	Результат.Вставить("КодыРегионов");
	Результат.Вставить("ПолучитьОКВ");
	Результат.Вставить("ПолучитьОКСМ");
	Результат.Вставить("ПолучитьОКЕИ");
	Результат.Вставить("НаименованиеЕдиницыИзмерения");
	Результат.Вставить("ПодходящиеЕдиницыИзмерения");
	Результат.Вставить("XML_КодЕдиницыИзмерения");
	Результат.Вставить("НаименованиеВалюты");
	Результат.Вставить("ПодходящаяВалюта");
	Результат.Вставить("ПараметрыИсчисленияВалюты");
	Результат.Вставить("НаименованиеСтраны");
	Результат.Вставить("КодСтраны");
	Результат.Вставить("ПодходящиеСтраны");
	Результат.Вставить("НаименованиеРегиона");
	Результат.Вставить("XML_В_ОбъектXDTO");
	Результат.Вставить("XML_В_ProtoСтруктура");
	Результат.Вставить("XML_В_ProtoОбъектXDTO");
	Результат.Вставить("XML_ФабрикаXDTO");
	Результат.Вставить("НовыйProtoОбъектXDTO");
	Результат.Вставить("ЗаполнитьПротоКонтентXDTOПоСтруктуре");
	Результат.Вставить("ЗаполнитьУКД736ПоДаннымУКД189");
	Результат.Вставить("ОбъектXDTO_В_ДвоичныеДанные");
	Результат.Вставить("ОбъектXDTO_В_ProtoСтруктура");
	Результат.Вставить("ОбъектXDTO_В_ProtoОбъектXDTO");
	Результат.Вставить("ПротоКонтент_В_ПротоКонтентXDTO");
	Результат.Вставить("ОбъектXDTOВСтруктуру");
	Результат.Вставить("ProtoФабрикаXDTO");
	Результат.Вставить("ПолучитьUtdSellerContentИзInvoiceContent");
	Результат.Вставить("ПолучитьUcdSellerContentИзInvoiceCorrectionContent");
	Результат.Вставить("СтрокаАдрес");
	
	Результат.Вставить("XML_ПараметрыФормированияФайла");
	Результат.Вставить("XML_ПолучитьXML_УПД_155");
	Результат.Вставить("XML_ПолучитьXML_УПД_820");
	Результат.Вставить("XML_ПолучитьXML_УКД");
	Результат.Вставить("XML_ПолучитьXML_УКД_736");
	Результат.Вставить("XML_ПолучитьXML_Акт_552");
	Результат.Вставить("XML_ПолучитьXML_Торг_551");
	Результат.Вставить("XML_ПолучитьXML_Счет");
	Результат.Вставить("XML_ПолучитьXML_Торг2");
	Результат.Вставить("XML_ПолучитьXML_АктСверки_405");
	Результат.Вставить("XML_ОтветныйТитул_УПД_820");
	Результат.Вставить("XML_ОтветныйТитул_УПД_155");
	Результат.Вставить("XML_ОтветныйТитул_УКД");
	Результат.Вставить("XML_ОтветныйТитул_УКД_736");
	Результат.Вставить("XML_ОтветныйТитул_Торг_551");
	Результат.Вставить("XML_ОтветныйТитул_Акт_552");
	Результат.Вставить("XML_ОтветныйТитул_Торг_172");
	Результат.Вставить("XML_ОтветныйТитул_Акт_172");
	Результат.Вставить("XML_ОтветныйТитул_Торг2");
	Результат.Вставить("XML_ОтветныйТитул_АктСверки_405");

	Результат.Вставить("ДобавитьОшибкуВалидации");
	Результат.Вставить("ДобавитьГруппуОшибокВалидации");
	Результат.Вставить("ДополнитьМассивОшибокВалидации");
	Результат.Вставить("ОформитьОшибкиВHTML");
	Результат.Вставить("КодЕдиницыИзмеренияСоответствуетКлассификатору");
	Результат.Вставить("СтруктураЗаполнена");
	
	Результат.Вставить("ВалидацияXmlTorg12Content");
	Результат.Вставить("ВалидацияXmlAcceptanceCertificateContent");
	Результат.Вставить("ВалидацияInvoiceContent");
	Результат.Вставить("ВалидацияInvoiceCorrectionContent");
	Результат.Вставить("ВалидацияUTD820");
	Результат.Вставить("ВалидацияUTD");
	Результат.Вставить("ВалидацияUCD");
	Результат.Вставить("ВалидацияUCD736");
	Результат.Вставить("ВалидацияТорг2");
	
	Возврат Результат;
	
КонецФункции

//}		ПОДКЛЮЧЕНИЕ В DDStandart


//**************************************
//{		ВЕРСИЯ

Функция ВерсияОбработки()
	
	Версия = "4.13.6";
	
	Результат = Версия + "." + НомерСборки();
	
	Возврат Результат;
	
КонецФункции

Функция НомерСборки()
	
	Возврат "162";
	
КонецФункции

//}		ВЕРСИЯ


//**************************************
//{		ПОДКЛЮЧЕНИЕ ВО ВНЕШНИЕ ОБРАБОТКИ

Функция СведенияОВнешнейОбработке() Экспорт
	
	ПараметрыРегистрации = Новый Структура;
	ПараметрыРегистрации.Вставить("Вид"			   , "ДополнительнаяОбработка");
	ПараметрыРегистрации.Вставить("Наименование"   , "Диадок (СКБ Контур): парсер электронных документов");
	ПараметрыРегистрации.Вставить("БезопасныйРежим", Ложь);
	ПараметрыРегистрации.Вставить("Версия"		   , ВерсияОбработки());
	ПараметрыРегистрации.Вставить("Информация"	   , "Парсер электронных документов Диадок");
	
	ПараметрыРегистрации.Вставить("Команды", Новый ТаблицаЗначений);
	ПараметрыРегистрации.Команды.Колонки.Добавить("Представление"		, Новый ОписаниеТипов("Строка")); 
	ПараметрыРегистрации.Команды.Колонки.Добавить("Идентификатор"		, Новый ОписаниеТипов("Строка"));
	ПараметрыРегистрации.Команды.Колонки.Добавить("Использование"		, Новый ОписаниеТипов("Строка"));
	ПараметрыРегистрации.Команды.Колонки.Добавить("ПоказыватьОповещение", Новый ОписаниеТипов("Булево"));
	ПараметрыРегистрации.Команды.Колонки.Добавить("Модификатор"		 	, Новый ОписаниеТипов("Строка"));
	
	Возврат ПараметрыРегистрации;
	
КонецФункции

//}		ПОДКЛЮЧЕНИЕ ВО ВНЕШНИЕ ОБРАБОТКИ


//******************************************
//{		КЛАССИФИКАТОРЫ

Функция КодыРегионов() Экспорт
	
	Результат = Кэш_Прочитать("КодыРегионов");
	
	Если Результат = Неопределено Тогда
		
		Результат = Новый Соответствие;
		
		// Сериализуем через произвольный ОбъектXDTO для совместимости с 8.1
		ОбъектXDTO = XML_В_XDTO(ПолучитьМакет("КодыРегионов").ПолучитьТекст());
		
		Для Каждого СтрокаXDTO Из ОбъектXDTO.pair Цикл
			Результат.Вставить(СтрокаXDTO.Key, СтрокаXDTO.Value);
		КонецЦикла;
		
		Кэш_Поместить("КодыРегионов", Результат);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьОКВ() Экспорт
	
	Результат = Кэш_Прочитать("ОКВ");
	
	Если Результат = Неопределено Тогда
		
		Результат = Новый ТаблицаЗначений;
		
		// Сериализуем через произвольный ОбъектXDTO для совместимости с 8.1
		ОбъектXDTO = XML_В_XDTO(ПолучитьМакет("ОКВ").ПолучитьТекст());
		
		МассивКолонок = РазложитьСтрокуВМассивСлов(ОбъектXDTO.Columns);
		Для Каждого ИмяКолонки Из МассивКолонок Цикл
			
			Если ИмяКолонки = "RBCLoading" Тогда
				ТипКолонки = Новый ОписаниеТипов("Булево");
			Иначе
				ТипКолонки = Новый ОписаниеТипов("Строка");
			КонецЕсли;
			
			Результат.Колонки.Добавить(ИмяКолонки, ТипКолонки);
			
		КонецЦикла;
		
		Для Каждого СтрокаXDTO Из ОбъектXDTO.Item Цикл
			
			НоваяСтрока = Результат.Добавить();
			
			ЗаполнитьЗначенияСвойств(НоваяСтрока, СтрокаXDTO, , "RBCLoading");
			
			НоваяСтрока.RBCLoading = НРег(СтрокаXDTO.RBCLoading) = "истина";
			
		КонецЦикла;
		
		Результат.Колонки.Code.Имя					= "Код";
		Результат.Колонки.CodeSymbol.Имя			= "КодСимвольный";
		Результат.Колонки.Name.Имя					= "Наименование";
		Результат.Колонки.Description.Имя			= "Описание";
		Результат.Колонки.RBCLoading.Имя			= "ЗагрузкаИзИнтернета";
		Результат.Колонки.NumerationItemOptions.Имя = "ПараметрыИсчисления";
		
		Результат.Индексы.Добавить("Код");
		
		ДобавитьСтроковыйИндексВТаблицуЗначений(Результат, "КодСимвольный");
		ДобавитьСтроковыйИндексВТаблицуЗначений(Результат, "Наименование");
		
		Кэш_Поместить("ОКВ", Результат);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьОКСМ() Экспорт
	
	Результат = Кэш_Прочитать("ОКСМ");
	
	Если Результат = Неопределено Тогда
		
		// Сериализуем через произвольный ОбъектXDTO для совместимости с 8.1
		Результат = XDTO_В_Таблицу(XML_В_XDTO(ПолучитьМакет("ОКСМ").ПолучитьТекст()));
		
		Результат.Колонки.Наименование.Имя = "НаименованиеКраткое";
		
		ДобавитьСтрокуВ_ОКСМ(Результат, "Евросоюз",			"", "", "980", "Европейский союз");
		ДобавитьСтрокуВ_ОКСМ(Результат, "ЕС",				"", "", "980", "Европейский союз");
		ДобавитьСтрокуВ_ОКСМ(Результат, "Европейский союз",	"", "", "980", "Европейский союз");
		ДобавитьСтрокуВ_ОКСМ(Результат, "ЕАЭС",				"", "", "981", "Евразийский экономический союз");
		
		ДобавитьСтроковыйИндексВТаблицуЗначений(Результат, "НаименованиеКраткое");
		ДобавитьСтроковыйИндексВТаблицуЗначений(Результат, "НаименованиеПолное");
		
		Кэш_Поместить("ОКСМ", Результат);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьОКЕИ() Экспорт
	
	Результат = Кэш_Прочитать("ОКЕИ");
	
	Если Результат = Неопределено Тогда
		
		// Сериализуем через произвольный ОбъектXDTO для совместимости с 8.1
		Дерево = XDTO_В_Дерево(XML_В_XDTO(ПолучитьМакет("ОКЕИ").ПолучитьТекст()));
		Дерево.Строки.Удалить(Дерево.Строки.Найти("Международные единицы измерения, не включенные в ОКЕИ"));
		
		Результат = Дерево_В_Таблицу(Дерево, Ложь);
		Дерево = Неопределено;
		
		Результат.Колонки.КодЧисловой.Имя					  = "Код";
		Результат.Колонки.Наименование.Имя					  = "НаименованиеПолное";
		Результат.Колонки.УсловноеОбозначениеНациональное.Имя = "НаименованиеКраткое";
		
		Для каждого СтрокаТЗ Из Результат Цикл
			СтрокаТЗ.НаименованиеПолное	 = СтрЗаменить(СтрокаТЗ.НаименованиеПолное , Символы.ПС, "/");
			СтрокаТЗ.НаименованиеКраткое = СтрЗаменить(СтрокаТЗ.НаименованиеКраткое, Символы.ПС, "/");
		КонецЦикла;
		
		Результат.Индексы.Добавить("Код");
		
		ДополнитьТаблицуОКЕИ(Результат);
		
		ДобавитьСтроковыйИндексВТаблицуЗначений(Результат, "НаименованиеКраткое");
		ДобавитьСтроковыйИндексВТаблицуЗначений(Результат, "НаименованиеПолное");
		
		Кэш_Поместить("ОКЕИ", Результат);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает наименование единицы измерения по коду.
//
// Параметры:
//	 КодОКЕИ			- Строка - Код единицы измерения.
//	 ПолноеНаименование - Булево - (необязательное) Признак того, что нужно вернуть полное наименование.
//
// Возвращаемое значение:
//	 Строка	- Наименование единицы измерения, пример:
//   	шт	  - Краткое Наименование
//		Штука - Полное наименование
//
Функция НаименованиеЕдиницыИзмерения(КодОКЕИ, ПолноеНаименование = Ложь) Экспорт
	
	Результат = "";
	
	Если ЗначениеЗаполнено(КодОКЕИ) Тогда
		
		СтрокаОКЕИ = ПолучитьОКЕИ().Найти(СокрЛП(КодОКЕИ), "Код");
		
		Если СтрокаОКЕИ <> Неопределено Тогда
			
			Если ПолноеНаименование = Истина Тогда
				Результат = СтрокаОКЕИ.НаименованиеПолное;
			Иначе
				Результат = СтрокаОКЕИ.НаименованиеКраткое;
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции		

// Возвращает строки таблицы значения ОКЕИ подходящие по краткому или полному наименованию.
//
// Параметры:
//	 МассивСтрока - Строка, Массив - Краткое или полное наименование единицы измерения или массив таких строк.
//
// Возвращаемое значение:
//	 Массив	- Массив строк таблицы значений ОКЕИ
//
Функция ПодходящиеЕдиницыИзмерения(МассивСтрока) Экспорт
	
	Результат = Новый Массив;
	
	Если ЗначениеЗаполнено(МассивСтрока) Тогда
		
		Если ТипЗнч(МассивСтрока) = Тип("Массив") Тогда
			МассивЗначений = МассивСтрока;
		Иначе
			МассивЗначений = Новый Массив;
			МассивЗначений.Добавить(МассивСтрока);
		КонецЕсли;
		
		Результат = СтрокиКлассификатораПоМассивуЗначений(ПолучитьОКЕИ(), МассивЗначений);
		
	КонецЕсли;
	
	Возврат Результат;
		
КонецФункции		

// Код единицы измерения по Общероссийскому классификатору единиц измерения 
// или "0000" (при отсутствии необходимой единицы измерения в ОКЕИ)
//
// Параметры:
//  КодЕдиницыИзмерения	 - Строка - Код единицы измерения.
//
// Возвращаемое значение:
//  Строка - Приведенный код единицы измерения к формату ФНС
//
Функция XML_КодЕдиницыИзмерения(КодЕдиницыИзмерения) Экспорт
	
	Результат = ?(ПолучитьОКЕИ().Найти(КодЕдиницыИзмерения, "Код") = Неопределено, "0000", КодЕдиницыИзмерения);
	Возврат Результат;
	
КонецФункции

// Возвращает наименование страны по коду.
//
// Параметры:
//	 КодСтраны		    - Строка - Код страны.
//	 ПолноеНаименование - Булево - (необязательное) Признак того, что нужно вернуть полное наименование.
//
// Возвращаемое значение:
//	 Строка	- Наименование страны, пример:
//   	РОССИЯ				 - Краткое Наименование
//		Российская Федерация - Полное наименование
//
Функция НаименованиеСтраны(КодСтраны, ПолноеНаименование = Ложь) Экспорт
	
	Результат = "";
	
	СтрокаОКСМ = ПолучитьОКСМ().Найти(КодСтраны, "Код");
	
	Если СтрокаОКСМ <> Неопределено Тогда
		
		Если ПолноеНаименование = Истина Тогда
			Результат = СтрокаОКСМ.НаименованиеПолное;
		Иначе
			Результат = СтрокаОКСМ.НаименованиеКраткое;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает код страны по краткому или полному наименованию.
//
// Параметры:
//	 НаименованиеСтраны - Строка - Краткое или полное наименование страны.
//
// Возвращаемое значение:
//	 Строка	- Код страны
//
Функция КодСтраны(НаименованиеСтраны) Экспорт
	
	Результат = "";
	
	ЗначениеИндекса = ВРег(СтрЗаменить(СтрЗаменить(НаименованиеСтраны, " ", ""), ".", ""));
	
	СтрокаОКСМ = ПолучитьОКСМ().Найти(ЗначениеИндекса, "Индекс_НаименованиеКраткое");
	
	Если СтрокаОКСМ = Неопределено Тогда
		СтрокаОКСМ = ПолучитьОКСМ().Найти(ЗначениеИндекса, "Индекс_НаименованиеПолное");
	КонецЕсли;
	
	Если СтрокаОКСМ <> Неопределено Тогда
		Результат = СтрокаОКСМ.Код;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает строки таблицы значения ОКСМ подходящие по краткому или полному наименованию.
//
// Параметры:
//	 МассивСтрока - Строка, Массив - Краткое или полное наименование страны или массив таких строк.
//
// Возвращаемое значение:
//	 Массив	- Массив строк таблицы значений ОКСМ
//
Функция ПодходящиеСтраны(МассивСтрока) Экспорт
	
	Результат = Новый Массив;
	
	Если ЗначениеЗаполнено(МассивСтрока) Тогда
		
		Если ТипЗнч(МассивСтрока) = Тип("Массив") Тогда
			МассивЗначений = МассивСтрока;
		Иначе
			МассивЗначений = Новый Массив;
			МассивЗначений.Добавить(МассивСтрока);
		КонецЕсли;
		
		Результат = СтрокиКлассификатораПоМассивуЗначений(ПолучитьОКСМ(), МассивЗначений);
		
	КонецЕсли;
	
	Возврат Результат;
		
КонецФункции		

// Возвращает символьный код или наименование валюты по коду.
//
// Параметры:
//	 КодВалюты	  - Строка - Код валюты
//	 Наименование - Булево - (необязательное) Признак того, что нужно вернуть наименование валюты.
//
// Возвращаемое значение:
//	 Строка	- Наименование валюты, пример:
//   	RUB 			 - Символьный код валюты
//		Российский рубль - Наименование валюты
//
Функция НаименованиеВалюты(КодВалюты, Наименование = Ложь) Экспорт
	
	Результат = "";
	
	СтрокаОКВ = ПолучитьОКВ().Найти(КодВалюты, "Код");
	
	Если СтрокаОКВ <> Неопределено Тогда
		
		Если Наименование = Истина Тогда
			Результат = СтрокаОКВ.Наименование;
		Иначе
			Результат = СтрокаОКВ.КодСимвольный;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает строки таблицы значения ОКВ подходящие по символьному коду или наименованию валюты.
//
// Параметры:
//	 МассивСтрока - Строка, Массив - Символьный код или наименование валюты или массив таких строк.
//
// Возвращаемое значение:
//	 Массив	- Массив строк таблицы значений ОКВ
//
Функция ПодходящаяВалюта(МассивСтрока) Экспорт
	
	Результат = Новый Массив;
	
	Если ЗначениеЗаполнено(МассивСтрока) Тогда
		
		Если ТипЗнч(МассивСтрока) = Тип("Массив") Тогда
			МассивЗначений = МассивСтрока;
		Иначе
			МассивЗначений = Новый Массив;
			МассивЗначений.Добавить(МассивСтрока);
		КонецЕсли;
		
		Результат = СтрокиКлассификатораПоМассивуЗначений(ПолучитьОКВ(), МассивЗначений);
		
	КонецЕсли;
	
	Возврат Результат;
		
КонецФункции		

// Возвращает параметры исчисления валюты по коду.
// Параметры исчисления есть только для RUB, EUR, USD.
//
// Параметры:
//	 КодВалюты - Строка - Код валюты.
//
// Возвращаемое значение:
//	 Строка	- Параметры исчисления валюты, пример: "рубль, рубля, рублей, м, копейка, копейки, копеек, ж, 2 знака"
//
Функция ПараметрыИсчисленияВалюты(КодВалюты) Экспорт
	
	Результат = "";

	СтрокаОКВ = ПолучитьОКВ().Найти(КодВалюты, "Код");
	
	Если СтрокаОКВ <> Неопределено Тогда
		Результат = СтрокаОКВ.ПараметрыИсчисления;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает наименование региона России, по числовому коду.
//
// Параметры:
//	 КодРегиона - Число, Строка - Код региона.
//
Функция НаименованиеРегиона(КодРегиона) Экспорт
	
	КодРегионаЧислом = Привести_К_Числу(КодРегиона);
	Результат = КодыРегионов()[КодРегионаЧислом];
	
	Если Не ЗначениеЗаполнено(Результат) Тогда
		Результат = "";
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СтрокиКлассификатораПоМассивуЗначений(Классификатор, МассивЗначений)
	
	Результат = Новый Массив;
	
	СтрокиРезультата = Новый Соответствие;
	
	Для Каждого Значение Из МассивЗначений Цикл
		
		ЗначениеИндекса = ВРег(СтрЗаменить(СтрЗаменить(Значение, " ", ""), ".", ""));
		
		Для Каждого Колонка Из Классификатор.Колонки Цикл
			
			Если Лев(Колонка.Имя, 7) = "Индекс_" Тогда
				
				НайденныеСтроки = Классификатор.НайтиСтроки(Новый Структура(Колонка.Имя, ЗначениеИндекса));
				
				Для Каждого Строка Из НайденныеСтроки Цикл
					
					Если СтрокиРезультата[Строка] <> Истина Тогда
						Результат.Добавить(Строка);
						СтрокиРезультата.Вставить(Строка, Истина);
					КонецЕсли;
					
				КонецЦикла;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ПодходящаяВалютаПоНаименованию(ИмяВалюты)
	
	ЗначениеИндекса = ВРег(СтрЗаменить(СтрЗаменить(ИмяВалюты, " ", ""), ".", ""));
	
	Классификатор = ПолучитьОКВ();
	
	Отбор = Новый Структура;
	Отбор.Вставить("Индекс_Наименование", ЗначениеИндекса);
	
	Результат = Классификатор.НайтиСтроки(Отбор);
	
	Возврат Результат;
	
КонецФункции

Процедура ДобавитьСтроковыйИндексВТаблицуЗначений(ТаблицаЗначений, ИмяКолонки)
	
	ИмяКолонкиИндекса = "Индекс_" + ИмяКолонки;
	
	ТаблицаЗначений.Колонки.Добавить(ИмяКолонкиИндекса, ТаблицаЗначений.Колонки[ИмяКолонки].ТипЗначения);
	
	Для Каждого Строка Из ТаблицаЗначений Цикл
		Строка[ИмяКолонкиИндекса] = Врег(СтрЗаменить(СтрЗаменить(Строка[ИмяКолонки], " ", ""), ".", ""));
	КонецЦикла;
	
	ТаблицаЗначений.Индексы.Добавить(ИмяКолонкиИндекса);
	
КонецПроцедуры

Процедура ДобавитьСтрокуВ_ОКСМ(ОКСМ, НаименованиеКраткое, КодАльфа2, КодАльфа3, Код, НаименованиеПолное)
	
	НоваяСтрока = ОКСМ.Добавить();
	НоваяСтрока.НаименованиеКраткое	= НаименованиеКраткое;
	НоваяСтрока.КодАльфа2			= КодАльфа2;
	НоваяСтрока.КодАльфа3			= КодАльфа3;
	НоваяСтрока.Код					= Код;
	НоваяСтрока.НаименованиеПолное	= НаименованиеПолное;
	
КонецПроцедуры

Процедура ДополнитьТаблицуОКЕИ(ОКЕИ)
	
	// Добавляем строку "Литр" с кратким наименованием "л".
	// Причем строка "л" будет основной, а строка "л (дм3)" - дополнительной.
	СтрокаТЗ = ОКЕИ.Найти("112", "Код");
	ЗаполнитьЗначенияСвойств(ОКЕИ.Добавить(), СтрокаТЗ);
	СтрокаТЗ.НаименованиеКраткое = "л";
	
	// Добавляем строку "Сутки" с кратким наименованием "сут".
	// Причем строка "сут" будет основной, а строка "сут/дн" - дополнительной.
	СтрокаТЗ = ОКЕИ.Найти("359", "Код");
	ЗаполнитьЗначенияСвойств(ОКЕИ.Добавить(), СтрокаТЗ);
	СтрокаТЗ.НаименованиеКраткое = "сут";
	
	// Добавляем строку "Тысяча штук" с кратким наименованием "тыс. шт".
	// Причем строка "тыс. шт" будет основной, а строка "тыс. шт/1000 шт" - дополнительной.
	СтрокаТЗ = ОКЕИ.Найти("798", "Код");
	ЗаполнитьЗначенияСвойств(ОКЕИ.Добавить(), СтрокаТЗ);
	СтрокаТЗ.НаименованиеКраткое = "тыс. шт";
	
	// Добавляем строку "Год" с кратким наименованием "г".
	// Причем строка "г" будет основной, а строка "г/лет" - дополнительной.
	СтрокаТЗ = ОКЕИ.Найти("366", "Код");
	ЗаполнитьЗначенияСвойств(ОКЕИ.Добавить(), СтрокаТЗ);
	СтрокаТЗ.НаименованиеКраткое = "г";
	
	// Добавляем строку "Километр" с кратким наименованием "км".
	// Причем строка "км" будет основной, а строка "км/1000 м" - дополнительной.
	СтрокаТЗ = ОКЕИ.Найти("008", "Код");
	ЗаполнитьЗначенияСвойств(ОКЕИ.Добавить(), СтрокаТЗ);
	СтрокаТЗ.НаименованиеКраткое = "км";
	
	// Добавляем строку "Тысяча кубических метров" с кратким наименованием "10^3 м3".
	// Причем строка "1000 м3" будет основной, а строка "10^3 м3" - дополнительной.
	СтрокаТЗ = ОКЕИ.Найти("114", "Код");
	ЗаполнитьЗначенияСвойств(ОКЕИ.Добавить(), СтрокаТЗ);
	СтрокаТЗ.НаименованиеКраткое = "10^3 м3";
	
КонецПроцедуры

Функция КодРегиона(Знач НаименованиеРегиона) Экспорт
	
	НаименованиеРегиона = НРег(НаименованиеРегиона);
	КодРегиона = "";
	
	Если СтрНайтиЛок(НаименованиеРегиона, "адыгея") > 0 Тогда
		КодРегиона = "01";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "башкортостан") > 0 Тогда
		КодРегиона = "02";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "алтайский") > 0 Тогда
		КодРегиона = "22";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "алтай") > 0 Тогда
		КодРегиона = "04";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "ингушетия") > 0 Тогда
		КодРегиона = "06";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "кабардино") > 0 Тогда
		КодРегиона = "07";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "карачаево") > 0 Тогда
		КодРегиона = "09";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "карелия") > 0 Тогда
		КодРегиона = "10";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "коми") > 0 Тогда
		КодРегиона = "11";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "марий") > 0 Тогда
		КодРегиона = "12";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "мордовия") > 0 Тогда
		КодРегиона = "13";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "татарстан") > 0 Тогда
		КодРегиона = "16";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "тыва") > 0 Тогда
		КодРегиона = "17";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "удмуртская") > 0 Тогда
		КодРегиона = "18";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "хакасия") > 0 Тогда
		КодРегиона = "19";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "чувашская") > 0 Тогда
		КодРегиона = "21";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "чувашия") > 0 Тогда
		КодРегиона = "21";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "краснодарский") > 0 Тогда
		КодРегиона = "23";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "приморский") > 0 Тогда
		КодРегиона = "25";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "ставропольский") > 0 Тогда
		КодРегиона = "26";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "хабаровский") > 0 Тогда
		КодРегиона = "27";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "амурская") > 0 Тогда
		КодРегиона = "28";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "архангельская") > 0 Тогда
		КодРегиона = "29";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "астраханская") > 0 Тогда
		КодРегиона = "30";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "белгородская") > 0 Тогда
		КодРегиона = "31";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "брянская") > 0 Тогда
		КодРегиона = "32";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "владимирская") > 0 Тогда
		КодРегиона = "33";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "волгоградская") > 0 Тогда
		КодРегиона = "34";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "вологодская") > 0 Тогда
		КодРегиона = "35";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "воронежская") > 0 Тогда
		КодРегиона = "36";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "ивановская") > 0 Тогда
		КодРегиона = "37";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "иркутская") > 0 Тогда
		КодРегиона = "38";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "калининградская") > 0 Тогда
		КодРегиона = "39";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "калужская") > 0 Тогда
		КодРегиона = "40";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "камчатский") > 0 Тогда
		КодРегиона = "41";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "кемеровская") > 0 Тогда
		КодРегиона = "42";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "кировская") > 0 Тогда
		КодРегиона = "43";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "костромская") > 0 Тогда
		КодРегиона = "44";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "курганская") > 0 Тогда
		КодРегиона = "45";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "курская") > 0 Тогда
		КодРегиона = "46";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "ленинградская") > 0 Тогда
		КодРегиона = "47";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "липецкая") > 0 Тогда
		КодРегиона = "48";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "магаданская") > 0 Тогда
		КодРегиона = "49";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "московская") > 0 Тогда
		КодРегиона = "50";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "мурманская") > 0 Тогда
		КодРегиона = "51";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "нижегородская") > 0 Тогда
		КодРегиона = "52";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "новосибирская") > 0 Тогда
		КодРегиона = "54";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "томская") > 0 Тогда
		КодРегиона = "70";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "омская") > 0 Тогда
		КодРегиона = "55";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "оренбургская") > 0 Тогда
		КодРегиона = "56";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "орловская") > 0 Тогда
		КодРегиона = "57";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "пензенская") > 0 Тогда
		КодРегиона = "58";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "пермский") > 0 Тогда
		КодРегиона = "59";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "псковская") > 0 Тогда
		КодРегиона = "60";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "ростовская") > 0 Тогда
		КодРегиона = "61";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "рязанская") > 0 Тогда
		КодРегиона = "62";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "самарская") > 0 Тогда
		КодРегиона = "63";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "саратовская") > 0 Тогда
		КодРегиона = "64";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "сахалинская") > 0 Тогда
		КодРегиона = "65";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "саха") > 0 Тогда
		КодРегиона = "14";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "якутия") > 0 Тогда
		КодРегиона = "14";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "смоленская") > 0 Тогда
		КодРегиона = "67";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "тамбовская") > 0 Тогда
		КодРегиона = "68";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "тверская") > 0 Тогда
		КодРегиона = "69";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "тульская") > 0 Тогда
		КодРегиона = "71";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "тюменская") > 0 Тогда
		КодРегиона = "72";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "ульяновская") > 0 Тогда
		КодРегиона = "73";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "челябинская") > 0 Тогда
		КодРегиона = "74";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "забайкальский") > 0 Тогда
		КодРегиона = "75";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "ярославская") > 0 Тогда
		КодРегиона = "76";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "еврейская") > 0 Тогда
		КодРегиона = "79";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "ненецкий") > 0 Тогда
		КодРегиона = ?(СтрНайтиЛок(НаименованиеРегиона, "ямало") > 0, "89", "83");
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "мансийский") > 0 Тогда
		КодРегиона = "86";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "чукотский") > 0 Тогда
		КодРегиона = "87";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "чеченская") > 0 Тогда
		КодРегиона = "20";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "свердловская") > 0 Тогда
		КодРегиона = "66";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "новгородская") > 0 Тогда
		КодРегиона = "53";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "горьковская") > 0 Тогда // синоним нижегородской
		КодРегиона = "52";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "бурятия") > 0 Тогда
		КодРегиона = "03";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "пермская") > 0 Тогда // Пермская обл - синоним Пермского края
		КодРегиона = "59";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "красноярский") > 0 Тогда
		КодРегиона = "24";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "северная осетия") > 0 Тогда
		КодРегиона = "15";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "камчатская") > 0 Тогда // Камчатская обл - синоним Камчатского края
		КодРегиона = "41";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "калмыкия") > 0 Тогда
		КодРегиона = "08";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "читинская") > 0 Тогда // Забайкальский край
		КодРегиона = "75";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "москва") > 0 Тогда
		КодРегиона = "77";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "дагестан") > 0 Тогда
		КодРегиона = "05";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "санкт-петербург") > 0 Тогда
		КодРегиона = "78";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "ленинград") > 0 Тогда
		КодРегиона = "78";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "крым") > 0 Тогда
		КодРегиона = "91";
	ИначеЕсли СтрНайтиЛок(НаименованиеРегиона, "севастополь") > 0 Тогда
		КодРегиона = "92";
	КонецЕсли;
	
	Возврат КодРегиона;
	
КонецФункции


//}		КЛАССИФИКАТОРЫ


//**************************************
//{ ФУНКЦИИ СЕРИАЛИЗАЦИИ

// Возвращает тип контента и его Proto структуру.
//
// Параметры:
//	 ВходящиеДанные - Контент в формате ФНС, типы:
//					- ОбъектXDTO 			 - см. метод ОбъектXDTO_В_ProtoСтруктура
//					- Строка, ДвоичныеДанные - см. метод XML_В_ОбъектXDTO
//
// Возвращаемое значение:
//	 Структура	- Структура с типом контента и самим контентом.
//
// Состав структуры:
//	 Контент		- Структура	- см. метод ОбъектXDTO_В_ProtoСтруктура
//	 ТипКонтента	- Строка	- Строковое представление типа контента,
//							  	  например: Torg12SellerContent или Torg12BuyerContent.
//
Функция ПолучитьProto(ВходящиеДанные) Экспорт
	
	Если ТипЗнч(ВходящиеДанные) = Тип("ОбъектXDTO") Тогда
		ОбъектXDTO = ВходящиеДанные;
	Иначе
		ОбъектXDTO = XML_В_ОбъектXDTO(ВходящиеДанные);
	КонецЕсли;
	
	Результат = Новый Структура;
	Результат.Вставить("Контент"	, ОбъектXDTO_В_ProtoСтруктура(ОбъектXDTO));
	Результат.Вставить("ТипКонтента", ПолучитьТипКонтентаОбъектаXDTO(ОбъектXDTO));
	
	Возврат Результат;
	
КонецФункции

// Возвращает объект XDTO по XML документу.
//
// Параметры:
//  ДокументXML - ДвоичныеДанные, Строка - Документ, по которому нужно создать объект XDTO.
//                                         Строкой можно передать текст xml либо путь к файлу.
//
// Возвращаемое значение:
//  ОбъектXDTO - объект созданный по xml документу.
//
Функция XML_В_ОбъектXDTO(ДокументXML) Экспорт
	
	СтрокаXML = Неопределено;
	
	Если ТипЗнч(ДокументXML) = Тип("ДвоичныеДанные") Тогда
		
		СтрокаXML = СодержимоеДвоичныхДанныхXML(ДокументXML);
		
	ИначеЕсли ЭтоСтрокаXML(ДокументXML) Тогда
		
		СтрокаXML = ДокументXML;
		
	ИначеЕсли ТипЗнч(ДокументXML) = Тип("Строка") Тогда
		
		СтрокаXML = СодержимоеФайлаXML(ДокументXML);
		
	Иначе
		
		ШаблонТекстаОшибки = НСтр("ru = 'Недопустимый тип данных документа XML.
				|Текущий тип: %1.
				|Допустимые типы: ДвоичныеДанные, Строка.'");
		ТипЗначенияXML = ТипЗнч(ДокументXML);
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(
				ШаблонТекстаОшибки,
				ТипЗначенияXML
			);
		
		ВызватьИсключение ТекстОшибки;
		
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(СтрокаXML) Тогда
		
		ТекстОшибки = НСтр("ru = 'Не удалось прочитать документ XML'");
		ВызватьИсключение ТекстОшибки;
		
	КонецЕсли;
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.УстановитьСтроку(СтрокаXML);
	ОбъектXDTO = ФабрикаXDTO.ПрочитатьXML(ЧтениеXML);
	ЧтениеXML.Закрыть();
	
	Возврат ОбъектXDTO;
	
КонецФункции

// Возвращает XDTO объект документа в терминах Proto структуры.
//
// Параметры:
//  ВходящиеДанные - Строка, ДвоичныеДанные - Контент документа в формате ФНС
//
// Возвращаемое значение:
//   Структура - Прото структура см. метод ОбъектXDTO_В_ProtoОбъектXDTO
//
Функция XML_В_ProtoСтруктура(ВходящиеДанные) Экспорт
	
	ОбъектXDTO = XML_В_ОбъектXDTO(ВходящиеДанные);
	
	ProtoОбъектXDTO = ОбъектXDTO_В_ProtoОбъектXDTO(ОбъектXDTO);
	
	Результат = ОбъектXDTOВСтруктуру(ProtoОбъектXDTO);
	
	Возврат Результат;
	
КонецФункции

// Возвращает XDTO объект документа в терминах Proto структуры.
//
// Параметры:
//	 ВходящиеДанные - Строка, ДвоичныеДанные - Контент документа в формате ФНС (см. метод XML_В_ОбъектXDTO)
//
// Возвращаемое значение:
//	 ОбъектXDTO - XDTO объект документа в терминах Proto (см. метод ОбъектXDTO_В_ProtoОбъектXDTO)
//
Функция XML_В_ProtoОбъектXDTO(ВходящиеДанные) Экспорт
	
	Возврат ОбъектXDTO_В_ProtoОбъектXDTO(XML_В_ОбъектXDTO(ВходящиеДанные));
	
КонецФункции

// Возвращает Proto структуру документа.
//
// Параметры:
//	 ОбъектXDTO - ОбъектXDTO - Контент документа в формате ФНС.
//
// Возвращаемое значение:
//	 Структура	- Объектная модель контента в терминах Proto структуры публичного API Диадока,
//				  например: http://api-docs.diadoc.ru/ru/latest/proto/TovTorgInfo.html?highlight=tovtorgsellertitleinfo
//
Функция ОбъектXDTO_В_ProtoСтруктура(ОбъектXDTO) Экспорт
	
	Возврат ОбъектXDTOВСтруктуру(ОбъектXDTO_В_ProtoОбъектXDTO(ОбъектXDTO), Истина);
	
КонецФункции	

// Возвращает XDTO объект документа в терминах Proto структуры.
//
// Параметры:
//	 ОбъектXDTO - ОбъектXDTO - Контент документа в формате ФНС.
//
// Возвращаемое значение:
//	 ОбъектXDTO	- XDTO объект документа в терминах Proto структуры публичного API Диадока.
//				  например: http://api-docs.diadoc.ru/ru/latest/proto/TovTorgInfo.html?highlight=tovtorgsellertitleinfo
//
Функция ОбъектXDTO_В_ProtoОбъектXDTO(ОбъектXDTO) Экспорт
	
	Результат = Неопределено;
	
	ТипКонтента = ПолучитьТипКонтентаОбъектаXDTO(ОбъектXDTO);
	
	Если Не ЗначениеЗаполнено(ТипКонтента) Тогда
		Возврат Результат;
	КонецЕсли;
	
	Результат = НовыйProtoОбъектXDTO(ТипКонтента);
	
	Если ТипКонтента = ТипПрото_ИнформацияПродавца970() Тогда
		
		Заполнить_Utd970SellerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПокупателя970() Тогда
		
		Заполнить_Utd970BuyerContent(Результат, ОбъектXDTO);
	
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавца155() Тогда
		
		Заполнить_UtdSellerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПокупателя155() Тогда
		
		Заполнить_UtdBuyerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПокупателя736() Тогда
		
		Заполнить_UcdBuyerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавца820() Тогда
		
		Заполнить_Utd820SellerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПокупателя820() Тогда
		
		Заполнить_Utd820BuyerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавца736() Тогда
		
		Если Найти(ОбъектXDTO.ИдФайл, "ON_NKORSCHFDOPPR") = 1 Тогда
			Заполнить_Ucd736SellerContent(Результат, ОбъектXDTO);
		Иначе
			Заполнить_UcdSellerContent(Результат, ОбъектXDTO);
		КонецЕсли;
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавца552() Тогда
		
		Заполнить_Act552SellerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПокупателя552() Тогда
		
		Заполнить_Act552BuyerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавца551() Тогда
		
		Заполнить_TovTorgSellerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПокупателя551() Тогда
		
		Заполнить_TovTorgBuyerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавцаНакладная172() Тогда
		
		Заполнить_Torg12SellerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПокупателяНакладная172() Тогда
		
		Заполнить_Torg12BuyerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавцаАкт172() Тогда
		
		Заполнить_AcceptanceCertificateSellerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПокупателяАкт172() Тогда
		
		Заполнить_AcceptanceCertificateBuyerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_СчетФактура93() Тогда
		
		Заполнить_InvoiceContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_КорректировочныйСчетФактура93() Тогда
		
		Заполнить_InvoiceCorrectionContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавцаСчетНаОплату() Тогда
		
		Заполнить_ProformaInvoiceSellerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавцаСчетНаОплату29() Тогда
		
		Заполнить_ProformaInvoice29SellerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияОтправителя423() Тогда
		
		Заполнить_Torg2SenderTitle(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПолучателя423() Тогда
		
		Заполнить_Torg2RecipientTitle(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияОтправителя405() Тогда
		
		Заполнить_ReconciliationActSenderTitle(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПолучателя405() Тогда
		
		Заполнить_ReconciliationActRecipientTitle(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавцаНакладнаяРБ01() Тогда
		
		Заполнить_TorgBlrSellerContent(Результат, ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПокупателяНакладнаяРБ01() Тогда
		
		Заполнить_TorgBlrBuyerContent(Результат, ОбъектXDTO);
		
	Иначе
		
		КомментарийЖР = Общее_ПодставитьПараметрыВСтроку(
			"Неизвестный тип протоконтента (%1): ИдФайл = %2",
			ТипКонтента,
			ОбъектXDTO.ИдФайл
		);
		
		ЗаписьЖурналаРегистрации(
			"Диадок.ПарсингXML",
			УровеньЖурналаРегистрации.Ошибка,
			Неопределено,
			Неопределено,
			КомментарийЖР
		);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция XML_В_XDTO(СтрокаXML)
	
	ЧтениеXML = Новый ЧтениеXML;
	ЧтениеXML.УстановитьСтроку(СтрокаXML);
	
	Возврат ФабрикаXDTO.ПрочитатьXML(ЧтениеXML);
	
КонецФункции

Функция XDTO_В_Таблицу(ОбъектXDTO)
	
	Таблица = Новый ТаблицаЗначений;
	
	СвойстваОбъектаXDTO = ОбъектXDTO.Свойства();
	
	Если СвойстваОбъектаXDTO.Получить("column") <> Неопределено Тогда
		Добавить_КолонкиXDTO_В_КоллекциюКолонок(ОбъектXDTO.column, Таблица.Колонки);
	КонецЕсли;
	
	Для Каждого СтрокаXDTO Из ОбъектXDTO.row Цикл
		
		НоваяСтрока = Таблица.Добавить();
		
		ИндексКолонки = -1;
		Для Каждого ЗначениеКолонки Из СтрокаXDTO.Value Цикл
			
			ИндексКолонки = ИндексКолонки + 1;
			
			Если ТипЗнч(ЗначениеКолонки) <> Тип("ОбъектXDTO") Тогда
				НоваяСтрока[ИндексКолонки] = ЗначениеКолонки;
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЦикла;
	
	Если СвойстваОбъектаXDTO.Получить("index") <> Неопределено Тогда
		Добавить_ИндексыXDTO_В_ИндексыКоллекции(ОбъектXDTO.index, Таблица.Индексы);
	КонецЕсли;
	
	Возврат Таблица;
	
КонецФункции

Функция XDTO_В_Дерево(ОбъектXDTO)
	
	Дерево = Новый ДеревоЗначений;
	
	Добавить_КолонкиXDTO_В_КоллекциюКолонок(ОбъектXDTO.column, Дерево.Колонки);
	
	Добавить_СписокXDTO_В_Дерево(ОбъектXDTO.row, Дерево.Строки);
	
	Возврат Дерево;
	
КонецФункции

Процедура Добавить_КолонкиXDTO_В_КоллекциюКолонок(КолонкиXDTO, КоллекцияКолонок)
	
	Для Каждого КолонкаXDTO Из КолонкиXDTO Цикл
		
		СвойстваValueType = КолонкаXDTO.ValueType.Свойства();
		
		МассивТипов = Новый Массив;
		КвалификаторыЧисла  = Неопределено;
		КвалификаторыСтроки = Неопределено;
		КвалификаторыДаты 	= Неопределено;
		
		Если СвойстваValueType.Получить("NumberQualifiers") <> Неопределено
			И КолонкаXDTO.ValueType.NumberQualifiers <> Неопределено Тогда
			
			Разрядность 			= Число(КолонкаXDTO.ValueType.NumberQualifiers.Digits);
			РазрядностьДробнойЧасти = Число(КолонкаXDTO.ValueType.NumberQualifiers.FractionDigits);
			
			Если ТипЗнч(КолонкаXDTO.ValueType.NumberQualifiers.AllowedSign) = Тип("ДопустимыйЗнак") Тогда
				_ДопустимыйЗнак = КолонкаXDTO.ValueType.NumberQualifiers.AllowedSign;
			Иначе
				_ДопустимыйЗнак = ДопустимыйЗнак[КолонкаXDTO.ValueType.NumberQualifiers.AllowedSign];
			КонецЕсли;
			
			КвалификаторыЧисла = Новый КвалификаторыЧисла(Разрядность, РазрядностьДробнойЧасти, _ДопустимыйЗнак);
			
			МассивТипов.Добавить(Тип("Число"));
			
		КонецЕсли;
		
		
		Если СвойстваValueType.Получить("StringQualifiers") <> Неопределено 
			И КолонкаXDTO.ValueType.StringQualifiers <> Неопределено Тогда
			
			Длина = Число(КолонкаXDTO.ValueType.StringQualifiers.Length);
			
			Если ТипЗнч(КолонкаXDTO.ValueType.StringQualifiers.AllowedLength) = Тип("ДопустимаяДлина") Тогда
				_ДопустимаяДлина = КолонкаXDTO.ValueType.StringQualifiers.AllowedLength;
			Иначе
				_ДопустимаяДлина = ДопустимаяДлина[КолонкаXDTO.ValueType.StringQualifiers.AllowedLength];
			КонецЕсли;
			
			КвалификаторыСтроки = Новый КвалификаторыСтроки(Длина, _ДопустимаяДлина);
			
			МассивТипов.Добавить(Тип("Строка"));
			
		КонецЕсли;
		
		Если СвойстваValueType.Получить("DateQualifiers") <> Неопределено
			И КолонкаXDTO.ValueType.DateQualifiers <> Неопределено Тогда
			
			Если ТипЗнч(КолонкаXDTO.ValueType.DateQualifiers.DateFractions) = Тип("ЧастиДаты") Тогда
				_ЧастиДаты = КолонкаXDTO.ValueType.DateQualifiers.DateFractions;
			Иначе
				_ЧастиДаты = ЧастиДаты[КолонкаXDTO.ValueType.DateQualifiers.DateFractions];
			КонецЕсли;
			
			КвалификаторыДаты = Новый КвалификаторыДаты(_ЧастиДаты);
			
			МассивТипов.Добавить(Тип("Дата"));
			
		КонецЕсли;
		
		Если Найти(НРег(КолонкаXDTO.ValueType.Type), "boolean") > 0 Тогда
			МассивТипов.Добавить(Тип("Булево"));
		КонецЕсли;
		
		Если ЗначениеЗаполнено(МассивТипов) Тогда
			ОписаниеТипаКолонки = Новый ОписаниеТипов(МассивТипов, КвалификаторыЧисла, КвалификаторыСтроки, КвалификаторыДаты);
		Иначе
			ОписаниеТипаКолонки = Неопределено;
		КонецЕсли;
		
		КоллекцияКолонок.Добавить(КолонкаXDTO.Name, ОписаниеТипаКолонки);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура Добавить_ИндексыXDTO_В_ИндексыКоллекции(ИндексыXDTO, ИндексыКоллекции)
	
	Для Каждого ИндексXDTO Из ИндексыXDTO Цикл
		
		Если ТипЗнч(ИндексXDTO.column) = Тип("СписокXDTO") Тогда
			
			КолонкиИндекса = "";
			Разделитель = "";
			
			Для Каждого КолонкаИндекса Из ИндексXDTO.column Цикл
				КолонкиИндекса = КолонкиИндекса + Разделитель + КолонкаИндекса;
				Разделитель = ", ";
			КонецЦикла;
			
		Иначе
			КолонкиИндекса = ИндексXDTO.column;
		КонецЕсли;
		
		ИндексыКоллекции.Добавить(КолонкиИндекса);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура Добавить_СписокXDTO_В_Дерево(СписокXDTO, СтрокиДерева)
	
	Для Каждого СтрокаXDTO Из СписокXDTO Цикл
		
		НоваяСтрока = СтрокиДерева.Добавить();
		
		ИндексКолонки = -1;
		Для Каждого ЗначениеКолонки Из СтрокаXDTO.Value Цикл
			
			ИндексКолонки = ИндексКолонки + 1;
			
			Если ТипЗнч(ЗначениеКолонки) <> Тип("ОбъектXDTO") Тогда
				НоваяСтрока[ИндексКолонки] = ЗначениеКолонки;
			КонецЕсли;
			
		КонецЦикла;
		
		Если СтрокаXDTO.Свойства().Получить("row") <> Неопределено Тогда
			
			Добавить_СписокXDTO_В_Дерево(СтрокаXDTO.row, НоваяСтрока.Строки);
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Функция Дерево_В_Таблицу(Дерево, СИерархией)
	
	Таблица = Новый ТаблицаЗначений;
	
	Для Каждого Колонка Из Дерево.Колонки Цикл
		Таблица.Колонки.Добавить(Колонка.Имя, Колонка.ТипЗначения);
	КонецЦикла;
	
	ЗаполнитьТаблицуИзДерева(Таблица, Дерево, СИерархией);
	
	Возврат Таблица;
	
КонецФункции

Процедура ЗаполнитьТаблицуИзДерева(Таблица, Дерево, СИерархией)
	
	Для Каждого СтрокаДерева Из Дерево.Строки Цикл
		
		Если ЗначениеЗаполнено(СтрокаДерева.Строки) Тогда
			
			Если СИерархией Тогда
				ЗаполнитьЗначенияСвойств(Таблица.Добавить(), СтрокаДерева);
			КонецЕсли;
			
			ЗаполнитьТаблицуИзДерева(Таблица, СтрокаДерева, СИерархией);
			
		Иначе
			ЗаполнитьЗначенияСвойств(Таблица.Добавить(), СтрокаДерева);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Функция РазложитьСтрокуВМассивСлов(Знач Строка, РазделителиСлов = ",")
	
	Слова = Новый Массив;
	
	Для Сч = 1 По СтрДлина(РазделителиСлов) Цикл
		Строка = СтрЗаменить(Строка, Сред(РазделителиСлов, Сч, 1), Символы.ПС);
	КонецЦикла;
	
	Для Сч = 1 По СтрЧислоСтрок(Строка) Цикл
		
		ТекСлово = СокрЛП(СтрПолучитьСтроку(Строка, Сч));
		Если ТекСлово <> "" Тогда
			Слова.Добавить(ТекСлово);
		КонецЕсли;
		
	КонецЦикла;	
	
	Возврат Слова;
	
КонецФункции

Функция НовыйОбъектXDTO(мФабрика, Тип_Основной, СоздатьВложенныеОбъекты = Ложь)
	
	Результат = мФабрика.Создать(Тип_Основной);
	
	Если СоздатьВложенныеОбъекты Тогда
		
		Для Каждого Эл Из Тип_Основной.Свойства Цикл
			
	        флСвойствоНеСписковое =	(Эл.ВерхняяГраница = 1);
			
			Если Эл.Тип.URIПространстваИмен = Тип_Основной.URIПространстваИмен 
				И флСвойствоНеСписковое	Тогда
				
				Если ТипЗнч(Эл.Тип) = Тип("ТипОбъектаXDTO") Тогда // вложенные свойства создаем только для объектов
					Результат[Эл.Имя] = НовыйОбъектXDTO(мФабрика, Эл.Тип, СоздатьВложенныеОбъекты);
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция НовыйДочернийОбъектXDTO(Фабрика, Родитель, ИмяЭлемента, ЗначениеЭлемента = Неопределено)
	
	СвойствоДочернего	 = Родитель.Тип().Свойства.Получить(ИмяЭлемента);
	ТипДочернего		 = СвойствоДочернего.Тип;
	ЭтоСписок			 = СвойствоДочернего.ВерхняяГраница <> 1;
	
	Если ЗначениеЭлемента = Неопределено Тогда
		Результат = Фабрика.Создать(ТипДочернего);
	Иначе
		Результат = Фабрика.Создать(ТипДочернего, ЗначениеЭлемента);
	КонецЕсли;
	
	Если ЭтоСписок Тогда
		Родитель[ИмяЭлемента].Добавить(Результат);
	Иначе
		Родитель[ИмяЭлемента] = Результат;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьЗначениеXDTO(Объект, ИмяСвойства, ЗначениеПоУмолчанию = Неопределено)
	
	Результат = ЗначениеПоУмолчанию;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Объект, ИмяСвойства) Тогда
		
		Попытка
			Результат = Объект[ИмяСвойства];
		Исключение
			Сообщить("Не удалось прочитать свойство " + ИмяСвойства);
		КонецПопытки;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Обертка для отладки: перехватывает исключение при установке значения неподходящего типа в реквизит.
//
Процедура УстановитьЗначениеXDTO(Элемент, ИмяРеквизита, ЗначениеРеквизита, ЭтоСсылка = Ложь)

	Если ЗначениеРеквизита = Неопределено
		Или ЗначениеРеквизита = NULL Тогда
		Возврат;
	КонецЕсли;
	
	Попытка
		
		// здесь можно добавить проверки на заполненность, вывод сообщений и т.д.
		
		Если ЭтоСсылка Тогда
			Элемент[ИмяРеквизита] = ЗначениеВСтрокуВнутр(ЗначениеРеквизита);
		ИначеЕсли ТипЗнч(ЗначениеРеквизита) = Тип("Строка") Тогда
			Элемент[ИмяРеквизита] = СокрЛП(ЗначениеРеквизита);
		Иначе	
			Элемент[ИмяРеквизита] = ЗначениеРеквизита;
		КонецЕсли;
		
	Исключение
		
		ОписаниеОшибки = ОписаниеОшибки();
		
		Сообщить("Исключение при заполнении реквизита """ + ИмяРеквизита + """:
				 |" + ОписаниеОшибки);
		
	КонецПопытки;

КонецПроцедуры

// Для совместимости с визуализацией печатных форм электронных документов
//
Функция ОбъектXDTOВСтруктуру(ОбъектXDTO, ПреобразовыватьСпискиВМассивы = Истина) Экспорт
	
	// второй параметр уже не имеет смысла, похоже
	
	Если ТипЗнч(ОбъектXDTO) = Тип("ОбъектXDTO") Тогда
		
		Результат = Новый Структура;
		
		Для Каждого Свойство Из ОбъектXDTO.Свойства() Цикл
			
			Если Найти(Свойство.Имя, "_ReadOnly") > 0 Тогда
				ИмяСвойстваДляВставки = СтрЗаменить(Сред(Свойство.Имя, 2), "_ReadOnly", "");
			Иначе
				ИмяСвойстваДляВставки = Свойство.Имя;
			КонецЕсли;
			
			ЭтоСсылка = ЭтоТипОбъектаXDTOСсылка(Свойство.Тип);
			
			Если Найти(ИмяСвойстваДляВставки, "Ссылка") > 0 ИЛИ ЭтоСсылка Тогда
				
				мЗначение = ОбъектXDTO[Свойство.Имя];
				Если ЗначениеЗаполнено(мЗначение) Тогда
					мСсылка = ЗначениеИзСтрокиВнутр(ОбъектXDTO[Свойство.Имя]);
					Результат.Вставить(ИмяСвойстваДляВставки, мСсылка);
				Иначе
					Результат.Вставить(ИмяСвойстваДляВставки, Неопределено);
				КонецЕсли;
				
			ИначеЕсли ТипЗнч(Свойство.Тип) = Тип("ТипЗначенияXDTO")
				Или Свойство.Тип.Имя = "anyType" Тогда // костыль для ссылок на справочники
				
				мЗначение = ОбъектXDTO[Свойство.Имя];
				
				Если ТипЗнч(мЗначение) = Тип("СписокXDTO") Тогда
					
					Если Не Результат.Свойство(ИмяСвойстваДляВставки) Тогда
						Результат.Вставить(ИмяСвойстваДляВставки, Новый Массив);
					КонецЕсли;
					
					Для Каждого ЭлементСписка Из мЗначение Цикл
						Результат[ИмяСвойстваДляВставки].Добавить(ЭлементСписка);
					КонецЦикла;
					
				Иначе
					
					Результат.Вставить(ИмяСвойстваДляВставки, мЗначение);
					
				КонецЕсли;
				
			ИначеЕсли ТипЗнч(Свойство.Тип) = Тип("ТипОбъектаXDTO") Тогда	
				
				Результат.Вставить(ИмяСвойстваДляВставки, ОбъектXDTOВСтруктуру(ОбъектXDTO[Свойство.Имя], ПреобразовыватьСпискиВМассивы));
				
			КонецЕсли;
			
		КонецЦикла;
	
	ИначеЕсли ТипЗнч(ОбъектXDTO) = Тип("СписокXDTO") Тогда
		
		Если ПреобразовыватьСпискиВМассивы Тогда
			
			Результат = Новый Массив;
			Для Каждого ЭлементСписка Из ОбъектXDTO Цикл
				Результат.Добавить(ОбъектXDTOВСтруктуру(ЭлементСписка, ПреобразовыватьСпискиВМассивы));
			КонецЦикла;
			
		Иначе	
			
			Результат = Новый ТаблицаЗначений;
			Для Каждого Свойство Из ОбъектXDTO.ВладеющееСвойство.Тип.Свойства Цикл
				Результат.Колонки.Добавить(Свойство.Имя);
			КонецЦикла;
			
			Для Каждого ЭлементСписка Из ОбъектXDTO Цикл
				
				НоваяСтрока = Результат.Добавить();
				ЗаполнитьЗначенияСвойств(НоваяСтрока, ЭлементСписка);
				// хорошо бы еще вложенные таблицы тоже преобразовать
				
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция НовыйProtoОбъектXDTO(Знач ИмяКонтента, СоздатьВсеСвойства = Истина) Экспорт
	
	Если НРег(ИмяКонтента) = НРег(ТипПрото_ИнформацияПокупателя736()) Тогда
		// Контент ответного титула УКД аналогичен УПД
		ИмяКонтента = ТипПрото_ИнформацияПокупателя155();
	КонецЕсли;
	
	ProtoФабрикаXDTO = ProtoФабрикаXDTO();
	
	ТипXDTO = ProtoФабрикаXDTO.Тип(ПространствоИменПрото(), ИмяКонтента);
	
	Возврат НовыйОбъектXDTO(ProtoФабрикаXDTO, ТипXDTO, СоздатьВсеСвойства);
	
КонецФункции

Функция ProtoФабрикаXDTO() Экспорт
	
	Результат = XML_ФабрикаXDTO("Proto");
	
	Возврат Результат;
	
КонецФункции

Функция ИдФайл_В_ТипКонтента(ИдФайл)
	// BSLLS:DeprecatedFind-off
	
	Результат = "";
	
	Если Найти(ИдФайл, "ON_SCHFDOPPR_") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияПродавца155();
		
	ИначеЕсли Найти(ИдФайл, "ON_SCHFDOPPOK_") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияПокупателя155();
		
	ИначеЕсли Найти(ИдФайл, "ON_KORSCHFDOPPR_") = 1 // 189@
		Или Найти(ИдФайл, "ON_NKORSCHFDOPPR") = 1 Тогда // 736@
		
		Результат = ТипПрото_ИнформацияПродавца736();
		
	ИначеЕсли Найти(ИдФайл, "ON_KORSCHFDOPPOK_") = 1 // 189@
		Или Найти(ИдФайл, "ON_NKORSCHFDOPPOK") = 1 Тогда // 736@
		
		Результат = ТипПрото_ИнформацияПокупателя736();
		
	ИначеЕсли Найти(ИдФайл, "DP_REZRUISP_") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияПродавца552();
		
	ИначеЕсли Найти(ИдФайл, "DP_REZRUZAK_") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияПокупателя552();
		
	ИначеЕсли Найти(ИдФайл, "DP_TOVTORGPR_") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияПродавца551();
		
	ИначеЕсли Найти(ИдФайл, "DP_TOVTORGPOK_") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияПокупателя551();
		
	ИначеЕсли Найти(ИдФайл, "DP_OTORG12_") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияПродавцаНакладная172();
		
	ИначеЕсли Найти(ИдФайл, "DP_PTORG12_") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияПокупателяНакладная172();
		
	ИначеЕсли Найти(ИдФайл, "DP_IAKTPRM_") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияПродавцаАкт172();
		
	ИначеЕсли Найти(ИдФайл, "DP_ZAKTPRM_") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияПокупателяАкт172();
		
	ИначеЕсли Найти(ИдФайл, "ON_SFAKT_") = 1 Тогда
		
		Результат = ТипПрото_СчетФактура93();
		
	ИначеЕсли Найти(ИдФайл, "ON_KORSFAKT_") = 1 Тогда
		
		Результат = ТипПрото_КорректировочныйСчетФактура93();
		
	ИначеЕсли Найти(ИдФайл, "ON_SCHET_") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияПродавцаСчетНаОплату();
		
	ИначеЕсли Найти(ИдФайл, "ON_CHETOP_") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияПродавцаСчетНаОплату29();
		
	ИначеЕсли Найти(ИдФайл, "DP_PRIRASXPRIN") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияОтправителя423();
		
	ИначеЕсли Найти(ИдФайл, "DP_PRIRASXSVED") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияПолучателя423();
		
	ИначеЕсли Найти(ИдФайл, "ON_AKTSVEROTP") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияОтправителя405();
		
	ИначеЕсли Найти(ИдФайл, "ON_AKTSVERPOL") = 1 Тогда
		
		Результат = ТипПрото_ИнформацияПолучателя405();
		
	Иначе
		
		ТекстОшибки = СтрЗаменить(
			"Не удалось определить тип контента по причине:
			|Неизвестный префикс атрибута ИдФайл (%1)",
			"%1",
			ИдФайл
		);
		
		Сообщить(ТекстОшибки);
		
	КонецЕсли;
	
	Возврат Результат;
	
	// BSLLS:DeprecatedFind-on
КонецФункции

Функция ПолучитьТипКонтентаОбъектаXDTO(ОбъектXDTO)
	
	Результат = Неопределено;
	
	Если XML_ЭтоТитулПродавцаУПД970(ОбъектXDTO) Тогда
		
		Результат = ТипПрото_ИнформацияПродавца970();

	ИначеЕсли XML_ЭтоТитулПокупателяУПД970(ОбъектXDTO) Тогда
		
		Результат = ТипПрото_ИнформацияПокупателя970();
		
	ИначеЕсли XML_ЭтоТитулПродавцаУПД820(ОбъектXDTO) Тогда
		
		Результат = ТипПрото_ИнформацияПродавца820();
		
	ИначеЕсли XML_ЭтоТитулПокупателяУПД820(ОбъектXDTO) Тогда
		
		Результат = ТипПрото_ИнформацияПокупателя820();
		
	ИначеЕсли XML_ЭтоТитулПродавцаНакладнаяРБ01(ОбъектXDTO) Тогда
		
		Результат = ТипПрото_ИнформацияПродавцаНакладнаяРБ01();
		
	ИначеЕсли XML_ЭтоТитулПокупателяНакладнаяРБ01(ОбъектXDTO) Тогда
		
		Результат = ТипПрото_ИнформацияПокупателяНакладнаяРБ01();
		
	Иначе
		
		ИдФайл = ПолучитьЗначениеXDTO(ОбъектXDTO, "ИдФайл", "");

		Результат = ИдФайл_В_ТипКонтента(ИдФайл);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТипПрото_ИнформацияПокупателя970()
	Возврат "Utd970BuyerContent";
КонецФункции

Функция НовыйЭлементСпискаXDTO(СписокXDTO)
	
	Возврат СписокXDTO.Добавить(НовыйProtoОбъектXDTO(СписокXDTO.ВладеющееСвойство.Тип.Имя));
	
КонецФункции

// Аналог процедуры ЗаполнитьЗначенияСвойств() для XDTO
//
Процедура ЗаполнитьСвойстваПриемникаXDTO(Приемник, Источник)
	
	Для Каждого Свойство Из Источник.Свойства() Цикл
		
		Если Приемник.Свойства().Получить(Свойство.Имя) <> Неопределено
			И Источник.Установлено(Свойство.Имя)
			И ТипЗнч(Источник[Свойство.Имя]) <> Тип("ОбъектXDTO")
			И ТипЗнч(Источник[Свойство.Имя]) <> Тип("СписокXDTO") Тогда
			
			// и еще бы проверить на совпадение типов
			Приемник[Свойство.Имя] = Источник[Свойство.Имя];
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

// Примерно как ЗаполнитьСвойстваПриемникаXDTO, только поля по-разному называются
//
Процедура ЗаполнитьСвойстваПриемникаXDTOПоМаппингу(Приемник, Источник, Маппинг)
	
	// Маппинг - это СписокЗначений
	// Представление = имя поля в приемнике
	// Значение = имя поля в источнике
	Для Каждого ЭлементСписка Из Маппинг Цикл
		
		Если Источник.Установлено(ЭлементСписка.Значение) Тогда
			Приемник[ЭлементСписка.Представление] = Источник[ЭлементСписка.Значение];
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Функция ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Объект, ИмяСвойства)
	
	Результат = Ложь;
	
	ТипОбъекта = ТипЗнч(Объект);
	
	Если ТипОбъекта = Тип("ОбъектXDTO") Тогда
		
		Результат = Объект.Свойства().Получить(ИмяСвойства) <> Неопределено;
		
	ИначеЕсли ТипОбъекта = Тип("Структура")
		Или ТипОбъекта = Тип("ФиксированнаяСтруктура") Тогда
		
		Результат = Объект.Свойство(ИмяСвойства);
		
	ИначеЕсли ТипОбъекта = Тип("СтрокаТаблицыЗначений") Тогда
		
		Результат = Объект.Владелец().Колонки.Найти(ИмяСвойства) <> Неопределено;
		
	КонецЕсли;
	
	Возврат Результат;

КонецФункции

Функция XML_ЭтоТитулПродавцаУПД970(ОбъектXDTO)
	
	Результат = Ложь;
	
	ИдФайл = ПолучитьЗначениеXDTO(ОбъектXDTO, "ИдФайл", "");
	
	Если Найти(ИдФайл, "ON_NSCHFDOPPR") = 1 Тогда
		Результат = (ОбъектXDTO.ВерсФорм = "5.02")
			ИЛИ (ОбъектXDTO.ВерсФорм = "5.03");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция XML_ЭтоТитулПокупателяУПД970(ОбъектXDTO)
	
	Результат = Ложь;
	
	ИдФайл = ПолучитьЗначениеXDTO(ОбъектXDTO, "ИдФайл", "");
	
	Если Найти(ИдФайл, "ON_NSCHFDOPPOK") = 1 Тогда
		Результат = (ОбъектXDTO.ВерсФорм = "5.02")
			ИЛИ (ОбъектXDTO.ВерсФорм = "5.03");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция XML_ЭтоТитулПродавцаУПД820(ОбъектXDTO)
	
	Результат = Ложь;
	
	ИдФайл = ПолучитьЗначениеXDTO(ОбъектXDTO, "ИдФайл", "");
	
	Если Найти(ИдФайл, "ON_NSCHFDOPPR") = 1 Тогда
		Результат = (ОбъектXDTO.ВерсФорм = "5.01");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция XML_ЭтоТитулПокупателяУПД820(ОбъектXDTO)
	
	Результат = Ложь;
	
	ИдФайл = ПолучитьЗначениеXDTO(ОбъектXDTO, "ИдФайл", "");
	
	Если Найти(ИдФайл, "ON_NSCHFDOPPOK") = 1 Тогда
		Результат = (ОбъектXDTO.ВерсФорм = "5.01");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

//} ФУНКЦИИ СЕРИАЛИЗАЦИИ

//**************************************
//{	МЕТОДЫ ДЛЯ ЗАПОЛНЕНИЯ Proto КОНТЕНТА


//{ Методы преобразования контента одного типа в другой

// Преобразует InvoiceContent в UtdSellerContent с функцией СЧФ (Invoice)
// Предназначена для быстрого перехода с формата счетов-фактур 5.02 на формат УПД.
Функция ПолучитьUtdSellerContentИзInvoiceContent(InvoiceContent) Экспорт
	
	Контент = НовыйProtoОбъектXDTO(ТипПрото_ИнформацияПродавца155());
	
	ЗаполнитьСвойстваПриемникаXDTO(Контент, InvoiceContent);
	
	Маппинг = Новый СписокЗначений;
	Маппинг.Добавить("InvoiceRevisionNumber",	"RevisionNumber");
	Маппинг.Добавить("InvoiceRevisionDate",		"RevisionDate");
	ЗаполнитьСвойстваПриемникаXDTOПоМаппингу(Контент, InvoiceContent, Маппинг);
	
	ЗаполнитьСвойстваПриемникаXDTO(Контент.Buyer,	InvoiceContent.Buyer);
	ЗаполнитьСвойстваПриемникаXDTO(Контент.Buyer.Address,	InvoiceContent.Buyer.Address);
	ЗаполнитьТипКонтрагента(Контент.Buyer);
	
	ЗаполнитьСвойстваПриемникаXDTO(Контент.Seller,	InvoiceContent.Seller);
	ЗаполнитьСвойстваПриемникаXDTO(Контент.Seller.Address,	InvoiceContent.Seller.Address);
	ЗаполнитьТипКонтрагента(Контент.Seller);
	
	// Грузоотправитель
	Если InvoiceContent.Shipper.SameAsSellerOrBuyer = Истина Тогда
		
		Контент.Shipper.SameAsSeller = Истина;
		
	ИначеЕсли InvoiceContent.Shipper.SameAsSellerOrBuyer = Ложь Тогда
		
		Контент.Shipper.SameAsSeller = Ложь;
		ЗаполнитьСвойстваПриемникаXDTO(Контент.Shipper.OrganizationInfo,			InvoiceContent.Shipper);  // Основные поля - ИНН, КПП, ...
		ЗаполнитьСвойстваПриемникаXDTO(Контент.Shipper.OrganizationInfo,			InvoiceContent.Shipper);  // Наименование
		ЗаполнитьСвойстваПриемникаXDTO(Контент.Shipper.OrganizationInfo.Address,	InvoiceContent.Shipper.Address);
		
	Иначе
		
		// не заполняем
		
	КонецЕсли;

	// Грузополучатель
	Если InvoiceContent.Consignee.SameAsSellerOrBuyer = Истина Тогда
		
		// не заполняем в этом случае
		
	ИначеЕсли InvoiceContent.Consignee.SameAsSellerOrBuyer = Ложь Тогда
		
		ЗаполнитьСвойстваПриемникаXDTO(Контент.Consignee,			InvoiceContent.Seller);  // Основные поля - ИНН, КПП, ...
		ЗаполнитьСвойстваПриемникаXDTO(Контент.Consignee,			InvoiceContent.Consignee);  // Наименование
		ЗаполнитьСвойстваПриемникаXDTO(Контент.Consignee.Address,	InvoiceContent.Consignee.Address);
		ЗаполнитьТипКонтрагента(Контент.Consignee);
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Function",					"Invoice");
	УстановитьЗначениеXDTO(Контент, "Creator",					"" + Контент.Seller.Name + " / " + Контент.Seller.Inn + " / " + Контент.Seller.Kpp);
	
	Скопировать_StructedAdditionalInfos(Контент.AdditionalInfoId.StructedAdditionalInfos, InvoiceContent.StructedAdditionalInfos);

	// Заполним товары
	Для Каждого Item Из InvoiceContent.Items Цикл
		
		НовЭлемент = НовыйЭлементСпискаXDTO(Контент.InvoiceTable.Items);
		ЗаполнитьСвойстваПриемникаXDTO(НовЭлемент, Item);
		
		Маппинг = Новый СписокЗначений;
		Маппинг.Добавить("Total",					"Subtotal");
		Маппинг.Добавить("TotalWithVatExcluded",	"SubtotalWithVatExcluded");
		ЗаполнитьСвойстваПриемникаXDTOПоМаппингу(НовЭлемент, Item, Маппинг);
		
		Скопировать_StructedAdditionalInfos(НовЭлемент.StructedAdditionalInfos, Item.StructedAdditionalInfos);
		
		// Теперь заполним номера ГТД
		Если ЗначениеЗаполнено(Item.CountriesOfOrigin)
			Или ЗначениеЗаполнено(Item.CustomsDeclarationNumbers) Тогда
			
			// Страна происхождения - Строка (3), одна на все элементы
			// А номер ГТД разбиваем по запятым
			НомераГТД = РазложитьСтрокуВМассивСлов(Item.CustomsDeclarationNumbers, ",");
			
			Для Каждого НомерГТД Из НомераГТД Цикл
				
				НовЭлементГТД = НовыйЭлементСпискаXDTO(НовЭлемент.CustomDeclarations);
				УстановитьЗначениеXDTO(НовЭлементГТД, "CountryCode", Item.CountriesOfOrigin);
				УстановитьЗначениеXDTO(НовЭлементГТД, "DeclarationNumber", НомерГТД);
				
			КонецЦикла;
			
			Если НомераГТД.Количество() = 0 Тогда
				
				// Случай, когда указана страна происхождения, но отсутствует номер ГТД
				// Например, страны Таможенного Союза
				НовЭлементГТД = НовыйЭлементСпискаXDTO(НовЭлемент.CustomDeclarations);
				УстановитьЗначениеXDTO(НовЭлементГТД, "CountryCode", Item.CountriesOfOrigin);
				
			КонецЕсли;
			
		КонецЕсли;
	
	КонецЦикла;
	
	ЗаполнитьИтогиУПД(Контент.InvoiceTable);
	
	Для Каждого Стр Из InvoiceContent.PaymentDocuments Цикл
		НовЭлемент = НовыйЭлементСпискаXDTO(Контент.PaymentDocuments);
		ЗаполнитьСвойстваПриемникаXDTO(НовЭлемент, Стр);
	КонецЦикла;
	
	Возврат Контент;
	
КонецФункции

// Преобразует UtdSellerContent в Utd820SellerContent
// Предназначена для быстрого перехода с формата УПД 155@ на формат 820@.
Функция ПолучитьUtd820SellerContentИзUtdSellerContent(UtdSellerContent) Экспорт
	
	Результат = НовыйProtoОбъектXDTO(ТипПрото_ИнформацияПродавца820());
	
	ЗаполнитьСвойстваПриемникаXDTO(Результат, UtdSellerContent);
	
	Маппинг = Новый СписокЗначений;
	Маппинг.Добавить("Creator",		"DocumentCreator");
	Маппинг.Добавить("CreatorBase",	"DocumentCreatorBase");
	Маппинг.Добавить("Name",		"DocumentName");
	Маппинг.Добавить("Date",		"DocumentDate");
	Маппинг.Добавить("Number",		"DocumentNumber");
	
	ЗаполнитьСвойстваПриемникаXDTOПоМаппингу(Результат, UtdSellerContent, Маппинг);
	
	Buyer = НовыйЭлементСпискаXDTO(Результат.Buyers);
	ЗаполнитьСвойстваПриемникаXDTO(Buyer, UtdSellerContent.Buyer);
	ЗаполнитьСвойстваПриемникаXDTO(Buyer.Address, UtdSellerContent.Buyer.Address);
	
	Seller = НовыйЭлементСпискаXDTO(Результат.Sellers);
	ЗаполнитьСвойстваПриемникаXDTO(Seller, UtdSellerContent.Seller);
	ЗаполнитьСвойстваПриемникаXDTO(Seller.Address, UtdSellerContent.Seller.Address);
	
	Shipper = НовыйЭлементСпискаXDTO(Результат.Shippers);
	УстановитьЗначениеXDTO(Shipper, "SameAsSeller", UtdSellerContent.Shipper.SameAsSeller);
	ЗаполнитьСвойстваПриемникаXDTO(Shipper.OrganizationInfo, UtdSellerContent.Shipper.OrganizationInfo);
	ЗаполнитьСвойстваПриемникаXDTO(Shipper.OrganizationInfo.Address, UtdSellerContent.Shipper.OrganizationInfo.Address);
	
	Consignee = НовыйЭлементСпискаXDTO(Результат.Consignees);
	ЗаполнитьСвойстваПриемникаXDTO(Consignee, UtdSellerContent.Consignee);
	ЗаполнитьСвойстваПриемникаXDTO(Consignee.Address, UtdSellerContent.Consignee.Address);
	
	УстановитьЗначениеXDTO(Результат.AdditionalInfoId, "InfoFileId", UtdSellerContent.AdditionalInfoId.InfoFileId);
	Скопировать_StructedAdditionalInfos(Результат.AdditionalInfoId.StructedAdditionalInfos, UtdSellerContent.AdditionalInfoId.StructedAdditionalInfos);
	
	Скопировать_StructedAdditionalInfos(Результат.PaymentDocuments, UtdSellerContent.PaymentDocuments);
	
	TransferInfo155 = UtdSellerContent.TransferInfo;
	TransferInfo820 = Результат.TransferInfo;
	
	ЗаполнитьСвойстваПриемникаXDTO(TransferInfo820, TransferInfo155);
	УстановитьЗначениеXDTO(TransferInfo820.AdditionalInfoId, "InfoFileId", TransferInfo155.AdditionalInfoId.InfoFileId);
	Скопировать_StructedAdditionalInfos(TransferInfo820.AdditionalInfoId.StructedAdditionalInfos, TransferInfo155.AdditionalInfoId.StructedAdditionalInfos);
	ЗаполнитьСвойстваПриемникаXDTO(TransferInfo820.Carrier, TransferInfo155.Carrier);
	ЗаполнитьСвойстваПриемникаXDTO(TransferInfo820.Carrier.Address, TransferInfo155.Carrier.Address);
	ЗаполнитьСвойстваПриемникаXDTO(TransferInfo820.Employee, TransferInfo155.Employee);
	ЗаполнитьСвойстваПриемникаXDTO(TransferInfo820.OtherIssuer, TransferInfo155.OtherIssuer);
	Скопировать_TransferBases(TransferInfo820.TransferBases, TransferInfo155.TransferBases);
	Скопировать_StructedAdditionalInfos(TransferInfo820.Waybills, TransferInfo155.Waybills);
	
	ЗаполнитьСвойстваПриемникаXDTO(Результат.Table, UtdSellerContent.InvoiceTable);
	
	МаппингСтрокиТЧ = Новый СписокЗначений;
	МаппингСтрокиТЧ.Добавить("ToRelease",	"ItemToRelease");
	МаппингСтрокиТЧ.Добавить("VendorCode",	"ItemVendorCode");
	МаппингСтрокиТЧ.Добавить("UnitCode",	"Unit");
	
	БезНДС = "true";
	
	Для Каждого Item Из UtdSellerContent.InvoiceTable.Items Цикл
		
		НовЭлемент = НовыйЭлементСпискаXDTO(Результат.Table.Items);
		ЗаполнитьСвойстваПриемникаXDTO(НовЭлемент, Item);
		ЗаполнитьСвойстваПриемникаXDTOПоМаппингу(НовЭлемент, Item, МаппингСтрокиТЧ);
		
		Скопировать_StructedAdditionalInfos(НовЭлемент.StructedAdditionalInfos, Item.StructedAdditionalInfos);
		Скопировать_StructedAdditionalInfos(НовЭлемент.CustomDeclarations, Item.CustomDeclarations);
		
		Если TaxRate_ЭтоЗначениеБезНДС(Item.TaxRate) Тогда
			НовЭлемент.WithoutVat = "true";
		Иначе
			БезНДС = "false";
		КонецЕсли;
		
	КонецЦикла;
	
	Результат.Table.WithoutVat = БезНДС;
	
	Возврат Результат;
	
КонецФункции

// Преобразует InvoiceCorrectionContent в UcdSellerContent с функцией КСЧФ (Invoice)
// Предназначена для быстрого перехода с формата КСФ 5.02 на формат УКД.
Функция ПолучитьUcdSellerContentИзInvoiceCorrectionContent(InvoiceContent) Экспорт
	
	Контент = НовыйProtoОбъектXDTO(ТипПрото_ИнформацияПродавца736());
	
	ЗаполнитьСвойстваПриемникаXDTO(Контент, InvoiceContent);
	
	Маппинг = Новый СписокЗначений;
	
	Маппинг.Добавить("InvoiceCorrectionRevisionNumber",	"CorrectionRevisionNumber");
	Маппинг.Добавить("InvoiceCorrectionRevisionDate",	"CorrectionRevisionDate");
	
	Маппинг.Добавить("InvoiceCorrectionNumber",	"Number");
	Маппинг.Добавить("InvoiceCorrectionDate",	"Date");
	
	ЗаполнитьСвойстваПриемникаXDTOПоМаппингу(Контент, InvoiceContent, Маппинг);
	
	ЗаполнитьСвойстваПриемникаXDTO(Контент.Buyer,			InvoiceContent.Buyer);
	ЗаполнитьСвойстваПриемникаXDTO(Контент.Buyer.Address,	InvoiceContent.Buyer.Address);
	ЗаполнитьТипКонтрагента(Контент.Buyer);
	
	ЗаполнитьСвойстваПриемникаXDTO(Контент.Seller,			InvoiceContent.Seller);
	ЗаполнитьСвойстваПриемникаXDTO(Контент.Seller.Address,	InvoiceContent.Seller.Address);
	ЗаполнитьТипКонтрагента(Контент.Seller);
	
	УстановитьЗначениеXDTO(Контент, "Function",					"Invoice");
	УстановитьЗначениеXDTO(Контент, "Creator",					"" + Контент.Seller.Name + " / " + Контент.Seller.Inn + " / " + Контент.Seller.Kpp);
	
	Скопировать_StructedAdditionalInfos(Контент.AdditionalInfoId.StructedAdditionalInfos, InvoiceContent.StructedAdditionalInfos);

	// Заполним товары
	Для Каждого Item Из InvoiceContent.Items Цикл
		
		НовЭлемент = НовыйЭлементСпискаXDTO(Контент.InvoiceCorrectionTable.Items);
		ЗаполнитьСвойстваПриемникаXDTO(НовЭлемент, Item);
		
		ЗаполнитьСвойстваПриемникаXDTO(НовЭлемент.AmountsDec,		Item.AmountsDec);
		ЗаполнитьСвойстваПриемникаXDTO(НовЭлемент.AmountsInc,		Item.AmountsInc);
		ЗаполнитьСвойстваПриемникаXDTO(НовЭлемент.CorrectedValues,	Item.CorrectedValues);
		ЗаполнитьСвойстваПриемникаXDTO(НовЭлемент.OriginalValues,	Item.OriginalValues);
		
		Скопировать_StructedAdditionalInfos(НовЭлемент.StructedAdditionalInfos, Item.StructedAdditionalInfos);
		
	КонецЦикла;
	
	Для Каждого OriginalInvoice Из InvoiceContent.OriginalInvoices Цикл
		
		ИсходныйСФ = НовыйЭлементСпискаXDTO(Контент.Invoices);
		
		ИсходныйСФ.InvoiceNumber	= OriginalInvoice.Number;
		ИсходныйСФ.InvoiceDate		= OriginalInvoice.Date;
		
		Если ЗначениеЗаполнено(OriginalInvoice.InvoiceRevisionDate) Тогда
			
			ОписаниеИСФ = НовыйЭлементСпискаXDTO(ИсходныйСФ.InvoiceRevisions);
			ОписаниеИСФ.RevisionDate		= OriginalInvoice.InvoiceRevisionDate;
			ОписаниеИСФ.RevisionNumber		= OriginalInvoice.InvoiceRevisionNumber;
			
		КонецЕсли;
		
	КонецЦикла;
	
	ЗаполнитьИтогиУКД(Контент.InvoiceCorrectionTable);
	
	Контент.EventContent.OperationContent	= "Уведомляю об изменении стоимости и/или количества";
	Контент.EventContent.TransferDocDetails	= "Отсутствует";
	
	CorrectionBase = НовыйЭлементСпискаXDTO(Контент.EventContent.CorrectionBases);
	CorrectionBase.BaseDocumentName		= "Отсутствует";
	
	Возврат Контент;
	
КонецФункции

//} Методы преобразования контента одного типа в другой

Процедура Заполнить_AcceptanceCertificateBuyerContent(Контент, Параметры)
	
	Документ = Параметры.Документ;
	Принял	 = Документ.СвАктИ.Принял;
	
	Заполнить_Signer(Контент, Документ, "Подписант");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Принял, "Претенз") Тогда
		УстановитьЗначениеXDTO(Контент, "Complaints", Принял.Претенз);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Принял, "ДатаЗаказ") Тогда
		УстановитьЗначениеXDTO(Контент, "SignatureDate", ДатаXML_в_Дата1С(Принял.ДатаЗаказ));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ.СвАктИ, "ИнфПол") И ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ.СвАктИ.ИнфПол, "ТекстИнф") Тогда
		УстановитьЗначениеXDTO(Контент, "AdditionalInfo", Документ.СвАктИ.ИнфПол.ТекстИнф);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Принял, "ПодписьЗаказ") Тогда
		Заполнить_Official(Контент.Official, Принял.ПодписьЗаказ);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Принял, "ДоверенЗаказ") Тогда
		Заполнить_Attorney(Контент.Attorney, Принял.ДоверенЗаказ);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_AcceptanceCertificateItem(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Name",						?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НаимРабот"),  Параметры.НаимРабот,  ""));
	УстановитьЗначениеXDTO(Контент, "Description",				?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Описание"),   Параметры.Описание,   ""));
	УстановитьЗначениеXDTO(Контент, "UnitCode",					?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ОКЕИ"),       Параметры.ОКЕИ,       ""));
	УстановитьЗначениеXDTO(Контент, "UnitName",					?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НаимЕдИзм"),  Параметры.НаимЕдИзм,  ""));
	УстановитьЗначениеXDTO(Контент, "Quantity",					?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Количество"), Параметры.Количество, 0));
	УстановитьЗначениеXDTO(Контент, "Price",					?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Цена"),       Параметры.Цена,       0));
	УстановитьЗначениеXDTO(Контент, "TotalWithVatExcluded",		?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СумБезНДС"),  Параметры.СумБезНДС,  0));
	УстановитьЗначениеXDTO(Контент, "Vat",						?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СумНДС"),     Параметры.СумНДС,     0));
	УстановитьЗначениеXDTO(Контент, "Total",					?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СумУчНДС"),   Параметры.СумУчНДС,   0));
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo",			?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИнфПолСтр"),  Параметры.ИнфПолСтр,  ""));
	
КонецПроцедуры

Процедура Заполнить_AcceptanceCertificateSellerContent(Контент, Параметры)
	
	Документ = Параметры.Документ;
	СвАктИ	 = Документ.СвАктИ;
	
	Контент.Seller.FnsParticipantId = Параметры.СвУчДокОбор.ИдОтпр;
	
	УстановитьЗначениеXDTO(Контент, "Title",	СвАктИ.Заголовок);
	УстановитьЗначениеXDTO(Контент, "Number",	СвАктИ.НомАкт);
	УстановитьЗначениеXDTO(Контент, "Date",		ДатаXML_в_Дата1С(СвАктИ.ДатаАкт));
	
	Заполнить_OrganizationInfo(Контент.Seller, СвАктИ.Исполнитель);
	Заполнить_Signer(Контент, Документ, "Подписант");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвАктИ, "Сдал") Тогда
		
		Сдал = СвАктИ.Сдал;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Сдал, "ДатаИсполн") Тогда
			УстановитьЗначениеXDTO(Контент, "SignatureDate", ДатаXML_в_Дата1С(Сдал.ДатаИсполн));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Сдал, "ПодписьИсполн") Тогда
			Заполнить_Official(Контент.Official, Сдал.ПодписьИсполн);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Сдал, "ДоверенИсполн") Тогда
			Заполнить_Attorney(Контент.Attorney, Сдал.ДоверенИсполн);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвАктИ, "ИнфПол") И ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвАктИ.ИнфПол, "ТекстИнф") Тогда
		УстановитьЗначениеXDTO(Контент, "AdditionalInfo", СвАктИ.ИнфПол.ТекстИнф);
	КонецЕсли;
	
	Для Каждого ОписРабот Из СписокXDTOИзЭлемента(СвАктИ, "ОписРабот") Цикл
		Для Каждого Работа Из СписокXDTOИзЭлемента(ОписРабот, "Работа") Цикл
			Заполнить_AcceptanceCertificateItem(НовыйЭлементСпискаXDTO(Контент.Items), Работа);
		КонецЦикла;
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_AdditionalInfoId(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИдФайлИнфПол") Тогда
		УстановитьЗначениеXDTO(Контент, "InfoFileId", ПолучитьЗначениеXDTO(Параметры, "ИдФайлИнфПол"));
	КонецЕсли;
	
	Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, Параметры, "ТекстИнф");
	
КонецПроцедуры

Процедура Заполнить_СписокKeyValue(СписокKeyValue, Параметры, ИмяСвойства)
	
	Для Каждого Элемент Из СписокXDTOИзЭлемента(Параметры, ИмяСвойства) Цикл
		Заполнить_KeyValue(НовыйЭлементСпискаXDTO(СписокKeyValue), Элемент);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_KeyValue(Контент, Параметры)
	
	// Записываем как есть, без сокращения пробелов
	Контент.Key		 = Параметры.Идентиф;
	Контент.Value	 = Параметры.Значен;
	
КонецПроцедуры

Процедура Заполнить_ЗначенИдентиф(Контент, Параметры)
	
	// Записываем как есть, без сокращения пробелов
	Контент.Значен	 = Параметры.Value;
	Контент.Идентиф	 = Параметры.Key;
	
КонецПроцедуры

Процедура Заполнить_AddressInfo(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "АдрРф") Тогда
		
		Контент.IsForeign	= Ложь;
		УстановитьЗначениеXDTO(Контент, "ZipCode",		ПолучитьЗначениеXDTO(Параметры.АдрРф,	"Индекс"));
		УстановитьЗначениеXDTO(Контент, "RegionCode",	ПолучитьЗначениеXDTO(Параметры.АдрРф,	"КодРегион"));
		УстановитьЗначениеXDTO(Контент, "Locality",		ПолучитьЗначениеXDTO(Параметры.АдрРф,	"НаселПункт"));
		УстановитьЗначениеXDTO(Контент, "Territory",	ПолучитьЗначениеXDTO(Параметры.АдрРф,	"Район"));
		УстановитьЗначениеXDTO(Контент, "City",			ПолучитьЗначениеXDTO(Параметры.АдрРф,	"Город"));
		УстановитьЗначениеXDTO(Контент, "Street",		ПолучитьЗначениеXDTO(Параметры.АдрРф,	"Улица"));
		УстановитьЗначениеXDTO(Контент, "Building",		ПолучитьЗначениеXDTO(Параметры.АдрРф,	"Дом"));
		УстановитьЗначениеXDTO(Контент, "Block",		ПолучитьЗначениеXDTO(Параметры.АдрРф,	"Корпус"));
		УстановитьЗначениеXDTO(Контент, "Apartment",	ПолучитьЗначениеXDTO(Параметры.АдрРф,	"Кварт"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "АдрИно") Тогда	
		
		Контент.IsForeign	= Истина;
		УстановитьЗначениеXDTO(Контент, "CountryCode",	ПолучитьЗначениеXDTO(Параметры.АдрИно,	"КодСтр"));
		УстановитьЗначениеXDTO(Контент, "AddressText",	ПолучитьЗначениеXDTO(Параметры.АдрИно,	"АдрТекст"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "АдрИнф") Тогда
		
		Контент.IsForeign	= Истина;
		УстановитьЗначениеXDTO(Контент, "CountryCode",	ПолучитьЗначениеXDTO(Параметры.АдрИнф,	"КодСтр"));
		УстановитьЗначениеXDTO(Контент, "AddressText",	ПолучитьЗначениеXDTO(Параметры.АдрИнф,	"АдрТекст"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "АдрТекст") Тогда	
		
		Контент.IsForeign	= Ложь;
		УстановитьЗначениеXDTO(Контент, "AddressText",	ПолучитьЗначениеXDTO(Параметры,	"АдрТекст"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "КодГАР") Тогда
		
		Контент.IsForeign	= Ложь;
		УстановитьЗначениеXDTO(Контент, "AddressCode",	ПолучитьЗначениеXDTO(Параметры,	"КодГАР"));
		
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_AddressInfo970(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ВидыАдресов = Перечисление_ВидыАдресов();
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "АдрРф") Тогда
		
		УстановитьЗначениеXDTO(Контент, "Type", ВидыАдресов.АдресРФ);
		
		Заполнить_AddressRFInfo(Контент.AddressRF, Параметры.АдрРф);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "АдрГАР") Тогда	
		
		УстановитьЗначениеXDTO(Контент, "Type", ВидыАдресов.АдресГАР);
		
		Заполнить_AddressSARInfo(Контент.AddressSAR, Параметры.АдрГАР);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "АдрИнф") Тогда	
		
		УстановитьЗначениеXDTO(Контент, "Type", ВидыАдресов.АдресСтрокой);
		
		Заполнить_AddressTextInfo(Контент.AddressText, Параметры.АдрИнф);
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "GLN", ПолучитьЗначениеXDTO(Параметры, "ГЛНМеста"));
	
КонецПроцедуры

Процедура Заполнить_AddressRFInfo(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Индекс = ПолучитьЗначениеXDTO(Параметры, "Индекс");
	Район = ПолучитьЗначениеXDTO(Параметры, "Район");
	Город = ПолучитьЗначениеXDTO(Параметры, "Город");
	НаселПункт = ПолучитьЗначениеXDTO(Параметры, "НаселПункт");
	Улица = ПолучитьЗначениеXDTO(Параметры, "Улица");
	Дом = ПолучитьЗначениеXDTO(Параметры, "Дом");
	Корпус = ПолучитьЗначениеXDTO(Параметры, "Корпус");
	Кварт = ПолучитьЗначениеXDTO(Параметры, "Кварт");
	ИныеСвед = ПолучитьЗначениеXDTO(Параметры, "ИныеСвед");
	
	УстановитьЗначениеXDTO(Контент, "ZipCode", Индекс);
	УстановитьЗначениеXDTO(Контент, "RegionCode", Параметры.КодРегион);
	УстановитьЗначениеXDTO(Контент, "Region", Параметры.НаимРегион);
	УстановитьЗначениеXDTO(Контент, "Territory", Район);
	УстановитьЗначениеXDTO(Контент, "City", Город);
	УстановитьЗначениеXDTO(Контент, "Locality", НаселПункт);
	УстановитьЗначениеXDTO(Контент, "Street", Улица);
	УстановитьЗначениеXDTO(Контент, "Building", Дом);
	УстановитьЗначениеXDTO(Контент, "Block", Корпус);
	УстановитьЗначениеXDTO(Контент, "Apartment", Кварт);
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo", ИныеСвед);
	
КонецПроцедуры

Процедура Заполнить_AddressSARInfo(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "ID", Параметры.ИдНом);
	УстановитьЗначениеXDTO(Контент, "ZipCode", ПолучитьЗначениеXDTO(Параметры, "Индекс"));
	УстановитьЗначениеXDTO(Контент, "Region", Параметры.НаимРегион);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "КодРегион") Тогда
		УстановитьЗначениеXDTO(Контент, "RegionCode", Параметры.КодРегион);
	Иначе
		УстановитьЗначениеXDTO(Контент, "RegionCode", Параметры.Регион);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "МуниципРайон") Тогда
		УстановитьЗначениеXDTO(Контент, "MunicipalDistrict", Параметры.МуниципРайон.Наим);
		УстановитьЗначениеXDTO(Контент, "MunicipalDistrictCode", Параметры.МуниципРайон.ВидКод);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ГородСелПоселен") Тогда
		УстановитьЗначениеXDTO(Контент, "Settlement", Параметры.ГородСелПоселен.Наим);
		УстановитьЗначениеXDTO(Контент, "SettlementCode", Параметры.ГородСелПоселен.ВидКод);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НаселенПункт") Тогда
		УстановитьЗначениеXDTO(Контент, "Locality", Параметры.НаселенПункт.Наим);
		УстановитьЗначениеXDTO(Контент, "LocalityType", Параметры.НаселенПункт.Вид);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ЭлПланСтруктур") Тогда
		УстановитьЗначениеXDTO(Контент, "PlanningElement", Параметры.ЭлПланСтруктур.Наим);
		УстановитьЗначениеXDTO(Контент, "PlanningElementType", Параметры.ЭлПланСтруктур.Тип);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ЭлУлДорСети") Тогда
		УстановитьЗначениеXDTO(Контент, "StreetElement", Параметры.ЭлУлДорСети.Наим);
		УстановитьЗначениеXDTO(Контент, "StreetElementType", Параметры.ЭлУлДорСети.Тип);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "LandPlot", ПолучитьЗначениеXDTO(Параметры, "ЗемелУчасток"));
	
	Для Каждого СведенияОЗдании Из СписокXDTOИзЭлемента(Параметры, "Здание") Цикл
		
		BuildingInfo = НовыйЭлементСпискаXDTO(Контент.Buildings);
		
		УстановитьЗначениеXDTO(BuildingInfo, "Type", СведенияОЗдании.Тип);
		УстановитьЗначениеXDTO(BuildingInfo, "Number", СведенияОЗдании.Номер);
		
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ПомещЗдания") Тогда
		УстановитьЗначениеXDTO(Контент, "House", Параметры.ПомещЗдания.Номер);
		УстановитьЗначениеXDTO(Контент, "HouseType", Параметры.ПомещЗдания.Тип);
	КонецЕсли;

	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ПомещКвартиры") Тогда
		УстановитьЗначениеXDTO(Контент, "Apartment", Параметры.ПомещКвартиры.Номер);
		УстановитьЗначениеXDTO(Контент, "ApartmentType", Параметры.ПомещКвартиры.Тип);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_AddressTextInfo(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "CountryCode", Параметры.КодСтр);
	УстановитьЗначениеXDTO(Контент, "Country", Параметры.НаимСтран);
	УстановитьЗначениеXDTO(Контент, "AddressText", Параметры.АдрТекст);
	
КонецПроцедуры

Процедура Заполнить_CodeNameAddressInfo(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Code", Параметры.ВидКод);
	УстановитьЗначениеXDTO(Контент, "Name", Параметры.Наим);
	
КонецПроцедуры

Процедура Заполнить_KindNameAddressInfo(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Kind", Параметры.Вид);
	УстановитьЗначениеXDTO(Контент, "Name", Параметры.Наим);
	
КонецПроцедуры

Процедура Заполнить_TypeNameAddressInfo(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Type", Параметры.Тип);
	УстановитьЗначениеXDTO(Контент, "Name", Параметры.Наим);
	
КонецПроцедуры

Процедура Заполнить_TypeNumberAddressInfo(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Type", Параметры.Тип);
	УстановитьЗначениеXDTO(Контент, "Number", Параметры.Номер);
	
КонецПроцедуры

Процедура Заполнить_AmountsDiff(Контент, Параметры, ИмяКонтента)
	
	AmountsDiff = Новый Структура;
	
	Если ИмяКонтента = "AmountsInc" Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "АкцизРазн") Тогда
			AmountsDiff.Вставить("Excise", ПолучитьЗначениеXDTO(Параметры.АкцизРазн, "СумУвел"));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СумНалРазн") Тогда
			AmountsDiff.Вставить("Vat", ПолучитьЗначениеXDTO(Параметры.СумНалРазн, "СумУвел"));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СтТовУчНал") Тогда
			AmountsDiff.Вставить("Total", ПолучитьЗначениеXDTO(Параметры.СтТовУчНал, "СтоимУвел"));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СтТовБезНДС") Тогда
			AmountsDiff.Вставить("TotalWithVatExcluded", ПолучитьЗначениеXDTO(Параметры.СтТовБезНДС, "СтоимУвел"));
		КонецЕсли;
		
	Иначе
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "АкцизРазн") Тогда
			AmountsDiff.Вставить("Excise", ПолучитьЗначениеXDTO(Параметры.АкцизРазн, "СумУм"));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СумНалРазн") Тогда
			AmountsDiff.Вставить("Vat", ПолучитьЗначениеXDTO(Параметры.СумНалРазн, "СумУм"));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СтТовУчНал") Тогда
			AmountsDiff.Вставить("Total", ПолучитьЗначениеXDTO(Параметры.СтТовУчНал, "СтоимУм"));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СтТовБезНДС") Тогда
			AmountsDiff.Вставить("TotalWithVatExcluded", ПолучитьЗначениеXDTO(Параметры.СтТовБезНДС, "СтоимУм"));
		КонецЕсли;
		
	КонецЕсли;
	
	Для Каждого КлючЗначение Из AmountsDiff Цикл
		УстановитьЗначениеXDTO(Контент, КлючЗначение.Ключ, КлючЗначение.Значение);
	КонецЦикла;

КонецПроцедуры

Процедура Заполнить_Attorney(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;

	УстановитьЗначениеXDTO(Контент, "Date",   ?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДатаДоверен"), ДатаXML_в_Дата1С(Параметры.ДатаДоверен), Дата(1, 1, 1)));
	УстановитьЗначениеXDTO(Контент, "Number", ?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НомДоверен"), Параметры.НомДоверен, ""));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ВыданаКем") Тогда
		УстановитьЗначениеXDTO(Контент, "IssuerAdditionalInfo",    ?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВыданаКем, "ДопСведКем"), Параметры.ВыданаКем.ДопСведКем, ""));
		УстановитьЗначениеXDTO(Контент, "IssuerOrganizationName",  ?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВыданаКем, "НаимОргКем"), Параметры.ВыданаКем.НаимОргКем, ""));
		Заполнить_Official(Контент.Issuer, Параметры.ВыданаКем);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ВыданаКому") Тогда
		УстановитьЗначениеXDTO(Контент, "RecipientAdditionalInfo", ?(ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВыданаКому, "ДопСведКому"), Параметры.ВыданаКому.ДопСведКому, ""));
		Заполнить_Official(Контент.Recipient, Параметры.ВыданаКому);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_ContentOperCode(Контент, Параметры)
	
	ДатаДокРасх = ПолучитьЗначениеXDTO(Параметры, "ДатаДокРасх");
	
	УстановитьЗначениеXDTO(Контент, "TotalCode",				Параметры.КодИтога);
	УстановитьЗначениеXDTO(Контент, "NameDiscrepDocument",		ПолучитьЗначениеXDTO(Параметры, "НаимДокРасх"));
	УстановитьЗначениеXDTO(Контент, "TypeDiscrepDocument",		ПолучитьЗначениеXDTO(Параметры, "ВидДокРасх"));
	УстановитьЗначениеXDTO(Контент, "NumberDiscrepDocument",	ПолучитьЗначениеXDTO(Параметры, "НомДокРасх"));
	УстановитьЗначениеXDTO(Контент, "DateDiscrepDocument",		ДатаXML_в_Дата1С(ДатаДокРасх));
	УстановитьЗначениеXDTO(Контент, "IdDiscrepDocument",		ПолучитьЗначениеXDTO(Параметры, "ИдФайлДокРасх"));
	
КонецПроцедуры

Процедура Заполнить_BuyerInfoCircumPublicProc(Контент, Параметры)
	
	ДатаОплПред = ПолучитьЗначениеXDTO(Параметры, "ДатаОплПред");
	
	УстановитьЗначениеXDTO(Контент, "ProcCode",					ПолучитьЗначениеXDTO(Параметры, "ИдКодЗак"));
	УстановитьЗначениеXDTO(Контент, "PersonalAccountBuyer",		Параметры.ЛицСчетПок);
	УстановитьЗначениеXDTO(Контент, "NameFinAuthority",			Параметры.НаимФинОргПок);
	УстановитьЗначениеXDTO(Контент, "BuyerRegistryEntryNumber",	Параметры.НомРеестрЗапПок);
	УстановитьЗначениеXDTO(Контент, "BuyerLiabNumber",			ПолучитьЗначениеXDTO(Параметры, "УчНомБюдОбязПок"));
	УстановитьЗначениеXDTO(Контент, "BuyerTreasuryCode",		ПолучитьЗначениеXDTO(Параметры, "КодКазначПок"));
	УстановитьЗначениеXDTO(Контент, "SellerTreasuryName",		ПолучитьЗначениеXDTO(Параметры, "НаимКазначПок"));
	УстановитьЗначениеXDTO(Контент, "OKTMOBuy",					Параметры.ОКТМОПок);
	УстановитьЗначениеXDTO(Контент, "OKTMOPlaceDelivery",		ПолучитьЗначениеXDTO(Параметры, "ОКТМОМесПост"));
	УстановитьЗначениеXDTO(Контент, "PayDeadLine",				ДатаXML_в_Дата1С(ДатаОплПред));
	УстановитьЗначениеXDTO(Контент, "NumberFundsLiab",			ПолучитьЗначениеXDTO(Параметры, "УчНомДенОбяз"));
	УстановитьЗначениеXDTO(Контент, "PriorityPayment",			ПолучитьЗначениеXDTO(Параметры, "ОчерПлат"));
	УстановитьЗначениеXDTO(Контент, "TypePayment",				ПолучитьЗначениеXDTO(Параметры, "ВидПлат"));
	
	Заполнить_СписокInfoFundsLiab(Контент.InfoFundsLiab, Параметры, "ИнфСведДенОбяз");
	
КонецПроцедуры
	
Процедура Заполнить_СписокInfoFundsLiab(СписокInfoFundsLiab, Параметры, ИмяСвойства)
	
	Для Каждого Элемент Из СписокXDTOИзЭлемента(Параметры, ИмяСвойства) Цикл
		Заполнить_InfoFundsLiab(НовыйЭлементСпискаXDTO(СписокInfoFundsLiab), Элемент);
	КонецЦикла;

КонецПроцедуры

Процедура Заполнить_InfoFundsLiab(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "NumberString",			Параметры.НомСтр);
	УстановитьЗначениеXDTO(Контент, "ObjectCodeFAIP",		ПолучитьЗначениеXDTO(Параметры, "КодОбъектФАИП"));
	УстановитьЗначениеXDTO(Контент, "TypeFundsSource",		Параметры.ВидСредств);
	УстановитьЗначениеXDTO(Контент, "BuyerBudjetClassCode",	Параметры.КодПокБюджКласс);
	УстановитьЗначениеXDTO(Контент, "BuyerTargetCode",		ПолучитьЗначениеXDTO(Параметры, "КодЦелиПокуп"));
	УстановитьЗначениеXDTO(Контент, "AmountAdvance",		Параметры.СумАванс);
	
КонецПроцедуры	

Процедура Заполнить_SellerInfoCircumPublicProc(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "DateStateContract",		ДатаXML_в_Дата1С(Параметры.ДатаГосКонт));
	УстановитьЗначениеXDTO(Контент, "NumberStateContract",		Параметры.НомерГосКонт);
	УстановитьЗначениеXDTO(Контент, "PersonalAccountSeller",	ПолучитьЗначениеXDTO(Параметры, "ЛицСчетПрод"));
	УстановитьЗначениеXDTO(Контент, "SellerBudjetClassCode",	ПолучитьЗначениеXDTO(Параметры, "КодПродБюджКласс"));
	УстановитьЗначениеXDTO(Контент, "SellerTargetCode",			ПолучитьЗначениеXDTO(Параметры, "КодЦелиПрод"));
	УстановитьЗначениеXDTO(Контент, "SellerTreasuryCode",		ПолучитьЗначениеXDTO(Параметры, "КодКазначПрод"));
	УстановитьЗначениеXDTO(Контент, "SellerTreasuryName",		ПолучитьЗначениеXDTO(Параметры, "НаимКазначПрод"));
	
КонецПроцедуры

Процедура Заполнить_DocumentShipment(Контент, Параметры)
	
	ДатаДокОтгр = ПолучитьЗначениеXDTO(Параметры, "ДатаДокОтгр");
	
	УстановитьЗначениеXDTO(Контент, "Name",		Параметры.НаимДокОтгр);
	УстановитьЗначениеXDTO(Контент, "Number",	Параметры.НомДокОтгр);
	УстановитьЗначениеXDTO(Контент, "Date",		ДатаXML_в_Дата1С(ДатаДокОтгр));
	
КонецПроцедуры

Процедура Заполнить_ContractContent(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Date",						Неопределено);
	УстановитьЗначениеXDTO(Контент, "Number",					Неопределено);
	УстановитьЗначениеXDTO(Контент, "Price",					Неопределено);
	УстановитьЗначениеXDTO(Контент, "ContractType",				Неопределено);
	
КонецПроцедуры

Процедура Заполнить_CorrectionRequestContent(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Comment", Неопределено);
	
КонецПроцедуры

Процедура Заполнить_CustomDeclaration(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "CountryCode", ПолучитьЗначениеXDTO(Параметры, "КодПроисх"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НомерТД") Тогда
		УстановитьЗначениеXDTO(Контент, "DeclarationNumber", Параметры.НомерТД);
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НомерДТ") Тогда
		УстановитьЗначениеXDTO(Контент, "DeclarationNumber", Параметры.НомерДТ);	
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДефКодПроисх") Тогда
		УстановитьЗначениеXDTO(Контент, "HyphenCountry", Истина);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_ContentOperCode970(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "TotalCode", Параметры.КодИтога);
	
	РеквДокРасх = ПолучитьЗначениеXDTO(Параметры, "РеквДокРасх");
	Заполнить_DocumentDetails(Контент.DiscrepDocument, РеквДокРасх);
	
КонецПроцедуры

Процедура Заполнить_Employee(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "EmployeePosition",			Параметры.Должность);
	УстановитьЗначениеXDTO(Контент, "EmployeeInfo",				ПолучитьЗначениеXDTO(Параметры, "ИныеСвед"));
	УстановитьЗначениеXDTO(Контент, "EmployeeBase",				ПолучитьЗначениеXDTO(Параметры, "ОснПолн"));
	УстановитьЗначениеXDTO(Контент, "Surname",					Параметры.ФИО.Фамилия);
	УстановитьЗначениеXDTO(Контент, "FirstName",				Параметры.ФИО.Имя);
	УстановитьЗначениеXDTO(Контент, "Patronymic",				ПолучитьЗначениеXDTO(Параметры.ФИО, "Отчество"));
	
КонецПроцедуры

Процедура Заполнить_ExtendedInvoiceItem(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Product",					Параметры.НаимТов);
	УстановитьЗначениеXDTO(Контент, "UnitCode",					ПолучитьЗначениеXDTO(Параметры, "ОКЕИ_Тов"));
	УстановитьЗначениеXDTO(Контент, "Quantity",					ПолучитьЗначениеXDTO(Параметры, "КолТов"));
	УстановитьЗначениеXDTO(Контент, "Price",					ПолучитьЗначениеXDTO(Параметры, "ЦенаТов"));
	УстановитьЗначениеXDTO(Контент, "Excise",					Акциз_Из_XML(Параметры.Акциз));
	УстановитьЗначениеXDTO(Контент, "TaxRate",					TaxRate_Из_НалСт(Параметры.НалСт));
	УстановитьЗначениеXDTO(Контент, "SubtotalWithVatExcluded",	ПолучитьЗначениеXDTO(Параметры, "СтТовБезНДС"));
	УстановитьЗначениеXDTO(Контент, "Vat",						СуммаНДС_Из_XML(Параметры.СумНал));
	УстановитьЗначениеXDTO(Контент, "Subtotal",					ПолучитьЗначениеXDTO(Параметры, "СтТовУчНал"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДопСведТов") Тогда
		
		УстановитьЗначениеXDTO(Контент, "UnitName",					ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "НаимЕдИзм"));
		УстановитьЗначениеXDTO(Контент, "AdditionalProperty",		ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "ДопПризн"));
		УстановитьЗначениеXDTO(Контент, "VendorCode",				ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "КодТов"));
		УстановитьЗначениеXDTO(Контент, "ToRelease",				ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "НадлОтп"));
		УстановитьЗначениеXDTO(Контент, "AccountDebit",				ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "КорСчДебет"));
		УстановитьЗначениеXDTO(Контент, "AccountCredit",			ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "КорСчКредит"));
		
		ItemMark = ItemMark(ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "ПрТовРаб"));
		УстановитьЗначениеXDTO(Контент, "ItemMark",					ItemMark);
		
	КонецЕсли;
	
	Для Каждого СвТД Из СписокXDTOИзЭлемента(Параметры, "СвТД") Цикл
		Заполнить_CustomDeclaration(НовыйЭлементСпискаXDTO(Контент.CustomDeclarations), СвТД);
	КонецЦикла;
	
	Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, Параметры, "ИнфПолФХЖ2");
	
КонецПроцедуры

Процедура Заполнить_ExtendedInvoiceItem820(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Product",					ПолучитьЗначениеXDTO(Параметры, "НаимТов"));
	УстановитьЗначениеXDTO(Контент, "Unit",						ПолучитьЗначениеXDTO(Параметры, "ОКЕИ_Тов"));
	УстановитьЗначениеXDTO(Контент, "Quantity",					ПолучитьЗначениеXDTO(Параметры, "КолТов"));
	УстановитьЗначениеXDTO(Контент, "Price",					ПолучитьЗначениеXDTO(Параметры, "ЦенаТов"));
	УстановитьЗначениеXDTO(Контент, "SubtotalWithVatExcluded",	ПолучитьЗначениеXDTO(Параметры, "СтТовБезНДС"));
	УстановитьЗначениеXDTO(Контент, "Subtotal",					ПолучитьЗначениеXDTO(Параметры, "СтТовУчНал"));
	УстановитьЗначениеXDTO(Контент, "TaxRate",					TaxRate_Из_НалСт(Параметры.НалСт));
	УстановитьЗначениеXDTO(Контент, "Excise",					Акциз_Из_XML(Параметры.Акциз));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДефОКЕИ_Тов") Тогда
		УстановитьЗначениеXDTO(Контент, "HyphenUnit", Истина);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДефСтТовУчНал") Тогда
		УстановитьЗначениеXDTO(Контент, "HyphenSubtotal", Истина);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СумНал, "СумНал") Тогда
		УстановитьЗначениеXDTO(Контент, "Vat", Параметры.СумНал.СумНал);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СумНал, "БезНДС") Тогда
		УстановитьЗначениеXDTO(Контент, "WithoutVat", "true");
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СумНал, "ДефНДС") Тогда
		УстановитьЗначениеXDTO(Контент, "HyphenVat", Истина);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДопСведТов") Тогда
		
		ItemMark = ItemMark(ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "ПрТовРаб"));
		
		УстановитьЗначениеXDTO(Контент, "ItemMark",				ItemMark);
		УстановитьЗначениеXDTO(Контент, "AdditionalProperty",	ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "ДопПризн"));
		УстановитьЗначениеXDTO(Контент, "UnitName",				ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "НаимЕдИзм"));
		УстановитьЗначениеXDTO(Контент, "ItemToRelease",		ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "НадлОтп"));
		УстановитьЗначениеXDTO(Контент, "ItemCharact",			ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "ХарактерТов"));
		УстановитьЗначениеXDTO(Контент, "ItemKind",				ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "СортТов"));
		УстановитьЗначениеXDTO(Контент, "ItemArticle",			ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "АртикулТов"));
		УстановитьЗначениеXDTO(Контент, "ItemVendorCode",		ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "КодТов"));
		УстановитьЗначениеXDTO(Контент, "CatalogCode",			ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "КодКат"));
		УстановитьЗначениеXDTO(Контент, "ItemTypeCode",			ПолучитьЗначениеXDTO(Параметры.ДопСведТов, "КодВидТов"));
		
		// В Прото нет поля для "КрНаимСтрПр".
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ДопСведТов, "СведПрослеж") Тогда
			
			СведПрослеж = Параметры.ДопСведТов.СведПрослеж;
			
			Для Каждого СведПрослеж Из СписокXDTOИзЭлемента(Параметры.ДопСведТов, "СведПрослеж") Цикл

				ItemTracingInfo = НовыйЭлементСпискаXDTO(Контент.ItemTracingInfos);
				
				УстановитьЗначениеXDTO(ItemTracingInfo, "RegNumberUnit",	СведПрослеж.НомТовПрослеж);
				УстановитьЗначениеXDTO(ItemTracingInfo, "Unit",				СведПрослеж.ЕдИзмПрослеж);
				УстановитьЗначениеXDTO(ItemTracingInfo, "UnitName",			ПолучитьЗначениеXDTO(СведПрослеж, "НаимЕдИзмПрослеж"));
				УстановитьЗначениеXDTO(ItemTracingInfo, "Quantity",			СведПрослеж.КолВЕдПрослеж);
				УстановитьЗначениеXDTO(ItemTracingInfo, "ItemAddInfo",		ПолучитьЗначениеXDTO(СведПрослеж, "ДопПрослеж"));
				
			КонецЦикла;
			
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ДопСведТов, "НомСредИдентТов") Тогда
			Заполнить_ItemIdentificationNumbers(Контент.ItemIdentificationNumbers,
												СписокXDTOИзЭлемента(Параметры.ДопСведТов, "НомСредИдентТов"));
		КонецЕсли;
		
	КонецЕсли;
	
	Для Каждого СвТД Из СписокXDTOИзЭлемента(Параметры, "СвТД") Цикл
		Заполнить_CustomDeclaration(НовыйЭлементСпискаXDTO(Контент.CustomDeclarations), СвТД);
	КонецЦикла;
	
	Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, Параметры, "ИнфПолФХЖ2");
	
КонецПроцедуры

Процедура Заполнить_ExtendedOrganizationInfo(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;

	УстановитьЗначениеXDTO(Контент, "BoxId",										Неопределено);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвЮЛУч") Тогда
		
		// Юр. лицо 820@, 155@, 189@
		УстановитьЗначениеXDTO(Контент, "Type",										"LegalEntity");
		УстановитьЗначениеXDTO(Контент, "Name",										Параметры.ИдСв.СвЮЛУч.НаимОрг);
		УстановитьЗначениеXDTO(Контент, "Inn",										ПолучитьЗначениеXDTO(Параметры.ИдСв.СвЮЛУч, "ИННЮЛ"));
		УстановитьЗначениеXDTO(Контент, "Kpp",										ПолучитьЗначениеXDTO(Параметры.ИдСв.СвЮЛУч, "КПП"));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв.СвЮЛУч, "ДефИННЮЛ") Тогда
			УстановитьЗначениеXDTO(Контент, "HyphenInn", Истина);
		КонецЕсли;
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвИП") Тогда
		
		// ИП 820@, 155@, 189@
		УстановитьЗначениеXDTO(Контент, "Type",										"IndividualEntity");
		УстановитьЗначениеXDTO(Контент, "Name",										XML_СтрокаФИО(Параметры.ИдСв.СвИП.ФИО));
		УстановитьЗначениеXDTO(Контент, "Inn",										ПолучитьЗначениеXDTO(Параметры.ИдСв.СвИП, "ИННФЛ"));
		УстановитьЗначениеXDTO(Контент, "IndividualEntityRegistrationCertificate",	ПолучитьЗначениеXDTO(Параметры.ИдСв.СвИП, "СвГосРегИП"));
		УстановитьЗначениеXDTO(Контент, "OrganizationOrPersonInfo",					ПолучитьЗначениеXDTO(Параметры.ИдСв.СвИП, "ИныеСвед"));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв.СвИП, "ДефИННФЛ") Тогда
			УстановитьЗначениеXDTO(Контент, "HyphenInn", Истина);
		КонецЕсли;
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвИнНеУч") Тогда
		
		// Иностранное лицо, не состоящее на учете в налоговых органах 820@, 155@, 189@
		УстановитьЗначениеXDTO(Контент, "Type",										"ForeignEntity");
		УстановитьЗначениеXDTO(Контент, "Name",										Параметры.ИдСв.СвИнНеУч.НаимОрг);
		УстановитьЗначениеXDTO(Контент, "OrganizationOrPersonInfo",					ПолучитьЗначениеXDTO(Параметры.ИдСв.СвИнНеУч, "ИныеСвед"));
		УстановитьЗначениеXDTO(Контент, "LegalEntityId",							ПолучитьЗначениеXDTO(Параметры.ИдСв.СвИнНеУч, "Идентиф"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвЮлНеУч") Тогда
		
		// Юр. лицо, не состоящее на учете в налоговых органах 155@
		УстановитьЗначениеXDTO(Контент, "Type",										"ForeignEntity");
		УстановитьЗначениеXDTO(Контент, "Name",										Параметры.ИдСв.СвЮлНеУч.НаимОрг);
		УстановитьЗначениеXDTO(Контент, "OrganizationOrPersonInfo",					ПолучитьЗначениеXDTO(Параметры.ИдСв.СвЮлНеУч, "ИныеСвед"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвФЛ") Тогда
		
		// Физ. лицо 551@ и 552@
		УстановитьЗначениеXDTO(Контент, "Type",										"PhysicalEntity");
		УстановитьЗначениеXDTO(Контент, "Name",										XML_СтрокаФИО(Параметры.ИдСв.СвФЛ.ФИО));
		УстановитьЗначениеXDTO(Контент, "Inn",										ПолучитьЗначениеXDTO(Параметры.ИдСв.СвФЛ, "ИННФЛ"));
		УстановитьЗначениеXDTO(Контент, "OrganizationOrPersonInfo",					ПолучитьЗначениеXDTO(Параметры.ИдСв.СвФЛ, "ИныеСвед"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвФЛУчастФХЖ") Тогда
		
		// Физ. лицо 820@
		УстановитьЗначениеXDTO(Контент, "Type",										"PhysicalEntity");
		УстановитьЗначениеXDTO(Контент, "Name",										XML_СтрокаФИО(Параметры.ИдСв.СвФЛУчастФХЖ.ФИО));
		УстановитьЗначениеXDTO(Контент, "Inn",										ПолучитьЗначениеXDTO(Параметры.ИдСв.СвФЛУчастФХЖ, "ИННФЛ"));
		УстановитьЗначениеXDTO(Контент, "OrganizationOrPersonInfo",					ПолучитьЗначениеXDTO(Параметры.ИдСв.СвФЛУчастФХЖ, "ИныеСвед"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвОрг") Тогда
		
		СвОрг = Параметры.ИдСв.СвОрг; // Сведения об организации 551@ и 552@
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвОрг, "СвЮЛ") Тогда
			
			// Российская организаця
			УстановитьЗначениеXDTO(Контент, "Type",									"LegalEntity");
			УстановитьЗначениеXDTO(Контент, "Name",									СвОрг.СвЮЛ.НаимОрг);
			УстановитьЗначениеXDTO(Контент, "Inn",									СвОрг.СвЮЛ.ИННЮЛ);
			УстановитьЗначениеXDTO(Контент, "Kpp",									ПолучитьЗначениеXDTO(СвОрг.СвЮЛ, "КПП"));
			
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвОрг, "ИнОрг") Тогда
			
			// Иностранная организация
			УстановитьЗначениеXDTO(Контент, "Type",									"ForeignEntity");
			УстановитьЗначениеXDTO(Контент, "Name",									СвОрг.ИнОрг.НаимОрг);
			УстановитьЗначениеXDTO(Контент, "OrganizationOrPersonInfo",				ПолучитьЗначениеXDTO(СвОрг.ИнОрг, "ИныеСвед"));
			УстановитьЗначениеXDTO(Контент, "Country",								ПолучитьЗначениеXDTO(СвОрг.ИнОрг, "Страна"));
			
		КонецЕсли;
		
	КонецЕсли;	
	
	УстановитьЗначениеXDTO(Контент, "Okpo",											ПолучитьЗначениеXDTO(Параметры, "ОКПО"));
	УстановитьЗначениеXDTO(Контент, "Department",									ПолучитьЗначениеXDTO(Параметры, "СтруктПодр"));
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo",								ПолучитьЗначениеXDTO(Параметры, "ИнфДляУчаст"));
		
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "БанкРекв") Тогда
		УстановитьЗначениеXDTO(Контент, "BankAccountNumber",						ПолучитьЗначениеXDTO(Параметры.БанкРекв, "НомерСчета"));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.БанкРекв, "СвБанк") Тогда
			УстановитьЗначениеXDTO(Контент, "BankName",								ПолучитьЗначениеXDTO(Параметры.БанкРекв.СвБанк, "НаимБанк"));
			УстановитьЗначениеXDTO(Контент, "BankId",								ПолучитьЗначениеXDTO(Параметры.БанкРекв.СвБанк, "БИК"));
			УстановитьЗначениеXDTO(Контент, "CorrespondentAccount",					ПолучитьЗначениеXDTO(Параметры.БанкРекв.СвБанк, "КорСчет"));
		КонецЕсли;
				
	КонецЕсли;

	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Контакт") Тогда
		УстановитьЗначениеXDTO(Контент, "Phone",									ПолучитьЗначениеXDTO(Параметры.Контакт, "Тлф"));
		УстановитьЗначениеXDTO(Контент, "Email",									ПолучитьЗначениеXDTO(Параметры.Контакт, "ЭлПочта"));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Адрес") Тогда
		Заполнить_AddressInfo(Контент.Address, Параметры.Адрес);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_ExtendedOrganizationInfo970(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ВидыЮрФизЛиц = Перечисление_ВидыЮрФизЛиц();
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвЮЛУч") Тогда
		
		СвЮЛУч = Параметры.ИдСв.СвЮЛУч;	
		
		УстановитьЗначениеXDTO(Контент, "Type", ВидыЮрФизЛиц.ЮридическоеЛицо);
		
		УстановитьЗначениеXDTO(Контент, "Name", СвЮЛУч.НаимОрг);
		УстановитьЗначениеXDTO(Контент, "Inn", СвЮЛУч.ИННЮЛ);
		УстановитьЗначениеXDTO(Контент, "Kpp", ПолучитьЗначениеXDTO(СвЮЛУч, "КПП"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвИП") Тогда
		
		СвИП = Параметры.ИдСв.СвИП;
		
		УстановитьЗначениеXDTO(Контент, "Type",	ВидыЮрФизЛиц.ИндивидуальныйПредприниматель);
		
		УстановитьЗначениеXDTO(Контент, "Name",	XML_СтрокаФИО(СвИП.ФИО));
		УстановитьЗначениеXDTO(Контент, "Inn", СвИП.ИННФЛ);
		
		УстановитьЗначениеXDTO(Контент, "IndividualEntityRegistrationCertificate", ПолучитьЗначениеXDTO(СвИП, "СвГосРегИП"));
		УстановитьЗначениеXDTO(Контент, "IndividualEntityRegistrationNumber", ПолучитьЗначениеXDTO(СвИП, "ОГРНИП"));
		УстановитьЗначениеXDTO(Контент, "IndividualEntityRegistrationDate", ДатаXML_в_Дата1С(ПолучитьЗначениеXDTO(СвИП, "ДатаОГРНИП")));
		УстановитьЗначениеXDTO(Контент, "OrganizationOrPersonInfo", ПолучитьЗначениеXDTO(СвИП, "ИныеСвед"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвФЛУч") Тогда
		
		СвФЛУч = Параметры.ИдСв.СвФЛУч;	
		
		УстановитьЗначениеXDTO(Контент, "Type", ВидыЮрФизЛиц.ФизическоеЛицо);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвФЛУч, "ФИО") Тогда
			УстановитьЗначениеXDTO(Контент, "Name", XML_СтрокаФИО(СвФЛУч.ФИО));
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Контент, "Inn", ПолучитьЗначениеXDTO(СвФЛУч, "ИННФЛ"));
		УстановитьЗначениеXDTO(Контент, "OrganizationOrPersonInfo", ПолучитьЗначениеXDTO(СвФЛУч, "ИныеСвед"));
		УстановитьЗначениеXDTO(Контент, "PhysicalEntityStatusID", ПолучитьЗначениеXDTO(СвФЛУч, "ИдСтатЛ"));
			
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвИнНеУч") Тогда
		
		СвИнНеУч = Параметры.ИдСв.СвИнНеУч;

		Если СвИнНеУч.ИдСтат = "ИО" Тогда
			ВидЮрФизЛица = ВидыЮрФизЛиц.ИностраннаяОрганизация;
		Иначе
			ВидЮрФизЛица = ВидыЮрФизЛиц.ИностранныйГражданин;
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Контент, "Type", ВидЮрФизЛица);
		
		УстановитьЗначениеXDTO(Контент, "Name", СвИнНеУч.Наим);
		УстановитьЗначениеXDTO(Контент, "OrganizationOrPersonInfo", ПолучитьЗначениеXDTO(СвИнНеУч, "ИныеСвед"));
		УстановитьЗначениеXDTO(Контент, "LegalEntityId", ПолучитьЗначениеXDTO(СвИнНеУч, "Идентиф"));
		
		УстановитьЗначениеXDTO(Контент, "Country", СвИнНеУч.НаимСтран);
		УстановитьЗначениеXDTO(Контент, "CountryCode", ПолучитьЗначениеXDTO(СвИнНеУч, "КодСтр"));
		
	КонецЕсли;	
	
	УстановитьЗначениеXDTO(Контент, "Okpo", ПолучитьЗначениеXDTO(Параметры, "ОКПО"));
	УстановитьЗначениеXDTO(Контент, "Okopf", ПолучитьЗначениеXDTO(Параметры, "КодОПФ"));
	УстановитьЗначениеXDTO(Контент, "FullNameOPF", ПолучитьЗначениеXDTO(Параметры, "ПолнНаимОПФ"));
	УстановитьЗначениеXDTO(Контент, "Department", ПолучитьЗначениеXDTO(Параметры, "СтруктПодр"));
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo", ПолучитьЗначениеXDTO(Параметры, "ИнфДляУчаст"));
		
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "БанкРекв") Тогда
		
		БанкРекв = Параметры.БанкРекв;
		
		УстановитьЗначениеXDTO(Контент, "BankAccountNumber", ПолучитьЗначениеXDTO(БанкРекв, "НомерСчета"));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(БанкРекв, "СвБанк") Тогда
			УстановитьЗначениеXDTO(Контент, "BankName", ПолучитьЗначениеXDTO(БанкРекв.СвБанк, "НаимБанк"));
			УстановитьЗначениеXDTO(Контент, "BankId", ПолучитьЗначениеXDTO(БанкРекв.СвБанк, "БИК"));
			УстановитьЗначениеXDTO(Контент, "CorrespondentAccount", ПолучитьЗначениеXDTO(БанкРекв.СвБанк, "КорСчет"));
		КонецЕсли;
				
	КонецЕсли;

	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Контакт") Тогда
		
		Для Каждого НомерТелефона Из СписокXDTOИзЭлемента(Параметры.Контакт, "Тлф") Цикл
			Контент.Phone.Добавить(НомерТелефона);
		КонецЦикла;
		
		Для Каждого ЭлПочта Из СписокXDTOИзЭлемента(Параметры.Контакт, "ЭлПочта") Цикл
			Контент.Email.Добавить(ЭлПочта);
		КонецЦикла;

		УстановитьЗначениеXDTO(Контент, "OtherContactInfo", ПолучитьЗначениеXDTO(Параметры.Контакт, "ИнКонт"));
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Адрес") Тогда
		Заполнить_AddressInfo970(Контент.Address, Параметры.Адрес);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_ExtendedSigner(Signers, Параметры, ИмяСвойства)
	
	Для Каждого Подписант Из СписокXDTOИзЭлемента(Параметры, ИмяСвойства) Цикл
		
		ExtendedSigner	 = НовыйЭлементСпискаXDTO(Signers);
		SignerDetails	 = ExtendedSigner.SignerDetails;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Подписант, "ОблПолн") Тогда
			УстановитьЗначениеXDTO(SignerDetails, "Powers", SignerPowers(Подписант.ОблПолн));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Подписант, "Статус") Тогда
			УстановитьЗначениеXDTO(SignerDetails, "Status", SignerStatus(Подписант.Статус));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Подписант, "ОснПолн") Тогда
			УстановитьЗначениеXDTO(SignerDetails, "PowersBase", Подписант.ОснПолн);
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Подписант, "ОснПолнПодп") Тогда
			УстановитьЗначениеXDTO(SignerDetails, "PowersBase", Подписант.ОснПолнПодп);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Подписант, "ОснПолнОрг") Тогда
			УстановитьЗначениеXDTO(SignerDetails, "OrganizationPowersBase", Подписант.ОснПолнОрг);
		КонецЕсли;
		
		ФИО = Неопределено;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Подписант, "ФЛ") Тогда
			
			ФЛ	 = Подписант.ФЛ;
			ФИО	 = ФЛ.ФИО;
			
			УстановитьЗначениеXDTO(SignerDetails, "SignerType",	"PhysicalPerson");
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ФЛ, "ГосРегИППодп") Тогда
				УстановитьЗначениеXDTO(SignerDetails, "RegistrationCertificate", ФЛ.ГосРегИППодп);
			КонецЕсли;
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ФЛ, "ГосРегИПВыдДов") Тогда
				УстановитьЗначениеXDTO(SignerDetails, "RegistrationCertificate", ФЛ.ГосРегИПВыдДов);
			КонецЕсли;
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ФЛ, "ИННФЛ") Тогда
				УстановитьЗначениеXDTO(SignerDetails, "Inn", ФЛ.ИННФЛ);
			КонецЕсли;
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ФЛ, "ИныеСвед") Тогда
				УстановитьЗначениеXDTO(SignerDetails, "SignerInfo", ФЛ.ИныеСвед);
			КонецЕсли;
			
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Подписант, "ИП") Тогда
			
			ИП	 = Подписант.ИП;
			ФИО	 = ИП.ФИО;
			
			УстановитьЗначениеXDTO(SignerDetails, "SignerType"	, "IndividualEntity");
			УстановитьЗначениеXDTO(SignerDetails, "Inn"			, ИП.ИННФЛ);
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ИП, "СвГосРегИП") Тогда
				УстановитьЗначениеXDTO(SignerDetails, "RegistrationCertificate", ИП.СвГосРегИП);
			КонецЕсли;
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ИП, "ИныеСвед") Тогда
				УстановитьЗначениеXDTO(SignerDetails, "SignerInfo", ИП.ИныеСвед);
			КонецЕсли;
			
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Подписант, "ЮЛ") Тогда
			
			ЮЛ	 = Подписант.ЮЛ;
			ФИО	 = ЮЛ.ФИО;
			
			УстановитьЗначениеXDTO(SignerDetails, "SignerType"	, "LegalEntity");
			УстановитьЗначениеXDTO(SignerDetails, "Inn"			, ЮЛ.ИННЮЛ);
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ЮЛ, "Должн") Тогда
				УстановитьЗначениеXDTO(SignerDetails, "JobTitle", ЮЛ.Должн);
			ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ЮЛ, "Должность") Тогда
				УстановитьЗначениеXDTO(SignerDetails, "JobTitle", ЮЛ.Должность);
			КонецЕсли;
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ЮЛ, "ГосРегИППодп") Тогда
				УстановитьЗначениеXDTO(SignerDetails, "RegistrationCertificate", ЮЛ.ГосРегИППодп);
			КонецЕсли;
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ЮЛ, "ГосРегИПВыдДов") Тогда
				УстановитьЗначениеXDTO(SignerDetails, "RegistrationCertificate", ЮЛ.ГосРегИПВыдДов);
			КонецЕсли;
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ЮЛ, "НаимОрг") Тогда
				УстановитьЗначениеXDTO(SignerDetails, "OrganizationName", ЮЛ.НаимОрг);
			КонецЕсли;
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ЮЛ, "ИныеСвед") Тогда
				УстановитьЗначениеXDTO(SignerDetails, "SignerInfo", ЮЛ.ИныеСвед);
			КонецЕсли;
			
		КонецЕсли;
		
		Если ФИО <> Неопределено Тогда
			
			УстановитьЗначениеXDTO(SignerDetails, "FirstName"	, ФИО.Имя);
			УстановитьЗначениеXDTO(SignerDetails, "Surname"		, ФИО.Фамилия);
			УстановитьЗначениеXDTO(SignerDetails, "Patronymic"	, ПолучитьЗначениеXDTO(ФИО, "Отчество"));
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_Signer970(Контент, Параметры)
	
	ФИО = ПолучитьЗначениеXDTO(Параметры, "ФИО");
	Если ФИО <> Неопределено Тогда
		Заполнить_ФИО(Контент, ФИО);
	КонецЕсли;
	
	Долж = ПолучитьЗначениеXDTO(Параметры, "Должн");
	СпосПодтПолном = ПолучитьЗначениеXDTO(Параметры, "СпосПодтПолном");
	ДопСведПодп = ПолучитьЗначениеXDTO(Параметры, "ДопСведПодп");
	ДатаПодДок = ДатаXML_в_Дата1С(ПолучитьЗначениеXDTO(Параметры, "ДатаПодДок"));
	
	УстановитьЗначениеXDTO(Контент, "JobTitle", Долж);
	УстановитьЗначениеXDTO(Контент, "DateSigningDocument", ДатаПодДок);
	УстановитьЗначениеXDTO(Контент, "ConfirmCredentials", СпосПодтПолном);
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo", ДопСведПодп);
	
	ТипПодписиXML = ПолучитьЗначениеXDTO(Параметры, "ТипПодпис"); 	
	ТипыПодписей = Перечисление_ТипыПодписей();
	
	Если ТипПодписиXML = "1" Тогда
		ТипПодписиПрото = ТипыПодписей.Квалифицированная;
	ИначеЕсли ТипПодписиXML = "2" Тогда
		ТипПодписиПрото = ТипыПодписей.Простая;
	ИначеЕсли ТипПодписиXML = "3" Тогда
		ТипПодписиПрото = ТипыПодписей.Неквалифицированная;
	Иначе
		ТипПодписиПрото = Неопределено;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "SignType", ТипПодписиПрото);
	
	Если Контент.ConfirmCredentials = "3" Тогда
		
		Для Каждого СвДоверЭл Из СписокXDTOИзЭлемента(Параметры, "СвДоверЭл") Цикл
			СведенияОМЧД = НовыйЭлементСпискаXDTO(Контент.ElectronicPOA);
			Заполнить_ElectronicAttorneyInfo970(СведенияОМЧД, СвДоверЭл);
		КонецЦикла;
		
	ИначеЕсли Контент.ConfirmCredentials = "5" Тогда
		
		Для Каждого СвДоверБум Из СписокXDTOИзЭлемента(Параметры, "СвДоверБум") Цикл
			СведенияОДоверенности = НовыйЭлементСпискаXDTO(Контент.PaperPOA);
			Заполнить_PaperAttorneyInfo970(СведенияОДоверенности, СвДоверБум);
		КонецЦикла;

	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_ElectronicAttorneyInfo970(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "ID", Параметры.НомДовер);
	УстановитьЗначениеXDTO(Контент, "IssueDate", ДатаXML_в_Дата1С(Параметры.ДатаВыдДовер));
	
	УстановитьЗначениеXDTO(Контент, "StorageID", Параметры.ИдСистХран);
	УстановитьЗначениеXDTO(Контент, "URL", ПолучитьЗначениеXDTO(Параметры, "УРЛСист"));
	
	УстановитьЗначениеXDTO(Контент, "Number", ПолучитьЗначениеXDTO(Параметры, "ВнНомДовер"));
	УстановитьЗначениеXDTO(Контент, "RegistrationDate", ДатаXML_в_Дата1С(ПолучитьЗначениеXDTO(Параметры, "ДатаВнРегДовер")));
	
КонецПроцедуры
	
Процедура Заполнить_PaperAttorneyInfo970(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ФИО") Тогда
		ФИО = Параметры.ФИО;
		УстановитьЗначениеXDTO(Контент, "IssuerFirstName", ФИО.Имя);
		УстановитьЗначениеXDTO(Контент, "IssuerSurname", ФИО.Фамилия);
		УстановитьЗначениеXDTO(Контент, "IssuerPatronymic", ПолучитьЗначениеXDTO(ФИО, "Отчество"));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Number", Параметры.ВнНомДовер);
	УстановитьЗначениеXDTO(Контент, "IssueDate", ДатаXML_в_Дата1С(Параметры.ДатаВыдДовер));
	
	УстановитьЗначениеXDTO(Контент, "PrincipalInfo", ПолучитьЗначениеXDTO(Параметры, "СвИдДовер"));
	
КонецПроцедуры

Процедура Заполнить_InvoiceContent(Контент, Параметры)
	
	Если Параметры.ВерсФорм = "5.01" Тогда
		Заполнить_InvoiceContent_5_01(Контент, Параметры);
	ИначеЕсли Параметры.ВерсФорм = "5.02" Тогда
		Заполнить_InvoiceContent_5_02(Контент, Параметры);
	ИначеЕсли Параметры.ВерсФорм = "5.03" Тогда
		Заполнить_InvoiceContent_5_03(Контент, Параметры);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_InvoiceContent_5_01(Контент, Параметры)
	
	Документ	 = Параметры.Документ;
	СвСчФакт	 = Документ.СвСчФакт;
	ТаблСчФакт	 = Документ.ТаблСчФакт;
	
	УстановитьЗначениеXDTO(Контент, "InvoiceVersion", Параметры.ВерсФорм);
	УстановитьЗначениеXDTO(Контент, "Number"		, СвСчФакт.НомерСчФ);
	УстановитьЗначениеXDTO(Контент, "Date"			, ДатаXML_в_Дата1С(СвСчФакт.ДатаСчФ));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ИспрСчФ") Тогда
		УстановитьЗначениеXDTO(Контент, "InvoiceRevisionDate", ДатаXML_в_Дата1С(СвСчФакт.ИспрСчФ.ДатаИспрСчФ));
		УстановитьЗначениеXDTO(Контент, "InvoiceRevisionNumber", СвСчФакт.ИспрСчФ.НомИспрСчФ);
	КонецЕсли;	
	
	УстановитьЗначениеXDTO(Контент, "Currency", СвСчФакт.КодОКВ);
	
	Заполнить_OrganizationInfo(Контент.Seller, СвСчФакт.СвПрод);
	Заполнить_OrganizationInfo(Контент.Buyer, СвСчФакт.СвПокуп);
	
	Заполнить_InvoiceTotals(Контент.Totals, ТаблСчФакт.ВсегоОпл);
	Заполнить_Signer(Контент, Документ, "Подписант");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ГрузОт") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт.ГрузОт, "ОнЖе") Тогда
			УстановитьЗначениеXDTO(Контент.Shipper, "SameAsSellerOrBuyer", Истина);
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт.ГрузОт, "ГрузОтпр") Тогда
			Заполнить_ShipperOrConsigneeInfo(Контент.Shipper, СвСчФакт.ГрузОт.ГрузОтпр);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ГрузПолуч") Тогда
		Заполнить_ShipperOrConsigneeInfo(Контент.Consignee, СвСчФакт.ГрузПолуч);
	КонецЕсли;
	
	Для Каждого СведТов Из СписокXDTOИзЭлемента(ТаблСчФакт, "СведТов") Цикл
		Заполнить_InvoiceItem(НовыйЭлементСпискаXDTO(Контент.Items), СведТов);
	КонецЦикла;
	
	Для Каждого СвПРД Из СписокXDTOИзЭлемента(СвСчФакт, "СвПРД") Цикл
		Заполнить_PaymentDocument(НовыйЭлементСпискаXDTO(Контент.PaymentDocuments), СвПРД);
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ИнфПол") Тогда
		Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, СвСчФакт.ИнфПол, "ТекстИнф");
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_InvoiceContent_5_02(Контент, Параметры)
	
	Документ = Параметры.Документ;
	СвСчФакт = Документ.СвСчФакт;
	ТаблСчФакт = Документ.ТаблСчФакт;
	
	УстановитьЗначениеXDTO(Контент, "InvoiceVersion", Параметры.ВерсФорм);
	УстановитьЗначениеXDTO(Контент, "Number", СвСчФакт.НомерСчФ);
	УстановитьЗначениеXDTO(Контент, "Date", ДатаXML_в_Дата1С(СвСчФакт.ДатаСчФ));
	УстановитьЗначениеXDTO(Контент, "Currency", СвСчФакт.КодОКВ);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ИспрСчФ") Тогда
		УстановитьЗначениеXDTO(Контент, "InvoiceRevisionDate", ДатаXML_в_Дата1С(СвСчФакт.ИспрСчФ.ДатаИспрСчФ));
		УстановитьЗначениеXDTO(Контент, "InvoiceRevisionNumber", СвСчФакт.ИспрСчФ.НомИспрСчФ);
	КонецЕсли;
	
	Заполнить_OrganizationInfo(Контент.Seller, СвСчФакт.СвПрод);
	Заполнить_OrganizationInfo(Контент.Buyer, СвСчФакт.СвПокуп);
	
	Заполнить_InvoiceTotals(Контент.Totals, ТаблСчФакт.ВсегоОпл);
	Заполнить_Signer(Контент, Документ, "Подписант");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ГрузОт") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт.ГрузОт, "ОнЖе") Тогда
			УстановитьЗначениеXDTO(Контент.Shipper, "SameAsSellerOrBuyer", Истина);
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт.ГрузОт, "ГрузОтпр") Тогда
			Заполнить_ShipperOrConsigneeInfo(Контент.Shipper, СвСчФакт.ГрузОт.ГрузОтпр);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ГрузПолуч") Тогда
		Заполнить_ShipperOrConsigneeInfo(Контент.Consignee, СвСчФакт.ГрузПолуч);
	КонецЕсли;
	
	Для Каждого СтрокаТоваров Из СписокXDTOИзЭлемента(ТаблСчФакт, "СведТов") Цикл
		Заполнить_InvoiceItem(НовыйЭлементСпискаXDTO(Контент.Items), СтрокаТоваров);
	КонецЦикла;
	
	Для Каждого СвПРД Из СписокXDTOИзЭлемента(СвСчФакт, "СвПРД") Цикл
		Заполнить_PaymentDocument(НовыйЭлементСпискаXDTO(Контент.PaymentDocuments), СвПРД);
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ИнфПол") Тогда
		Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, СвСчФакт.ИнфПол, "ТекстИнф");
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_InvoiceContent_5_03(Контент, Параметры)
	
	Документ = Параметры.Документ;
	СвСчФакт = Документ.СвСчФакт;
	ТаблСчФакт = Документ.ТаблСчФакт;
	
	УстановитьЗначениеXDTO(Контент, "InvoiceVersion", Параметры.ВерсФорм);
	УстановитьЗначениеXDTO(Контент, "Number", СвСчФакт.НомерСчФ);
	УстановитьЗначениеXDTO(Контент, "Date", ДатаXML_в_Дата1С(СвСчФакт.ДатаСчФ));
	УстановитьЗначениеXDTO(Контент, "Currency", СвСчФакт.КодОКВ);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ИспрСчФ") Тогда
		УстановитьЗначениеXDTO(Контент, "InvoiceRevisionDate", ДатаXML_в_Дата1С(СвСчФакт.ИспрСчФ.ДатаИспрСчФ));
		УстановитьЗначениеXDTO(Контент, "InvoiceRevisionNumber", СвСчФакт.ИспрСчФ.НомИспрСчФ);
	КонецЕсли;
	
	Заполнить_OrganizationInfo(Контент.Seller, СвСчФакт.СвПрод);
	Заполнить_OrganizationInfo(Контент.Buyer, СвСчФакт.СвПокуп);
	
	Заполнить_InvoiceTotals(Контент.Totals, ТаблСчФакт.ВсегоОпл);
	Заполнить_Signer(Контент, Документ, "Подписант");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ГрузОт") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт.ГрузОт, "ОнЖе") Тогда
			УстановитьЗначениеXDTO(Контент.Shipper, "SameAsSellerOrBuyer", Истина);
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт.ГрузОт, "ГрузОтпр") Тогда
			Заполнить_ShipperOrConsigneeInfo(Контент.Shipper, СвСчФакт.ГрузОт.ГрузОтпр);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ГрузПолуч") Тогда
		Заполнить_ShipperOrConsigneeInfo(Контент.Consignee, СвСчФакт.ГрузПолуч);
	КонецЕсли;
	
	Для Каждого СтрокаТоваров Из СписокXDTOИзЭлемента(ТаблСчФакт, "СведТов") Цикл
		Заполнить_InvoiceItem(НовыйЭлементСпискаXDTO(Контент.Items), СтрокаТоваров);
	КонецЦикла;
	
	Для Каждого СвПРД Из СписокXDTOИзЭлемента(СвСчФакт, "СвПРД") Цикл
		Заполнить_PaymentDocument(НовыйЭлементСпискаXDTO(Контент.PaymentDocuments), СвПРД);
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ИнфПол") Тогда
		Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, СвСчФакт.ИнфПол, "ТекстИнф");
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_InvoiceCorrectionContent(Контент, Параметры)
	
	Если Параметры.ВерсФорм = "5.02" Тогда
		Заполнить_InvoiceCorrectionContent_5_02(Контент, Параметры);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_InvoiceCorrectionContent_5_02(Контент, Параметры)
	
	Документ = Параметры.Документ;
	СвКСчФ	 = Документ.СвКСчФ;
	ТаблКСчФ = Документ.ТаблКСчФ;
	
	УстановитьЗначениеXDTO(Контент, "Currency"					, СвКСчФ.КодОКВ);
	УстановитьЗначениеXDTO(Контент, "InvoiceVersion"			, Параметры.ВерсФорм);
	УстановитьЗначениеXDTO(Контент, "InvoiceCorrectionDate"		, ДатаXML_в_Дата1С(СвКСчФ.ДатаКСчФ));
	УстановитьЗначениеXDTO(Контент, "InvoiceCorrectionNumber"	, СвКСчФ.НомерКСчФ);
	
	Заполнить_OrganizationInfo(Контент.Seller, СвКСчФ.СвПрод);
	Заполнить_OrganizationInfo(Контент.Buyer, СвКСчФ.СвПокуп);
	Заполнить_Signer(Контент, Документ, "Подписант");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвКСчФ, "ИспрКСчФ") Тогда
		УстановитьЗначениеXDTO(Контент, "InvoiceCorrectionRevisionDate", ДатаXML_в_Дата1С(СвКСчФ.ИспрКСчФ.ДатаИспрКСчФ));
		УстановитьЗначениеXDTO(Контент, "InvoiceCorrectionRevisionNumber", СвКСчФ.ИспрКСчФ.НомИспрКСчФ);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ТаблКСчФ, "ВсегоУвел") Тогда
		Заполнить_InvoiceTotalsDiff(Контент.TotalsInc, ТаблКСчФ.ВсегоУвел);
	КонецЕсли;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ТаблКСчФ, "ВсегоУм") Тогда
		Заполнить_InvoiceTotalsDiff(Контент.TotalsDec, ТаблКСчФ.ВсегоУм);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвКСчФ, "ИнфПол") Тогда 
		Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, СвКСчФ.ИнфПол, "ТекстИнф");
	КонецЕсли;
	
	Для Каждого СчФ Из СписокXDTOИзЭлемента(СвКСчФ, "СчФ") Цикл
		Заполнить_OriginalInvoice(НовыйЭлементСпискаXDTO(Контент.OriginalInvoices), СчФ);
	КонецЦикла;
	
	Для Каждого СведТов Из СписокXDTOИзЭлемента(ТаблКСчФ, "СведТов") Цикл
		Заполнить_InvoiceCorrectionItem(НовыйЭлементСпискаXDTO(Контент.Items), СведТов);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_ProformaInvoiceSellerContent(Контент, Параметры)
	
	Документ = Параметры.Документ;
	СвСНО	 = Документ.СвСНО;
	
	УстановитьЗначениеXDTO(Контент, "DocumentDate"	, ДатаXML_в_Дата1С(СвСНО.ДатаДок));
	УстановитьЗначениеXDTO(Контент, "DocumentNumber", СвСНО.НомерДок);
	
	Контент.Seller.FnsParticipantId	 = Параметры.СвУчДокОбор.ИдОтпр;
	Контент.Buyer.FnsParticipantId	 = Параметры.СвУчДокОбор.ИдПол;
	
	Заполнить_ExtendedOrganizationInfo(Контент.Seller,	СвСНО.СвПрод);
	Заполнить_ExtendedOrganizationInfo(Контент.Buyer,	СвСНО.СвПокуп);
	
	Заполнить_Signer(Контент, Документ, "Подписант");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСНО, "СвПлат") Тогда
		Заполнить_ExtendedOrganizationInfo(Контент.Payer, СвСНО.СвПлат);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСНО, "СвПолуч") Тогда
		Заполнить_ExtendedOrganizationInfo(Контент.Recipient, СвСНО.СвПолуч);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСНО, "ИдГосКон") Тогда
		УстановитьЗначениеXDTO(Контент, "GovernmentContractInfo", СвСНО.ИдГосКон);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСНО, "НазначПлатеж") Тогда
		УстановитьЗначениеXDTO(Контент, "PaymentPurpose", СвСНО.НазначПлатеж);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСНО, "ДенИзм") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСНО.ДенИзм, "КодОКВ") Тогда
			УстановитьЗначениеXDTO(Контент, "Currency", СвСНО.ДенИзм.КодОКВ);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСНО.ДенИзм, "КурсВал") Тогда
			УстановитьЗначениеXDTO(Контент, "CurrencyRate", СвСНО.ДенИзм.КурсВал);
		КонецЕсли;
	
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСНО, "Договор") Тогда
		
		УстановитьЗначениеXDTO(Контент.Contract, "Name",	СвСНО.Договор.НаимДог);
		УстановитьЗначениеXDTO(Контент.Contract, "Number",	ПолучитьЗначениеXDTO(СвСНО.Договор, "НомерДог"));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСНО.Договор, "ДатаДог") Тогда
			УстановитьЗначениеXDTO(Контент.Contract, "Date", ДатаXML_в_Дата1С(СвСНО.Договор.ДатаДог));
		КонецЕсли;
	
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ТаблСНО") Тогда
		
		Заполнить_ProformaInvoiceTable(Контент.ProformaInvoiceTable, Документ.ТаблСНО);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ.ТаблСНО, "СведЛицСчет") Тогда
			Заполнить_СписокKeyValue(Контент.PersonalAccountInfo, Документ.ТаблСНО.СведЛицСчет, "ЛицСчетИнф");
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ИнфПол") Тогда
		Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, Документ, "ИнфПол");
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_InvoiceCorrectionItem(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Product", Параметры.НаимТов);
	
	Заполнить_InvoiceItemFields(Контент.OriginalValues	, Параметры, "OriginalValues");
	Заполнить_InvoiceItemFields(Контент.CorrectedValues	, Параметры, "CorrectedValues");
	
	Заполнить_AmountsDiff(Контент.AmountsInc, Параметры, "AmountsInc");
	Заполнить_AmountsDiff(Контент.AmountsDec, Параметры, "AmountsDec");
	
	Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, Параметры, "ИнфПолСтр");
	Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, Параметры, "ИнфПолФХЖ2");
	
КонецПроцедуры

Процедура Заполнить_ExtendedInvoiceCorrectionItem(Контент, Параметры)
	
	// Документация: http://diadocsdk-1c.readthedocs.io/ru/latest/ExtendedInvoiceCorrectionItem.html
	
	Заполнить_InvoiceCorrectionItem(Контент, Параметры);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ПорНомТовВСЧФ") Тогда
		УстановитьЗначениеXDTO(Контент, "OriginalNumber", Параметры.ПорНомТовВСЧФ);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДопСведТов") Тогда
		
		ДопСведТов = Параметры.ДопСведТов;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСведТов, "КорСчДебет") Тогда
			УстановитьЗначениеXDTO(Контент, "ItemAccountDebit", ДопСведТов.КорСчДебет);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСведТов, "КорСчКредит") Тогда
			УстановитьЗначениеXDTO(Контент, "ItemAccountCredit", ДопСведТов.КорСчКредит);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ДопСведТов, "НаимЕдИзмДо") Тогда
			УстановитьЗначениеXDTO(Контент.OriginalValues, "UnitName", Параметры.ДопСведТов.НаимЕдИзмДо);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ДопСведТов, "НаимЕдИзмПосле") Тогда
			УстановитьЗначениеXDTO(Контент.CorrectedValues, "UnitName", Параметры.ДопСведТов.НаимЕдИзмПосле);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ДопСведТов, "АртикулТов") Тогда
			УстановитьЗначениеXDTO(Контент, "ItemArticle", Параметры.ДопСведТов.АртикулТов);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ДопСведТов, "КодВидТов") Тогда
			УстановитьЗначениеXDTO(Контент, "ItemTypeCode", Параметры.ДопСведТов.КодВидТов);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ДопСведТов, "КодТов") Тогда
			УстановитьЗначениеXDTO(Контент, "ItemVendorCode", Параметры.ДопСведТов.КодТов);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ДопСведТов, "СортТов") Тогда
			УстановитьЗначениеXDTO(Контент, "ItemKind", Параметры.ДопСведТов.СортТов);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ДопСведТов, "ХарактерТов") Тогда
			УстановитьЗначениеXDTO(Контент, "ItemCharact", Параметры.ДопСведТов.ХарактерТов);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НомСредИдентТовДо") Тогда
		Заполнить_ItemIdentificationNumbers(Контент.OriginalItemIdentificationNumbers,
											СписокXDTOИзЭлемента(Параметры, "НомСредИдентТовДо"));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НомСредИдентТовПосле") Тогда
		Заполнить_ItemIdentificationNumbers(Контент.CorrectedItemIdentificationNumbers,
											СписокXDTOИзЭлемента(Параметры, "НомСредИдентТовПосле"));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СведПрослеж") Тогда
		
		Для Каждого СведПрослеж Из СписокXDTOИзЭлемента(Параметры, "СведПрослеж") Цикл
			
			ItemTracingInfo = НовыйЭлементСпискаXDTO(Контент.ItemTracingInfos);
			
			УстановитьЗначениеXDTO(ItemTracingInfo, "ItemAddInfo", ПолучитьЗначениеXDTO(СведПрослеж, "ДопПрослеж"));
			УстановитьЗначениеXDTO(ItemTracingInfo, "RegNumberUnit", СведПрослеж.НомТовПрослеж);
			УстановитьЗначениеXDTO(ItemTracingInfo, "Unit", СведПрослеж.ЕдИзмПрослеж);
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СведПрослеж, "ИзмКолЕдПТ") Тогда
				УстановитьЗначениеXDTO(ItemTracingInfo.QuantityDiff, "OriginalQuantity", СведПрослеж.ИзмКолЕдПТ.КолТовЕдПТДо);
				УстановитьЗначениеXDTO(ItemTracingInfo.QuantityDiff, "CorrectedQuantity", СведПрослеж.ИзмКолЕдПТ.КолТовЕдПТПосле);
				
				Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СведПрослеж.ИзмКолЕдПТ, "КолТовЕдПТУвел") Тогда
					УстановитьЗначениеXDTO(ItemTracingInfo.QuantityDiff, "QuantityInc", СведПрослеж.ИзмКолЕдПТ.КолТовЕдПТУвел);
				Иначе
					УстановитьЗначениеXDTO(ItemTracingInfo.QuantityDiff, "QuantityDec", СведПрослеж.ИзмКолЕдПТ.КолТовЕдПТУм);
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_InvoiceCorrectionTable(Контент, Параметры)
	
	Для Каждого СведТов Из СписокXDTOИзЭлемента(Параметры, "СведТов") Цикл
		Заполнить_ExtendedInvoiceCorrectionItem(НовыйЭлементСпискаXDTO(Контент.Items), СведТов);
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ВсегоУвел") Тогда
		Заполнить_InvoiceTotalsDiff(Контент.TotalsInc, Параметры.ВсегоУвел);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ВсегоУм") Тогда
		Заполнить_InvoiceTotalsDiff(Контент.TotalsDec, Параметры.ВсегоУм);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_InvoiceItem(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Product"				, Параметры.НаимТов);
	УстановитьЗначениеXDTO(Контент, "UnitCode"				, ПолучитьЗначениеXDTO(Параметры, "ОКЕИ_Тов"));
	УстановитьЗначениеXDTO(Контент, "Quantity"				, ПолучитьЗначениеXDTO(Параметры, "КолТов"));
	УстановитьЗначениеXDTO(Контент, "Price"					, ПолучитьЗначениеXDTO(Параметры, "ЦенаТов"));
	УстановитьЗначениеXDTO(Контент, "TaxRate"				, TaxRate_Из_НалСт(Параметры.НалСт));
	УстановитьЗначениеXDTO(Контент, "TotalWithVatExcluded"	, ПолучитьЗначениеXDTO(Параметры, "СтТовБезНДС"));
	УстановитьЗначениеXDTO(Контент, "Total"					, Параметры.СтТовУчНал);
	УстановитьЗначениеXDTO(Контент, "Excise"				, Акциз_Из_XML(Параметры.Акциз));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СумНал") Тогда
		УстановитьЗначениеXDTO(Контент, "Vat", СуммаНДС_Из_XML(Параметры.СумНал));
	КонецЕсли;
	
	Заполнить_InvoiceItem_CustomsDeclarationNumbers(Контент, Параметры);
	
	Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, Параметры, "ИнфПолСтр");
	Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, Параметры, "ИнфПолФХЖ2");
	
КонецПроцедуры

Процедура Заполнить_InvoiceItem_CustomsDeclarationNumbers(Контент, Параметры)
	
	КодПроисх	 = "";
	НомераГТД	 = "";
	Разделитель	 = "";
	
	Для Каждого СвТД Из СписокXDTOИзЭлемента(Параметры, "СвТД") Цикл
		
		КодПроисх	 = СвТД.КодПроисх;
		НомераГТД	 = НомераГТД + Разделитель + СвТД.НомерТД;
		Разделитель	 = ",";
		
	КонецЦикла;
	
	Если ЗначениеЗаполнено(КодПроисх) Тогда
		УстановитьЗначениеXDTO(Контент, "CountriesOfOrigin", КодПроисх);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(НомераГТД) Тогда
		УстановитьЗначениеXDTO(Контент, "CustomsDeclarationNumbers", НомераГТД);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_InvoiceItemFields(Контент, Параметры, ИмяКонтента)
	
	InvoiceItemFields = Новый Структура;
	
	Если ИмяКонтента = "OriginalValues" Тогда
		
		InvoiceItemFields.Вставить("UnitCode", ПолучитьЗначениеXDTO(Параметры, "ОКЕИ_ТовДо"));
		InvoiceItemFields.Вставить("Quantity", ПолучитьЗначениеXDTO(Параметры, "КолТовДо"));
		InvoiceItemFields.Вставить("Price", ПолучитьЗначениеXDTO(Параметры, "ЦенаТовДо"));
		InvoiceItemFields.Вставить("TaxRate", TaxRate_Из_НалСт(Параметры.НалСтДо));
		InvoiceItemFields.Вставить("Excise", Акциз_Из_XML(Параметры.АкцизДо));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СтТовБезНДС") Тогда
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СтТовБезНДС, "СтоимДоИзм") Тогда
				
				InvoiceItemFields.Вставить("TotalWithVatExcluded", Параметры.СтТовБезНДС.СтоимДоИзм);
				
			Иначе
				
				InvoiceItemFields.Вставить("TotalWithVatExcluded", 0);
				
			КонецЕсли;
				
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДопСведТов") Тогда
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ДопСведТов, "НаимЕдИзмДо") Тогда
				InvoiceItemFields.Вставить("UnitName", Параметры.ДопСведТов.НаимЕдИзмДо);
			КонецЕсли;
			
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СумНалДо") Тогда
			InvoiceItemFields.Вставить("Vat", СуммаНДС_Из_XML(Параметры.СумНалДо));
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СумНалДо, "БезНДС") Тогда
				InvoiceItemFields.Вставить("WithoutVat", "true");
			КонецЕсли;
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СтТовУчНал") Тогда
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СтТовУчНал, "СтоимДоИзм") Тогда
				InvoiceItemFields.Вставить("Total", Параметры.СтТовУчНал.СтоимДоИзм);
			КонецЕсли;
			
		КонецЕсли;
		
	Иначе
		
		InvoiceItemFields.Вставить("UnitCode", ПолучитьЗначениеXDTO(Параметры, "ОКЕИ_ТовПосле"));
		InvoiceItemFields.Вставить("Quantity", ПолучитьЗначениеXDTO(Параметры, "КолТовПосле"));
		InvoiceItemFields.Вставить("Price", ПолучитьЗначениеXDTO(Параметры, "ЦенаТовПосле"));
		InvoiceItemFields.Вставить("TaxRate", TaxRate_Из_НалСт(Параметры.НалСтПосле));
		InvoiceItemFields.Вставить("Excise", Акциз_Из_XML(Параметры.АкцизПосле));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СтТовБезНДС") Тогда
			InvoiceItemFields.Вставить("TotalWithVatExcluded", Параметры.СтТовБезНДС.СтоимПослеИзм);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДопСведТов") Тогда
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ДопСведТов, "НаимЕдИзмПосле") Тогда
				InvoiceItemFields.Вставить("UnitName", Параметры.ДопСведТов.НаимЕдИзмПосле);
			КонецЕсли;
			
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СумНалПосле") Тогда
			InvoiceItemFields.Вставить("Vat", СуммаНДС_Из_XML(Параметры.СумНалПосле));
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СумНалПосле, "БезНДС") Тогда
				InvoiceItemFields.Вставить("WithoutVat", "true");
			КонецЕсли;
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СтТовУчНал") Тогда
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СтТовУчНал, "СтоимПослеИзм") Тогда
				InvoiceItemFields.Вставить("Total", Параметры.СтТовУчНал.СтоимПослеИзм);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Для Каждого КлючЗначение Из InvoiceItemFields Цикл
		УстановитьЗначениеXDTO(Контент, КлючЗначение.Ключ, КлючЗначение.Значение);
	КонецЦикла;

КонецПроцедуры

Процедура Заполнить_InvoiceTable(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл, "СтТовБезНДСВсего") Тогда
		УстановитьЗначениеXDTO(Контент, "TotalWithVatExcluded", Параметры.ВсегоОпл.СтТовБезНДСВсего);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл, "СтТовУчНалВсего") Тогда
		УстановитьЗначениеXDTO(Контент, "Total", Параметры.ВсегоОпл.СтТовУчНалВсего);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл, "НеттоВс") Тогда
		УстановитьЗначениеXDTO(Контент, "TotalNet", ПолучитьЗначениеXDTO(Параметры.ВсегоОпл, "НеттоВс"));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл.СумНалВсего, "СумНал") Тогда
		УстановитьЗначениеXDTO(Контент, "Vat", Параметры.ВсегоОпл.СумНалВсего.СумНал);
	КонецЕсли;
	
	Для Каждого СведТов Из СписокXDTOИзЭлемента(Параметры, "СведТов") Цикл
		Заполнить_ExtendedInvoiceItem(НовыйЭлементСпискаXDTO(Контент.Items), СведТов);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_InvoiceTable820(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл, "СтТовБезНДСВсего") Тогда
		УстановитьЗначениеXDTO(Контент, "TotalWithVatExcluded", Параметры.ВсегоОпл.СтТовБезНДСВсего);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл, "СтТовУчНалВсего") Тогда
		УстановитьЗначениеXDTO(Контент, "Total", Параметры.ВсегоОпл.СтТовУчНалВсего);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл, "ДефСтТовУчНалВсего") Тогда
		УстановитьЗначениеXDTO(Контент, "HyphenTotal", Истина);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл, "КолНеттоВс") Тогда
		УстановитьЗначениеXDTO(Контент, "TotalNet", ПолучитьЗначениеXDTO(Параметры.ВсегоОпл, "КолНеттоВс"));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл.СумНалВсего, "СумНал") Тогда
		УстановитьЗначениеXDTO(Контент, "Vat", Параметры.ВсегоОпл.СумНалВсего.СумНал);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл.СумНалВсего, "БезНДС") Тогда
		УстановитьЗначениеXDTO(Контент, "WithoutVat", "true");
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл.СумНалВсего, "ДефНДС") Тогда
		УстановитьЗначениеXDTO(Контент, "HyphenVat", Истина);
	КонецЕсли;
	
	Для Каждого СведТов Из СписокXDTOИзЭлемента(Параметры, "СведТов") Цикл
		Заполнить_ExtendedInvoiceItem820(НовыйЭлементСпискаXDTO(Контент.Items), СведТов);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_InvoiceTable970(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл, "СтТовБезНДСВсего") Тогда
		УстановитьЗначениеXDTO(Контент, "TotalWithVatExcluded", Параметры.ВсегоОпл.СтТовБезНДСВсего);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл, "СтТовУчНалВсего") Тогда
		УстановитьЗначениеXDTO(Контент, "Total", Параметры.ВсегоОпл.СтТовУчНалВсего);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл, "КолНеттоВс") Тогда
		УстановитьЗначениеXDTO(Контент, "TotalNet", ПолучитьЗначениеXDTO(Параметры.ВсегоОпл, "КолНеттоВс"));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл.СумНалВсего, "СумНал") Тогда
		УстановитьЗначениеXDTO(Контент, "Vat", Параметры.ВсегоОпл.СумНалВсего.СумНал);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ВсегоОпл.СумНалВсего, "БезНДС") Тогда
		УстановитьЗначениеXDTO(Контент, "WithoutVat", "true");
	КонецЕсли;
	
	Для Каждого СведТов Из СписокXDTOИзЭлемента(Параметры, "СведТов") Цикл
		Заполнить_Item970(НовыйЭлементСпискаXDTO(Контент.Items), СведТов);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_Item970(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Product", ПолучитьЗначениеXDTO(Параметры, "НаимТов"));
	УстановитьЗначениеXDTO(Контент, "Unit", ПолучитьЗначениеXDTO(Параметры, "ОКЕИ_Тов"));
	УстановитьЗначениеXDTO(Контент, "UnitName", ПолучитьЗначениеXDTO(Параметры, "НаимЕдИзм"));
	УстановитьЗначениеXDTO(Контент, "Quantity", ПолучитьЗначениеXDTO(Параметры, "КолТов"));
	УстановитьЗначениеXDTO(Контент, "Price", ПолучитьЗначениеXDTO(Параметры, "ЦенаТов"));
	УстановитьЗначениеXDTO(Контент, "SubtotalWithVatExcluded", ПолучитьЗначениеXDTO(Параметры, "СтТовБезНДС"));
	УстановитьЗначениеXDTO(Контент, "Subtotal", ПолучитьЗначениеXDTO(Параметры, "СтТовУчНал"));
	УстановитьЗначениеXDTO(Контент, "TaxRate", TaxRate_Из_НалСт(Параметры.НалСт));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Акциз") Тогда
		УстановитьЗначениеXDTO(Контент, "Excise", Акциз_Из_XML(Параметры.Акциз));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СумНал, "СумНал") Тогда
		УстановитьЗначениеXDTO(Контент, "Vat", Параметры.СумНал.СумНал);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СумНал, "БезНДС") Тогда
		УстановитьЗначениеXDTO(Контент, "WithoutVat", "true");
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДопСведТов") Тогда
		
		ДопСведТов = Параметры.ДопСведТов;
		
		ItemMark = ItemMark(ПолучитьЗначениеXDTO(ДопСведТов, "ПрТовРаб"));
		
		УстановитьЗначениеXDTO(Контент, "ItemMark",	ItemMark);
		УстановитьЗначениеXDTO(Контент, "AdditionalProperty", ПолучитьЗначениеXDTO(ДопСведТов, "ДопПризн"));
		УстановитьЗначениеXDTO(Контент, "ItemToRelease", ПолучитьЗначениеXDTO(ДопСведТов, "НадлОтп"));
		УстановитьЗначениеXDTO(Контент, "ItemCharact", ПолучитьЗначениеXDTO(ДопСведТов, "ХарактерТов"));
		УстановитьЗначениеXDTO(Контент, "ItemKind", ПолучитьЗначениеXDTO(ДопСведТов, "СортТов"));
		УстановитьЗначениеXDTO(Контент, "ItemArticle", ПолучитьЗначениеXDTO(ДопСведТов, "АртикулТов"));
		УстановитьЗначениеXDTO(Контент, "ItemVendorCode", ПолучитьЗначениеXDTO(ДопСведТов, "КодТов"));
		УстановитьЗначениеXDTO(Контент, "CatalogCode", ПолучитьЗначениеXDTO(ДопСведТов, "КодКат"));
		УстановитьЗначениеXDTO(Контент, "ItemTypeCode", ПолучитьЗначениеXDTO(ДопСведТов, "КодВидТов"));
		
		КрНаимСтрПрЗнач = ПолучитьЗначениеXDTO(ДопСведТов, "КрНаимСтрПр");
		Если ЭтоСписокXDTO(КрНаимСтрПрЗнач) Тогда
			КрНаимСтрПрСтрока = ПолучитьЗначенияСпискаXDTOСтрокой(КрНаимСтрПрЗнач);
			УстановитьЗначениеXDTO(Контент, "Country", КрНаимСтрПрСтрока);
		Иначе
			УстановитьЗначениеXDTO(Контент, "Country", КрНаимСтрПрЗнач);
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Контент, "GTIN", ПолучитьЗначениеXDTO(ДопСведТов, "ГТИН"));
		УстановитьЗначениеXDTO(Контент, "Series", ПолучитьЗначениеXDTO(ДопСведТов, "СерияТов"));
		УстановитьЗначениеXDTO(Контент, "ProductTypeCode", ПолучитьЗначениеXDTO(ДопСведТов, "КодВидПр"));
		УстановитьЗначениеXDTO(Контент, "CodeRCPEA", ПолучитьЗначениеXDTO(ДопСведТов, "КодТовОКПД2"));
		УстановитьЗначениеXDTO(Контент, "AdditionalInfoTypeOperations", ПолучитьЗначениеXDTO(ДопСведТов, "ДопИнфПВидО"));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСведТов, "СумНалВосст") Тогда
			
			СуммаНалога = ПолучитьЗначениеXDTO(ДопСведТов.СумНалВосст, "СумНал");
			
			ЗначениеТэгаБезНДС = ПолучитьЗначениеXDTO(ДопСведТов.СумНалВосст, "БезНДС");
			ЭтоСуммаНалогаБезНДС = (ЗначениеТэгаБезНДС = СтрокаБезНДС());
			
			УстановитьЗначениеXDTO(Контент, "RecoveredWithoutVat", ЭтоСуммаНалогаБезНДС);
			УстановитьЗначениеXDTO(Контент, "RecoveredVat", СуммаНалога);
			
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСведТов, "НалУчАморт") Тогда
			Заполнить_TaxDeprication(Контент.TaxDeprication, ДопСведТов.НалУчАморт);
		КонецЕсли;
		
		Для Каждого СведПрослеж Из СписокXDTOИзЭлемента(ДопСведТов, "СведПрослеж") Цикл
			НовыйЭлементСписка = НовыйЭлементСпискаXDTO(Контент.ItemTracingInfos); 
			Заполнить_ItemTracingInfo970(НовыйЭлементСписка, СведПрослеж);
		КонецЦикла;
		
		Для Каждого НомСредИдентТов Из СписокXDTOИзЭлемента(ДопСведТов, "НомСредИдентТов") Цикл
			НовыйЭлементСписка = НовыйЭлементСпискаXDTO(Контент.ItemIdentificationNumbers); 
			Заполнить_ItemIdentificationNumber970(НовыйЭлементСписка, НомСредИдентТов);
		КонецЦикла;
		
		Для Каждого СопрДокТов Из СписокXDTOИзЭлемента(ДопСведТов, "СопрДокТов") Цикл
			НовыйЭлементСписка = НовыйЭлементСпискаXDTO(Контент.ItemAccompanyingDocuments);
			Заполнить_DocumentDetails(НовыйЭлементСписка, СопрДокТов);
		КонецЦикла;
		
		Для Каждого СвГосСист Из СписокXDTOИзЭлемента(ДопСведТов, "СвГосСист") Цикл
			НовыйЭлементСписка = НовыйЭлементСпискаXDTO(Контент.AdditionalInfoStateSystem);
			Заполнить_AdditionalInfoStateSystem(НовыйЭлементСписка, СвГосСист);
		КонецЦикла;
		
	КонецЕсли;
	
	Для Каждого СвДТ Из СписокXDTOИзЭлемента(Параметры, "СвДТ") Цикл
		НовыйЭлементСписка = НовыйЭлементСпискаXDTO(Контент.CustomDeclarations);
		Заполнить_CustomDeclaration(НовыйЭлементСписка, СвДТ);
	КонецЦикла;
	
	Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, Параметры, "ИнфПолФХЖ2");
	
КонецПроцедуры

Процедура Заполнить_ItemTracingInfo970(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "RegNumberUnit", Параметры.НомТовПрослеж);
	УстановитьЗначениеXDTO(Контент, "Unit", Параметры.ЕдИзмПрослеж);
	УстановитьЗначениеXDTO(Контент, "UnitName", Параметры.НаимЕдИзмПрослеж);
	УстановитьЗначениеXDTO(Контент, "Quantity", Параметры.КолВЕдПрослеж);
	УстановитьЗначениеXDTO(Контент, "SumWithoutVat", Параметры.СтТовБезНДСПрослеж);
	УстановитьЗначениеXDTO(Контент, "ItemAddInfo", ПолучитьЗначениеXDTO(Параметры, "ДопИнфПрослеж"));
	
КонецПроцедуры

Процедура Заполнить_ItemIdentificationNumber970(Контент, Параметры)
	
	КоллекцияКодов = СписокXDTOИзЭлемента(Параметры, "КИЗ");
	Для Каждого Значение Из КоллекцияКодов Цикл
		Контент.Units.Добавить(Значение);
	КонецЦикла;
	
	КоллекцияКодов = СписокXDTOИзЭлемента(Параметры, "НомУпак");
	Для Каждого Значение Из КоллекцияКодов Цикл
		Контент.PackageIds.Добавить(Значение);
	КонецЦикла;
	
	УстановитьЗначениеXDTO(Контент, "TransPackageId", ПолучитьЗначениеXDTO(Параметры, "ИдентТрансУпак"));
	УстановитьЗначениеXDTO(Контент, "MarkedItemsQuantity", ПолучитьЗначениеXDTO(Параметры, "КолВедМарк"));
	УстановитьЗначениеXDTO(Контент, "BatchCode", ПолучитьЗначениеXDTO(Параметры, "ПрПартМарк"));

КонецПроцедуры

Процедура Заполнить_AdditionalInfoStateSystem(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "SystemName", Параметры.НаимГосСист);
	УстановитьЗначениеXDTO(Контент, "Unit", ПолучитьЗначениеXDTO(Параметры, "УчетЕд"));
	УстановитьЗначениеXDTO(Контент, "OtherInfo", ПолучитьЗначениеXDTO(Параметры, "ИнаяИнф"));
	
	Для Каждого ИдНомУчетЕд Из СписокXDTOИзЭлемента(Параметры, "ФактСрокИсп") Цикл
		Контент.IDUnit.Добавить(ИдНомУчетЕд);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_TaxDeprication(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Code", Параметры.АмГруппа);
	УстановитьЗначениеXDTO(Контент, "OKOF", Параметры.КодОКОФ);
	УстановитьЗначениеXDTO(Контент, "UsefulPeriod", Параметры.СрПолИспОС);
	УстановитьЗначениеXDTO(Контент, "ActualPeriod", Параметры.ФактСрокИсп);
	
КонецПроцедуры

Процедура Заполнить_ProformaInvoiceTable(Контент, Параметры)
	
	ВсегоОпл = Параметры.ВсегоОпл;
	
	УстановитьЗначениеXDTO(Контент, "TotalWithVatExcluded"	, ВсегоОпл.СтТовБезНДСВсего);
	УстановитьЗначениеXDTO(Контент, "Total"					, ВсегоОпл.СтТовУчНалВсего);
	УстановитьЗначениеXDTO(Контент, "TotalVat"				, СуммаНДС_Из_XML(ВсегоОпл.СумНалВсего));
	УстановитьЗначениеXDTO(Контент, "GrandTotal"			, ПолучитьЗначениеXDTO(ВсегоОпл, "Итого"));
	
	Для Каждого СведТов Из СписокXDTOИзЭлемента(Параметры, "СведТов") Цикл
		Заполнить_ProformaInvoiceTableItem(НовыйЭлементСпискаXDTO(Контент.Items), СведТов);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_ProformaInvoiceTableItem(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Product"					, ПолучитьЗначениеXDTO(Параметры, "НаимТов"));
	УстановитьЗначениеXDTO(Контент, "Price"						, ПолучитьЗначениеXDTO(Параметры, "ЦенаТов"));
	УстановитьЗначениеXDTO(Контент, "Quantity"					, ПолучитьЗначениеXDTO(Параметры, "КолТов"));
	УстановитьЗначениеXDTO(Контент, "Subtotal"					, ПолучитьЗначениеXDTO(Параметры, "СтТовУчНал"));
	УстановитьЗначениеXDTO(Контент, "SubtotalWithVatExcluded"	, ПолучитьЗначениеXDTO(Параметры, "СтТовБезНДС"));
	УстановитьЗначениеXDTO(Контент, "Excise"					, Акциз_Из_XML(Параметры.Акциз));
	УстановитьЗначениеXDTO(Контент, "Vat"						, СуммаНДС_Из_XML(Параметры.СумНал));
	УстановитьЗначениеXDTO(Контент, "TaxRate" 					, TaxRate_Из_НалСт(Параметры.НалСт));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ОКЕИ_Тов") Тогда
		УстановитьЗначениеXDTO(Контент, "Unit", Параметры.ОКЕИ_Тов);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НаимЕдИзм") Тогда
		УстановитьЗначениеXDTO(Контент, "UnitName", Параметры.НаимЕдИзм);
	КонецЕсли;
	
	Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, Параметры, "ДопСведТов");
	
КонецПроцедуры

Процедура Заполнить_InvoiceTotals(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "TotalWithVatExcluded", ПолучитьЗначениеXDTO(Параметры, "СтТовБезНДСВсего"));
	УстановитьЗначениеXDTO(Контент, "Vat", СуммаНДС_Из_XML(Параметры.СумНалВсего));
	УстановитьЗначениеXDTO(Контент, "Total", Параметры.СтТовУчНалВсего);
	
КонецПроцедуры

Процедура Заполнить_InvoiceTotalsDiff(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "TotalWithVatExcluded", ПолучитьЗначениеXDTO(Параметры, "СтТовБезНДСВсего"));
	УстановитьЗначениеXDTO(Контент, "Vat", СуммаНДС_Из_XML(Параметры.СумНал));
	УстановитьЗначениеXDTO(Контент, "Total", Параметры.СтТовУчНалВсего);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СумНал, "БезНДС") Тогда
		УстановитьЗначениеXDTO(Контент, "WithoutVat", "true");
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_NonformilizedContent(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Date",						Неопределено);
	УстановитьЗначениеXDTO(Контент, "Number",					Неопределено);
	УстановитьЗначениеXDTO(Контент, "NeedRecipientSignature",	Неопределено);
	
КонецПроцедуры

Процедура Заполнить_Official(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ФИО") Тогда
		УстановитьЗначениеXDTO(Контент, "FirstName"	, Параметры.ФИО.Имя);
		УстановитьЗначениеXDTO(Контент, "Surname"	, Параметры.ФИО.Фамилия);
		УстановитьЗначениеXDTO(Контент, "Patronymic", ПолучитьЗначениеXDTO(Параметры.ФИО, "Отчество"));
	Иначе
		УстановитьЗначениеXDTO(Контент, "FirstName"	, "");
		УстановитьЗначениеXDTO(Контент, "Surname"	, "");
		УстановитьЗначениеXDTO(Контент, "Patronymic", "");
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Должность") Тогда
		Должность = Параметры.Должность;
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДолжнКем") Тогда
		Должность = Параметры.ДолжнКем;
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Должн") Тогда
		Должность = Параметры.Должн;
	Иначе
		Должность = "";
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "JobTitle", Должность);
	
КонецПроцедуры

Процедура Заполнить_OrganizationInfo(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Okpo", ПолучитьЗначениеXDTO(Параметры, "ОКПО"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИдСв") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвЮЛ") Тогда
			
			СвЮЛ = Параметры.ИдСв.СвЮЛ;
			УстановитьЗначениеXDTO(Контент, "IsSoleProprietor", Ложь);
			УстановитьЗначениеXDTO(Контент, "Name", СвЮл.НаимОрг);
			УстановитьЗначениеXDTO(Контент, "INN", ПолучитьЗначениеXDTO(СвЮЛ, "ИННЮЛ"));
			УстановитьЗначениеXDTO(Контент, "KPP", ПолучитьЗначениеXDTO(СвЮЛ, "КПП"));
			
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвФЛ") Тогда
			
			СвФЛ = Параметры.ИдСв.СвФЛ;
			УстановитьЗначениеXDTO(Контент, "IsSoleProprietor", Истина);
			УстановитьЗначениеXDTO(Контент, "INN", ПолучитьЗначениеXDTO(СвФЛ, "ИННФЛ"));
			УстановитьЗначениеXDTO(Контент, "Name", СвФЛ.ФИОИП.Фамилия + " " + СвФЛ.ФИОИП.Имя + " " + ПолучитьЗначениеXDTO(СвФЛ.ФИОИП, "Отчество"));
			
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвЮЛУч") Тогда
			
			// в InvoiceContent СвЮЛУч - нет, оставлю для совместимости с другими контентами
			УстановитьЗначениеXDTO(Контент, "IsSoleProprietor",	Ложь);
			УстановитьЗначениеXDTO(Контент, "Name", ПолучитьЗначениеXDTO(Параметры.ИдСв.СвЮЛУч, "НаимОрг"));
			УстановитьЗначениеXDTO(Контент, "Inn", ПолучитьЗначениеXDTO(Параметры.ИдСв.СвЮЛУч, "ИННЮЛ"));
			УстановитьЗначениеXDTO(Контент, "Kpp", ПолучитьЗначениеXDTO(Параметры.ИдСв.СвЮЛУч, "КПП"));
			
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвИП") Тогда
			
			СвИП = Параметры.ИдСв.СвИП;
			УстановитьЗначениеXDTO(Контент, "IsSoleProprietor", Истина);
			УстановитьЗначениеXDTO(Контент, "INN", ПолучитьЗначениеXDTO(СвИП, "ИННФЛ"));
			УстановитьЗначениеXDTO(Контент, "Name", СвИП.ФИО.Фамилия + " " + СвИП.ФИО.Имя + " " + ПолучитьЗначениеXDTO(СвИП.ФИО, "Отчество"));
			
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвЮлНеУч") Тогда
			
			СвЮлНеУч = Параметры.ИдСв.СвЮлНеУч;
			УстановитьЗначениеXDTO(Контент, "IsSoleProprietor",	Ложь);
			УстановитьЗначениеXDTO(Контент, "Name", СвЮлНеУч.НаимОрг);
			
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвИнНеУч") Тогда
			
			СвИнНеУч = Параметры.ИдСв.СвИнНеУч;
			УстановитьЗначениеXDTO(Контент, "IsSoleProprietor",	Истина);
			УстановитьЗначениеXDTO(Контент, "Name", СвИнНеУч.НаимОрг);
			
		КонецЕсли;
		
	КонецЕсли;	
	
	Заполнить_AddressInfo(Контент.Address, ПолучитьЗначениеXDTO(Параметры, "Адрес"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Контакт") Тогда
		УстановитьЗначениеXDTO(Контент, "Phone", ПолучитьЗначениеXDTO(Параметры.Контакт, "Тлф"));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "БанкРекв") Тогда

		БанкРекв = Параметры.БанкРекв;
		УстановитьЗначениеXDTO(Контент, "BankAccountNumber", ПолучитьЗначениеXDTO(БанкРекв, "НомерСчета"));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(БанкРекв, "СвБанк") Тогда
			СвБанк = БанкРекв.СвБанк;
			УстановитьЗначениеXDTO(Контент, "BankId", ПолучитьЗначениеXDTO(СвБанк, "БИК"));
			УстановитьЗначениеXDTO(Контент, "BankName", ПолучитьЗначениеXDTO(СвБанк, "НаимБанк"));
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_OriginalInvoice(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Date", ДатаXML_в_Дата1С(Параметры.ДатаСчФ));
	УстановитьЗначениеXDTO(Контент, "Number", Параметры.НомерСчФ);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИспрСчФ") Тогда
		УстановитьЗначениеXDTO(Контент, "InvoiceRevisionDate", ДатаXML_в_Дата1С(Параметры.ИспрСчФ.ДатаИспрСчФ));
		УстановитьЗначениеXDTO(Контент, "InvoiceRevisionNumber", Параметры.ИспрСчФ.НомИспрСчФ);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_OtherIssuer(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ПредОргПер") Тогда
		
		УстановитьЗначениеXDTO(Контент, "EmployeePosition",			Параметры.ПредОргПер.Должность);
		УстановитьЗначениеXDTO(Контент, "EmployeeInfo",				ПолучитьЗначениеXDTO(Параметры.ПредОргПер, "ИныеСвед"));
		УстановитьЗначениеXDTO(Контент, "OrganizationName",			Параметры.ПредОргПер.НаимОргПер);
		УстановитьЗначениеXDTO(Контент, "OrganizationBase",			ПолучитьЗначениеXDTO(Параметры.ПредОргПер, "ОснДоверОргПер"));
		УстановитьЗначениеXDTO(Контент, "EmployeeBase",				ПолучитьЗначениеXDTO(Параметры.ПредОргПер, "ОснПолнПредОформ"));
		УстановитьЗначениеXDTO(Контент, "Surname",					Параметры.ПредОргПер.ФИО.Фамилия);
		УстановитьЗначениеXDTO(Контент, "FirstName",				Параметры.ПредОргПер.ФИО.Имя);
		УстановитьЗначениеXDTO(Контент, "Patronymic",				ПолучитьЗначениеXDTO(Параметры.ПредОргПер.ФИО, "Отчество"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ПредОргПрин") Тогда
		
		УстановитьЗначениеXDTO(Контент, "EmployeePosition",	Параметры.ПредОргПрин.Должность);
		УстановитьЗначениеXDTO(Контент, "EmployeeInfo",		ПолучитьЗначениеXDTO(Параметры.ПредОргПрин, "ИныеСвед"));
		УстановитьЗначениеXDTO(Контент, "OrganizationName", Параметры.ПредОргПрин.НаимОргПрин);
		УстановитьЗначениеXDTO(Контент, "OrganizationBase",	ПолучитьЗначениеXDTO(Параметры.ПредОргПрин, "ОснДоверОргПрин"));
		УстановитьЗначениеXDTO(Контент, "EmployeeBase",		ПолучитьЗначениеXDTO(Параметры.ПредОргПрин, "ОснПолнПредПрин"));
		УстановитьЗначениеXDTO(Контент, "Surname",			Параметры.ПредОргПрин.ФИО.Фамилия);
		УстановитьЗначениеXDTO(Контент, "FirstName",		Параметры.ПредОргПрин.ФИО.Имя);
		УстановитьЗначениеXDTO(Контент, "Patronymic",		ПолучитьЗначениеXDTO(Параметры.ПредОргПрин.ФИО, "Отчество"));	
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ФЛПер") Тогда
		
		УстановитьЗначениеXDTO(Контент, "EmployeeInfo",				ПолучитьЗначениеXDTO(Параметры.ФЛПер, "ИныеСвед"));
		УстановитьЗначениеXDTO(Контент, "OrganizationBase",			ПолучитьЗначениеXDTO(Параметры.ФЛПер, "ОснДоверФЛ"));
		УстановитьЗначениеXDTO(Контент, "Surname",					Параметры.ФЛПер.ФИО.Фамилия);
		УстановитьЗначениеXDTO(Контент, "FirstName",				Параметры.ФЛПер.ФИО.Имя);
		УстановитьЗначениеXDTO(Контент, "Patronymic",				ПолучитьЗначениеXDTO(Параметры.ФЛПер.ФИО, "Отчество"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ФЛПрин") Тогда
		
		УстановитьЗначениеXDTO(Контент, "EmployeeInfo",				ПолучитьЗначениеXDTO(Параметры.ФЛПрин, "ИныеСвед"));
		УстановитьЗначениеXDTO(Контент, "OrganizationBase",			ПолучитьЗначениеXDTO(Параметры.ФЛПрин, "ОснДоверФЛ"));
		УстановитьЗначениеXDTO(Контент, "Surname",					Параметры.ФЛПрин.ФИО.Фамилия);
		УстановитьЗначениеXDTO(Контент, "FirstName",				Параметры.ФЛПрин.ФИО.Имя);
		УстановитьЗначениеXDTO(Контент, "Patronymic",				ПолучитьЗначениеXDTO(Параметры.ФЛПрин.ФИО, "Отчество"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ФЛ") Тогда
		
		УстановитьЗначениеXDTO(Контент, "EmployeeInfo",				ПолучитьЗначениеXDTO(Параметры.ФЛ, "ИныеСвед"));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ФЛ, "ОснДоверФЛПер") Тогда
			УстановитьЗначениеXDTO(Контент, "OrganizationBase",			ПолучитьЗначениеXDTO(Параметры.ФЛ, "ОснДоверФЛПер"));
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ФЛ, "ОснДоверФЛ") Тогда
			УстановитьЗначениеXDTO(Контент, "OrganizationBase",			ПолучитьЗначениеXDTO(Параметры.ФЛ, "ОснДоверФЛ"));
		КонецЕсли;
			
		УстановитьЗначениеXDTO(Контент, "Surname",					Параметры.ФЛ.ФИО.Фамилия);
		УстановитьЗначениеXDTO(Контент, "FirstName",				Параметры.ФЛ.ФИО.Имя);
		УстановитьЗначениеXDTO(Контент, "Patronymic",				ПолучитьЗначениеXDTO(Параметры.ФЛ.ФИО, "Отчество"));
	
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_OtherIssuer970(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ПредОргПер") Тогда
		
		ПредОргПер = Параметры.ПредОргПер;
		
		ИныеСведения = ПолучитьЗначениеXDTO(ПредОргПер, "ИныеСвед");
		ИНН = ПолучитьЗначениеXDTO(ПредОргПер, "ИННЮЛПер");
		
		УстановитьЗначениеXDTO(Контент, "EmployeePosition", ПредОргПер.Должность);
		УстановитьЗначениеXDTO(Контент, "EmployeeInfo", ИныеСведения);
		УстановитьЗначениеXDTO(Контент, "OrganizationName", ПредОргПер.НаимОргПер);
		УстановитьЗначениеXDTO(Контент, "Inn", ИНН);
		
		Заполнить_ФИО(Контент, ПредОргПер.ФИО);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПредОргПер, "ОснДоверОргПер") Тогда
			Заполнить_DocumentDetails(Контент.OrganizationBase, ПредОргПер.ОснДоверОргПер);	
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПредОргПер, "ОснПолнПредПер") Тогда
			Заполнить_DocumentDetails(Контент.EmployeeBase, ПредОргПер.ОснПолнПредПер);	
		КонецЕсли;
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ПредОргПрин") Тогда
		
		ПредОргПрин = Параметры.ПредОргПрин;

		ИныеСведения = ПолучитьЗначениеXDTO(ПредОргПрин, "ИныеСвед");
		ИНН = ПолучитьЗначениеXDTO(ПредОргПрин, "ИННОргПрин");
		
		УстановитьЗначениеXDTO(Контент, "EmployeePosition", ПредОргПрин.Должность);
		УстановитьЗначениеXDTO(Контент, "EmployeeInfo", ИныеСведения);
		УстановитьЗначениеXDTO(Контент, "OrganizationName", ПредОргПрин.НаимОргПрин);
		УстановитьЗначениеXDTO(Контент, "Inn", ИНН);
		
		Заполнить_ФИО(Контент, ПредОргПрин.ФИО);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПредОргПрин, "ОснДоверОргПрин") Тогда
			Заполнить_DocumentDetails(Контент.OrganizationBase, ПредОргПрин.ОснДоверОргПрин);	
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПредОргПрин, "ОснПолнПредПрин") Тогда
			Заполнить_DocumentDetails(Контент.EmployeeBase, ПредОргПрин.ОснПолнПредПрин);	
		КонецЕсли;

	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ФЛПер") Тогда
		
		ФЛПер = Параметры.ФЛПер;
		
		ИныеСведения = ПолучитьЗначениеXDTO(ФЛПер, "ИныеСвед");
		ИНН = ПолучитьЗначениеXDTO(ФЛПер, "ИННФЛПер");
		
		УстановитьЗначениеXDTO(Контент, "EmployeeInfo",	ИныеСведения);
		УстановитьЗначениеXDTO(Контент, "Inn", ИНН);
		
		Заполнить_ФИО(Контент, ФЛПер.ФИО);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ФЛПер, "ОснДоверФЛ") Тогда
			Заполнить_DocumentDetails(Контент.OrganizationBase, ФЛПер.ОснДоверФЛ);	
		КонецЕсли;
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ФЛПрин") Тогда
		
		ФЛПрин = Параметры.ФЛПрин;
		
		ИныеСведения = ПолучитьЗначениеXDTO(ФЛПрин, "ИныеСвед");
		ИНН = ПолучитьЗначениеXDTO(ФЛПрин, "ИННФЛПрин");
		
		УстановитьЗначениеXDTO(Контент, "EmployeeInfo",	ИныеСведения);
		УстановитьЗначениеXDTO(Контент, "Inn", ИНН);
		
		Заполнить_ФИО(Контент, ФЛПрин.ФИО);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ФЛПрин, "ОснДоверФЛ") Тогда
			Заполнить_DocumentDetails(Контент.OrganizationBase, ФЛПрин.ОснДоверФЛ);	
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_PaymentDocument(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДатаПРД") Тогда
		УстановитьЗначениеXDTO(Контент, "Date", ДатаXML_в_Дата1С(Параметры.ДатаПРД));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НомерПРД") Тогда
		УстановитьЗначениеXDTO(Контент, "Number", Параметры.НомерПРД);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СуммаПРД") Тогда
		УстановитьЗначениеXDTO(Контент, "Total", Параметры.СуммаПРД);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_Shipper(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ОнЖе") Тогда
		УстановитьЗначениеXDTO(Контент, "SameAsSeller",				Истина);
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ГрузОтпр") Тогда
		УстановитьЗначениеXDTO(Контент, "SameAsSeller",				Ложь);
		Заполнить_ExtendedOrganizationInfo		(Контент.OrganizationInfo, Параметры.ГрузОтпр);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_Shipper970(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ОнЖе") Тогда
		УстановитьЗначениеXDTO(Контент, "SameAsSeller", Истина);
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ГрузОтпр") Тогда
		УстановитьЗначениеXDTO(Контент, "SameAsSeller", Ложь);
		Заполнить_ExtendedOrganizationInfo970(Контент.OrganizationInfo, Параметры.ГрузОтпр);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_ShipperOrConsigneeInfo(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НаимГОП") Тогда
		
		// формат с/ф 5.02
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.НаимГОП, "НаимОрг") Тогда
			УстановитьЗначениеXDTO(Контент, "IsSoleProprietor", Ложь);
			УстановитьЗначениеXDTO(Контент, "Name", Параметры.НаимГОП.НаимОрг);
		Иначе
			УстановитьЗначениеXDTO(Контент, "IsSoleProprietor", Истина);
			УстановитьЗначениеXDTO(Контент, "Name", Параметры.НаимГОП.ФИОИП.Фамилия + " " + Параметры.НаимГОП.ФИОИП.Имя + " " + ПолучитьЗначениеXDTO(Параметры.НаимГОП.ФИОИП, "Отчество"));
		КонецЕсли;
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИдСв") Тогда
		
		// формат с/ф 5.01
		ИдСв = Параметры.ИдСв;
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ИдСв, "СвИП") Тогда
			СвИП = ИдСв.СвИП;
			УстановитьЗначениеXDTO(Контент, "IsSoleProprietor", Истина);
			УстановитьЗначениеXDTO(Контент, "Name", СвИП.ФИО.Фамилия + " " + СвИП.ФИО.Имя + " " + ПолучитьЗначениеXDTO(СвИП.ФИО, "Отчество"));
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ИдСв, "СвЮЛУч") Тогда
			СвЮЛУч = ИдСв.СвЮЛУч;
			УстановитьЗначениеXDTO(Контент, "IsSoleProprietor", Ложь);
			УстановитьЗначениеXDTO(Контент, "Name", СвЮЛУч.НаимОрг);
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ИдСв, "СвЮлНеУч") Тогда
			СвЮлНеУч = ИдСв.СвЮлНеУч;
			УстановитьЗначениеXDTO(Контент, "IsSoleProprietor", Ложь);
			УстановитьЗначениеXDTO(Контент, "Name", СвЮлНеУч.НаимОрг);
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ИдСв, "СвИнНеУч") Тогда
			СвИнНеУч = ИдСв.СвИнНеУч;
			УстановитьЗначениеXDTO(Контент, "IsSoleProprietor", Истина);
			УстановитьЗначениеXDTO(Контент, "Name", СвИнНеУч.НаимОрг);
		КонецЕсли;
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "SameAsSellerOrBuyer", Ложь);
	
	Заполнить_AddressInfo(Контент.Address, Параметры.Адрес);
	
КонецПроцедуры

Процедура Заполнить_Signer(Контент, Параметры, ИмяСвойства)
	
	Для Каждого Подписант Из СписокXDTOИзЭлемента(Параметры, ИмяСвойства) Цикл
		
		Signer = XML_ПолучитьПустойОбъектXDTO("Signer", "Proto");
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Подписант, "ЮЛ") Тогда
			
			УстановитьЗначениеXDTO(Signer, "IsSoleProprietor"	, Ложь);
			УстановитьЗначениеXDTO(Signer, "Inn"				, Подписант.ЮЛ.ИннЮЛ);
			
			Заполнить_Official(Signer, Подписант.ЮЛ);
			
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Подписант, "ФЛ") Тогда
			
			УстановитьЗначениеXDTO(Signer, "IsSoleProprietor"	, Истина);
			УстановитьЗначениеXDTO(Signer, "Inn"				, Подписант.ФЛ.ИннФЛ);
			УстановитьЗначениеXDTO(Signer, "SoleProprietorRegistrationCertificate", ПолучитьЗначениеXDTO(Подписант.ФЛ, "ГосРегИПВыдДов"));
			Заполнить_Official(Signer, Подписант.ФЛ);
			
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Подписант, "ИП") Тогда
			
			УстановитьЗначениеXDTO(Signer, "IsSoleProprietor"	, Истина);
			УстановитьЗначениеXDTO(Signer, "Inn"				, Подписант.ИП.ИННФЛ);
			УстановитьЗначениеXDTO(Signer, "SoleProprietorRegistrationCertificate", ПолучитьЗначениеXDTO(Подписант.ИП, "СвГосРегИП"));
			
			Заполнить_Official(Signer, Подписант.ИП);
			
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Контент, "Signer", Signer);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_Torg12BuyerContent(Контент, Параметры)
	
	Документ = Параметры.Документ;
	СвТНП	 = Документ.СвТНП;
	
	Заполнить_Signer(Контент, Документ, "Подписант");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвТНП, "ПолучилГруз") Тогда
		
		ПолучилГруз = СвТНП.ПолучилГруз;
		
		УстановитьЗначениеXDTO(Контент, "ShipmentReceiptDate", ДатаXML_в_Дата1С(ПолучилГруз.ДатаПолуч));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПолучилГруз, "Доверенность") Тогда
			Заполнить_Attorney(Контент.Attorney, ПолучилГруз.Доверенность);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПолучилГруз, "ГрузПолучил") Тогда
			Заполнить_Official(Контент.Receiver, ПолучилГруз.ГрузПолучил);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПолучилГруз, "ГрузПринял") Тогда
			Заполнить_Official(Контент.Accepter, ПолучилГруз.ГрузПринял);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвТНП, "ИнфПол") Тогда
		УстановитьЗначениеXDTO(Контент, "AdditionalInfo", ПолучитьЗначениеXDTO(СвТНП.ИнфПол, "ТекстИнф"));
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_Torg12Commons(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "TotalParcelsQuantity",        ПолучитьЗначениеXDTO(Параметры, "ВсМест"));
	УстановитьЗначениеXDTO(Контент, "TotalGrossQuantity",          ПолучитьЗначениеXDTO(Параметры, "Брутто"));
	УстановитьЗначениеXDTO(Контент, "TotalQuantity",               ПолучитьЗначениеXDTO(Параметры, "Нетто"));
	УстановитьЗначениеXDTO(Контент, "TotalSum",                    Неопределено);
	УстановитьЗначениеXDTO(Контент, "TotalParcelsQuantityInWords", ПолучитьЗначениеXDTO(Параметры, "ВсМестПр"));
	УстановитьЗначениеXDTO(Контент, "TotalGrossQuantityInWords",   ПолучитьЗначениеXDTO(Параметры, "БруттоПр"));
	УстановитьЗначениеXDTO(Контент, "TotalQuantityInWords",        ПолучитьЗначениеXDTO(Параметры, "НеттоПр"));
	УстановитьЗначениеXDTO(Контент, "TotalSumInWords",             Неопределено);
	
КонецПроцедуры

Процедура Заполнить_Torg12Item(Контент, Параметры)
	
	//ExternalCode (строка, чтение/запись) - внешний идентификатор товара. Может быть установлен только покупателем товара.
	
	УстановитьЗначениеXDTO(Контент, "Product",              ПолучитьЗначениеXDTO(Параметры, "НаимТов"));
	УстановитьЗначениеXDTO(Контент, "ProductCode",          ПолучитьЗначениеXDTO(Параметры, "КодТов"));
	УстановитьЗначениеXDTO(Контент, "Feature",              ПолучитьЗначениеXDTO(Параметры, "ХарактерТов"));
	УстановитьЗначениеXDTO(Контент, "Sort",                 ПолучитьЗначениеXDTO(Параметры, "СортТов"));
	УстановитьЗначениеXDTO(Контент, "Article",              ПолучитьЗначениеXDTO(Параметры, "АртикулТов"));
	УстановитьЗначениеXDTO(Контент, "UnitCode",             ПолучитьЗначениеXDTO(Параметры, "ОКЕИ_Тов"));
	УстановитьЗначениеXDTO(Контент, "UnitName",             Параметры.НаимЕдИзм);
	УстановитьЗначениеXDTO(Контент, "ParcelsQuantity",      ПолучитьЗначениеXDTO(Параметры, "КолМест"));
	УстановитьЗначениеXDTO(Контент, "ParcelType",           ПолучитьЗначениеXDTO(Параметры, "ВидУпак"));
	УстановитьЗначениеXDTO(Контент, "ParcelCapacity",       ПолучитьЗначениеXDTO(Параметры, "Место"));
	УстановитьЗначениеXDTO(Контент, "GrossQuantity",        ПолучитьЗначениеXDTO(Параметры, "Брутто"));
	УстановитьЗначениеXDTO(Контент, "Quantity",             ПолучитьЗначениеXDTO(Параметры, "Нетто"));
	УстановитьЗначениеXDTO(Контент, "Price",                ПолучитьЗначениеXDTO(Параметры, "Цена"));
	УстановитьЗначениеXDTO(Контент, "TotalWithVatExcluded", ПолучитьЗначениеXDTO(Параметры, "СумБезНДС"));
	УстановитьЗначениеXDTO(Контент, "TaxRate",              ПолучитьЗначениеXDTO(Параметры, "СтавкаНДС"));
	УстановитьЗначениеXDTO(Контент, "Vat",                  ПолучитьЗначениеXDTO(Параметры, "СумНДС"));
	УстановитьЗначениеXDTO(Контент, "Total",                ПолучитьЗначениеXDTO(Параметры, "СумУчНДС"));
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo",       ПолучитьЗначениеXDTO(Параметры, "ИнфПолСтр"));
	УстановитьЗначениеXDTO(Контент, "ExternalCode",         Неопределено);
	
КонецПроцедуры

Процедура Заполнить_Torg12SellerContent(Контент, Параметры)
	
	Документ = Параметры.Документ;
	СвТНО	 = Документ.СвТНО;
	ТН		 = СвТНО.ТН;
	
	Контент.Seller.FnsParticipantId	 = Параметры.СвУчДокОбор.ИдОтпр;
	Контент.Buyer.FnsParticipantId	 = Параметры.СвУчДокОбор.ИдПок;
	
	УстановитьЗначениеXDTO(Контент, "Date"	, ДатаXML_в_Дата1С(ТН.ДатаТН));
	УстановитьЗначениеXDTO(Контент, "Number", ПолучитьЗначениеXDTO(ТН, "НомТН"));
	
	Заполнить_OrganizationInfo(Контент.Seller	, СвТНО.Поставщик);
	Заполнить_OrganizationInfo(Контент.Buyer	, СвТНО.Плательщик);
	
	Заполнить_Signer(Контент, Документ, "Подписант");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвТНО, "ТранНакл") Тогда
		УстановитьЗначениеXDTO(Контент, "WaybillDate", ДатаXML_в_Дата1С(ПолучитьЗначениеXDTO(СвТНО.ТранНакл, "ДатаТранНакл")));
		УстановитьЗначениеXDTO(Контент, "WaybillNumber", ПолучитьЗначениеXDTO(СвТНО.ТранНакл, "НомТранНакл"));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "OperationCode", ПолучитьЗначениеXDTO(СвТНО, "ВидОперации"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвТНО, "Основание") Тогда
		УстановитьЗначениеXDTO(Контент, "GroundName", ПолучитьЗначениеXDTO(СвТНО.Основание, "НаимОсн"));
		УстановитьЗначениеXDTO(Контент, "GroundDate", ДатаXML_в_Дата1С(ПолучитьЗначениеXDTO(СвТНО.Основание, "ДатаОсн")));
		УстановитьЗначениеXDTO(Контент, "GroundNumber", ПолучитьЗначениеXDTO(СвТНО.Основание, "НомОсн"));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвТНО, "ГрузОт") Тогда 
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвТНО.ГрузОт, "ГрузОтпр") Тогда
			Заполнить_OrganizationInfo(Контент.Shipper,	СвТНО.ГрузОт.ГрузОтпр);
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Контент, "ShipperDepartment", ПолучитьЗначениеXDTO(СвТНО.ГрузОт, "СтруктПодр"));
		УстановитьЗначениеXDTO(Контент, "ShipperOkdp", ПолучитьЗначениеXDTO(СвТНО.ГрузОт, "ОКДП"));
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвТНО, "ГрузПолуч") Тогда
		Заполнить_OrganizationInfo(Контент.Consignee, СвТНО.ГрузПолуч);
	КонецЕсли;
	
	Для Каждого СвТов Из СписокXDTOИзЭлемента(ТН.Таблица, "СвТов") Цикл
		Заполнить_Torg12Item(НовыйЭлементСпискаXDTO(Контент.Items), СвТов);
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ТН.Таблица, "ВсегоНакл") Тогда
		Заполнить_Torg12Totals(Контент.Totals, ТН.Таблица.ВсегоНакл);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ТН, "ТНОбщ") Тогда
		Заполнить_Torg12Commons(Контент.Commons, ТН.ТНОбщ);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвТНО, "ОтпускГруз") Тогда
		
		ОтпускГруз = СвТНО.ОтпускГруз;
		
		УстановитьЗначениеXDTO(Контент, "AttachmentSheetsQuantity", ПолучитьЗначениеXDTO(ОтпускГруз, "КолПрил"));
		УстановитьЗначениеXDTO(Контент, "SupplyDate",               ДатаXML_в_Дата1С(ПолучитьЗначениеXDTO(ОтпускГруз, "ДатаОтпуск")));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОтпускГруз, "ОтпускРазреш") Тогда
			Заполнить_Official(Контент.SupplyAllowedBy, ОтпускГруз.ОтпускРазреш);
		КонецЕсли;		
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОтпускГруз, "Бухгалтер") Тогда
			Заполнить_Official(Контент.ChiefAccountant, ОтпускГруз.Бухгалтер);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОтпускГруз, "ОтпускПроизв") Тогда
			Заполнить_Official(Контент.SupplyPerformedBy, ОтпускГруз.ОтпускПроизв);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвТНО, "ИнфПол") Тогда
		УстановитьЗначениеXDTO(Контент, "AdditionalInfo", ПолучитьЗначениеXDTO(СвТНО.ИнфПол, "ТекстИнф"));
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_Torg12Totals(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "ParcelsQuantity",      ПолучитьЗначениеXDTO(Параметры, "КолМестВс"));
	УстановитьЗначениеXDTO(Контент, "GrossQuantity",        ПолучитьЗначениеXDTO(Параметры, "БруттоВс"));
	УстановитьЗначениеXDTO(Контент, "Quantity",             ПолучитьЗначениеXDTO(Параметры, "НеттоВс"));
	УстановитьЗначениеXDTO(Контент, "TotalWithVatExcluded", ПолучитьЗначениеXDTO(Параметры, "СумБезНДСВс"));
	УстановитьЗначениеXDTO(Контент, "Vat",                  ПолучитьЗначениеXDTO(Параметры, "СумНДСВс"));
	УстановитьЗначениеXDTO(Контент, "Total",                ПолучитьЗначениеXDTO(Параметры, "СумУчНДСВс"));
	
КонецПроцедуры

Процедура Заполнить_TransferBase(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "DocumentName",		Параметры.НаимОсн);
	УстановитьЗначениеXDTO(Контент, "DocumentNumber",	ПолучитьЗначениеXDTO(Параметры, "НомОсн"));
	УстановитьЗначениеXDTO(Контент, "DocumentInfo",		ПолучитьЗначениеXDTO(Параметры, "ДопСвОсн"));
	
	Если Параметры.НаимОсн <> "Отсутствует" Тогда
		УстановитьЗначениеXDTO(Контент, "DocumentDate",	ДатаXML_в_Дата1С(ПолучитьЗначениеXDTO(Параметры, "ДатаОсн", Дата(1, 1, 1))));
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_TransferBase820(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "DocumentName",		Параметры.НаимОсн);
	УстановитьЗначениеXDTO(Контент, "DocumentNumber",	ПолучитьЗначениеXDTO(Параметры, "НомОсн"));
	УстановитьЗначениеXDTO(Контент, "DocumentInfo",		ПолучитьЗначениеXDTO(Параметры, "ДопСвОсн"));
	УстановитьЗначениеXDTO(Контент, "BaseDocumentId",	ПолучитьЗначениеXDTO(Параметры, "ИдентОсн"));
	
	Если Параметры.НаимОсн <> "Без документа-основания" Тогда
		УстановитьЗначениеXDTO(Контент, "DocumentDate",	ДатаXML_в_Дата1С(ПолучитьЗначениеXDTO(Параметры, "ДатаОсн", Дата(1, 1, 1))));
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_TransferInfo(Контент, Параметры)
	
	СвПер = Параметры.СвПер;
	
	УстановитьЗначениеXDTO(Контент, "OperationInfo", СвПер.СодОпер);
	УстановитьЗначениеXDTO(Контент, "OperationType", ПолучитьЗначениеXDTO(СвПер, "ВидОпер"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "ДатаПер") Тогда
		УстановитьЗначениеXDTO(Контент, "TransferDate", ДатаXML_в_Дата1С(СвПер.ДатаПер));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "ТранГруз") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.ТранГруз, "СвТранГруз") Тогда
			УстановитьЗначениеXDTO(Контент, "TransferTextInfo", СвПер.ТранГруз.СвТранГруз);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.ТранГруз, "Перевозчик") Тогда
			Заполнить_ExtendedOrganizationInfo(Контент.Carrier, СвПер.ТранГруз.Перевозчик);
		КонецЕсли;
		
		Для Каждого ТранНакл Из СписокXDTOИзЭлемента(СвПер.ТранГруз, "ТранНакл") Цикл
			Заполнить_Waybill(НовыйЭлементСпискаXDTO(Контент.Waybills), ТранНакл);
		КонецЦикла;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "СвПерВещи") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.СвПерВещи, "ДатаПерВещ") Тогда
			УстановитьЗначениеXDTO(Контент, "CreatedThingTransferDate", ДатаXML_в_Дата1С(СвПер.СвПерВещи.ДатаПерВещ));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.СвПерВещи, "СвПерВещ") Тогда
			УстановитьЗначениеXDTO(Контент, "CreatedThingInfo", СвПер.СвПерВещи.СвПерВещ);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "СвЛицПер") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.СвЛицПер, "РабОргПрод") Тогда
			Заполнить_Employee(Контент.Employee, СвПер.СвЛицПер.РабОргПрод);
		Иначе 
			Заполнить_OtherIssuer(Контент.OtherIssuer, СвПер.СвЛицПер.ИнЛицо);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИнфПолФХЖ3") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfoId, Параметры.ИнфПолФХЖ3);
	КонецЕсли;
	
	Для Каждого ОснПер Из СписокXDTOИзЭлемента(СвПер, "ОснПер") Цикл
		Заполнить_TransferBase(НовыйЭлементСпискаXDTO(Контент.TransferBases), ОснПер);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_TransferInfo820(Контент, Параметры)
	
	СвПер = Параметры.СвПер;
	
	УстановитьЗначениеXDTO(Контент, "OperationInfo", СвПер.СодОпер);
	УстановитьЗначениеXDTO(Контент, "OperationType", ПолучитьЗначениеXDTO(СвПер, "ВидОпер"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "ДатаПер") Тогда
		УстановитьЗначениеXDTO(Контент, "TransferDate", ДатаXML_в_Дата1С(СвПер.ДатаПер));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "ДатаНач") Тогда
		УстановитьЗначениеXDTO(Контент, "TransferStartDate", ДатаXML_в_Дата1С(СвПер.ДатаНач));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "ДатаОкон") Тогда
		УстановитьЗначениеXDTO(Контент, "TransferEndDate", ДатаXML_в_Дата1С(СвПер.ДатаОкон));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "ТранГруз") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.ТранГруз, "СвТранГруз") Тогда
			УстановитьЗначениеXDTO(Контент, "TransferTextInfo", СвПер.ТранГруз.СвТранГруз);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.ТранГруз, "Перевозчик") Тогда
			Заполнить_ExtendedOrganizationInfo(Контент.Carrier, СвПер.ТранГруз.Перевозчик);
		КонецЕсли;
		
		Для Каждого ТранНакл Из СписокXDTOИзЭлемента(СвПер.ТранГруз, "ТранНакл") Цикл
			Заполнить_Waybill(НовыйЭлементСпискаXDTO(Контент.Waybills), ТранНакл);
		КонецЦикла;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "СвПерВещи") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.СвПерВещи, "ДатаПерВещ") Тогда
			УстановитьЗначениеXDTO(Контент, "CreatedThingTransferDate", ДатаXML_в_Дата1С(СвПер.СвПерВещи.ДатаПерВещ));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.СвПерВещи, "СвПерВещ") Тогда
			УстановитьЗначениеXDTO(Контент, "CreatedThingInfo", СвПер.СвПерВещи.СвПерВещ);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "СвЛицПер") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.СвЛицПер, "РабОргПрод") Тогда
			Заполнить_Employee(Контент.Employee, СвПер.СвЛицПер.РабОргПрод);
		Иначе 
			Заполнить_OtherIssuer(Контент.OtherIssuer, СвПер.СвЛицПер.ИнЛицо);
		КонецЕсли;
		
	КонецЕсли;
	
	Для Каждого ОснПер Из СписокXDTOИзЭлемента(СвПер, "ОснПер") Цикл
		Заполнить_TransferBase820(НовыйЭлементСпискаXDTO(Контент.TransferBases), ОснПер);
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИнфПолФХЖ3") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfoId, Параметры.ИнфПолФХЖ3);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_TransferInfo970(Контент, Параметры)
	
	СвПер = Параметры.СвПер;
	
	УстановитьЗначениеXDTO(Контент, "OperationInfo", СвПер.СодОпер);
	
	ВидОпер = ПолучитьЗначениеXDTO(СвПер, "ВидОпер");
	УстановитьЗначениеXDTO(Контент, "OperationType", ВидОпер);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "ДатаПер") Тогда
		УстановитьЗначениеXDTO(Контент, "TransferDate", ДатаXML_в_Дата1С(СвПер.ДатаПер));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "ДатаНачПер") Тогда
		УстановитьЗначениеXDTO(Контент, "TransferStartDate", ДатаXML_в_Дата1С(СвПер.ДатаНачПер));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "ДатаОконПер") Тогда
		УстановитьЗначениеXDTO(Контент, "TransferEndDate", ДатаXML_в_Дата1С(СвПер.ДатаОконПер));
	КонецЕсли;
			
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "БезДокОснПер") Тогда
		WithoutTransferBase = (СвПер.БезДокОснПер = "1");
		УстановитьЗначениеXDTO(Контент, "WithoutTransferBase", WithoutTransferBase);
	КонецЕсли;

	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "Тран") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.Тран, "СвТран") Тогда
			УстановитьЗначениеXDTO(Контент, "TransferTextInfo", СвПер.Тран.СвТран);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.Тран, "Инкотермс") Тогда
			УстановитьЗначениеXDTO(Контент, "Incoterms", СвПер.Тран.Инкотермс);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.Тран, "ВерИнкотермс") Тогда
			УстановитьЗначениеXDTO(Контент, "IncotermsVersion", СвПер.Тран.ВерИнкотермс);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "СвПерВещи") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.СвПерВещи, "ДатаПерВещ") Тогда
			УстановитьЗначениеXDTO(Контент, "CreatedThingTransferDate", ДатаXML_в_Дата1С(СвПер.СвПерВещи.ДатаПерВещ));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.СвПерВещи, "СвПерВещ") Тогда
			УстановитьЗначениеXDTO(Контент, "CreatedThingInfo", СвПер.СвПерВещи.СвПерВещ);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.СвПерВещи, "ДокПерВещ") Тогда
			Заполнить_DocumentDetails(Контент.CreatedThingTransferDoc, СвПер.СвПерВещи.ДокПерВещ);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер, "СвЛицПер") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПер.СвЛицПер, "РабОргПрод") Тогда
			Заполнить_Employee(Контент.Employee, СвПер.СвЛицПер.РабОргПрод);
		Иначе 
			Заполнить_OtherIssuer970(Контент.OtherIssuer, СвПер.СвЛицПер.ИнЛицо);
		КонецЕсли;
		
	КонецЕсли;
	
	Для Каждого ОснПер Из СписокXDTOИзЭлемента(СвПер, "ОснПер") Цикл
		TransferBases = НовыйЭлементСпискаXDTO(Контент.TransferBases); 
		Заполнить_DocumentDetails(TransferBases, ОснПер);
	КонецЦикла;
		
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИнфПолФХЖ3") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfoId, Параметры.ИнфПолФХЖ3);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_UtdBuyerContent(Контент, Параметры)
	
	ИнфПок = Параметры.ИнфПок;
	СодФХЖ4 = ИнфПок.СодФХЖ4;
	
	УстановитьЗначениеXDTO(Контент, "Creator",			ИнфПок.НаимЭконСубСост);
	УстановитьЗначениеXDTO(Контент, "CreatorBase",		ПолучитьЗначениеXDTO(ИнфПок, "ОснДоверОргСост"));
	УстановитьЗначениеXDTO(Контент, "OperationCode",	ПолучитьЗначениеXDTO(СодФХЖ4, "ВидОперации"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ4, "СвПрин") Тогда
		
		// УПД
		СвПрин = СодФХЖ4.СвПрин;
		
		УстановитьЗначениеXDTO(Контент, "OperationContent",    СвПрин.СодОпер);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПрин, "ДатаПрин") Тогда
			УстановитьЗначениеXDTO(Контент, "AcceptanceDate", ДатаXML_в_Дата1С(СвПрин.ДатаПрин));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПрин, "СвЛицПрин") Тогда
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПрин.СвЛицПрин, "РабОргПок") Тогда
				Заполнить_Employee(Контент.Employee, СвПрин.СвЛицПрин.РабОргПок);
			Иначе
				Заполнить_OtherIssuer(Контент.OtherIssuer, СвПрин.СвЛицПрин.ИнЛицо);
			КонецЕсли;
		КонецЕсли;
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ4, "СвСоглас") Тогда
		
		// УКД
		СвСоглас = СодФХЖ4.СвСоглас;
		УстановитьЗначениеXDTO(Контент, "OperationContent",    СвСоглас.СодОпер);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСоглас, "ДатаСоглас") Тогда
			УстановитьЗначениеXDTO(Контент, "AcceptanceDate", ДатаXML_в_Дата1С(СвСоглас.ДатаСоглас));
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ4, "ИнфПолФХЖ4") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfoId, СодФХЖ4.ИнфПолФХЖ4);
	КонецЕсли;
	
	Заполнить_ExtendedSigner(Контент.Signers, ИнфПок, "Подписант");
	
КонецПроцедуры

Процедура Заполнить_UcdBuyerContent(Контент, Параметры)
	
	// Контент ответного титула УКД аналогичен УПД
	Заполнить_UtdBuyerContent(Контент, Параметры);
	
КонецПроцедуры

Процедура Заполнить_UtdSellerContent(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Function",					Параметры.Документ.Функция);
	ФункцииУПД = Перечисление_ФункцииУПД();
	Если Не Параметры.Документ.Функция = ФункцииУПД.СЧФ Тогда
		УстановитьЗначениеXDTO(Контент, "Name",					Параметры.Документ.НаимДокОпр);
	КонецЕсли;
	УстановитьЗначениеXDTO(Контент, "Currency",					Параметры.Документ.СвСчФакт.КодОКВ);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "ДатаСчФ") Тогда
		УстановитьЗначениеXDTO(Контент, "Date", ДатаXML_в_Дата1С(Параметры.Документ.СвСчФакт.ДатаСчФ));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "НомерСчФ") Тогда
		УстановитьЗначениеXDTO(Контент, "Number", Параметры.Документ.СвСчФакт.НомерСчФ);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "ИспрСчф") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт.ИспрСчФ, "ДатаИспрСчф") Тогда
			УстановитьЗначениеXDTO(Контент, "RevisionDate", ДатаXML_в_Дата1С(Параметры.Документ.СвСчФакт.ИспрСчФ.ДатаИспрСчф));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт.ИспрСчФ, "НомИспрСчФ") Тогда
			УстановитьЗначениеXDTO(Контент, "RevisionNumber", Параметры.Документ.СвСчФакт.ИспрСчФ.НомИспрСчФ);
		КонецЕсли;
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Creator",					Параметры.Документ.НаимЭконСубСост);
	УстановитьЗначениеXDTO(Контент, "CreatorBase",				ПолучитьЗначениеXDTO(Параметры.Документ, "ОснДоверОргСост"));
	
	Заполнить_ExtendedOrganizationInfo(Контент.Seller, Параметры.Документ.СвСчФакт.СвПрод);
	Контент.Seller.FnsParticipantId = Параметры.СвУчДокОбор.ИдОтпр;
	
	Заполнить_ExtendedOrganizationInfo(Контент.Buyer, Параметры.Документ.СвСчФакт.СвПокуп);
	Контент.Buyer.FnsParticipantId = Параметры.СвУчДокОбор.ИдПол;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "ГрузОт") Тогда
		Заполнить_Shipper(Контент.Shipper, Параметры.Документ.СвСчФакт.ГрузОт);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "ГрузПолуч") Тогда
		Заполнить_ExtendedOrganizationInfo(Контент.Consignee, Параметры.Документ.СвСчФакт.ГрузПолуч);
	КонецЕсли; 
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ, "ТаблСчФакт") Тогда
		Заполнить_InvoiceTable(Контент.InvoiceTable, Параметры.Документ.ТаблСчФакт);
	КонецЕсли;
		
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "ИнфПолФХЖ1") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfoId, Параметры.Документ.СвСчФакт.ИнфПолФХЖ1);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "ДопСвФХЖ1") Тогда
		УстановитьЗначениеXDTO(Контент, "CurrencyRate",				ПолучитьЗначениеXDTO(Параметры.Документ.СвСчФакт.ДопСвФХЖ1, "КурсВал")); 
		УстановитьЗначениеXDTO(Контент, "GovernmentContractInfo",	ПолучитьЗначениеXDTO(Параметры.Документ.СвСчФакт.ДопСвФХЖ1, "ИдГосКон"));
	КонецЕсли;
		
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ, "СвПродПер") Тогда
		Заполнить_TransferInfo(Контент.TransferInfo, Параметры.Документ.СвПродПер);
	КонецЕсли;
	
	Для Каждого СвПРД Из СписокXDTOИзЭлемента(Параметры.Документ.СвСчФакт, "СвПРД") Цикл
		Заполнить_PaymentDocument(НовыйЭлементСпискаXDTO(Контент.PaymentDocuments), СвПРД);
	КонецЦикла;
	
	Заполнить_ExtendedSigner(Контент.Signers, Параметры.Документ, "Подписант");
	
КонецПроцедуры

Процедура Заполнить_Utd820BuyerContent(Контент, Параметры)
	
	ИнфПок = Параметры.ИнфПок;
	СодФХЖ4 = ИнфПок.СодФХЖ4;
	
	УстановитьЗначениеXDTO(Контент, "DocumentCreator",		ИнфПок.НаимЭконСубСост);
	УстановитьЗначениеXDTO(Контент, "DocumentCreatorBase",	ПолучитьЗначениеXDTO(ИнфПок, "ОснДоверОргСост"));
	УстановитьЗначениеXDTO(Контент, "OperationCode",		ПолучитьЗначениеXDTO(СодФХЖ4, "ВидОперации"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ4, "СвПрин") Тогда
		
		СвПрин = СодФХЖ4.СвПрин;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПрин, "СодОпер") Тогда
			УстановитьЗначениеXDTO(Контент, "OperationContent",	СвПрин.СодОпер);
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПрин, "КодСодОпер") Тогда
			Заполнить_ContentOperCode(Контент.ContentOperCode, СвПрин.КодСодОпер);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПрин, "ДатаПрин") Тогда
			УстановитьЗначениеXDTO(Контент, "AcceptanceDate", ДатаXML_в_Дата1С(СвПрин.ДатаПрин));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПрин, "СвЛицПрин") Тогда
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПрин.СвЛицПрин, "РабОргПок") Тогда
				Заполнить_Employee(Контент.Employee, СвПрин.СвЛицПрин.РабОргПок);
			Иначе
				Заполнить_OtherIssuer(Контент.OtherIssuer, СвПрин.СвЛицПрин.ИнЛицо);
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ4, "ИнфПолФХЖ4") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfoId, СодФХЖ4.ИнфПолФХЖ4);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ИнфПок, "ИнфПокГосЗакКазн") Тогда
		Заполнить_BuyerInfoCircumPublicProc(Контент.BuyerInfoCircumPublicProc, ИнфПок.ИнфПокГосЗакКазн);
	КонецЕсли;
	
	Заполнить_ExtendedSigner(Контент.Signers, ИнфПок, "Подписант");
	
КонецПроцедуры

Процедура Заполнить_Utd820SellerContent(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Function",	Параметры.Документ.Функция);
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ, "НаимДокОпр") Тогда
		УстановитьЗначениеXDTO(Контент, "DocumentName", Параметры.Документ.НаимДокОпр);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "DocumentCreator",							Параметры.Документ.НаимЭконСубСост);
	УстановитьЗначениеXDTO(Контент, "DocumentCreatorBase",						ПолучитьЗначениеXDTO(Параметры.Документ, "ОснДоверОргСост"));
	УстановитьЗначениеXDTO(Контент, "ApprovedStructureAdditionalInfoFields",	ПолучитьЗначениеXDTO(Параметры.Документ, "СоглСтрДопИнф"));
	УстановитьЗначениеXDTO(Контент, "Currency",									Параметры.Документ.СвСчФакт.КодОКВ);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "ДатаСчФ") Тогда
		УстановитьЗначениеXDTO(Контент, "DocumentDate", ДатаXML_в_Дата1С(Параметры.Документ.СвСчФакт.ДатаСчФ));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "НомерСчФ") Тогда
		УстановитьЗначениеXDTO(Контент, "DocumentNumber", Параметры.Документ.СвСчФакт.НомерСчФ);
	КонецЕсли;
	
	// Заполнение таблиц сведений о счете-фактуре 
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "ИспрСчф") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт.ИспрСчФ, "ДатаИспрСчф") Тогда
			УстановитьЗначениеXDTO(Контент, "RevisionDate", ДатаXML_в_Дата1С(Параметры.Документ.СвСчФакт.ИспрСчФ.ДатаИспрСчф));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт.ИспрСчФ, "НомИспрСчФ") Тогда
			УстановитьЗначениеXDTO(Контент, "RevisionNumber", Параметры.Документ.СвСчФакт.ИспрСчФ.НомИспрСчФ);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт.ИспрСчФ, "ДефНомИспрСчФ") Тогда
			УстановитьЗначениеXDTO(Контент, "HyphenRevisionNumber", Истина);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт.ИспрСчФ, "ДефДатаИспрСчФ") Тогда
			УстановитьЗначениеXDTO(Контент, "HyphenRevisionDate", Истина);
		КонецЕсли;
		
	КонецЕсли;
	
	Для Каждого СвПрод Из СписокXDTOИзЭлемента(Параметры.Документ.СвСчФакт, "СвПрод") Цикл
		Seller = НовыйЭлементСпискаXDTO(Контент.Sellers);
		Заполнить_ExtendedOrganizationInfo(Seller, СвПрод);
		Seller.FnsParticipantId = Параметры.СвУчДокОбор.ИдОтпр; // TODO: пока для всех продавцов один FnsParticipantId
	КонецЦикла;
	
	Для Каждого СвПокуп Из СписокXDTOИзЭлемента(Параметры.Документ.СвСчФакт, "СвПокуп") Цикл
		Buyer = НовыйЭлементСпискаXDTO(Контент.Buyers);
		Заполнить_ExtendedOrganizationInfo(Buyer, СвПокуп);
		Buyer.FnsParticipantId = Параметры.СвУчДокОбор.ИдПол; // TODO: пока для всех покупателей один FnsParticipantId
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "ГрузОт") Тогда
		Для Каждого ГрузОт Из СписокXDTOИзЭлемента(Параметры.Документ.СвСчФакт, "ГрузОт") Цикл
			Shipper = НовыйЭлементСпискаXDTO(Контент.Shippers);
			Заполнить_Shipper(Shipper, ГрузОт);
		КонецЦикла;
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "ГрузПолуч") Тогда
		Для Каждого ГрузПолуч Из СписокXDTOИзЭлемента(Параметры.Документ.СвСчФакт, "ГрузПолуч") Цикл
			Consignee = НовыйЭлементСпискаXDTO(Контент.Consignees);
			Заполнить_ExtendedOrganizationInfo(Consignee, ГрузПолуч);
		КонецЦикла;
	КонецЕсли;
	
	Для Каждого СвПРД Из СписокXDTOИзЭлемента(Параметры.Документ.СвСчФакт, "СвПРД") Цикл
		Заполнить_PaymentDocument(НовыйЭлементСпискаXDTO(Контент.PaymentDocuments), СвПРД);
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "ДопСвФХЖ1") Тогда
		
		УстановитьЗначениеXDTO(Контент, "GovernmentContractInfo",	ПолучитьЗначениеXDTO(Параметры.Документ.СвСчФакт.ДопСвФХЖ1, "ИдГосКон"));
		УстановитьЗначениеXDTO(Контент, "CurrencyRate",				ПолучитьЗначениеXDTO(Параметры.Документ.СвСчФакт.ДопСвФХЖ1, "КурсВал"));
		УстановитьЗначениеXDTO(Контент, "CircumFormatInvoice",		ПолучитьЗначениеXDTO(Параметры.Документ.СвСчФакт.ДопСвФХЖ1, "ОбстФормСЧФ"));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт.ДопСвФХЖ1, "СвФактор") Тогда
			Заполнить_ExtendedOrganizationInfo(Контент.FactorInfo, Параметры.Документ.СвСчФакт.ДопСвФХЖ1.СвФактор);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт.ДопСвФХЖ1, "ОснУстДенТреб") Тогда
			Заполнить_TransferBase820(Контент.MainAssignMonetaryClaim, Параметры.Документ.СвСчФакт.ДопСвФХЖ1.ОснУстДенТреб);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт.ДопСвФХЖ1, "ИнфПродГосЗакКазн") Тогда
			Заполнить_SellerInfoCircumPublicProc(Контент.SellerInfoCircumPublicProc, Параметры.Документ.СвСчФакт.ДопСвФХЖ1.ИнфПродГосЗакКазн);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "ДокПодтвОтгр") Тогда
		Для Каждого ДокПодтвОтгр Из СписокXDTOИзЭлемента(Параметры.Документ.СвСчФакт, "ДокПодтвОтгр") Цикл
			DocumentShipment = НовыйЭлементСпискаXDTO(Контент.DocumentShipments);
			Заполнить_DocumentShipment(DocumentShipment, ДокПодтвОтгр);
		КонецЦикла;
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ.СвСчФакт, "ИнфПолФХЖ1") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfoId, Параметры.Документ.СвСчФакт.ИнфПолФХЖ1);
	КонецЕсли;
	
	// Сведения таблицы счета-фактуры 
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ, "ТаблСчФакт") Тогда
		Заполнить_InvoiceTable820(Контент.Table, Параметры.Документ.ТаблСчФакт);
	КонецЕсли;
	
	// СФХЖ3
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.Документ, "СвПродПер") Тогда
		Заполнить_TransferInfo820(Контент.TransferInfo, Параметры.Документ.СвПродПер);
	КонецЕсли;
	
	Заполнить_ExtendedSigner(Контент.Signers, Параметры.Документ, "Подписант");
	
КонецПроцедуры

Процедура Заполнить_Utd970SellerContent(Контент, Параметры)
	
	Документ = Параметры.Документ;
	
	УстановитьЗначениеXDTO(Контент, "Function", Документ.Функция);
	
	НаимДокОпр = ПолучитьЗначениеXDTO(Документ, "НаимДокОпр");
	УстановитьЗначениеXDTO(Контент, "DocumentName", НаимДокОпр);
	
	УИД = ПолучитьЗначениеXDTO(Документ, "УИД");
	УстановитьЗначениеXDTO(Контент, "DocumentUID", УИД);
	
	СвСчФакт = Документ.СвСчФакт;
	
	НаимЭконСубСост = ПолучитьЗначениеXDTO(Документ, "НаимЭконСубСост");
	УстановитьЗначениеXDTO(Контент, "DocumentCreator", НаимЭконСубСост);
	
	СоглСтрДопИнф = ПолучитьЗначениеXDTO(Документ, "СоглСтрДопИнф");
	УстановитьЗначениеXDTO(Контент, "ApprovedStructureAdditionalInfoFields", СоглСтрДопИнф);
	
	ДатаДок = ПолучитьЗначениеXDTO(СвСчФакт, "ДатаДок");
	УстановитьЗначениеXDTO(Контент, "DocumentDate", ДатаXML_в_Дата1С(ДатаДок));
	
	НомерДок = ПолучитьЗначениеXDTO(СвСчФакт, "НомерДок");
	УстановитьЗначениеXDTO(Контент, "DocumentNumber", НомерДок);
	
	УстановитьЗначениеXDTO(Контент, "Currency", СвСчФакт.ДенИзм.КодОКВ);
	УстановитьЗначениеXDTO(Контент, "CurrencyName", СвСчФакт.ДенИзм.НаимОКВ);
	
	КурсВал = ПолучитьЗначениеXDTO(СвСчФакт.ДенИзм, "КурсВал");
	УстановитьЗначениеXDTO(Контент, "CurrencyRate", КурсВал);
	
	ИмяФайлИспрПрод = ПолучитьЗначениеXDTO(СвСчФакт, "ИмяФайлИспрПрод");
	УстановитьЗначениеXDTO(Контент, "SourceFileNameSeller", ИмяФайлИспрПрод);
	
	ИмяФайлИспрПок = ПолучитьЗначениеXDTO(СвСчФакт, "ИмяФайлИспрПок");
	УстановитьЗначениеXDTO(Контент, "SourceFileNameBuyer", ИмяФайлИспрПок);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ИспрДок") Тогда
		УстановитьЗначениеXDTO(Контент, "RevisionDate", ДатаXML_в_Дата1С(СвСчФакт.ИспрДок.ДатаИспр));
		УстановитьЗначениеXDTO(Контент, "RevisionNumber", СвСчФакт.ИспрДок.НомИспр);
	КонецЕсли;
	
	СведенияИдФайл = XML_СведенияИдФайлУПД970(Параметры.ИдФайл);
	
	ИдПолучателя = СведенияИдФайл.ИдПолучателя;
	ИдОтправителя = СведенияИдФайл.ИдОтправителя;
	
	Для Каждого СвПрод Из СписокXDTOИзЭлемента(СвСчФакт, "СвПрод") Цикл
		Seller = НовыйЭлементСпискаXDTO(Контент.Sellers);
		Заполнить_ExtendedOrganizationInfo970(Seller, СвПрод);
		Seller.FnsParticipantId = ИдОтправителя;
	КонецЦикла;
	
	Для Каждого СвПокуп Из СписокXDTOИзЭлемента(СвСчФакт, "СвПокуп") Цикл
		Buyer = НовыйЭлементСпискаXDTO(Контент.Buyers);
		Заполнить_ExtendedOrganizationInfo970(Buyer, СвПокуп);
		Buyer.FnsParticipantId = ИдПолучателя;
	КонецЦикла;
	
	Для Каждого ГрузОт Из СписокXDTOИзЭлемента(СвСчФакт, "ГрузОт") Цикл
		Shipper = НовыйЭлементСпискаXDTO(Контент.Shippers);
		Заполнить_Shipper970(Shipper, ГрузОт);
	КонецЦикла;
	
	Для Каждого ГрузПолуч Из СписокXDTOИзЭлемента(СвСчФакт, "ГрузПолуч") Цикл
		Consignee = НовыйЭлементСпискаXDTO(Контент.Consignees);
		Заполнить_ExtendedOrganizationInfo970(Consignee, ГрузПолуч);
	КонецЦикла;
	
	Для Каждого СвПРД Из СписокXDTOИзЭлемента(СвСчФакт, "СвПРД") Цикл
		PaymentDocumentInfo = НовыйЭлементСпискаXDTO(Контент.PaymentDocuments);
		Заполнить_PaymentDocument(PaymentDocumentInfo, СвПРД);
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ДопСвФХЖ1") Тогда
		
		ДопСвФХЖ1 = СвСчФакт.ДопСвФХЖ1;
		
		УстановитьЗначениеXDTO(Контент, "GovernmentContractInfo", ПолучитьЗначениеXDTO(ДопСвФХЖ1, "ИдГосКон"));
		
		ФункцииУПД = Перечисление_ФункцииУПД();
		
		Если Документ.Функция = ФункцииУПД.СЧФ 
			И ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСвФХЖ1, "СпОбстФСЧФ") Тогда
			УстановитьЗначениеXDTO(Контент, "CircumFormatInvoice", ДопСвФХЖ1.СпОбстФСЧФ);
		ИначеЕсли Документ.Функция = ФункцииУПД.СЧФДОП 
			И ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСвФХЖ1, "СпОбстФСЧФДОП") Тогда
			УстановитьЗначениеXDTO(Контент, "CircumFormatInvoice", ДопСвФХЖ1.СпОбстФСЧФДОП);
		ИначеЕсли Документ.Функция = ФункцииУПД.ДОП 
			И ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСвФХЖ1, "СпОбстФДОП") Тогда
			УстановитьЗначениеXDTO(Контент, "CircumFormatInvoice", ДопСвФХЖ1.СпОбстФДОП);
		КонецЕсли;
		
		Для Каждого ВидОбяз Из СписокXDTOИзЭлемента(ДопСвФХЖ1, "ВидОбяз") Цикл
			CommitmentType = НовыйЭлементСпискаXDTO(Контент.CommitmentType);
			Заполнить_CommitmentType(CommitmentType, ВидОбяз);
		КонецЦикла;
		
		Для Каждого СопрДокФХЖ Из СписокXDTOИзЭлемента(ДопСвФХЖ1, "СопрДокФХЖ") Цикл
			AccompanyingDocument = НовыйЭлементСпискаXDTO(Контент.AccompanyingDocuments);
			Заполнить_DocumentDetails(AccompanyingDocument, СопрДокФХЖ);
		КонецЦикла;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСвФХЖ1, "СвФактор") Тогда
			Заполнить_ExtendedOrganizationInfo970(Контент.FactorInfo, ДопСвФХЖ1.СвФактор);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСвФХЖ1, "ОснУстДенТреб") Тогда
			Заполнить_DocumentDetails(Контент.MainAssignMonetaryClaim, ДопСвФХЖ1.ОснУстДенТреб);
		КонецЕсли;
		
		Если Параметры.ВерсФорм = "5.03" Тогда
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСвФХЖ1, "ИнфПродЗаГоскКазн") Тогда
				Заполнить_SellerInfoCircumPublicProc(Контент.SellerInfoCircumPublicProc, ДопСвФХЖ1.ИнфПродЗаГоскКазн);
			КонецЕсли;
		Иначе
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСвФХЖ1, "ИнфПродЗаГосКазн") Тогда
				Заполнить_SellerInfoCircumPublicProc(Контент.SellerInfoCircumPublicProc, ДопСвФХЖ1.ИнфПродЗаГосКазн);
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ОснДоверОргСост") Тогда
		Заполнить_DocumentDetails(Контент.DocumentCreatorBase, Документ.ОснДоверОргСост);
	КонецЕсли;
	
	Для Каждого ДокПодтвОтгрНом Из СписокXDTOИзЭлемента(СвСчФакт, "ДокПодтвОтгрНом") Цикл
		DocumentShipment = НовыйЭлементСпискаXDTO(Контент.DocumentShipments);
		Заполнить_DocumentDetails(DocumentShipment, ДокПодтвОтгрНом);
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСчФакт, "ИнфПолФХЖ1") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfoId, СвСчФакт.ИнфПолФХЖ1);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ТаблСчФакт") Тогда
		Заполнить_InvoiceTable970(Контент.Table, Документ.ТаблСчФакт);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "СвПродПер") Тогда
		Заполнить_TransferInfo970(Контент.TransferInfo, Документ.СвПродПер);
	КонецЕсли;
	
	Для Каждого Подписант Из СписокXDTOИзЭлемента(Документ, "Подписант") Цикл
		Signer = НовыйЭлементСпискаXDTO(Контент.Signers);
		Заполнить_Signer970(Signer, Подписант);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_Utd970BuyerContent(Контент, Параметры)
	
	Документ = Параметры.Документ;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "НаимЭконСубСост") Тогда
		УстановитьЗначениеXDTO(Контент, "DocumentCreator", Документ.НаимЭконСубСост);
	КонецЕсли;
	
	ОснДоверОргСост = ПолучитьЗначениеXDTO(Документ, "ОснДоверОргСост");
	Заполнить_DocumentDetails(Контент.DocumentCreatorBase, ОснДоверОргСост);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ИнфПокГосЗакКазн") Тогда
		Заполнить_BuyerInfoCircumPublicProc(Контент.BuyerInfoCircumPublicProc, Документ.ИнфПокГосЗакКазн);
	КонецЕсли;
	
	Для Каждого Подписант Из СписокXDTOИзЭлемента(Документ, "Подписант") Цикл
		Signer = НовыйЭлементСпискаXDTO(Контент.Signers);
		Заполнить_Signer970(Signer, Подписант);
	КонецЦикла;
	
	СодФХЖ4 = Документ.СодФХЖ4;
	
	ВидОперации = ПолучитьЗначениеXDTO(СодФХЖ4, "ВидОпер");
	УстановитьЗначениеXDTO(Контент, "OperationCode", ВидОперации);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ4, "СвПрин") Тогда
		
		СвПрин = СодФХЖ4.СвПрин;
		
		СодОпер = ПолучитьЗначениеXDTO(СвПрин, "СодОпер");	
		УстановитьЗначениеXDTO(Контент, "OperationContent",	СодОпер);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПрин, "ДатаПрин") Тогда
			ДатаПринятия = ДатаXML_в_Дата1С(СвПрин.ДатаПрин);
			УстановитьЗначениеXDTO(Контент, "AcceptanceDate", ДатаПринятия);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПрин, "КодСодОпер") Тогда
			Заполнить_ContentOperCode970(Контент.ContentOperCode, СвПрин.КодСодОпер);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПрин, "СвЛицПрин") Тогда
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвПрин.СвЛицПрин, "РабОргПок") Тогда
				Заполнить_Employee(Контент.Employee, СвПрин.СвЛицПрин.РабОргПок);
			Иначе
				Заполнить_OtherIssuer970(Контент.OtherIssuer, СвПрин.СвЛицПрин.ИнЛицо);
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ4, "ИнфПолФХЖ4") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfoId, СодФХЖ4.ИнфПолФХЖ4);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_UcdSellerContent(Контент, Параметры)
	
	// Документация: http://diadocsdk-1c.readthedocs.io/ru/latest/UcdSellerContent.html
	
	Документ = Параметры.Документ;
	СвКСчФ = Документ.СвКСчФ;
	
	Контент.Seller.FnsParticipantId	 = Параметры.СвУчДокОбор.ИдОтпр;
	Контент.Buyer.FnsParticipantId	 = Параметры.СвУчДокОбор.ИдПол;
	
	УстановитьЗначениеXDTO(Контент, "FormatVersion", "ucd");
	УстановитьЗначениеXDTO(Контент, "Number"	, СвКСчФ.НомерКСчФ);
	УстановитьЗначениеXDTO(Контент, "Date"		, ДатаXML_в_Дата1С(СвКСчФ.ДатаКСчФ));
	УстановитьЗначениеXDTO(Контент, "Function"	, Документ.Функция);
	УстановитьЗначениеXDTO(Контент, "Creator"	, Документ.НаимЭконСубСост);
	УстановитьЗначениеXDTO(Контент, "Currency"	, СвКСчФ.КодОКВ);
	
	Заполнить_ExtendedOrganizationInfo(Контент.Seller	, СвКСчФ.СвПрод);
	Заполнить_ExtendedOrganizationInfo(Контент.Buyer	, СвКСчФ.СвПокуп);
	
	Заполнить_EventContent(Контент.EventContent, Документ.СодФХЖ3);
	Заполнить_ExtendedSigner(Контент.Signers, Документ, "Подписант");
	Заполнить_InvoiceCorrectionTable(Контент.InvoiceCorrectionTable, Документ.ТаблКСчФ);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "НаимДокОпр") Тогда
		УстановитьЗначениеXDTO(Контент, "Name", Документ.НаимДокОпр);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвКСчФ, "ДопСвФХЖ1") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвКСчФ.ДопСвФХЖ1, "КурсВал") Тогда
			УстановитьЗначениеXDTO(Контент, "CurrencyRate", СвКСчФ.ДопСвФХЖ1.КурсВал);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвКСчФ.ДопСвФХЖ1, "ИдГосКон") Тогда
			УстановитьЗначениеXDTO(Контент, "GovernmentContractInfo", СвКСчФ.ДопСвФХЖ1.ИдГосКон);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвКСчФ, "ИспрКСчФ") Тогда
		УстановитьЗначениеXDTO(Контент, "RevisionDate"	, ДатаXML_в_Дата1С(СвКСчФ.ИспрКСчФ.ДатаИспрКСчФ));
		УстановитьЗначениеXDTO(Контент, "RevisionNumber", СвКСчФ.ИспрКСчФ.НомИспрКСчФ);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвКСчФ, "ИнфПолФХЖ1") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfoId, СвКСчФ.ИнфПолФХЖ1);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ОснДоверОргСост") Тогда
		УстановитьЗначениеXDTO(Контент, "CreatorBase", Документ.ОснДоверОргСост);
	КонецЕсли;
	
	// Номера корректируемых документов.
	Для Каждого СчФ Из СписокXDTOИзЭлемента(СвКСчФ, "СчФ") Цикл
		Заполнить_InvoiceForCorrectionInfo(НовыйЭлементСпискаXDTO(Контент.Invoices), СчФ);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_Ucd736SellerContent(Контент, Параметры)
	
	Документ = Параметры.Документ;
	СвКСчФ = Документ.СвКСчФ;
	
	УстановитьЗначениеXDTO(Контент, "FormatVersion", "ucd736");
	УстановитьЗначениеXDTO(Контент, "Function", Документ.Функция);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "НаимДокОпр") Тогда
		УстановитьЗначениеXDTO(Контент, "Name", Документ.НаимДокОпр);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Date", ДатаXML_в_Дата1С(СвКСчФ.ДатаКСчФ));
	УстановитьЗначениеXDTO(Контент, "Number", СвКСчФ.НомерКСчФ);
	
	Для Каждого СчФ Из СписокXDTOИзЭлемента(СвКСчФ, "СчФ") Цикл
		Заполнить_InvoiceForCorrectionInfo(НовыйЭлементСпискаXDTO(Контент.Invoices), СчФ);
	КонецЦикла;
	
	Заполнить_ExtendedOrganizationInfo(Контент.Seller, СвКСчФ.СвПрод);
	Контент.Seller.FnsParticipantId = Параметры.СвУчДокОбор.ИдОтпр;
	
	Заполнить_ExtendedOrganizationInfo(Контент.Buyer, СвКСчФ.СвПокуп);
	Контент.Buyer.FnsParticipantId = Параметры.СвУчДокОбор.ИдПол;
	
	Заполнить_ExtendedSigner(Контент.Signers, Документ, "Подписант");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "СодФХЖ3") Тогда
		Заполнить_EventContent(Контент.EventContent, Документ.СодФХЖ3);
	КонецЕсли;
	
	Заполнить_InvoiceCorrectionTable(Контент.InvoiceCorrectionTable, Документ.ТаблКСчФ);
	
	УстановитьЗначениеXDTO(Контент, "Currency", СвКСчФ.КодОКВ);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвКСчФ, "ДопСвФХЖ1") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвКСчФ.ДопСвФХЖ1, "КурсВал") Тогда
			УстановитьЗначениеXDTO(Контент, "CurrencyRate", СвКСчФ.ДопСвФХЖ1.КурсВал);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвКСчФ.ДопСвФХЖ1, "ИдГосКон") Тогда
			УстановитьЗначениеXDTO(Контент, "GovernmentContractInfo", СвКСчФ.ДопСвФХЖ1.ИдГосКон);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвКСчФ.ДопСвФХЖ1, "НаимОКВ") Тогда
			УстановитьЗначениеXDTO(Контент, "CurrencyName", ?(ЗначениеЗаполнено(СвКСчФ.ДопСвФХЖ1.НаимОКВ), 1, 0));
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвКСчФ, "ИспрКСчФ") Тогда
		УстановитьЗначениеXDTO(Контент, "RevisionDate"	, ДатаXML_в_Дата1С(СвКСчФ.ИспрКСчФ.ДатаИспрКСчФ));
		УстановитьЗначениеXDTO(Контент, "RevisionNumber", СвКСчФ.ИспрКСчФ.НомИспрКСчФ);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвКСчФ, "ИнфПолФХЖ1") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfoId, СвКСчФ.ИнфПолФХЖ1);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Creator", Документ.НаимЭконСубСост);
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ОснДоверОргСост") Тогда
		УстановитьЗначениеXDTO(Контент, "CreatorBase", Документ.ОснДоверОргСост);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_InvoiceForCorrectionInfo(Контент, Параметры)
	
	// Документация: http://diadocsdk-1c.readthedocs.io/ru/latest/InvoiceForCorrectionInfo.html
	
	УстановитьЗначениеXDTO(Контент, "InvoiceDate",		ДатаXML_в_Дата1С(Параметры.ДатаСчФ));
	УстановитьЗначениеXDTO(Контент, "InvoiceNumber",	Параметры.НомерСчФ);
	
	// Номера предыдущих исправлений
	Для Каждого ИспрСчФ Из СписокXDTOИзЭлемента(Параметры, "ИспрСчФ") Цикл
		Заполнить_InvoiceRevisionInfo(НовыйЭлементСпискаXDTO(Контент.InvoiceRevisions), ИспрСчФ);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_InvoiceRevisionInfo(Контент, Параметры)
	
	// Документация: http://diadocsdk-1c.readthedocs.io/ru/latest/InvoiceRevisionInfo.html
	
	УстановитьЗначениеXDTO(Контент, "RevisionDate",		ДатаXML_в_Дата1С(Параметры.ДатаИспрСчФ));
	УстановитьЗначениеXDTO(Контент, "RevisionNumber",	Параметры.НомИспрСчФ);
	
КонецПроцедуры

Процедура Заполнить_EventContent(Контент, Параметры)
	
	// Документация: http://diadocsdk-1c.readthedocs.io/ru/latest/EventContent.html
	
	Если ТипЗнч(Параметры.ПередатДокум) = Тип("Строка") Тогда
		// 189@
		УстановитьЗначениеXDTO(Контент, "TransferDocDetails", Параметры.ПередатДокум);
	Иначе
		// 736@
		Для Каждого ПередатДокум Из СписокXDTOИзЭлемента(Параметры, "ПередатДокум") Цикл
			Заполнить_CorrectionBases(НовыйЭлементСпискаXDTO(Контент.TransferDocDetails736), ПередатДокум);
		КонецЦикла;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "OperationContent", Параметры.СодОпер);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИныеСвИзмСтоим") Тогда
		УстановитьЗначениеXDTO(Контент, "CostChangeInfo", Параметры.ИныеСвИзмСтоим);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДатаНапр") Тогда
		УстановитьЗначениеXDTO(Контент, "NotificationDate", ДатаXML_в_Дата1С(Параметры.ДатаНапр));
	КонецЕсли;
	
	ИмяСвойства = "ОснКор"; // 189@
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДокумОснКор") Тогда
		// 736@
		ИмяСвойства = "ДокумОснКор";
	КонецЕсли;
	Для Каждого ОснКор Из СписокXDTOИзЭлемента(Параметры, ИмяСвойства) Цикл
		Заполнить_CorrectionBases(НовыйЭлементСпискаXDTO(Контент.CorrectionBases), ОснКор);
	КонецЦикла;
		
КонецПроцедуры

Процедура Заполнить_CorrectionBases(Контент, Параметры)
	
	// Документация: http://diadocsdk-1c.readthedocs.io/ru/latest/CorrectionBase.html
	
	УстановитьЗначениеXDTO(Контент, "BaseDocumentName", Параметры.НаимОсн);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НомОсн") Тогда
		УстановитьЗначениеXDTO(Контент, "BaseDocumentNumber", Параметры.НомОсн);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДатаОсн") Тогда
		УстановитьЗначениеXDTO(Контент, "BaseDocumentDate", ДатаXML_в_Дата1С(Параметры.ДатаОсн));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДопСвОсн") Тогда
		УстановитьЗначениеXDTO(Контент, "AdditionalInfo", Параметры.ДопСвОсн);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИдФайлОсн") Тогда
		УстановитьЗначениеXDTO(Контент, "BaseIdFile", Параметры.ИдФайлОсн);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_Waybill(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "DocumentNumber",			Параметры.НомТранНакл);
	УстановитьЗначениеXDTO(Контент, "DocumentDate",				ДатаXML_в_Дата1С(Параметры.ДатаТранНакл));
	
КонецПроцедуры

Процедура Заполнить_Act552SellerContent(Контент, Параметры)
	
	Документ = Параметры.Документ;
	СвДокПРУ = Документ.СвДокПРУ;
	СодФХЖ1	 = СвДокПРУ.СодФХЖ1;
	
	Контент.Seller.FnsParticipantId	 = Параметры.СвУчДокОбор.ИдОтпр;
	Контент.Buyer.FnsParticipantId	 = Параметры.СвУчДокОбор.ИдПол;
	
	УстановитьЗначениеXDTO(Контент, "DocumentDate"	, ДатаXML_в_Дата1С(СвДокПРУ.ИдентДок.ДатаДокПРУ));
	УстановитьЗначениеXDTO(Контент, "DocumentNumber", ПолучитьЗначениеXDTO(СвДокПРУ.ИдентДок, "НомДокПРУ"));
	
	УстановитьЗначениеXDTO(Контент, "Currency"		, СвДокПРУ.ДенИзм.КодОКВ);
	УстановитьЗначениеXDTO(Контент, "CurrencyRate"	, ПолучитьЗначениеXDTO(СвДокПРУ.ДенИзм, "КурсВал"));
	
	УстановитьЗначениеXDTO(Контент, "DocumentName"			, СвДокПРУ.НаимДок.НаимДокОпр);
	УстановитьЗначениеXDTO(Контент, "DocumentCreator"		, Документ.НаимЭконСубСост);
	УстановитьЗначениеXDTO(Контент, "DocumentCreatorBase"	, ПолучитьЗначениеXDTO(Документ, "ОснДоверОргСост"));
	УстановитьЗначениеXDTO(Контент, "OperationType"			, ПолучитьЗначениеXDTO(СодФХЖ1, "ВидОперации"));
	УстановитьЗначениеXDTO(Контент, "OperationTitle"		, ПолучитьЗначениеXDTO(СодФХЖ1, "ЗагСодОпер"));
	УстановитьЗначениеXDTO(Контент, "GovernmentContractInfo", ПолучитьЗначениеXDTO(СодФХЖ1, "ИдГосКон"));
	
	Заполнить_ExtendedOrganizationInfo(Контент.Seller	, СодФХЖ1.Исполнитель);
	Заполнить_ExtendedOrganizationInfo(Контент.Buyer	, СодФХЖ1.Заказчик);
	
	Заполнить_ExtendedSigner(Контент.Signers, Документ, "Подписант");
	
	Для Каждого Основание Из СписокXDTOИзЭлемента(СодФХЖ1, "Основание") Цикл
		Заполнить_GroundInfo(НовыйЭлементСпискаXDTO(Контент.Grounds), Основание);
	КонецЦикла;
	
	Для Каждого ОписРабот Из СписокXDTOИзЭлемента(СодФХЖ1, "ОписРабот") Цикл
		Заполнить_Act552WorkDescription(НовыйЭлементСпискаXDTO(Контент.Works), ОписРабот);
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвДокПРУ, "ИспрДокПРУ") Тогда
		УстановитьЗначениеXDTO(Контент, "RevisionDate"	, ДатаXML_в_Дата1С(СвДокПРУ.ИспрДокПРУ.ДатаИспрДокПРУ));
		УстановитьЗначениеXDTO(Контент, "RevisionNumber", СвДокПРУ.ИспрДокПРУ.НомИспрДокПРУ);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ1, "ИнфПолФХЖ1") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfo, СодФХЖ1.ИнфПолФХЖ1);
	КонецЕсли;
	
	Заполнить_Act552TransferInfo(Контент.TransferInfo, Документ.СодФХЖ2);
	
КонецПроцедуры

Процедура Заполнить_GroundInfo(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Name",		Параметры.НаимОсн);
	УстановитьЗначениеXDTO(Контент, "Number",	ПолучитьЗначениеXDTO(Параметры, "НомОсн"));
	УстановитьЗначениеXDTO(Контент, "Info",	ПолучитьЗначениеXDTO(Параметры, "ДопСвОсн"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДатаОсн") Тогда
		УстановитьЗначениеXDTO(Контент, "Date",	ДатаXML_в_Дата1С(Параметры.ДатаОсн));
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_Act552WorkDescription(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Total",				Параметры.СтУчНДСИт);
	УстановитьЗначениеXDTO(Контент, "TotalVat",				ПолучитьЗначениеXDTO(Параметры, "СумНДСИт"));
	УстановитьЗначениеXDTO(Контент, "TotalWithVatExcluded",	ПолучитьЗначениеXDTO(Параметры, "СтБезНДСИт"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НачРабот") Тогда
		УстановитьЗначениеXDTO(Контент, "StartingDate", ДатаXML_в_Дата1С(Параметры.НачРабот));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "КонРабот") Тогда
		УстановитьЗначениеXDTO(Контент, "CompletionDate", ДатаXML_в_Дата1С(Параметры.КонРабот));
	КонецЕсли;
	
	Для Каждого Работа Из СписокXDTOИзЭлемента(Параметры, "Работа") Цикл
		Заполнить_Act552WorkItem(НовыйЭлементСпискаXDTO(Контент.Items), Работа);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_Act552WorkItem(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НаимРабот") Тогда
		УстановитьЗначениеXDTO(Контент, "Name", 					ПолучитьЗначениеXDTO(Параметры, "НаимРабот"));
	КонецЕсли;  
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Описание") Тогда
		УстановитьЗначениеXDTO(Контент, "Description",				ПолучитьЗначениеXDTO(Параметры, "Описание"));
	КонецЕсли;  
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ОКЕИ") Тогда
		УстановитьЗначениеXDTO(Контент, "UnitCode",					ПолучитьЗначениеXDTO(Параметры, "ОКЕИ"));
	КонецЕсли;  
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НаимЕдИзм") Тогда
		УстановитьЗначениеXDTO(Контент, "UnitName",					ПолучитьЗначениеXDTO(Параметры, "НаимЕдИзм"));
	КонецЕсли;  
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Цена") Тогда
		УстановитьЗначениеXDTO(Контент, "Price",					ПолучитьЗначениеXDTO(Параметры, "Цена"));
	КонецЕсли;  
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Количество") Тогда
		УстановитьЗначениеXDTO(Контент, "Quantity",					ПолучитьЗначениеXDTO(Параметры, "Количество"));
	КонецЕсли;  
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СтоимБезНДС") Тогда
		УстановитьЗначениеXDTO(Контент, "SubtotalWithVatExcluded",	ПолучитьЗначениеXDTO(Параметры, "СтоимБезНДС"));
	КонецЕсли;  
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СумНДС") Тогда
		УстановитьЗначениеXDTO(Контент, "Vat",						ПолучитьЗначениеXDTO(Параметры, "СумНДС"));
	КонецЕсли;  
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СтоимУчНДС") Тогда
		УстановитьЗначениеXDTO(Контент, "Subtotal",					ПолучитьЗначениеXDTO(Параметры, "СтоимУчНДС"));
	КонецЕсли;  
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НалСт") Тогда
		УстановитьЗначениеXDTO(Контент, "TaxRate",					TaxRate_Из_НалСт(ПолучитьЗначениеXDTO(Параметры, "НалСт")));
	КонецЕсли;  
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "КоррСчДебет") Тогда
		УстановитьЗначениеXDTO(Контент, "ItemAccountDebit",			ПолучитьЗначениеXDTO(Параметры, "КоррСчДебет"));
	КонецЕсли;  
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "КоррСчКредит") Тогда
		УстановитьЗначениеXDTO(Контент, "ItemAccountCredit",		ПолучитьЗначениеXDTO(Параметры, "КоррСчКредит"));
	КонецЕсли;
	
	Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, Параметры, "ИнфПолеОписРабот");
	
КонецПроцедуры

Процедура Заполнить_Act552TransferInfo(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "OperationInfo",	Параметры.СодОпер);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДатаПер") Тогда
		УстановитьЗначениеXDTO(Контент, "TransferDate",	ДатаXML_в_Дата1С(Параметры.ДатаПер));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СвПерВещи") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СвПерВещи, "ДатаПерВещ") Тогда
			УстановитьЗначениеXDTO(Контент, "CreatedThingTransferDate", ДатаXML_в_Дата1С(Параметры.СвПерВещи.ДатаПерВещ));
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Контент, "CreatedThingInfo", ПолучитьЗначениеXDTO(Параметры.СвПерВещи, "СвПерВещ"));
		
	КонецЕсли;
	
	Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, Параметры, "ИнфПолФХЖ2");
	
КонецПроцедуры

Процедура Заполнить_Act552BuyerContent(Контент, Параметры)
	
	Документ = Параметры.Документ;
	СодФХЖ3 = Документ.СодФХЖ3;
	
	УстановитьЗначениеXDTO(Контент, "DocumentCreator",		Документ.НаимЭконСубСост);
	УстановитьЗначениеXDTO(Контент, "DocumentCreatorBase",	ПолучитьЗначениеXDTO(Документ, "ОснДоверОргСост"));
	УстановитьЗначениеXDTO(Контент, "OperationType",		ПолучитьЗначениеXDTO(СодФХЖ3, "ВидОперации"));
	УстановитьЗначениеXDTO(Контент, "OperationContent",		СодФХЖ3.РезПринял.СодОпер);
	
	Заполнить_ExtendedSigner(Контент.Signers, Документ, "Подписант");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ3.РезПринял, "ДатаПрием") Тогда
		УстановитьЗначениеXDTO(Контент, "AcceptanceDate",	ДатаXML_в_Дата1С(СодФХЖ3.РезПринял.ДатаПрием));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ3, "СвПолВещи") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ3.СвПолВещи, "ДатаПол") Тогда
			УстановитьЗначениеXDTO(Контент, "CreatedThingAcceptDate", ДатаXML_в_Дата1С(СодФХЖ3.СвПолВещи.ДатаПол));
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Контент, "CreatedThingInfo", ПолучитьЗначениеXDTO(СодФХЖ3.СвПолВещи, "СвПол"));
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ3, "ИнфПолФХЖ3") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfoId, СодФХЖ3.ИнфПолФХЖ3);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_TovTorgSellerContent(Контент, Параметры)
	
	Документ		 = Параметры.Документ;
	СвДокПТПрКроме	 = Документ.СвДокПТПрКроме;
	СвДокПТПр		 = СвДокПТПрКроме.СвДокПТПр;
	СодФХЖ1			 = СвДокПТПр.СодФХЖ1;
	
	Контент.Seller.FnsParticipantId	 = Параметры.СвУчДокОбор.ИдОтпр;
	Контент.Buyer.FnsParticipantId	 = Параметры.СвУчДокОбор.ИдПол;
	
	УстановитьЗначениеXDTO(Контент, "DocumentDate"	, ДатаXML_в_Дата1С(СвДокПТПр.ИдентДок.ДатаДокПТ));
	УстановитьЗначениеXDTO(Контент, "DocumentNumber", ПолучитьЗначениеXDTO(СвДокПТПр.ИдентДок, "НомДокПТ"));
	
	УстановитьЗначениеXDTO(Контент, "Currency"		, СвДокПТПр.ДенИзм.КодОКВ);
	УстановитьЗначениеXDTO(Контент, "CurrencyRate"	, ПолучитьЗначениеXDTO(СвДокПТПр.ДенИзм, "КурсВал"));
	
	УстановитьЗначениеXDTO(Контент, "DocumentCreator"		, Документ.НаимЭконСубСост);
	УстановитьЗначениеXDTO(Контент, "DocumentCreatorBase"	, ПолучитьЗначениеXDTO(Документ, "ОснДоверОргСост"));
	УстановитьЗначениеXDTO(Контент, "OperationType"			, ПолучитьЗначениеXDTO(СодФХЖ1, "ВидОперации"));
	УстановитьЗначениеXDTO(Контент, "GovernmentContractInfo", ПолучитьЗначениеXDTO(СодФХЖ1, "ИдГосКон"));
	
	Заполнить_ExtendedOrganizationInfo(Контент.Seller	, СодФХЖ1.Продавец);
	Заполнить_ExtendedOrganizationInfo(Контент.Buyer	, СодФХЖ1.Покупатель);
	
	Заполнить_ExtendedSigner(Контент.Signers, Документ, "Подписант");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ1, "ГрузОтпр") Тогда
		Заполнить_ExtendedOrganizationInfo(Контент.Shipper, СодФХЖ1.ГрузОтпр);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ1, "ГрузПолуч") Тогда
		Заполнить_ExtendedOrganizationInfo(Контент.Consignee, СодФХЖ1.ГрузПолуч);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ1, "Перевозчик") Тогда
		Заполнить_ExtendedOrganizationInfo(Контент.Carrier, СодФХЖ1.Перевозчик);
	КонецЕсли;
	
	Для Каждого Основание Из СписокXDTOИзЭлемента(СодФХЖ1, "Основание") Цикл
		Заполнить_GroundInfo(НовыйЭлементСпискаXDTO(Контент.Grounds), Основание);
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвДокПТПр, "ИспрДокПТ") Тогда
		УстановитьЗначениеXDTO(Контент, "RevisionDate",		ДатаXML_в_Дата1С(СвДокПТПр.ИспрДокПТ.ДатаИспрДокПТ));
		УстановитьЗначениеXDTO(Контент, "RevisionNumber",	СвДокПТПр.ИспрДокПТ.НомИспрДокПТ);
	КонецЕсли;
	
	Заполнить_TovTorgTransferInfo(Контент.TransferInfo, Документ.СодФХЖ3);
	
	Заполнить_TovTorgTable(Контент.Table, СвДокПТПрКроме.СодФХЖ2);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ1, "ИнфПолФХЖ1") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfoId, СодФХЖ1.ИнфПолФХЖ1);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "DocumentName", СвДокПТПр.НаимДок.НаимДокОпр);
	
КонецПроцедуры

Процедура Заполнить_TovTorgTransferInfo(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "OperationInfo", Параметры.СодОпер);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДатаОтпуск") Тогда
		УстановитьЗначениеXDTO(Контент, "TransferDate", ДатаXML_в_Дата1С(Параметры.ДатаОтпуск));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "КолПрил") Тогда
		УстановитьЗначениеXDTO(Контент, "Attachment", Параметры.КолПрил);
	КонецЕсли;
	
	Для Каждого ТранНакл Из СписокXDTOИзЭлемента(Параметры, "ТранНакл") Цикл
		Заполнить_Waybill(НовыйЭлементСпискаXDTO(Контент.Waybills), ТранНакл);
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СвЛицОтпГруз") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СвЛицОтпГруз, "РабОргПрод") Тогда
			Заполнить_Employee(Контент.Employee, Параметры.СвЛицОтпГруз.РабОргПрод);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СвЛицОтпГруз, "ИнЛицо") Тогда
			Заполнить_OtherIssuer(Контент.OtherIssuer, Параметры.СвЛицОтпГруз.ИнЛицо);
		КонецЕсли;
		
	КонецЕсли;
	
	Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, Параметры, "ИнфПолФХЖ3");
	
КонецПроцедуры

Процедура Заполнить_TovTorgTable(Контент, Параметры)
	
	Всего = Параметры.Всего;
	УстановитьЗначениеXDTO(Контент, "TotalQuantity"			, ПолучитьЗначениеXDTO(Всего, "КолМестВс"));
	УстановитьЗначениеXDTO(Контент, "TotalGross"			, ПолучитьЗначениеXDTO(Всего, "БруттоВс"));
	УстановитьЗначениеXDTO(Контент, "TotalNet"				, ПолучитьЗначениеXDTO(Всего, "НеттоВс"));
	УстановитьЗначениеXDTO(Контент, "TotalWithVatExcluded"	, ПолучитьЗначениеXDTO(Всего, "СтБезНДСВс"));
	УстановитьЗначениеXDTO(Контент, "TotalVat"				, ПолучитьЗначениеXDTO(Всего, "СумНДСВс"));
	УстановитьЗначениеXDTO(Контент, "Total"					, Всего.СтУчНДСВс);
	
	Для Каждого СвТов Из СписокXDTOИзЭлемента(Параметры , "СвТов") Цикл
		Заполнить_TovTorgItem(НовыйЭлементСпискаXDTO(Контент.Items), СвТов);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_TovTorgItem(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Product",					ПолучитьЗначениеXDTO(Параметры, "НаимТов"));
	УстановитьЗначениеXDTO(Контент, "Feature",					ПолучитьЗначениеXDTO(Параметры, "ХарактерТов"));
	УстановитьЗначениеXDTO(Контент, "Sort",						ПолучитьЗначениеXDTO(Параметры, "СортТов"));
	УстановитьЗначениеXDTO(Контент, "VendorCode",				ПолучитьЗначениеXDTO(Параметры, "АртикулТов"));
	УстановитьЗначениеXDTO(Контент, "ProductCode",				ПолучитьЗначениеXDTO(Параметры, "КодТов"));
	УстановитьЗначениеXDTO(Контент, "UnitName",					Параметры.НаимЕдИзм);
	УстановитьЗначениеXDTO(Контент, "Unit",						Параметры.ОКЕИ_Тов);
	УстановитьЗначениеXDTO(Контент, "PackageType",				ПолучитьЗначениеXDTO(Параметры, "ВидУпак"));
	УстановитьЗначениеXDTO(Контент, "QuantityInPack",			ПолучитьЗначениеXDTO(Параметры, "Место"));
	УстановитьЗначениеXDTO(Контент, "Quantity",					ПолучитьЗначениеXDTO(Параметры, "КолМест"));
	УстановитьЗначениеXDTO(Контент, "Gross",					ПолучитьЗначениеXDTO(Параметры, "Брутто"));
	УстановитьЗначениеXDTO(Контент, "Net",						Параметры.НеттоПередано);
	УстановитьЗначениеXDTO(Контент, "ItemToRelease",			ПолучитьЗначениеXDTO(Параметры, "НадлОтп"));
	УстановитьЗначениеXDTO(Контент, "Price",					ПолучитьЗначениеXDTO(Параметры, "Цена"));
	УстановитьЗначениеXDTO(Контент, "SubtotalWithVatExcluded",	ПолучитьЗначениеXDTO(Параметры, "СтБезНДС"));
	УстановитьЗначениеXDTO(Контент, "Vat",						ПолучитьЗначениеXDTO(Параметры, "СумНДС"));
	УстановитьЗначениеXDTO(Контент, "Subtotal",					Параметры.СтУчНДС);
	УстановитьЗначениеXDTO(Контент, "ItemAccountDebit",			ПолучитьЗначениеXDTO(Параметры, "КорСчДебет"));
	УстановитьЗначениеXDTO(Контент, "ItemAccountCredit",		ПолучитьЗначениеXDTO(Параметры, "КорСчКредит"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НалСт") Тогда
		УстановитьЗначениеXDTO(Контент, "TaxRate", TaxRate_Из_НалСт(Параметры.НалСт));
	КонецЕсли;
	
	Заполнить_СписокKeyValue(Контент.StructedAdditionalInfos, Параметры, "ИнфПолФХЖ2");
	
КонецПроцедуры

Процедура Заполнить_TovTorgBuyerContent(Контент, Параметры)
	
	Документ = Параметры.Документ;
	СодФХЖ4 = Документ.СодФХЖ4;
	ГрузПолучил = СодФХЖ4.ГрузПолучил;
	
	УстановитьЗначениеXDTO(Контент, "DocumentCreator",		Документ.НаимЭконСубСост);
	УстановитьЗначениеXDTO(Контент, "DocumentCreatorBase",	ПолучитьЗначениеXDTO(Документ, "ОснДоверОргСост"));
	УстановитьЗначениеXDTO(Контент, "OperationCode",		ПолучитьЗначениеXDTO(СодФХЖ4, "ВидОперации"));
	УстановитьЗначениеXDTO(Контент, "OperationContent",		ГрузПолучил.СодОпер);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ГрузПолучил, "ДатаПолуч") Тогда
		УстановитьЗначениеXDTO(Контент, "AcceptanceDate",	ДатаXML_в_Дата1С(ГрузПолучил.ДатаПолуч));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ГрузПолучил, "СвЛицПолГруз") Тогда
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ГрузПолучил.СвЛицПолГруз, "РабОргПок") Тогда
			Заполнить_Employee(Контент.Employee, ГрузПолучил.СвЛицПолГруз.РабОргПок);
		Иначе
			Заполнить_OtherIssuer(Контент.OtherIssuer, ГрузПолучил.СвЛицПолГруз.ИнЛицо);
		КонецЕсли;
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодФХЖ4, "ИнфПолФХЖ4") Тогда
		Заполнить_AdditionalInfoId(Контент.AdditionalInfo, СодФХЖ4.ИнфПолФХЖ4);
	КонецЕсли;
	
	Заполнить_ExtendedSigner(Контент.Signers, Документ, "Подписант");
	
КонецПроцедуры

Процедура Заполнить_Torg2SenderTitle(Контент, Параметры)
	
	Документ = Параметры.Документ;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ИспрДокПР") Тогда
		УстановитьЗначениеXDTO(Контент.RevisionInfo, "RevisionDate", ДатаXML_в_Дата1С(Документ.ИспрДокПР.ДатаИспрДокПР));
		УстановитьЗначениеXDTO(Контент.RevisionInfo, "RevisionNumber", Документ.ИспрДокПР.НомИспрДокПР);
	КонецЕсли;
	
	Заполнить_CircumstancesAcceptanceInfo(Контент.CircumstancesAcceptanceInfo, Документ.СодФХЖ1);
	
	// заполним идентификаторы отправителя и получателя
	Контент.CircumstancesAcceptanceInfo.Seller.OrganizationDetails.FnsParticipantId = Параметры.СвУчДокОбор.ИдОтпр;
	Контент.CircumstancesAcceptanceInfo.Buyer.OrganizationDetails.FnsParticipantId = Параметры.СвУчДокОбор.ИдПол;
	
	Заполнить_EvidenceAcceptanceInfo(Контент.EvidenceAcceptanceInfo, Документ.СодФХЖ2);
	
	Заполнить_Signers(Контент.Signers, СписокXDTOИзЭлемента(Документ, "Подписант"));
	
	Заполнить_DocumentAdditionalInfos(Контент.DocumentAdditionalInfos, СписокXDTOИзЭлемента(Документ, "ИнфДопСв"));
	
	УстановитьЗначениеXDTO(Контент, "DocumentNumber", Документ.ИдентДокПР.НомДокПР);
	УстановитьЗначениеXDTO(Контент, "DocumentDate", ДатаXML_в_Дата1С(Документ.ИдентДокПР.ДатаДокПР));
	УстановитьЗначениеXDTO(Контент, "DocumentName", Документ.НаимДокОпр);
	УстановитьЗначениеXDTO(Контент, "SenderFnsParticipantId", Параметры.СвУчДокОбор.ИдОтпр);
	УстановитьЗначениеXDTO(Контент, "RecipientFnsParticipantId", Параметры.СвУчДокОбор.ИдПол);
	УстановитьЗначениеXDTO(Контент, "DocumentCreator", Документ.НаимЭконСубСост);
	УстановитьЗначениеXDTO(Контент, "DocumentCreatorBase", ПолучитьЗначениеXDTO(Документ, "ОснДоверОргСост"));
	УстановитьЗначениеXDTO(Контент, "AdditionalInfosPurpose", ПолучитьЗначениеXDTO(Документ, "НазнДопСв"));
	УстановитьЗначениеXDTO(Контент, "CircumDraft", ПолучитьЗначениеXDTO(Документ, "ОбстСостДок"));
	УстановитьЗначениеXDTO(Контент, "IsInformed", ПолучитьЗначениеXDTO(Документ, "ПризнОзнак"));
	
КонецПроцедуры

Процедура Заполнить_CircumstancesAcceptanceInfo(Контент, Параметры)
	
	Заполнить_ExtendedOrganizationInfo_Torg2(Контент.Seller, Параметры.Продавец);
	Заполнить_ExtendedOrganizationInfo_Torg2(Контент.Buyer, Параметры.Покупатель);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Грузоотправитель") Тогда
		Заполнить_ExtendedOrganizationInfo_Torg2(Контент.Shipper, Параметры.Грузоотправитель);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Грузополучатель") Тогда
		Заполнить_ExtendedOrganizationInfo_Torg2(Контент.Consignee, Параметры.Грузополучатель);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СтрахКом") Тогда
		Заполнить_ExtendedOrganizationInfo_Torg2(Контент.InsuranceCompany, Параметры.СтрахКом);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Приказ") Тогда
		УстановитьЗначениеXDTO(Контент.Order, "Date", ДатаXML_в_Дата1С(Параметры.Приказ.ДатаПрик));
		УстановитьЗначениеXDTO(Контент.Order, "Number", Параметры.Приказ.НомПрик);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СвОсмГруз") Тогда
		Заполнить_CargoInspectionInformation(Контент.CargoInspectionInformation, Параметры.СвОсмГруз);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СвВремПрием") Тогда
		Заполнить_ReceptionInformation(Контент.ReceptionInformation, Параметры.СвВремПрием);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СвСопрДок") Тогда
		Заполнить_AccompanyingDocumentTable(Контент.AccompanyingDocumentTable, СписокXDTOИзЭлемента(Параметры, "СвСопрДок"));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СвЛицПрин") Тогда
		Заполнить_PersonAcceptedCargoInfo(Контент.PersonAcceptedCargoInfo, Параметры.СвЛицПрин);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДрОбстПрием") Тогда
		Заполнить_OtherCircumstancesAcceptanceInfo(Контент.OtherCircumstancesAcceptanceInfo, Параметры.ДрОбстПрием);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИнфПолФХЖ1") Тогда
		Заполнить_AdditionalInfoId_Torg2(Контент.AdditionalInfoId, Параметры.ИнфПолФХЖ1);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "CircumFormat", ПолучитьЗначениеXDTO(Параметры, "ОбстИсп"));
	УстановитьЗначениеXDTO(Контент, "GovernmentContractInfo", ПолучитьЗначениеXDTO(Параметры, "ИдГосКон"));
	
КонецПроцедуры

Процедура Заполнить_EvidenceAcceptanceInfo(Контент, Параметры)
	
	Заполнить_AcceptanceResults(Контент.AcceptanceResults, СписокXDTOИзЭлемента(Параметры, "РезПрием"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ПрилДок") Тогда
		Заполнить_AttachedDocuments(Контент.AttachedDocuments, СписокXDTOИзЭлемента(Параметры, "ПрилДок"));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "OperationType", ПолучитьЗначениеXDTO(Параметры, "ВидОперации"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИнфПолФХЖ2") Тогда
		Заполнить_СписокIdValue(Контент.AdditionalInfos.AdditionalInfo, Параметры, "ИнфПолФХЖ2");
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "EventSummary", Параметры.СодСоб);
	УстановитьЗначениеXDTO(Контент, "Conclusion", ПолучитьЗначениеXDTO(Параметры, "Заключение"));
	
КонецПроцедуры

Процедура Заполнить_Signers(Контент, Параметры)
	
	Для Каждого Подписант Из Параметры Цикл
		
		Signer = НовыйЭлементСпискаXDTO(Контент);
		SignerDetails = Signer.SignerDetails;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Подписант, "ФЛ") Тогда
			
			// Физическое лицо
			
			УстановитьЗначениеXDTO(SignerDetails, "SignerType", "PhysicalPerson");
			
			ФЛ = Подписант.ФЛ;
			
			УстановитьЗначениеXDTO(SignerDetails, "LastName", ФЛ.ФИО.Фамилия);
			УстановитьЗначениеXDTO(SignerDetails, "FirstName", ФЛ.ФИО.Имя);
			УстановитьЗначениеXDTO(SignerDetails, "MiddleName", ПолучитьЗначениеXDTO(ФЛ.ФИО, "Отчество"));
			УстановитьЗначениеXDTO(SignerDetails, "Inn", ПолучитьЗначениеXDTO(ФЛ, "ИННФЛ"));
			УстановитьЗначениеXDTO(SignerDetails, "SignerInfo", ПолучитьЗначениеXDTO(ФЛ, "ИныеСвед"));
			
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Подписант, "ИП") Тогда
			
			// Индивидуальный предприниматель
			
			УстановитьЗначениеXDTO(SignerDetails, "SignerType", "IndividualEntity");
			
			ИП = Подписант.ИП;
			
			УстановитьЗначениеXDTO(SignerDetails, "LastName", ИП.ФИО.Фамилия);
			УстановитьЗначениеXDTO(SignerDetails, "FirstName", ИП.ФИО.Имя);
			УстановитьЗначениеXDTO(SignerDetails, "MiddleName", ПолучитьЗначениеXDTO(ИП.ФИО, "Отчество"));
			УстановитьЗначениеXDTO(SignerDetails, "Inn", ПолучитьЗначениеXDTO(ИП, "ИННФЛ"));
			УстановитьЗначениеXDTO(SignerDetails, "RegistrationCertificate", ПолучитьЗначениеXDTO(ИП, "СвГосРегИП"));
			УстановитьЗначениеXDTO(SignerDetails, "SignerInfo", ПолучитьЗначениеXDTO(ИП, "ИныеСвед"));
			
		Иначе
			
			// Представитель юридического лица "ЮЛ"
			
			УстановитьЗначениеXDTO(SignerDetails, "SignerType", "LegalEntity");
			
			ЮЛ = Подписант.ЮЛ;
			
			УстановитьЗначениеXDTO(SignerDetails, "LastName", ЮЛ.ФИО.Фамилия);
			УстановитьЗначениеXDTO(SignerDetails, "FirstName", ЮЛ.ФИО.Имя);
			УстановитьЗначениеXDTO(SignerDetails, "MiddleName", ПолучитьЗначениеXDTO(ЮЛ.ФИО, "Отчество"));
			УстановитьЗначениеXDTO(SignerDetails, "Inn", ЮЛ.ИННЮЛ);
			УстановитьЗначениеXDTO(SignerDetails, "SignerOrganizationName", ПолучитьЗначениеXDTO(ЮЛ, "НаимОрг"));
			УстановитьЗначениеXDTO(SignerDetails, "Position", ЮЛ.Должн);
			УстановитьЗначениеXDTO(SignerDetails, "SignerInfo", ПолучитьЗначениеXDTO(ЮЛ, "ИныеСвед"));
			
		КонецЕсли;
		
		УстановитьЗначениеXDTO(SignerDetails, "SignerPowers", SignerPowers(Подписант.ОблПолн));
		УстановитьЗначениеXDTO(SignerDetails, "SignerStatus", SignerStatus(Подписант.Статус));
		УстановитьЗначениеXDTO(SignerDetails, "SignerPowersBase", Подписант.ОснПолн);
		УстановитьЗначениеXDTO(SignerDetails, "SignerOrgPowersBase", ПолучитьЗначениеXDTO(Подписант, "ОснПолнОрг"));
		
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_DocumentAdditionalInfos(Контент, Параметры)
	
	Для Каждого ИнфДопСв Из Параметры Цикл
		Контент.DocumentAdditionalInfo.Добавить(ИнфДопСв);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_ExtendedOrganizationInfo_Torg2(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвФЛ") Тогда
		
		// Сведения о физическом лице
		УстановитьЗначениеXDTO(Контент.OrganizationDetails, "OrgType", "PhysicalEntity");
		
		СвФЛ = Параметры.ИдСв.СвФЛ;
		
		УстановитьЗначениеXDTO(Контент.OrganizationDetails, "OrgName", XML_СтрокаФИО(СвФЛ.ФИО));
		УстановитьЗначениеXDTO(Контент.OrganizationDetails, "Inn", ПолучитьЗначениеXDTO(СвФЛ, "ИННФЛ"));
		УстановитьЗначениеXDTO(Контент.OrganizationDetails, "OrganizationOrPersonInfo", ПолучитьЗначениеXDTO(СвФЛ, "ИныеСвед"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвИП") Тогда
		
		// Сведения об индивидуальном предпринимателе
		УстановитьЗначениеXDTO(Контент.OrganizationDetails, "OrgType", "IndividualEntity");
		
		СвИП = Параметры.ИдСв.СвИП;
		
		УстановитьЗначениеXDTO(Контент.OrganizationDetails, "OrgName", XML_СтрокаФИО(СвИП.ФИО));
		УстановитьЗначениеXDTO(Контент.OrganizationDetails, "Inn", СвИП.ИННФЛ);
		УстановитьЗначениеXDTO(Контент.OrganizationDetails, "IndividualEntityRegistrationCertificate", ПолучитьЗначениеXDTO(СвИП, "СвГосРегИП"));
		УстановитьЗначениеXDTO(Контент.OrganizationDetails, "OrganizationOrPersonInfo", ПолучитьЗначениеXDTO(СвИП, "ИныеСвед"));
		
	Иначе
		
		// Сведения об организации "СвОрг"
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв.СвОрг, "СвОргУч") Тогда
			
			// Сведения об организации, состоящей на учете в налоговом органе
			УстановитьЗначениеXDTO(Контент.OrganizationDetails, "OrgType", "LegalEntity");
			
			СвОргУч = Параметры.ИдСв.СвОрг.СвОргУч;
			
			УстановитьЗначениеXDTO(Контент.OrganizationDetails, "Inn", СвОргУч.ИННЮЛ);
			УстановитьЗначениеXDTO(Контент.OrganizationDetails, "OrgName", СвОргУч.НаимОрг);
			УстановитьЗначениеXDTO(Контент.OrganizationDetails, "Kpp", ПолучитьЗначениеXDTO(СвОргУч, "КПП"));
			
		Иначе
			
			// Сведения об иностранном лице, не состоящем на учете в налоговых органах в качестве налогоплательщика "СвИнНеУч"
			УстановитьЗначениеXDTO(Контент.OrganizationDetails, "OrgType", "ForeignEntity");
			
			СвИнНеУч = Параметры.ИдСв.СвОрг.СвИнНеУч;
			
			УстановитьЗначениеXDTO(Контент.OrganizationDetails, "OrgName", СвИнНеУч.НаимОрг);
			УстановитьЗначениеXDTO(Контент.OrganizationDetails, "LegalEntityId", ПолучитьЗначениеXDTO(СвИнНеУч, "Идентиф"));
			УстановитьЗначениеXDTO(Контент.OrganizationDetails, "OrganizationOrPersonInfo", ПолучитьЗначениеXDTO(СвИнНеУч, "ИныеСвед"));
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Адрес") Тогда
		Заполнить_Address_Torg2(Контент.OrganizationDetails.Address, Параметры.Адрес);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Контакт") Тогда
		УстановитьЗначениеXDTO(Контент.OrganizationDetails, "Phone", ПолучитьЗначениеXDTO(Параметры.Контакт, "Тлф"));
		УстановитьЗначениеXDTO(Контент.OrganizationDetails, "Email", ПолучитьЗначениеXDTO(Параметры.Контакт, "ЭлПочта"));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "БанкРекв") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.БанкРекв, "СвБанк") Тогда
			УстановитьЗначениеXDTO(Контент.OrganizationDetails, "BankName", ПолучитьЗначениеXDTO(Параметры.БанкРекв.СвБанк, "НаимБанк"));
			УстановитьЗначениеXDTO(Контент.OrganizationDetails, "BankId", ПолучитьЗначениеXDTO(Параметры.БанкРекв.СвБанк, "БИК"));
			УстановитьЗначениеXDTO(Контент.OrganizationDetails, "CorrespondentAccount", ПолучитьЗначениеXDTO(Параметры.БанкРекв.СвБанк, "КорСчет"));
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Контент.OrganizationDetails, "BankAccountNumber", ПолучитьЗначениеXDTO(Параметры.БанкРекв, "НомерСчета"));
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент.OrganizationDetails, "Okpo", ПолучитьЗначениеXDTO(Параметры, "ОКПО"));
	УстановитьЗначениеXDTO(Контент.OrganizationDetails, "Department", ПолучитьЗначениеXDTO(Параметры, "СтруктПодр"));
	УстановитьЗначениеXDTO(Контент.OrganizationDetails, "OrganizationAdditionalInfo", ПолучитьЗначениеXDTO(Параметры, "ИнфДляУчаст"));
	
КонецПроцедуры

Процедура Заполнить_CargoInspectionInformation(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СертСоотв") Тогда
		СертСоотв = СписокXDTOИзЭлемента(Параметры, "СертСоотв");
		Для Каждого Элемент Из СертСоотв Цикл
			Контент.CertificateConformityNumbers.CertificateConformityNumber.Добавить(Элемент);
		КонецЦикла;
	КонецЕсли;
	
	Заполнить_AccompanyingDocument(Контент.AccompanyingDocumentInfo, Параметры.СопрДок);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИнфПолСвОсм") Тогда
		Заполнить_AdditionalInfoId_Torg2(Контент.AdditionalInfoId, Параметры.ИнфПолСвОсм);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДатаОсм") Тогда
		УстановитьЗначениеXDTO(Контент, "ReceptionDate", ДатаXML_в_Дата1С(Параметры.ДатаОсм));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "ReceptionPlace", ПолучитьЗначениеXDTO(Параметры, "МестоСост"));
	УстановитьЗначениеXDTO(Контент, "ReceptionStartTime", ПолучитьЗначениеXDTO(Параметры, "ВремяНач"));
	УстановитьЗначениеXDTO(Контент, "ReceptionFinishTime", ПолучитьЗначениеXDTO(Параметры, "ВремяОконч"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДатаОтпр") Тогда
		УстановитьЗначениеXDTO(Контент, "DeliveryDate", ДатаXML_в_Дата1С(Параметры.ДатаОтпр));
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_ReceptionInformation(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "CargoArrivalDateTime", ДатаВремяXML_В_Дата1С(ПолучитьЗначениеXDTO(Параметры, "ПрибГруз")));
	УстановитьЗначениеXDTO(Контент, "DeliveryPoint", ДатаВремяXML_В_Дата1С(ПолучитьЗначениеXDTO(Параметры, "ВыдачГруз")));
	УстановитьЗначениеXDTO(Контент, "CargoOpeningDateTime", ДатаВремяXML_В_Дата1С(ПолучитьЗначениеXDTO(Параметры, "ВскрытГруз")));
	УстановитьЗначениеXDTO(Контент, "DeliveryDateTime", ДатаВремяXML_В_Дата1С(ПолучитьЗначениеXDTO(Параметры, "ДостГруз")));
	
КонецПроцедуры

Процедура Заполнить_AdditionalInfoId_Torg2(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "InfoFileId", ПолучитьЗначениеXDTO(Параметры, "ИдФайлИнфПол"));
	Заполнить_СписокIdValue(Контент.AdditionalInfo, Параметры, "ТекстИнф");
	
КонецПроцедуры

Процедура Заполнить_CommitmentType(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Code", ПолучитьЗначениеXDTO(Параметры, "КодВидОбяз"));
	УстановитьЗначениеXDTO(Контент, "Name", ПолучитьЗначениеXDTO(Параметры, "НаимВидОбяз"));
	
КонецПроцедуры

Процедура Заполнить_DocumentDetails(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Name", ПолучитьЗначениеXDTO(Параметры, "РеквНаимДок"));
	УстановитьЗначениеXDTO(Контент, "Number", ПолучитьЗначениеXDTO(Параметры, "РеквНомерДок"));
	УстановитьЗначениеXDTO(Контент, "Date", ДатаXML_в_Дата1С(ПолучитьЗначениеXDTO(Параметры, "РеквДатаДок")));
	УстановитьЗначениеXDTO(Контент, "FileID", ПолучитьЗначениеXDTO(Параметры, "РеквИдФайлДок"));
	УстановитьЗначениеXDTO(Контент, "ID", ПолучитьЗначениеXDTO(Параметры, "РеквИдДок"));
	УстановитьЗначениеXDTO(Контент, "StorageID", ПолучитьЗначениеXDTO(Параметры, "РИдСистХранД"));
	УстановитьЗначениеXDTO(Контент, "URL", ПолучитьЗначениеXDTO(Параметры, "РеквУРЛСистДок"));
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo", ПолучитьЗначениеXDTO(Параметры, "РеквДопСведДок"));
	
	Для Каждого РеквИдРекСост Из СписокXDTOИзЭлемента(Параметры, "РеквИдРекСост") Цикл
		EconomicEntity = НовыйЭлементСпискаXDTO(Контент.EconomicEntities);
		Заполнить_EconomicEntities(EconomicEntity, РеквИдРекСост);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_EconomicEntities(Контент, Параметры)
	
	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;

	ВидыЮрФизЛиц = Перечисление_ВидыЮрФизЛиц();
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИННЮЛ") Тогда
		
		УстановитьЗначениеXDTO(Контент, "Type", ВидыЮрФизЛиц.ЮридическоеЛицо);
		УстановитьЗначениеXDTO(Контент, "Inn", Параметры.ИННЮЛ);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИННФЛ") Тогда
		
		УстановитьЗначениеXDTO(Контент, "Type", ВидыЮрФизЛиц.ФизическоеЛицо);
		УстановитьЗначениеXDTO(Контент, "Inn", Параметры.ИННФЛ);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДаннИно") Тогда
		
		ДаннИно = Параметры.ДаннИно;
		
		Если ДаннИно.ИдСтат = "ИО" Тогда
			ВидЮрФизЛица = ВидыЮрФизЛиц.ИностраннаяОрганизация;
		Иначе
			ВидЮрФизЛица = ВидыЮрФизЛиц.ИностранныйГражданин;
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Контент, "Type", ВидЮрФизЛица);
		
		УстановитьЗначениеXDTO(Контент, "Name", ДаннИно.Наим);
		УстановитьЗначениеXDTO(Контент, "OtherInfo", ПолучитьЗначениеXDTO(ДаннИно, "ИныеСвед"));
		УстановитьЗначениеXDTO(Контент, "LegalEntityId", ПолучитьЗначениеXDTO(ДаннИно, "Идентиф"));
		УстановитьЗначениеXDTO(Контент, "Country", ДаннИно.НаимСтран);
		УстановитьЗначениеXDTO(Контент, "CountryCode", ДаннИно.КодСтр);
			
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НаимОИВ") Тогда
		
		УстановитьЗначениеXDTO(Контент, "Type", ВидыЮрФизЛиц.ОрганИсполнительнойВласти);
		УстановитьЗначениеXDTO(Контент, "Name", Параметры.НаимОИВ);
		
	КонецЕсли;	
	
КонецПроцедуры

Процедура Заполнить_AccompanyingDocumentTable(Контент, Параметры)
	
	Для Каждого СвСопрДок Из Параметры Цикл
		
		Item = НовыйЭлементСпискаXDTO(Контент.Item);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСопрДок, "МассаДок") Тогда
			Заполнить_WeightCargo(Item.DocumentWeight, СвСопрДок.МассаДок);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСопрДок, "МассаФакт") Тогда
			Заполнить_WeightCargo(Item.FactWeight, СвСопрДок.МассаФакт);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвСопрДок, "ИнфПолСопрДок") Тогда
			УстановитьЗначениеXDTO(Item.AdditionalInfo, "Id", СвСопрДок.ИнфПолСопрДок.Идентиф);
			УстановитьЗначениеXDTO(Item.AdditionalInfo, "Value", СвСопрДок.ИнфПолСопрДок.Значен);
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Item, "SealingMark", ПолучитьЗначениеXDTO(СвСопрДок, "ОтмПломб"));
		УстановитьЗначениеXDTO(Item, "PackingType", ПолучитьЗначениеXDTO(СвСопрДок, "ВидУпак"));
		УстановитьЗначениеXDTO(Item, "Quantity", ПолучитьЗначениеXDTO(СвСопрДок, "КолМест"));
		УстановитьЗначениеXDTO(Item, "CargoName", СвСопрДок.НаимГруз);
		УстановитьЗначениеXDTO(Item, "Unit", ПолучитьЗначениеXDTO(СвСопрДок, "ОКЕИ_Тов"));
		УстановитьЗначениеXDTO(Item, "UnitName", ПолучитьЗначениеXDTO(СвСопрДок, "НаимЕдИзм"));
		
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_PersonAcceptedCargoInfo(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "РабОргПок") Тогда
		// Работник организации покупателя
		Заполнить_Employee_Torg2(Контент.Employee, Параметры.РабОргПок);
	Иначе
		// Иное лицо "ИнЛицо"
		Заполнить_OtherIssuer_Torg2(Контент.OtherIssuer, Параметры.ИнЛицо);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_OtherCircumstancesAcceptanceInfo(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Перевозчик") Тогда
		Заполнить_ExtendedOrganizationInfo_Torg2(Контент.Carrier, Параметры.Перевозчик);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СопрТрансДок") Тогда
		Заполнить_AccompanyingDocument(Контент.AccompanyingTransferDocumentInfo, Параметры.СопрТрансДок);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "StorageConditions", ПолучитьЗначениеXDTO(Параметры, "УсловХран"));
	УстановитьЗначениеXDTO(Контент, "CargoActualState", ПолучитьЗначениеXDTO(Параметры, "СостТара"));
	УстановитьЗначениеXDTO(Контент, "MissingAssets", ПолучитьЗначениеXDTO(Параметры, "ОпредНедост"));
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo", ПолучитьЗначениеXDTO(Параметры, "ДопСв"));
	
КонецПроцедуры

Процедура Заполнить_СписокIdValue(СписокIdValue, Параметры, ИмяСвойства)
	
	Для Каждого Элемент Из СписокXDTOИзЭлемента(Параметры, ИмяСвойства) Цикл
		Заполнить_IdValue(НовыйЭлементСпискаXDTO(СписокIdValue), Элемент);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_IdValue(Контент, Параметры)
	
	// Записываем как есть, без сокращения пробелов
	Контент.Id		 = Параметры.Идентиф;
	Контент.Value	 = Параметры.Значен;
	
КонецПроцедуры

Процедура Заполнить_WeightCargo(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "GrossWeight", ПолучитьЗначениеXDTO(Параметры, "МассаБр"));
	УстановитьЗначениеXDTO(Контент, "NetWeight", ПолучитьЗначениеXDTO(Параметры, "МассаНет"));
	
КонецПроцедуры

Процедура Заполнить_Employee_Torg2(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Position", Параметры.Должность);
	УстановитьЗначениеXDTO(Контент, "EmployeeInfo", ПолучитьЗначениеXDTO(Параметры, "ИныеСвед"));
	УстановитьЗначениеXDTO(Контент, "EmployeeBase", Параметры.ОснПолн);
	УстановитьЗначениеXDTO(Контент, "LastName", Параметры.ФИО.Фамилия);
	УстановитьЗначениеXDTO(Контент, "FirstName", Параметры.ФИО.Имя);
	УстановитьЗначениеXDTO(Контент, "MiddleName", ПолучитьЗначениеXDTO(Параметры.ФИО, "Отчество"));
	
КонецПроцедуры

Процедура Заполнить_OtherIssuer_Torg2(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ПредОргПрин") Тогда
		
		// Представитель организации, уполномоченной на принятие товаров (груза)
		
		ПредОргПрин = Параметры.ПредОргПрин;
		
		УстановитьЗначениеXDTO(Контент, "Position", ПредОргПрин.Должность);
		УстановитьЗначениеXDTO(Контент, "EmployeeInfo", ПолучитьЗначениеXDTO(ПредОргПрин, "ИныеСвед"));
		УстановитьЗначениеXDTO(Контент, "EmployeeBase", ПолучитьЗначениеXDTO(ПредОргПрин, "ОснПолнПредПрин"));
		УстановитьЗначениеXDTO(Контент, "OrganizationName", ПредОргПрин.НаимОргПрин);
		УстановитьЗначениеXDTO(Контент, "OrganizationBase", ПолучитьЗначениеXDTO(ПредОргПрин, "ОснДоверОргПрин"));
		УстановитьЗначениеXDTO(Контент, "LastName", ПредОргПрин.ФИО.Фамилия);
		УстановитьЗначениеXDTO(Контент, "FirstName", ПредОргПрин.ФИО.Имя);
		УстановитьЗначениеXDTO(Контент, "MiddleName", ПолучитьЗначениеXDTO(ПредОргПрин.ФИО, "Отчество"));
		
	Иначе
		
		// Физическое лицо, которое уполномочено на принятие товаров (груза) "ФЛПрин"
		
		ФЛПрин = Параметры.ФЛПрин;
		
		УстановитьЗначениеXDTO(Контент, "EmployeeInfo", ПолучитьЗначениеXDTO(ФЛПрин, "ИныеСвед"));
		УстановитьЗначениеXDTO(Контент, "EmployeeBase", ПолучитьЗначениеXDTO(ФЛПрин, "ОснДоверФЛ"));
		УстановитьЗначениеXDTO(Контент, "LastName", ФЛПрин.ФИО.Фамилия);
		УстановитьЗначениеXDTO(Контент, "FirstName", ФЛПрин.ФИО.Имя);
		УстановитьЗначениеXDTO(Контент, "MiddleName", ПолучитьЗначениеXDTO(ФЛПрин.ФИО, "Отчество"));
		
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_AccompanyingDocument(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Name", Параметры.НаимСопрДок);
	УстановитьЗначениеXDTO(Контент, "Number", ПолучитьЗначениеXDTO(Параметры, "НомСопрДок"));
	УстановитьЗначениеXDTO(Контент, "Date", ДатаXML_в_Дата1С(ПолучитьЗначениеXDTO(Параметры, "ДатаСопрДок")));
	УстановитьЗначениеXDTO(Контент, "AdditionalIdentifier", ПолучитьЗначениеXDTO(Параметры, "ДопИдСопрДок"));
	
КонецПроцедуры

Процедура Заполнить_Address_Torg2(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "АдрРФ") Тогда
		
		// Адрес, указанный в Едином государственном реестре юридических лиц/почтовый адрес/адрес места жительства индивидуального предпринимателя (реквизиты адреса на территории Российской Федерации)
		
		АдрРФ = Параметры.АдрРФ;
		УстановитьЗначениеXDTO(Контент.RussianAddress, "ZipCode", ПолучитьЗначениеXDTO(АдрРФ, "Индекс"));
		УстановитьЗначениеXDTO(Контент.RussianAddress, "Region", АдрРФ.КодРегион);
		УстановитьЗначениеXDTO(Контент.RussianAddress, "Territory", ПолучитьЗначениеXDTO(АдрРФ, "Район"));
		УстановитьЗначениеXDTO(Контент.RussianAddress, "City", ПолучитьЗначениеXDTO(АдрРФ, "Город"));
		УстановитьЗначениеXDTO(Контент.RussianAddress, "Locality", ПолучитьЗначениеXDTO(АдрРФ, "НаселПункт"));
		УстановитьЗначениеXDTO(Контент.RussianAddress, "Street", ПолучитьЗначениеXDTO(АдрРФ, "Улица"));
		УстановитьЗначениеXDTO(Контент.RussianAddress, "Building", ПолучитьЗначениеXDTO(АдрРФ, "Дом"));
		УстановитьЗначениеXDTO(Контент.RussianAddress, "Block", ПолучитьЗначениеXDTO(АдрРФ, "Корпус"));
		УстановитьЗначениеXDTO(Контент.RussianAddress, "Apartment", ПолучитьЗначениеXDTO(АдрРФ, "Кварт"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "АдрИнф") Тогда
		
		// Адрес, указанный в Едином государственном реестре юридических лиц/почтовый адрес/адрес места жительства индивидуального предпринимателя (информация об адресе, в том числе об адресе за пределами территории Российской Федерации)
		
		АдрИнф = Параметры.АдрИнф;
		УстановитьЗначениеXDTO(Контент.ForeignAddress, "Country", АдрИнф.КодСтр);
		УстановитьЗначениеXDTO(Контент.ForeignAddress, "Address", АдрИнф.АдрТекст);
		
	Иначе
		
		// Уникальный номер адреса объекта адресации в государственном адресном реестре "КодГАР"
		
		УстановитьЗначениеXDTO(Контент, "AddressCode", Параметры.КодГАР);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_AcceptanceResults(Контент, Параметры)
	
	Для Каждого РезПрием Из Параметры Цикл
		
		AcceptanceResult = НовыйЭлементСпискаXDTO(Контент.AcceptanceResult);
		
		Ценности = РезПрием.Ценности;
		Assets = AcceptanceResult.Assets;
		
		УстановитьЗначениеXDTO(Assets, "AccompanyingDocumentLineNumber", ПолучитьЗначениеXDTO(Ценности, "НомСтрСопрДок"));
		УстановитьЗначениеXDTO(Assets, "Product", ПолучитьЗначениеXDTO(Ценности, "НаимТов"));
		УстановитьЗначениеXDTO(Assets, "ItemCharact", ПолучитьЗначениеXDTO(Ценности, "ХарактТов"));
		УстановитьЗначениеXDTO(Assets, "ItemKind", ПолучитьЗначениеXDTO(Ценности, "СортТов"));
		УстановитьЗначениеXDTO(Assets, "ItemArticle", ПолучитьЗначениеXDTO(Ценности, "АртикулТов"));
		УстановитьЗначениеXDTO(Assets, "ItemVendorCode", ПолучитьЗначениеXDTO(Ценности, "КодТов"));
		УстановитьЗначениеXDTO(Assets, "PassportNumber", ПолучитьЗначениеXDTO(Ценности, "Паспорт"));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(РезПрием, "ПоДокум") Тогда
			Заполнить_AcceptedProductData(AcceptanceResult.AccordingToSellerDocument, РезПрием.ПоДокум);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(РезПрием, "ПоФакту") Тогда
			Заполнить_AcceptedProductData(AcceptanceResult.ActuallyAccepted, РезПрием.ПоФакту);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(РезПрием, "Брак") Тогда
			Заполнить_DiscrepancyData(AcceptanceResult.Reject, РезПрием.Брак);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(РезПрием, "Бой") Тогда
			Заполнить_DiscrepancyData(AcceptanceResult.Breakage, РезПрием.Бой);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(РезПрием, "Недостача") Тогда
			Заполнить_DiscrepancyData(AcceptanceResult.Shortfall, РезПрием.Недостача);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(РезПрием, "Излишки") Тогда
			Заполнить_DiscrepancyData(AcceptanceResult.Surplus, РезПрием.Излишки);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(РезПрием, "ЦенОтклон") Тогда
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(РезПрием.ЦенОтклон, "ПолДельта") Тогда
				Заполнить_Delta(AcceptanceResult.PriceDeviations.PositiveDeviations, РезПрием.ЦенОтклон.ПолДельта);
			Иначе
				Заполнить_Delta(AcceptanceResult.PriceDeviations.NegativeDeviations, РезПрием.ЦенОтклон.ОтрицатДельта);
			КонецЕсли;
			
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(РезПрием, "ИнфПолРезПрием") Тогда
			Заполнить_СписокIdValue(AcceptanceResult.AdditionalInfos.AdditionalInfo, РезПрием, "ИнфПолРезПрием");
		КонецЕсли;
		
		УстановитьЗначениеXDTO(AcceptanceResult, "Unit", ПолучитьЗначениеXDTO(РезПрием, "ОКЕИ_Тов"));
		УстановитьЗначениеXDTO(AcceptanceResult, "UnitName", ПолучитьЗначениеXDTO(РезПрием, "НаимЕдИзм"));
		УстановитьЗначениеXDTO(AcceptanceResult, "AcceptanceDiscrepancyInfo", ПолучитьЗначениеXDTO(РезПрием, "РасхРабУслПрав"));
		
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_AttachedDocuments(Контент, Параметры)
	
	Для Каждого ПрилДок Из Параметры Цикл
		
		AttachedDocument = НовыйЭлементСпискаXDTO(Контент.AttachedDocument);
		
		УстановитьЗначениеXDTO(AttachedDocument, "Type", ПрилДок.ВидПрилДок);
		УстановитьЗначениеXDTO(AttachedDocument, "Number", ПрилДок.НомерПрилДок);
		УстановитьЗначениеXDTO(AttachedDocument, "Date", ДатаXML_в_Дата1С(ПрилДок.ДатаПрилДок));
		
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_AcceptedProductData(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НомСредИдентТов") Тогда
		Заполнить_ItemIdentificationNumbersTorg2(Контент.ItemIdentificationNumbers.ItemIdentificationNumber,
												 СписокXDTOИзЭлемента(Параметры, "НомСредИдентТов"));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Quantity", Параметры.КолТовПрин);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ЦенаПрин") Тогда
		УстановитьЗначениеXDTO(Контент, "Price", ПолучитьЗначениеXDTO(Параметры, "ЦенаПрин"));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СтавНДСПрин") Тогда
		УстановитьЗначениеXDTO(Контент, "TaxRate", TaxRate_Из_НалСт(ПолучитьЗначениеXDTO(Параметры, "СтавНДСПрин")));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СтоимТовНДСПрин") Тогда
		УстановитьЗначениеXDTO(Контент, "Subtotal", ПолучитьЗначениеXDTO(Параметры, "СтоимТовНДСПрин"));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СумНДСПрин") Тогда
		УстановитьЗначениеXDTO(Контент, "Vat", ПолучитьЗначениеXDTO(Параметры, "СумНДСПрин"));
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_DiscrepancyData(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НомСредИдентТов") Тогда
		Заполнить_ItemIdentificationNumbersTorg2(Контент.ItemIdentificationNumbers.ItemIdentificationNumber,
												 СписокXDTOИзЭлемента(Параметры, "НомСредИдентТов"));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Quantity", Параметры.КолТовРасх);
	УстановитьЗначениеXDTO(Контент, "Subtotal", ПолучитьЗначениеXDTO(Параметры, "СтоимТовНДСРасх"));
	УстановитьЗначениеXDTO(Контент, "Vat", ПолучитьЗначениеXDTO(Параметры, "СумНДСРасх"));
	УстановитьЗначениеXDTO(Контент, "Explanations", ПолучитьЗначениеXDTO(Параметры, "ПоясненияРасх"));
	
КонецПроцедуры

Процедура Заполнить_Delta(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Price", ПолучитьЗначениеXDTO(Параметры, "ДельтаЦена"));
	УстановитьЗначениеXDTO(Контент, "Subtotal", Параметры.ДельтаСтоимНДС);
	УстановитьЗначениеXDTO(Контент, "Vat", ПолучитьЗначениеXDTO(Параметры, "ДельтаСумНДС"));
	УстановитьЗначениеXDTO(Контент, "Explanations", ПолучитьЗначениеXDTO(Параметры, "ПояснДельта"));
	
КонецПроцедуры

Процедура Заполнить_ItemIdentificationNumbersTorg2(Контент, Параметры)
	
	Для Каждого НомСредИдентТов Из Параметры Цикл
		
		ItemIdentificationNumber = НовыйЭлементСпискаXDTO(Контент);
		
		КоллекцияКодов = СписокXDTOИзЭлемента(НомСредИдентТов, "КИЗ");
		Для Каждого Значение Из КоллекцияКодов Цикл
			ItemIdentificationNumber.Unit.Добавить(Значение);
		КонецЦикла;
		
		КоллекцияКодов = СписокXDTOИзЭлемента(НомСредИдентТов, "НомУпак");
		Для Каждого Значение Из КоллекцияКодов Цикл
			ItemIdentificationNumber.PackageId.Добавить(Значение);
		КонецЦикла;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(НомСредИдентТов, "ИдентТрансУпак") Тогда
			УстановитьЗначениеXDTO(ItemIdentificationNumber, "TransPackageId", НомСредИдентТов.ИдентТрансУпак);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_ItemIdentificationNumbers(Контент, Параметры)
	
	Для Каждого НомСредИдентТов Из Параметры Цикл
		
		ItemIdentificationNumber = НовыйЭлементСпискаXDTO(Контент);
		
		КоллекцияКодов = СписокXDTOИзЭлемента(НомСредИдентТов, "КИЗ");
		Для Каждого Значение Из КоллекцияКодов Цикл
			ItemIdentificationNumber.Units.Добавить(Значение);
		КонецЦикла;
		
		КоллекцияКодов = СписокXDTOИзЭлемента(НомСредИдентТов, "НомУпак");
		Для Каждого Значение Из КоллекцияКодов Цикл
			ItemIdentificationNumber.PackageIds.Добавить(Значение);
		КонецЦикла;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(НомСредИдентТов, "ИдентТрансУпак") Тогда
			УстановитьЗначениеXDTO(ItemIdentificationNumber, "TransPackageId", НомСредИдентТов.ИдентТрансУпак);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_Torg2RecipientTitle(Контент, Параметры)
	
	Документ = Параметры.Документ;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ.СвИтПрием, "ИнфПол") Тогда
		Заполнить_AdditionalInfoId_Torg2(Контент.AdditionalInfoId, Документ.СвИтПрием.ИнфПол);
	КонецЕсли;
	
	Заполнить_Signers(Контент.Signers, СписокXDTOИзЭлемента(Документ, "Подписант"));
	
	УстановитьЗначениеXDTO(Контент, "DocumentCreator", Документ.НаимЭконСубСост);
	УстановитьЗначениеXDTO(Контент, "DocumentCreatorBase", ПолучитьЗначениеXDTO(Документ, "ОснДоверОргСост"));
	УстановитьЗначениеXDTO(Контент, "CompilationInfoDate", ДатаXML_в_Дата1С(Документ.СвИтПрием.ДатаСвИтПрием));
	УстановитьЗначениеXDTO(Контент, "TotalAcceptanceContent", Документ.СвИтПрием.СодСвИтПрием);
	
КонецПроцедуры

Процедура Заполнить_ReconciliationActSenderTitle(Контент, Параметры)
	
	Документ = Параметры.Документ;
	
	ДатаИнфОтпр = ДатаXML_в_Дата1С(Документ.ДатаИнфОтпр);
	ДатаНачПер = ДатаXML_в_Дата1С(Документ.ДатаНачПер);
	ДатаОкПер = ДатаXML_в_Дата1С(Документ.ДатаОкПер);
	
	УстановитьЗначениеXDTO(Контент, "TimeCreateFile", Документ.ВрИнфОтпр);
	УстановитьЗначениеXDTO(Контент, "DateCreateFile", ДатаИнфОтпр);
	УстановитьЗначениеXDTO(Контент, "ReconcilationDateFrom", ДатаНачПер);
	УстановитьЗначениеXDTO(Контент, "ReconcilationDateTo", ДатаОкПер);
	УстановитьЗначениеXDTO(Контент, "ActNumber", Документ.НомерАкт);
	
	Заполнить_AdditionalInfoReconciliationAct(Контент.ReconciliationAct.AdditionalInfoReconciliationAct, Документ.СвДокКрАкт);
	
	Заполнить_SenderReconciliationActTable(Контент.ReconciliationAct.ReconciliationActTable, Документ.ТаблАкт);
	
	Заполнить_Signer_ReconciliationAct(Контент.ReconciliationAct.Signer, Документ.Подписант);
	
КонецПроцедуры

Процедура Заполнить_AdditionalInfoReconciliationAct(Контент, Параметры)
	
	Заполнить_ExtendedOrganizationInfo_ReconciliationAct(Контент.Sender, Параметры.СвОтпр);
	Заполнить_ExtendedOrganizationInfo_ReconciliationAct(Контент.Recipient, Параметры.СвПол);
	
	УстановитьЗначениеXDTO(Контент, "Currency", Параметры.КодОКВ);
	
КонецПроцедуры

Процедура Заполнить_ExtendedOrganizationInfo_ReconciliationAct(Контент, Параметры)

	Если Параметры = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвЮЛУч") Тогда
		
		// Юр. лицо
		УстановитьЗначениеXDTO(Контент, "Type", "LegalEntity");
		УстановитьЗначениеXDTO(Контент, "Name", Параметры.ИдСв.СвЮЛУч.НаимОрг);
		УстановитьЗначениеXDTO(Контент, "Inn", ПолучитьЗначениеXDTO(Параметры.ИдСв.СвЮЛУч, "ИННЮЛ"));
		УстановитьЗначениеXDTO(Контент, "Kpp", ПолучитьЗначениеXDTO(Параметры.ИдСв.СвЮЛУч, "КПП"));
		УстановитьЗначениеXDTO(Контент, "Ogrn", ПолучитьЗначениеXDTO(Параметры.ИдСв.СвЮЛУч, "ОГРН"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвИП") Тогда
		
		// ИП
		УстановитьЗначениеXDTO(Контент, "Type", "IndividualEntity");
		УстановитьЗначениеXDTO(Контент, "Name", XML_СтрокаФИО(Параметры.ИдСв.СвИП.ФИО));
		УстановитьЗначениеXDTO(Контент, "Inn", ПолучитьЗначениеXDTO(Параметры.ИдСв.СвИП, "ИННФЛ"));
		УстановитьЗначениеXDTO(Контент, "Ogrn", ПолучитьЗначениеXDTO(Параметры.ИдСв.СвИП, "ОГРНИП"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвИнНеУч") Тогда
		
		// Иностранное лицо, не состоящее на учете в налоговых органах
		УстановитьЗначениеXDTO(Контент, "Type", "ForeignEntity");
		УстановитьЗначениеXDTO(Контент, "Name", Параметры.ИдСв.СвИнНеУч.НаимОрг);
		УстановитьЗначениеXDTO(Контент, "LegalEntityId", ПолучитьЗначениеXDTO(Параметры.ИдСв.СвИнНеУч, "Идентиф"));
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвФЛ") Тогда
		
		// Физ. лицо
		УстановитьЗначениеXDTO(Контент, "Type", "PhysicalEntity");
		УстановитьЗначениеXDTO(Контент, "Name", XML_СтрокаФИО(Параметры.ИдСв.СвФЛ.ФИО));
		УстановитьЗначениеXDTO(Контент, "Inn", ПолучитьЗначениеXDTO(Параметры.ИдСв.СвФЛ, "ИННФЛ"));
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Department", ПолучитьЗначениеXDTO(Параметры, "СтруктПодр"));
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo", ПолучитьЗначениеXDTO(Параметры, "ИнфДляУчаст"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Контакт") Тогда
		УстановитьЗначениеXDTO(Контент, "Phone", ПолучитьЗначениеXDTO(Параметры.Контакт, "Тлф"));
		УстановитьЗначениеXDTO(Контент, "Email", ПолучитьЗначениеXDTO(Параметры.Контакт, "ЭлПочта"));
		УстановитьЗначениеXDTO(Контент, "AdditionalCont", ПолучитьЗначениеXDTO(Параметры.Контакт, "ИнКонт"));
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_Signer_ReconciliationAct(Контент, Параметры)
	
	Заполнить_Official(Контент.Official, Параметры);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СвДовер") Тогда
		Заполнить_DigitalAttorneyInfo(Контент.DigitalAttorneyInfo, Параметры.СвДовер);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СвДоверБум") Тогда
		Заполнить_PaperAttorneyInfo(Контент.PaperAttorneyInfo, Параметры.СвДоверБум);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СтатПодп") Тогда
		
		СтатусПодписанта = SignerStatus405(Параметры.СтатПодп);
		УстановитьЗначениеXDTO(Контент, "SignerStatus", СтатусПодписанта);
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ТипПодпис") Тогда
		
		ТипПодписи = SignatureType405(Параметры.ТипПодпис);
		УстановитьЗначениеXDTO(Контент, "SignatureType", ТипПодписи);
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИдСистХран") Тогда
		УстановитьЗначениеXDTO(Контент, "IdSystemStorage", Параметры.ИдСистХран);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Должн") Тогда
		УстановитьЗначениеXDTO(Контент, "JobTitle", Параметры.Должн);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_DigitalAttorneyInfo(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НомДовер") Тогда
		УстановитьЗначениеXDTO(Контент, "Number", Параметры.НомДовер);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДатаНач") Тогда
		УстановитьЗначениеXDTO(Контент, "Date", ДатаXML_в_Дата1С(Параметры.ДатаНач));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ВнНомДовер") Тогда
		УстановитьЗначениеXDTO(Контент, "InternalNumber", Параметры.ВнНомДовер);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДатаВнРегДовер") Тогда
		УстановитьЗначениеXDTO(Контент, "InternalDate", ДатаXML_в_Дата1С(Параметры.ДатаВнРегДовер));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СведСистОтм") Тогда
		УстановитьЗначениеXDTO(Контент, "CancelAdditionalInfo", Параметры.СведСистОтм);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_PaperAttorneyInfo(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДатаДовер") Тогда
		УстановитьЗначениеXDTO(Контент, "Date", ДатаXML_в_Дата1С(Параметры.ДатаДовер));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ВнНомДовер") Тогда
		УстановитьЗначениеXDTO(Контент, "InternalNumber", Параметры.ВнНомДовер);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СвИдДовер") Тогда
		УстановитьЗначениеXDTO(Контент, "IssuerAdditionalInfo", Параметры.СвИдДовер);
	КонецЕсли;
	
	Заполнить_Official(Контент.Official, Параметры);
	
КонецПроцедуры

Процедура Заполнить_SenderReconciliationActTable(Контент, Параметры)
	
	СведенияОДоговорах = СписокXDTOИзЭлемента(Параметры, "ДогСв");
	
	Для Каждого ДогСв Из СведенияОДоговорах Цикл
		
		ContractInfo = НовыйЭлементСпискаXDTO(Контент.ContractInfo);
		
		Заполнить_SenderContractInfo(ContractInfo, ДогСв);
		
	КонецЦикла;
	
	УстановитьЗначениеXDTO(Контент, "BalanceDebitFrom", Параметры.СальдоНачДеб);
	УстановитьЗначениеXDTO(Контент, "BalanceAmountFrom", Параметры.СальдоНачКр);
	УстановитьЗначениеXDTO(Контент, "TurnoverDebit", Параметры.ОборотДеб);
	УстановитьЗначениеXDTO(Контент, "TurnoverAmount", Параметры.ОборотКр);
	УстановитьЗначениеXDTO(Контент, "BalanceDebitEnd", Параметры.СальдоКонДеб);
	УстановитьЗначениеXDTO(Контент, "BalanceAmountEnd", Параметры.СальдоКонКр);
	
КонецПроцедуры

Процедура Заполнить_SenderContractInfo(Контент, Параметры)
	
	СведенияОДокументах = СписокXDTOИзЭлемента(Параметры, "ДокСв");
	
	Для Каждого ДокСв Из СведенияОДокументах Цикл
		
		DocumentInfo = НовыйЭлементСпискаXDTO(Контент.DocumentInfo);
		
		Заполнить_SenderDocumentInfo(DocumentInfo, ДокСв);
		
	КонецЦикла;
	
	ContractDate = ПолучитьЗначениеXDTO(Параметры, "ДатаДог");
	
	УстановитьЗначениеXDTO(Контент, "ContractId", ПолучитьЗначениеXDTO(Параметры, "ИдДог"));
	УстановитьЗначениеXDTO(Контент, "ContractAdditionalInfo", ПолучитьЗначениеXDTO(Параметры, "ОписТипДог"));
	УстановитьЗначениеXDTO(Контент, "ContractNumber", ПолучитьЗначениеXDTO(Параметры, "НомДог"));
	УстановитьЗначениеXDTO(Контент, "ContractDate", ДатаXML_в_Дата1С(ContractDate));
	УстановитьЗначениеXDTO(Контент, "BalanceDebitFrom", ПолучитьЗначениеXDTO(Параметры, "СальдоНачДеб"));
	УстановитьЗначениеXDTO(Контент, "BalanceAmountFrom", ПолучитьЗначениеXDTO(Параметры, "СальдоНачКр"));
	УстановитьЗначениеXDTO(Контент, "TurnoverDebit", ПолучитьЗначениеXDTO(Параметры, "ОборотДеб"));
	УстановитьЗначениеXDTO(Контент, "TurnoverAmount", ПолучитьЗначениеXDTO(Параметры, "ОборотКр"));
	УстановитьЗначениеXDTO(Контент, "BalanceDebitEnd", ПолучитьЗначениеXDTO(Параметры, "СальдоКонДеб"));
	УстановитьЗначениеXDTO(Контент, "BalanceAmountEnd", ПолучитьЗначениеXDTO(Параметры, "СальдоКонКр"));
	
КонецПроцедуры

Процедура Заполнить_SenderDocumentInfo(Контент, Параметры)
	
	СведенияОбОперациях = СписокXDTOИзЭлемента(Параметры, "СвОпер");
	
	Для Каждого СвОпер Из СведенияОбОперациях Цикл
		
		OperationInfo = НовыйЭлементСпискаXDTO(Контент.OperationInfo);
		
		Заполнить_SenderOperationInfo(OperationInfo, СвОпер);
		
	КонецЦикла;
	
	УстановитьЗначениеXDTO(Контент, "DocumentName", Параметры.НаимДок);
	УстановитьЗначениеXDTO(Контент, "DocumentNumber", Параметры.НомДок);
	УстановитьЗначениеXDTO(Контент, "DocumentDate", ДатаXML_в_Дата1С(Параметры.ДатаДок));
	УстановитьЗначениеXDTO(Контент, "DocumentId", ПолучитьЗначениеXDTO(Параметры, "ИдДок"));
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo", ПолучитьЗначениеXDTO(Параметры, "ДопИнф"));
	
КонецПроцедуры

Процедура Заполнить_SenderOperationInfo(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "StringNumber", Параметры.НомСтр);
	УстановитьЗначениеXDTO(Контент, "OperationDate", ДатаXML_в_Дата1С(Параметры.ДатаОпер));
	УстановитьЗначениеXDTO(Контент, "OperationName", Параметры.НаимОпер);
	УстановитьЗначениеXDTO(Контент, "SumDebit", ПолучитьЗначениеXDTO(Параметры, "СумДебет"));
	УстановитьЗначениеXDTO(Контент, "SumAmount", ПолучитьЗначениеXDTO(Параметры, "СумКредит"));
	
КонецПроцедуры

Процедура Заполнить_ReconciliationActRecipientTitle(Контент, Параметры)
	
	Документ = Параметры.Документ;
	
	ДатаИнфПол = ДатаXML_в_Дата1С(Документ.ДатаИнфПол);
	ПризнРазн = SignDisagreement405(Параметры.ПризнРазн);
	
	УстановитьЗначениеXDTO(Контент, "SignDisagreement", ПризнРазн);
	УстановитьЗначениеXDTO(Контент, "DateCreateFile", ДатаИнфПол);
	УстановитьЗначениеXDTO(Контент, "TimeCreateFile", Документ.ВрИнфПол);
	
	Заполнить_IdentityInfoSender(Контент.ReconciliationAct.IdentityInfoSender, Документ.ИдИнфОтпр);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ТаблАкт") Тогда
		Заполнить_RecipientReconciliationActTable(Контент.ReconciliationAct.ReconciliationActTable, Документ.ТаблАкт);
	КонецЕсли;
	
	Заполнить_Signer_ReconciliationAct(Контент.ReconciliationAct.Signer, Документ.Подписант);
	
КонецПроцедуры

Процедура Заполнить_IdentityInfoSender(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "BaseIdFile", Параметры.ИдФайлИнфОтпр);
	УстановитьЗначениеXDTO(Контент, "DateFileInfoSender", ДатаXML_в_Дата1С(Параметры.ДатФайлИнфОтпр));
	УстановитьЗначениеXDTO(Контент, "TimeFileInfoSender", Параметры.ВрФайлИнфОтпр);
	УстановитьЗначениеXDTO(Контент, "SignSender", Параметры.ЭП);
	
КонецПроцедуры

Процедура Заполнить_RecipientReconciliationActTable(Контент, Параметры)
	
	СведенияОДоговорах = СписокXDTOИзЭлемента(Параметры, "ДогСв");
	
	Для Каждого ДогСв Из СведенияОДоговорах Цикл
		
		ContractInfo = НовыйЭлементСпискаXDTO(Контент.ContractInfo);
		
		Заполнить_RecipientContractInfo(ContractInfo, ДогСв);
		
	КонецЦикла;
	
	УстановитьЗначениеXDTO(Контент, "BalanceDebitFrom", ПолучитьЗначениеXDTO(Параметры, "СальдоНачДеб"));
	УстановитьЗначениеXDTO(Контент, "BalanceAmountFrom", ПолучитьЗначениеXDTO(Параметры, "СальдоНачКр"));
	УстановитьЗначениеXDTO(Контент, "TurnoverDebit", ПолучитьЗначениеXDTO(Параметры, "ОборотДеб"));
	УстановитьЗначениеXDTO(Контент, "TurnoverAmount", ПолучитьЗначениеXDTO(Параметры, "ОборотКр"));
	УстановитьЗначениеXDTO(Контент, "BalanceDebitEnd", ПолучитьЗначениеXDTO(Параметры, "СальдоКонДеб"));
	УстановитьЗначениеXDTO(Контент, "BalanceAmountEnd", ПолучитьЗначениеXDTO(Параметры, "СальдоКонКр"));
	УстановитьЗначениеXDTO(Контент, "TotalConsumptionDebit", ПолучитьЗначениеXDTO(Параметры, "ИтогоРасхДеб"));
	УстановитьЗначениеXDTO(Контент, "TotalConsumptionAmount", ПолучитьЗначениеXDTO(Параметры, "ИтогоРасхКр"));
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo", ПолучитьЗначениеXDTO(Параметры, "ДопИнф"));
	
КонецПроцедуры

Процедура Заполнить_RecipientContractInfo(Контент, Параметры)
	
	СведенияОДокументах = СписокXDTOИзЭлемента(Параметры, "ДокСв");
	
	Для Каждого ДокСв Из СведенияОДокументах Цикл
		
		DocumentInfo = НовыйЭлементСпискаXDTO(Контент.DocumentInfo);
		
		Заполнить_RecipientDocumentInfo(DocumentInfo, ДокСв);
		
	КонецЦикла;
	
	ContractDate = ПолучитьЗначениеXDTO(Параметры, "ДатаДог");
	
	УстановитьЗначениеXDTO(Контент, "ContractId", ПолучитьЗначениеXDTO(Параметры, "ИдДог"));
	УстановитьЗначениеXDTO(Контент, "ContractAdditionalInfo", ПолучитьЗначениеXDTO(Параметры, "ОписТипДог"));
	УстановитьЗначениеXDTO(Контент, "ContractNumber", ПолучитьЗначениеXDTO(Параметры, "НомДог"));
	УстановитьЗначениеXDTO(Контент, "ContractDate", ДатаXML_в_Дата1С(ContractDate));
	УстановитьЗначениеXDTO(Контент, "BalanceDebitFrom", ПолучитьЗначениеXDTO(Параметры, "СальдоНачДеб"));
	УстановитьЗначениеXDTO(Контент, "BalanceAmountFrom", ПолучитьЗначениеXDTO(Параметры, "СальдоНачКр"));
	УстановитьЗначениеXDTO(Контент, "TurnoverDebit", ПолучитьЗначениеXDTO(Параметры, "ОборотДеб"));
	УстановитьЗначениеXDTO(Контент, "TurnoverAmount", ПолучитьЗначениеXDTO(Параметры, "ОборотКр"));
	УстановитьЗначениеXDTO(Контент, "BalanceDebitEnd", ПолучитьЗначениеXDTO(Параметры, "СальдоКонДеб"));
	УстановитьЗначениеXDTO(Контент, "BalanceAmountEnd", ПолучитьЗначениеXDTO(Параметры, "СальдоКонКр"));
	УстановитьЗначениеXDTO(Контент, "TotalConsumptionDebit", ПолучитьЗначениеXDTO(Параметры, "ИтогоРасхДеб"));
	УстановитьЗначениеXDTO(Контент, "TotalConsumptionAmount", ПолучитьЗначениеXDTO(Параметры, "ИтогоРасхКр"));
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo", ПолучитьЗначениеXDTO(Параметры, "ДопИнф"));
	
КонецПроцедуры

Процедура Заполнить_RecipientDocumentInfo(Контент, Параметры)
	
	СведенияОбОперациях = СписокXDTOИзЭлемента(Параметры, "СвОпер");
	
	Для Каждого СвОпер Из СведенияОбОперациях Цикл
		
		OperationInfo = НовыйЭлементСпискаXDTO(Контент.OperationInfo);
		
		Заполнить_RecipientOperationInfo(OperationInfo, СвОпер);
		
	КонецЦикла;
	
	УстановитьЗначениеXDTO(Контент, "DocumentName", Параметры.НаимДок);
	УстановитьЗначениеXDTO(Контент, "DocumentNumber", Параметры.НомДок);
	УстановитьЗначениеXDTO(Контент, "DocumentDate", ДатаXML_в_Дата1С(Параметры.ДатаДок));
	УстановитьЗначениеXDTO(Контент, "TotalConsumption", Параметры.ИтогоРасх);
	УстановитьЗначениеXDTO(Контент, "DocumentId", ПолучитьЗначениеXDTO(Параметры, "ИдДок"));
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo", ПолучитьЗначениеXDTO(Параметры, "ДопИнф"));
	
КонецПроцедуры

Процедура Заполнить_RecipientOperationInfo(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "StringNumber", Параметры.НомСтр);
	УстановитьЗначениеXDTO(Контент, "OperationDate", ДатаXML_в_Дата1С(Параметры.ДатаОпер));
	УстановитьЗначениеXDTO(Контент, "OperationName", Параметры.НаимОпер);
	УстановитьЗначениеXDTO(Контент, "SumDebit", ПолучитьЗначениеXDTO(Параметры, "СумДебет"));
	УстановитьЗначениеXDTO(Контент, "SumAmount", ПолучитьЗначениеXDTO(Параметры, "СумКредит"));
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo", ПолучитьЗначениеXDTO(Параметры, "ДопИнф"));
	
КонецПроцедуры

Процедура ЗаполнитьИтогиУКД(Table)
	
	Vat_dec						= 0;
	Total_dec					= 0;
	TotalWithVatExcluded_dec	= 0;
	
	Vat_inc						= 0;
	Total_inc					= 0;
	TotalWithVatExcluded_inc	= 0;
	
	Для Каждого Item Из Table.Items Цикл
		
		// dec
		Если Item.AmountsDec.Установлено("Total") Тогда
			Total_dec = Total_dec + Item.AmountsDec.Total;
		КонецЕсли;
		
		Если Item.AmountsDec.Установлено("TotalWithVatExcluded") Тогда
			TotalWithVatExcluded_dec = TotalWithVatExcluded_dec + Item.AmountsDec.TotalWithVatExcluded;
		КонецЕсли;
		
		Если Item.AmountsDec.Установлено("Vat") Тогда
			Vat_dec = Vat_dec + Item.AmountsDec.Vat;
		КонецЕсли;
		
		// inc
		Если Item.AmountsDec.Установлено("Total") Тогда
			Total_inc = Total_inc + Item.AmountsDec.Total;
		КонецЕсли;
		
		Если Item.AmountsDec.Установлено("TotalWithVatExcluded") Тогда
			TotalWithVatExcluded_inc = TotalWithVatExcluded_inc + Item.AmountsDec.TotalWithVatExcluded;
		КонецЕсли;
		
		Если Item.AmountsDec.Установлено("Vat") Тогда
			Vat_inc = Vat_inc + Item.AmountsDec.Vat;
		КонецЕсли;
		
	КонецЦикла;
	
	Table.TotalsDec.Vat						= Vat_dec;
	Table.TotalsDec.TotalWithVatExcluded	= TotalWithVatExcluded_dec;
	Table.TotalsDec.Total					= Total_dec;
	
	Table.TotalsInc.Vat						= Vat_inc;
	Table.TotalsInc.TotalWithVatExcluded	= TotalWithVatExcluded_inc;
	Table.TotalsInc.Total					= Total_inc;
	
КонецПроцедуры

Процедура ЗаполнитьИтогиУПД(Table)
	
	Vat						= 0;
	Total					= 0;
	TotalWithVatExcluded	= 0;
	
	Для Каждого Item Из Table.Items Цикл
		
		Если Item.Установлено("Subtotal") Тогда
			Total = Total + Item.Subtotal;
		КонецЕсли;
		
		Если Item.Установлено("SubtotalWithVatExcluded") Тогда
			TotalWithVatExcluded = TotalWithVatExcluded + Item.SubtotalWithVatExcluded;
		КонецЕсли;
		
		Если Item.Установлено("Vat") Тогда
			Vat = Vat + Item.Vat;
		КонецЕсли;
		
	КонецЦикла;
	
	Table.Vat					= Vat;
	Table.TotalWithVatExcluded	= TotalWithVatExcluded;
	Table.Total					= Total;
	
КонецПроцедуры

// По длине ИНН определяет, кто у нас - ФЛ или ЮЛ,
// иностранные организации определять не умеет, это отдельно надо делать.
//
Процедура ЗаполнитьТипКонтрагента(ExtendedOrgInfo)
	
	Если СтрДлина(ExtendedOrgInfo.Inn) = 12 Тогда
		ExtendedOrgInfo.Type = "IndividualEntity";
	ИначеЕсли СтрДлина(ExtendedOrgInfo.Inn) = 10 Тогда
		ExtendedOrgInfo.Type = "LegalEntity";  // иностранные придется вручную вносить
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_ФИО(Контент, Парамеры)
	
	УстановитьЗначениеXDTO(Контент, "Surname", Парамеры.Фамилия);
	УстановитьЗначениеXDTO(Контент, "FirstName", Парамеры.Имя);
	
	Отчество = ПолучитьЗначениеXDTO(Парамеры, "Отчество");
	УстановитьЗначениеXDTO(Контент, "Patronymic", Отчество);
	
КонецПроцедуры

Процедура Скопировать_StructedAdditionalInfos(Приемник, Источник)

	Для Каждого Стр Из Источник Цикл
		
		НовЭлемент = НовыйЭлементСпискаXDTO(Приемник);
		ЗаполнитьСвойстваПриемникаXDTO(НовЭлемент, Стр);
	
	КонецЦикла;

КонецПроцедуры


Процедура Скопировать_TransferBases(Приемник, Источник)

	Для Каждого Стр Из Источник Цикл
		
		НовЭлемент = НовыйЭлементСпискаXDTO(Приемник);
		ЗаполнитьСвойстваПриемникаXDTO(НовЭлемент, Стр);
		
		Если Стр.DocumentName = "Отсутствует" Тогда
			НовЭлемент.DocumentName = "Без документа-основания";	
		КонецЕсли; 
	
	КонецЦикла;

КонецПроцедуры

Функция SignerPowers(Powers)
	
	SignerPowers = Новый Соответствие;
	SignerPowers.Вставить("0", "InvoiceSigner");
	SignerPowers.Вставить("1", "PersonMadeOperation");
	SignerPowers.Вставить("2", "MadeAndSignOperation");
	SignerPowers.Вставить("3", "PersonDocumentedOperation");
	SignerPowers.Вставить("4", "MadeOperationAndSignedInvoice");
	SignerPowers.Вставить("5", "MadeAndResponsibleForOperationAndSignedInvoice");
	SignerPowers.Вставить("6", "ResponsibleForOperationAndSignerForInvoice");
	SignerPowers.Вставить("7", "ChairmanCommission");
	SignerPowers.Вставить("8", "MemberCommission");
	SignerPowers.Вставить("21", "PersonApprovedDocument");
	SignerPowers.Вставить("22", "PersonConfirmedDocument");
	SignerPowers.Вставить("23", "PersonAgreedOnDocument");
	SignerPowers.Вставить("29", "PersonOtherPower");
	
	Возврат SignerPowers[Powers];
	
КонецФункции

Функция SignerStatus(Status)
	
	SignerStatus = Новый Соответствие;
	SignerStatus.Вставить("1", "SellerEmployee");
	SignerStatus.Вставить("2", "InformationCreatorEmployee");
	SignerStatus.Вставить("3", "OtherOrganizationEmployee");
	SignerStatus.Вставить("4", "AuthorizedPerson");
	SignerStatus.Вставить("5", "BuyerEmployee");
	SignerStatus.Вставить("6", "InformationCreatorBuyerEmployee");
	
	Возврат SignerStatus[Status];
	
КонецФункции

Функция SignerStatus405(СтатПодп)
	
	СтатусыПодписанта = Новый Соответствие;
	СтатусыПодписанта.Вставить("1", "WithoutPowerOfAttorney");
	СтатусыПодписанта.Вставить("2", "DigitalPowerOfAttorney");
	СтатусыПодписанта.Вставить("3", "PaperPowerOfAttorney");
	
	Возврат СтатусыПодписанта.Получить(СтатПодп);
	
КонецФункции

Функция SignatureType405(ТипПодпис)
	
	ТипыПодписей = Новый Соответствие;
	ТипыПодписей.Вставить("1", "EnhancedQualifiedSignature");
	ТипыПодписей.Вставить("2", "SimpleSignature");
	ТипыПодписей.Вставить("3", "EnhancedUnqualifiedSignature");
	
	Возврат ТипыПодписей.Получить(ТипПодпис);
	
КонецФункции

Функция SignDisagreement405(ПризнРазн)
	
	ПризнакиРазногласий = Новый Соответствие;
	ПризнакиРазногласий.Вставить("1", "WithoutDisagreements");
	ПризнакиРазногласий.Вставить("2", "WithDisagreements");
	
	Возврат ПризнакиРазногласий.Получить(ПризнРазн);
	
КонецФункции

Функция ItemMark(Код)
	
	// Соответствие ItemMark в терминах компоненты и ПрТовРаб в XML
	Результат = "NotSpecified";
	
	Если ЗначениеЗаполнено(Код) Тогда
		
		ВидыТоваров = Перечисление_ItemMarks();
		
		ItemMarks = Новый Соответствие;
		ItemMarks.Вставить("1", ВидыТоваров.Имущество);
		ItemMarks.Вставить("2", ВидыТоваров.Работа);
		ItemMarks.Вставить("3", ВидыТоваров.Услуга);
		ItemMarks.Вставить("4", ВидыТоваров.ИмущественныеПрава);
		ItemMarks.Вставить("5", ВидыТоваров.Иное);
		
		Результат = ItemMarks[Код];
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Заглушка для обращения к множественным элементам XML,
// которые платформенный парсер распознал как единичные
//
Функция СписокXDTOИзЭлемента(Параметры, ИмяСвойства)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, ИмяСвойства) Тогда
		
		Элемент = Параметры[ИмяСвойства];
		
		Если ТипЗнч(Элемент) = Тип("СписокXDTO") Тогда
			Результат = Элемент;
		Иначе
			Результат = Новый Массив;
			Результат.Добавить(Элемент);
		КонецЕсли;
		
	Иначе
		Результат = Новый Массив;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ДатаXML_в_Дата1С(Строка)
	
	Если ТипЗнч(Строка) = Тип("Дата") Тогда	
		Возврат Строка;
	КонецЕсли;
	
	Если ПустаяСтрока(Строка) Тогда
		Возврат Дата(1, 1, 1);
	Иначе
		Возврат Дата(Прав(Строка, 4), Сред(Строка, 4, 2), Лев(Строка, 2));
	КонецЕсли;	
		
КонецФункции

// Преобразует строку длиной 19 и формата "22.04.2020 01.01.01" в дату 1С
//
Функция ДатаВремяXML_В_Дата1С(ДатаВремяXML)
	
	Если ТипЗнч(ДатаВремяXML) = Тип("Дата") Или ДатаВремяXML = Неопределено Тогда
		Возврат ДатаВремяXML;
	КонецЕсли;
	
	Если ПустаяСтрока(ДатаВремяXML) И СтрДлина(ДатаВремяXML) <> 19 Тогда
		Возврат Дата(1, 1, 1);
	КонецЕсли;
	
	Год = Сред(ДатаВремяXML, 7, 4);
	Месяц = Сред(ДатаВремяXML, 4, 2);
	День = Лев(ДатаВремяXML, 2);
	Час = Сред(ДатаВремяXML, 12, 2);
	Минута = Сред(ДатаВремяXML, 15, 2);
	Секунда = Прав(ДатаВремяXML, 2);
	
	Возврат Дата(Год, Месяц, День, Час, Минута, Секунда);
	
КонецФункции

Функция СуммаНДС_Из_XML(СумНДС)
	
	Результат = Неопределено;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СумНДС, "СумНал") Тогда
		
		Результат = СумНДС.СумНал;
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СумНДС, "СумНДС") Тогда
		
		Результат = СумНДС.СумНДС;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Акциз_Из_XML(Акциз)
	
	Результат = Неопределено;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Акциз, "СумАкциз") Тогда
		
		Результат = Акциз.СумАкциз;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ЗаполнитьПротоКонтентXDTOПоСтруктуре(Контент, Структура) Экспорт
	
	Для Каждого Эл Из Структура Цикл
		
		Если ТипЗнч(Эл.Значение) = Тип("Структура") Тогда
			
			Если Эл.Ключ = "ЕдиницаИзмеренияСсылка"
				Или Эл.Ключ = "СсылкаНаЕИ" Тогда
				УстановитьЗначениеXDTO(Контент, Эл.Ключ, Эл.Значение.Ссылка, Истина);
				Продолжить;
			КонецЕсли;
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Контент, Эл.Ключ) Тогда
				ЗаполнитьПротоКонтентXDTOПоСтруктуре(Контент[Эл.Ключ], Эл.Значение);
			КонецЕсли;
			
		ИначеЕсли ТипЗнч(Эл.Значение) = Тип("Массив")
			Или ТипЗнч(Эл.Значение) = Тип("ТаблицаЗначений") Тогда
			
			Если Не ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Контент, Эл.Ключ) Тогда
				Продолжить;
			КонецЕсли;
			
			ТипСтроки = Контент.Свойства().Получить(Эл.Ключ).Тип.Имя;
			
			Если ТипЗнч(Эл.Значение) = Тип("ТаблицаЗначений") Тогда
				_Массив = ТаблицаЗначений_2_МассивСтруктур(Эл.Значение);
			Иначе
				_Массив = Эл.Значение;
			КонецЕсли;
			
			Для Каждого ЭлементМассива Из _Массив Цикл
				
				Если ТипЗнч(ЭлементМассива) = Тип("Структура") Тогда
					
					НовыйКонтентXDTO = НовыйProtoОбъектXDTO(ТипСтроки, Истина);
					НовЭлемент = Контент[Эл.Ключ].Добавить(НовыйКонтентXDTO);
					ЗаполнитьПротоКонтентXDTOПоСтруктуре(НовЭлемент, ЭлементМассива);
					
				Иначе
					
					Контент[Эл.Ключ].Добавить(ЭлементМассива);
					
				КонецЕсли;
				
			КонецЦикла;
			
		ИначеЕсли Эл.Значение <> Неопределено И Эл.Значение <> NULL И Эл.Значение <> "" Тогда
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Контент, Эл.Ключ) Тогда
				
				Если Эл.Ключ = "Price" Тогда
					
					Попытка
						Контент.Price = Эл.Значение;
					Исключение
						Контент.Price = Число(Эл.Значение);
					КонецПопытки;
					
				ИначеЕсли Эл.Ключ = "DocumentDate"
					ИЛИ Эл.Ключ = "BaseDocumentDate" Тогда
					
					Попытка
						Контент[Эл.Ключ] = Дата(Эл.Значение);
					Исключение
						Контент[Эл.Ключ] = ДатаXML_в_Дата1С(Эл.Значение);
					КонецПопытки;
					
				ИначеЕсли Эл.Ключ = "Key" Или Эл.Ключ = "Value" Тогда
					
					Контент[Эл.Ключ] = Эл.Значение; // Записываем как есть, без сокращения пробелов
					
				Иначе
					
					ЭтоИмяСвойстваСсылки	 = ЭтоИмяСвойстваСсылки(Эл.Ключ);
					ЭтоОбъектXDTOСсылки		 = ЭтоОбъектXDTOСсылки(Контент, Эл.Ключ);
					ЭтоСсылка				 = ЭтоИмяСвойстваСсылки ИЛИ ЭтоОбъектXDTOСсылки;
					
					УстановитьЗначениеXDTO(Контент, Эл.Ключ, Эл.Значение, ЭтоСсылка);
					
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЦикла;
	
	
КонецПроцедуры

Процедура ЗаполнитьУКД736ПоДаннымУКД189(ProtoОбъектXDTO) Экспорт 
	
	Если ЗначениеЗаполнено(ProtoОбъектXDTO.EventContent.TransferDocDetails) 
		И Не ЗначениеЗаполнено(ProtoОбъектXDTO.EventContent.TransferDocDetails736) Тогда 
		
		ЗаполнитьПередаточныеДокументыУКД736ПоДаннымУКД189(ProtoОбъектXDTO.EventContent.TransferDocDetails736,
														   ProtoОбъектXDTO.EventContent.TransferDocDetails);
	КонецЕсли;
	
	Для Каждого CorrectionBase Из ProtoОбъектXDTO.EventContent.CorrectionBases Цикл
		
		Если CorrectionBase.BaseDocumentName = "Отсутствует" Тогда
			CorrectionBase.BaseDocumentName = "Без документа-основания";
		КонецЕсли;
		
	КонецЦикла;
	
	Для Каждого Item Из ProtoОбъектXDTO.InvoiceCorrectionTable.Items Цикл
		
		Если Не ЗначениеЗаполнено(Item.OriginalValues.UnitName)
			И ЗначениеЗаполнено(Item.OriginalValues.UnitCode) Тогда
			Item.OriginalValues.UnitName = НаименованиеЕдиницыИзмерения(Item.OriginalValues.UnitCode);
		КонецЕсли;
		
		Если Не ЗначениеЗаполнено(Item.CorrectedValues.UnitName)
			И ЗначениеЗаполнено(Item.CorrectedValues.UnitCode) Тогда
			Item.CorrectedValues.UnitName = НаименованиеЕдиницыИзмерения(Item.CorrectedValues.UnitCode);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьПередаточныеДокументыУКД736ПоДаннымУКД189(TransferDocDetails736, TransferDocDetails)
	
	TransferDocDetails736.Очистить();
	
	Если Найти(TransferDocDetails, ",") Тогда
		ПередаточныеДокументы = РазложитьСтрокуВМассивСлов(TransferDocDetails, ",");
	ИначеЕсли Найти(TransferDocDetails, ";") Тогда
		ПередаточныеДокументы = РазложитьСтрокуВМассивСлов(TransferDocDetails, ";");
	Иначе
		ПередаточныеДокументы = Новый Массив;
		ПередаточныеДокументы.Добавить(TransferDocDetails);
	КонецЕсли;
	
	Для Каждого ПередаточныйДокумент Из ПередаточныеДокументы Цикл
		
		// поиск наименования, номера и даты документа в строке вида:
		// УПД №УП-3 от 27.01.2021
		
		НаименованиеДокумента = Неопределено;
		ДатаДокумента = Неопределено;
		
		ПозицияНомера = Найти(ПередаточныйДокумент, "№");
		ПозицияТочки = Найти(ПередаточныйДокумент, "."); // позиция первой точки в дате
		
		Если ПозицияНомера > 0 Тогда
			НаименованиеДокумента = СокрЛП(Лев(ПередаточныйДокумент, ПозицияНомера - 1));
		КонецЕсли;
		
		Если ПозицияТочки > 0
			И СтрДлина(ПередаточныйДокумент) >= ПозицияТочки + 7
			И Сред(ПередаточныйДокумент, ПозицияТочки + 3, 1) = "." Тогда
			
			НомерДокумента = СокрЛП(Сред(ПередаточныйДокумент, ПозицияНомера + 1, ПозицияТочки - ПозицияНомера - 7));
			
			Попытка
				ДатаДокумента = Дата(Сред(ПередаточныйДокумент, ПозицияТочки + 4, 4),
									 Сред(ПередаточныйДокумент, ПозицияТочки + 1, 2),
									 Сред(ПередаточныйДокумент, ПозицияТочки - 2, 2));
			Исключение
				Продолжить;
			КонецПопытки;
			
		КонецЕсли;
		
		Если ЗначениеЗаполнено(НаименованиеДокумента)
			И ЗначениеЗаполнено(ДатаДокумента) Тогда
			
			НовыйTransferDocDetails736 = НовыйЭлементСпискаXDTO(TransferDocDetails736);
			НовыйTransferDocDetails736.BaseDocumentNumber	= НомерДокумента;
			НовыйTransferDocDetails736.BaseDocumentDate		= ДатаДокумента;
			НовыйTransferDocDetails736.BaseDocumentName		= НаименованиеДокумента;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Функция Контракт_ТитулПокупателяUtd970() Экспорт
	
	СоздаватьВсеСвойства = Истина;
	ИмяКонтента = ТипПрото_ИнформацияПокупателя970();
	
	ОбъектXDTO = НовыйProtoОбъектXDTO(ИмяКонтента, СоздаватьВсеСвойства);
	
	ПреобразовыватьСпискиВМассив = Истина;
	
	Результат = ОбъектXDTOВСтруктуру(ОбъектXDTO, ПреобразовыватьСпискиВМассив);
	
	Возврат Результат;
	
КонецФункции

Процедура Заполнить_ПризнакиПрослеживаемыхИМаркируемыхТоваровДокументаУПД970(Контент, ПризнакиТоваров) Экспорт
	
	ВидыТоваровДокумента = Контент.СлужебнаяИнформация.ContentsDocumentProductsType;
	
	ВидыТоваровДокумента.ContainsTraceableProducts = ПризнакиТоваров.ЕстьПрослеживаемаяПродукция;
	ВидыТоваровДокумента.ContainsMarkedProducts = ПризнакиТоваров.ЕстьМаркируемаяПродукция;
	ВидыТоваровДокумента.ContainsAlcoholeProducts = ПризнакиТоваров.ЕстьАлкогольнаяПродукция;
	ВидыТоваровДокумента.ContainsTobaccoProducts = ПризнакиТоваров.ЕстьТабачнаяПродукция;
	ВидыТоваровДокумента.ContainsPetroleumProducts = ПризнакиТоваров.ЕстьНефтепродукты;
	
КонецПроцедуры

Функция Новый_ПризнакиПрослеживаемыхИМаркируемыхТоваровУПД970(
	ЕстьПрослеживаемаяПродукция = Ложь,
	ЕстьМаркируемаяПродукция = Ложь,
	ЕстьАлкогольнаяПродукция = Ложь,
	ЕстьТабачнаяПродукция = Ложь,
	ЕстьНефтепродукты = Ложь) Экспорт
		
	Результат = Новый Структура;
	Результат.Вставить("ЕстьПрослеживаемаяПродукция", ЕстьПрослеживаемаяПродукция);
	Результат.Вставить("ЕстьМаркируемаяПродукция", ЕстьМаркируемаяПродукция);
	Результат.Вставить("ЕстьАлкогольнаяПродукция", ЕстьАлкогольнаяПродукция);
	Результат.Вставить("ЕстьТабачнаяПродукция", ЕстьТабачнаяПродукция);
	Результат.Вставить("ЕстьНефтепродукты", ЕстьНефтепродукты);
	
	Возврат Результат;
	
КонецФункции

Функция Перечисление_ItemMarks() Экспорт
	
	Результат = Кэш_Прочитать("ItemMarks");
	Если Результат = Неопределено Тогда
		Результат = Новый Структура;
		Кэш_Поместить("ItemMarks", Результат);
	КонецЕсли;
	
	Результат.Вставить("Имущество"			, "Property");
	Результат.Вставить("Работа"				, "Job");
	Результат.Вставить("Услуга"				, "Service");
	Результат.Вставить("ИмущественныеПрава"	, "PropertyRights");
	Результат.Вставить("Иное"				, "Other");
	Результат.Вставить("НеУказано"			, "NotSpecified");
	
	Возврат Результат;
	
КонецФункции

//}	МЕТОДЫ ДЛЯ ЗАПОЛНЕНИЯ Proto КОНТЕНТА


//{	Методы для заполнения универсальной табличной части

Функция ЭДО_ПолучитьТабличнуюЧастьИзКонтентаXML(Контент, НеИспользуется_ЭтоКорректировка = Ложь) Экспорт
	
	Если ТипЗнч(Контент) = Тип("Строка")
		Или ТипЗнч(Контент) = Тип("ДвоичныеДанные") Тогда
		ОбъектXDTO = XML_В_ОбъектXDTO(Контент);
	Иначе
		ОбъектXDTO = Контент;
	КонецЕсли;
	
	ТипКонтента = ПолучитьТипКонтентаОбъектаXDTO(ОбъектXDTO);
	
	Если ТипКонтента = ТипПрото_ИнформацияПродавца155() Тогда
		
		Результат = ТабличнаяЧасть_UniversalTransferDocument(ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавца970() Тогда
		
		Результат = ТабличнаяЧасть_UniversalTransferDocument970(ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавца820() Тогда
		
		Результат = ТабличнаяЧасть_UniversalTransferDocument820(ОбъектXDTO);	
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавца736() Тогда
		
		Результат = ТабличнаяЧасть_UniversalCorrectionDocument(ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавца552() Тогда
		
		Результат = ТабличнаяЧасть_XmlAcceptanceCertificate552(ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавца551() Тогда
		
		Результат = ТабличнаяЧасть_TovTorg(ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавцаНакладная172() Тогда
		
		Результат = ТабличнаяЧасть_XmlTorg12(ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавцаАкт172() Тогда
		
		Результат = ТабличнаяЧасть_XmlAcceptanceCertificate(ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавцаСчетНаОплату() Тогда
		
		Результат = ТабличнаяЧасть_ProformaInvoice(ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияОтправителя423() Тогда
		
		Результат = ТабличнаяЧасть_Torg2(ОбъектXDTO);
		
	ИначеЕсли ТипКонтента = ТипПрото_ИнформацияПродавцаСчетНаОплату29() Тогда
		
		Результат = ТабличнаяЧасть_ProformaInvoice29(ОбъектXDTO);
		
	Иначе
		
		ТекстОшибки = НСтр("ru = 'Невозможно получить табличную часть xml документа, неизвестный формат.'");
		
		ВызватьИсключение ТекстОшибки;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ТабличнаяЧасть_UniversalTransferDocument(ОбъектXDTO)
	
	Результат	 = Новый_ТабличнаяЧасть();
	СписокXDTO	 = СписокXDTOИзЭлемента(ОбъектXDTO.Документ.ТаблСчФакт, "СведТов");
	
	Для Каждого СтрокаXDTO Из СписокXDTO Цикл
		
		ИнфоПоля = ПрочитатьТекстИнфТипUtd(СтрокаXDTO);
		ИдентификаторНоменклатуры = ПрочитатьИдентификаторНоменклатуры(ИнфоПоля);
		НоменклатураЭД = НоменклатураЭлектронногоДокумента155(СтрокаXDTO, ИдентификаторНоменклатуры);
		
		СтрокаТЧ = Результат.Добавить();
		
		ЭДО_ЗаполнитьДопИнформациюСтрокиТЧ(СтрокаТЧ, ИнфоПоля);
		ПередаточныеДокументы_ЗаполнитьСтрокуТабличнойЧастиПоНоменклатуреДокумента(
			СтрокаТЧ,
			НоменклатураЭД
		);
		
		СтрокаТЧ.Количество		 = ПолучитьЗначениеXDTO(СтрокаXDTO, "КолТов");
		СтрокаТЧ.Цена			 = ПолучитьЗначениеXDTO(СтрокаXDTO, "ЦенаТов");
		СтрокаТЧ.СуммаБезНДС	 = ПолучитьЗначениеXDTO(СтрокаXDTO, "СтТовБезНДС");
		СтрокаТЧ.СуммаСНДС		 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO, "СтТовУчНал", 0));
		
		СтрокаТЧ.СуммаНДС	 = СуммаНДС_Из_XML(СтрокаXDTO.СумНал);
		СтрокаТЧ.СуммаАкциза = Акциз_Из_XML(СтрокаXDTO.Акциз);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ДопСведТов") Тогда
			
			ДопСведТов = СтрокаXDTO.ДопСведТов;
			
			СтрокаТЧ.ВидНоменклатуры	 = ПолучитьЗначениеXDTO(ДопСведТов, "ПрТовРаб"); // 1 - имущество; 2 - работа; 3 - услуга; 4 - имущественные права; 5 - иное
			СтрокаТЧ.ВидНоменклатурыДоп	 = ПолучитьЗначениеXDTO(ДопСведТов, "ДопПризн");
			СтрокаТЧ.Страна				 = ПолучитьЗначениеXDTO(ДопСведТов, "КрНаимСтрПр");
			СтрокаТЧ.КорСчетДт			 = ПолучитьЗначениеXDTO(ДопСведТов, "КорСчДебет");
			СтрокаТЧ.КорСчетКт			 = ПолучитьЗначениеXDTO(ДопСведТов, "КорСчКредит");
			
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "СвТД") Тогда
			ЭДО_ЗаполнитьДанныеИзСпискаЭлементовXML(СтрокаТЧ, СтрокаXDTO.СвТД, "НомерТД", "КодПроисх", "НомерГТД", "КодСтраны");
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТабличнаяЧасть_UniversalTransferDocument820(ОбъектXDTO)
	
	Результат	 = Новый_ТабличнаяЧасть();
	СписокXDTO	 = СписокXDTOИзЭлемента(ОбъектXDTO.Документ.ТаблСчФакт, "СведТов");
	
	Для Каждого СтрокаXDTO Из СписокXDTO Цикл
		
		ИнфоПоля = ПрочитатьТекстИнфТипUtd(СтрокаXDTO);
		ИдентификаторНоменклатуры = ПрочитатьИдентификаторНоменклатуры(ИнфоПоля);
		НоменклатураЭД = НоменклатураЭлектронногоДокумента820(СтрокаXDTO, ИдентификаторНоменклатуры);
		
		СтрокаТЧ = Результат.Добавить();
		
		ЭДО_ЗаполнитьДопИнформациюСтрокиТЧ(СтрокаТЧ, ИнфоПоля);
		ПередаточныеДокументы_ЗаполнитьСтрокуТабличнойЧастиПоНоменклатуреДокумента(
			СтрокаТЧ,
			НоменклатураЭД
		);
		
		СтрокаТЧ.СведенияОМаркировке		 = Новый_СведенияОМаркировке();
		СтрокаТЧ.СведенияПрослеживаемости	 = Новый_СведенияПрослеживаемости();
		
		СтрокаТЧ.Количество		 = ПолучитьЗначениеXDTO(СтрокаXDTO, "КолТов");
		СтрокаТЧ.Цена			 = ПолучитьЗначениеXDTO(СтрокаXDTO, "ЦенаТов");
		СтрокаТЧ.СуммаБезНДС	 = ПолучитьЗначениеXDTO(СтрокаXDTO, "СтТовБезНДС");
		СтрокаТЧ.СуммаСНДС		 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO, "СтТовУчНал", 0));
		
		СтрокаТЧ.СуммаНДС	 = СуммаНДС_Из_XML(СтрокаXDTO.СумНал);
		СтрокаТЧ.СуммаАкциза = Акциз_Из_XML(СтрокаXDTO.Акциз);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ДопСведТов") Тогда
			
			ДопСведТов = СтрокаXDTO.ДопСведТов;
			
			СтрокаТЧ.ВидНоменклатуры	 = ПолучитьЗначениеXDTO(ДопСведТов, "ПрТовРаб"); // 1 - имущество; 2 - работа; 3 - услуга; 4 - имущественные права; 5 - иное
			СтрокаТЧ.ВидНоменклатурыДоп	 = ПолучитьЗначениеXDTO(ДопСведТов, "ДопПризн");
			СтрокаТЧ.Страна				 = ПолучитьЗначениеXDTO(ДопСведТов, "КрНаимСтрПр");
			СтрокаТЧ.КолЗаказанное		 = ПолучитьЗначениеXDTO(ДопСведТов, "НадлОтп");
			СтрокаТЧ.КодКаталога		 = ПолучитьЗначениеXDTO(ДопСведТов, "КодКат");
			СтрокаТЧ.КодВидаТоваров		 = ПолучитьЗначениеXDTO(ДопСведТов, "КодВидТов");
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСведТов, "НомСредИдентТов") Тогда
				ЗаполнитьСведенияОМаркировке(СтрокаТЧ.СведенияОМаркировке, СписокXDTOИзЭлемента(ДопСведТов, "НомСредИдентТов"));
			КонецЕсли;
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСведТов, "СведПрослеж") Тогда
				ЗаполнитьСведенияПрослеживаемости(СтрокаТЧ.СведенияПрослеживаемости, СписокXDTOИзЭлемента(ДопСведТов, "СведПрослеж"), Ложь);
			КонецЕсли;
			
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "СвТД") Тогда
			ЭДО_ЗаполнитьДанныеИзСпискаЭлементовXML(СтрокаТЧ, СтрокаXDTO.СвТД, "НомерТД", "КодПроисх", "НомерГТД", "КодСтраны");
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТабличнаяЧасть_UniversalTransferDocument970(ОбъектXDTO)
	
	Результат	 = Новый_ТабличнаяЧасть();
	СписокXDTO	 = СписокXDTOИзЭлемента(ОбъектXDTO.Документ.ТаблСчФакт, "СведТов");
	
	Для Каждого СтрокаXDTO Из СписокXDTO Цикл
		
		ИнфоПоля = ПрочитатьТекстИнфТипUtd(СтрокаXDTO);
		ИдентификаторНоменклатуры = ПрочитатьИдентификаторНоменклатуры(ИнфоПоля);
		НоменклатураЭД = НоменклатураЭлектронногоДокумента970(СтрокаXDTO, ИдентификаторНоменклатуры);
		
		СтрокаТЧ = Результат.Добавить();
		
		ЭДО_ЗаполнитьДопИнформациюСтрокиТЧ(СтрокаТЧ, ИнфоПоля);
		ПередаточныеДокументы_ЗаполнитьСтрокуТабличнойЧастиПоНоменклатуреДокумента(
			СтрокаТЧ,
			НоменклатураЭД
		);
		
		СтрокаТЧ.СведенияОМаркировке		 = Новый_СведенияОМаркировке();
		СтрокаТЧ.СведенияПрослеживаемости	 = Новый_СведенияПрослеживаемости();
		
		СтрокаТЧ.Количество	 = ПолучитьЗначениеXDTO(СтрокаXDTO, "КолТов");
		СтрокаТЧ.Цена		 = ПолучитьЗначениеXDTO(СтрокаXDTO, "ЦенаТов");
		СтрокаТЧ.СуммаБезНДС = ПолучитьЗначениеXDTO(СтрокаXDTO, "СтТовБезНДС");
		СтрокаТЧ.СуммаСНДС	 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO, "СтТовУчНал", 0));
		
		СтрокаТЧ.СуммаНДС	 = СуммаНДС_Из_XML(СтрокаXDTO.СумНал);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "Акциз") Тогда
			СтрокаТЧ.СуммаАкциза = Акциз_Из_XML(СтрокаXDTO.Акциз);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ДопСведТов") Тогда
			
			ДопСведТов = СтрокаXDTO.ДопСведТов;
			
			СтрокаТЧ.ВидНоменклатуры	 = ПолучитьЗначениеXDTO(ДопСведТов, "ПрТовРаб"); // 1 - имущество; 2 - работа; 3 - услуга; 4 - имущественные права; 5 - иное
			СтрокаТЧ.ВидНоменклатурыДоп	 = ПолучитьЗначениеXDTO(ДопСведТов, "ДопПризн");
			СтрокаТЧ.Страна				 = ПолучитьЗначениеXDTO(ДопСведТов, "КрНаимСтрПр");
			СтрокаТЧ.КолЗаказанное		 = ПолучитьЗначениеXDTO(ДопСведТов, "НадлОтп");
			СтрокаТЧ.КодКаталога		 = ПолучитьЗначениеXDTO(ДопСведТов, "КодКат");
			СтрокаТЧ.КодВидаТоваров		 = ПолучитьЗначениеXDTO(ДопСведТов, "КодВидТов");
			СтрокаТЧ.Серия		 		 = ПолучитьЗначениеXDTO(ДопСведТов, "СерияТов");
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСведТов, "НомСредИдентТов") Тогда
				ЗаполнитьСведенияОМаркировке(СтрокаТЧ.СведенияОМаркировке, СписокXDTOИзЭлемента(ДопСведТов, "НомСредИдентТов"));
			КонецЕсли;
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДопСведТов, "СведПрослеж") Тогда
				ЗаполнитьСведенияПрослеживаемости(СтрокаТЧ.СведенияПрослеживаемости, СписокXDTOИзЭлемента(ДопСведТов, "СведПрослеж"), Ложь);
			КонецЕсли;
			
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "СвДТ") Тогда
			ЭДО_ЗаполнитьДанныеИзСпискаЭлементовXML(СтрокаТЧ, СтрокаXDTO.СвДТ, "НомерДТ", "КодПроисх", "НомерГТД", "КодСтраны");
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТабличнаяЧасть_UniversalCorrectionDocument(ОбъектXDTO)
	
	Результат	 = Новый_ТабличнаяЧастьКорректировки();
	СписокXDTO	 = СписокXDTOИзЭлемента(ОбъектXDTO.Документ.ТаблКСчФ, "СведТов");
	
	Для Каждого СтрокаXDTO Из СписокXDTO Цикл
		
		СтрокаТЧ = Результат.Добавить();
		
		ИнфоПоля = ПрочитатьТекстИнфТип736(СтрокаXDTO);
		ЭДО_ЗаполнитьДопИнформациюСтрокиТЧ(СтрокаТЧ, ИнфоПоля);
		
		ИдентификаторНоменклатуры = ПрочитатьИдентификаторНоменклатуры(ИнфоПоля);
		НоменклатураЭД_До = НоменклатураЭлектронногоДокумента736_До(СтрокаXDTO, ИдентификаторНоменклатуры);
		НоменклатураЭД_После = НоменклатураЭлектронногоДокумента736_После(СтрокаXDTO, ИдентификаторНоменклатуры);
		
		КорректировочныеДокументы_ЗаполнитьСтрокуТабличнойЧастиПоНоменклатуреДокумента(
			СтрокаТЧ,
			НоменклатураЭД_До,
			НоменклатураЭД_После
		);
		
		СтрокаТЧ.СведенияОМаркировке		 = Новый_СведенияОМаркировке();
		СтрокаТЧ.СведенияОМаркировке_До		 = Новый_СведенияОМаркировке();
		СтрокаТЧ.СведенияПрослеживаемости	 = Новый_СведенияПрослеживаемости();
		
		СтрокаТЧ.Количество_До	 = ПолучитьЗначениеXDTO(СтрокаXDTO, "КолТовДо");
		СтрокаТЧ.Количество		 = ПолучитьЗначениеXDTO(СтрокаXDTO, "КолТовПосле");
		СтрокаТЧ.Цена_До		 = ПолучитьЗначениеXDTO(СтрокаXDTO, "ЦенаТовДо");
		СтрокаТЧ.Цена			 = ПолучитьЗначениеXDTO(СтрокаXDTO, "ЦенаТовПосле");
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "СтТовУчНал") Тогда
			СтрокаТЧ.СуммаСНДС_До	 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO.СтТовУчНал, "СтоимДоИзм"	, 0));
			СтрокаТЧ.СуммаСНДС		 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO.СтТовУчНал, "СтоимПослеИзм", 0));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "СтТовБезНДС") Тогда
			СтрокаТЧ.СуммаБезНДС_До	 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO.СтТовБезНДС, "СтоимДоИзм"		, 0));
			СтрокаТЧ.СуммаБезНДС	 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO.СтТовБезНДС, "СтоимПослеИзм"	, 0));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ДопСведТов") Тогда
			
			ДопСведТов = СтрокаXDTO.ДопСведТов;
			
			СтрокаТЧ.КорСчетДт = ПолучитьЗначениеXDTO(ДопСведТов, "КорСчДебет");
			СтрокаТЧ.КорСчетКт = ПолучитьЗначениеXDTO(ДопСведТов, "КорСчКредит");
			
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "СумНалДо") Тогда
			СтрокаТЧ.СуммаНДС_До = СуммаНДС_Из_XML(СтрокаXDTO.СумНалДо);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "СумНалПосле") Тогда
			СтрокаТЧ.СуммаНДС = СуммаНДС_Из_XML(СтрокаXDTO.СумНалПосле);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "АкцизДо") Тогда
			СтрокаТЧ.СуммаАкциза_До = Акциз_Из_XML(СтрокаXDTO.АкцизДо);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "АкцизПосле") Тогда
			СтрокаТЧ.СуммаАкциза = Акциз_Из_XML(СтрокаXDTO.АкцизПосле);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "НомСредИдентТовДо") Тогда
			ЗаполнитьСведенияОМаркировке(СтрокаТЧ.СведенияОМаркировке_До, СписокXDTOИзЭлемента(СтрокаXDTO, "НомСредИдентТовДо"));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "НомСредИдентТовПосле") Тогда
			ЗаполнитьСведенияОМаркировке(СтрокаТЧ.СведенияОМаркировке, СписокXDTOИзЭлемента(СтрокаXDTO, "НомСредИдентТовПосле"));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "СведПрослеж") Тогда
			ЗаполнитьСведенияПрослеживаемости(СтрокаТЧ.СведенияПрослеживаемости, СписокXDTOИзЭлемента(СтрокаXDTO, "СведПрослеж"), Истина);
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТабличнаяЧасть_XmlTorg12(ОбъектXDTO)
	
	Результат	 = Новый_ТабличнаяЧасть();
	СписокXDTO	 = СписокXDTOИзЭлемента(ОбъектXDTO.Документ.СвТНО.ТН.Таблица, "СвТов");
	
	Для Каждого СтрокаXDTO Из СписокXDTO Цикл
		
		ИнфоПоля = ПрочитатьТекстИнфТипНакладная172(СтрокаXDTO);
		ИдентификаторНоменклатуры = ПрочитатьИдентификаторНоменклатуры(ИнфоПоля);
		НоменклатураЭД = НоменклатураЭлектроннойНакладной172(СтрокаXDTO, ИдентификаторНоменклатуры);
		
		СтрокаТЧ = Результат.Добавить();
		
		ЭДО_ЗаполнитьДопИнформациюСтрокиТЧ(СтрокаТЧ, ИнфоПоля);
		ПередаточныеДокументы_ЗаполнитьСтрокуТабличнойЧастиПоНоменклатуреДокумента(
			СтрокаТЧ,
			НоменклатураЭД
		);
		
		СтрокаТЧ.ВидУпаковки		 = ПолучитьЗначениеXDTO(СтрокаXDTO, "ВидУпак");
		СтрокаТЧ.Количество			 = ПолучитьЗначениеXDTO(СтрокаXDTO, "Нетто");
		СтрокаТЧ.МассаБрутто		 = ПолучитьЗначениеXDTO(СтрокаXDTO, "Брутто");
		СтрокаТЧ.Цена				 = ПолучитьЗначениеXDTO(СтрокаXDTO, "Цена");
		СтрокаТЧ.СуммаСНДС			 = ПолучитьЗначениеXDTO(СтрокаXDTO, "СумУчНДС");
		СтрокаТЧ.СуммаБезНДС		 = ПолучитьЗначениеXDTO(СтрокаXDTO, "СумБезНДС");
		СтрокаТЧ.СуммаНДС			 = ПолучитьЗначениеXDTO(СтрокаXDTO, "СумНДС");
		СтрокаТЧ.КолВОдномМесте		 = ПолучитьЗначениеXDTO(СтрокаXDTO, "Место");
		СтрокаТЧ.КолМест			 = ПолучитьЗначениеXDTO(СтрокаXDTO, "КолМест");
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТабличнаяЧасть_XmlAcceptanceCertificate(ОбъектXDTO)
	
	Результат	 = Новый_ТабличнаяЧасть();
	СписокXDTO	 = СписокXDTOИзЭлемента(ОбъектXDTO.Документ.СвАктИ, "ОписРабот");
	
	Для Каждого СтрокаXDTO Из СписокXDTO Цикл
		
		Для Каждого Работа Из СписокXDTOИзЭлемента(СтрокаXDTO, "Работа") Цикл
			
			ИнфоПоля = ПрочитатьТекстИнфТипАкт172(СтрокаXDTO);
			ИдентификаторНоменклатуры = ПрочитатьИдентификаторНоменклатуры(ИнфоПоля);
			НоменклатураЭД = НоменклатураЭлектронногоАкта172(СтрокаXDTO, ИдентификаторНоменклатуры);
			
			СтрокаТЧ = Результат.Добавить();
			
			ЭДО_ЗаполнитьДопИнформациюСтрокиТЧ(СтрокаТЧ, ИнфоПоля);
			ПередаточныеДокументы_ЗаполнитьСтрокуТабличнойЧастиПоНоменклатуреДокумента(
				СтрокаТЧ,
				НоменклатураЭД
			);
			
			СтрокаТЧ.Количество			 = ПолучитьЗначениеXDTO(Работа, "Количество");
			СтрокаТЧ.Цена				 = ПолучитьЗначениеXDTO(Работа, "Цена");
			СтрокаТЧ.СуммаСНДС			 = ПолучитьЗначениеXDTO(Работа, "СумУчНДС");
			СтрокаТЧ.СуммаБезНДС		 = ПолучитьЗначениеXDTO(Работа, "СумБезНДС");
			СтрокаТЧ.СуммаНДС			 = ПолучитьЗначениеXDTO(Работа, "СумНДС");
			
			СтрокаТЧ.СтавкаНДС = TaxRate_Рассчитать(СтрокаТЧ.СуммаБезНДС, СтрокаТЧ.СуммаНДС);
			
		КонецЦикла;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТабличнаяЧасть_XmlAcceptanceCertificate552(ОбъектXDTO)
	
	Результат	 = Новый_ТабличнаяЧасть();
	СписокXDTO	 = СписокXDTOИзЭлемента(ОбъектXDTO.Документ.СвДокПРУ.СодФХЖ1, "ОписРабот");
	
	Для Каждого СтрокаXDTO Из СписокXDTO Цикл
		
		Для Каждого Работа Из СписокXDTOИзЭлемента(СтрокаXDTO, "Работа") Цикл
			
			ИнфоПоля = ПрочитатьТекстИнфТип552(Работа);
			ИдентификаторНоменклатуры = ПрочитатьИдентификаторНоменклатуры(ИнфоПоля);
			НоменклатураЭД = НоменклатураЭлектронногоАкта552(Работа, ИдентификаторНоменклатуры);
			
			СтрокаТЧ = Результат.Добавить();
			
			ЭДО_ЗаполнитьДопИнформациюСтрокиТЧ(СтрокаТЧ, ИнфоПоля);
			ПередаточныеДокументы_ЗаполнитьСтрокуТабличнойЧастиПоНоменклатуреДокумента(
				СтрокаТЧ,
				НоменклатураЭД
			);
			
			СтрокаТЧ.Количество			= ПолучитьЗначениеXDTO(Работа, "Количество");
			СтрокаТЧ.СуммаБезНДС		= ПолучитьЗначениеXDTO(Работа, "СтоимБезНДС");
			СтрокаТЧ.СуммаСНДС			= ПолучитьЗначениеXDTO(Работа, "СтоимУчНДС");
			СтрокаТЧ.СуммаНДС			= ПолучитьЗначениеXDTO(Работа, "СумНДС");
			СтрокаТЧ.Цена				= ПолучитьЗначениеXDTO(Работа, "Цена");
			СтрокаТЧ.КорСчетДт			= ПолучитьЗначениеXDTO(Работа, "КоррСчДебет");
			СтрокаТЧ.КорСчетКт			= ПолучитьЗначениеXDTO(Работа, "КоррСчКредит");
			
		КонецЦикла;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТабличнаяЧасть_TovTorg(ОбъектXDTO)
	
	Результат	 = Новый_ТабличнаяЧасть();
	СписокXDTO	 = СписокXDTOИзЭлемента(ОбъектXDTO.Документ.СвДокПТПрКроме.СодФХЖ2, "СвТов");
	
	Для Каждого СтрокаXDTO Из СписокXDTO Цикл
		
		ИнфоПоля = ПрочитатьТекстИнфТип551(СтрокаXDTO);
		ИдентификаторНоменклатуры = ПрочитатьИдентификаторНоменклатуры(ИнфоПоля);
		НоменклатураЭД = НоменклатураЭлектроннойНакладной551(СтрокаXDTO, ИдентификаторНоменклатуры);
		
		СтрокаТЧ = Результат.Добавить();
		
		ЭДО_ЗаполнитьДопИнформациюСтрокиТЧ(СтрокаТЧ, ИнфоПоля);
		ПередаточныеДокументы_ЗаполнитьСтрокуТабличнойЧастиПоНоменклатуреДокумента(
			СтрокаТЧ,
			НоменклатураЭД
		);
		
		СтрокаТЧ.Количество			 = ПолучитьЗначениеXDTO(СтрокаXDTO, "НеттоПередано");
		СтрокаТЧ.МассаБрутто		 = ПолучитьЗначениеXDTO(СтрокаXDTO, "Брутто");
		СтрокаТЧ.Цена				 = ПолучитьЗначениеXDTO(СтрокаXDTO, "Цена");
		СтрокаТЧ.СуммаСНДС			 = ПолучитьЗначениеXDTO(СтрокаXDTO, "СтУчНДС");
		СтрокаТЧ.СуммаБезНДС		 = ПолучитьЗначениеXDTO(СтрокаXDTO, "СтБезНДС");
		СтрокаТЧ.СуммаНДС			 = ПолучитьЗначениеXDTO(СтрокаXDTO, "СумНДС");
		СтрокаТЧ.ВидУпаковки		 = ПолучитьЗначениеXDTO(СтрокаXDTO, "ВидУпак");
		СтрокаТЧ.КолВОдномМесте		 = ПолучитьЗначениеXDTO(СтрокаXDTO, "Место");
		СтрокаТЧ.КолМест			 = ПолучитьЗначениеXDTO(СтрокаXDTO, "КолМест");
		СтрокаТЧ.КорСчетДт			 = ПолучитьЗначениеXDTO(СтрокаXDTO, "КорСчДебет");
		СтрокаТЧ.КорСчетКт			 = ПолучитьЗначениеXDTO(СтрокаXDTO, "КорСчКредит");
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТабличнаяЧасть_ProformaInvoice(ОбъектXDTO)
	
	Результат	 = Новый_ТабличнаяЧасть();
	СписокXDTO	 = СписокXDTOИзЭлемента(ОбъектXDTO.Документ.ТаблСНО, "СведТов");
	
	Для Каждого СтрокаXDTO Из СписокXDTO Цикл
		
		ИнфоПоля = ПрочитатьТекстИнфТипФормализованногоСчета(СтрокаXDTO);
		ИдентификаторНоменклатуры = ПрочитатьИдентификаторНоменклатуры(ИнфоПоля);
		НоменклатураЭД = НоменклатураЭлектронногоСчетаДД(СтрокаXDTO, ИдентификаторНоменклатуры);
		
		СтрокаТЧ = Результат.Добавить();
		
		ЭДО_ЗаполнитьДопИнформациюСтрокиТЧ(СтрокаТЧ, ИнфоПоля);
		ПередаточныеДокументы_ЗаполнитьСтрокуТабличнойЧастиПоНоменклатуреДокумента(
			СтрокаТЧ,
			НоменклатураЭД
		);
		
		СтрокаТЧ.Количество			 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO, "КолТов"		, 0));
		СтрокаТЧ.Цена				 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO, "ЦенаТов"		, 0));
		СтрокаТЧ.СуммаСНДС			 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO, "СтТовУчНал"	, 0));
		СтрокаТЧ.СуммаБезНДС		 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO, "СтТовБезНДС"	, 0));
		
		СтрокаТЧ.СуммаНДС	 = СуммаНДС_Из_XML(СтрокаXDTO.СумНал);
		СтрокаТЧ.СуммаАкциза = Акциз_Из_XML(СтрокаXDTO.Акциз);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ТабличнаяЧасть_Torg2(ОбъектXDTO)

	Результат	= Новый_ТабличнаяЧастьАктаОРасхождениях();
	СписокXDTO	= СписокXDTOИзЭлемента(ОбъектXDTO.Документ.СодФХЖ2, "РезПрием");
	
	Для Каждого СтрокаXDTO Из СписокXDTO Цикл
		
		ИнфоПоля = ПрочитатьТекстИнфТип423(СтрокаXDTO);
		ИдентификаторНоменклатуры = ПрочитатьИдентификаторНоменклатуры(ИнфоПоля);
		НоменклатураДокумента = НоменклатураЭлектронногоДокумента423(СтрокаXDTO, ИдентификаторНоменклатуры);
		
		СтрокаТЧ = Результат.Добавить();
		
		ЭДО_ЗаполнитьДопИнформациюСтрокиТЧ(СтрокаТЧ, ИнфоПоля);
		АктыОРасхождениях_ЗаполнитьСтрокуТабличнойЧастиПоНоменклатуреДокумента(СтрокаТЧ, НоменклатураДокумента);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ПоДокум") Тогда
			
			ПоДокум = СтрокаXDTO.ПоДокум;
			
			СтрокаТЧ.КоличествоПоДокументу	= Привести_К_Числу(ПолучитьЗначениеXDTO(ПоДокум, "КолТовПрин"));
			СтрокаТЧ.СуммаПоДокументу		= Привести_К_Числу(ПолучитьЗначениеXDTO(ПоДокум, "СтоимТовНДСПрин"));
			СтрокаТЧ.СуммаНДСПоДокументу	= Привести_К_Числу(ПолучитьЗначениеXDTO(ПоДокум, "СумНДСПрин"));
			СтрокаТЧ.ЦенаПоДокументу		= Привести_К_Числу(ПолучитьЗначениеXDTO(ПоДокум, "ЦенаПрин"));
			СтрокаТЧ.СтавкаНДСПоДокументу	= ПолучитьЗначениеXDTO(ПоДокум, "СтавНДСПрин");
			
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ПоФакту") Тогда
			
			ПоФакту = СтрокаXDTO.ПоФакту;
			
			СтрокаТЧ.Количество	= Привести_К_Числу(ПолучитьЗначениеXDTO(ПоФакту, "КолТовПрин"));
			СтрокаТЧ.Сумма		= Привести_К_Числу(ПолучитьЗначениеXDTO(ПоФакту, "СтоимТовНДСПрин"));
			СтрокаТЧ.СуммаНДС	= Привести_К_Числу(ПолучитьЗначениеXDTO(ПоФакту, "СумНДСПрин"));
			СтрокаТЧ.Цена		= Привести_К_Числу(ПолучитьЗначениеXDTO(ПоФакту, "ЦенаПрин"));
			СтрокаТЧ.СтавкаНДС	= ПолучитьЗначениеXDTO(ПоФакту, "СтавНДСПрин");
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;

КонецФункции

Функция Новый_ТабличнаяЧасть()
	
	Результат = Новый ТаблицаЗначений;
	
	Результат.Колонки.Добавить("НоменклатураДокумента");
	Результат.Колонки.Добавить("ДополнительнаяИнформация");
	Результат.Колонки.Добавить("Наименование");
	Результат.Колонки.Добавить("Код");
	Результат.Колонки.Добавить("Артикул");
	Результат.Колонки.Добавить("Характеристика");
	Результат.Колонки.Добавить("Сорт");
	Результат.Колонки.Добавить("НаименованиеЕдИзм");
	Результат.Колонки.Добавить("КодЕдИзм");
	Результат.Колонки.Добавить("ВидНоменклатуры");
	Результат.Колонки.Добавить("ВидНоменклатурыДоп");
	Результат.Колонки.Добавить("Количество");
	Результат.Колонки.Добавить("МассаБрутто");
	Результат.Колонки.Добавить("Цена");
	Результат.Колонки.Добавить("СуммаСНДС");
	Результат.Колонки.Добавить("СуммаБезНДС");
	Результат.Колонки.Добавить("СуммаНДС");
	Результат.Колонки.Добавить("СтавкаНДС");
	Результат.Колонки.Добавить("СуммаАкциза");
	Результат.Колонки.Добавить("НомерГТД");
	Результат.Колонки.Добавить("Страна");
	Результат.Колонки.Добавить("КодСтраны");
	Результат.Колонки.Добавить("КорСчетДт");
	Результат.Колонки.Добавить("КорСчетКт");
	Результат.Колонки.Добавить("ДопИнформацияИд");
	Результат.Колонки.Добавить("ДопИнформация");
	Результат.Колонки.Добавить("ВидУпаковки");
	Результат.Колонки.Добавить("КолВОдномМесте");
	Результат.Колонки.Добавить("КолМест");
	Результат.Колонки.Добавить("КолЗаказанное");
	Результат.Колонки.Добавить("КодКаталога");
	Результат.Колонки.Добавить("КодВидаТоваров");
	Результат.Колонки.Добавить("СведенияОМаркировке");
	Результат.Колонки.Добавить("СведенияПрослеживаемости");
	Результат.Колонки.Добавить("ИдентификаторНоменклатуры");
	Результат.Колонки.Добавить("Серия");
	
	Возврат Результат;
	
КонецФункции

Функция Новый_ТабличнаяЧастьКорректировки()
	
	Результат = Новый ТаблицаЗначений;
	
	Результат.Колонки.Добавить("НоменклатураДокумента");
	Результат.Колонки.Добавить("ДополнительнаяИнформация");
	Результат.Колонки.Добавить("Наименование");
	Результат.Колонки.Добавить("Код");
	Результат.Колонки.Добавить("Артикул");
	Результат.Колонки.Добавить("Характеристика");
	Результат.Колонки.Добавить("Сорт");
	Результат.Колонки.Добавить("НаименованиеЕдИзм");
	Результат.Колонки.Добавить("КодЕдИзм");
	Результат.Колонки.Добавить("НаименованиеЕдИзм_До");
	Результат.Колонки.Добавить("КодЕдИзм_До");
	Результат.Колонки.Добавить("Количество");
	Результат.Колонки.Добавить("Количество_До");
	Результат.Колонки.Добавить("Цена");
	Результат.Колонки.Добавить("Цена_До");
	Результат.Колонки.Добавить("СуммаСНДС");
	Результат.Колонки.Добавить("СуммаСНДС_До");
	Результат.Колонки.Добавить("СуммаБезНДС");
	Результат.Колонки.Добавить("СуммаБезНДС_До");
	Результат.Колонки.Добавить("СуммаНДС");
	Результат.Колонки.Добавить("СуммаНДС_До");
	Результат.Колонки.Добавить("СтавкаНДС");
	Результат.Колонки.Добавить("СтавкаНДС_До");
	Результат.Колонки.Добавить("СуммаАкциза");
	Результат.Колонки.Добавить("СуммаАкциза_До");
	Результат.Колонки.Добавить("КорСчетДт");
	Результат.Колонки.Добавить("КорСчетКт");
	Результат.Колонки.Добавить("ДопИнформацияИд");
	Результат.Колонки.Добавить("ДопИнформация");
	Результат.Колонки.Добавить("СведенияОМаркировке");
	Результат.Колонки.Добавить("СведенияОМаркировке_До");
	Результат.Колонки.Добавить("СведенияПрослеживаемости");
	Результат.Колонки.Добавить("ИдентификаторНоменклатуры");
	
	Возврат Результат;
	
КонецФункции

Функция Новый_ТабличнаяЧастьАктаОРасхождениях()

	Результат = Новый ТаблицаЗначений;
	
	Результат.Колонки.Добавить("НоменклатураДокумента");
	Результат.Колонки.Добавить("ДополнительнаяИнформация");
	Результат.Колонки.Добавить("КоличествоПоДокументу");
	Результат.Колонки.Добавить("Количество");
	Результат.Колонки.Добавить("ЦенаПоДокументу");
	Результат.Колонки.Добавить("Цена");
	Результат.Колонки.Добавить("СтавкаНДСПоДокументу");
	Результат.Колонки.Добавить("СтавкаНДС");
	Результат.Колонки.Добавить("СуммаПоДокументу");
	Результат.Колонки.Добавить("Сумма");
	Результат.Колонки.Добавить("СуммаНДСПоДокументу");
	Результат.Колонки.Добавить("СуммаНДС");
	Результат.Колонки.Добавить("НомерГТДПоДокументу");
	Результат.Колонки.Добавить("НомерГТД");
	Результат.Колонки.Добавить("СтранаПроисхожденияПоДокументу");
	Результат.Колонки.Добавить("СтранаПроисхождения");
	Результат.Колонки.Добавить("СведенияОМаркировке");
	Результат.Колонки.Добавить("ИдентификаторНоменклатуры");

	Возврат Результат;

КонецФункции

Функция Новый_СведенияОМаркировке()
	
	Результат = Новый Структура;
	Результат.Вставить("ТранспортныеУпаковки", Новый Массив);
	Результат.Вставить("ИндивидуальныеУпаковки", Новый Массив);
	Результат.Вставить("КонтрольныеИдентификационныеЗнаки", Новый Массив);
	Результат.Вставить("КоличествоТовараВУпаковке", 0);
	
	Возврат Результат;
	
КонецФункции

Функция Новый_СведенияПрослеживаемости()
	
	Результат = Новый Массив;
	
	Возврат Результат;
	
КонецФункции

// Конструктор описания номенклатуры электронного документа.
//
// Параметры:
//  Наименование - Строка - наименование номенклатуры.
//  Артикул - Строка - артикул номенклатуры.
//  Код - Строка - код номенклатуры.
//  Характеристика - Строка - наименование характеристики.
//  Идентификатор - Строка - идентификатор номенклатуры документа. Если не указан, формируется автоматически.
// 
// Возвращаемое значение:
//  Структура - см. Контракт_НоменклатураЭлектронногоДокумента.
//
Функция Новый_НоменклатураЭлектронногоДокумента(Идентификатор = "", Наименование = "", Артикул = "", Код = "", Характеристика = "") Экспорт
	
	ПустаяСтрока = "";
	
	Результат = Контракт_НоменклатураЭлектронногоДокумента();
	Результат.Идентификатор = Идентификатор;
	
	Если ЗначениеЗаполнено(Наименование) Тогда
		Результат.Наименование = Наименование;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Артикул) Тогда
		Результат.Артикул = Артикул;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Код) Тогда
		Результат.Код = Код;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Характеристика) Тогда
		Результат.Характеристика = Характеристика;
	КонецЕсли;
	
	ПроверитьЗаполнитьИдентификаторНоменклатуры(Результат);
	
	Возврат Результат;
	
КонецФункции

Процедура ЭДО_ЗаполнитьДанныеИзСпискаЭлементовXML(СтрокаТЧ, СписокXDTO, Источник1, Источник2, Приемник1, Приемник2)
	
	Если ТипЗнч(СписокXDTO) = Тип("СписокXDTO") Тогда
		
		Разделитель1 = "";
		Разделитель2 = "";
		
		СтрокаТЧ[Приемник1] = "";
		СтрокаТЧ[Приемник2] = "";
		
		Для Каждого СтрокаXDTO Из СписокXDTO Цикл
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, Источник1) Тогда
				СтрокаТЧ[Приемник1] = СтрокаТЧ[Приемник1] + Разделитель1 + СтрокаXDTO[Источник1];
				Разделитель1 = "|";
			КонецЕсли;
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, Источник2) Тогда
				СтрокаТЧ[Приемник2] = СтрокаТЧ[Приемник2] + Разделитель2 + СтрокаXDTO[Источник2];
				Разделитель2 = "|";
			КонецЕсли;
			
		КонецЦикла;
		
	ИначеЕсли ТипЗнч(СписокXDTO) = Тип("ОбъектXDTO") Тогда
		
		СтрокаТЧ[Приемник1] = ПолучитьЗначениеXDTO(СписокXDTO, Источник1);
		СтрокаТЧ[Приемник2] = ПолучитьЗначениеXDTO(СписокXDTO, Источник2);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ЭДО_ЗаполнитьДопИнформациюСтрокиТЧ(СтрокаТЧ, ИнфоПоля)
	
	СтрокаТЧ.ДополнительнаяИнформация = _ФиксированныеДанные(ИнфоПоля);
	
	ЕстьУстаревшиеКолонки = (СтрокаТЧ.Владелец().Колонки.Найти("ДопИнформацияИд") <> Неопределено);
	
	Если НЕ ЕстьУстаревшиеКолонки Тогда
		Возврат;
	КонецЕсли;
	
	Разделитель = "";
	Ключи = "";
	Значения = "";
	
	Для Каждого КлючЗначение Из ИнфоПоля Цикл
		
		Ключи = Ключи + Разделитель + КлючЗначение.Ключ;
		Значения = Значения + Разделитель + КлючЗначение.Значение;
		
		Разделитель = "|";
		
	КонецЦикла;
	
	СтрокаТЧ.ДопИнформацияИд = Ключи;
	СтрокаТЧ.ДопИнформация = Значения;
	
КонецПроцедуры

Процедура ЗаполнитьСведенияОМаркировке(СведенияОМаркировке, СписокНомСредИдентТов)
	
	Для Каждого НомСредИдентТов Из СписокНомСредИдентТов Цикл
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(НомСредИдентТов, "ИдентТрансУпак") Тогда
			СведенияОМаркировке.ТранспортныеУпаковки.Добавить(НомСредИдентТов.ИдентТрансУпак);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(НомСредИдентТов, "КИЗ") Тогда
			ДобавитьВМассивЭлементыСпискаXDTO(СведенияОМаркировке.КонтрольныеИдентификационныеЗнаки, СписокXDTOИзЭлемента(НомСредИдентТов, "КИЗ"));
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(НомСредИдентТов, "НомУпак") Тогда
			ДобавитьВМассивЭлементыСпискаXDTO(СведенияОМаркировке.ИндивидуальныеУпаковки, СписокXDTOИзЭлемента(НомСредИдентТов, "НомУпак"));
		КонецЕсли;
		
		КолВедМарк = ПолучитьЗначениеXDTO(НомСредИдентТов, "КолВедМарк");
		Если ЗначениеЗаполнено(КолВедМарк) Тогда
			СведенияОМаркировке.КоличествоТовараВУпаковке = XMLЗначение(Тип("Число"), КолВедМарк);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьСведенияПрослеживаемости(СведенияПрослеживаемости, СведПрослеж, ЭтоУКД)
	
	Для Каждого ЭлементСведПрослеж Из СведПрослеж Цикл
		
		СписокПолей	 = "НомТовПрослеж, ЕдИзмПрослеж, НаимЕдИзмПрослеж, КолВЕдПрослеж, ДопПрослеж, СтТовБезНДСПрослеж";
		Если ЭтоУКД = Истина Тогда
			СписокПолей = СписокПолей + ", ИзмКолЕдПТ";
		КонецЕсли; 
		
		СтрокаПрослеживаемости					 = Новый Структура(СписокПолей);
		СтрокаПрослеживаемости.НомТовПрослеж	 = ПолучитьЗначениеXDTO(ЭлементСведПрослеж, "НомТовПрослеж");
		СтрокаПрослеживаемости.ЕдИзмПрослеж		 = ПолучитьЗначениеXDTO(ЭлементСведПрослеж, "ЕдИзмПрослеж");
		СтрокаПрослеживаемости.НаимЕдИзмПрослеж	 = ПолучитьЗначениеXDTO(ЭлементСведПрослеж, "НаимЕдИзмПрослеж");
		СтрокаПрослеживаемости.КолВЕдПрослеж	 = ПолучитьЗначениеXDTO(ЭлементСведПрослеж, "КолВЕдПрослеж");
		СтрокаПрослеживаемости.ДопПрослеж		 = ПолучитьЗначениеXDTO(ЭлементСведПрослеж, "ДопПрослеж");
		СтрокаПрослеживаемости.СтТовБезНДСПрослеж = ПолучитьЗначениеXDTO(ЭлементСведПрослеж, "СтТовБезНДСПрослеж");
		
		Если ЭтоУКД = Истина 
			И ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ЭлементСведПрослеж, "ИзмКолЕдПТ") Тогда
			
			ЭлементИзмКолЕдПТ = ЭлементСведПрослеж.ИзмКолЕдПТ;
			СписокПолейИзмКолЕдПТ = "КолТовЕдПТДо, КолТовЕдПТПосле, КолТовЕдПТУвел, КолТовЕдПТУм";
			
			ИзмКолЕдПТ							 = Новый Структура(СписокПолейИзмКолЕдПТ);
			ИзмКолЕдПТ.КолТовЕдПТДо				 = ПолучитьЗначениеXDTO(ЭлементИзмКолЕдПТ, "КолТовЕдПТДо");
			ИзмКолЕдПТ.КолТовЕдПТПосле			 = ПолучитьЗначениеXDTO(ЭлементИзмКолЕдПТ, "КолТовЕдПТПосле");
			ИзмКолЕдПТ.КолТовЕдПТУвел			 = ПолучитьЗначениеXDTO(ЭлементИзмКолЕдПТ, "КолТовЕдПТУвел");
			ИзмКолЕдПТ.КолТовЕдПТУм				 = ПолучитьЗначениеXDTO(ЭлементИзмКолЕдПТ, "КолТовЕдПТУм");
			
			СтрокаПрослеживаемости.ИзмКолЕдПТ	 = ИзмКолЕдПТ;
			
		КонецЕсли;
		
		СведенияПрослеживаемости.Добавить(СтрокаПрослеживаемости);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ЗаполнитьНоменклатуруДокумента(СтрокаТЧ, НоменклатураДокумента)

	СтрокаТЧ.ИдентификаторНоменклатуры = НоменклатураДокумента.Идентификатор;
	СтрокаТЧ.НоменклатураДокумента = НоменклатураДокумента;

КонецПроцедуры

Процедура ДобавитьВМассивЭлементыСпискаXDTO(Массив, СписокXDTO)
	
	Для Каждого Элемент Из СписокXDTO Цикл
		Массив.Добавить(Элемент);
	КонецЦикла;
	
КонецПроцедуры

// Формирует массив структур по коллекции информационных полей.
//
// Параметры:
//  СписокИлиОбъектXDTO - СписокXDTO, ОбъектXDTO - информационные поля.
//  ПолеИдентификатор - Строка - Имя атрибута, содержащего идентификатор поля.
//  ПолеЗначение - Строка - Имя атрибута, содержащего значение поля.
// 
// Возвращаемое значение:
//  Массив из НовыйКлючЗначение.
//
Функция ТекстоваяИнформацияЭлектронногоДокумента(СписокИлиОбъектXDTO, ПолеИдентификатор, ПолеЗначение)
	
	Результат = Новый Массив;
	
	Если ТипЗнч(СписокИлиОбъектXDTO) = Тип("СписокXDTO") Тогда
		КоллекцияОбъектовXDTO = СписокИлиОбъектXDTO;
	ИначеЕсли ТипЗнч(СписокИлиОбъектXDTO) = Тип("ОбъектXDTO") Тогда
		КоллекцияОбъектовXDTO = Новый Массив;
		КоллекцияОбъектовXDTO.Добавить(СписокИлиОбъектXDTO);
	Иначе
		КоллекцияОбъектовXDTO = Новый Массив;
	КонецЕсли;
	
	Для Каждого СписокXDTO Из КоллекцияОбъектовXDTO Цикл
		
		Идентификатор = ПолучитьЗначениеXDTO(СписокXDTO, ПолеИдентификатор);
		Значение = ПолучитьЗначениеXDTO(СписокXDTO, ПолеЗначение);
		
		КлючЗначение = НовыйКлючЗначение(Идентификатор, Значение);
		Результат.Добавить(КлючЗначение);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Получает сведения из типового элемента ТекстИнфТип с полями "Идентиф" и "Значен".
//
// Параметры:
//  ТекстИнфТип - СписокXDTO - текстовая информация.
// 
// Возвращаемое значение:
//  Массив из НовыйКлючЗначение.
//
Функция ПрочитатьТекстИнфТипПоУмолчанию(ТекстИнфТип)
	
	ПолеИдентификатор = "Идентиф";
	ПолеЗначение = "Значен";
	
	Результат = ТекстоваяИнформацияЭлектронногоДокумента(
		ТекстИнфТип,
		ПолеИдентификатор,
		ПолеЗначение
	);
	
	Возврат Результат;
	
КонецФункции

// Парсит коллекцию информационных полей в формате приказов 155@, 820@, 970@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.ТаблСчФакт.СведТов
// 
// Возвращаемое значение:
//  Массив из НовыйКлючЗначение.
//
Функция ПрочитатьТекстИнфТипUtd(СтрокаXDTO)
	
	Результат = Новый Массив;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ИнфПолФХЖ2") Тогда
		
		ТекстИнфТип = СтрокаXDTO.ИнфПолФХЖ2;
		Результат = ПрочитатьТекстИнфТипПоУмолчанию(ТекстИнфТип);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Парсит коллекцию информационных полей в формате формализованного счета Диадока.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.ТаблСНО.СведТов
// 
// Возвращаемое значение:
//  Массив из НовыйКлючЗначение.
//
Функция ПрочитатьТекстИнфТипФормализованногоСчета(СтрокаXDTO)
	
	Результат = Новый Массив;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ДопСведТов") Тогда
		
		ТекстИнфТип = СтрокаXDTO.ДопСведТов;
		Результат = ПрочитатьТекстИнфТипПоУмолчанию(ТекстИнфТип);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Парсит коллекцию информационных полей в формате приказа 736@ и 189@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.ТаблКСчФ.СведТов
// 
// Возвращаемое значение:
//  Массив из НовыйКлючЗначение.
//
Функция ПрочитатьТекстИнфТип736(СтрокаXDTO)
	
	Результат = Новый Массив;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ИнфПолФХЖ2") Тогда
		
		ТекстИнфТип = СтрокаXDTO.ИнфПолФХЖ2;
		Результат = ПрочитатьТекстИнфТипПоУмолчанию(ТекстИнфТип);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Парсит коллекцию информационных полей в формате приказа 551@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.СвДокПТПрКроме.СодФХЖ2.СвТов
// 
// Возвращаемое значение:
//  Массив из НовыйКлючЗначение.
//
Функция ПрочитатьТекстИнфТип551(СтрокаXDTO)
	
	Результат = Новый Массив;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ИнфПолФХЖ2") Тогда
		
		ТекстИнфТип = СтрокаXDTO.ИнфПолФХЖ2;
		Результат = ПрочитатьТекстИнфТипПоУмолчанию(ТекстИнфТип);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Парсит коллекцию информационных полей в формате приказа 423@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.СодФХЖ2.РезПрием
// 
// Возвращаемое значение:
//  Массив из НовыйКлючЗначение.
//
Функция ПрочитатьТекстИнфТип423(СтрокаXDTO)
	
	Результат = Новый Массив;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ИнфПолРезПрием") Тогда
		ТекстИнфТип = СтрокаXDTO.ИнфПолРезПрием;
		ИнфоПоля = ПрочитатьТекстИнфТипПоУмолчанию(ТекстИнфТип);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Парсит коллекцию информационных полей в формате приказа 552@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.СвДокПРУ.СодФХЖ1.ОписРабот.Работа
// 
// Возвращаемое значение:
//  Массив из НовыйКлючЗначение.
//
Функция ПрочитатьТекстИнфТип552(СтрокаXDTO)
	
	Результат = Новый Массив;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ИнфПолеОписРабот") Тогда
		
		ТекстИнфТип = СтрокаXDTO.ИнфПолеОписРабот;
		Результат = ПрочитатьТекстИнфТипПоУмолчанию(ТекстИнфТип);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Парсит коллекцию информационных полей накладной в формате приказа 172@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.СвТНО.ТН.Таблица.СвТов
// 
// Возвращаемое значение:
//  Массив из НовыйКлючЗначение.
//
Функция ПрочитатьТекстИнфТипНакладная172(СтрокаXDTO)
	
	Результат = Новый Массив;
	
	Ключ = "";
	Значение = ПолучитьЗначениеXDTO(СтрокаXDTO, "ИнфПолСтр");
	
	Если ЗначениеЗаполнено(Ключ)
		Или ЗначениеЗаполнено(Значение) Тогда
		
		КлючЗначение = НовыйКлючЗначение(Ключ, Значение);
		Результат.Добавить(КлючЗначение);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Парсит коллекцию информационных полей акта в формате приказа 172@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.СвАктИ.ОписРабот.Работа
// 
// Возвращаемое значение:
//  Массив из НовыйКлючЗначение.
//
Функция ПрочитатьТекстИнфТипАкт172(СтрокаXDTO)
	
	Результат = Новый Массив;
	
	Наименование = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимРабот");
	
	Ключ = "";
	Если ЗначениеЗаполнено(Наименование) Тогда
		Ключ = ПолучитьЗначениеXDTO(СтрокаXDTO, "Описание");
	КонецЕсли;
	
	Значение = ПолучитьЗначениеXDTO(СтрокаXDTO, "ИнфПолСтр");
	
	Если ЗначениеЗаполнено(Ключ)
		Или ЗначениеЗаполнено(Значение) Тогда
		
		КлючЗначение = НовыйКлючЗначение(Ключ, Значение);
		Результат.Добавить(КлючЗначение);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает имя информационного поля, которое используется
// для передачи идентификатора номенклатуры.
// 
// Возвращаемое значение:
//  Строка.
//
Функция ИмяПоляИдентификаторНоменклатуры()
	Возврат "ИдентификаторНоменклатуры";
КонецФункции

// Возвращает имя информационного поля, которое используется для передачи
// идентификатора номенклатуры в библиотеке электронных документов (1С-ЭДО).
// 
// Возвращаемое значение:
//  Строка.
//
Функция ИмяПоляИдентификаторНоменклатурыБЭД()
	Возврат "Для1С_Идентификатор";
КонецФункции

// Получает идентификатор номенклатуры, указанный в информационных полях строки документа.
//
// Параметры:
//  ИнфоПоля - Массив из Структура - коллекция дополнительных полей (см. НовыйКлючЗначение).
// 
// Возвращаемое значение:
//  Строка - Идентификатор номенклатуры.
//  Неопределено - если не найдено информационное поле с подходящим именем.
//
Функция ПрочитатьИдентификаторНоменклатуры(ИнфоПоля)
	
	Результат = ЗначениеПоКлючу(ИнфоПоля, ИмяПоляИдентификаторНоменклатуры());
	
	Если Не ЗначениеЗаполнено(Результат) Тогда
		Результат = ЗначениеПоКлючу(ИнфоПоля, ИмяПоляИдентификаторНоменклатурыБЭД());
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Получает значение информационного поля по указанному идентификатору.
//
// Параметры:
//  ИнфоПоля - Массив из Структура - коллекция дополнительных полей (см. НовыйКлючЗначение).
//  Ключ - Строка - идентификатор информационного поля, значение которого нужно получить.
// 
// Возвращаемое значение:
//  Строка - значение информационного поля.
//  Неопределено - если поле с указанным ключом не найдено.
//
Функция ЗначениеПоКлючу(ИнфоПоля, Ключ)
	
	Результат = Неопределено;
	
	Для Каждого КлючЗначение Из ИнфоПоля Цикл
		
		Если КлючЗначение.Ключ = Ключ Тогда
			Результат = КлючЗначение.Значение;
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Конструктор структуры с полями "Ключ" и "Значение".
//
// Параметры:
//  Ключ - Строка - значение поля "Ключ".
//  Значение - Строка - значение поля "Значение".
// 
// Возвращаемое значение:
//  Структура:
//    * Ключ - Строка
//    * Значение - Строка
//
Функция НовыйКлючЗначение(Ключ, Значение)
	
	Результат = Новый Структура;
	Результат.Вставить("Ключ", Ключ);
	Результат.Вставить("Значение", Значение);
	
	Возврат Результат;
	
КонецФункции

// Проверяет, заполнен ли идентификатор номенклатуры в описании номенклатуры электронного документа.
// Если нет, вычисляет его на основании наименования, характеристики, кода и артикула.
// Полученный идентификатор сокращается до 110 символом путем замены окончания строки
// на контрольную сумму, вычисленную по алгоритму MD5.
//
// Параметры:
//  НоменклатураЭД - Структура - см. Контракт_НоменклатураЭлектронногоДокумента.
// 
Процедура ПроверитьЗаполнитьИдентификаторНоменклатуры(НоменклатураЭД)
	
	Если ЗначениеЗаполнено(НоменклатураЭД.Идентификатор) Тогда
		Возврат;
	КонецЕсли;
	
	ФрагментыПредставления = Новый Массив;
	ФрагментыПредставления.Добавить(НоменклатураЭД.Наименование);
	ФрагментыПредставления.Добавить(НоменклатураЭД.Характеристика);
	ФрагментыПредставления.Добавить(НоменклатураЭД.Код);
	ФрагментыПредставления.Добавить(НоменклатураЭД.Артикул);
	
	Представление = "";
	ВсеПоляПустые = Истина;
	Разделитель = "";
	
	Для Каждого Фрагмент Из ФрагментыПредставления Цикл
		
		Если ЗначениеЗаполнено(Фрагмент) Тогда
			ВсеПоляПустые = Ложь;
		КонецЕсли;
		
		Представление = Представление + Разделитель + СокрЛП(Фрагмент);
		Разделитель = "#";
		
	КонецЦикла;
	
	Если ВсеПоляПустые Тогда
		Представление = "";
	КонецЕсли;
	
	МаксимальнаяДлинаИдентификатора = МаксимальнаяДлинаИдентификатораНоменклатуры();
	НоменклатураЭД.Идентификатор = ИдентификаторПоСтроке(Представление, МаксимальнаяДлинаИдентификатора);
	
КонецПроцедуры

// Приводит поля номенклатуры электронного документа к строке.
//
// Параметры:
//  НоменклатураЭД - Структура - см. Контракт_НоменклатураЭлектронногоДокумента
//
Процедура НормализоватьПоляНоменклатурыЭлектронногоДокумента(НоменклатураЭД)
	
	Для Каждого Элемент Из НоменклатураЭД Цикл
		
		НормализованноеЗначение = СокрЛП(XMLСтрока(Элемент.Значение));
		НоменклатураЭД[Элемент.Ключ] = НормализованноеЗначение;
		
	КонецЦикла;
	
КонецПроцедуры

// Длина поля Идентификатор справочника НоменклатураПоставщиков в старых версиях типовых конфигураций.
// 
// Возвращаемое значение:
//  Число.
//
Функция МаксимальнаяДлинаИдентификатораНоменклатуры()
	Возврат 110;
КонецФункции

// Генерирует уникальный строковый идентификатор с ограниченной длиной.
// Входная строка приводится к верхнему регистру, из нее удаляются все пробелы.
// Если длина нормализованной строки превышает заданное ограничение,
// её "хвост" заменяется на хеш-строку, вычисленную по алгоритму MD5.
//
// Параметры:
//  ВходнаяСтрока - Строка - произвольная строка.
//  МаксимальнаяДлина - Число - (Необязательный) максимальная длина формируемого идентификатора.
// 
// Возвращаемое значение:
//  Строка - идентификатор входной строки.
//
Функция ИдентификаторПоСтроке(Знач ВходнаяСтрока, Знач МаксимальнаяДлина = Неопределено)
	
	Если Не ЗначениеЗаполнено(МаксимальнаяДлина) Тогда 
		МаксимальнаяДлина = 110;
	КонецЕсли;
	
	НормализованнаяСтрока = СокрЛП(ВходнаяСтрока);
	НормализованнаяСтрока = ВРег(НормализованнаяСтрока);
	НормализованнаяСтрока = СтрЗаменить(НормализованнаяСтрока, " ", "");
	
	Результат = СократитьСтрокуКонтрольнойСуммой(НормализованнаяСтрока, МаксимальнаяДлина);
	
	Возврат Результат;
	
КонецФункции

// Сокращает строку до нужной длины, при этом обрезанная часть хешируется,
// обеспечивая уникальность строки. Проверяет длину строки на входе и, в случае
// превышения максимальной длины, преобразует ее конец по алгоритму MD5 в
// уникальную строку из 32 символов.
//
// Параметры:
//  Строка            - Строка - исходная строка произвольной длины.
//  МаксимальнаяДлина - Число  - требуемое максимальное количество символов в строке,
//                               минимальное значение: 32.
// 
// Возвращаемое значение:
//   Строка - строка, не превышающая максимальную длину.
//
Функция СократитьСтрокуКонтрольнойСуммой(Знач Строка, Знач МаксимальнаяДлина)
	
	Результат = Строка;
	
	ДлинаИсходнойСтроки = СтрДлина(Строка);
	
	Если ДлинаИсходнойСтроки > МаксимальнаяДлина Тогда
		
		ХешСтрока = ХешСтрокиПоАлгоритмуMD5(Строка);
		ХешСтрока = СтрЗаменить(ХешСтрока, " ", "");
		
		ДлинаХешСтроки = СтрДлина(ХешСтрока);
		
		Если МаксимальнаяДлина < ДлинаХешСтроки Тогда
			
			Ошибка = НСтр("ru = 'Параметр МаксимальнаяДлина не может быть меньше %1 (длина строки, закодированной по алгоритму MD5)'");
			Ошибка = Общее_ПодставитьПараметрыВСтроку(Ошибка, ДлинаХешСтроки);
			ВызватьИсключение Ошибка;
			
		КонецЕсли;
		
		Результат = Лев(Результат, МаксимальнаяДлина - ДлинаХешСтроки) + ХешСтрока;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// ВычислитьХешСтрокиПоАлгоритмуMD5

Функция ХешСтрокиПоАлгоритмуMD5(Знач Строка)
	
	Если ИспользоватьХешированиеДанных() Тогда
		Результат = ХешСтрокиПоАлгоритмуMD5_Платформа(Строка);
	Иначе
		Результат = ХешСтрокиПоАлгоритмуMD5_БСП(Строка);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ХешСтрокиПоАлгоритмуMD5_Платформа(Знач Строка)
	
	Хеширование = Вычислить("Новый ХешированиеДанных(ХешФункция.MD5)");
	Хеширование.Добавить(Строка);
	
	ХешСтрока = Строка(Хеширование.ХешСумма);
	
	Возврат ХешСтрока;
	
КонецФункции

// Возвращает хеш по алгоритму MD5 для произвольной строки.
//
// Параметры:
//  Строка - Строка - произвольная строка любой длины.
//
// Возвращаемое значение:
//  Строка - хеш, вычисленный из строки.
//
Функция ХешСтрокиПоАлгоритмуMD5_БСП(Знач Строка)
	
	a = ЧислоВБинарныйМассив(1732584193); // 01 23 45 67; (шестнадцатеричное представление, сначала младший байт)
	b = ЧислоВБинарныйМассив(4023233417); // 89 AB CD EF;
	c = ЧислоВБинарныйМассив(2562383102); // FE DC BA 98;
	d = ЧислоВБинарныйМассив(271733878);  // 76 54 32 10;
	
	X = Новый Массив(16); // X - блок данных размером 512 бит, массив из 32-битных слов
	
	// разбиваем строку на блоки по 512 бит
	МассивБлоковИзСтроки = ПолучитьМассивБлоковБайтовИзСтроки(Строка);
	
	// для каждого блока производим расчет
	Для НомерБлока = 0 По МассивБлоковИзСтроки.Количество() - 1 Цикл 
		Блок = МассивБлоковИзСтроки[НомерБлока];
		Для НомерСлова = 0 По 15 Цикл
			Слово = 0;
			Для НомерБайта = 1 По 4 Цикл
				Слово = Слово + Блок[НомерСлова * 4 + НомерБайта - 1] * Pow(256, НомерБайта - 1);
			КонецЦикла;
			X[НомерСлова] = Слово; 
		КонецЦикла;
		
		// в последний блок дописываем длину строки в битах
		Если НомерБлока = МассивБлоковИзСтроки.Количество() - 1 Тогда
			РазмерСтрокиВБитах = СтрДлина(Строка) * 8;
			X[14] = РазмерСтрокиВБитах % Pow(2, 32); // сначала младшие 4 байта
			X[15] = Цел(РазмерСтрокиВБитах / Pow(2, 32)) % Pow(2, 64); // если длина превосходит (2^64 - 1) бит, то старшие биты откидываем
		КонецЕсли;
		РассчитатьБлок(a, b, c, d, X);
	КонецЦикла;
	
	Результат = ЧислоВШестнадцатеричномПредставлении(БинарныйМассивВЧисло(a))
			  + ЧислоВШестнадцатеричномПредставлении(БинарныйМассивВЧисло(b))
			  + ЧислоВШестнадцатеричномПредставлении(БинарныйМассивВЧисло(c))
			  + ЧислоВШестнадцатеричномПредставлении(БинарныйМассивВЧисло(d));
	
	Возврат Результат;
	
КонецФункции

Функция ПолучитьМассивБлоковБайтовИзСтроки(Знач Строка)
	Результат = Новый Массив;
	Строка = Строка + Символ(128); // единичный бит + выравнивание нолями до байта
	Пока СтрДлина(Строка) > 0 Цикл
		БлокБайтов = ПолучитьБлокБайтовИзСтроки(Лев(Строка, 64));
		Результат.Добавить(БлокБайтов);
		
		// если последние 8 байт получились не нулевые, то добавляем еще один нулевой блок
		Если СтрДлина(Строка) > 56 И СтрДлина(Строка) < 64 Тогда
			Результат.Добавить(ПолучитьБлокБайтовИзСтроки(""));
		КонецЕсли;
		
		Строка = Сред(Строка, 65);
	КонецЦикла;
	Возврат Результат;
КонецФункции

Функция ПолучитьБлокБайтовИзСтроки(Знач Строка)
	Результат = Новый Массив;
	Для Позиция = 1 По СтрДлина(Строка) Цикл
		Результат.Добавить(КодСимвола(Сред(Строка, Позиция, 1)));
	КонецЦикла;
	
	// дополняем блок нолями
	Для Индекс = Позиция По 64 Цикл
		Результат.Добавить(0);
	КонецЦикла;
	
	Возврат Результат;
КонецФункции

Процедура РассчитатьБлок(a, b, c, d, X)
	
	aa = СкопироватьМассивЛок(a);
	bb = СкопироватьМассивЛок(b);
	cc = СкопироватьМассивЛок(c);
	dd = СкопироватьМассивЛок(d);
	
	// раунд 1
	ВыполнитьОперациюСФункциейF(a, b, c, d, X[ 0],  7, 3614090360); // 0xd76aa478 /* 1 */
	ВыполнитьОперациюСФункциейF(d, a, b, c, X[ 1], 12, 3905402710); // 0xe8c7b756 /* 2 */
	ВыполнитьОперациюСФункциейF(c, d, a, b, X[ 2], 17,  606105819); // 0x242070db /* 3 */
	ВыполнитьОперациюСФункциейF(b, c, d, a, X[ 3], 22, 3250441966); // 0xc1bdceee /* 4 */
	ВыполнитьОперациюСФункциейF(a, b, c, d, X[ 4],  7, 4118548399); // 0xf57c0faf /* 5 */
	ВыполнитьОперациюСФункциейF(d, a, b, c, X[ 5], 12, 1200080426); // 0x4787c62a /* 6 */
	ВыполнитьОперациюСФункциейF(c, d, a, b, X[ 6], 17, 2821735955); // 0xa8304613 /* 7 */
	ВыполнитьОперациюСФункциейF(b, c, d, a, X[ 7], 22, 4249261313); // 0xfd469501 /* 8 */
	ВыполнитьОперациюСФункциейF(a, b, c, d, X[ 8],  7, 1770035416); // 0x698098d8 /* 9 */
	ВыполнитьОперациюСФункциейF(d, a, b, c, X[ 9], 12, 2336552879); // 0x8b44f7af /* 10 */
	ВыполнитьОперациюСФункциейF(c, d, a, b, X[10], 17, 4294925233); // 0xffff5bb1 /* 11 */
	ВыполнитьОперациюСФункциейF(b, c, d, a, X[11], 22, 2304563134); // 0x895cd7be /* 12 */
	ВыполнитьОперациюСФункциейF(a, b, c, d, X[12],  7, 1804603682); // 0x6b901122 /* 13 */
	ВыполнитьОперациюСФункциейF(d, a, b, c, X[13], 12, 4254626195); // 0xfd987193 /* 14 */
	ВыполнитьОперациюСФункциейF(c, d, a, b, X[14], 17, 2792965006); // 0xa679438e /* 15 */
	ВыполнитьОперациюСФункциейF(b, c, d, a, X[15], 22, 1236535329); // 0x49b40821 /* 16 */
	
	// раунд 2
	ВыполнитьОперациюСФункциейG(a, b, c, d, X[ 1],  5, 4129170786); // 0xf61e2562 /* 17 */
	ВыполнитьОперациюСФункциейG(d, a, b, c, X[ 6],  9, 3225465664); // 0xc040b340 /* 18 */
	ВыполнитьОперациюСФункциейG(c, d, a, b, X[11], 14,  643717713); // 0x265e5a51 /* 19 */
	ВыполнитьОперациюСФункциейG(b, c, d, a, X[ 0], 20, 3921069994); // 0xe9b6c7aa /* 20 */
	ВыполнитьОперациюСФункциейG(a, b, c, d, X[ 5],  5, 3593408605); // 0xd62f105d /* 21 */
	ВыполнитьОперациюСФункциейG(d, a, b, c, X[10],  9,   38016083); //  0x2441453 /* 22 */
	ВыполнитьОперациюСФункциейG(c, d, a, b, X[15], 14, 3634488961); // 0xd8a1e681 /* 23 */
	ВыполнитьОперациюСФункциейG(b, c, d, a, X[ 4], 20, 3889429448); // 0xe7d3fbc8 /* 24 */
	ВыполнитьОперациюСФункциейG(a, b, c, d, X[ 9],  5,  568446438); // 0x21e1cde6 /* 25 */
	ВыполнитьОперациюСФункциейG(d, a, b, c, X[14],  9, 3275163606); // 0xc33707d6 /* 26 */
	ВыполнитьОперациюСФункциейG(c, d, a, b, X[ 3], 14, 4107603335); // 0xf4d50d87 /* 27 */
	ВыполнитьОперациюСФункциейG(b, c, d, a, X[ 8], 20, 1163531501); // 0x455a14ed /* 28 */
	ВыполнитьОперациюСФункциейG(a, b, c, d, X[13],  5, 2850285829); // 0xa9e3e905 /* 29 */
	ВыполнитьОперациюСФункциейG(d, a, b, c, X[ 2],  9, 4243563512); // 0xfcefa3f8 /* 30 */
	ВыполнитьОперациюСФункциейG(c, d, a, b, X[ 7], 14, 1735328473); // 0x676f02d9 /* 31 */
	ВыполнитьОперациюСФункциейG(b, c, d, a, X[12], 20, 2368359562); // 0x8d2a4c8a /* 32 */
	
	// раунд 3
	ВыполнитьОперациюСФункциейH(a, b, c, d, X[ 5],  4, 4294588738); // 0xfffa3942 /* 33 */
	ВыполнитьОперациюСФункциейH(d, a, b, c, X[ 8], 11, 2272392833); // 0x8771f681 /* 34 */
	ВыполнитьОперациюСФункциейH(c, d, a, b, X[11], 16, 1839030562); // 0x6d9d6122 /* 35 */
	ВыполнитьОперациюСФункциейH(b, c, d, a, X[14], 23, 4259657740); // 0xfde5380c /* 36 */
	ВыполнитьОперациюСФункциейH(a, b, c, d, X[ 1],  4, 2763975236); // 0xa4beea44 /* 37 */
	ВыполнитьОперациюСФункциейH(d, a, b, c, X[ 4], 11, 1272893353); // 0x4bdecfa9 /* 38 */
	ВыполнитьОперациюСФункциейH(c, d, a, b, X[ 7], 16, 4139469664); // 0xf6bb4b60 /* 39 */
	ВыполнитьОперациюСФункциейH(b, c, d, a, X[10], 23, 3200236656); // 0xbebfbc70 /* 40 */
	ВыполнитьОперациюСФункциейH(a, b, c, d, X[13],  4,  681279174); // 0x289b7ec6 /* 41 */
	ВыполнитьОперациюСФункциейH(d, a, b, c, X[ 0], 11, 3936430074); // 0xeaa127fa /* 42 */
	ВыполнитьОперациюСФункциейH(c, d, a, b, X[ 3], 16, 3572445317); // 0xd4ef3085 /* 43 */
	ВыполнитьОперациюСФункциейH(b, c, d, a, X[ 6], 23,   76029189); //  0x4881d05 /* 44 */
	ВыполнитьОперациюСФункциейH(a, b, c, d, X[ 9],  4, 3654602809); // 0xd9d4d039 /* 45 */
	ВыполнитьОперациюСФункциейH(d, a, b, c, X[12], 11, 3873151461); // 0xe6db99e5 /* 46 */
	ВыполнитьОперациюСФункциейH(c, d, a, b, X[15], 16,  530742520); // 0x1fa27cf8 /* 47 */
	ВыполнитьОперациюСФункциейH(b, c, d, a, X[ 2], 23, 3299628645); // 0xc4ac5665 /* 48 */
	
	// раунд 4
	ВыполнитьОперациюСФункциейI(a, b, c, d, X[ 0],  6, 4096336452); // 0xf4292244 /* 49 */
	ВыполнитьОперациюСФункциейI(d, a, b, c, X[ 7], 10, 1126891415); // 0x432aff97 /* 50 */
	ВыполнитьОперациюСФункциейI(c, d, a, b, X[14], 15, 2878612391); // 0xab9423a7 /* 51 */
	ВыполнитьОперациюСФункциейI(b, c, d, a, X[ 5], 21, 4237533241); // 0xfc93a039 /* 52 */
	ВыполнитьОперациюСФункциейI(a, b, c, d, X[12],  6, 1700485571); // 0x655b59c3 /* 53 */
	ВыполнитьОперациюСФункциейI(d, a, b, c, X[ 3], 10, 2399980690); // 0x8f0ccc92 /* 54 */
	ВыполнитьОперациюСФункциейI(c, d, a, b, X[10], 15, 4293915773); // 0xffeff47d /* 55 */
	ВыполнитьОперациюСФункциейI(b, c, d, a, X[ 1], 21, 2240044497); // 0x85845dd1 /* 56 */
	ВыполнитьОперациюСФункциейI(a, b, c, d, X[ 8],  6, 1873313359); // 0x6fa87e4f /* 57 */
	ВыполнитьОперациюСФункциейI(d, a, b, c, X[15], 10, 4264355552); // 0xfe2ce6e0 /* 58 */
	ВыполнитьОперациюСФункциейI(c, d, a, b, X[ 6], 15, 2734768916); // 0xa3014314 /* 59 */
	ВыполнитьОперациюСФункциейI(b, c, d, a, X[13], 21, 1309151649); // 0x4e0811a1 /* 60 */
	ВыполнитьОперациюСФункциейI(a, b, c, d, X[ 4],  6, 4149444226); // 0xf7537e82 /* 61 */
	ВыполнитьОперациюСФункциейI(d, a, b, c, X[11], 10, 3174756917); // 0xbd3af235 /* 62 */
	ВыполнитьОперациюСФункциейI(c, d, a, b, X[ 2], 15,  718787259); // 0x2ad7d2bb /* 63 */
	ВыполнитьОперациюСФункциейI(b, c, d, a, X[ 9], 21, 3951481745); // 0xeb86d391 /* 64 */
	
	a = БинарнаяСумма(a, aa);
	b = БинарнаяСумма(b, bb);
	c = БинарнаяСумма(c, cc);
	d = БинарнаяСумма(d, dd);
	
КонецПроцедуры

Процедура ВыполнитьОперациюСФункциейF(a, b, c, d, X, s, t)
	ВыполнитьОперацию(ПобитоваяОперацияИли(ПобитоваяОперацияИ(b, c), ПобитоваяОперацияИ(ПобитоваяОперацияНе(b), d)), a, b, X, s, t);
КонецПроцедуры

Процедура ВыполнитьОперациюСФункциейG(a, b, c, d, X, s, t)
	ВыполнитьОперацию(ПобитоваяОперацияИли(ПобитоваяОперацияИ(b, d), ПобитоваяОперацияИ(ПобитоваяОперацияНе(d), c)), a, b, X, s, t);
КонецПроцедуры

Процедура ВыполнитьОперациюСФункциейH(a, b, c, d, X, s, t)
	ВыполнитьОперацию(ПобитоваяОперацияИсключающееИли(ПобитоваяОперацияИсключающееИли(b, c), d), a, b, X, s, t);
КонецПроцедуры

Процедура ВыполнитьОперациюСФункциейI(a, b, c, d, X, s, t)
	ВыполнитьОперацию(ПобитоваяОперацияИсключающееИли(ПобитоваяОперацияИли(ПобитоваяОперацияНе(d), b), c), a, b, X, s, t);
КонецПроцедуры

Процедура ВыполнитьОперацию(q, a, b, X, s, t)
	a = БинарнаяСумма(ЦиклическийСдвигВлево(БинарнаяСумма(БинарнаяСумма(a, q), ЧислоВБинарныйМассив((X + t) % Pow(2, 32))), s), b);
КонецПроцедуры

Функция ЧислоВБинарныйМассив(Знач Число, Знач ВсегоРазрядов = 32)
	Результат = Новый Массив;
	ТекущийРазряд = 0;
	Пока ТекущийРазряд < ВсегоРазрядов Цикл
		ТекущийРазряд = ТекущийРазряд + 1;
		Результат.Добавить(Булево(Число % 2));
		Число = Цел(Число / 2);
	КонецЦикла;
	Возврат Результат;
КонецФункции

Функция БинарныйМассивВЧисло(Массив)
	Результат = 0;
	Для НомерРазряда = -(Массив.Количество() - 1) По 0 Цикл
		Результат = Результат * 2 + Число(Массив[-НомерРазряда]);
	КонецЦикла;
	Возврат Результат;
КонецФункции

Функция ЧислоВШестнадцатеричномПредставлении(Знач Число, Знач КоличествоБайт = 4)
	Результат = "";
	СтрокаСимволов = "0123456789ABCDEF";
	Для НомерБайта = 1 По КоличествоБайт Цикл
		ПредставлениеБайта = "";
		Для Счетчик = 1 По 2 Цикл
			ПредставлениеБайта = Сред(СтрокаСимволов, Число % 16 + 1, 1) + ПредставлениеБайта;
			Число = Цел(Число / 16);
		КонецЦикла;
		
		// порядок байтов: начиная с младшего
		Результат = Результат + ПредставлениеБайта;
	КонецЦикла;
	Возврат Результат;
КонецФункции

Функция ПобитоваяОперацияИ(БинарныйМассив1, БинарныйМассив2)
	Результат = Новый Массив;
	Для Индекс = 0 По БинарныйМассив1.Количество() - 1 Цикл
		Результат.Добавить(БинарныйМассив1[Индекс] И БинарныйМассив2[Индекс]);
	КонецЦикла;	
	Возврат Результат;
КонецФункции

Функция ПобитоваяОперацияИли(БинарныйМассив1, БинарныйМассив2)
	Результат = Новый Массив;
	Для Индекс = 0 По БинарныйМассив1.Количество() - 1 Цикл
		Результат.Добавить(БинарныйМассив1[Индекс] Или БинарныйМассив2[Индекс]);
	КонецЦикла;	
	Возврат Результат;
КонецФункции

Функция ПобитоваяОперацияИсключающееИли(БинарныйМассив1, БинарныйМассив2)
	Результат = Новый Массив;
	Для Индекс = 0 По БинарныйМассив1.Количество() - 1 Цикл
		Результат.Добавить(БинарныйМассив1[Индекс] <> БинарныйМассив2[Индекс]);
	КонецЦикла;	
	Возврат Результат;
КонецФункции

Функция ПобитоваяОперацияНе(БинарныйМассив)
	Результат = Новый Массив;
	Для Каждого Элемент Из БинарныйМассив Цикл
		Результат.Добавить(Не Элемент);
	КонецЦикла;	
	Возврат Результат;
КонецФункции

Функция ЦиклическийСдвигВлево(БинарныйМассив, КоличествоРазрядов)
	Результат = Новый Массив(БинарныйМассив.Количество());
	Для Разряд = 0 По БинарныйМассив.Количество() - 1 Цикл
		Результат[(Разряд + КоличествоРазрядов) % БинарныйМассив.Количество()] = БинарныйМассив[Разряд];
	КонецЦикла;
	Возврат Результат;
КонецФункции

Функция БинарнаяСумма(БинарныйМассив1, БинарныйМассив2)
	Результат = Новый Массив;
	ДобавитьКРазряду = Ложь;
	Для Индекс = 0 По БинарныйМассив1.Количество() - 1 Цикл
		Результат.Добавить(БинарныйМассив1[Индекс] <> БинарныйМассив2[Индекс] <> ДобавитьКРазряду);
		ДобавитьКРазряду = (0 + БинарныйМассив1[Индекс] + БинарныйМассив2[Индекс] + ДобавитьКРазряду) > 1;
	КонецЦикла;
	Возврат Результат;
КонецФункции

Функция СкопироватьМассивЛок(Массив)
	Результат = Новый Массив;
	Для Каждого Элемент Из Массив Цикл
		Результат.Добавить(Элемент);
	КонецЦикла;
	Возврат Результат;
КонецФункции

// Конец ВычислитьХешСтрокиПоАлгоритмуMD5

//{	Методы для заполнения универсальной табличной части


//{ МЕТОДЫ ГЕНЕРАЦИИ XML-файлов ИЗ Proto

Процедура XML_ВыгрузитьФайл(ИмяПакетаXDTO, ОбъектXDTO, ИмяФайла, ТипКодировки = "windows-1251") Экспорт
	
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.УстановитьСтроку(ТипКодировки);
	ЗаписьXML.ЗаписатьОбъявлениеXML();
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);	
	Фабрика.ЗаписатьXML(ЗаписьXML, ОбъектXDTO, , , , НазначениеТипаXML.Неявное);
	
	СтрокаXML = ЗаписьXML.Закрыть();
	
	XML_УдалитьОбъявлениеПрефиксовПространстваИмен(ИмяПакетаXDTO, СтрокаXML);
	
	ТД = Новый ТекстовыйДокумент;
	ТД.УстановитьТекст(СтрокаXML);
	ТД.Записать(ИмяФайла, ТипКодировки);
	
КонецПроцедуры

Функция ОбъектXDTO_В_ДвоичныеДанные(ОбъектXDTO, ТипКодировки = "windows-1251") Экспорт
	
	ИмяПакетаXDTO = ОбъектXDTO.Тип().URIПространстваИмен;
	
	Если ПриложениеСтаршеВерсии("8.3.9") Тогда
		
		ИмяФайла = ПолучитьИмяВременногоФайла("xml");
		
		XML_ВыгрузитьФайл(ИмяПакетаXDTO, ОбъектXDTO, ИмяФайла, ТипКодировки);
		
		Результат = Новый ДвоичныеДанные(ИмяФайла);
		
		Попытка
			
			УдалитьФайлы(ИмяФайла);
			
		Исключение
			
			Ошибка = ИнформацияОбОшибке();
			
			ЖурналРегистрации_ЗаписатьПредупреждение(
				"ФормированиеXML",
				ПодробноеПредставлениеОшибки(Ошибка)
			);
			
		КонецПопытки;
		
	Иначе
		
		ЗаписьXML = Новый ЗаписьXML;
		ЗаписьXML.УстановитьСтроку(ТипКодировки);
		ЗаписьXML.ЗаписатьОбъявлениеXML();
		
		Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
		Фабрика.ЗаписатьXML(ЗаписьXML, ОбъектXDTO, , , , НазначениеТипаXML.Неявное);
		
		СтрокаXML = ЗаписьXML.Закрыть();
		
		XML_УдалитьОбъявлениеПрефиксовПространстваИмен(ИмяПакетаXDTO, СтрокаXML);
		
		Поток = Новый("ПотокВПамяти");
		ПараметрыЗаписи = Новый Массив;
		ПараметрыЗаписи.Добавить(Поток);
		ПараметрыЗаписи.Добавить(ТипКодировки);
		Запись = Новый("ЗаписьДанных", ПараметрыЗаписи);
		
		Запись.ЗаписатьСимволы(СтрокаXML);
		Запись.Закрыть();
		
		Результат = Поток.ЗакрытьИПолучитьДвоичныеДанные();
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ОбъектXDTO_В_СтрокуXML(ОбъектXDTO, ТипКодировки = "windows-1251") Экспорт
	
	ИмяПакетаXDTO = ОбъектXDTO.Тип().URIПространстваИмен;
	
	ЗаписьXML = Новый ЗаписьXML;
	ЗаписьXML.УстановитьСтроку(ТипКодировки);
	ЗаписьXML.ЗаписатьОбъявлениеXML();
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	Фабрика.ЗаписатьXML(ЗаписьXML, ОбъектXDTO, , , , НазначениеТипаXML.Неявное);
	
	Результат = ЗаписьXML.Закрыть();
	
	XML_УдалитьОбъявлениеПрефиксовПространстваИмен(ИмяПакетаXDTO, Результат);
	
	Возврат Результат;
	
КонецФункции

Функция ПротоКонтент_В_ПротоКонтентXDTO(ПротоКонтент, ТипКонтента) Экспорт
	
	Результат = НовыйProtoОбъектXDTO(ТипКонтента);
	
	ЗаполнитьПротоКонтентXDTOПоСтруктуре(Результат, ПротоКонтент);
	
	Возврат Результат;
	
КонецФункции

Функция XML_ПолучитьПустойОбъектXDTO(Тип, ИмяПакетаXDTO) Экспорт
	
	Результат = Неопределено;
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ЧастиПути = РазложитьСтрокуВМассивСлов(Тип, ".");
	
	Если ИмяПакетаXDTO = "Proto" Тогда
		ПространствоИмен = ПространствоИменПрото();
	Иначе
		ПространствоИмен = ИмяПакетаXDTO;
	КонецЕсли;
	
	ТипОбъекта = Фабрика.Тип(ПространствоИмен, ЧастиПути[0]);
	
	ЧастиПути.Удалить(0);
	
	Коллекция = ТипОбъекта.Свойства;
	
	Пока ЗначениеЗаполнено(ЧастиПути) Цикл
		
		Если Коллекция = Неопределено Тогда
			ТипОбъекта = Неопределено;
			Прервать;
		КонецЕсли;
		
		Свойство = Коллекция.Получить(ЧастиПути[0]);
		
		Если Свойство = Неопределено Тогда
			ТипОбъекта = Неопределено;
			Прервать;
		КонецЕсли;
		
		ТипОбъекта = Свойство.Тип;
		
		ЧастиПути.Удалить(0);
		
		Попытка
			Коллекция = ТипОбъекта.Свойства;
		Исключение
			Прервать;
		КонецПопытки;
		
	КонецЦикла;
	
	Если ТипОбъекта <> Неопределено Тогда
		Результат = Фабрика.Создать(ТипОбъекта);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// XML: основные экспортные функции

// Возвращает базовые параметры для формирования файлов XML
//
// Возвращаемое значение:
//   - Структура
//
Функция XML_ПараметрыФормированияФайла() Экспорт
	
	Результат = Новый Структура;
	
	Результат.Вставить("ДатаФормирования"		, ПолучитьТекущуюДатуСеанса());
	Результат.Вставить("УникальныйИдентификатор", Строка(Новый УникальныйИдентификатор));
	
	Результат.Вставить("ИдентификаторОтправителя", "");
	Результат.Вставить("ИдентификаторПолучателя", "");
	
	Результат.Вставить("ТочностьЦены"		, 2);
	Результат.Вставить("ТочностьКоличества"	, 3);
	
	Возврат Результат;
	
КонецФункции

// Возвращает УПД в формате 155@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO - см. функцию ОбъектXDTO_В_ProtoОбъектXDTO();
//  ПараметрыФормированияФайла - Структура  - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - УПД в формате 155@
//
Функция XML_ПолучитьXML_УПД_155(ПротоКонтент, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = "1115125";
	ВерсияФормата	 = "5.01";
	ИмяПакетаXDTO	 = "ON_SCHFDOPPR";
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	
	XML_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл, Документ
	, ПротоКонтент, НалоговыйКод, ВерсияФормата, ПараметрыФормированияФайла);
	
	XML_УПД_155_ЗаполнитьСведенияСФ(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_УПД_155_ЗаполнитьСведенияТаблицыСФ(ИмяПакетаXDTO, Документ, ПротоКонтент, ПараметрыФормированияФайла);
	
	XML_УПД_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_ЗаполнитьРасширенныхПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	Возврат Файл;
	
КонецФункции

// Возвращает УПД в формате 820@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO - см. функцию ОбъектXDTO_В_ProtoОбъектXDTO();
//  ПараметрыФормированияФайла - Структура  - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - УПД в формате 155@
//
Функция XML_ПолучитьXML_УПД_820(ПротоКонтент, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = XML_ОсновнойТитул_НалоговыйКод();
	ВерсияФормата	 = "5.01";
	ИмяПакетаXDTO	 = "ON_NSCHFDOPPR";
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	
	ДопПрефикс = XML_УПД_820_ДопПрефиксИмениФайла(ПротоКонтент);
	
	XML_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл, Документ,
		ПротоКонтент, НалоговыйКод, ВерсияФормата, ПараметрыФормированияФайла, ДопПрефикс);
	
	XML_УПД_820_ЗаполнитьСведенияСФ(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_УПД_820_ЗаполнитьСведенияТаблицыСФ(ИмяПакетаXDTO, Документ, ПротоКонтент, ПараметрыФормированияФайла);
	
	XML_УПД_820_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_ЗаполнитьРасширенныхПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	Возврат Файл;
	
КонецФункции

// Возвращает УПД в формате 970@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO - см. функцию ОбъектXDTO_В_ProtoОбъектXDTO();
//  ПараметрыФормированияФайла - Структура  - см. функцию XML_ПараметрыФормированияФайла();
//
// Возвращаемое значение:
//   - ОбъектXDTO - УПД в формате 970@
//
Функция XML_ПолучитьXML_УПД_970(ПротоКонтент, ПараметрыФормированияФайла) Экспорт
	
	Если XML_УПД_970_ИспользуетсяФормат5_03() Тогда
		Результат = XML_ПолучитьXML_УПД_970_5_03(ПротоКонтент, ПараметрыФормированияФайла);
	Иначе
		Результат = XML_ПолучитьXML_УПД_970_5_02(ПротоКонтент, ПараметрыФормированияФайла);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает УПД в формате 970@5.02
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO - см. функцию ОбъектXDTO_В_ProtoОбъектXDTO();
//  ПараметрыФормированияФайла - Структура  - см. функцию XML_ПараметрыФормированияФайла();
//
// Возвращаемое значение:
//   - ОбъектXDTO - УПД в формате 970@
//
Функция XML_ПолучитьXML_УПД_970_5_02(ПротоКонтент, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод = XML_ОсновнойТитул_НалоговыйКод();
	ВерсияФормата = "5.02";
	ИмяПакетаXDTO = "ON_NSCHFDOPPR_1_997_01_05_02_01";
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	
	XML_УПД_970_ЗаполнитьСведенияОФайле(Файл, ПротоКонтент, "ON_NSCHFDOPPR", ВерсияФормата, ПараметрыФормированияФайла);
	
	XML_УПД_970_ЗаполнитьОсновныеСведенияОДокументе(
		Фабрика,
		Документ,
		ПротоКонтент,
		НалоговыйКод,
		ПараметрыФормированияФайла);
	
	XML_УПД_970_ЗаполнитьСведенияСФ(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_УПД_970_ЗаполнитьСведенияТаблицыСФ(ИмяПакетаXDTO, Документ, ПротоКонтент, ПараметрыФормированияФайла);
	
	XML_УПД_970_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_УПД_970_ЗаполнитьПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	Возврат Файл;
	
КонецФункции

// Возвращает УПД в формате 970@5.03
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO - см. функцию ОбъектXDTO_В_ProtoОбъектXDTO();
//  ПараметрыФормированияФайла - Структура  - см. функцию XML_ПараметрыФормированияФайла();
//
// Возвращаемое значение:
//   - ОбъектXDTO - УПД в формате 970@
//
Функция XML_ПолучитьXML_УПД_970_5_03(ПротоКонтент, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод = XML_ОсновнойТитул_НалоговыйКод();
	ВерсияФормата = "5.03";
	ИмяПакетаXDTO = "ON_NSCHFDOPPR_1_997_01_05_03_01";
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	
	XML_УПД_970_ЗаполнитьСведенияОФайле(Файл, ПротоКонтент, "ON_NSCHFDOPPR", ВерсияФормата, ПараметрыФормированияФайла);
	
	XML_УПД_970_ЗаполнитьОсновныеСведенияОДокументе(
		Фабрика,
		Документ,
		ПротоКонтент,
		НалоговыйКод,
		ПараметрыФормированияФайла);
	
	XML_УПД_970_ЗаполнитьСведенияСФ(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_УПД_970_ЗаполнитьСведенияТаблицыСФ(ИмяПакетаXDTO, Документ, ПротоКонтент, ПараметрыФормированияФайла);
	
	XML_УПД_970_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_УПД_970_ЗаполнитьПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	Возврат Файл;
	
КонецФункции

// Возвращает УКД в формате 189@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO - титул продавца в терминах прото - структуры Диадока,
//                                            см. функцию ОбъектXDTO_В_ProtoОбъектXDTO();
//  ПараметрыФормированияФайла - Структура  - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - УКД в формате 189@
//
Функция XML_ПолучитьXML_УКД(ПротоКонтент, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = "1115127";
	ВерсияФормата	 = "5.01";
	ИмяПакетаXDTO	 = "ON_KORSCHFDOPPR";
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	
	XML_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл, Документ
	, ПротоКонтент, НалоговыйКод, ВерсияФормата, ПараметрыФормированияФайла);
	
	XML_УКД_ЗаполнитьСведенияСФ(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_УКД_ЗаполнитьСведенияТаблицыСФ(ИмяПакетаXDTO, Документ, ПротоКонтент, ПараметрыФормированияФайла);
	
	XML_УКД_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_ЗаполнитьРасширенныхПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	Возврат Файл;
	
КонецФункции

// Возвращает УКД в формате 736@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO - титул продавца в терминах прото - структуры Диадока,
//                                            см. функцию ОбъектXDTO_В_ProtoОбъектXDTO();
//  ПараметрыФормированияФайла - Структура  - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - УКД в формате 736@
//
Функция XML_ПолучитьXML_УКД_736(ПротоКонтент, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = "1115133";
	ВерсияФормата	 = "5.01";
	ИмяПакетаXDTO	 = "ON_NKORSCHFDOPPR";
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	
	ДопПрефикс = XML_УКД_736_ДопПрефиксИмениФайла(ПротоКонтент);
	
	XML_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл, Документ
	, ПротоКонтент, НалоговыйКод, ВерсияФормата, ПараметрыФормированияФайла, ДопПрефикс);
	
	XML_УКД_736_ЗаполнитьСведенияСФ(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_УКД_736_ЗаполнитьСведенияТаблицыСФ(ИмяПакетаXDTO, Документ, ПротоКонтент, ПараметрыФормированияФайла);
	
	XML_УКД_736_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_ЗаполнитьРасширенныхПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	Возврат Файл;
	
КонецФункции

// Возвращает Акт в формате 552@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO - титул продавца в терминах прото - структуры Диадока,
//                                            см. функцию ОбъектXDTO_В_ProtoОбъектXDTO();
//  ПараметрыФормированияФайла - Структура  - см. функцию XML_ПараметрыФормированияФайла()
//                                             игнорируются свойства:
//                                               * ТочностьЦены
//                                               * ТочностьКоличества
// 
// Возвращаемое значение:
//   - ОбъектXDTO - Акт в формате 552@
//
Функция XML_ПолучитьXML_Акт_552(ПротоКонтент, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = "1175012";
	ВерсияФормата	 = "5.02";
	ИмяПакетаXDTO	 = "DP_REZRUISP";
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	
	XML_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл, Документ
	, ПротоКонтент, НалоговыйКод, ВерсияФормата, ПараметрыФормированияФайла);
	
	XML_Акт_552_ЗаполнитьСведенияДокумента(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_Акт_552_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_ЗаполнитьРасширенныхПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	Возврат Файл;
	
КонецФункции

// Возвращает накладную в формате 551@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO - титул продавца в терминах прото - структуры Диадока,
//                                            см. функцию ОбъектXDTO_В_ProtoОбъектXDTO();
//  ПараметрыФормированияФайла - Структура  - см. функцию XML_ПараметрыФормированияФайла()
//                                             игнорируются свойства:
//                                               * ТочностьЦены
//                                               * ТочностьКоличества
// 
// Возвращаемое значение:
//   - ОбъектXDTO - накладная в формате 551@
//
Функция XML_ПолучитьXML_Торг_551(ПротоКонтент, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = "1175010";
	ВерсияФормата	 = "5.02";
	ИмяПакетаXDTO	 = "DP_TOVTORGPR";
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	
	XML_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл, Документ
	, ПротоКонтент, НалоговыйКод, ВерсияФормата, ПараметрыФормированияФайла);
	
	XML_Торг_551_ЗаполнитьСведенияДокумента(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_Торг_551_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_ЗаполнитьРасширенныхПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	Возврат Файл;
	
КонецФункции

// Возвращает счет в формате Диадока
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO - титул продавца в терминах прото - структуры Диадока,
//                                            см. функцию ОбъектXDTO_В_ProtoОбъектXDTO();
//  ПараметрыФормированияФайла - Структура  - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - счет во формате Диадока
//
Функция XML_ПолучитьXML_Счет(ПротоКонтент, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = "";
	ВерсияФормата	 = "1.01";
	ИмяПакетаXDTO	 = "ON_SCHET";
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	
	XML_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл, Документ
	, ПротоКонтент, НалоговыйКод, ВерсияФормата, ПараметрыФормированияФайла);
	
	XML_Счет_ЗаполнитьСведенияДокумента(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_Счет_ЗаполнитьСведенияТабЧасти(ИмяПакетаXDTO, Документ, ПротоКонтент, ПараметрыФормированияФайла);
	
	XML_ЗаполнитьПодписанта(ИмяПакетаXDTO, Документ, ПротоКонтент, "Signer");
	
	Возврат Файл;
	
КонецФункции

// Возвращает Торг2 в формате 423@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO - титул продавца в терминах прото - структуры Диадока,
//                                            см. функцию ОбъектXDTO_В_ProtoОбъектXDTO();
//  ПараметрыФормированияФайла - Структура  - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - счет во формате Диадока
//
Функция XML_ПолучитьXML_Торг2(ПротоКонтент, ПараметрыФормированияФайла) Экспорт 
	
	НалоговыйКод	 = "1175014";
	ВерсияФормата	 = "5.01";
	ИмяПакетаXDTO	 = "DP_PRIRASXPRIN";
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	
	ДопПрефикс = XML_Торг2_ДопПрефиксИмениФайла(ПротоКонтент);
	
	XML_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл, Документ,
		ПротоКонтент, НалоговыйКод, ВерсияФормата, ПараметрыФормированияФайла, ДопПрефикс);
	
	СодФХЖ1 = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СодФХЖ1");
	XML_Торг2_ЗаполнитьСведенияОбОбстоятельствахПриемки(ИмяПакетаXDTO, СодФХЖ1, ПротоКонтент.CircumstancesAcceptanceInfo, ПараметрыФормированияФайла);
	
	СодФХЖ2 = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СодФХЖ2");
	XML_Торг2_ЗаполнитьСведенияОФактеПриемки(ИмяПакетаXDTO, СодФХЖ2, ПротоКонтент.EvidenceAcceptanceInfo, ПараметрыФормированияФайла);
	
	XML_Торг2_ЗаполнитьПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент.Signers);
	
	Возврат Файл;
	
КонецФункции

// Возвращает акт сверки в формате 405@
//
// Параметры:
//  ПротоКонтент - ОбъектXDTO - титул отправителя в терминах прото (ReconciliationActSenderTitle)
//  ПараметрыФормированияФайла - Структура - см. функцию XML_ПараметрыФормированияФайла()
// 
// Возвращаемое значение:
//  ОбъектXDTO - титул отправителя акта сверки по схеме ФНС (см. ON_AKTSVEROTP_xsd)
//
Функция XML_ПолучитьXML_АктСверки_405(ПротоКонтент, ПараметрыФормированияФайла) Экспорт 
	
	НалоговыйКод = "1110332";
	ВерсияФормата = "5.01";
	ИмяПакетаXDTO = "ON_AKTSVEROTP";
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	
	XML_АктСверки_ЗаполнитьСведенияОДокументе(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_АктСверки_ЗаполнитьТаблицуВзаиморасчетов(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_АктСверки_ЗаполнитьПодписанта(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_АктСверки_ЗаполнитьОсновныеСведенияФайла(
		ИмяПакетаXDTO,
		Файл,
		Документ,
		ПротоКонтент,
		НалоговыйКод,
		ВерсияФормата,
		ПараметрыФормированияФайла
	);
	
	Возврат Файл;
	
КонецФункции

// Возвращает ответный титул УПД в формате 820@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO     - ответный титул в терминах прото структуры Диадока;
//  ФайлТитулаПродавца         - ДвоичныеДанные - файл УПД в формате 820@;
//  ФайлыПодписейПродавца      - Массив         - содержит все файлы подписей продавца;
//                             - ДвоичныеДанные - если файл подписи продавца всего один;
//  ПараметрыФормированияФайла - Структура      - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - ответный титул УПД в формате 820@
//
Функция XML_ОтветныйТитул_УПД_820(ПротоКонтент, ФайлТитулаПродавца, ФайлыПодписейПродавца, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = XML_ОтветныйТитул_НалоговыйКод();
	ВерсияФормата	 = "5.01";
	ИмяПакетаXDTO	 = "ON_NSCHFDOPPOK";
	
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ТитулПродавца	 = XML_В_ОбъектXDTO(ФайлТитулаПродавца);
	
	ИдентификаторПродавца	 = ТитулПродавца.СвУчДокОбор.ИдОтпр;
	ИдентификаторПокупателя	 = ТитулПродавца.СвУчДокОбор.ИдПол;
	
	ИмяФайлаПродавца	 = ТитулПродавца.ИдФайл;
	ДатаФайлаПродавца	 = ТитулПродавца.Документ.ДатаИнфПр;
	ВремяФайлаПродавца	 = ТитулПродавца.Документ.ВремИнфПр;
	
	ДатаДокумента	 = ТитулПродавца.Документ.СвСчФакт.ДатаСчФ;
	НомерДокумента	 = ТитулПродавца.Документ.СвСчФакт.НомерСчФ;
	
	НаименованиеДокумента	 = ТитулПродавца.Документ.НаимДокОпр;
	ФункцияДокумента		 = ТитулПродавца.Документ.Функция;
	
	Файл				= XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	ИнфПок		 		= НовыйДочернийОбъектXDTO(Фабрика, Файл,	"ИнфПок");
	ИдИнфПрод			= НовыйДочернийОбъектXDTO(Фабрика, ИнфПок,	"ИдИнфПрод");
	СодФХЖ4		 		= НовыйДочернийОбъектXDTO(Фабрика, ИнфПок,	"СодФХЖ4");
	
	Если Найти(ИмяФайлаПродавца, "ON_NSCHFDOPPRPROS") = 1 Тогда
		ДопПрефикс = "PROS";
	ИначеЕсли Найти(ИмяФайлаПродавца, "ON_NSCHFDOPPRMARK") = 1 Тогда
		ДопПрефикс = "MARK";
	КонецЕсли;
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл
	, ВерсияФормата, ИдентификаторПродавца, ИдентификаторПокупателя, ПараметрыФормированияФайла, ДопПрефикс);
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияПокупателя(ИнфПок
	, ПротоКонтент, НалоговыйКод, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_ЗаполнитьСведенияПродавца(ИмяПакетаXDTO, ИдИнфПрод
	, ИмяФайлаПродавца, ДатаФайлаПродавца, ВремяФайлаПродавца, ФайлыПодписейПродавца);
	
	XML_ОтветныйТитул_УПД_820_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, СодФХЖ4
	, ПротоКонтент, НаименованиеДокумента, ФункцияДокумента, НомерДокумента, ДатаДокумента);
	
	XML_ОтветныйТитул_УПД_ЗаполнитьИнфПокГосЗакКазн(ИмяПакетаXDTO, ИнфПок, ПротоКонтент);

	XML_ОтветныйТитул_УПД_820_ЗаполнитьРасширенныхПодписантов(ИмяПакетаXDTO, ИнфПок, ПротоКонтент);
		
	Возврат Файл;
	
КонецФункции

// Возвращает ответный титул УПД в формате 970@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO     - ответный титул в терминах прото структуры Диадока;
//  ФайлТитулаПродавца         - ДвоичныеДанные - файл УПД в формате 820@;
//  ФайлыПодписейПродавца      - Массив         - содержит все файлы подписей продавца;
//                             - ДвоичныеДанные - если файл подписи продавца всего один;
//  ПараметрыФормированияФайла - Структура      - см. функцию XML_ПараметрыФормированияФайла();
//
// Возвращаемое значение:
//   - ОбъектXDTO - ответный титул УПД в формате 970@
//
Функция XML_ОтветныйТитул_УПД_970(
		ПротоКонтент,
		ФайлТитулаПродавца,
		ФайлыПодписейПродавца,
		ПараметрыФормированияФайла) Экспорт
		
	ТитулПродавца = XML_В_ОбъектXDTO(ФайлТитулаПродавца);
	
	Если ТитулПродавца.ВерсФорм = "5.03" Тогда
		ИмяПакетаXDTO = XML_ОтветныйТитул_УПД_970_ИмяПакета_5_03();
	Иначе
		ИмяПакетаXDTO = XML_ОтветныйТитул_УПД_970_ИмяПакета_5_02();
	КонецЕсли;
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	
	ИдИнфПрод = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ИдИнфПрод");
	СодФХЖ4 = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СодФХЖ4");
	
	XML_ОтветныйТитул_УПД_970_ЗаполнитьОсновныеСведенияФайла(Файл, ТитулПродавца, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_УПД_970_ЗаполнитьОсновныеДокумента(
		Документ,
		ПротоКонтент,
		ТитулПродавца,
		ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_УПД_970_ЗаполнитьСведенияПродавца(ИмяПакетаXDTO, ИдИнфПрод, ТитулПродавца, ФайлыПодписейПродавца);
	
	XML_ОтветныйТитул_УПД_970_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, СодФХЖ4, ПротоКонтент, ТитулПродавца);
	
	XML_ОтветныйТитул_УПД_ЗаполнитьИнфПокГосЗакКазн(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_УПД_970_ЗаполнитьПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	Возврат Файл;
	
КонецФункции

// Возвращает ответный титул УПД в формате 155@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO     - ответный титул в терминах прото структуры Диадока;
//  ФайлТитулаПродавца         - ДвоичныеДанные - файл УПД в формате 155@;
//  ФайлыПодписейПродавца      - Массив         - содержит все файлы подписей продавца;
//                             - ДвоичныеДанные - если файл подписи продавца всего один;
//  ПараметрыФормированияФайла - Структура      - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - ответный титул УПД в формате 155@
//
Функция XML_ОтветныйТитул_УПД_155(ПротоКонтент, ФайлТитулаПродавца, ФайлыПодписейПродавца, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = "1115126";
	ВерсияФормата	 = "5.01";
	ИмяПакетаXDTO	 = "ON_SCHFDOPPOK";
	
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ТитулПродавца	 = XML_В_ОбъектXDTO(ФайлТитулаПродавца);
	
	ИдентификаторПродавца	 = ТитулПродавца.СвУчДокОбор.ИдОтпр;
	ИдентификаторПокупателя	 = ТитулПродавца.СвУчДокОбор.ИдПол;
	
	ИмяФайлаПродавца	 = ТитулПродавца.ИдФайл;
	ДатаФайлаПродавца	 = ТитулПродавца.Документ.ДатаИнфПр;
	ВремяФайлаПродавца	 = ТитулПродавца.Документ.ВремИнфПр;
	
	ДатаДокумента	 = ТитулПродавца.Документ.СвСчФакт.ДатаСчФ;
	НомерДокумента	 = ТитулПродавца.Документ.СвСчФакт.НомерСчФ;
	
	НаименованиеДокумента	 = ТитулПродавца.Документ.НаимДокОпр;
	ФункцияДокумента		 = ТитулПродавца.Документ.Функция;
	
	Файл		 = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	ИнфПок		 = НовыйДочернийОбъектXDTO(Фабрика, Файл	, "ИнфПок");
	ИдИнфПрод	 = НовыйДочернийОбъектXDTO(Фабрика, ИнфПок	, "ИдИнфПрод");
	СодФХЖ4		 = НовыйДочернийОбъектXDTO(Фабрика, ИнфПок	, "СодФХЖ4");
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл
	, ВерсияФормата, ИдентификаторПродавца, ИдентификаторПокупателя, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияПокупателя(ИнфПок
	, ПротоКонтент, НалоговыйКод, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_ЗаполнитьСведенияПродавца(ИмяПакетаXDTO, ИдИнфПрод
	, ИмяФайлаПродавца, ДатаФайлаПродавца, ВремяФайлаПродавца, ФайлыПодписейПродавца);
	
	XML_ОтветныйТитул_УПД_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, СодФХЖ4
	, ПротоКонтент, НаименованиеДокумента, ФункцияДокумента, НомерДокумента, ДатаДокумента);
	
	XML_ЗаполнитьРасширенныхПодписантов(ИмяПакетаXDTO, ИнфПок, ПротоКонтент);
	
	Возврат Файл;
	
КонецФункции

// Возвращает ответный титул УКД в формате 189@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO     - ответный титул в терминах прото структуры Диадока;
//  ФайлТитулаПродавца         - ДвоичныеДанные - файл УПД в формате 189@;
//  ФайлыПодписейПродавца      - Массив         - содержит все файлы подписей продавца;
//                             - ДвоичныеДанные - если файл подписи продавца всего один;
//  ПараметрыФормированияФайла - Структура      - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - ответный титул УКД в формате 189@
//
Функция XML_ОтветныйТитул_УКД(ПротоКонтент, ФайлТитулаПродавца, ФайлыПодписейПродавца, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = "1115128";
	ВерсияФормата	 = "5.01";
	ИмяПакетаXDTO	 = "ON_KORSCHFDOPPOK";
	
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ТитулПродавца	 = XML_В_ОбъектXDTO(ФайлТитулаПродавца);
	
	ИдентификаторПродавца	 = ТитулПродавца.СвУчДокОбор.ИдОтпр;
	ИдентификаторПокупателя	 = ТитулПродавца.СвУчДокОбор.ИдПол;
	
	ИмяФайлаПродавца	 = ТитулПродавца.ИдФайл;
	ДатаФайлаПродавца	 = ТитулПродавца.Документ.ДатаИнфПр;
	ВремяФайлаПродавца	 = ТитулПродавца.Документ.ВремИнфПр;
	
	ДатаДокумента	 = ТитулПродавца.Документ.СвКСчФ.ДатаКСчФ;
	НомерДокумента	 = ТитулПродавца.Документ.СвКСчФ.НомерКСчФ;
	
	НаименованиеДокумента	 = ТитулПродавца.Документ.НаимДокОпр;
	ФункцияДокумента		 = ТитулПродавца.Документ.Функция;
	
	Файл		 = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	ИнфПок		 = НовыйДочернийОбъектXDTO(Фабрика, Файл	, "ИнфПок");
	ИдИнфПрод	 = НовыйДочернийОбъектXDTO(Фабрика, ИнфПок	, "ИдИнфПрод");
	СодФХЖ4		 = НовыйДочернийОбъектXDTO(Фабрика, ИнфПок	, "СодФХЖ4");
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл
	, ВерсияФормата, ИдентификаторПродавца, ИдентификаторПокупателя, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияПокупателя(ИнфПок
	, ПротоКонтент, НалоговыйКод, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_ЗаполнитьСведенияПродавца(ИмяПакетаXDTO, ИдИнфПрод
	, ИмяФайлаПродавца, ДатаФайлаПродавца, ВремяФайлаПродавца, ФайлыПодписейПродавца);
	
	XML_ОтветныйТитул_УКД_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, СодФХЖ4
	, ПротоКонтент, НаименованиеДокумента, ФункцияДокумента, НомерДокумента, ДатаДокумента);
	
	XML_ЗаполнитьРасширенныхПодписантов(ИмяПакетаXDTO, ИнфПок, ПротоКонтент);
	
	Возврат Файл;
	
КонецФункции

// Возвращает ответный титул УКД в формате 736@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO     - ответный титул в терминах прото структуры Диадока;
//  ФайлТитулаПродавца         - ДвоичныеДанные - файл УПД в формате 736@;
//  ФайлыПодписейПродавца      - Массив         - содержит все файлы подписей продавца;
//                             - ДвоичныеДанные - если файл подписи продавца всего один;
//  ПараметрыФормированияФайла - Структура      - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - ответный титул УКД в формате 736@
//
Функция XML_ОтветныйТитул_УКД_736(ПротоКонтент, ФайлТитулаПродавца, ФайлыПодписейПродавца, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = "1115134";
	ВерсияФормата	 = "5.01";
	ИмяПакетаXDTO	 = "ON_NKORSCHFDOPPOK";
	
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ТитулПродавца	 = XML_В_ОбъектXDTO(ФайлТитулаПродавца);
	
	ИдентификаторПродавца	 = ТитулПродавца.СвУчДокОбор.ИдОтпр;
	ИдентификаторПокупателя	 = ТитулПродавца.СвУчДокОбор.ИдПол;
	
	ИмяФайлаПродавца	 = ТитулПродавца.ИдФайл;
	ДатаФайлаПродавца	 = ТитулПродавца.Документ.ДатаИнфПр;
	ВремяФайлаПродавца	 = ТитулПродавца.Документ.ВремИнфПр;
	
	ДатаДокумента	 = ТитулПродавца.Документ.СвКСчФ.ДатаКСчФ;
	НомерДокумента	 = ТитулПродавца.Документ.СвКСчФ.НомерКСчФ;
	
	НаименованиеДокумента	 = ТитулПродавца.Документ.НаимДокОпр;
	ФункцияДокумента		 = ТитулПродавца.Документ.Функция;
	
	ДопПрефикс = "";
	Если Найти(ИмяФайлаПродавца, "ON_NKORSCHFDOPPRMARK") = 1 Тогда
		ДопПрефикс = "MARK";
	ИначеЕсли Найти(ИмяФайлаПродавца, "ON_NKORSCHFDOPPRPROS") = 1 Тогда
		ДопПрефикс = "PROS";
	КонецЕсли;
	
	Файл		 = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	ИнфПок		 = НовыйДочернийОбъектXDTO(Фабрика, Файл	, "ИнфПок");
	ИдИнфПрод	 = НовыйДочернийОбъектXDTO(Фабрика, ИнфПок	, "ИдИнфПрод");
	СодФХЖ4		 = НовыйДочернийОбъектXDTO(Фабрика, ИнфПок	, "СодФХЖ4");
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл
	, ВерсияФормата, ИдентификаторПродавца, ИдентификаторПокупателя, ПараметрыФормированияФайла, ДопПрефикс);
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияПокупателя(ИнфПок
	, ПротоКонтент, НалоговыйКод, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_ЗаполнитьСведенияПродавца(ИмяПакетаXDTO, ИдИнфПрод
	, ИмяФайлаПродавца, ДатаФайлаПродавца, ВремяФайлаПродавца, ФайлыПодписейПродавца);
	
	XML_ОтветныйТитул_УКД_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, СодФХЖ4
	, ПротоКонтент, НаименованиеДокумента, ФункцияДокумента, НомерДокумента, ДатаДокумента);
	
	XML_ЗаполнитьРасширенныхПодписантов(ИмяПакетаXDTO, ИнфПок, ПротоКонтент);
	
	Возврат Файл;
	
КонецФункции

// Возвращает ответный титул накладной в формате 551@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO     - ответный титул в терминах прото структуры Диадока;
//  ФайлТитулаПродавца         - ДвоичныеДанные - файл накладной в формате 551@;
//  ФайлыПодписейПродавца      - Массив         - содержит все файлы подписей продавца;
//                             - ДвоичныеДанные - если файл подписи продавца всего один;
//  ПараметрыФормированияФайла - Структура      - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - ответный титул накладной в формате 551@
//
Функция XML_ОтветныйТитул_Торг_551(ПротоКонтент, ФайлТитулаПродавца, ФайлыПодписейПродавца, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = "1175011";
	ВерсияФормата	 = "5.02";
	ИмяПакетаXDTO	 = "DP_TOVTORGPOK";
	
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ТитулПродавца	 = XML_В_ОбъектXDTO(ФайлТитулаПродавца);
	
	ИдентификаторПродавца	 = ТитулПродавца.СвУчДокОбор.ИдОтпр;
	ИдентификаторПокупателя	 = ТитулПродавца.СвУчДокОбор.ИдПол;
	
	ИмяФайлаПродавца	 = ТитулПродавца.ИдФайл;
	ДатаФайлаПродавца	 = ТитулПродавца.Документ.ДатаИнфПр;
	ВремяФайлаПродавца	 = ТитулПродавца.Документ.ВремИнфПр;
	
	ДатаДокумента			 = ТитулПродавца.Документ.СвДокПТПрКроме.СвДокПТПр.ИдентДок.ДатаДокПТ;
	НомерДокумента			 = ТитулПродавца.Документ.СвДокПТПрКроме.СвДокПТПр.ИдентДок.НомДокПТ;
	НаименованиеДокумента	 = ТитулПродавца.Документ.СвДокПТПрКроме.СвДокПТПр.НаимДок.НаимДокОпр;
	
	Файл		 = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ	 = НовыйДочернийОбъектXDTO(Фабрика, Файл	, "Документ");
	ИдДокПТПр	 = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ИдДокПТПр");
	СодФХЖ4		 = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СодФХЖ4");
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл
	, ВерсияФормата, ИдентификаторПродавца, ИдентификаторПокупателя, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияПокупателя(Документ
	, ПротоКонтент, НалоговыйКод, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_ЗаполнитьСведенияПродавца(ИмяПакетаXDTO, ИдДокПТПр
	, ИмяФайлаПродавца, ДатаФайлаПродавца, ВремяФайлаПродавца, ФайлыПодписейПродавца);
	
	XML_ОтветныйТитул_Торг_551_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, СодФХЖ4
	, ПротоКонтент, НаименованиеДокумента, НомерДокумента, ДатаДокумента);
	
	XML_ЗаполнитьРасширенныхПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	Возврат Файл;
	
КонецФункции

// Возвращает титул заказчитка в формате 552@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO     - титул заказчика в терминах прото структуры Диадока;
//  ФайлТитулаПродавца         - ДвоичныеДанные - файл акта в формате 552@;
//  ФайлыПодписейПродавца      - Массив         - содержит все файлы подписей исполнителя;
//                             - ДвоичныеДанные - если файл подписи исполнителя всего один;
//  ПараметрыФормированияФайла - Структура      - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - титул заказчитка в формате 552@
//
Функция XML_ОтветныйТитул_Акт_552(ПротоКонтент, ФайлТитулаПродавца, ФайлыПодписейПродавца, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = "1175013";
	ВерсияФормата	 = "5.02";
	ИмяПакетаXDTO	 = "DP_REZRUZAK";
	
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ТитулПродавца	 = XML_В_ОбъектXDTO(ФайлТитулаПродавца);
	
	ИдентификаторПродавца	 = ТитулПродавца.СвУчДокОбор.ИдОтпр;
	ИдентификаторПокупателя	 = ТитулПродавца.СвУчДокОбор.ИдПол;
	
	ИмяФайлаПродавца	 = ТитулПродавца.ИдФайл;
	ДатаФайлаПродавца	 = ТитулПродавца.Документ.ДатаИнфИсп;
	ВремяФайлаПродавца	 = ТитулПродавца.Документ.ВремИнфИсп;
	
	ДатаДокумента			 = ТитулПродавца.Документ.СвДокПРУ.ИдентДок.ДатаДокПРУ;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ТитулПродавца.Документ.СвДокПРУ.ИдентДок, "НомДокПРУ") Тогда
		НомерДокумента = ТитулПродавца.Документ.СвДокПРУ.ИдентДок.НомДокПРУ;
	КонецЕсли;
	
	НаименованиеДокумента	 = ТитулПродавца.Документ.СвДокПРУ.НаимДок.НаимДокОпр;
	
	Файл		 = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ	 = НовыйДочернийОбъектXDTO(Фабрика, Файл	, "Документ");
	ИдДокПРУИсп	 = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ИдДокПРУИсп");
	СодФХЖ3		 = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СодФХЖ3");
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл
	, ВерсияФормата, ИдентификаторПродавца, ИдентификаторПокупателя, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияПокупателя(Документ
	, ПротоКонтент, НалоговыйКод, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_ЗаполнитьСведенияПродавца(ИмяПакетаXDTO, ИдДокПРУИсп
	, ИмяФайлаПродавца, ДатаФайлаПродавца, ВремяФайлаПродавца, ФайлыПодписейПродавца);
	
	XML_ОтветныйТитул_Акт_552_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, СодФХЖ3
	, ПротоКонтент, НаименованиеДокумента, НомерДокумента, ДатаДокумента);
	
	XML_ЗаполнитьРасширенныхПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	Возврат Файл;
	
КонецФункции

// Возвращает ответный титул накладной в формате 172@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO     - ответный титул в терминах прото структуры Диадока;
//  ФайлТитулаПродавца         - ДвоичныеДанные - файл накладной в формате 172@;
//  ПараметрыФормированияФайла - Структура      - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - ответный титул накладной в формате 172@
//
Функция XML_ОтветныйТитул_Торг_172(ПротоКонтент, ФайлТитулаПродавца, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = "1175005";
	ВерсияФормата	 = "5.01";
	ИмяПакетаXDTO	 = "DP_PTORG12";
	
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ТитулПродавца	 = XML_В_ОбъектXDTO(ФайлТитулаПродавца);
	
	ИдентификаторПродавца	 = ТитулПродавца.СвУчДокОбор.ИдОтпр;
	ИдентификаторПокупателя	 = ТитулПродавца.СвУчДокОбор.ИдПок;
	
	ИмяФайлаПродавца	 = ТитулПродавца.ИдФайл;
	ДатаФайлаПродавца	 = ТитулПродавца.Документ.ДатаДок;
	ВремяФайлаПродавца	 = ТитулПродавца.Документ.ВремДок;
	
	ДатаДокумента	 = ТитулПродавца.Документ.СвТНО.ТН.ДатаТН;
	НомерДокумента	 = ТитулПродавца.Документ.СвТНО.ТН.НомТН;
	
	Файл	 = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл	, "Документ");
	ИдТНО	 = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ИдТНО");
	СвТНП	 = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СвТНП");
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл
	, ВерсияФормата, ИдентификаторПродавца, ИдентификаторПокупателя, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияПокупателя(Документ
	, ПротоКонтент, НалоговыйКод, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_ЗаполнитьСведенияПокупателя_Торг_172(ИмяПакетаXDTO, СвТНП
	, ПротоКонтент, НомерДокумента, ДатаДокумента);
	
	XML_ОтветныйТитул_ЗаполнитьСведенияПродавца(ИмяПакетаXDTO, ИдТНО
	, ИмяФайлаПродавца, ДатаФайлаПродавца, ВремяФайлаПродавца);
	
	XML_ЗаполнитьПодписанта(ИмяПакетаXDTO, Документ, ПротоКонтент, "Signer");
	
	Возврат Файл;
	
КонецФункции

// Возвращает титул заказчика в формате 172@
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO     - титул заказчика в терминах прото структуры Диадока;
//  ФайлТитулаПродавца         - ДвоичныеДанные - файл акта в формате 172@;
//  ПараметрыФормированияФайла - Структура      - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - титул заказчика в формате 172@
//
Функция XML_ОтветныйТитул_Акт_172(ПротоКонтент, ФайлТитулаПродавца, ПараметрыФормированияФайла) Экспорт
	
	НалоговыйКод	 = "1175007";
	ВерсияФормата	 = "5.01";
	ИмяПакетаXDTO	 = "DP_ZAKTPRM";
	
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ТитулПродавца	 = XML_В_ОбъектXDTO(ФайлТитулаПродавца);
	
	ИдентификаторПродавца	 = ТитулПродавца.СвУчДокОбор.ИдОтпр;
	ИдентификаторПокупателя	 = ТитулПродавца.СвУчДокОбор.ИдПок;
	
	ИмяФайлаПродавца	 = ТитулПродавца.ИдФайл;
	ДатаФайлаПродавца	 = ТитулПродавца.Документ.ДатаДок;
	ВремяФайлаПродавца	 = ТитулПродавца.Документ.ВремДок;
	
	ДатаДокумента	 = ТитулПродавца.Документ.СвАктИ.ДатаАкт;
	НомерДокумента	 = ТитулПродавца.Документ.СвАктИ.НомАкт;
	
	Файл	 = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл	, "Документ");
	СвАктИ	 = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СвАктИ");
	ИдАктИ	 = НовыйДочернийОбъектXDTO(Фабрика, СвАктИ	, "ИдАктИ");
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл
	, ВерсияФормата, ИдентификаторПродавца, ИдентификаторПокупателя, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияПокупателя(Документ
	, ПротоКонтент, НалоговыйКод, ПараметрыФормированияФайла);
	
	XML_ОтветныйТитул_ЗаполнитьСведенияПокупателя_Акт_172(ИмяПакетаXDTO, СвАктИ
	, ПротоКонтент, НомерДокумента, ДатаДокумента);
	
	XML_ОтветныйТитул_ЗаполнитьСведенияПродавца(ИмяПакетаXDTO, ИдАктИ
	, ИмяФайлаПродавца, ДатаФайлаПродавца, ВремяФайлаПродавца);
	
	XML_ЗаполнитьПодписанта(ИмяПакетаXDTO, Документ, ПротоКонтент, "Signer");
	
	Возврат Файл;
	
КонецФункции

// Возвращает ответный титул акта о расхождениях в формате 423@
//
// Параметры:
//  ПротоКонтент - ОбъектXDTO - ответный титул в терминах прото (см. Torg2RecipientTitle)
//  ФайлТитулаОтправителя - ДвоичныеДанные - акт о расхождениях (информация отправителя)
//  ФайлыПодписейОтправителя - ДвоичныеДанные, Массив из ДвоичныеДанные - подписи к файлу отправителя
//  ПараметрыФормированияФайла - Структура - см. XML_ПараметрыФормированияФайла
// 
// Возвращаемое значение:
//  ОбъектXDTO - акт о расхождениях (информация получателя), см. DP_PRIRASXSVED_xsd
//
Функция XML_ОтветныйТитул_Торг2(ПротоКонтент, ФайлТитулаОтправителя, ФайлыПодписейОтправителя, ПараметрыФормированияФайла) Экспорт 
	
	НалоговыйКод	 = "1175015";
	ВерсияФормата	 = "5.01";
	ИмяПакетаXDTO	 = "DP_PRIRASXSVED";
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ТитулОтправителя = XML_В_ОбъектXDTO(ФайлТитулаОтправителя);
	
	ИдентификаторОтправителя	= ТитулОтправителя.СвУчДокОбор.ИдОтпр;
	ИдентификаторПолучателя		= ТитулОтправителя.СвУчДокОбор.ИдПол;
	ИмяФайлаОтправителя			= ТитулОтправителя.ИдФайл;
	ДатаФайлаОтправителя		= ТитулОтправителя.Документ.ДатаИнфПок;
	ВремяФайлаОтправителя		= ТитулОтправителя.Документ.ВремИнфПок;
	
	ДопПрефикс = "";
	
	Если Найти(ИмяФайлаОтправителя, "DP_PRIRASXPRINMARK") = 1 Тогда
		ДопПрефикс = "MARK";
	КонецЕсли;
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл, ВерсияФормата, ИдентификаторПолучателя,
		ИдентификаторОтправителя, ПараметрыФормированияФайла, ДопПрефикс);
	
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияПокупателя(Документ, ПротоКонтент, НалоговыйКод, ПараметрыФормированияФайла);
	
	ИдИнфПок = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ИдИнфПок");
	XML_ОтветныйТитул_ЗаполнитьСведенияПродавца(ИмяПакетаXDTO, ИдИнфПок, ИмяФайлаОтправителя, ДатаФайлаОтправителя,
		ВремяФайлаОтправителя, ФайлыПодписейОтправителя);
	
	СвИтПрием = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СвИтПрием");
	XML_ОтветныйТитул_ЗаполнитьСведенияПоИтогамПриемки(ИмяПакетаXDTO, СвИтПрием, ПротоКонтент);
	
	XML_Торг2_ЗаполнитьПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент.Signers);
	
	Возврат Файл;
	
КонецФункции

// Возвращает ответный титул акта сверки в формате 405@
//
// Параметры:
//  ПротоКонтент - ОбъектXDTO - титул получателя в терминах прото (ReconciliationActRecipientTitle)
//  ФайлОтправителя - ДвоичныеДанные - файл акта в формате 405@
//  ПараметрыФормированияФайла - Структура - см. функцию XML_ПараметрыФормированияФайла()
// 
// Возвращаемое значение:
//  ОбъектXDTO - титул получателя акта сверки по схеме ФНС (см. ON_AKTSVERPOL_xsd)
//
Функция XML_ОтветныйТитул_АктСверки_405(ПротоКонтент, ФайлОтправителя, ФайлыПодписейОтправителя, ПараметрыФормированияФайла) Экспорт 
	
	НалоговыйКод = "1110333";
	ВерсияФормата = "5.01";
	ИмяПакетаXDTO = "ON_AKTSVERPOL";
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ТитулОтправителя = XML_В_ОбъектXDTO(ФайлОтправителя);
	
	ИмяФайлаОтправителя			= ТитулОтправителя.ИдФайл;
	ДатаФайлаОтправителя		= ТитулОтправителя.Документ.ДатаИнфОтпр;
	ВремяФайлаОтправителя		= ТитулОтправителя.Документ.ВрИнфотпр;
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	
	XML_АктСверки_ОтветныйТитул_ЗаполнитьДанныеФайлаОтправителя(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_АктСверки_ОтветныйТитул_ЗаполнитьТаблицуВзаиморасчетов(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_АктСверки_ЗаполнитьПодписанта(ИмяПакетаXDTO, Документ, ПротоКонтент);
	
	XML_АктСверки_ОтветныйТитул_ЗаполнитьОсновныеСведенияФайла(
		ИмяПакетаXDTO,
		Файл,
		Документ,
		ПротоКонтент,
		НалоговыйКод,
		ВерсияФормата,
		ПараметрыФормированияФайла
	);
	
	Возврат Файл;
	
КонецФункции

// Возвращает универсального подписанта в формате Диадока для подписания документов в 970 формате
//
// Параметры:
//  Сертификат - Структура - см. функцию Контракт_ОписаниеСертификата() модуля объекта ВнешниеСервисы.epf
//  ДанныеДоверенности - Структура - см. функцию Контракт_МЧД() модуля объекта Ядро.epf
//	ПараметрыФормированияФайла - Структура - см. Новый_ПараметрыФормированияSignerContent 
// 
// Возвращаемое значение:
//  ОбъектXDTO - универсальный подписант по схеме Диадока (см. Signer_xsd)
//
Функция XML_УПД_970_УниверсальныйПодписант(Сертификат, ДанныеДоверенности, ПараметрыФормированияФайла) Экспорт
	
	Фабрика = XML_ФабрикаXDTO("Signer");
	
	Результат = XML_ПолучитьПустойОбъектXDTO("Signers", "Signer");
	
	Signer = XML_ПолучитьXML_УниверсальныйПодписант(
		Сертификат,
		Неопределено,
		ПараметрыФормированияФайла);
	
	УстановитьЗначениеXDTO(Signer.Position, "PositionSource", "StorageByTitleTypeId");	
		
	СпосПодтПолном = XML_СпособПодтвержденияПолномочийПодписанта(Сертификат, ДанныеДоверенности, ПараметрыФормированияФайла);
	УстановитьЗначениеXDTO(Signer, "SignerPowersConfirmationMethod", СпосПодтПолном);
	
	УстановитьЗначениеXDTO(Результат, "BoxId", ПараметрыФормированияФайла.АдресЯщикаОрганизации);
	УстановитьЗначениеXDTO(Результат, "Signer", Signer);
	
	Возврат Результат;
	
КонецФункции

// Возвращает универсального подписанта в формате Диадока для подписания актов сверки в 405 формате
//
// Параметры:
//  Сертификат - Структура - см. функцию Контракт_ОписаниеСертификата() модуля объекта ВнешниеСервисы.epf
//  ДанныеДоверенности - Структура - см. функцию Контракт_МЧД() модуля объекта Ядро.epf
//  ПараметрыФормированияФайла - Структура - см. Новый_ПараметрыФормированияSignerContent 
// 
// Возвращаемое значение:
//  ОбъектXDTO - универсальный подписант по схеме Диадока (см. Signer_xsd)
//
Функция XML_АктСверки_УниверсальныйПодписант(Сертификат, ДанныеДоверенности, ПараметрыФормированияФайла) Экспорт
	
	Фабрика = XML_ФабрикаXDTO("Signer");
	
	Результат = XML_ПолучитьПустойОбъектXDTO("Signers", "Signer");
	
	Signer = XML_ПолучитьXML_УниверсальныйПодписант(
		Сертификат,
		ДанныеДоверенности,
		ПараметрыФормированияФайла);
	
	ТребуетсяМЧД = (Сертификат.ЭтоКЭП
		И Сертификат.Тип = "ФЛ"
		И ЗначениеЗаполнено(ДанныеДоверенности));
	
	Если ТребуетсяМЧД Тогда
		
		PowerOfAttorney = НовыйДочернийОбъектXDTO(Фабрика, Signer, "PowerOfAttorney");
		Electronic = НовыйДочернийОбъектXDTO(Фабрика, PowerOfAttorney, "Electronic");
		Storage = НовыйДочернийОбъектXDTO(Фабрика, Electronic, "Storage");
		FullId = НовыйДочернийОбъектXDTO(Фабрика, Storage, "FullId");
		
		ИспользоватьДоверенностьПоУмолчанию = Ложь;
		УстановитьЗначениеXDTO(Storage, "UseDefault", ИспользоватьДоверенностьПоУмолчанию);
		УстановитьЗначениеXDTO(FullId, "RegistrationNumber", ДанныеДоверенности.Идентификатор);
		УстановитьЗначениеXDTO(FullId, "IssuerInn", ДанныеДоверенности.Доверитель.ИНН);
		УстановитьЗначениеXDTO(FullId, "RepresentativeInn", ДанныеДоверенности.Представитель.ИНН);
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Результат, "BoxId", ПараметрыФормированияФайла.АдресЯщикаОрганизации);
	УстановитьЗначениеXDTO(Результат, "Signer", Signer);
	
	Возврат Результат;
	
КонецФункции

// Возвращает универсального подписанта в формате Диадока
//
// Параметры:
//  Сертификат - Структура - см. функцию Контракт_ОписаниеСертификата() модуля объекта ВнешниеСервисы.epf
//  ДанныеДоверенности - Структура - см. функцию Контракт_МЧД() модуля объекта Ядро.epf
//  ПараметрыФормированияФайла - Структура - см. Новый_ПараметрыФормированияSignerContent
// 
// Возвращаемое значение:
//  ОбъектXDTO - универсальный подписант по схеме Диадока (см. Signer_xsd)
//
Функция XML_ПолучитьXML_УниверсальныйПодписант(Сертификат, ДанныеДоверенности, ПараметрыФормированияФайла) Экспорт
	
	Фабрика = XML_ФабрикаXDTO("Signer");
	
	Результат = XML_ПолучитьПустойОбъектXDTO("Signer", "Signer");
		
	Certificate = НовыйДочернийОбъектXDTO(Фабрика, Результат, "Certificate");
	Position = НовыйДочернийОбъектXDTO(Фабрика, Результат, "Position");
	
	УстановитьЗначениеXDTO(Certificate, "CertificateThumbprint", Сертификат.ОтпечатокСертификата);
	УстановитьЗначениеXDTO(Position, "PositionSource", "Employee");
	
	Если Сертификат.ЭтоКЭП Тогда
		УстановитьЗначениеXDTO(Результат, "SignatureType", "1");
	КонецЕсли;
	
	ТипСертификатаФЛ = "ФЛ";
	
	Если Сертификат.ЭтоКЭП
		И Сертификат.Тип = ТипСертификатаФЛ
		И ЗначениеЗаполнено(ДанныеДоверенности) Тогда
		
		ИспользоватьДоверенностьПоУмолчанию = Ложь;
		
		ИННДоверителя = ДанныеДоверенности.Доверитель.ИНН;
		РегНомерМЧД = ДанныеДоверенности.Идентификатор;
		ИННПредставителя = ДанныеДоверенности.Представитель.ИНН;
		
		PowerOfAttorney = НовыйДочернийОбъектXDTO(Фабрика, Результат, "PowerOfAttorney");
		Electronic = НовыйДочернийОбъектXDTO(Фабрика, PowerOfAttorney, "Electronic");
		Storage = НовыйДочернийОбъектXDTO(Фабрика, Electronic, "Storage");
		FullId = НовыйДочернийОбъектXDTO(Фабрика, Storage, "FullId");
		
		Если ПараметрыФормированияФайла.ПередаватьМЧДФайлом Тогда
			СпособПередачиМЧД = "1";
		Иначе
			СпособПередачиМЧД = "2";
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Electronic, "MethodOfProviding", СпособПередачиМЧД);
		УстановитьЗначениеXDTO(Storage, "UseDefault", ИспользоватьДоверенностьПоУмолчанию);
		УстановитьЗначениеXDTO(FullId, "RegistrationNumber", РегНомерМЧД);
		УстановитьЗначениеXDTO(FullId, "IssuerInn", ИННДоверителя);
		УстановитьЗначениеXDTO(FullId, "RepresentativeInn", ИННПредставителя);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Параметры для заполнения XML файла универсального подписанта
//
// Параметры:
//	АдресЯщикаОрганизации 	- Строка - адрес ящика организации
//	ПередаватьМЧДФайлом 	- Булево - признак передачи МЧД файлом (по умолчанию - Ложь)
//	ИННОрганизации 			- Строка - ИНН организации (владельца ящика)
// 
// Возвращаемое значение:
//  Структура - заполненныйы контракт (см. Контракт_ПараметрыФормированияУниверсальногоПодписанта)
//
Функция Новый_ПараметрыФормированияУниверсальногоПодписанта(АдресЯщика, ИННОрганизации, ПередаватьМЧДФайлом) Экспорт

	Результат = Контракт_ПараметрыФормированияУниверсальногоПодписанта();
	
	Результат.АдресЯщикаОрганизации = АдресЯщика;
	Результат.ИННОрганизации = ИННОрганизации;
	Результат.ПередаватьМЧДФайлом = ПередаватьМЧДФайлом;

	Возврат Результат;
	
КонецФункции

// Контракт параметров для заполнения XML файла универсального подписанта
// 
// Возвращаемое значение:
//  Структура - параметры
//		АдресЯщикаОрганизации 	- Строка - адрес ящика организации
//		ПередаватьМЧДФайлом 	- Булево - признак передачи МЧД файлом (по умолчанию - Ложь)
//		ИННОрганизации 			- Строка - ИНН организации (владельца ящика)
//
Функция Контракт_ПараметрыФормированияУниверсальногоПодписанта()

	Результат = Новый Структура;
	
	Результат.Вставить("АдресЯщикаОрганизации", "");
	Результат.Вставить("ИННОрганизации", "");
	Результат.Вставить("ПередаватьМЧДФайлом", Ложь);
	
	Возврат Результат;
	
КонецФункции


// XML: функции заполнения УПД

Функция XML_УПД_ПолучитьФункцию(ContentFunction)
	
	ФункцииУПД = Перечисление_ФункцииУПД();
	
	СоответствиеФункций = Новый Соответствие;
	
	СоответствиеФункций.Вставить("InvoiceAndBasic", ФункцииУПД.СЧФДОП);
	СоответствиеФункций.Вставить("Invoice", 		ФункцииУПД.СЧФ);
	СоответствиеФункций.Вставить("Basic", 			ФункцииУПД.ДОП);
	
	ФункцияДокумента = СоответствиеФункций.Получить(ContentFunction);
	
	Если ФункцияДокумента = Неопределено Тогда
		ФункцияДокумента = ContentFunction;
	КонецЕсли;
	
	Возврат ФункцияДокумента;

КонецФункции

Функция Перечисление_ФункцииУПД() Экспорт
	
	Результат = Новый Структура;
	
	Результат.Вставить("СЧФДОП", 	"СЧФДОП");
	Результат.Вставить("СЧФ", 		"СЧФ");
	Результат.Вставить("ДОП", 		"ДОП");
	
	Возврат Результат;

КонецФункции

Процедура XML_УПД_155_ЗаполнитьСведенияСФ(ИмяПакетаXDTO, Документ, Content)
	
	ФорматДаты	 = "ДФ=dd.MM.yyyy";
	Фабрика		 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Seller		= Content.Seller;
	Buyer		= Content.Buyer;
	Consignee	= Content.Consignee;
	Shipper		= Content.Shipper;
		
	// Сведения о счете-фактуре
	СвСчФакт = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СвСчФакт");
	УстановитьЗначениеXDTO(СвСчФакт, "НомерСчФ"	, Content.Number);
	УстановитьЗначениеXDTO(СвСчФакт, "ДатаСчФ"	, Дата_В_Формат(Content.Date, ФорматДаты));
	УстановитьЗначениеXDTO(СвСчФакт, "КодОКВ"	, Content.Currency);
	
	// Исправление
	ИспрСчФ = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвСчФакт.ИспрСчФ", ИмяПакетаXDTO);
	
	Если ЗначениеЗаполнено(Content.RevisionNumber) И ЗначениеЗаполнено(Content.RevisionDate) Тогда
		
		ИспрСчФ = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ИспрСчФ");
		УстановитьЗначениеXDTO(ИспрСчФ	, "НомИспрСчФ"	, Content.RevisionNumber);
		УстановитьЗначениеXDTO(ИспрСчФ	, "ДатаИспрСчФ"	, Дата_В_Формат(Content.RevisionDate, ФорматДаты));
		УстановитьЗначениеXDTO(СвСчФакт	, "ИспрСчФ"		, ИспрСчФ);
		
	КонецЕсли;
	
	// Продавец
	СвПрод = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "СвПрод");
	XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, СвПрод, Seller);
		
	// Покупатель
	СвПокуп = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "СвПокуп");
	XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, СвПокуп, Buyer);
		
	// Грузополучатель
	Если ЗначениеЗаполнено(Consignee.Name) Тогда
		ГрузПолуч = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ГрузПолуч");
		XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, ГрузПолуч, Consignee);
	КонецЕсли;
	
	// Грузоотправитель
	Если Shipper.SameAsSeller = Истина ИЛИ ЗначениеЗаполнено(Shipper.OrganizationInfo.Name) Тогда
		
		ГрузОт = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ГрузОт");
		
		Если Shipper.SameAsSeller = Истина Тогда
			УстановитьЗначениеXDTO(ГрузОт, "ОнЖе", "он же");
		Иначе
			ГрузОтпр = НовыйДочернийОбъектXDTO(Фабрика, ГрузОт, "ГрузОтпр");
			XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, ГрузОтпр, Shipper.OrganizationInfo);
		КонецЕсли;
		
	КонецЕсли;
	
	// Платежно-расчетные документы
	Для Каждого PaymentDocument Из Content.PaymentDocuments Цикл
		СвПРД = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "СвПРД");
		УстановитьЗначениеXDTO(СвПРД, "НомерПРД", PaymentDocument.Number);
		УстановитьЗначениеXDTO(СвПРД, "ДатаПРД"	, Дата_В_Формат(PaymentDocument.Date, ФорматДаты));
	КонецЦикла;
			
	// Дополнительные сведения об участниках факта хозяйственной жизни, основаниях и обстоятельствах его проведения
	ДопСвФХЖ1 = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ДопСвФХЖ1");
	УстановитьЗначениеXDTO(ДопСвФХЖ1, "ИдГосКон", Content.GovernmentContractInfo);
	УстановитьЗначениеXDTO(ДопСвФХЖ1, "НаимОКВ"	, НаименованиеВалюты(Content.Currency, Истина));
	УстановитьЗначениеXDTO(ДопСвФХЖ1, "КурсВал"	, Content.CurrencyRate);
	
	// Информационное поле - 1
	Если ЗначениеЗаполнено(Content.AdditionalInfoId.InfoFileId)
		ИЛИ ЗначениеЗаполнено(Content.AdditionalInfoId.StructedAdditionalInfos) Тогда
		
		ИнфПолФХЖ1 = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ИнфПолФХЖ1");
		УстановитьЗначениеXDTO(ИнфПолФХЖ1, "ИдФайлИнфПол", Content.AdditionalInfoId.InfoFileId);
		
		XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, ИнфПолФХЖ1.ТекстИнф, Content.AdditionalInfoId.StructedAdditionalInfos);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УПД_155_ЗаполнитьСведенияТаблицыСФ(ИмяПакетаXDTO, Документ, Content, ПараметрыФормированияФайла)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Table = Content.InvoiceTable;
	
	Если ЗначениеЗаполнено(Table.Items) Тогда
		
		ТаблСчФакт = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ТаблСчФакт");
		
		ВсеСтрокиБезНДС = Истина;
		НеОблагаетсяНДС = Истина;
		
		Для НомерСтроки = 1 По Table.Items.Количество() Цикл
		
			СведТов = НовыйДочернийОбъектXDTO(Фабрика, ТаблСчФакт, "СведТов");
			
			УстановитьЗначениеXDTO(СведТов, "НомСтр", НомерСтроки);
			
			Item = Table.Items[НомерСтроки - 1];
			
			XML_УПД_155_ЗаполнитьСтрокуТабЧасти(ИмяПакетаXDTO, СведТов, Item, ПараметрыФормированияФайла);
			
			СтавкаБезНДС = TaxRate_ЭтоЗначениеБезНДС(Item.TaxRate);
			
			Если СтавкаБезНДС Или Item.Vat <> Неопределено Тогда
				НеОблагаетсяНДС = Ложь;
			КонецЕсли;
			
			Если Не СтавкаБезНДС Тогда
				ВсеСтрокиБезНДС = Ложь;
			КонецЕсли;
			
		КонецЦикла;
		
		// Итоги
		ВсегоОпл	 = НовыйДочернийОбъектXDTO(Фабрика, ТаблСчФакт	, "ВсегоОпл");
		СумНалВсего	 = НовыйДочернийОбъектXDTO(Фабрика, ВсегоОпл	, "СумНалВсего");
		
		УстановитьЗначениеXDTO(ВсегоОпл, "СтТовБезНДСВсего"	, XML_ФорматЧисла(Table.TotalWithVatExcluded, 2));
		УстановитьЗначениеXDTO(ВсегоОпл, "СтТовУчНалВсего"	, XML_ФорматЧисла(Table.Total, 2));
		
		УстановитьЗначениеXDTO(ВсегоОпл, "НеттоВс", Table.TotalNet);
		
		Если ЗначениеЗаполнено(Table.Total) Тогда
			УстановитьЗначениеXDTO(ВсегоОпл, "СтТовУчНалВсего", XML_ФорматЧисла(Table.Total, 2));
		КонецЕсли;
		
		Если ВсеСтрокиБезНДС Тогда
			УстановитьЗначениеXDTO(СумНалВсего, "БезНДС", СтрокаБезНДС());
		Иначе
			УстановитьЗначениеXDTO(СумНалВсего, "СумНал", XML_ФорматЧисла(Table.Vat, 2));
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УПД_155_ЗаполнитьСтрокуТабЧасти(ИмяПакетаXDTO, СведТов, Item, ПараметрыФормированияФайла)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(СведТов, "НаимТов"	, Item.Product);
	УстановитьЗначениеXDTO(СведТов, "ОКЕИ_Тов"	, Item.UnitCode);
	
	УстановитьЗначениеXDTO(СведТов, "КолТов"	, XML_ФорматЧисла(Item.Quantity	, ПараметрыФормированияФайла.ТочностьКоличества));
	УстановитьЗначениеXDTO(СведТов, "ЦенаТов"	, XML_ФорматЧисла(Item.Price	, ПараметрыФормированияФайла.ТочностьЦены));
	
	УстановитьЗначениеXDTO(СведТов, "СтТовБезНДС"	, XML_ФорматЧисла(Item.SubtotalWithVatExcluded, 2));
	УстановитьЗначениеXDTO(СведТов, "НалСт"			, НалСт_Из_TaxRate(Item.TaxRate));
	УстановитьЗначениеXDTO(СведТов, "СтТовУчНал"	, XML_ФорматЧисла(Item.Subtotal, 2));
	
	// Акциз
	Акциз = НовыйДочернийОбъектXDTO(Фабрика, СведТов, "Акциз");
	Если НЕ ЗначениеЗаполнено(Item.Excise) ИЛИ Число(Item.Excise) = 0 Тогда
		УстановитьЗначениеXDTO(Акциз, "БезАкциз", "без акциза");
	Иначе
		УстановитьЗначениеXDTO(Акциз, "СумАкциз", XML_ФорматЧисла(Число(Item.Excise), 2));
	КонецЕсли;
	
	// Сумма НДС
	СумНал = НовыйДочернийОбъектXDTO(Фабрика, СведТов, "СумНал");
	Если TaxRate_ЭтоЗначениеБезНДС(Item.TaxRate) Тогда
		УстановитьЗначениеXDTO(СумНал, "БезНДС", СтрокаБезНДС());
	ИначеЕсли Item.Vat <> Неопределено Тогда
		УстановитьЗначениеXDTO(СумНал, "СумНал", XML_ФорматЧисла(Item.Vat, 2));
	КонецЕсли;
	
	СтранаПроисхождения = Неопределено;
	
	// ГТД
	Для Каждого CustomDeclaration Из Item.CustomDeclarations Цикл
		
		СвТД = НовыйДочернийОбъектXDTO(Фабрика, СведТов, "СвТД");
		
		УстановитьЗначениеXDTO(СвТД, "НомерТД", CustomDeclaration.DeclarationNumber);
		
		Если ЗначениеЗаполнено(CustomDeclaration.CountryCode) Тогда
			УстановитьЗначениеXDTO(СвТД, "КодПроисх", CustomDeclaration.CountryCode);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(CustomDeclaration.CountryCode)
			И Не ЗначениеЗаполнено(СтранаПроисхождения) Тогда
			СтранаПроисхождения = НаименованиеСтраны(CustomDeclaration.CountryCode);
		КонецЕсли;
		
	КонецЦикла;
	
	// Доп. сведения
	ДопСведТов = НовыйДочернийОбъектXDTO(Фабрика, СведТов, "ДопСведТов");
	УстановитьЗначениеXDTO(ДопСведТов, "КрНаимСтрПр", СтранаПроисхождения);
	УстановитьЗначениеXDTO(ДопСведТов, "ПрТовРаб"	, XML_ПолучитьПризнакНоменклатуры(Item.ItemMark));
	УстановитьЗначениеXDTO(ДопСведТов, "КодТов"		, Item.VendorCode);
	УстановитьЗначениеXDTO(ДопСведТов, "НадлОтп"	, Item.ToRelease);
	УстановитьЗначениеXDTO(ДопСведТов, "ДопПризн"	, Item.AdditionalProperty);
	УстановитьЗначениеXDTO(ДопСведТов, "НаимЕдИзм"	, Item.UnitName);
	
	УстановитьЗначениеXDTO(ДопСведТов, "КорСчДебет"	, Item.AccountDebit);
	УстановитьЗначениеXDTO(ДопСведТов, "КорСчКредит", Item.AccountCredit);
	
	// Информационное поле события (факта хозяйственной жизни) 2
	XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, СведТов.ИнфПолФХЖ2, Item.StructedAdditionalInfos);
	
КонецПроцедуры

Процедура XML_УПД_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, Content)
	
	ФорматДаты	 = "ДФ=dd.MM.yyyy";
	TransferInfo = Content.TransferInfo;
	Фабрика		 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	// Содержание факта хозяйственной жизни 3 (Обязателен при Функция=СЧФДОП или Функция=ДОП)
	Если ЗначениеЗаполнено(TransferInfo.OperationInfo) Тогда
		
		// Сведения о факте отгрузки товаров
		СвПродПер	 = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СвПродПер");
		СвПер		 = НовыйДочернийОбъектXDTO(Фабрика, СвПродПер, "СвПер");
		
		УстановитьЗначениеXDTO(СвПер, "СодОпер", TransferInfo.OperationInfo);
		УстановитьЗначениеXDTO(СвПер, "ВидОпер", TransferInfo.OperationType);
		
		Если ЗначениеЗаполнено(TransferInfo.TransferDate) Тогда
			УстановитьЗначениеXDTO(СвПер, "ДатаПер", Дата_В_Формат(TransferInfo.TransferDate, ФорматДаты));
		КонецЕсли;
		
		// Основание отгрузки товаров
		Если ЗначениеЗаполнено(TransferInfo.TransferBases) Тогда
			
			Для Каждого TransferBase Из TransferInfo.TransferBases Цикл
				
				ОснПер = НовыйДочернийОбъектXDTO(Фабрика, СвПер, "ОснПер");
				УстановитьЗначениеXDTO(ОснПер, "НаимОсн"	, TransferBase.DocumentName);
				УстановитьЗначениеXDTO(ОснПер, "НомОсн"		, TransferBase.DocumentNumber);
				УстановитьЗначениеXDTO(ОснПер, "ДопСвОсн"	, TransferBase.DocumentInfo);
				
				Если ЗначениеЗаполнено(TransferBase.DocumentDate) Тогда
					УстановитьЗначениеXDTO(ОснПер, "ДатаОсн", Дата_В_Формат(TransferBase.DocumentDate, ФорматДаты));
				КонецЕсли;
				
			КонецЦикла;
			
		Иначе
			ОснПер = НовыйДочернийОбъектXDTO(Фабрика, СвПер, "ОснПер");
			УстановитьЗначениеXDTO(ОснПер, "НаимОсн", "Отсутствует");
		КонецЕсли;
		
		// Сведения о лице, передавшем товар
		Если ЗначениеЗаполнено(TransferInfo.Employee.Surname) ИЛИ ЗначениеЗаполнено(TransferInfo.OtherIssuer.Surname) Тогда
			
			СвЛицПер = НовыйДочернийОбъектXDTO(Фабрика, СвПер, "СвЛицПер");
			
			// Работник организации продавца
			Если ЗначениеЗаполнено(TransferInfo.Employee.Surname) Тогда
				
				РабОргПрод = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПер, "РабОргПрод");
				XML_ЗаполнитьРаботникаОрганизации(ИмяПакетаXDTO, РабОргПрод, TransferInfo.Employee);
				
			// Иное лицо
			ИначеЕсли ЗначениеЗаполнено(TransferInfo.OtherIssuer.Surname) Тогда
				
				ИнЛицо = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПер, "ИнЛицо");
				
				Если ЗначениеЗаполнено(TransferInfo.OtherIssuer.EmployeePosition) Тогда
					
					ПредОргПер = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ПредОргПер");
					XML_ЗаполнитьПредставителяОрганизации(ИмяПакетаXDTO, ПредОргПер, TransferInfo.OtherIssuer);
					
				Иначе
					
					ФЛПер = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ФЛПер");
					XML_ЗаполнитьФизическоеЛицо(ИмяПакетаXDTO, ФЛПер, TransferInfo.OtherIssuer);
					
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЕсли;
		
		// Транспортировка и груз
		Если ЗначениеЗаполнено(TransferInfo.TransferTextInfo)
			ИЛИ ЗначениеЗаполнено(TransferInfo.Carrier.Name)
			ИЛИ ЗначениеЗаполнено(TransferInfo.Waybills) Тогда
			
			ТранГруз = НовыйДочернийОбъектXDTO(Фабрика, СвПер, "ТранГруз");
			
			УстановитьЗначениеXDTO(ТранГруз, "СвТранГруз", TransferInfo.TransferTextInfo);
			
			Для Каждого Waybill Из TransferInfo.Waybills Цикл
				ТранНакл = НовыйДочернийОбъектXDTO(Фабрика, ТранГруз, "ТранНакл");
				УстановитьЗначениеXDTO(ТранНакл, "НомТранНакл"	, Waybill.DocumentNumber);
				УстановитьЗначениеXDTO(ТранНакл, "ДатаТранНакл"	, Дата_В_Формат(Waybill.DocumentDate, ФорматДаты));
			КонецЦикла;
			
			// Перевозчик
			Если ЗначениеЗаполнено(TransferInfo.Carrier.Name) Тогда
				Перевозчик = НовыйДочернийОбъектXDTO(Фабрика, ТранГруз, "Перевозчик");
				XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, Перевозчик, TransferInfo.Carrier);
			КонецЕсли;
			
		КонецЕсли;
		
		// Сведения о передаче вещи, изготовленной по договору подряда
		Если ЗначениеЗаполнено(TransferInfo.CreatedThingTransferDate) Тогда
			СвПерВещи = НовыйДочернийОбъектXDTO(Фабрика, СвПер, "СвПерВещи");
			УстановитьЗначениеXDTO(СвПерВещи, "СвПерВещ"	, TransferInfo.CreatedThingInfo);
			УстановитьЗначениеXDTO(СвПерВещи, "ДатаПерВещ"	, Дата_В_Формат(TransferInfo.CreatedThingTransferDate, ФорматДаты));
		КонецЕсли;
		
		// Информационное поле - 3
		Если ЗначениеЗаполнено(TransferInfo.AdditionalInfoId.InfoFileId)
			ИЛИ ЗначениеЗаполнено(TransferInfo.AdditionalInfoId.StructedAdditionalInfos) Тогда
			
			ИнфПолФХЖ3 = НовыйДочернийОбъектXDTO(Фабрика, СвПродПер, "ИнфПолФХЖ3");
			
			УстановитьЗначениеXDTO(ИнфПолФХЖ3, "ИдФайлИнфПол", TransferInfo.AdditionalInfoId.InfoFileId);
			
			XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, ИнфПолФХЖ3.ТекстИнф, TransferInfo.AdditionalInfoId.StructedAdditionalInfos);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ОтветныйТитул_УПД_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, СодФХЖ4
	, ПротоКонтент
	, НаименованиеДокумента
	, ФункцияДокумента
	, НомерДокумента
	, ДатаДокумента)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	УстановитьЗначениеXDTO(СодФХЖ4, "Функция"		, ФункцияДокумента);
	УстановитьЗначениеXDTO(СодФХЖ4, "ВидОперации"	, ПротоКонтент.OperationCode);
	УстановитьЗначениеXDTO(СодФХЖ4, "НаимДокОпрПр"	, НаименованиеДокумента);
	УстановитьЗначениеXDTO(СодФХЖ4, "ДатаСчФИнфПр"	, Дата_В_Формат(ДатаДокумента, ФорматДаты));
	СодФХЖ4.НомСчФИнфПр = НомерДокумента;
	
	// Сведения о принятии товаров, работ, услуг
	СвПрин = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ4, "СвПрин");
	УстановитьЗначениеXDTO(СвПрин, "СодОпер"	, ПротоКонтент.OperationContent);
	УстановитьЗначениеXDTO(СвПрин, "ДатаПрин"	, Дата_В_Формат(ПротоКонтент.AcceptanceDate, ФорматДаты));
	
	ЕстьСведенияОРаботнике = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "Employee") И ЗначениеЗаполнено(ПротоКонтент.Employee.Surname);
	ЕстьСведенияОбИномЛице = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "OtherIssuer") И ЗначениеЗаполнено(ПротоКонтент.OtherIssuer.Surname);
	
	Если ЕстьСведенияОРаботнике Или ЕстьСведенияОбИномЛице Тогда
		
		// Сведения о лице, принявшем товары (груз)
		СвЛицПрин = НовыйДочернийОбъектXDTO(Фабрика, СвПрин, "СвЛицПрин");
		
		Если ЕстьСведенияОРаботнике Тогда
			
			// Работник организации покупателя
			РабОргПок = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПрин, "РабОргПок");
			XML_ЗаполнитьРаботникаОрганизации(ИмяПакетаXDTO, РабОргПок, ПротоКонтент.Employee);
			
		ИначеЕсли ЕстьСведенияОбИномЛице Тогда
			
			// Иное лицо
			ИнЛицо = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПрин, "ИнЛицо");
			
			Если ЗначениеЗаполнено(ПротоКонтент.OtherIssuer.EmployeePosition) Тогда
				
				// Представитель организации, которой доверено принятие товаров (груза)
				ПредОргПрин = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ПредОргПрин");
				XML_ЗаполнитьПредставителяОрганизации(ИмяПакетаXDTO, ПредОргПрин, ПротоКонтент.OtherIssuer);
				
			Иначе
				
				// Физическое лицо, которому доверено принятие товаров (груза)
				ФЛПрин = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ФЛПрин");
				XML_ЗаполнитьФизическоеЛицо(ИмяПакетаXDTO, ФЛПрин, ПротоКонтент.OtherIssuer);
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	XML_ЗаполнитьИнфоПоляФХЖ(ИмяПакетаXDTO, СодФХЖ4, ПротоКонтент.AdditionalInfoId, "ИнфПолФХЖ4");
	
КонецПроцедуры

// XML: функции заполнения УПД 820

Функция XML_УПД_820_ДопПрефиксИмениФайла(ПротоКонтент)
	
	Результат = "";
	
	Для Каждого Item Из ПротоКонтент.Table.Items Цикл
		Если Item.ItemIdentificationNumbers.Количество() > 0 Тогда
			Результат = "MARK";
			Прервать;
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Item Из ПротоКонтент.Table.Items Цикл
		Если Item.ItemTracingInfos.Количество() > 0 Тогда
			Результат = "PROS";
			Прервать;
		КонецЕсли;
	КонецЦикла;	
	
	Возврат Результат;
	
КонецФункции

Процедура XML_УПД_820_ЗаполнитьСведенияСФ(ИмяПакетаXDTO, Документ, Content)
	
	ФорматДаты	 = "ДФ=dd.MM.yyyy";
	Фабрика		 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
		
	// Сведения о счете-фактуре
	СвСчФакт = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СвСчФакт");
	УстановитьЗначениеXDTO(СвСчФакт, "НомерСчФ",	Content.DocumentNumber);
	УстановитьЗначениеXDTO(СвСчФакт, "ДатаСчФ",		Дата_В_Формат(Content.DocumentDate, ФорматДаты));
	УстановитьЗначениеXDTO(СвСчФакт, "КодОКВ",		Content.Currency);
	
	Если ЗначениеЗаполнено(Content.RevisionNumber)
		Или ЗначениеЗаполнено(Content.RevisionDate)
		Или ЭтоИстина(Content.HyphenRevisionNumber)
		Или ЭтоИстина(Content.HyphenRevisionDate) Тогда
		
		ИспрСчФ = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ИспрСчФ");
		
		// Номер исправления
		Если ЗначениеЗаполнено(Content.RevisionNumber) И ЗначениеЗаполнено(Content.RevisionDate) Тогда
			УстановитьЗначениеXDTO(ИспрСчФ, "НомИспрСчФ", Content.RevisionNumber);
		КонецЕсли;
		
		// Дата исправления
		Если ЗначениеЗаполнено(Content.RevisionDate) Тогда
			УстановитьЗначениеXDTO(ИспрСчФ, "ДатаИспрСчФ", Дата_В_Формат(Content.RevisionDate, ФорматДаты));
		КонецЕсли;
		
		// Дефис вместо номера исправления
		Если ЭтоИстина(Content.HyphenRevisionNumber) Тогда
			УстановитьЗначениеXDTO(ИспрСчФ, "ДефНомИспрСчФ", СтрокаДефис());
		КонецЕсли;
		
		// Дефис вместо даты исправления
		Если ЭтоИстина(Content.HyphenRevisionDate) Тогда
			УстановитьЗначениеXDTO(ИспрСчФ, "ДефДатаИспрСчФ", СтрокаДефис());
		КонецЕсли;
		
	КонецЕсли;
	
	// Продавец
	Для Каждого Seller Из Content.Sellers Цикл
		СвПрод = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "СвПрод");
		XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, СвПрод, Seller);
	КонецЦикла;
	
	// Покупатель
	Для Каждого Buyer Из Content.Buyers Цикл
		СвПокуп = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "СвПокуп");
		XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, СвПокуп, Buyer);
	КонецЦикла;
	
	// Грузополучатель
	Для Каждого Consignee Из Content.Consignees Цикл
		Если ЗначениеЗаполнено(Consignee.Name) Тогда
			ГрузПолуч = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ГрузПолуч");
			XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, ГрузПолуч, Consignee);
		КонецЕсли;
	КонецЦикла;
	
	// Грузоотправитель
	Для Каждого Shipper Из Content.Shippers Цикл
		Если Shipper.SameAsSeller = Истина ИЛИ ЗначениеЗаполнено(Shipper.OrganizationInfo.Name) Тогда
			
			ГрузОт = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ГрузОт");
			
			Если Shipper.SameAsSeller = Истина Тогда
				УстановитьЗначениеXDTO(ГрузОт, "ОнЖе", "он же");
			Иначе
				ГрузОтпр = НовыйДочернийОбъектXDTO(Фабрика, ГрузОт, "ГрузОтпр");
				XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, ГрузОтпр, Shipper.OrganizationInfo);
			КонецЕсли;
			
		КонецЕсли;
	КонецЦикла;
	
	// Платежно-расчетные документы
	Для Каждого PaymentDocument Из Content.PaymentDocuments Цикл
		
		СвПРД = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "СвПРД");
		УстановитьЗначениеXDTO(СвПРД, "НомерПРД", PaymentDocument.Number);
		УстановитьЗначениеXDTO(СвПРД, "ДатаПРД"	, Дата_В_Формат(PaymentDocument.Date, ФорматДаты));
		УстановитьЗначениеXDTO(СвПРД, "СуммаПРД", PaymentDocument.Total);
		
	КонецЦикла;
			
	// Дополнительные сведения об участниках факта хозяйственной жизни, основаниях и обстоятельствах его проведения
	ДопСвФХЖ1 = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ДопСвФХЖ1");
	УстановитьЗначениеXDTO(ДопСвФХЖ1, "ИдГосКон",		Content.GovernmentContractInfo);
	УстановитьЗначениеXDTO(ДопСвФХЖ1, "НаимОКВ",		НаименованиеВалюты(Content.Currency, Истина));
	УстановитьЗначениеXDTO(ДопСвФХЖ1, "КурсВал",		Content.CurrencyRate);
	УстановитьЗначениеXDTO(ДопСвФХЖ1, "ОбстФормСЧФ",	Content.CircumFormatInvoice);
	
	// Сведения о факторе
	Если ЗначениеЗаполнено(Content.FactorInfo.Type) Тогда
		СвФактор = НовыйДочернийОбъектXDTO(Фабрика, ДопСвФХЖ1, "СвФактор");
		XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, СвФактор, Content.FactorInfo);
	КонецЕсли;
	
	// Основание уступки денежного требования
	Если ЗначениеЗаполнено(Content.MainAssignMonetaryClaim.DocumentName) Тогда
		
		ОснУстДенТреб = НовыйДочернийОбъектXDTO(Фабрика, ДопСвФХЖ1, "ОснУстДенТреб");
        
		УстановитьЗначениеXDTO(ОснУстДенТреб, "НаимОсн",	Content.MainAssignMonetaryClaim.DocumentName);
		УстановитьЗначениеXDTO(ОснУстДенТреб, "НомОсн",		Content.MainAssignMonetaryClaim.DocumentNumber);
		УстановитьЗначениеXDTO(ОснУстДенТреб, "ДопСвОсн",	Content.MainAssignMonetaryClaim.DocumentInfo);
		
		Если ЗначениеЗаполнено(Content.MainAssignMonetaryClaim.DocumentDate) Тогда
			УстановитьЗначениеXDTO(ОснУстДенТреб, "ДатаОсн", Дата_В_Формат(Content.MainAssignMonetaryClaim.DocumentDate, ФорматДаты));
		КонецЕсли;
		
		УстановитьЗначениеXDTO(ОснУстДенТреб, "ИдентОсн",	Content.MainAssignMonetaryClaim.BaseDocumentId);
		
	КонецЕсли;
	
	// Информация продавца об обстоятельствах закупок для государственных и муниципальных нужд
	Если ЗначениеЗаполнено(Content.SellerInfoCircumPublicProc.DateStateContract) Тогда
		
		ИнфПродГосЗакКазн = НовыйДочернийОбъектXDTO(Фабрика, ДопСвФХЖ1, "ИнфПродГосЗакКазн");
        
		УстановитьЗначениеXDTO(ИнфПродГосЗакКазн, "ДатаГосКонт",		Дата_В_Формат(Content.SellerInfoCircumPublicProc.DateStateContract, ФорматДаты));
		УстановитьЗначениеXDTO(ИнфПродГосЗакКазн, "НомерГосКонт",		Content.SellerInfoCircumPublicProc.NumberStateContract);
		УстановитьЗначениеXDTO(ИнфПродГосЗакКазн, "ЛицСчетПрод",		Content.SellerInfoCircumPublicProc.PersonalAccountSeller);
		УстановитьЗначениеXDTO(ИнфПродГосЗакКазн, "КодПродБюджКласс",	Content.SellerInfoCircumPublicProc.SellerBudjetClassCode);
		УстановитьЗначениеXDTO(ИнфПродГосЗакКазн, "КодЦелиПрод",		Content.SellerInfoCircumPublicProc.SellerTargetCode);
		УстановитьЗначениеXDTO(ИнфПродГосЗакКазн, "КодКазначПрод",		Content.SellerInfoCircumPublicProc.SellerTreasuryCode);
		УстановитьЗначениеXDTO(ИнфПродГосЗакКазн, "НаимКазначПрод",		Content.SellerInfoCircumPublicProc.SellerTreasuryName);
		
	КонецЕсли;
	
	// Документ, подтверждающий отгрузку товаров
	Для Каждого DocumentShipment Из Content.DocumentShipments Цикл
		
		Если ЗначениеЗаполнено(DocumentShipment.Name) Тогда
			
			ДокПодтвОтгр = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ДокПодтвОтгр");
			
			УстановитьЗначениеXDTO(ДокПодтвОтгр, "НаимДокОтгр",	DocumentShipment.Name);
			УстановитьЗначениеXDTO(ДокПодтвОтгр, "НомДокОтгр",	DocumentShipment.Number);
			УстановитьЗначениеXDTO(ДокПодтвОтгр, "ДатаДокОтгр",	Дата_В_Формат(DocumentShipment.Date, ФорматДаты));
			
		КонецЕсли;
		
	КонецЦикла;
	
	// Информационное поле - 1
	Если ЗначениеЗаполнено(Content.AdditionalInfoId.InfoFileId)
		ИЛИ ЗначениеЗаполнено(Content.AdditionalInfoId.StructedAdditionalInfos) Тогда
		
		ИнфПолФХЖ1 = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ИнфПолФХЖ1");
		УстановитьЗначениеXDTO(ИнфПолФХЖ1, "ИдФайлИнфПол", Content.AdditionalInfoId.InfoFileId);
		
		XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, ИнфПолФХЖ1.ТекстИнф, Content.AdditionalInfoId.StructedAdditionalInfos);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УПД_820_ЗаполнитьСведенияТаблицыСФ(ИмяПакетаXDTO, Документ, Content, ПараметрыФормированияФайла)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Table = Content.Table;
	
	Если ЗначениеЗаполнено(Table.Items) Тогда
		
		ТаблСчФакт = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ТаблСчФакт");
		
		Для НомерСтроки = 1 По Table.Items.Количество() Цикл
		
			СведТов = НовыйДочернийОбъектXDTO(Фабрика, ТаблСчФакт, "СведТов");
			
			УстановитьЗначениеXDTO(СведТов, "НомСтр", НомерСтроки);
			
			Item = Table.Items[НомерСтроки - 1];
			
			XML_УПД_820_ЗаполнитьСтрокуТабЧасти(ИмяПакетаXDTO, СведТов, Item, ПараметрыФормированияФайла);
			
		КонецЦикла;
		
		// Итоги
		ВсегоОпл	= НовыйДочернийОбъектXDTO(Фабрика, ТаблСчФакт,	"ВсегоОпл");
		СумНалВсего	= НовыйДочернийОбъектXDTO(Фабрика, ВсегоОпл,	"СумНалВсего");
		
		УстановитьЗначениеXDTO(ВсегоОпл, "СтТовБезНДСВсего",	XML_ФорматЧисла(Table.TotalWithVatExcluded, 2));
		УстановитьЗначениеXDTO(ВсегоОпл, "КолНеттоВс",			Table.TotalNet);
				
		Если ЭтоИстина(Table.HyphenTotal) Или Не ЗначениеЗаполнено(Table.Total) Тогда
			УстановитьЗначениеXDTO(ВсегоОпл, "ДефСтТовУчНалВсего", СтрокаДефис());
		Иначе
			УстановитьЗначениеXDTO(ВсегоОпл, "СтТовУчНалВсего", XML_ФорматЧисла(Table.Total, 2));
		КонецЕсли;
		
		Если ЭтоИстина(Table.WithoutVat) Тогда
			УстановитьЗначениеXDTO(СумНалВсего, "БезНДС", СтрокаБезНДС());
		ИначеЕсли ЭтоИстина(Table.HyphenVat) Или Table.Vat = Неопределено Тогда
			УстановитьЗначениеXDTO(СумНалВсего, "ДефНДС", СтрокаДефис());
		Иначе
			УстановитьЗначениеXDTO(СумНалВсего, "СумНал", XML_ФорматЧисла(Table.Vat, 2));
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УПД_820_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, Content)
	
	ФорматДаты	 = "ДФ=dd.MM.yyyy";
	TransferInfo = Content.TransferInfo;
	Фабрика		 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	// Содержание факта хозяйственной жизни 3 (Обязателен при Функция=СЧФДОП или Функция=ДОП)
	Если ЗначениеЗаполнено(TransferInfo.OperationInfo) Тогда
		
		// Сведения о факте отгрузки товаров
		СвПродПер	 = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СвПродПер");
		СвПер		 = НовыйДочернийОбъектXDTO(Фабрика, СвПродПер, "СвПер");
		
		УстановитьЗначениеXDTO(СвПер, "СодОпер", TransferInfo.OperationInfo);
		УстановитьЗначениеXDTO(СвПер, "ВидОпер", TransferInfo.OperationType);
		
		Если ЗначениеЗаполнено(TransferInfo.TransferDate) Тогда
			УстановитьЗначениеXDTO(СвПер, "ДатаПер", Дата_В_Формат(TransferInfo.TransferDate, ФорматДаты));
		КонецЕсли;
		
		Если ЗначениеЗаполнено(TransferInfo.TransferStartDate) Тогда
			УстановитьЗначениеXDTO(СвПер, "ДатаНач", Дата_В_Формат(TransferInfo.TransferStartDate, ФорматДаты));
		КонецЕсли;
		
		Если ЗначениеЗаполнено(TransferInfo.TransferEndDate) Тогда
			УстановитьЗначениеXDTO(СвПер, "ДатаОкон", Дата_В_Формат(TransferInfo.TransferEndDate, ФорматДаты));
		КонецЕсли;
		
		// Основание отгрузки товаров
		Если ЗначениеЗаполнено(TransferInfo.TransferBases) Тогда
			
			Для Каждого TransferBase Из TransferInfo.TransferBases Цикл
				
				ОснПер = НовыйДочернийОбъектXDTO(Фабрика, СвПер, "ОснПер");
				УстановитьЗначениеXDTO(ОснПер, "НаимОсн", 	TransferBase.DocumentName);
				УстановитьЗначениеXDTO(ОснПер, "НомОсн", 	TransferBase.DocumentNumber);
				УстановитьЗначениеXDTO(ОснПер, "ДопСвОсн",	TransferBase.DocumentInfo);
				УстановитьЗначениеXDTO(ОснПер, "ИдентОсн", 	TransferBase.BaseDocumentId);
				
				Если ЗначениеЗаполнено(TransferBase.DocumentDate) Тогда
					УстановитьЗначениеXDTO(ОснПер, "ДатаОсн", Дата_В_Формат(TransferBase.DocumentDate, ФорматДаты));
				КонецЕсли;
				
			КонецЦикла;
			
		Иначе
			ОснПер = НовыйДочернийОбъектXDTO(Фабрика, СвПер, "ОснПер");
			УстановитьЗначениеXDTO(ОснПер, "НаимОсн", "Без документа-основания");
		КонецЕсли;
		
		// Сведения о лице, передавшем товар
		Если ЗначениеЗаполнено(TransferInfo.Employee.Surname) ИЛИ ЗначениеЗаполнено(TransferInfo.OtherIssuer.Surname) Тогда
			
			СвЛицПер = НовыйДочернийОбъектXDTO(Фабрика, СвПер, "СвЛицПер");
			
			// Работник организации продавца
			Если ЗначениеЗаполнено(TransferInfo.Employee.Surname) Тогда
				
				РабОргПрод = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПер, "РабОргПрод");
				XML_ЗаполнитьРаботникаОрганизации(ИмяПакетаXDTO, РабОргПрод, TransferInfo.Employee);
				
			// Иное лицо
			ИначеЕсли ЗначениеЗаполнено(TransferInfo.OtherIssuer.Surname) Тогда
				
				ИнЛицо = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПер, "ИнЛицо");
				
				Если ЗначениеЗаполнено(TransferInfo.OtherIssuer.EmployeePosition) Тогда
					
					ПредОргПер = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ПредОргПер");
					XML_ЗаполнитьПредставителяОрганизации(ИмяПакетаXDTO, ПредОргПер, TransferInfo.OtherIssuer);
					
				Иначе
					
					ФЛПер = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ФЛПер");
					XML_ЗаполнитьФизическоеЛицо(ИмяПакетаXDTO, ФЛПер, TransferInfo.OtherIssuer)
					
				КонецЕсли;
				
			КонецЕсли;
			
		КонецЕсли;
		
		// Транспортировка и груз
		Если ЗначениеЗаполнено(TransferInfo.TransferTextInfo)
			ИЛИ ЗначениеЗаполнено(TransferInfo.Carrier.Name)
			ИЛИ ЗначениеЗаполнено(TransferInfo.Waybills) Тогда
			
			ТранГруз = НовыйДочернийОбъектXDTO(Фабрика, СвПер, "ТранГруз");
			
			УстановитьЗначениеXDTO(ТранГруз, "СвТранГруз", TransferInfo.TransferTextInfo);
			
			Для Каждого Waybill Из TransferInfo.Waybills Цикл
				ТранНакл = НовыйДочернийОбъектXDTO(Фабрика, ТранГруз, "ТранНакл");
				УстановитьЗначениеXDTO(ТранНакл, "НомТранНакл"	, Waybill.DocumentNumber);
				УстановитьЗначениеXDTO(ТранНакл, "ДатаТранНакл"	, Дата_В_Формат(Waybill.DocumentDate, ФорматДаты));
			КонецЦикла;
			
			// Перевозчик
			Если ЗначениеЗаполнено(TransferInfo.Carrier.Name) Тогда
				Перевозчик = НовыйДочернийОбъектXDTO(Фабрика, ТранГруз, "Перевозчик");
				XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, Перевозчик, TransferInfo.Carrier);
			КонецЕсли;
			
		КонецЕсли;
		
		// Сведения о передаче вещи, изготовленной по договору подряда
		Если ЗначениеЗаполнено(TransferInfo.CreatedThingTransferDate) Тогда
			СвПерВещи = НовыйДочернийОбъектXDTO(Фабрика, СвПер, "СвПерВещи");
			УстановитьЗначениеXDTO(СвПерВещи, "СвПерВещ"	, TransferInfo.CreatedThingInfo);
			УстановитьЗначениеXDTO(СвПерВещи, "ДатаПерВещ"	, Дата_В_Формат(TransferInfo.CreatedThingTransferDate, ФорматДаты));
		КонецЕсли;
		
		// Информационное поле - 3
		Если ЗначениеЗаполнено(TransferInfo.AdditionalInfoId.InfoFileId)
			ИЛИ ЗначениеЗаполнено(TransferInfo.AdditionalInfoId.StructedAdditionalInfos) Тогда
			
			ИнфПолФХЖ3 = НовыйДочернийОбъектXDTO(Фабрика, СвПродПер, "ИнфПолФХЖ3");
			
			УстановитьЗначениеXDTO(ИнфПолФХЖ3, "ИдФайлИнфПол", TransferInfo.AdditionalInfoId.InfoFileId);
			
			XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, ИнфПолФХЖ3.ТекстИнф, TransferInfo.AdditionalInfoId.StructedAdditionalInfos);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УПД_820_ЗаполнитьСтрокуТабЧасти(ИмяПакетаXDTO, СведТов, Item, ПараметрыФормированияФайла)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Если ЗначениеЗаполнено(Item.Product) Тогда
		УстановитьЗначениеXDTO(СведТов, "НаимТов", Item.Product);
	КонецЕсли;
	
	Если ЭтоИстина(Item.HyphenUnit) Или Не ЗначениеЗаполнено(Item.Unit) Тогда
		УстановитьЗначениеXDTO(СведТов, "ДефОКЕИ_Тов", СтрокаДефис());
	Иначе
		УстановитьЗначениеXDTO(СведТов, "ОКЕИ_Тов", Item.Unit);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "КолТов", XML_ФорматЧисла(Item.Quantity, ПараметрыФормированияФайла.ТочностьКоличества));
	
	Если ЗначениеЗаполнено(Item.Price) Тогда
		
		ЦенаТовара		= Item.Price;
		ТочностьЦены	= ПараметрыФормированияФайла.ТочностьЦены;
		
		Если ТочностьЦены = Неопределено Тогда
			
			ТочностьЦеныПоУмолчанию = XML_ПараметрыФормированияФайла().ТочностьЦены;
			
			ЦенаПоУмолчанию = XML_ФорматЧисла(ЦенаТовара, ТочностьЦеныПоУмолчанию); 
			
			Если Число(ЦенаПоУмолчанию) = ЦенаТовара Тогда
				УстановитьЗначениеXDTO(СведТов, "ЦенаТов", ЦенаПоУмолчанию);
			Иначе
				УстановитьЗначениеXDTO(СведТов, "ЦенаТов", XML_ФорматЧисла(ЦенаТовара, ТочностьЦены));
            КонецЕсли;
			
		Иначе
			УстановитьЗначениеXDTO(СведТов, "ЦенаТов", XML_ФорматЧисла(ЦенаТовара, ТочностьЦены));
		КонецЕсли;
		
	Иначе	
		УстановитьЗначениеXDTO(СведТов, "ЦенаТов", 0);
	КонецЕсли;	
	
	УстановитьЗначениеXDTO(СведТов, "СтТовБезНДС", XML_ФорматЧисла(Item.SubtotalWithVatExcluded, 2));
	
	Если ЭтоИстина(Item.HyphenSubtotal) Или Не ЗначениеЗаполнено(Item.Subtotal) Тогда
		УстановитьЗначениеXDTO(СведТов, "ДефСтТовУчНал", СтрокаДефис());
	Иначе
		УстановитьЗначениеXDTO(СведТов, "СтТовУчНал", XML_ФорматЧисла(Item.Subtotal, 2));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "НалСт", НалСт_Из_TaxRate(Item.TaxRate));
	
	// Акциз
	Акциз = НовыйДочернийОбъектXDTO(Фабрика, СведТов, "Акциз");
	
	Если НЕ ЗначениеЗаполнено(Строка(Item.Excise)) Тогда
		УстановитьЗначениеXDTO(Акциз, "БезАкциз", "без акциза");
	Иначе
		УстановитьЗначениеXDTO(Акциз, "СумАкциз", XML_ФорматЧисла(Число(Item.Excise), 2));
	КонецЕсли;
	
	// Сумма НДС
	СумНал = НовыйДочернийОбъектXDTO(Фабрика, СведТов, "СумНал");
	
	Если ЭтоИстина(Item.WithoutVat) Тогда
		УстановитьЗначениеXDTO(СумНал, "БезНДС", СтрокаБезНДС());
	ИначеЕсли ЭтоИстина(Item.HyphenVat) Или Item.Vat = Неопределено Тогда
		УстановитьЗначениеXDTO(СумНал, "ДефНДС", СтрокаДефис());
	Иначе
		УстановитьЗначениеXDTO(СумНал, "СумНал", XML_ФорматЧисла(Item.Vat, 2));
	КонецЕсли;
	
	СтранаПроисхождения = Неопределено;
	
	// ГТД
	Для Каждого CustomDeclaration Из Item.CustomDeclarations Цикл
		
		СвТД = НовыйДочернийОбъектXDTO(Фабрика, СведТов, "СвТД");
		
		Если ЗначениеЗаполнено(CustomDeclaration.DeclarationNumber) Тогда
			УстановитьЗначениеXDTO(СвТД, "НомерТД", CustomDeclaration.DeclarationNumber);
		КонецЕсли;	
		
		Если ЭтоИстина(CustomDeclaration.HyphenCountry)
			Или Не ЗначениеЗаполнено(CustomDeclaration.CountryCode) Тогда
			УстановитьЗначениеXDTO(СвТД, "ДефКодПроисх", СтрокаДефис());
		Иначе
			УстановитьЗначениеXDTO(СвТД, "КодПроисх", CustomDeclaration.CountryCode);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(CustomDeclaration.CountryCode)
			И Не ЗначениеЗаполнено(СтранаПроисхождения) Тогда
			СтранаПроисхождения = НаименованиеСтраны(CustomDeclaration.CountryCode);
		КонецЕсли;
		
	КонецЦикла;
	
	// Доп. сведения
	ДопСведТов = НовыйДочернийОбъектXDTO(Фабрика, СведТов, "ДопСведТов");
	УстановитьЗначениеXDTO(ДопСведТов, "КрНаимСтрПр", СтранаПроисхождения);
	XML_УПД_820_ЗаполнитьДопСведТов(ДопСведТов, Item);
	
	// Сведения о прослеживаемых товарах
	Если ЗначениеЗаполнено(Item.ItemTracingInfos) Тогда
		XML_УПД_820_ЗаполнитьСведПрослеж(ИмяПакетаXDTO, ДопСведТов, Item.ItemTracingInfos, "СведПрослеж");
	КонецЕсли;
	
	// Сведения о маркировке
	Если ЗначениеЗаполнено(Item.ItemIdentificationNumbers) Тогда
		XML_ЗаполнитьНомСредИдентТов(ИмяПакетаXDTO, ДопСведТов, Item.ItemIdentificationNumbers, "НомСредИдентТов");
	КонецЕсли;
	
	// Информационное поле события (факта хозяйственной жизни) 2
	XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, СведТов.ИнфПолФХЖ2, Item.StructedAdditionalInfos);
	
КонецПроцедуры

Процедура XML_УПД_820_ЗаполнитьДопСведТов(ДопСведТов, ПротоКонтент)
	
	УстановитьЗначениеXDTO(ДопСведТов, "ПрТовРаб",		XML_ПолучитьПризнакНоменклатуры(ПротоКонтент.ItemMark));
	УстановитьЗначениеXDTO(ДопСведТов, "КодТов",		ПротоКонтент.ItemVendorCode);
	УстановитьЗначениеXDTO(ДопСведТов, "НадлОтп",		ПротоКонтент.ItemToRelease);
	УстановитьЗначениеXDTO(ДопСведТов, "ДопПризн",		ПротоКонтент.AdditionalProperty);
	УстановитьЗначениеXDTO(ДопСведТов, "НаимЕдИзм",		ПротоКонтент.UnitName);
	УстановитьЗначениеXDTO(ДопСведТов, "ХарактерТов",	ПротоКонтент.ItemCharact);
	УстановитьЗначениеXDTO(ДопСведТов, "СортТов",		ПротоКонтент.ItemKind);
	УстановитьЗначениеXDTO(ДопСведТов, "АртикулТов", 	ПротоКонтент.ItemArticle);
	УстановитьЗначениеXDTO(ДопСведТов, "КодКат", 		ПротоКонтент.CatalogCode);
	УстановитьЗначениеXDTO(ДопСведТов, "КодВидТов", 	ПротоКонтент.ItemTypeCode);
	
КонецПроцедуры

Процедура XML_УПД_820_ЗаполнитьСведПрослеж(ИмяПакетаXDTO, ОбъектXDTO, ItemTracingInfos, ИмяИнфоПоля)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого ItemTracingInfo Из ItemTracingInfos Цикл
		Если ЗначениеЗаполнено(ItemTracingInfo.RegNumberUnit) Тогда
			СведПрослеж = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, ИмяИнфоПоля);
			XML_УПД_ЗаполнитьЗначениеСведПрослеж(СведПрослеж, ItemTracingInfo);
		КонецЕсли;
	КонецЦикла;
		
КонецПроцедуры

Процедура XML_УПД_ЗаполнитьЗначениеСведПрослеж(СведПрослеж, ПротоКонтент)
	
	УстановитьЗначениеXDTO(СведПрослеж, "НомТовПрослеж",	ПротоКонтент.RegNumberUnit);
	УстановитьЗначениеXDTO(СведПрослеж, "ЕдИзмПрослеж",		ПротоКонтент.Unit);
	УстановитьЗначениеXDTO(СведПрослеж, "НаимЕдИзмПрослеж",	ПротоКонтент.UnitName);
	УстановитьЗначениеXDTO(СведПрослеж, "КолВЕдПрослеж",	ПротоКонтент.Quantity);
	УстановитьЗначениеXDTO(СведПрослеж, "ДопПрослеж",		ПротоКонтент.ItemAddInfo);
	
КонецПроцедуры

Процедура XML_ЗаполнитьНомСредИдентТов(ИмяПакетаXDTO, ОбъектXDTO, ItemIdentificationNumbers, ИмяИнфоПоля)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого ItemIdentificationNumber Из ItemIdentificationNumbers Цикл
		НомСредИдентТов = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, ИмяИнфоПоля);
		XML_ЗаполнитьЗначениеНомСредИдентТов(ИмяПакетаXDTO, НомСредИдентТов, ItemIdentificationNumber);
	КонецЦикла;
		
КонецПроцедуры

Процедура XML_ЗаполнитьЗначениеНомСредИдентТов(ИмяПакетаXDTO, НомСредИдентТов, ПротоКонтент)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(НомСредИдентТов, "ИдентТрансУпак", ПротоКонтент.TransPackageId);
	
	Для Каждого Unit Из ПротоКонтент.Units Цикл
		НомСредИдентТов.КИЗ.Добавить(Unit);
	КонецЦикла;
	
	Для Каждого PackageId Из ПротоКонтент.PackageIds Цикл
		НомСредИдентТов.НомУпак.Добавить(PackageId);
	КонецЦикла;
		
КонецПроцедуры

Процедура XML_ОтветныйТитул_УПД_820_ЗаполнитьРасширенныхПодписантов(ИмяПакетаXDTO, ОбъектXDTO, ПротоКонтент)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого ExtendedSigner Из СписокXDTOИзЭлемента(ПротоКонтент, "Signers") Цикл
		
		SignerDetails		 = ExtendedSigner.SignerDetails;
		Статус				 = XML_ОтветныйТитул_УПД_820_СтатусПодписанта(SignerDetails.Status);
		ОбластьПолномочий	 = XML_ОбластьПолномочийПодписанта(SignerDetails.Powers);
		
		// Сведения о лице, подписавшем информацию покупателя в электронном виде
		Подписант = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "Подписант");
		XML_ЗаполнитьРасширенногоПодписанта(ИмяПакетаXDTO, Подписант, SignerDetails, Статус, ОбластьПолномочий);
		
	КонецЦикла;
	
КонецПроцедуры

Функция XML_ОтветныйТитул_УПД_820_СтатусПодписанта(Status)
	
	СоответствиеСтатусов = Новый Соответствие;
		
	СоответствиеСтатусов.Вставить("OtherOrganizationEmployee",			3); // - работник иной уполномоченной организации
	СоответствиеСтатусов.Вставить("AuthorizedPerson",					4); // - уполномоченное физическое лицо (в том числе индивидуальный предприниматель)
	СоответствиеСтатусов.Вставить("BuyerEmployee",						5); // - работник организации - покупателя
	СоответствиеСтатусов.Вставить("SellerEmployee",						5); // - работник организации - покупателя // в Диадоке статус подписанта №3 покупателя и продавца имеют одинаковое значение
	СоответствиеСтатусов.Вставить("InformationCreatorBuyerEmployee",	6); // - работник организации - составителя файла обмена информации покупателя, если составитель файла обмена информации покупателя не является покупателем
	
	Результат = СоответствиеСтатусов.Получить(Status);

	Возврат Результат;
	
КонецФункции

Процедура XML_ОтветныйТитул_УПД_820_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, СодФХЖ4
	, ПротоКонтент
	, НаименованиеДокумента
	, ФункцияДокумента
	, НомерДокумента
	, ДатаДокумента)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	УстановитьЗначениеXDTO(СодФХЖ4, "Функция",		ФункцияДокумента);
	УстановитьЗначениеXDTO(СодФХЖ4, "ВидОперации",	ПротоКонтент.OperationCode);
	УстановитьЗначениеXDTO(СодФХЖ4, "НаимДокОпрПр",	НаименованиеДокумента);
	УстановитьЗначениеXDTO(СодФХЖ4, "ДатаСчФИнфПр",	Дата_В_Формат(ДатаДокумента, ФорматДаты));
	СодФХЖ4.НомСчФИнфПр = НомерДокумента;
	
	// Сведения о принятии товаров, работ, услуг
	СвПрин = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ4, "СвПрин");
	УстановитьЗначениеXDTO(СвПрин, "ДатаПрин",	Дата_В_Формат(ПротоКонтент.AcceptanceDate, ФорматДаты));
	
	ЕстьСведенияОРаботнике = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "Employee") И ЗначениеЗаполнено(ПротоКонтент.Employee.Surname);
	ЕстьСведенияОбИномЛице = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "OtherIssuer") И ЗначениеЗаполнено(ПротоКонтент.OtherIssuer.Surname);
	
	Если ЕстьСведенияОРаботнике Или ЕстьСведенияОбИномЛице Тогда
		
		// Сведения о лице, принявшем товары (груз)
		СвЛицПрин = НовыйДочернийОбъектXDTO(Фабрика, СвПрин, "СвЛицПрин");
		
		Если ЕстьСведенияОРаботнике Тогда
			
			// Работник организации покупателя
			РабОргПок = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПрин, "РабОргПок");
			XML_ЗаполнитьРаботникаОрганизации(ИмяПакетаXDTO, РабОргПок, ПротоКонтент.Employee);
			
		ИначеЕсли ЕстьСведенияОбИномЛице Тогда
			
			// Иное лицо
			ИнЛицо = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПрин, "ИнЛицо");
			
			Если ЗначениеЗаполнено(ПротоКонтент.OtherIssuer.EmployeePosition) Тогда
				
				// Представитель организации, которой доверено принятие товаров (груза)
				ПредОргПрин = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ПредОргПрин");
				XML_ЗаполнитьПредставителяОрганизации(ИмяПакетаXDTO, ПредОргПрин, ПротоКонтент.OtherIssuer);
				
			Иначе
				
				// Физическое лицо, которому доверено принятие товаров (груза)
				ФЛПрин = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ФЛПрин");
				XML_ЗаполнитьФизическоеЛицо(ИмяПакетаXDTO, ФЛПрин, ПротоКонтент.OtherIssuer);
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	ЕстьСодержаниеОперации		= ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "OperationContent") И ЗначениеЗаполнено(ПротоКонтент.OperationContent);
	ЕстьКодСодержанияОперации	= ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "ContentOperCode") И ЗначениеЗаполнено(ПротоКонтент.ContentOperCode.TotalCode); 
	
	Если ЕстьСодержаниеОперации Тогда
		УстановитьЗначениеXDTO(СвПрин, "СодОпер", ПротоКонтент.OperationContent);
	КонецЕсли;	
		
	Если ЕстьКодСодержанияОперации Тогда
		
		КодСодОпер = НовыйДочернийОбъектXDTO(Фабрика, СвПрин, "КодСодОпер");
		XML_ЗаполнитьКодСодержанияОперации(ИмяПакетаXDTO, КодСодОпер, ПротоКонтент.ContentOperCode);
		
	КонецЕсли;	
	
	XML_ЗаполнитьИнфоПоляФХЖ(ИмяПакетаXDTO, СодФХЖ4, ПротоКонтент.AdditionalInfoId, "ИнфПолФХЖ4", "Файл.ИнфПок.СодФХЖ4.ИнфПолФХЖ4.ТекстИнф");
	
КонецПроцедуры

Процедура XML_ОтветныйТитул_УПД_820_ЗаполнитьИнфПокГосЗакКазн(ИмяПакетаXDTO, ИнфПок, ПротоКонтент)
	
	Если ЗначениеЗаполнено(ПротоКонтент.BuyerInfoCircumPublicProc.PersonalAccountBuyer) Тогда
		
		Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
		ИнфПокГосЗакКазн = НовыйДочернийОбъектXDTO(Фабрика, ИнфПок,	"ИнфПокГосЗакКазн");
		
		ФорматДаты		= "ДФ=dd.MM.yyyy";
		КонтентГосЗак	= ПротоКонтент.BuyerInfoCircumPublicProc;
		
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "ИдКодЗак",		КонтентГосЗак.ProcCode);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "ЛицСчетПок",		КонтентГосЗак.PersonalAccountBuyer);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "НаимФинОргПок",	КонтентГосЗак.NameFinAuthority);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "НомРеестрЗапПок",	КонтентГосЗак.BuyerRegistryEntryNumber);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "УчНомБюдОбязПок",	КонтентГосЗак.BuyerLiabNumber);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "КодКазначПок",	КонтентГосЗак.BuyerTreasuryCode);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "НаимКазначПок",	КонтентГосЗак.SellerTreasuryName);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "ОКТМОПок",		КонтентГосЗак.OKTMOBuy);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "ОКТМОМесПост",	КонтентГосЗак.OKTMOPlaceDelivery);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "ДатаОплПред",		Дата_В_Формат(КонтентГосЗак.PayDeadLine, ФорматДаты));
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "УчНомДенОбяз",	КонтентГосЗак.NumberFundsLiab);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "ОчерПлат",		КонтентГосЗак.PriorityPayment);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "ВидПлат",			КонтентГосЗак.TypePayment);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(КонтентГосЗак, "InfoFundsLiab") И ЗначениеЗаполнено(КонтентГосЗак.InfoFundsLiab) Тогда
			XML_ОтветныйТитул_УПД_820_ЗаполнитьИнфСведДенОбяз(ИмяПакетаXDTO, ИнфПокГосЗакКазн, КонтентГосЗак.InfoFundsLiab, "ИнфСведДенОбяз");
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ОтветныйТитул_УПД_820_ЗаполнитьИнфСведДенОбяз(ИмяПакетаXDTO, ОбъектXDTO, InfoFundsLiabs, ИмяИнфоПоля)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого InfoFundsLiab Из InfoFundsLiabs Цикл
		ИнфСведДенОбяз = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, ИмяИнфоПоля);
		XML_ОтветныйТитул_УПД_820_ЗаполнитьЗначениеИнфСведДенОбяз(ИнфСведДенОбяз, InfoFundsLiab);
	КонецЦикла;
		
КонецПроцедуры

Процедура XML_ОтветныйТитул_УПД_820_ЗаполнитьЗначениеИнфСведДенОбяз(ИнфСведДенОбяз, ПротоКонтент)
	
	УстановитьЗначениеXDTO(ИнфСведДенОбяз, "НомСтр",			ПротоКонтент.NumberString);
	УстановитьЗначениеXDTO(ИнфСведДенОбяз, "КодОбъектФАИП",		ПротоКонтент.ObjectCodeFAIP);
	УстановитьЗначениеXDTO(ИнфСведДенОбяз, "ВидСредств",		ПротоКонтент.TypeFundsSource);
	УстановитьЗначениеXDTO(ИнфСведДенОбяз, "КодПокБюджКласс",	ПротоКонтент.BuyerBudjetClassCode);
	УстановитьЗначениеXDTO(ИнфСведДенОбяз, "КодЦелиПокуп",		ПротоКонтент.BuyerTargetCode);
	УстановитьЗначениеXDTO(ИнфСведДенОбяз, "СумАванс",			ПротоКонтент.AmountAdvance);
	
КонецПроцедуры

// XML: функции заполнения УПД 970

Процедура XML_УПД_970_ЗаполнитьПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент)

	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого Signer Из СписокXDTOИзЭлемента(ПротоКонтент, "Signers") Цикл

		Подписант = НовыйДочернийОбъектXDTO(Фабрика, Документ, "Подписант");
		
		УстановитьЗначениеXDTO(Подписант, "Должн", Signer.JobTitle);
		УстановитьЗначениеXDTO(Подписант, "СпосПодтПолном", Signer.ConfirmCredentials);
		УстановитьЗначениеXDTO(Подписант, "ДопСведПодп", Signer.AdditionalInfo);
		
		ДатаПодписанияДокумента = Дата1С_в_ДатаXML(Signer.DateSigningDocument);
		УстановитьЗначениеXDTO(Подписант, "ДатаПодДок", ДатаПодписанияДокумента);
		
		ТипПодписиXML = XML_ТипПодписи(Signer.SignType);	
		УстановитьЗначениеXDTO(Подписант, "ТипПодпис", ТипПодписиXML);
		
		Если ЗначениеЗаполнено(Signer.Surname) Тогда
			ФИО = НовыйДочернийОбъектXDTO(Фабрика, Подписант, "ФИО");
			XML_ЗаполнитьФИОТип(ФИО, Signer);
		КонецЕсли;
		
		Если XML_УПД_970_ЭтоСпособПодтвержденияПолномочийЭлектроннойДоверенностью(Signer.ConfirmCredentials) Тогда
			
			Для Каждого ElectronicPOA Из СписокXDTOИзЭлемента(Signer, "ElectronicPOA") Цикл
				
				СвДоверЭл = НовыйДочернийОбъектXDTO(Фабрика, Подписант, "СвДоверЭл");
				
				ДатаВыдачи = Дата1С_в_ДатаXML(ElectronicPOA.IssueDate);
				УстановитьЗначениеXDTO(СвДоверЭл, "ДатаВыдДовер", ДатаВыдачи);
				УстановитьЗначениеXDTO(СвДоверЭл, "НомДовер", ElectronicPOA.ID);
				
				ДатаРегистрации = Дата1С_в_ДатаXML(ElectronicPOA.RegistrationDate);
				УстановитьЗначениеXDTO(СвДоверЭл, "ДатаВнРегДовер", ДатаРегистрации);
				УстановитьЗначениеXDTO(СвДоверЭл, "ВнНомДовер", ElectronicPOA.Number);
				
				УстановитьЗначениеXDTO(СвДоверЭл, "ИдСистХран", ElectronicPOA.StorageID);
				УстановитьЗначениеXDTO(СвДоверЭл, "УРЛСист", ElectronicPOA.URL);
				
			КонецЦикла;			
			
		ИначеЕсли XML_УПД_970_ЭтоСпособПодтвержденияПолномочийДоверенностьюНаБумаге(Signer.ConfirmCredentials) Тогда
			
			Для Каждого PaperPOA Из СписокXDTOИзЭлемента(Signer, "PaperPOA") Цикл
				
				СвДоверБум = НовыйДочернийОбъектXDTO(Фабрика, Подписант, "СвДоверБум");
				
				ДатаВыдачи = Дата1С_в_ДатаXML(PaperPOA.IssueDate);
				УстановитьЗначениеXDTO(СвДоверБум, "ДатаВыдДовер", ДатаВыдачи);
				УстановитьЗначениеXDTO(СвДоверБум, "ВнНомДовер", PaperPOA.Number);
				УстановитьЗначениеXDTO(СвДоверБум, "СвИдДовер", PaperPOA.PrincipalInfo);
				
				Если ЗначениеЗаполнено(PaperPOA.IssuerFirstName)
					И ЗначениеЗаполнено(PaperPOA.IssuerSurname) Тогда
					
					ФИО = НовыйДочернийОбъектXDTO(Фабрика, СвДоверБум, "ФИО");
				
					УстановитьЗначениеXDTO(ФИО, "Фамилия", PaperPOA.IssuerSurname);
					УстановитьЗначениеXDTO(ФИО, "Имя", PaperPOA.IssuerFirstName);
					УстановитьЗначениеXDTO(ФИО, "Отчество", ПолучитьЗначениеXDTO(PaperPOA, "IssuerPatronymic"));
					
				КонецЕсли;
				
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьСведенияСФ(ИмяПакетаXDTO, Документ, ПротоКонтент)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	СвСчФакт = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СвСчФакт");
	
	ДатаДокумента = Дата1С_в_ДатаXML(ПротоКонтент.DocumentDate);
	
	УстановитьЗначениеXDTO(СвСчФакт, "НомерДок", ПротоКонтент.DocumentNumber);
	УстановитьЗначениеXDTO(СвСчФакт, "ДатаДок", ДатаДокумента);
	
	Если ЗначениеЗаполнено(ПротоКонтент.RevisionNumber)
		ИЛИ ЗначениеЗаполнено(ПротоКонтент.RevisionDate) Тогда
		
		ИспрДок = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ИспрДок");
		
		ДатаИсправления = Дата1С_в_ДатаXML(ПротоКонтент.RevisionDate);
		
		УстановитьЗначениеXDTO(ИспрДок, "НомИспр", ПротоКонтент.RevisionNumber);
		УстановитьЗначениеXDTO(ИспрДок, "ДатаИспр", ДатаИсправления);
		
	КонецЕсли;
	
	Для Каждого Seller Из ПротоКонтент.Sellers Цикл
		СвПрод = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "СвПрод");
		XML_ЗаполнитьСведенияОКонтрагенте_970(ИмяПакетаXDTO, СвПрод, Seller);
	КонецЦикла;
	
	Для Каждого Buyer Из ПротоКонтент.Buyers Цикл
		СвПокуп = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "СвПокуп");
		XML_ЗаполнитьСведенияОКонтрагенте_970(ИмяПакетаXDTO, СвПокуп, Buyer);
	КонецЦикла;
	
	Для Каждого Consignee Из ПротоКонтент.Consignees Цикл
		Если ЗначениеЗаполнено(Consignee.Name) Тогда
			ГрузПолуч = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ГрузПолуч");
			XML_ЗаполнитьСведенияОКонтрагенте_970(ИмяПакетаXDTO, ГрузПолуч, Consignee);
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Shipper Из ПротоКонтент.Shippers Цикл
		Если Shipper.SameAsSeller = Истина ИЛИ ЗначениеЗаполнено(Shipper.OrganizationInfo.Name) Тогда
			
			ГрузОт = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ГрузОт");
			
			Если Shipper.SameAsSeller = Истина Тогда
				УстановитьЗначениеXDTO(ГрузОт, "ОнЖе", "он же");
			Иначе
				ГрузОтпр = НовыйДочернийОбъектXDTO(Фабрика, ГрузОт, "ГрузОтпр");
				XML_ЗаполнитьСведенияОКонтрагенте_970(ИмяПакетаXDTO, ГрузОтпр, Shipper.OrganizationInfo);
			КонецЕсли;
			
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого PaymentDocument Из ПротоКонтент.PaymentDocuments Цикл
		
		СвПРД = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "СвПРД");
		
		ДатаДокументаОплаты = Дата1С_в_ДатаXML(PaymentDocument.Date);
		
		УстановитьЗначениеXDTO(СвПРД, "НомерПРД", PaymentDocument.Number);
		УстановитьЗначениеXDTO(СвПРД, "ДатаПРД", ДатаДокументаОплаты);
		УстановитьЗначениеXDTO(СвПРД, "СуммаПРД", PaymentDocument.Total);
		
	КонецЦикла;
	
	ДенИзм = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ДенИзм");
	
	НаименованиеВалюты = НаименованиеВалюты(ПротоКонтент.Currency, Истина);
	
	УстановитьЗначениеXDTO(ДенИзм, "КодОКВ", ПротоКонтент.Currency);
	УстановитьЗначениеXDTO(ДенИзм, "НаимОКВ", НаименованиеВалюты);
	УстановитьЗначениеXDTO(ДенИзм, "КурсВал", ПротоКонтент.CurrencyRate);
	
	Для Каждого DocumentShipment Из ПротоКонтент.DocumentShipments Цикл
		Если ЗначениеЗаполнено(DocumentShipment.Name) Тогда
			ДокПодтвОтгрНом = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ДокПодтвОтгрНом");
			XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, ДокПодтвОтгрНом, DocumentShipment);
		КонецЕсли;
	КонецЦикла;
	
	AdditionalInfoId = ПротоКонтент.AdditionalInfoId;
	Если ЗначениеЗаполнено(AdditionalInfoId.InfoFileId)
		ИЛИ ЗначениеЗаполнено(AdditionalInfoId.StructedAdditionalInfos) Тогда
		
		ИнфПолФХЖ1 = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ИнфПолФХЖ1");
		УстановитьЗначениеXDTO(ИнфПолФХЖ1, "ИдФайлИнфПол", AdditionalInfoId.InfoFileId);
		
		XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, ИнфПолФХЖ1.ТекстИнф, AdditionalInfoId.StructedAdditionalInfos);
		
	КонецЕсли;
	
	НеобходимоДобавитьДопСвФХЖ1 = XML_УПД_970_НеобходимоДобавитьДопСвФХЖ1(ПротоКонтент);
	
	Если НеобходимоДобавитьДопСвФХЖ1 Тогда
		
		ДопСвФХЖ1 = НовыйДочернийОбъектXDTO(Фабрика, СвСчФакт, "ДопСвФХЖ1");
		
		УстановитьЗначениеXDTO(ДопСвФХЖ1, "ИдГосКон", ПротоКонтент.GovernmentContractInfo);
		
		ФункцииУПД = Перечисление_ФункцииУПД();
		
		XML_УПД_970_ЗаполнитьОбстоятельстваФормирования(ДопСвФХЖ1, ПротоКонтент, ФункцииУПД);
		
		Если ЗначениеЗаполнено(ПротоКонтент.FactorInfo.Type) Тогда
			СвФактор = НовыйДочернийОбъектXDTO(Фабрика, ДопСвФХЖ1, "СвФактор");
			XML_ЗаполнитьСведенияОКонтрагенте_970(ИмяПакетаXDTO, СвФактор, ПротоКонтент.FactorInfo);
		КонецЕсли;
		
		MainAssignMonetaryClaim = ПротоКонтент.MainAssignMonetaryClaim;
		Если ЗначениеЗаполнено(MainAssignMonetaryClaim.Name) Тогда
			
			ОснУстДенТреб = НовыйДочернийОбъектXDTO(Фабрика, ДопСвФХЖ1, "ОснУстДенТреб");
			XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, ОснУстДенТреб, MainAssignMonetaryClaim);
			
		КонецЕсли;
		
		SellerInfoCircumPublicProc = ПротоКонтент.SellerInfoCircumPublicProc;
		Если ЗначениеЗаполнено(SellerInfoCircumPublicProc.DateStateContract) Тогда
			
			Если XML_УПД_970_ИспользуетсяФормат5_03() Тогда
				ИнфПродГосЗакКазн = НовыйДочернийОбъектXDTO(Фабрика, ДопСвФХЖ1, "ИнфПродЗаГоскКазн");
			Иначе
				ИнфПродГосЗакКазн = НовыйДочернийОбъектXDTO(Фабрика, ДопСвФХЖ1, "ИнфПродЗаГосКазн");
			КонецЕсли;
			
			ДатаГосКонтракта = Дата1С_в_ДатаXML(SellerInfoCircumPublicProc.DateStateContract);
			
			УстановитьЗначениеXDTO(ИнфПродГосЗакКазн, "ДатаГосКонт", ДатаГосКонтракта);
			УстановитьЗначениеXDTO(ИнфПродГосЗакКазн, "НомерГосКонт", SellerInfoCircumPublicProc.NumberStateContract);
			УстановитьЗначениеXDTO(ИнфПродГосЗакКазн, "ЛицСчетПрод", SellerInfoCircumPublicProc.PersonalAccountSeller);
			УстановитьЗначениеXDTO(ИнфПродГосЗакКазн, "КодПродБюджКласс", SellerInfoCircumPublicProc.SellerBudjetClassCode);
			УстановитьЗначениеXDTO(ИнфПродГосЗакКазн, "КодЦелиПрод", SellerInfoCircumPublicProc.SellerTargetCode);
			УстановитьЗначениеXDTO(ИнфПродГосЗакКазн, "КодКазначПрод", SellerInfoCircumPublicProc.SellerTreasuryCode);
			УстановитьЗначениеXDTO(ИнфПродГосЗакКазн, "НаимКазначПрод", SellerInfoCircumPublicProc.SellerTreasuryName);
			
		КонецЕсли;
		
		Для Каждого CommitmentType Из ПротоКонтент.CommitmentType Цикл
			
			ДокПодтвОтгрНом = НовыйДочернийОбъектXDTO(Фабрика, ДопСвФХЖ1, "ВидОбяз");
			
			УстановитьЗначениеXDTO(ДокПодтвОтгрНом, "КодВидОбяз", CommitmentType.Code);
			УстановитьЗначениеXDTO(ДокПодтвОтгрНом, "НаимВидОбяз", CommitmentType.Name);
			
		КонецЦикла;
		
		Для Каждого AccompanyingDocument Из ПротоКонтент.AccompanyingDocuments Цикл
			СопрДокФХЖ = НовыйДочернийОбъектXDTO(Фабрика, ДопСвФХЖ1, "СопрДокФХЖ");
			XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, СопрДокФХЖ, AccompanyingDocument);
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьОбстоятельстваФормирования(ДопСвФХЖ1, Знач ПротоКонтент, Знач ФункцииУПД)
	
	Если XML_УПД_970_ИспользуетсяФормат5_03()
		И НЕ ЗначениеЗаполнено(ПротоКонтент.CircumFormatInvoice) Тогда
		Возврат;
	КонецЕсли;
	
	// BSLLS:IfElseIfEndsWithElse-off
	Если ПротоКонтент.Function = ФункцииУПД.СЧФ Тогда
		СпОбстФСЧФ = XML_УПД_970_ПолучитьОбстоятельствоФормированияСЧФ(ПротоКонтент);
		УстановитьЗначениеXDTO(ДопСвФХЖ1, "СпОбстФСЧФ", СпОбстФСЧФ);
	ИначеЕсли ПротоКонтент.Function = ФункцииУПД.СЧФДОП Тогда
		СпОбстФСЧФДОП = XML_УПД_970_ПолучитьОбстоятельствоФормированияСЧФДОПиДОП(ПротоКонтент);
		УстановитьЗначениеXDTO(ДопСвФХЖ1, "СпОбстФСЧФДОП", СпОбстФСЧФДОП);
	ИначеЕсли ПротоКонтент.Function = ФункцииУПД.ДОП Тогда
		СпОбстФДОП = XML_УПД_970_ПолучитьОбстоятельствоФормированияСЧФДОПиДОП(ПротоКонтент);
		УстановитьЗначениеXDTO(ДопСвФХЖ1, "СпОбстФДОП", СпОбстФДОП);
	КонецЕсли;
	// BSLLS:IfElseIfEndsWithElse-on
	
КонецПроцедуры

Функция XML_УПД_970_ИспользуетсяФормат5_03()
	
	Результат = Ложь;
	
	ДатаФормата5_03 = Дата("20250122");
	Если ТекущаяУниверсальнаяДата() + 3 * 60 * 60 >= ДатаФормата5_03 Тогда
		Результат = Истина;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Если мы добавляем структуру ДопСвФХЖ1, то по формату в нем обязательно должно быть одно из полей:
// СпОбстФСЧФ | СпОбстФСЧФДОП | СпОбстФДОП.
// Остальные поля структуры ДопСвФХЖ1 опускаем, т.к. в интеграционном модуле они не заполняются
// Если будут обращения, дополнить условие.
//
// Параметры:
//  Content	 - Структура - прото структура
//
// Возвращаемое значение:
//  Булево - Истина - добавить структуру ДопСвФХЖ1
//
Функция XML_УПД_970_НеобходимоДобавитьДопСвФХЖ1(ПротоКонтент)
	
	Результат = (ЗначениеЗаполнено(ПротоКонтент.GovernmentContractInfo)
		Или ЗначениеЗаполнено(ПротоКонтент.CircumFormatInvoice)
		Или ЗначениеЗаполнено(ПротоКонтент.FactorInfo.Type)
		Или ЗначениеЗаполнено(ПротоКонтент.MainAssignMonetaryClaim.Name)
		Или ЗначениеЗаполнено(ПротоКонтент.SellerInfoCircumPublicProc.DateStateContract)
		Или ПротоКонтент.CommitmentType.Количество() > 0
		Или ПротоКонтент.AccompanyingDocuments.Количество() > 0);
	
	Возврат Результат;
	
КонецФункции

// Возвращает значение для поля СпОбстФСЧФ
// Если в прото значение не указано, то возвращает значение по умолчанию
// По формату нет значений по умолчанию, но без заполнения полей СпОбстФСЧФ | СпОбстФСЧФДОП | СпОбстФДОП
// не получится заполнить вложенные поля структуры ДопСвФХЖ1
//
// Параметры:
//  Content	 - Структура - прото структура
// 
// Возвращаемое значение:
//  Строка - значение для поля СпОбстФСЧФ
//
Функция XML_УПД_970_ПолучитьОбстоятельствоФормированияСЧФ(ПротоКонтент)

	Результат = ПротоКонтент.CircumFormatInvoice;
	
	Если Не ЗначениеЗаполнено(Результат) Тогда
		Результат = "1";
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает значение для полей СпОбстФСЧФДОП и СпОбстФДОП
// Если в прото значение не указано, то возвращает значение по умолчанию
// По формату нет значений по умолчанию, но без заполнения полей СпОбстФСЧФ | СпОбстФСЧФДОП | СпОбстФДОП
// не получится заполнить вложенные поля структуры ДопСвФХЖ1
//
// Параметры:
//  Content	 - Структура - прото структура
// 
// Возвращаемое значение:
//  Строка - значение для поля СпОбстФСЧФДОП
//
Функция XML_УПД_970_ПолучитьОбстоятельствоФормированияСЧФДОПиДОП(ПротоКонтент)

	Результат = ПротоКонтент.CircumFormatInvoice;
	
	Если Не ЗначениеЗаполнено(Результат) Тогда
		Результат = "00005";
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура XML_УПД_970_ЗаполнитьСведенияТаблицыСФ(ИмяПакетаXDTO, Документ, ПротоКонтент, ПараметрыФормированияФайла)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Table = ПротоКонтент.Table;
	
	Если ЗначениеЗаполнено(Table.Items) Тогда
		
		ТаблСчФакт = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ТаблСчФакт");
		
		Для НомерСтроки = 1 По Table.Items.Количество() Цикл
			
			СведТов = НовыйДочернийОбъектXDTO(Фабрика, ТаблСчФакт, "СведТов");
			
			УстановитьЗначениеXDTO(СведТов, "НомСтр", НомерСтроки);
			
			Item = Table.Items[НомерСтроки - 1];
			
			XML_УПД_970_ЗаполнитьСтрокуТабЧасти(
				ИмяПакетаXDTO,
				СведТов,
				Item,
				ПараметрыФормированияФайла);
			
		КонецЦикла;
		
		ВсегоОпл = НовыйДочернийОбъектXDTO(Фабрика, ТаблСчФакт, "ВсегоОпл");
		СумНалВсего = НовыйДочернийОбъектXDTO(Фабрика, ВсегоОпл, "СумНалВсего");
		
		УстановитьЗначениеXDTO(ВсегоОпл, "СтТовБезНДСВсего", XML_ФорматЧисла(Table.TotalWithVatExcluded, 2));
		УстановитьЗначениеXDTO(ВсегоОпл, "КолНеттоВс", Table.TotalNet);
		
		УстановитьЗначениеXDTO(ВсегоОпл, "СтТовУчНалВсего", XML_ФорматЧисла(Table.Total, 2));
		
		Если ЭтоИстина(Table.WithoutVat) Тогда
			УстановитьЗначениеXDTO(СумНалВсего, "БезНДС", СтрокаБезНДС());
		Иначе
			УстановитьЗначениеXDTO(СумНалВсего, "СумНал", XML_ФорматЧисла(Table.Vat, 2));
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьСтрокуТабЧасти(
		ИмяПакетаXDTO,
		СведТов,
		Item,
		ПараметрыФормированияФайла)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Если ЗначениеЗаполнено(Item.Product) Тогда
		УстановитьЗначениеXDTO(СведТов, "НаимТов", Item.Product);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "ОКЕИ_Тов", Item.Unit);
	УстановитьЗначениеXDTO(СведТов, "НаимЕдИзм", Item.UnitName);
	
	Если ЗначениеЗаполнено(Item.Quantity) Тогда
		Количество = XML_ФорматЧисла(Item.Quantity, ПараметрыФормированияФайла.ТочностьКоличества);
		УстановитьЗначениеXDTO(СведТов, "КолТов", Количество);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Item.Price) Тогда
		
		ЦенаТовара = Item.Price;
		ТочностьЦены = ПараметрыФормированияФайла.ТочностьЦены;
		
		Если ТочностьЦены = Неопределено Тогда
			
			ТочностьЦеныПоУмолчанию = XML_ПараметрыФормированияФайла().ТочностьЦены;
			
			ЦенаПоУмолчанию = XML_ФорматЧисла(ЦенаТовара, ТочностьЦеныПоУмолчанию);
			
			Если Число(ЦенаПоУмолчанию) = ЦенаТовара Тогда
				УстановитьЗначениеXDTO(СведТов, "ЦенаТов", ЦенаПоУмолчанию);
			Иначе
				УстановитьЗначениеXDTO(СведТов, "ЦенаТов", XML_ФорматЧисла(ЦенаТовара, ТочностьЦены));
			КонецЕсли;
			
		Иначе
			УстановитьЗначениеXDTO(СведТов, "ЦенаТов", XML_ФорматЧисла(ЦенаТовара, ТочностьЦены));
		КонецЕсли;
		
	КонецЕсли;
	
	СтоимостьБезНДС = XML_ФорматЧисла(Item.SubtotalWithVatExcluded, 2);
	УстановитьЗначениеXDTO(СведТов, "СтТовБезНДС", СтоимостьБезНДС);
	
	СтоимостьСНДС = XML_ФорматЧисла(Item.Subtotal, 2);
	УстановитьЗначениеXDTO(СведТов, "СтТовУчНал", СтоимостьСНДС);
	
	СтавкаНДС = НалСт_Из_TaxRate(Item.TaxRate);
	УстановитьЗначениеXDTO(СведТов, "НалСт", СтавкаНДС);
	
	СумНал = НовыйДочернийОбъектXDTO(Фабрика, СведТов, "СумНал");
	XML_ЗаполнитьНДС(СумНал, Item.Vat, Item.WithoutVat);
	
	Акциз = НовыйДочернийОбъектXDTO(Фабрика, СведТов, "Акциз");
	
	Если НЕ ЗначениеЗаполнено(Строка(Item.Excise)) Тогда
		УстановитьЗначениеXDTO(Акциз, "БезАкциз", "без акциза");
	Иначе
		СуммаАкциза = XML_ФорматЧисла(Число(Item.Excise), 2);
		УстановитьЗначениеXDTO(Акциз, "СумАкциз", СуммаАкциза);
	КонецЕсли;
	
	ДопСведТов = НовыйДочернийОбъектXDTO(Фабрика, СведТов, "ДопСведТов");
	
	Для Каждого CustomDeclaration Из Item.CustomDeclarations Цикл
		
		СвДТ = НовыйДочернийОбъектXDTO(Фабрика, СведТов, "СвДТ");
		
		Если ЗначениеЗаполнено(CustomDeclaration.DeclarationNumber) Тогда
			УстановитьЗначениеXDTO(СвДТ, "НомерДТ", CustomDeclaration.DeclarationNumber);
		КонецЕсли;
		
		УстановитьЗначениеXDTO(СвДТ, "КодПроисх", CustomDeclaration.CountryCode);
		
	КонецЦикла;
	
	Если XML_УПД_970_ИспользуетсяФормат5_03() Тогда
		XML_УПД_970_УстановитьСтрануПроисхожденияДопСведТов_5_03(ИмяПакетаXDTO, ДопСведТов, Item);
	Иначе
		XML_УПД_970_УстановитьСтрануПроисхожденияДопСведТов_5_02(ИмяПакетаXDTO, ДопСведТов, Item);
	КонецЕсли;
	
	XML_УПД_970_ЗаполнитьДопСведТов(ИмяПакетаXDTO, ДопСведТов, Item);
	
	XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, СведТов.ИнфПолФХЖ2, Item.StructedAdditionalInfos);
	
КонецПроцедуры

Процедура XML_УПД_970_УстановитьСтрануПроисхожденияДопСведТов_5_02(ИмяПакетаXDTO, ДопСведТов, СтрокаТоварыПрото)
	
	СтранаПроисхождения = Неопределено;
	
	Для Каждого CustomDeclaration Из СтрокаТоварыПрото.CustomDeclarations Цикл
		
		Если ЗначениеЗаполнено(CustomDeclaration.CountryCode)
			И НЕ ЗначениеЗаполнено(СтранаПроисхождения) Тогда
			СтранаПроисхождения = НаименованиеСтраны(CustomDeclaration.CountryCode);
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
	УстановитьЗначениеXDTO(ДопСведТов, "КрНаимСтрПр", СтранаПроисхождения);
	
КонецПроцедуры

Процедура XML_УПД_970_УстановитьСтрануПроисхожденияДопСведТов_5_03(ИмяПакетаXDTO, ДопСведТов, СтрокаТоварыПрото)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого CustomDeclaration Из СтрокаТоварыПрото.CustomDeclarations Цикл
		
		Если ЗначениеЗаполнено(CustomDeclaration.CountryCode) Тогда
			СтранаПроисхождения = НаименованиеСтраны(CustomDeclaration.CountryCode);
			ДопСведТов.КрНаимСтрПр.Добавить(СтранаПроисхождения);
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьДопСведТов(ИмяПакетаXDTO, ДопСведТов, СтрокаТоварыПрото)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ПризнакНоменклатуры = XML_ПолучитьПризнакНоменклатуры(СтрокаТоварыПрото.ItemMark);
	
	УстановитьЗначениеXDTO(ДопСведТов, "ПрТовРаб", ПризнакНоменклатуры);
	УстановитьЗначениеXDTO(ДопСведТов, "ДопПризн", СтрокаТоварыПрото.AdditionalProperty);
	УстановитьЗначениеXDTO(ДопСведТов, "НадлОтп", СтрокаТоварыПрото.ItemToRelease);
	УстановитьЗначениеXDTO(ДопСведТов, "ХарактерТов", СтрокаТоварыПрото.ItemCharact);
	УстановитьЗначениеXDTO(ДопСведТов, "СортТов", СтрокаТоварыПрото.ItemKind);
	УстановитьЗначениеXDTO(ДопСведТов, "СерияТов", СтрокаТоварыПрото.Series);
	УстановитьЗначениеXDTO(ДопСведТов, "АртикулТов", СтрокаТоварыПрото.ItemArticle);
	УстановитьЗначениеXDTO(ДопСведТов, "КодТов", СтрокаТоварыПрото.ItemVendorCode);
	УстановитьЗначениеXDTO(ДопСведТов, "ГТИН", GTINКаноническогоВида(СтрокаТоварыПрото.GTIN));
	УстановитьЗначениеXDTO(ДопСведТов, "КодКат", СтрокаТоварыПрото.CatalogCode);
	УстановитьЗначениеXDTO(ДопСведТов, "КодВидТов", СтрокаТоварыПрото.ItemTypeCode);
	УстановитьЗначениеXDTO(ДопСведТов, "КодВидПр", СтрокаТоварыПрото.ProductTypeCode);
	
	Если XML_УПД_970_ИспользуетсяФормат5_03() Тогда
		УстановитьЗначениеXDTO(ДопСведТов, "КодТовОКПД2", СтрокаТоварыПрото.CodeRCPEA);
	Иначе
		УстановитьЗначениеXDTO(ДопСведТов, "КодТовОКДП2", СтрокаТоварыПрото.CodeRCPEA);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СтрокаТоварыПрото.RecoveredVat) Тогда
		СумНалВосст = НовыйДочернийОбъектXDTO(Фабрика, ДопСведТов, "СумНалВосст");
		XML_ЗаполнитьНДС(СумНалВосст, СтрокаТоварыПрото.RecoveredVat, СтрокаТоварыПрото.RecoveredWithoutVat);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СтрокаТоварыПрото.AdditionalInfoTypeOperations) Тогда
		УстановитьЗначениеXDTO(ДопСведТов, "ДопИнфПВидО", СтрокаТоварыПрото.AdditionalInfoTypeOperations);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СтрокаТоварыПрото.ItemTracingInfos) Тогда
		
		XML_УПД_970_ЗаполнитьСведПрослеж(ИмяПакетаXDTO, ДопСведТов, СтрокаТоварыПрото.ItemTracingInfos, "СведПрослеж");
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СтрокаТоварыПрото.ItemIdentificationNumbers) Тогда
		XML_УПД_970_ЗаполнитьНомСредИдентТов(
			ИмяПакетаXDTO,
			ДопСведТов,
			СтрокаТоварыПрото.ItemIdentificationNumbers,
			"НомСредИдентТов");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СтрокаТоварыПрото.AdditionalInfoStateSystem) Тогда
		XML_УПД_970_ЗаполнитьСвГосСист(ИмяПакетаXDTO, ДопСведТов, СтрокаТоварыПрото.AdditionalInfoStateSystem, "СвГосСист");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СтрокаТоварыПрото.ItemAccompanyingDocuments) Тогда
		XML_УПД_970_ЗаполнитьСопрДокТов(ИмяПакетаXDTO, ДопСведТов, СтрокаТоварыПрото.ItemAccompanyingDocuments, "СопрДокТов");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(СтрокаТоварыПрото.TaxDeprication.Code) Тогда
		
		НалУчАморт = НовыйДочернийОбъектXDTO(Фабрика, ДопСведТов, "НалУчАморт");
		
		TaxDeprication = СтрокаТоварыПрото.TaxDeprication;
		
		УстановитьЗначениеXDTO(НалУчАморт, "АмГруппа", TaxDeprication.Code);
		УстановитьЗначениеXDTO(НалУчАморт, "КодОКОФ", TaxDeprication.OKOF);
		УстановитьЗначениеXDTO(НалУчАморт, "СрПолИспОС", TaxDeprication.UsefulPeriod);
		УстановитьЗначениеXDTO(НалУчАморт, "ФактСрокИсп", TaxDeprication.ActualPeriod);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьНомСредИдентТов(ИмяПакетаXDTO, ОбъектXDTO, ItemIdentificationNumbers, ИмяИнфоПоля)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого ItemIdentificationNumber Из ItemIdentificationNumbers Цикл
		
		НомСредИдентТов = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, ИмяИнфоПоля);
		
		XML_ЗаполнитьЗначениеНомСредИдентТов(ИмяПакетаXDTO, НомСредИдентТов, ItemIdentificationNumber);
		
		Если ЗначениеЗаполнено(ItemIdentificationNumber.MarkedItemsQuantity) Тогда
			УстановитьЗначениеXDTO(НомСредИдентТов, "КолВедМарк", ItemIdentificationNumber.MarkedItemsQuantity);
		КонецЕсли;
		
		УстановитьЗначениеXDTO(НомСредИдентТов, "ПрПартМарк", ItemIdentificationNumber.BatchCode);

	КонецЦикла;
		
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьСвГосСист(ИмяПакетаXDTO, ОбъектXDTO, StructedAdditionalInfos, ИмяИнфоПоля)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого StructedAdditionalInfo Из StructedAdditionalInfos Цикл
		
		СвГосСист = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, ИмяИнфоПоля);
		
		УстановитьЗначениеXDTO(СвГосСист, "НаимГосСист", StructedAdditionalInfo.SystemName);
		УстановитьЗначениеXDTO(СвГосСист, "УчетЕд", StructedAdditionalInfo.Unit);
  		УстановитьЗначениеXDTO(СвГосСист, "ИнаяИнф", StructedAdditionalInfo.OtherInfo);
		УстановитьЗначениеXDTO(СвГосСист, "ИдНомУчетЕд", StructedAdditionalInfo.IDUnit);

	КонецЦикла;
		
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьСопрДокТов(ИмяПакетаXDTO, ОбъектXDTO, ItemAccompanyingDocuments, ИмяИнфоПоля)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого AccompanyingDocument Из ItemAccompanyingDocuments Цикл
		СопрДокТов = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "СопрДокТов");
		XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, СопрДокТов, AccompanyingDocument);
	КонецЦикла;
		
КонецПроцедуры

Процедура XML_ЗаполнитьНДС(ОбъектXDTO, Сумма, БезНДС)
	
	Если ЭтоИстина(БезНДС) Тогда
		УстановитьЗначениеXDTO(ОбъектXDTO, "БезНДС", СтрокаБезНДС());
	Иначе
		СуммаНДС = XML_ФорматЧисла(Сумма, 2); 
		УстановитьЗначениеXDTO(ОбъектXDTO, "СумНал", СуммаНДС);
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьСведПрослеж(ИмяПакетаXDTO, ОбъектXDTO, ItemTracingInfos, ИмяИнфоПоля)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого ItemTracingInfo Из ItemTracingInfos Цикл
		
		Если ЗначениеЗаполнено(ItemTracingInfo.RegNumberUnit) Тогда
			
			СведПрослеж = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, ИмяИнфоПоля);
			
			XML_УПД_ЗаполнитьЗначениеСведПрослеж(СведПрослеж, ItemTracingInfo);
			
			Если XML_УПД_970_ИспользуетсяФормат5_03() Тогда
				СтТовБезНДСПрослеж = Формат(ItemTracingInfo.SumWithoutVat, "ЧДЦ=2; ЧРД=.; ЧН=0; ЧГ=");
			Иначе
				СтТовБезНДСПрослеж = ItemTracingInfo.SumWithoutVat;
			КонецЕсли;
			
			УстановитьЗначениеXDTO(СведПрослеж, "СтТовБезНДСПрослеж", СтТовБезНДСПрослеж);
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьСведенияОФайле(Файл, ПротоКонтент, ПрефиксИдФайла, ВерсияФормата, ПараметрыФормированияФайла)
	
	ВерсПрог = "Diadoc 1.0";
	
	Разделитель = "_";
	
	ПредставлениеДатыФормирования = Формат(ПараметрыФормированияФайла.ДатаФормирования, "ДФ=yyyyMMdd");
	
	ПризнакПрослеживаемыеТовары = XML_УПД_970_ПризнакНаличияТоваровПодлежащихПрослеживаемости(ПротоКонтент);
	ПризнакМаркируемыеТовары = XML_УПД_970_ПризнакНаличияТоваровПодлежащихМаркировке(ПротоКонтент);
	ПризнакАлкогольнаяПродукция = XML_УПД_970_ПризнакНаличияАлкогольнойПродукции(ПротоКонтент);
	ПризнакТабачнаяПродукция = XML_УПД_970_ПризнакНаличияТабачнойПродукции(ПротоКонтент);
	ПризнакНефтепродукты = XML_УПД_970_ПризнакНаличияНефтепродуктов(ПротоКонтент);
	
	СвободноеЗначение = "00";
	
	ИдентификаторПолучателя	= ПротоКонтент.Buyers[0].FnsParticipantId;
	Если Не ЗначениеЗаполнено(ИдентификаторПолучателя) Тогда
		ПараметрыФормированияФайла.Свойство("ИдентификаторПолучателя", ИдентификаторПолучателя);
	КонецЕсли;
	
	ИдентификаторОтправителя = ПротоКонтент.Sellers[0].FnsParticipantId;
	Если Не ЗначениеЗаполнено(ИдентификаторОтправителя) Тогда
		ПараметрыФормированияФайла.Свойство("ИдентификаторОтправителя", ИдентификаторОтправителя);
	КонецЕсли;

	ЧастиИдФайл = Новый Массив;
	ЧастиИдФайл.Добавить(ПрефиксИдФайла);
	ЧастиИдФайл.Добавить(ИдентификаторПолучателя);
	ЧастиИдФайл.Добавить(ИдентификаторОтправителя);
	ЧастиИдФайл.Добавить(ПредставлениеДатыФормирования);
	ЧастиИдФайл.Добавить(ПараметрыФормированияФайла.УникальныйИдентификатор);
	ЧастиИдФайл.Добавить(ПризнакПрослеживаемыеТовары);
	ЧастиИдФайл.Добавить(ПризнакМаркируемыеТовары);
	ЧастиИдФайл.Добавить(ПризнакАлкогольнаяПродукция);
	ЧастиИдФайл.Добавить(ПризнакТабачнаяПродукция);
	ЧастиИдФайл.Добавить(ПризнакНефтепродукты);
	ЧастиИдФайл.Добавить(СвободноеЗначение);
	
	ИдФайл = Общее_СоединитьСтроку(ЧастиИдФайл, Разделитель);

	УстановитьЗначениеXDTO(Файл, "ИдФайл", ИдФайл);
	УстановитьЗначениеXDTO(Файл, "ВерсФорм", ВерсияФормата);
	УстановитьЗначениеXDTO(Файл, "ВерсПрог", ВерсПрог);
	
КонецПроцедуры

// Возвращает признак наличия в документе товаров, подлежащих прослеживаемости
//
// Параметры:
//  ПротоКонтент - Структура - заполненное прото (см. Заполнить_Utd970SellerContent)
// 
// Возвращаемое значение:
//  Строка - "1", если документ содержит товары, подлежащие прослеживаемости. Иначе - "0"
//
Функция XML_УПД_970_ПризнакНаличияТоваровПодлежащихПрослеживаемости(ПротоКонтент)
	
	ЕстьПрослеживаемыеТовары = Ложь;
	
	ContentsDocumentProductsType = ПолучитьЗначениеXDTO(ПротоКонтент.СлужебнаяИнформация, "ContentsDocumentProductsType"); 
	Если ContentsDocumentProductsType <> Неопределено Тогда
		ЕстьПрослеживаемыеТовары = ContentsDocumentProductsType.ContainsTraceableProducts;
	КонецЕсли;
	
	Если Не ЕстьПрослеживаемыеТовары Тогда
		// Признак могли не заполнить в ПМ (например, при "ручном" заполнении контента)
		Для Каждого Item Из ПротоКонтент.Table.Items Цикл
			Если Item.ItemTracingInfos.Количество() > 0 Тогда
				ЕстьПрослеживаемыеТовары = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Результат = Формат(ЕстьПрослеживаемыеТовары, "БЛ=0; БИ=1");
	
	Возврат Результат;
	
КонецФункции

// Возвращает признак наличия в документе товаров, подлежащих маркировке
//
// Параметры:
//  ПротоКонтент - Структура - заполненное прото (см. Заполнить_Utd970SellerContent)
// 
// Возвращаемое значение:
//  Строка - "1", если документ содержит товары, подлежащие маркировке. Иначе - "0"
//
Функция XML_УПД_970_ПризнакНаличияТоваровПодлежащихМаркировке(ПротоКонтент)
	
	ЕстьМаркируемыеТовары = Ложь;
	
	ContentsDocumentProductsType = ПолучитьЗначениеXDTO(ПротоКонтент.СлужебнаяИнформация, "ContentsDocumentProductsType"); 
	Если ContentsDocumentProductsType <> Неопределено Тогда
		ЕстьМаркируемыеТовары = ContentsDocumentProductsType.ContainsMarkedProducts;
	КонецЕсли;
	
	Если Не ЕстьМаркируемыеТовары Тогда
		// Признак могли не заполнить в ПМ (например, при "ручном" заполнении контента)
		Для Каждого Item Из ПротоКонтент.Table.Items Цикл
			Если Item.ItemIdentificationNumbers.Количество() > 0 Тогда
				ЕстьМаркируемыеТовары = Истина;
				Прервать;
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
	Результат = Формат(ЕстьМаркируемыеТовары, "БЛ=0; БИ=1");
	
	Возврат Результат;
	
КонецФункции

// Возвращает признак наличия в документе алкогольной продукции, подлежащей маркировке
//
// Параметры:
//  ПротоКонтент - Структура - заполненное прото (см. Заполнить_Utd970SellerContent)
// 
// Возвращаемое значение:
//  Строка - "1", если документ содержит товары, подлежащие маркировке. Иначе - "0"
//
Функция XML_УПД_970_ПризнакНаличияАлкогольнойПродукции(ПротоКонтент)
	
	ЕстьАлкогольнаяПродукция = Ложь;
	
	ContentsDocumentProductsType = ПолучитьЗначениеXDTO(ПротоКонтент.СлужебнаяИнформация, "ContentsDocumentProductsType"); 
	Если ContentsDocumentProductsType <> Неопределено Тогда
		ЕстьАлкогольнаяПродукция = ContentsDocumentProductsType.ContainsAlcoholeProducts;
	КонецЕсли;
	
	Результат = Формат(ЕстьАлкогольнаяПродукция, "БЛ=0; БИ=1");
	
	Возврат Результат;
	
КонецФункции

// Возвращает признак наличия в документе табачной продукции, подлежащей маркировке
//
// Параметры:
//  ПротоКонтент - Структура - заполненное прото (см. Заполнить_Utd970SellerContent)
// 
// Возвращаемое значение:
//  Строка - "1", если документ содержит товары, подлежащие маркировке. Иначе - "0"
//
Функция XML_УПД_970_ПризнакНаличияТабачнойПродукции(ПротоКонтент)
	
	ЕстьТабачнаяПродукция = Ложь;
	
	ContentsDocumentProductsType = ПолучитьЗначениеXDTO(ПротоКонтент.СлужебнаяИнформация, "ContentsDocumentProductsType"); 
	Если ContentsDocumentProductsType <> Неопределено Тогда
		ЕстьТабачнаяПродукция = ContentsDocumentProductsType.ContainsTobaccoProducts;
	КонецЕсли;
	
	Результат = Формат(ЕстьТабачнаяПродукция, "БЛ=0; БИ=1");
	
	Возврат Результат;
	
КонецФункции

// Возвращает признак наличия в документе нефтепродуктов, подлежащей маркировке
//
// Параметры:
//  ПротоКонтент - Структура - заполненное прото (см. Заполнить_Utd970SellerContent)
// 
// Возвращаемое значение:
//  Строка - "1", если документ содержит товары, подлежащие маркировке. Иначе - "0"
//
Функция XML_УПД_970_ПризнакНаличияНефтепродуктов(ПротоКонтент)
	
	ЕстьНефтепродукты = Ложь;
	
	ContentsDocumentProductsType = ПолучитьЗначениеXDTO(ПротоКонтент.СлужебнаяИнформация, "ContentsDocumentProductsType"); 
	Если ContentsDocumentProductsType <> Неопределено Тогда
		ЕстьНефтепродукты = ContentsDocumentProductsType.ContainsPetroleumProducts;
	КонецЕсли;
	
	Результат = Формат(ЕстьНефтепродукты, "БЛ=0; БИ=1");
	
	Возврат Результат;
	
КонецФункции

Процедура XML_УПД_970_ЗаполнитьОсновныеСведенияОДокументе(Фабрика, Документ, ПротоКонтент, КНД, ПараметрыФормированияФайла)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "КНД") Тогда
		УстановитьЗначениеXDTO(Документ, "КНД", КНД);
	КонецЕсли;
	
	ДатаФормирования = Дата1С_в_ДатаXML(ПараметрыФормированияФайла.ДатаФормирования);
	ВремяФормирования = Дата_В_Формат(ПараметрыФормированияФайла.ДатаФормирования, "ДФ=HH.mm.ss");
	
	УстановитьЗначениеXDTO(Документ, "ДатаИнфПр", ДатаФормирования);
	УстановитьЗначениеXDTO(Документ, "ВремИнфПр", ВремяФормирования);
	
	УстановитьЗначениеXDTO(Документ, "НаимЭконСубСост", ПротоКонтент.DocumentCreator);
	УстановитьЗначениеXDTO(Документ, "СоглСтрДопИнф", ПротоКонтент.ApprovedStructureAdditionalInfoFields);
	УстановитьЗначениеXDTO(Документ, "УИД", ПротоКонтент.DocumentUID);
	
	НомерДокумента = ПолучитьЗначениеXDTO(ПротоКонтент.DocumentCreatorBase, "Number");
	Если ЗначениеЗаполнено(НомерДокумента) Тогда
		ОснДоверОргСост = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ОснДоверОргСост");	
		XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, ОснДоверОргСост, ПротоКонтент.DocumentCreatorBase);
	КонецЕсли;
	
	XML_УПД_970_ЗаполнитьСведенияОДокументе(Документ, ПротоКонтент);
		
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, ПротоКонтент)
	
	TransferInfo = ПротоКонтент.TransferInfo;
	Фабрика	= XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	// Содержание факта хозяйственной жизни 3 (Обязателен при Функция=СЧФДОП или Функция=ДОП)
	Если ЗначениеЗаполнено(TransferInfo.OperationInfo) Тогда
		
		// Сведения о факте отгрузки товаров
		СвПродПер = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СвПродПер");
		СвПер = НовыйДочернийОбъектXDTO(Фабрика, СвПродПер, "СвПер");
		
		УстановитьЗначениеXDTO(СвПер, "СодОпер", TransferInfo.OperationInfo);
		УстановитьЗначениеXDTO(СвПер, "ВидОпер", TransferInfo.OperationType);
		
		Если ЗначениеЗаполнено(TransferInfo.TransferDate) Тогда
			ДатаОтгрузки = Дата1С_в_ДатаXML(TransferInfo.TransferDate); 
			УстановитьЗначениеXDTO(СвПер, "ДатаПер", ДатаОтгрузки);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(TransferInfo.TransferStartDate) Тогда
			ДатаНачала = Дата1С_в_ДатаXML(TransferInfo.TransferStartDate);
			УстановитьЗначениеXDTO(СвПер, "ДатаНачПер", ДатаНачала);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(TransferInfo.TransferEndDate) Тогда
			ДатаОкончания = Дата1С_в_ДатаXML(TransferInfo.TransferEndDate);
			УстановитьЗначениеXDTO(СвПер, "ДатаОконПер", ДатаОкончания);
		КонецЕсли;
		
		Если TransferInfo.WithoutTransferBase = Истина Тогда
			УстановитьЗначениеXDTO(СвПер, "БезДокОснПер", "1");
		ИначеЕсли ЗначениеЗаполнено(TransferInfo.TransferBases) Тогда
			Для Каждого TransferBase Из TransferInfo.TransferBases Цикл
				ОснПер = НовыйДочернийОбъектXDTO(Фабрика, СвПер, "ОснПер");
				XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, ОснПер, TransferBase);
			КонецЦикла;
		КонецЕсли;
		
		Employee = TransferInfo.Employee;
		OtherIssuer = TransferInfo.OtherIssuer;
		
		Если ЗначениеЗаполнено(Employee.Surname) Тогда
			
			СвЛицПер = НовыйДочернийОбъектXDTO(Фабрика, СвПер, "СвЛицПер");
	
			РабОргПрод = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПер, "РабОргПрод");
			XML_УПД_970_ЗаполнитьРаботникаОрганизации(ИмяПакетаXDTO, РабОргПрод, Employee);
			
		ИначеЕсли ЗначениеЗаполнено(OtherIssuer.Surname) Тогда
			
			СвЛицПер = НовыйДочернийОбъектXDTO(Фабрика, СвПер, "СвЛицПер");

			ИнЛицо = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПер, "ИнЛицо");
			
			Если ЗначениеЗаполнено(OtherIssuer.EmployeePosition) Тогда
				ПредОргПер = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ПредОргПер");
				XML_УПД_970_ЗаполнитьПредставителяОрганизации(ИмяПакетаXDTO, ПредОргПер, TransferInfo.OtherIssuer);
			Иначе
				ФЛПер = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ФЛПер");
				XML_УПД_970_ЗаполнитьФизическоеЛицо(ИмяПакетаXDTO, ФЛПер, TransferInfo.OtherIssuer)
			КонецЕсли;
			
		КонецЕсли;
		
		Если ЗначениеЗаполнено(TransferInfo.TransferTextInfo)
			ИЛИ ЗначениеЗаполнено(TransferInfo.Incoterms)
			ИЛИ ЗначениеЗаполнено(TransferInfo.IncotermsVersion) Тогда
			
			ТранГруз = НовыйДочернийОбъектXDTO(Фабрика, СвПер, "Тран");
			
			УстановитьЗначениеXDTO(ТранГруз, "СвТран", TransferInfo.TransferTextInfo);
			УстановитьЗначениеXDTO(ТранГруз, "Инкотермс", TransferInfo.Incoterms);
			УстановитьЗначениеXDTO(ТранГруз, "ВерИнкотермс", TransferInfo.IncotermsVersion);
			
		КонецЕсли;
		
		XML_УПД_970_ЗаполнитьСведенияОПередачеВещи(Фабрика, СвПер, TransferInfo);
		
		Если ЗначениеЗаполнено(TransferInfo.AdditionalInfoId.InfoFileId)
			ИЛИ ЗначениеЗаполнено(TransferInfo.AdditionalInfoId.StructedAdditionalInfos) Тогда
			
			ИнфПолФХЖ3 = НовыйДочернийОбъектXDTO(Фабрика, СвПродПер, "ИнфПолФХЖ3");
			
			УстановитьЗначениеXDTO(ИнфПолФХЖ3, "ИдФайлИнфПол", TransferInfo.AdditionalInfoId.InfoFileId);
			
			XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, ИнфПолФХЖ3.ТекстИнф, TransferInfo.AdditionalInfoId.StructedAdditionalInfos);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьСведенияОПередачеВещи(Фабрика, Контент, TransferInfo)
	
	CreatedThingTransferDoc = TransferInfo.CreatedThingTransferDoc;
	
	ЕстьДокументОПередачеВещи = (CreatedThingTransferDoc <> Неопределено
		И ЗначениеЗаполнено(CreatedThingTransferDoc.Name)
		И ЗначениеЗаполнено(CreatedThingTransferDoc.Number)
		И ЗначениеЗаполнено(CreatedThingTransferDoc.Date));
		
	ЗаполнитьТэгСвПерВещи = (ЕстьДокументОПередачеВещи
		Или ЗначениеЗаполнено(TransferInfo.CreatedThingTransferDate)
		Или ЗначениеЗаполнено(TransferInfo.CreatedThingInfo));
		
	Если ЗаполнитьТэгСвПерВещи Тогда
		
		СвПерВещи = НовыйДочернийОбъектXDTO(Фабрика, Контент, "СвПерВещи");
		
		УстановитьЗначениеXDTO(СвПерВещи, "СвПерВещ", TransferInfo.CreatedThingInfo);
		
		ДатаПередачи = Дата1С_в_ДатаXML(TransferInfo.CreatedThingTransferDate); 
		УстановитьЗначениеXDTO(СвПерВещи, "ДатаПерВещ", ДатаПередачи);
		
		Если ЕстьДокументОПередачеВещи Тогда
			ДокПерВещ = НовыйДочернийОбъектXDTO(Фабрика, СвПерВещи, "ДокПерВещ");
			XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, ДокПерВещ, CreatedThingTransferDoc);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьРаботникаОрганизации(ИмяПакетаXDTO, ОбъектXDTO, Employee)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ФИО");
	XML_ЗаполнитьФИОТип(ФИО, Employee);
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "Должность", Employee.EmployeePosition);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИныеСвед", Employee.EmployeeInfo);
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьПредставителяОрганизации(ИмяПакетаXDTO, ОбъектXDTO, OtherIssuer)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ФИО");
	XML_ЗаполнитьФИОТип(ФИО, OtherIssuer);
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "Должность", OtherIssuer.EmployeePosition);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИныеСвед", OtherIssuer.EmployeeInfo);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИныеСвед", OtherIssuer.EmployeeInfo);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "НаимОргПер") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "НаимОргПер", OtherIssuer.OrganizationName);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ИННЮЛПер", OtherIssuer.Inn);
		
		XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, ОбъектXDTO.ОснДоверОргПер, OtherIssuer.OrganizationBase);
		
		XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, ОбъектXDTO.ОснПолнПредПер, OtherIssuer.EmployeeBase);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "НаимОргПрин") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "НаимОргПрин", OtherIssuer.OrganizationName);
		
		Если ЗначениеЗаполнено(OtherIssuer.Inn) Тогда
			УстановитьЗначениеXDTO(ОбъектXDTO, "ИННОргПрин", OtherIssuer.Inn);
		Иначе
			
			ОснДоверОргПрин = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ОснДоверОргПрин");
			
			XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, ОснДоверОргПрин, OtherIssuer.OrganizationBase);
			
		КонецЕсли;
		
		Если ЗначениеЗаполнено(OtherIssuer.EmployeeBase.Name) Тогда
			
			ОснПолнПредПрин = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ОснПолнПредПрин");
			
			XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, ОснПолнПредПрин, OtherIssuer.EmployeeBase);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьФизическоеЛицо(ИмяПакетаXDTO, ОбъектXDTO, OtherIssuer)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ФИО");
	XML_ЗаполнитьФИОТип(ФИО, OtherIssuer);
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИныеСвед", OtherIssuer.EmployeeInfo);
	
	Если ЗначениеЗаполнено(OtherIssuer.Inn) Тогда
		УстановитьЗначениеXDTO(ОбъектXDTO, "ИННФЛПрин", OtherIssuer.Inn);
	Иначе
		
		ОснДоверФЛ = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ОснДоверФЛ");
		
		XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, ОснДоверФЛ, OtherIssuer.EmployeeBase);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, РеквДокТип, СведенияОДокументе)
	
	Если ЗначениеЗаполнено(СведенияОДокументе.Date) Тогда
		ДатаДокумента = Дата1С_в_ДатаXML(СведенияОДокументе.Date);
	Иначе
		ДатаДокумента = Неопределено;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(РеквДокТип, "РеквНаимДок", СведенияОДокументе.Name);
	УстановитьЗначениеXDTO(РеквДокТип, "РеквНомерДок", СведенияОДокументе.Number);
	УстановитьЗначениеXDTO(РеквДокТип, "РеквДатаДок", ДатаДокумента);
	УстановитьЗначениеXDTO(РеквДокТип, "РеквИдФайлДок", СведенияОДокументе.FileID);
	УстановитьЗначениеXDTO(РеквДокТип, "РеквИдДок", СведенияОДокументе.ID);
	УстановитьЗначениеXDTO(РеквДокТип, "РИдСистХранД", СведенияОДокументе.StorageID);
	УстановитьЗначениеXDTO(РеквДокТип, "РеквУРЛСистДок", СведенияОДокументе.URL);
	УстановитьЗначениеXDTO(РеквДокТип, "РеквДопСведДок", СведенияОДокументе.AdditionalInfo);
	
	ВидыЮрФизЛиц = Перечисление_ВидыЮрФизЛиц();
	
	Для Каждого EconomicEntity Из СведенияОДокументе.EconomicEntities Цикл
		
		РеквИдРекСост = НовыйДочернийОбъектXDTO(Фабрика, РеквДокТип, "РеквИдРекСост");
		
		Если EconomicEntity.Type = ВидыЮрФизЛиц.ЮридическоеЛицо Тогда
			
			УстановитьЗначениеXDTO(РеквИдРекСост, "ИННЮЛ", EconomicEntity.Inn);
			
		ИначеЕсли EconomicEntity.Type = ВидыЮрФизЛиц.ИндивидуальныйПредприниматель
			Или EconomicEntity.Type = ВидыЮрФизЛиц.ФизическоеЛицо Тогда
			
			УстановитьЗначениеXDTO(РеквИдРекСост, "ИННФЛ", EconomicEntity.Inn);
			
		ИначеЕсли EconomicEntity.Type = ВидыЮрФизЛиц.ОрганИсполнительнойВласти Тогда
			
			УстановитьЗначениеXDTO(РеквИдРекСост, "НаимОИВ", EconomicEntity.Name);
			
		ИначеЕсли EconomicEntity.Type = ВидыЮрФизЛиц.ИностраннаяОрганизация
			Или EconomicEntity.Type = ВидыЮрФизЛиц.ИностранныйГражданин Тогда
			
			ДаннИно = НовыйДочернийОбъектXDTO(Фабрика, РеквИдРекСост, "ДаннИно");
			
			XML_УПД_970_ЗаполнитьИдентификаторСтатусаИностранногоЮрФизЛица(EconomicEntity.Type, ДаннИно);
			
			УстановитьЗначениеXDTO(ДаннИно, "КодСтр", EconomicEntity.CountryCode);
			УстановитьЗначениеXDTO(ДаннИно, "НаимСтран", EconomicEntity.Country);
			УстановитьЗначениеXDTO(ДаннИно, "Наим", EconomicEntity.Name);
			УстановитьЗначениеXDTO(ДаннИно, "Идентиф", EconomicEntity.LegalEntityID);
			УстановитьЗначениеXDTO(ДаннИно, "ИныеСвед", EconomicEntity.OtherInfo);
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьИдентификаторСтатусаИностранногоЮрФизЛица(ТипЮрФизЛица, ОбъектXDTO)

	ВидыЮрФизЛиц = Перечисление_ВидыЮрФизЛиц();
	
	Если ТипЮрФизЛица = ВидыЮрФизЛиц.ИностраннаяОрганизация Тогда
		ИдСтат = "ИО";
	Иначе
		ИдСтат = "ИГ";
	КонецЕсли;
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИдСтат", ИдСтат);
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьСведенияОИностраннойОрганизации(ДаннИно, ПротоКонтент)
	
	XML_УПД_970_ЗаполнитьИдентификаторСтатусаИностранногоЮрФизЛица(ПротоКонтент.Type, ДаннИно);
	
	УстановитьЗначениеXDTO(ДаннИно, "Наим", ПротоКонтент.Name);
	УстановитьЗначениеXDTO(ДаннИно, "ИныеСвед", ПротоКонтент.OrganizationOrPersonInfo);
	УстановитьЗначениеXDTO(ДаннИно, "Идентиф", ПротоКонтент.LegalEntityId);
	УстановитьЗначениеXDTO(ДаннИно, "КодСтр", ПротоКонтент.CountryCode);
	УстановитьЗначениеXDTO(ДаннИно, "НаимСтран", ПротоКонтент.Country);

КонецПроцедуры

Функция XML_ТипПодписи(ТипПодписиПрото)

	ТипыПодписей = Перечисление_ТипыПодписей();
	
	Если ТипПодписиПрото = ТипыПодписей.Квалифицированная Тогда
		Результат = "1";	
	ИначеЕсли ТипПодписиПрото = ТипыПодписей.Простая Тогда
		Результат = "2";
	Иначе
		Результат = "3";
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура XML_ОтветныйТитул_УПД_970_ЗаполнитьОсновныеСведенияФайла(Файл, ТитулПродавца, ПараметрыФормированияФайла)
	
	ВерсПрог = "Diadoc 1.0";
	ВерсияФормата = ТитулПродавца.ВерсФорм;
	
	ИдФайлПокупателя = XML_ОтветныйТитул_УПД_970_НовыйИдФайл(ТитулПродавца.ИдФайл, ПараметрыФормированияФайла);
	
	УстановитьЗначениеXDTO(Файл, "ИдФайл", ИдФайлПокупателя);
	УстановитьЗначениеXDTO(Файл, "ВерсПрог", ВерсПрог);
	УстановитьЗначениеXDTO(Файл, "ВерсФорм", ВерсияФормата);
	
КонецПроцедуры

Процедура XML_ОтветныйТитул_УПД_970_ЗаполнитьОсновныеДокумента(ДокументXDTO, ПротоКонтент, ТитулПродавца, ПараметрыФормированияФайла)
	
	Если ТитулПродавца.ВерсФорм = "5.03" Тогда
		ИмяПакетаXDTO = XML_ОтветныйТитул_УПД_970_ИмяПакета_5_03();
	Иначе
		ИмяПакетаXDTO = XML_ОтветныйТитул_УПД_970_ИмяПакета_5_02();
	КонецЕсли;
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	НалоговыйКод = XML_ОтветныйТитул_НалоговыйКод();
	УстановитьЗначениеXDTO(ДокументXDTO, "КНД", НалоговыйКод);
	
	УИД = ПолучитьЗначениеXDTO(ТитулПродавца.Документ, "УИД");
	УстановитьЗначениеXDTO(ДокументXDTO, "УИД", УИД);
	
	ДатаФормирования = Дата_В_Формат(ПараметрыФормированияФайла.ДатаФормирования, "ДФ=dd.MM.yyyy");
	ВремяФормирования = Дата_В_Формат(ПараметрыФормированияФайла.ДатаФормирования, "ДФ=HH.mm.ss");
	
	УстановитьЗначениеXDTO(ДокументXDTO, "ДатаИнфПок", ДатаФормирования);
	УстановитьЗначениеXDTO(ДокументXDTO, "ВремИнфПок", ВремяФормирования);
	
	Если ЗначениеЗаполнено(ПротоКонтент.DocumentCreator) Тогда
		УстановитьЗначениеXDTO(ДокументXDTO, "НаимЭконСубСост", ПротоКонтент.DocumentCreator);
	КонецЕсли;
	
	НомерДокумента = ПолучитьЗначениеXDTO(ПротоКонтент.DocumentCreatorBase, "Number");
	Если ЗначениеЗаполнено(НомерДокумента) Тогда
		ОснДоверОргСост = НовыйДочернийОбъектXDTO(Фабрика, ДокументXDTO, "ОснДоверОргСост");
		XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, ОснДоверОргСост, ПротоКонтент.DocumentCreatorBase);
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ОтветныйТитул_УПД_970_ЗаполнитьСведенияПродавца(ИмяПакетаXDTO, ИдИнфПрод, ТитулПродавца, ФайлыПодписейПродавца)
	
    ИмяФайлаПродавца = ТитулПродавца.ИдФайл;
	ДатаФайлаПродавца = ТитулПродавца.Документ.ДатаИнфПр;
	ВремяФайлаПродавца = ТитулПродавца.Документ.ВремИнфПр;

	XML_ОтветныйТитул_ЗаполнитьСведенияПродавца(ИмяПакетаXDTO, ИдИнфПрод
		, ИмяФайлаПродавца, ДатаФайлаПродавца, ВремяФайлаПродавца, ФайлыПодписейПродавца);

КонецПроцедуры

Процедура XML_ОтветныйТитул_УПД_970_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, СодФХЖ4, ПротоКонтент, ТитулПродавца)

	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ДатаДокумента = ТитулПродавца.Документ.СвСчФакт.ДатаДок;
	НомерДокумента = ТитулПродавца.Документ.СвСчФакт.НомерДок;
	
	НаименованиеДокумента = ТитулПродавца.Документ.НаимДокОпр;
	ФункцияДокумента = ТитулПродавца.Документ.Функция;
	
	УстановитьЗначениеXDTO(СодФХЖ4, "Функция", ФункцияДокумента);
	УстановитьЗначениеXDTO(СодФХЖ4, "ВидОпер", ПротоКонтент.OperationCode);
	УстановитьЗначениеXDTO(СодФХЖ4, "НаимДокОпрПр", НаименованиеДокумента);
	УстановитьЗначениеXDTO(СодФХЖ4, "ДатаДокИнфПр", Дата1С_в_ДатаXML(ДатаДокумента));
	УстановитьЗначениеXDTO(СодФХЖ4, "ПорНомДокИнфПр", НомерДокумента);
	
	СвПрин = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ4, "СвПрин");
	УстановитьЗначениеXDTO(СвПрин, "ДатаПрин",	Дата1С_в_ДатаXML(ПротоКонтент.AcceptanceDate));
	
	КодСодержанияОперации = XML_ОтветныйТитул_УПД_970_ЗначениеКодаСодержанияОперации(ПротоКонтент);
	
	Если ЗначениеЗаполнено(КодСодержанияОперации) Тогда
		
		КодСодОпер = НовыйДочернийОбъектXDTO(Фабрика, СвПрин, "КодСодОпер");
		УстановитьЗначениеXDTO(КодСодОпер, "КодИтога", КодСодержанияОперации);
		
		Если НЕ XML_ОтветныйТитул_РезультатПриемкиПринятоБезРазногласий(КодСодержанияОперации) Тогда
			XML_ОтветныйТитул_УПД_970_ЗаполнитьРеквизитыДокументаОРазногласиях(ПротоКонтент, КодСодОпер, Фабрика);
		КонецЕсли;
		
	КонецЕсли;
	
	OperationContent = ПолучитьЗначениеXDTO(ПротоКонтент, "OperationContent");
	Если ЗначениеЗаполнено(OperationContent)
		И НЕ XML_ОтветныйТитул_РезультатПриемкиНеПринято(КодСодержанияОперации) Тогда
		УстановитьЗначениеXDTO(СвПрин, "СодОпер", OperationContent);
	КонецЕсли;

	ЕстьСведенияОРаботнике = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "Employee")
		И ЗначениеЗаполнено(ПротоКонтент.Employee.Surname);
		
	ЕстьСведенияОбИномЛице = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "OtherIssuer")
		И ЗначениеЗаполнено(ПротоКонтент.OtherIssuer.Surname);
	
	Если ЕстьСведенияОРаботнике Или ЕстьСведенияОбИномЛице Тогда
		
		СвЛицПрин = НовыйДочернийОбъектXDTO(Фабрика, СвПрин, "СвЛицПрин");
		
		Если ЕстьСведенияОРаботнике Тогда
			
			РабОргПок = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПрин, "РабОргПок");
			XML_УПД_970_ЗаполнитьРаботникаОрганизации(ИмяПакетаXDTO, РабОргПок, ПротоКонтент.Employee);
			
		ИначеЕсли ЕстьСведенияОбИномЛице Тогда
			
			ИнЛицо = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПрин, "ИнЛицо");
			
			Если ЗначениеЗаполнено(ПротоКонтент.OtherIssuer.EmployeePosition) Тогда
				
				ПредОргПрин = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ПредОргПрин");
				XML_УПД_970_ЗаполнитьПредставителяОрганизации(ИмяПакетаXDTO, ПредОргПрин, ПротоКонтент.OtherIssuer);
				
			Иначе
				
				ФЛПрин = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ФЛПрин");
				XML_УПД_970_ЗаполнитьФизическоеЛицо(ИмяПакетаXDTO, ФЛПрин, ПротоКонтент.OtherIssuer)
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	XML_ЗаполнитьИнфоПоляФХЖ(ИмяПакетаXDTO, СодФХЖ4, ПротоКонтент.AdditionalInfoId, "ИнфПолФХЖ4", "Файл.Документ.СодФХЖ4.ИнфПолФХЖ4.ТекстИнф");
	
КонецПроцедуры

Функция XML_ОтветныйТитул_УПД_970_НовыйИдФайл(ИдФайлПродавца, ПараметрыФормированияФайла)
	
	ПрефиксИдФайла = "ON_NSCHFDOPPOK";
	
	ПредставлениеДатыФормирования = Формат(ПараметрыФормированияФайла.ДатаФормирования, "ДФ=yyyyMMdd");
	
	СведенияИдФайлПродавца = XML_СведенияИдФайлУПД970(ИдФайлПродавца);
	
	ЧастиИдФайл = Новый Массив;
	ЧастиИдФайл.Добавить(ПрефиксИдФайла);
	ЧастиИдФайл.Добавить(СведенияИдФайлПродавца.ИдОтправителя);
	ЧастиИдФайл.Добавить(СведенияИдФайлПродавца.ИдПолучателя);
	ЧастиИдФайл.Добавить(ПредставлениеДатыФормирования);
	ЧастиИдФайл.Добавить(ПараметрыФормированияФайла.УникальныйИдентификатор);
	ЧастиИдФайл.Добавить(СведенияИдФайлПродавца.ПризнакЕстьПрослежка);
	ЧастиИдФайл.Добавить(СведенияИдФайлПродавца.ПризнакЕстьМаркировка);
	ЧастиИдФайл.Добавить(СведенияИдФайлПродавца.ПризнакЕстьАлкоголь);
	ЧастиИдФайл.Добавить(СведенияИдФайлПродавца.ПризнакЕстьТабак);
	ЧастиИдФайл.Добавить(СведенияИдФайлПродавца.ПризнакЕстьНефтепродукты);
	ЧастиИдФайл.Добавить(СведенияИдФайлПродавца.СвободноеЗначение);
	
	Разделитель = "_";
	Результат = Общее_СоединитьСтроку(ЧастиИдФайл, Разделитель);
	
	Возврат Результат;
	
КонецФункции

Процедура XML_ОтветныйТитул_УПД_970_ЗаполнитьРеквизитыДокументаОРазногласиях(ПротоКонтент, КодСодОпер, Фабрика)
	
	DiscrepDocument = ПолучитьЗначениеXDTO(ПротоКонтент.ContentOperCode, "DiscrepDocument");
	Если DiscrepDocument = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ЗначениеЗаполнено(DiscrepDocument.Name)
		И ЗначениеЗаполнено(DiscrepDocument.Name)
		И ЗначениеЗаполнено(DiscrepDocument.Date) Тогда
		
		РеквДокРасх = НовыйДочернийОбъектXDTO(Фабрика, КодСодОпер, "РеквДокРасх");
		XML_УПД_970_ЗаполнитьРеквДокТип(Фабрика, РеквДокРасх, DiscrepDocument);
		
	КонецЕсли;
	
КонецПроцедуры

Функция XML_ОтветныйТитул_УПД_970_ЗначениеКодаСодержанияОперации(ПротоКонтент)
	
	Результат = "";
	
	ContentOperCode = ПолучитьЗначениеXDTO(ПротоКонтент, "ContentOperCode");
	Если ContentOperCode <> Неопределено Тогда
		Результат = ContentOperCode.TotalCode;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция XML_ОтветныйТитул_УПД_970_ИмяПакета_5_02()
	
	Возврат "ON_NSCHFDOPPOK_1_997_02_05_02_01";
	
КонецФункции

Функция XML_ОтветныйТитул_УПД_970_ИмяПакета_5_03()
	
	Возврат "ON_NSCHFDOPPOK_1_997_02_05_03_01";
	
КонецФункции

Функция XML_ОтветныйТитул_НалоговыйКод()

	Возврат "1115132";
	
КонецФункции

Функция XML_ОсновнойТитул_НалоговыйКод()

	Возврат "1115131";
	
КонецФункции

Функция XML_ОтветныйТитул_РезультатПриемкиНеПринято(КодСодержанияОперации)
	
	Результат = (КодСодержанияОперации = "3");
	Возврат Результат;
	
КонецФункции

Функция XML_ОтветныйТитул_РезультатПриемкиПринятоБезРазногласий(КодСодержанияОперации)

	Результат = (КодСодержанияОперации = "1");
	Возврат Результат;
	
КонецФункции

Функция XML_СведенияИдФайлУПД970(ИдФайл)
	
	Разделитель = "_";
	ЧастиИдФайл = РазложитьСтрокуВМассивСлов(ИдФайл, Разделитель);
	
	Если ЧастиИдФайл.Количество() <> 12 Тогда
		ВызватьИсключение "Неверный формат значения ИдФайл. По формату значение должно содержать 12 элементов, разделенных знаком ""_""";	
	КонецЕсли;
	
	Префикс = ЧастиИдФайл[0] + Разделитель + ЧастиИдФайл[1]; 
	
	Результат = Новый Структура;
	Результат.Вставить("Префикс", Префикс);
	Результат.Вставить("ИдПолучателя", ЧастиИдФайл[2]);
	Результат.Вставить("ИдОтправителя", ЧастиИдФайл[3]);
	Результат.Вставить("ДатаФормирования", ЧастиИдФайл[4]);
	Результат.Вставить("УникальныйНомерФайла", ЧастиИдФайл[5]);
	Результат.Вставить("ПризнакЕстьПрослежка", ЧастиИдФайл[6]);
	Результат.Вставить("ПризнакЕстьМаркировка", ЧастиИдФайл[7]);
	Результат.Вставить("ПризнакЕстьАлкоголь", ЧастиИдФайл[8]);
	Результат.Вставить("ПризнакЕстьТабак", ЧастиИдФайл[9]);
	Результат.Вставить("ПризнакЕстьНефтепродукты", ЧастиИдФайл[10]);
	Результат.Вставить("СвободноеЗначение", ЧастиИдФайл[11]);

	Возврат Результат;

КонецФункции

// XML: функции заполнения УКД

Функция XML_УКД_ПолучитьФункцию(ContentFunction)
	
	СоответствиеФункций = Новый Соответствие;
	
	ФункцииУКД = Перечисление_ФункцииУКД();
	
	СоответствиеФункций.Вставить("InvoiceAndBasic", ФункцииУКД.КСЧФДИС);
	СоответствиеФункций.Вставить("Invoice", 		ФункцииУКД.КСЧФ);
	СоответствиеФункций.Вставить("Basic", 			ФункцииУКД.ДИС);
	
	ФункцияДокумента = СоответствиеФункций.Получить(ContentFunction);
	
	Если ФункцияДокумента = Неопределено Тогда
		ФункцияДокумента = ContentFunction;
	КонецЕсли;
	
	Возврат ФункцияДокумента;

КонецФункции

Функция Перечисление_ФункцииУКД()
	
	Результат = Новый Структура;
	
	Результат.Вставить("КСЧФДИС", 	"КСЧФДИС");
	Результат.Вставить("КСЧФ", 		"КСЧФ");
	Результат.Вставить("ДИС", 		"ДИС");
	
	Возврат Результат;

КонецФункции

Процедура XML_УКД_ЗаполнитьСведенияСФ(ИмяПакетаXDTO, Документ, Content)

	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	// Сведения о корректировочном счете-фактуре
	СвКСчФ = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвКСчФ", ИмяПакетаXDTO);
	УстановитьЗначениеXDTO(СвКСчФ, "НомерКСчФ", Content.Number);
	УстановитьЗначениеXDTO(СвКСчФ, "ДатаКСчФ", Дата_В_Формат(Content.Date, ФорматДаты));
	УстановитьЗначениеXDTO(СвКСчФ, "КодОКВ", Content.Currency);
	
	// Корректируемые СФ
	
	Для Каждого InvoiceForCorrectionInfo Из Content.Invoices Цикл
		
		СчФ = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвКСчФ.СчФ", ИмяПакетаXDTO);
		УстановитьЗначениеXDTO(СчФ, "НомерСчФ", InvoiceForCorrectionInfo.InvoiceNumber);
		УстановитьЗначениеXDTO(СчФ, "ДатаСчФ", Дата_В_Формат(InvoiceForCorrectionInfo.InvoiceDate, ФорматДаты));
		
		Для Каждого InvoiceRevisionInfo Из InvoiceForCorrectionInfo.InvoiceRevisions Цикл
			
			ИспрСчФ = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвКСчФ.СчФ.ИспрСчФ", ИмяПакетаXDTO);
			УстановитьЗначениеXDTO(ИспрСчФ, "НомИспрСчФ", InvoiceRevisionInfo.RevisionNumber);
			УстановитьЗначениеXDTO(ИспрСчФ, "ДатаИспрСчФ", Дата_В_Формат(InvoiceRevisionInfo.RevisionDate, ФорматДаты));
			СчФ.ИспрСчФ.Добавить(ИспрСчФ);
			
		КонецЦикла;		
		
		СвКСчФ.СчФ.Добавить(СчФ);
		
	КонецЦикла;
	
	// Исправление УКД
	
	Если ЗначениеЗаполнено(Content.RevisionNumber) И ЗначениеЗаполнено(Content.RevisionDate) Тогда
		
		ИспрКСчФ = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвКСчФ.ИспрКСчФ", ИмяПакетаXDTO);		
		УстановитьЗначениеXDTO(ИспрКСчФ, "НомИспрКСчФ", Content.RevisionNumber);
		УстановитьЗначениеXDTO(ИспрКСчФ, "ДатаИспрКСчФ", Дата_В_Формат(Content.RevisionDate, ФорматДаты));		
		УстановитьЗначениеXDTO(СвКСчФ, "ИспрКСчФ", ИспрКСчФ);
		
	КонецЕсли;
	
	// Продавец
	СвПрод = XML_ПолучитьПустойОбъектXDTO("СвПродПокТип", ИмяПакетаXDTO);	
	XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, СвПрод, Content.Seller);
	УстановитьЗначениеXDTO(СвКСчФ, "СвПрод", СвПрод);
	
	// Покупатель
	СвПокуп = XML_ПолучитьПустойОбъектXDTO("СвПродПокТип", ИмяПакетаXDTO);
	XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, СвПокуп, Content.Buyer);
	УстановитьЗначениеXDTO(СвКСчФ, "СвПокуп", СвПокуп);
	
	// Дополнительные сведения об участниках факта хозяйственной жизни, основаниях и обстоятельствах его проведения
	ДопСвФХЖ1 = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвКСчФ.ДопСвФХЖ1", ИмяПакетаXDTO);
	УстановитьЗначениеXDTO(ДопСвФХЖ1, "ИдГосКон", Content.GovernmentContractInfo);
	УстановитьЗначениеXDTO(ДопСвФХЖ1, "НаимОКВ", НаименованиеВалюты(Content.Currency, Истина));
	УстановитьЗначениеXDTO(ДопСвФХЖ1, "КурсВал", Content.CurrencyRate);
	УстановитьЗначениеXDTO(СвКСчФ, "ДопСвФХЖ1", ДопСвФХЖ1);
	
	// Информационное поле - 1
	Если ЗначениеЗаполнено(Content.AdditionalInfoId.InfoFileId) ИЛИ Content.AdditionalInfoId.StructedAdditionalInfos.Количество() > 0 Тогда
		
		ИнфПолФХЖ1 = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвКСчФ.ИнфПолФХЖ1", ИмяПакетаXDTO);
		УстановитьЗначениеXDTO(ИнфПолФХЖ1, "ИдФайлИнфПол", Content.AdditionalInfoId.InfoFileId);
		XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, ИнфПолФХЖ1.ТекстИнф, Content.AdditionalInfoId.StructedAdditionalInfos);
		УстановитьЗначениеXDTO(СвКСчФ, "ИнфПолФХЖ1", ИнфПолФХЖ1);
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Документ, "СвКСчФ", СвКСчФ);
	
КонецПроцедуры

Процедура XML_УКД_ЗаполнитьСведенияТаблицыСФ(ИмяПакетаXDTO, Документ, Content, ПараметрыФормированияФайла)
	
	// Таблица счета-фактуры
	Если Content.InvoiceCorrectionTable.Items.Количество() > 0 Тогда
		
		ТаблКСчФ = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблКСчФ", ИмяПакетаXDTO);
		
		ВсеСтрокиБезНДСДо = Истина;
		ВсеСтрокиБезНДСПосле = Истина;
		НомерСтроки = 1;
		
		Для Каждого ExtendedInvoiceCorrectionItem Из Content.InvoiceCorrectionTable.Items Цикл
						
			СведТов = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблКСчФ.СведТов", ИмяПакетаXDTO);
			УстановитьЗначениеXDTO(СведТов, "НомСтр", НомерСтроки);			
			XML_УКД_ЗаполнитьСтрокуТабЧасти(ИмяПакетаXDTO, СведТов, ExtendedInvoiceCorrectionItem, ВсеСтрокиБезНДСДо, ВсеСтрокиБезНДСПосле, ПараметрыФормированияФайла);
			
			ТаблКСчФ.СведТов.Добавить(СведТов);
			НомерСтроки = НомерСтроки + 1;
			
		КонецЦикла;
		
		// Всего увеличение
		Если Content.InvoiceCorrectionTable.TotalsInc.Total <> Неопределено Тогда
			
			ВсегоУвел = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблКСчФ.ВсегоУвел", ИмяПакетаXDTO);
			
			УстановитьЗначениеXDTO(ВсегоУвел, "СтТовБезНДСВсего", 	XML_ФорматЧисла(Content.InvoiceCorrectionTable.TotalsInc.TotalWithVatExcluded, 2));
			УстановитьЗначениеXDTO(ВсегоУвел, "СтТовУчНалВсего", 	XML_ФорматЧисла(Content.InvoiceCorrectionTable.TotalsInc.Total, 2));
			
			СумНал = XML_ПолучитьПустойОбъектXDTO("СумНДСТип", ИмяПакетаXDTO);
			
			Если ВсеСтрокиБезНДСДо Тогда
				УстановитьЗначениеXDTO(СумНал, "БезНДС", СтрокаБезНДС());
			Иначе			
				УстановитьЗначениеXDTO(СумНал, "СумНДС", XML_ФорматЧисла(Content.InvoiceCorrectionTable.TotalsInc.Vat, 2));
			КонецЕсли;
			
			УстановитьЗначениеXDTO(ВсегоУвел, "СумНал", СумНал);		
			УстановитьЗначениеXDTO(ТаблКСчФ, "ВсегоУвел", ВсегоУвел);
			
		КонецЕсли;
		
		// Всего уменьшение
		Если Content.InvoiceCorrectionTable.TotalsDec.Total <> Неопределено Тогда
			
			ВсегоУм = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблКСчФ.ВсегоУм", ИмяПакетаXDTO);
			
			УстановитьЗначениеXDTO(ВсегоУм, "СтТовБезНДСВсего", XML_ФорматЧисла(Content.InvoiceCorrectionTable.TotalsDec.TotalWithVatExcluded, 2));
			УстановитьЗначениеXDTO(ВсегоУм, "СтТовУчНалВсего", 	XML_ФорматЧисла(Content.InvoiceCorrectionTable.TotalsDec.Total, 2));
			
			СумНал = XML_ПолучитьПустойОбъектXDTO("СумНДСТип", ИмяПакетаXDTO);
			
			Если ВсеСтрокиБезНДСДо Тогда
				УстановитьЗначениеXDTO(СумНал, "БезНДС", СтрокаБезНДС());
			Иначе			
				УстановитьЗначениеXDTO(СумНал, "СумНДС", XML_ФорматЧисла(Content.InvoiceCorrectionTable.TotalsDec.Vat, 2));
			КонецЕсли;
			
			УстановитьЗначениеXDTO(ВсегоУм, "СумНал", СумНал);		
			УстановитьЗначениеXDTO(ТаблКСчФ, "ВсегоУм", ВсегоУм);
			
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Документ, "ТаблКСчФ", ТаблКСчФ);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УКД_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, Content)		

	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	// Информационное поле - 3 (сведения о факте согласования)
	СодФХЖ3 = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СодФХЖ3", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(СодФХЖ3, "ИныеСвИзмСтоим",	Content.EventContent.CostChangeInfo);
	УстановитьЗначениеXDTO(СодФХЖ3, "ПередатДокум", 	Content.EventContent.TransferDocDetails );
	УстановитьЗначениеXDTO(СодФХЖ3, "СодОпер", 			Content.EventContent.OperationContent);
	УстановитьЗначениеXDTO(СодФХЖ3, "ДатаНапр", 		Дата_В_Формат(Content.EventContent.NotificationDate, ФорматДаты));
	
	// Основание корректировки
	Для Каждого CorrectionBase Из Content.EventContent.CorrectionBases Цикл
		
		ОснКор = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СодФХЖ3.ОснКор", ИмяПакетаXDTO);
		
		УстановитьЗначениеXDTO(ОснКор, "НаимОсн",	CorrectionBase.BaseDocumentName);
		УстановитьЗначениеXDTO(ОснКор, "НомОсн",	CorrectionBase.BaseDocumentNumber);
		УстановитьЗначениеXDTO(ОснКор, "ДатаОсн",	Дата_В_Формат(CorrectionBase.BaseDocumentDate, ФорматДаты));
		УстановитьЗначениеXDTO(ОснКор, "ДопСвОсн",	CorrectionBase.AdditionalInfo);
		
		СодФХЖ3.ОснКор.Добавить(ОснКор);
	
	КонецЦикла;
		
	УстановитьЗначениеXDTO(Документ, "СодФХЖ3", СодФХЖ3);
	
КонецПроцедуры

Процедура XML_УКД_ЗаполнитьСтрокуТабЧасти(ИмяПакетаXDTO, СведТов, ExtendedInvoiceCorrectionItem, ВсеСтрокиБезНДСДо, ВсеСтрокиБезНДСПосле, ПараметрыФормированияФайла)

	УстановитьЗначениеXDTO(СведТов, "НаимТов", 		ExtendedInvoiceCorrectionItem.Product);			
	УстановитьЗначениеXDTO(СведТов, "ОКЕИ_ТовДо", 	ExtendedInvoiceCorrectionItem.OriginalValues.UnitCode);
	УстановитьЗначениеXDTO(СведТов, "ОКЕИ_ТовПосле", ExtendedInvoiceCorrectionItem.CorrectedValues.UnitCode);
	
	УстановитьЗначениеXDTO(СведТов, "КолТовДо", 	XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.OriginalValues.Quantity,	ПараметрыФормированияФайла.ТочностьКоличества));
	УстановитьЗначениеXDTO(СведТов, "КолТовПосле", 	XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.CorrectedValues.Quantity,	ПараметрыФормированияФайла.ТочностьКоличества));
	УстановитьЗначениеXDTO(СведТов, "ЦенаТовДо", 	XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.OriginalValues.Price,		ПараметрыФормированияФайла.ТочностьЦены));
	УстановитьЗначениеXDTO(СведТов, "ЦенаТовПосле", XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.CorrectedValues.Price,	ПараметрыФормированияФайла.ТочностьЦены));
	
	УстановитьЗначениеXDTO(СведТов, "НалСтДо", 		НалСт_Из_TaxRate(ExtendedInvoiceCorrectionItem.OriginalValues.TaxRate));
	УстановитьЗначениеXDTO(СведТов, "НалСтПосле", 	НалСт_Из_TaxRate(ExtendedInvoiceCorrectionItem.CorrectedValues.TaxRate));
	
	// - Стоимость без учета НДС
	
	СтТовБезНДС = XML_ПолучитьПустойОбъектXDTO("СтоимТип", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(СтТовБезНДС, "СтоимДоИзм", 		XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.OriginalValues.TotalWithVatExcluded, 2));
	УстановитьЗначениеXDTO(СтТовБезНДС, "СтоимПослеИзм",	XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.CorrectedValues.TotalWithVatExcluded, 2));
	УстановитьЗначениеXDTO(СтТовБезНДС, "СтоимУвел", 		XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.AmountsInc.TotalWithVatExcluded, 2));
	УстановитьЗначениеXDTO(СтТовБезНДС, "СтоимУм", 			XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.AmountsDec.TotalWithVatExcluded, 2));
	
	УстановитьЗначениеXDTO(СведТов, "СтТовБезНДС", СтТовБезНДС);
	
	// - Акциз
	
	АкцизДо = XML_ПолучитьПустойОбъектXDTO("СумАкцизТип", ИмяПакетаXDTO);
	
	Если НЕ ЗначениеЗаполнено(ExtendedInvoiceCorrectionItem.OriginalValues.Excise) ИЛИ Число(ExtendedInvoiceCorrectionItem.OriginalValues.Excise) = 0 Тогда
		УстановитьЗначениеXDTO(АкцизДо, "БезАкциз", "без акциза");
	Иначе
		УстановитьЗначениеXDTO(АкцизДо, "СумАкциз", XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.OriginalValues.Excise, 2));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "АкцизДо", АкцизДо);
	
	АкцизПосле = XML_ПолучитьПустойОбъектXDTO("СумАкцизТип", ИмяПакетаXDTO);
	
	Если НЕ ЗначениеЗаполнено(ExtendedInvoiceCorrectionItem.CorrectedValues.Excise) ИЛИ Число(ExtendedInvoiceCorrectionItem.CorrectedValues.Excise) = 0 Тогда
		УстановитьЗначениеXDTO(АкцизПосле, "БезАкциз", "без акциза");
	Иначе
		УстановитьЗначениеXDTO(АкцизПосле, "СумАкциз", XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.CorrectedValues.Excise, 2));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "АкцизПосле", АкцизПосле);
	
	АкцизРазн = XML_ПолучитьПустойОбъектXDTO("РазнСумНалТип", ИмяПакетаXDTO);
	
	Если НЕ ЗначениеЗаполнено(ExtendedInvoiceCorrectionItem.AmountsInc.Excise) 
		И НЕ ЗначениеЗаполнено(ExtendedInvoiceCorrectionItem.AmountsDec.Excise) Тогда
		УстановитьЗначениеXDTO(АкцизРазн, "СумУвел",	XML_ФорматЧисла(0, 2));
		
	ИначеЕсли ЗначениеЗаполнено(ExtendedInvoiceCorrectionItem.AmountsInc.Excise) Тогда
		УстановитьЗначениеXDTO(АкцизРазн, "СумУвел",	XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.AmountsInc.Excise, 2));
		
	ИначеЕсли ЗначениеЗаполнено(ExtendedInvoiceCorrectionItem.AmountsDec.Excise) Тогда
		УстановитьЗначениеXDTO(АкцизРазн, "СумУм",		XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.AmountsDec.Excise, 2));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "АкцизРазн", АкцизРазн);
	
	// - Сумма НДС
	
	СумНалДо = XML_ПолучитьПустойОбъектXDTO("СумНДСТип", ИмяПакетаXDTO);
	
	Если TaxRate_ЭтоЗначениеБезНДС(ExtendedInvoiceCorrectionItem.OriginalValues.TaxRate) Тогда
		УстановитьЗначениеXDTO(СумНалДо, "БезНДС", СтрокаБезНДС());
	Иначе
		ВсеСтрокиБезНДСДо = Ложь;
		УстановитьЗначениеXDTO(СумНалДо, "СумНДС", XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.OriginalValues.Vat, 2));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "СумНалДо", СумНалДо);
	
	
	СумНалПосле = XML_ПолучитьПустойОбъектXDTO("СумНДСТип", ИмяПакетаXDTO);
	
	Если TaxRate_ЭтоЗначениеБезНДС(ExtendedInvoiceCorrectionItem.CorrectedValues.TaxRate) Тогда
		УстановитьЗначениеXDTO(СумНалПосле, "БезНДС", СтрокаБезНДС());
	Иначе
		ВсеСтрокиБезНДСПосле = Ложь;
		УстановитьЗначениеXDTO(СумНалПосле, "СумНДС", XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.CorrectedValues.Vat, 2));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "СумНалПосле", СумНалПосле);
	
	СумНалРазн = XML_ПолучитьПустойОбъектXDTO("РазнСумНалТип", ИмяПакетаXDTO);
	
	Если НЕ ЗначениеЗаполнено(ExtendedInvoiceCorrectionItem.AmountsInc.Vat) 
		И НЕ ЗначениеЗаполнено(ExtendedInvoiceCorrectionItem.AmountsDec.Vat) Тогда
		УстановитьЗначениеXDTO(СумНалРазн, "СумУвел",	XML_ФорматЧисла(0, 2));
		
	ИначеЕсли ЗначениеЗаполнено(ExtendedInvoiceCorrectionItem.AmountsInc.Vat) Тогда
		УстановитьЗначениеXDTO(СумНалРазн, "СумУвел",	XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.AmountsInc.Vat, 2));
		
	ИначеЕсли ЗначениеЗаполнено(ExtendedInvoiceCorrectionItem.AmountsDec.Vat) Тогда
		УстановитьЗначениеXDTO(СумНалРазн, "СумУм", 	XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.AmountsDec.Vat, 2));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "СумНалРазн", СумНалРазн);
	
	// - Стоимость с учетом НДС
	
	СтТовУчНал = XML_ПолучитьПустойОбъектXDTO("СтоимТип", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(СтТовУчНал, "СтоимДоИзм", 	XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.OriginalValues.Total, 2));
	УстановитьЗначениеXDTO(СтТовУчНал, "СтоимПослеИзм",	XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.CorrectedValues.Total, 2));
	УстановитьЗначениеXDTO(СтТовУчНал, "СтоимУвел", 	XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.AmountsInc.Total, 2));
	УстановитьЗначениеXDTO(СтТовУчНал, "СтоимУм", 		XML_ФорматЧисла(ExtendedInvoiceCorrectionItem.AmountsDec.Total, 2));
	
	УстановитьЗначениеXDTO(СведТов, "СтТовУчНал", СтТовУчНал);
	
	// - Информационное поле события (факта хозяйственной жизни) 2
	
	XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, СведТов.ИнфПолФХЖ2, ExtendedInvoiceCorrectionItem.StructedAdditionalInfos, "Файл.Документ.ТаблКСчФ.СведТов.ИнфПолФХЖ2");
	
	// - Доп. сведения
	
	ДопСведТов = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблКСчФ.СведТов.ДопСведТов", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(ДопСведТов, "КорСчДебет", ExtendedInvoiceCorrectionItem.ItemAccountDebit);
	УстановитьЗначениеXDTO(ДопСведТов, "КорСчКредит", ExtendedInvoiceCorrectionItem.ItemAccountCredit);
	
	УстановитьЗначениеXDTO(СведТов, "ДопСведТов", ДопСведТов);
	
КонецПроцедуры

Процедура XML_ОтветныйТитул_УКД_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO
	, СодФХЖ4
	, ПротоКонтент
	, НаименованиеДокумента
	, ФункцияДокумента
	, НомерДокумента
	, ДатаДокумента)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	УстановитьЗначениеXDTO(СодФХЖ4, "ФункцияПр"		, ФункцияДокумента);
	УстановитьЗначениеXDTO(СодФХЖ4, "НаимДокОпрПр"	, НаименованиеДокумента);
	УстановитьЗначениеXDTO(СодФХЖ4, "ДатаДокИнфПр"	, Дата_В_Формат(ДатаДокумента, ФорматДаты));
	СодФХЖ4.НомДокИнфПр = НомерДокумента;
	
	// Сведения о согласовании изменения стоимости
	СвСоглас = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ4, "СвСоглас");
	УстановитьЗначениеXDTO(СвСоглас, "СодОпер"		, ПротоКонтент.OperationContent);
	УстановитьЗначениеXDTO(СвСоглас, "ДатаСоглас"	, Дата_В_Формат(ПротоКонтент.AcceptanceDate, ФорматДаты));
	
	XML_ЗаполнитьИнфоПоляФХЖ(ИмяПакетаXDTO, СодФХЖ4, ПротоКонтент.AdditionalInfoId, "ИнфПолФХЖ4");
	
КонецПроцедуры

Функция XML_УКД_736_ДопПрефиксИмениФайла(ПротоКонтент)
	
	Результат = "";
	
	Для Каждого Item Из ПротоКонтент.InvoiceCorrectionTable.Items Цикл
		
		Если Item.OriginalItemIdentificationNumbers.Количество() > 0
			Или Item.CorrectedItemIdentificationNumbers.Количество() > 0 Тогда
			
			Результат = "MARK";
			Прервать;
			
		ИначеЕсли Item.ItemTracingInfos.Количество() > 0 Тогда
			
			Результат = "PROS";
			Прервать;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура XML_УКД_736_ЗаполнитьСведенияСФ(ИмяПакетаXDTO, Документ, Content)

	XML_УКД_ЗаполнитьСведенияСФ(ИмяПакетаXDTO, Документ, Content);
	
КонецПроцедуры

Процедура XML_УКД_736_ЗаполнитьСведенияТаблицыСФ(ИмяПакетаXDTO, Документ, Content, ПараметрыФормированияФайла)
	
	// Таблица счета-фактуры
	Если Content.InvoiceCorrectionTable.Items.Количество() > 0 Тогда
		
		ТаблКСчФ = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблКСчФ", ИмяПакетаXDTO);
		
		НомерСтроки = 1;
		
		Для Каждого ExtendedInvoiceCorrectionItem Из Content.InvoiceCorrectionTable.Items Цикл
			
			СведТов = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблКСчФ.СведТов", ИмяПакетаXDTO);
			УстановитьЗначениеXDTO(СведТов, "НомСтр", НомерСтроки);
			XML_УКД_736_ЗаполнитьСтрокуТабЧасти(ИмяПакетаXDTO, СведТов, ExtendedInvoiceCorrectionItem, ПараметрыФормированияФайла);
			
			ТаблКСчФ.СведТов.Добавить(СведТов);
			НомерСтроки = НомерСтроки + 1;
			
		КонецЦикла;
		
		// Всего увеличение
		Если Content.InvoiceCorrectionTable.TotalsInc.Total <> Неопределено Тогда
			
			ВсегоУвел = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблКСчФ.ВсегоУвел", ИмяПакетаXDTO);
			
			УстановитьЗначениеXDTO(ВсегоУвел, "СтТовБезНДСВсего", 	XML_ФорматЧисла(Content.InvoiceCorrectionTable.TotalsInc.TotalWithVatExcluded, 2));
			УстановитьЗначениеXDTO(ВсегоУвел, "СтТовУчНалВсего", 	XML_ФорматЧисла(Content.InvoiceCorrectionTable.TotalsInc.Total, 2));
			
			СумНал = XML_ПолучитьПустойОбъектXDTO("СумНДСТип", ИмяПакетаXDTO);
			
			Если ЭтоИстина(Content.InvoiceCorrectionTable.TotalsInc.WithoutVat) Тогда
				УстановитьЗначениеXDTO(СумНал, "БезНДС", СтрокаБезНДС());
			Иначе			
				УстановитьЗначениеXDTO(СумНал, "СумНДС", XML_ФорматЧисла(Content.InvoiceCorrectionTable.TotalsInc.Vat, 2));
			КонецЕсли;
			
			УстановитьЗначениеXDTO(ВсегоУвел, "СумНал", СумНал);
			УстановитьЗначениеXDTO(ТаблКСчФ, "ВсегоУвел", ВсегоУвел);
			
		КонецЕсли;
		
		// Всего уменьшение
		Если Content.InvoiceCorrectionTable.TotalsDec.Total <> Неопределено Тогда
			
			ВсегоУм = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблКСчФ.ВсегоУм", ИмяПакетаXDTO);
			
			УстановитьЗначениеXDTO(ВсегоУм, "СтТовБезНДСВсего", XML_ФорматЧисла(Content.InvoiceCorrectionTable.TotalsDec.TotalWithVatExcluded, 2));
			УстановитьЗначениеXDTO(ВсегоУм, "СтТовУчНалВсего", 	XML_ФорматЧисла(Content.InvoiceCorrectionTable.TotalsDec.Total, 2));
			
			СумНал = XML_ПолучитьПустойОбъектXDTO("СумНДСТип", ИмяПакетаXDTO);
			
			Если ЭтоИстина(Content.InvoiceCorrectionTable.TotalsDec.WithoutVat) Тогда
				УстановитьЗначениеXDTO(СумНал, "БезНДС", СтрокаБезНДС());
			Иначе			
				УстановитьЗначениеXDTO(СумНал, "СумНДС", XML_ФорматЧисла(Content.InvoiceCorrectionTable.TotalsDec.Vat, 2));
			КонецЕсли;
			
			УстановитьЗначениеXDTO(ВсегоУм, "СумНал", СумНал);
			УстановитьЗначениеXDTO(ТаблКСчФ, "ВсегоУм", ВсегоУм);
			
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Документ, "ТаблКСчФ", ТаблКСчФ);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УКД_736_ЗаполнитьСтрокуТабЧасти(ИмяПакетаXDTO, СведТов, Item, ПараметрыФормированияФайла)

	УстановитьЗначениеXDTO(СведТов, "НаимТов", 		Item.Product);
	УстановитьЗначениеXDTO(СведТов, "ОКЕИ_ТовДо", 	Item.OriginalValues.UnitCode);
	УстановитьЗначениеXDTO(СведТов, "ОКЕИ_ТовПосле", Item.CorrectedValues.UnitCode);
	
	УстановитьЗначениеXDTO(СведТов, "КолТовДо", 	XML_ФорматЧисла(Item.OriginalValues.Quantity,	ПараметрыФормированияФайла.ТочностьКоличества));
	УстановитьЗначениеXDTO(СведТов, "КолТовПосле", 	XML_ФорматЧисла(Item.CorrectedValues.Quantity,	ПараметрыФормированияФайла.ТочностьКоличества));
	УстановитьЗначениеXDTO(СведТов, "ЦенаТовДо", 	XML_ФорматЧисла(Item.OriginalValues.Price,		ПараметрыФормированияФайла.ТочностьЦены));
	УстановитьЗначениеXDTO(СведТов, "ЦенаТовПосле", XML_ФорматЧисла(Item.CorrectedValues.Price,		ПараметрыФормированияФайла.ТочностьЦены));
	
	УстановитьЗначениеXDTO(СведТов, "НалСтДо", 		НалСт_Из_TaxRate(Item.OriginalValues.TaxRate));
	УстановитьЗначениеXDTO(СведТов, "НалСтПосле", 	НалСт_Из_TaxRate(Item.CorrectedValues.TaxRate));
	УстановитьЗначениеXDTO(СведТов, "ПорНомТовВСЧФ", Item.OriginalNumber);
	
	// - Стоимость без учета НДС
	
	СтТовБезНДС = XML_ПолучитьПустойОбъектXDTO("СтоимТип", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(СтТовБезНДС, "СтоимДоИзм", 	XML_ФорматЧисла(Item.OriginalValues.TotalWithVatExcluded, 2));
	УстановитьЗначениеXDTO(СтТовБезНДС, "СтоимПослеИзм", XML_ФорматЧисла(Item.CorrectedValues.TotalWithVatExcluded, 2));
	УстановитьЗначениеXDTO(СтТовБезНДС, "СтоимУвел", 	XML_ФорматЧисла(Item.AmountsInc.TotalWithVatExcluded, 2));
	УстановитьЗначениеXDTO(СтТовБезНДС, "СтоимУм", 		XML_ФорматЧисла(Item.AmountsDec.TotalWithVatExcluded, 2));
	
	УстановитьЗначениеXDTO(СведТов, "СтТовБезНДС", СтТовБезНДС);
	
	// - Акциз
	
	АкцизДо = XML_ПолучитьПустойОбъектXDTO("СумАкцизТип", ИмяПакетаXDTO);
	
	Если НЕ ЗначениеЗаполнено(Item.OriginalValues.Excise) Тогда
		УстановитьЗначениеXDTO(АкцизДо, "БезАкциз", "без акциза");
	Иначе
		УстановитьЗначениеXDTO(АкцизДо, "СумАкциз", XML_ФорматЧисла(Item.OriginalValues.Excise, 2));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "АкцизДо", АкцизДо);
	
	АкцизПосле = XML_ПолучитьПустойОбъектXDTO("СумАкцизТип", ИмяПакетаXDTO);
	
	Если НЕ ЗначениеЗаполнено(Item.CorrectedValues.Excise) Тогда
		УстановитьЗначениеXDTO(АкцизПосле, "БезАкциз", "без акциза");
	Иначе
		УстановитьЗначениеXDTO(АкцизПосле, "СумАкциз", XML_ФорматЧисла(Item.CorrectedValues.Excise, 2));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "АкцизПосле", АкцизПосле);
	
	Если Привести_К_Числу(Item.AmountsInc.Excise) > 0 Или Привести_К_Числу(Item.AmountsDec.Excise) > 0 Тогда
		
		АкцизРазн = XML_ПолучитьПустойОбъектXDTO("РазнСумНалТип", ИмяПакетаXDTO);
		
		Если Привести_К_Числу(Item.AmountsInc.Excise) > 0 Тогда
			УстановитьЗначениеXDTO(АкцизРазн, "СумУвел",	XML_ФорматЧисла(Item.AmountsInc.Excise, 2));
			
		ИначеЕсли Привести_К_Числу(Item.AmountsDec.Excise) > 0 Тогда
			УстановитьЗначениеXDTO(АкцизРазн, "СумУм",		XML_ФорматЧисла(Item.AmountsDec.Excise, 2));
		КонецЕсли;
		
		УстановитьЗначениеXDTO(СведТов, "АкцизРазн", АкцизРазн);
		
	КонецЕсли;
	
	// - Сумма НДС
	
	СумНалДо = XML_ПолучитьПустойОбъектXDTO("СумНДСТип", ИмяПакетаXDTO);
	
	Если ЭтоИстина(Item.OriginalValues.WithoutVat) Тогда
		УстановитьЗначениеXDTO(СумНалДо, "БезНДС", СтрокаБезНДС());
	Иначе
		УстановитьЗначениеXDTO(СумНалДо, "СумНДС", XML_ФорматЧисла(Item.OriginalValues.Vat, 2));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "СумНалДо", СумНалДо);
	
	СумНалПосле = XML_ПолучитьПустойОбъектXDTO("СумНДСТип", ИмяПакетаXDTO);
	
	Если ЭтоИстина(Item.CorrectedValues.WithoutVat) Тогда
		УстановитьЗначениеXDTO(СумНалПосле, "БезНДС", СтрокаБезНДС());
	Иначе
		УстановитьЗначениеXDTO(СумНалПосле, "СумНДС", XML_ФорматЧисла(Item.CorrectedValues.Vat, 2));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "СумНалПосле", СумНалПосле);
	
	СтавкаНДС_ИсчНалАг = TaxRate_ЭтоЗначениеИсчНалАг(Item.OriginalValues.TaxRate);
	
	Если НЕ СтавкаНДС_ИсчНалАг Тогда
		
		// - Сумма НДС
		
		СумНалРазн = XML_ПолучитьПустойОбъектXDTO("РазнСумНалТип", ИмяПакетаXDTO);
		
		Если НЕ ЗначениеЗаполнено(Item.AmountsInc.Vat) 
			И НЕ ЗначениеЗаполнено(Item.AmountsDec.Vat) Тогда
			
			УстановитьЗначениеXDTO(СумНалРазн, "СумУвел",	XML_ФорматЧисла(0, 2));
			
		ИначеЕсли ЗначениеЗаполнено(Item.AmountsInc.Vat) Тогда
			УстановитьЗначениеXDTO(СумНалРазн, "СумУвел",	XML_ФорматЧисла(Item.AmountsInc.Vat, 2));
		ИначеЕсли ЗначениеЗаполнено(Item.AmountsDec.Vat) Тогда
			УстановитьЗначениеXDTO(СумНалРазн, "СумУм", 	XML_ФорматЧисла(Item.AmountsDec.Vat, 2));
		КонецЕсли;
		
		УстановитьЗначениеXDTO(СведТов, "СумНалРазн", СумНалРазн);
		
	КонецЕсли;
	
	// - Стоимость с учетом НДС
	
	СтТовУчНал = XML_ПолучитьПустойОбъектXDTO("СтоимТип", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(СтТовУчНал, "СтоимДоИзм", 	XML_ФорматЧисла(Item.OriginalValues.Total, 2));
	УстановитьЗначениеXDTO(СтТовУчНал, "СтоимПослеИзм",	XML_ФорматЧисла(Item.CorrectedValues.Total, 2));
	УстановитьЗначениеXDTO(СтТовУчНал, "СтоимУвел", 	XML_ФорматЧисла(Item.AmountsInc.Total, 2));
	УстановитьЗначениеXDTO(СтТовУчНал, "СтоимУм", 		XML_ФорматЧисла(Item.AmountsDec.Total, 2));
	
	УстановитьЗначениеXDTO(СведТов, "СтТовУчНал", СтТовУчНал);
	
	// - Информационное поле события (факта хозяйственной жизни) 2
	
	XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, СведТов.ИнфПолФХЖ2, Item.StructedAdditionalInfos, "Файл.Документ.ТаблКСчФ.СведТов.ИнфПолФХЖ2");
	
	// - Доп. сведения
	
	ДопСведТов = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблКСчФ.СведТов.ДопСведТов", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(ДопСведТов, "АртикулТов", Item.ItemArticle);
	УстановитьЗначениеXDTO(ДопСведТов, "КодВидТов", Item.ItemTypeCode);
	УстановитьЗначениеXDTO(ДопСведТов, "КодТов", Item.ItemVendorCode);
	УстановитьЗначениеXDTO(ДопСведТов, "НаимЕдИзмДо", Item.OriginalValues.UnitName);
	УстановитьЗначениеXDTO(ДопСведТов, "НаимЕдИзмПосле", Item.CorrectedValues.UnitName);
	УстановитьЗначениеXDTO(ДопСведТов, "СортТов", Item.ItemKind);
	УстановитьЗначениеXDTO(ДопСведТов, "ХарактерТов", Item.ItemCharact);
	
	УстановитьЗначениеXDTO(СведТов, "ДопСведТов", ДопСведТов);
	
	// Сведения о маркировке
	Если ЗначениеЗаполнено(Item.OriginalItemIdentificationNumbers) Тогда
		XML_ЗаполнитьНомСредИдентТов(ИмяПакетаXDTO, СведТов, Item.OriginalItemIdentificationNumbers, "НомСредИдентТовДо");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Item.CorrectedItemIdentificationNumbers) Тогда
		XML_ЗаполнитьНомСредИдентТов(ИмяПакетаXDTO, СведТов, Item.CorrectedItemIdentificationNumbers, "НомСредИдентТовПосле");
	КонецЕсли;
	
	Если Item.ItemTracingInfos.Количество() > 0 Тогда
		XML_УКД_736_ЗаполнитьСведПрослеж(ИмяПакетаXDTO, СведТов.СведПрослеж, Item.ItemTracingInfos);
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УКД_736_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, Content)
	
	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	// Информационное поле - 3 (сведения о факте согласования)
	СодФХЖ3 = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СодФХЖ3", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(СодФХЖ3, "ИныеСвИзмСтоим", Content.EventContent.CostChangeInfo);
	УстановитьЗначениеXDTO(СодФХЖ3, "СодОпер", Content.EventContent.OperationContent);
	УстановитьЗначениеXDTO(СодФХЖ3, "ДатаНапр", Дата_В_Формат(Content.EventContent.NotificationDate, ФорматДаты));
	
	XML_ЗаполнитьДокумТип(ИмяПакетаXDTO, СодФХЖ3.ПередатДокум, Content.EventContent.TransferDocDetails736);
	XML_ЗаполнитьДокумТип(ИмяПакетаXDTO, СодФХЖ3.ДокумОснКор, Content.EventContent.CorrectionBases);
	
	УстановитьЗначениеXDTO(Документ, "СодФХЖ3", СодФХЖ3);
	
КонецПроцедуры

Процедура XML_УКД_736_ЗаполнитьСведПрослеж(ИмяПакетаXDTO, СведПрослеж, ItemTracingInfos)
	
	Для Каждого ItemTracingInfo Из ItemTracingInfos Цикл
		
		НовыйСведПрослеж = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблКСчФ.СведТов.СведПрослеж", ИмяПакетаXDTO);
		
		УстановитьЗначениеXDTO(НовыйСведПрослеж, "ДопПрослеж", ItemTracingInfo.ItemAddInfo);
		УстановитьЗначениеXDTO(НовыйСведПрослеж, "НомТовПрослеж", ItemTracingInfo.RegNumberUnit);
		УстановитьЗначениеXDTO(НовыйСведПрослеж, "ЕдИзмПрослеж", ItemTracingInfo.Unit);
		
		ИзмКолЕдПТ = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблКСчФ.СведТов.СведПрослеж.ИзмКолЕдПТ", ИмяПакетаXDTO);
		
		УстановитьЗначениеXDTO(ИзмКолЕдПТ, "КолТовЕдПТДо", ItemTracingInfo.QuantityDiff.OriginalQuantity);
		УстановитьЗначениеXDTO(ИзмКолЕдПТ, "КолТовЕдПТПосле", ItemTracingInfo.QuantityDiff.CorrectedQuantity);
		УстановитьЗначениеXDTO(ИзмКолЕдПТ, "КолТовЕдПТУвел", ItemTracingInfo.QuantityDiff.QuantityInc);
		УстановитьЗначениеXDTO(ИзмКолЕдПТ, "КолТовЕдПТУм", ItemTracingInfo.QuantityDiff.QuantityDec);
		
		НовыйСведПрослеж.ИзмКолЕдПТ = ИзмКолЕдПТ;
		
		СведПрослеж.Добавить(НовыйСведПрослеж);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_ЗаполнитьДокумТип(ИмяПакетаXDTO, ДокумТип, BaseDocumentNames)
	
	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	Для Каждого BaseDocumentName Из BaseDocumentNames Цикл
		
		НовыйДокумТип = XML_ПолучитьПустойОбъектXDTO("ДокумТип", ИмяПакетаXDTO);
		
		УстановитьЗначениеXDTO(НовыйДокумТип, "НаимОсн", BaseDocumentName.BaseDocumentName);
		УстановитьЗначениеXDTO(НовыйДокумТип, "НомОсн", BaseDocumentName.BaseDocumentNumber);
		УстановитьЗначениеXDTO(НовыйДокумТип, "ДатаОсн", Дата_В_Формат(BaseDocumentName.BaseDocumentDate, ФорматДаты));
		УстановитьЗначениеXDTO(НовыйДокумТип, "ДопСвОсн", BaseDocumentName.AdditionalInfo);
		УстановитьЗначениеXDTO(НовыйДокумТип, "ИдФайлОсн", BaseDocumentName.BaseIdFile);
		
		ДокумТип.Добавить(НовыйДокумТип);
		
	КонецЦикла;
	
КонецПроцедуры

// XML: функции заполнения Акт_552

Процедура XML_Акт_552_ЗаполнитьСведенияДокумента(ИмяПакетаXDTO, Документ, Content)

	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	СвДокПРУ = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвДокПРУ", ИмяПакетаXDTO);
	
	// Наименование первичного документа
	
	НаимДок = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвДокПРУ.НаимДок", ИмяПакетаXDTO);
	
	НаимДок.ПоФактХЖ = "Документ о передаче результатов работ (Документ об оказании услуг)";
	
	УстановитьЗначениеXDTO(НаимДок, "НаимДокОпр", Content.DocumentName);
	
	УстановитьЗначениеXDTO(СвДокПРУ, "НаимДок", НаимДок);
	
	// Дата и номер документа
	
	ИдентДок = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвДокПРУ.ИдентДок", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(ИдентДок, "НомДокПРУ", Content.DocumentNumber);
	УстановитьЗначениеXDTO(ИдентДок, "ДатаДокПРУ", Дата_В_Формат(Content.DocumentDate, ФорматДаты));
	
	УстановитьЗначениеXDTO(СвДокПРУ, "ИдентДок", ИдентДок);
	
	// Исправление
	
	Если ЗначениеЗаполнено(Content.RevisionNumber) И ЗначениеЗаполнено(Content.RevisionDate) Тогда
		
		ИспрДокПРУ = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвДокПРУ.ИспрДокПРУ", ИмяПакетаXDTO);
		
		УстановитьЗначениеXDTO(ИспрДокПРУ, "НомИспрДокПРУ", Content.RevisionNumber);
		УстановитьЗначениеXDTO(ИспрДокПРУ, "ДатаИспрДокПРУ", Дата_В_Формат(Content.RevisionDate, ФорматДаты));
		
		УстановитьЗначениеXDTO(СвДокПРУ, "ИспрДокПРУ", ИспрДокПРУ);
		
	КонецЕсли;

	// Единица денежного измерения
	
	ДенИзм = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвДокПРУ.ДенИзм", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(ДенИзм, "КодОКВ", Content.Currency);
	УстановитьЗначениеXDTO(ДенИзм, "НаимОКВ", НаименованиеВалюты(Content.Currency, Истина));
	УстановитьЗначениеXDTO(ДенИзм, "КурсВал", Content.CurrencyRate);
	
	УстановитьЗначениеXDTO(СвДокПРУ, "ДенИзм", ДенИзм);
	
	// Содержание факта хозяйственной жизни (1)
	
	СодФХЖ1 = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвДокПРУ.СодФХЖ1", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(СодФХЖ1, "ЗагСодОпер", Content.OperationTitle);
	
	// Исполнитель
	
	Исполнитель = XML_ПолучитьПустойОбъектXDTO("УчастникТип", ИмяПакетаXDTO);
	XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, Исполнитель, Content.Seller);
	УстановитьЗначениеXDTO(СодФХЖ1, "Исполнитель", Исполнитель);
	
	// Заказчик
	
	Заказчик = XML_ПолучитьПустойОбъектXDTO("УчастникТип", ИмяПакетаXDTO);
	XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, Заказчик, Content.Buyer);
	УстановитьЗначениеXDTO(СодФХЖ1, "Заказчик", Заказчик);
	
	// Основание
			
	Для Каждого GroundInfo Из Content.Grounds Цикл
		
		Основание = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвДокПРУ.СодФХЖ1.Основание", ИмяПакетаXDTO);				
		
		УстановитьЗначениеXDTO(Основание, "НаимОсн", GroundInfo.Name);
		УстановитьЗначениеXDTO(Основание, "НомОсн", GroundInfo.Number);
		УстановитьЗначениеXDTO(Основание, "ДатаОсн", Дата_В_Формат(GroundInfo.Date, ФорматДаты));
		УстановитьЗначениеXDTO(Основание, "ДопСвОсн", GroundInfo.Info);
		
		СодФХЖ1.Основание.Добавить(Основание);
		
	КонецЦикла;
		
	// Идентификатор государственного контракта
		
	УстановитьЗначениеXDTO(СодФХЖ1, "ИдГосКон", Content.GovernmentContractInfo);
	
	// Вид операции
	
	УстановитьЗначениеXDTO(СодФХЖ1, "ВидОперации", Content.OperationType);
	
	// Описание выполненных работ (услуг)
	
	Для Каждого Act552WorkDescription Из Content.Works Цикл
		
		ОписРабот = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвДокПРУ.СодФХЖ1.ОписРабот", ИмяПакетаXDTO);
		XML_Акт_552_ЗаполнитьСтрокуТабЧасти(ИмяПакетаXDTO, ОписРабот, Act552WorkDescription);
		СодФХЖ1.ОписРабот.Добавить(ОписРабот);
		
	КонецЦикла;
	
	// Информационное поле факта хозяйственной жизни (1)
	
	Если ЗначениеЗаполнено(Content.AdditionalInfo.InfoFileId) ИЛИ Content.AdditionalInfo.StructedAdditionalInfos.Количество() > 0 Тогда
		
		ИнфПолФХЖ1 = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвДокПРУ.СодФХЖ1.ИнфПолФХЖ1", ИмяПакетаXDTO);		
		УстановитьЗначениеXDTO(ИнфПолФХЖ1, "ИдФайлИнфПол", Content.AdditionalInfo.InfoFileId);		
		XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, ИнфПолФХЖ1.ТекстИнф, Content.AdditionalInfo.StructedAdditionalInfos);
		УстановитьЗначениеXDTO(СодФХЖ1, "ИнфПолФХЖ1", ИнфПолФХЖ1);
		
	КонецЕсли;	
	
	УстановитьЗначениеXDTO(СвДокПРУ, "СодФХЖ1", СодФХЖ1);
	УстановитьЗначениеXDTO(Документ, "СвДокПРУ", СвДокПРУ);
	
КонецПроцедуры

Процедура XML_Акт_552_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, Content)

	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	// Содержание факта хозяйственной жизни (2)
	
	СодФХЖ2 = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СодФХЖ2", ИмяПакетаXDTO);	
	УстановитьЗначениеXDTO(СодФХЖ2, "СодОпер", Content.TransferInfo.OperationInfo);	
	УстановитьЗначениеXDTO(СодФХЖ2, "ДатаПер", Дата_В_Формат(Content.TransferInfo.TransferDate, ФорматДаты));
	
	// Сведения о передаче вещи, изготовленной по договору подряда			
		
	Если ЗначениеЗаполнено(Content.TransferInfo.CreatedThingTransferDate) Тогда
		
		СвПерВещи = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СодФХЖ2.СвПерВещи", ИмяПакетаXDTO);		
		УстановитьЗначениеXDTO(СвПерВещи, "ДатаПерВещ", Дата_В_Формат(Content.TransferInfo.CreatedThingTransferDate, ФорматДаты));
		УстановитьЗначениеXDTO(СвПерВещи, "СвПерВещ", Content.TransferInfo.CreatedThingInfo);
		УстановитьЗначениеXDTO(СодФХЖ2, "СвПерВещи", СвПерВещи);
		
	КонецЕсли;
	
	// Информационное поле факта хозяйственной жизни (2)
	
	XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, СодФХЖ2.ИнфПолФХЖ2, Content.TransferInfo.StructedAdditionalInfos);	
	
	УстановитьЗначениеXDTO(Документ, "СодФХЖ2", СодФХЖ2);
	
КонецПроцедуры

Процедура XML_Акт_552_ЗаполнитьСтрокуТабЧасти(ИмяПакетаXDTO, ОписРабот, Act552WorkDescription)
	
	УстановитьЗначениеXDTO(ОписРабот, "НачРабот", 	Дата_В_Формат(Act552WorkDescription.StartingDate, "ДФ=dd.MM.yyyy"));
	УстановитьЗначениеXDTO(ОписРабот, "КонРабот", 	Дата_В_Формат(Act552WorkDescription.CompletionDate, "ДФ=dd.MM.yyyy"));		
	УстановитьЗначениеXDTO(ОписРабот, "СтБезНДСИт", Act552WorkDescription.TotalWithVatExcluded);
	УстановитьЗначениеXDTO(ОписРабот, "СумНДСИт", 	Act552WorkDescription.TotalVat);
	УстановитьЗначениеXDTO(ОписРабот, "СтУчНДСИт", 	Act552WorkDescription.Total);
	
	НомерСтроки = 1;
	
	Для Каждого Act552WorkItem Из Act552WorkDescription.Items Цикл
		
		Работа = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвДокПРУ.СодФХЖ1.ОписРабот.Работа", ИмяПакетаXDTO);
		
		УстановитьЗначениеXDTO(Работа, "Номер", НомерСтроки);
		
		УстановитьЗначениеXDTO(Работа, "НаимРабот", 	Act552WorkItem.Name);
		УстановитьЗначениеXDTO(Работа, "НаимЕдИзм", 	Act552WorkItem.UnitName);
		УстановитьЗначениеXDTO(Работа, "ОКЕИ",			Act552WorkItem.UnitCode);
		УстановитьЗначениеXDTO(Работа, "Цена", 			Act552WorkItem.Price);
		УстановитьЗначениеXDTO(Работа, "Количество", 	Act552WorkItem.Quantity);
		УстановитьЗначениеXDTO(Работа, "СтоимБезНДС", 	Act552WorkItem.SubtotalWithVatExcluded);
		УстановитьЗначениеXDTO(Работа, "НалСт", 		НалСт_Из_TaxRate(Act552WorkItem.TaxRate));
		УстановитьЗначениеXDTO(Работа, "СумНДС", 		Act552WorkItem.Vat);
		УстановитьЗначениеXDTO(Работа, "СтоимУчНДС", 	Act552WorkItem.Subtotal);			
		УстановитьЗначениеXDTO(Работа, "КоррСчДебет", 	Act552WorkItem.ItemAccountDebit);
		УстановитьЗначениеXDTO(Работа, "КоррСчКредит",	Act552WorkItem.ItemAccountCredit);
		УстановитьЗначениеXDTO(Работа, "Описание", 		Act552WorkItem.Description);
		
		// - Информационное поле описания работ
		
		XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, Работа.ИнфПолеОписРабот, Act552WorkItem.StructedAdditionalInfos);
		
		ОписРабот.Работа.Добавить(Работа);
		НомерСтроки = НомерСтроки + 1;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_ОтветныйТитул_Акт_552_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO
	, СодФХЖ3
	, ПротоКонтент
	, НаименованиеДокумента
	, НомерДокумента
	, ДатаДокумента)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	УстановитьЗначениеXDTO(СодФХЖ3, "ВидОперации"	, ПротоКонтент.OperationType);
	УстановитьЗначениеXDTO(СодФХЖ3, "НаимДокОпрИсп"	, НаименованиеДокумента);
	УстановитьЗначениеXDTO(СодФХЖ3, "ДатаДокПРУИсп"	, Дата_В_Формат(ДатаДокумента, ФорматДаты));
	
	Если ЗначениеЗаполнено(НомерДокумента) Тогда
		СодФХЖ3.НомДокПРУИсп = НомерДокумента;
	КонецЕсли;
	
	// Сведения о приемке результатов работ (подтверждение факта оказания услуг)
	РезПринял = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ3, "РезПринял");
	УстановитьЗначениеXDTO(РезПринял, "СодОпер"		, ПротоКонтент.OperationContent);
	УстановитьЗначениеXDTO(РезПринял, "ДатаПрием"	, Дата_В_Формат(ПротоКонтент.AcceptanceDate, ФорматДаты));
	
	ЕстьДатаПолучения		 = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "CreatedThingAcceptDate") И ЗначениеЗаполнено(ПротоКонтент.CreatedThingAcceptDate);
	ЕстьСведенияОПолучении	 = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "CreatedThingInfo") И ЗначениеЗаполнено(ПротоКонтент.CreatedThingInfo);
	
	Если ЕстьДатаПолучения Или ЕстьСведенияОПолучении Тогда
		
		// Сведения о получении вещи, изготовленной по договору подряда
		СвПолВещи = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ3, "СвПолВещи");
		
		Если ЕстьДатаПолучения Тогда
			УстановитьЗначениеXDTO(СвПолВещи, "ДатаПол", Дата_В_Формат(ПротоКонтент.CreatedThingAcceptDate, ФорматДаты));
		КонецЕсли;
		
		Если ЕстьСведенияОПолучении Тогда
			УстановитьЗначениеXDTO(СвПолВещи, "СвПол", ПротоКонтент.CreatedThingInfo);
		КонецЕсли;
		
	КонецЕсли;
	
	XML_ЗаполнитьИнфоПоляФХЖ(ИмяПакетаXDTO, СодФХЖ3, ПротоКонтент.AdditionalInfoId, "ИнфПолФХЖ3");
	
КонецПроцедуры

// XML: функции заполнения Торг12_551

Процедура XML_Торг_551_ЗаполнитьСведенияДокумента(ИмяПакетаXDTO, Документ, Content)

	Фабрика		 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ФорматДаты	 = "ДФ=dd.MM.yyyy";
	
	СвДокПТПрКроме = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СвДокПТПрКроме");
	СвДокПТПр = НовыйДочернийОбъектXDTO(Фабрика, СвДокПТПрКроме, "СвДокПТПр");
	
	// Наименование первичного документа
	
	НаимДок = НовыйДочернийОбъектXDTO(Фабрика, СвДокПТПр, "НаимДок");
	
	УстановитьЗначениеXDTO(НаимДок, "ПоФактХЖ", "Документ о передаче товара при торговых операциях");
	УстановитьЗначениеXDTO(НаимДок, "НаимДокОпр", Content.DocumentName);
	
	// Дата и номер документа
	
	ИдентДок = НовыйДочернийОбъектXDTO(Фабрика, СвДокПТПр, "ИдентДок");
	
	УстановитьЗначениеXDTO(ИдентДок, "НомДокПТ", Content.DocumentNumber);
	УстановитьЗначениеXDTO(ИдентДок, "ДатаДокПТ", Дата_В_Формат(Content.DocumentDate, ФорматДаты));
	
	// Исправление
	
	Если ЗначениеЗаполнено(Content.RevisionNumber) И ЗначениеЗаполнено(Content.RevisionDate) Тогда
		
		// протестировать отдельно!
		
		ИспрДокПТ = НовыйДочернийОбъектXDTO(Фабрика, СвДокПТПр, "ИспрДокПТ");
		
		УстановитьЗначениеXDTO(ИспрДокПТ, "НомИспрДокПТ", Content.RevisionNumber);
		УстановитьЗначениеXDTO(ИспрДокПТ, "ДатаИспрДокПТ", Дата_В_Формат(Content.RevisionDate, ФорматДаты));
		
	КонецЕсли;

	// Единица денежного измерения
	
	ДенИзм = НовыйДочернийОбъектXDTO(Фабрика, СвДокПТПр, "ДенИзм");
	
	УстановитьЗначениеXDTO(ДенИзм, "КодОКВ", Content.Currency);
	УстановитьЗначениеXDTO(ДенИзм, "НаимОКВ", НаименованиеВалюты(Content.Currency, Истина));
	УстановитьЗначениеXDTO(ДенИзм, "КурсВал", Content.CurrencyRate);
	
	// Содержание факта хозяйственной жизни (1)
	
	СодФХЖ1 = НовыйДочернийОбъектXDTO(Фабрика, СвДокПТПр, "СодФХЖ1");
	
	// Грузоотправитель
	
	Если ЗначениеЗаполнено(Content.Shipper.Name) Тогда
		ГрузОтпр = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "ГрузОтпр");
		XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, ГрузОтпр, Content.Shipper);
	КонецЕсли;

	// Грузополучатель
	
	Если ЗначениеЗаполнено(Content.Consignee.Name) Тогда
		ГрузПолуч = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "ГрузПолуч");
		XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, ГрузПолуч, Content.Consignee);
	КонецЕсли;

	// Продавец
	
	Продавец = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "Продавец");
	XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, Продавец, Content.Seller);
	
	// Покупатель
	
	Покупатель = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "Покупатель");
	XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, Покупатель, Content.Buyer);
	
	// Перевозчик
	
	Если ЗначениеЗаполнено(Content.Carrier.Name) Тогда
		Перевозчик = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "Перевозчик");
		XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, Перевозчик, Content.Carrier);
	КонецЕсли;
	
	// Основание
			
	Для Каждого GroundInfo Из Content.Grounds Цикл
		
		Основание = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "Основание");
		
		УстановитьЗначениеXDTO(Основание, "НаимОсн", GroundInfo.Name);
		УстановитьЗначениеXDTO(Основание, "НомОсн", GroundInfo.Number);
		УстановитьЗначениеXDTO(Основание, "ДатаОсн", Дата_В_Формат(GroundInfo.Date, ФорматДаты));
		УстановитьЗначениеXDTO(Основание, "ДопСвОсн", GroundInfo.Info);
		
	КонецЦикла;
		
	// Идентификатор государственного контракта
		
	УстановитьЗначениеXDTO(СодФХЖ1, "ИдГосКон", Content.GovernmentContractInfo);
	
	// Вид операции
	
	УстановитьЗначениеXDTO(СодФХЖ1, "ВидОперации", Content.OperationType);
	
	// Описание товаров
	
	СодФХЖ2 = НовыйДочернийОбъектXDTO(Фабрика, СвДокПТПрКроме, "СодФХЖ2");
	
	НомерСтроки = 0;
	Для Каждого TovTorgItem Из Content.Table.Items Цикл
		
		НомерСтроки = НомерСтроки + 1;
		СвТов = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ2, "СвТов");
		XML_Торг_551_ЗаполнитьСтрокуТабЧасти(ИмяПакетаXDTO, СвТов, TovTorgItem, НомерСтроки);
		
	КонецЦикла;
	
	// Итоги по товарам
	
	Всего = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ2, "Всего");
	
	УстановитьЗначениеXDTO(Всего, "КолМестВс",		Content.Table.TotalQuantity);
	УстановитьЗначениеXDTO(Всего, "БруттоВс",		Content.Table.TotalGross);
	УстановитьЗначениеXDTO(Всего, "НеттоВс",		Content.Table.TotalNet);
	
	// Возможны проблемы с форматированием данных полей - схема предполагает 2 знака после запятой. Надо проверить.
	УстановитьЗначениеXDTO(Всего, "СтБезНДСВс",		Content.Table.TotalWithVatExcluded);
	УстановитьЗначениеXDTO(Всего, "СумНДСВс",		Content.Table.TotalVat);
	УстановитьЗначениеXDTO(Всего, "СтУчНДСВс",		Content.Table.Total);
	
	// Информационное поле факта хозяйственной жизни (1)
	
	Если ЗначениеЗаполнено(Content.AdditionalInfoId.InfoFileId) ИЛИ Content.AdditionalInfoId.StructedAdditionalInfos.Количество() > 0 Тогда
		
		ИнфПолФХЖ1 = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "ИнфПолФХЖ1");
		
		УстановитьЗначениеXDTO(ИнфПолФХЖ1, "ИдФайлИнфПол", Content.AdditionalInfoId.InfoFileId);		
		XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, ИнфПолФХЖ1.ТекстИнф, Content.AdditionalInfoId.StructedAdditionalInfos);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_Торг_551_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO, Документ, Content)

	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);

	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	// Содержание факта хозяйственной жизни (3)
	
	СодФХЖ3 = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СодФХЖ3");
	
	УстановитьЗначениеXDTO(СодФХЖ3, "СодОпер", Content.TransferInfo.OperationInfo);	
	УстановитьЗначениеXDTO(СодФХЖ3, "ДатаОтпуск", Дата_В_Формат(Content.TransferInfo.TransferDate, ФорматДаты));
	
	УстановитьЗначениеXDTO(СодФХЖ3, "КолПрил", Content.TransferInfo.Attachment);	
	
	// Транспортные накладные
	
	Для Каждого WayBill Из Content.TransferInfo.Waybills Цикл
		
		ТранНакл = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ3, "ТранНакл");
		УстановитьЗначениеXDTO(ТранНакл, "НомТранНакл", WayBill.DocumentNumber);
		УстановитьЗначениеXDTO(ТранНакл, "ДатаТранНакл", Дата_В_Формат(WayBill.DocumentDate, ФорматДаты));
		
	КонецЦикла;
	
	Если ЗначениеЗаполнено(Content.TransferInfo.Employee.Surname)
		Или ЗначениеЗаполнено(Content.TransferInfo.OtherIssuer.Surname) Тогда
		
		// Лицо, отпустившее груз
		СвЛицОтпГруз = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ3, "СвЛицОтпГруз");
		
		Если ЗначениеЗаполнено(Content.TransferInfo.Employee.Surname) Тогда
			
			// Работник организации
			РабОргПрод = НовыйДочернийОбъектXDTO(Фабрика, СвЛицОтпГруз, "РабОргПрод");
			XML_ЗаполнитьРаботникаОрганизации(ИмяПакетаXDTO, РабОргПрод, Content.TransferInfo.Employee);
			
		ИначеЕсли ЗначениеЗаполнено(Content.TransferInfo.OtherIssuer.Surname) Тогда
			
			// Иное лицо
			ИнЛицо = НовыйДочернийОбъектXDTO(Фабрика, СвЛицОтпГруз, "ИнЛицо");
			
			Если ЗначениеЗаполнено(Content.OtherIssuer.EmployeePosition)
				И ЗначениеЗаполнено(Content.OtherIssuer.OrganizationName) Тогда
				
				ПредОргПер = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ПредОргПер");
				XML_ЗаполнитьПредставителяОрганизации(ИмяПакетаXDTO, ПредОргПер, Content.TransferInfo.OtherIssuer);
				
			Иначе
				
				ФЛПер = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ФЛПер");
				XML_ЗаполнитьФизическоеЛицо(ИмяПакетаXDTO, ФЛПер, Content.TransferInfo.OtherIssue)
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	// Информационное поле факта хозяйственной жизни (3)
	
	XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, СодФХЖ3.ИнфПолФХЖ3, Content.TransferInfo.StructedAdditionalInfos);	
	
КонецПроцедуры

Процедура XML_Торг_551_ЗаполнитьСтрокуТабЧасти(ИмяПакетаXDTO, СвТов, Item, НомерСтроки)
	
	УстановитьЗначениеXDTO(СвТов, "НомТов",			НомерСтроки);
	УстановитьЗначениеXDTO(СвТов, "НаимТов",		Item.Product);
	УстановитьЗначениеXDTO(СвТов, "ХарактерТов",	Item.Feature);
	УстановитьЗначениеXDTO(СвТов, "СортТов",		Item.Sort);
	УстановитьЗначениеXDTO(СвТов, "АртикулТов",		Item.VendorCode);
	УстановитьЗначениеXDTO(СвТов, "КодТов",			Item.ProductCode );
	УстановитьЗначениеXDTO(СвТов, "НаимЕдИзм",		Item.UnitName);
	УстановитьЗначениеXDTO(СвТов, "ОКЕИ_Тов",		Item.Unit);
	УстановитьЗначениеXDTO(СвТов, "ВидУпак",		Item.PackageType);
	УстановитьЗначениеXDTO(СвТов, "Место",			Item.QuantityInPack);
	УстановитьЗначениеXDTO(СвТов, "КолМест",		Item.Quantity);
	УстановитьЗначениеXDTO(СвТов, "Брутто",			Item.Gross);
	УстановитьЗначениеXDTO(СвТов, "НеттоПередано",	Item.Net);
	УстановитьЗначениеXDTO(СвТов, "НадлОтп",		Item.ItemToRelease);
	УстановитьЗначениеXDTO(СвТов, "Цена",			XML_ФорматЧисла(Item.Price, 2));
	УстановитьЗначениеXDTO(СвТов, "НалСт",			НалСт_Из_TaxRate(Item.TaxRate));
	УстановитьЗначениеXDTO(СвТов, "КорСчДебет",		Item.ItemAccountDebit);
	УстановитьЗначениеXDTO(СвТов, "КорСчКредит",	Item.ItemAccountCredit);
	
	// Возможны проблемы с форматированием данных полей - схема предполагает 2 знака после запятой. Надо проверить.
	УстановитьЗначениеXDTO(СвТов, "СтБезНДС",		XML_ФорматЧисла(Item.SubtotalWithVatExcluded, 2));
	УстановитьЗначениеXDTO(СвТов, "СумНДС",			XML_ФорматЧисла(Item.Vat, 2));
	УстановитьЗначениеXDTO(СвТов, "СтУчНДС",		XML_ФорматЧисла(Item.Subtotal, 2));
	
	XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, СвТов.ИнфПолФХЖ2, Item.StructedAdditionalInfos);
	
КонецПроцедуры

Процедура XML_ОтветныйТитул_Торг_551_ЗаполнитьСодержаниеФХЖ(ИмяПакетаXDTO
	, СодФХЖ4
	, ПротоКонтент
	, НаименованиеДокумента
	, НомерДокумента
	, ДатаДокумента)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	УстановитьЗначениеXDTO(СодФХЖ4, "ВидОперации"	, ПротоКонтент.OperationCode);
	УстановитьЗначениеXDTO(СодФХЖ4, "НаимДокОпрПр"	, НаименованиеДокумента);
	УстановитьЗначениеXDTO(СодФХЖ4, "ДатаДокПТПр"	, Дата_В_Формат(ДатаДокумента, ФорматДаты));
	СодФХЖ4.НомДокПТПр = НомерДокумента;
	
	// Сведения о принятии товара (получении груза)
	СвПрин = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ4, "ГрузПолучил");
	УстановитьЗначениеXDTO(СвПрин, "СодОпер"	, ПротоКонтент.OperationContent);
	УстановитьЗначениеXDTO(СвПрин, "ДатаПолуч"	, Дата_В_Формат(ПротоКонтент.AcceptanceDate, ФорматДаты));
	
	ЕстьСведенияОРаботнике = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "Employee") И ЗначениеЗаполнено(ПротоКонтент.Employee.Surname);
	ЕстьСведенияОбИномЛице = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "OtherIssuer") И ЗначениеЗаполнено(ПротоКонтент.OtherIssuer.Surname);
	
	Если ЕстьСведенияОРаботнике Или ЕстьСведенияОбИномЛице Тогда
		
		// Сведения о лице, принявшем товары (груз)
		СвЛицПолГруз = НовыйДочернийОбъектXDTO(Фабрика, СвПрин, "СвЛицПолГруз");
		
		Если ЕстьСведенияОРаботнике Тогда
			
			// Работник организации покупателя
			РабОргПок = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПолГруз, "РабОргПок");
			XML_ЗаполнитьРаботникаОрганизации(ИмяПакетаXDTO, РабОргПок, ПротоКонтент.Employee);
			
		ИначеЕсли ЕстьСведенияОбИномЛице Тогда
			
			// Иное лицо
			ИнЛицо = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПолГруз, "ИнЛицо");
			
			Если ЗначениеЗаполнено(ПротоКонтент.OtherIssuer.EmployeePosition) Тогда
				
				// Представитель организации, которой доверено принятие товаров (груза)
				ПредОргПол = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ПредОргПол");
				XML_ЗаполнитьПредставителяОрганизации(ИмяПакетаXDTO, ПредОргПол, ПротоКонтент.OtherIssuer);
				
			Иначе
				
				// Физическое лицо, которому доверено принятие товаров (груза)
				ФЛ = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ФЛ");
				XML_ЗаполнитьФизическоеЛицо(ИмяПакетаXDTO, ФЛ, ПротоКонтент.OtherIssuer);
				
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	XML_ЗаполнитьИнфоПоляФХЖ(ИмяПакетаXDTO, СодФХЖ4, ПротоКонтент.AdditionalInfo, "ИнфПолФХЖ4", "Файл.Документ.СодФХЖ4.ИнфПолФХЖ4.ТекстИнф");
	
КонецПроцедуры

// XML: функции заполнения Счета

Процедура XML_Счет_ЗаполнитьСведенияДокумента(ИмяПакетаXDTO, Документ, Content)
	
	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	СвСНО = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвСНО", ИмяПакетаXDTO);
	УстановитьЗначениеXDTO(СвСНО, "НомерДок", Content.DocumentNumber);
	УстановитьЗначениеXDTO(СвСНО, "ДатаДок"	, Дата_В_Формат(Content.DocumentDate, ФорматДаты));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Content, "GovernmentContractInfo") Тогда
		УстановитьЗначениеXDTO(СвСНО, "ИдГосКон", Content.GovernmentContractInfo);
	КонецЕсли;
	
	// Назначение платежа
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Content, "PaymentPurpose") Тогда
		УстановитьЗначениеXDTO(СвСНО, "НазначПлатеж", Content.PaymentPurpose);
	КонецЕсли;
	
	// Валюта
	ДенИзм = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвСНО.ДенИзм", ИмяПакетаXDTO);
	УстановитьЗначениеXDTO(ДенИзм, "КодОКВ"	, Content.Currency);
	УстановитьЗначениеXDTO(ДенИзм, "НаимОКВ", НаименованиеВалюты(Content.Currency, Истина));
	УстановитьЗначениеXDTO(ДенИзм, "КурсВал", Content.CurrencyRate);
	УстановитьЗначениеXDTO(СвСНО, "ДенИзм", ДенИзм);
	
	// Продавец
	СвПрод = XML_ПолучитьПустойОбъектXDTO("УчастникТип", ИмяПакетаXDTO);
	XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, СвПрод, Content.Seller);
	УстановитьЗначениеXDTO(СвСНО, "СвПрод", СвПрод);
	
	// Покупатель
	СвПокуп = XML_ПолучитьПустойОбъектXDTO("УчастникТип", ИмяПакетаXDTO);
	XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, СвПокуп, Content.Buyer);
	УстановитьЗначениеXDTO(СвСНО, "СвПокуп", СвПокуп);
	
	// Плательщик
	Если ЗначениеЗаполнено(Content.Payer.Name) Тогда 
		
		СвПлат = XML_ПолучитьПустойОбъектXDTO("УчастникТип", ИмяПакетаXDTO);
		XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, СвПлат, Content.Payer);
		УстановитьЗначениеXDTO(СвСНО, "СвПлат", СвПлат);
		
	КонецЕсли;
	
	// Получатель платежа
	Если ЗначениеЗаполнено(Content.Recipient.Name) Тогда 
		
		СвПолуч = XML_ПолучитьПустойОбъектXDTO("УчастникТип", ИмяПакетаXDTO);
		XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, СвПолуч, Content.Recipient);
		УстановитьЗначениеXDTO(СвСНО, "СвПолуч", СвПолуч);
		
	КонецЕсли;
	
	// Договор
	Если ЗначениеЗаполнено(Content.Contract.Name) Тогда
		
		Договор = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.СвСНО.Договор", ИмяПакетаXDTO);
		УстановитьЗначениеXDTO(Договор, "НаимДог"	, Content.Contract.Name);
		УстановитьЗначениеXDTO(Договор, "НомерДог"	, Content.Contract.Number);
		УстановитьЗначениеXDTO(Договор, "ДатаДог"	, Дата_В_Формат(Content.Contract.Date, ФорматДаты));
		
		УстановитьЗначениеXDTO(СвСНО, "Договор", Договор);
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Документ, "СвСНО", СвСНО);
	
	// Доп. сведения
	XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, Документ.ИнфПол, Content.StructedAdditionalInfos);
	
КонецПроцедуры

Процедура XML_Счет_ЗаполнитьСведенияТабЧасти(ИмяПакетаXDTO, Документ, Content, ПараметрыФормированияФайла)
	
	ProformaInvoiceTable = Content.ProformaInvoiceTable;
	Items				 = ProformaInvoiceTable.Items;
	
	ТаблСНО = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблСНО", ИмяПакетаXDTO);
	
	// Лицевые счета
	СведЛицСчет = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблСНО.СведЛицСчет", ИмяПакетаXDTO);
	Если ЗначениеЗаполнено(Content.PersonalAccountInfo) Тогда
		XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, СведЛицСчет.ЛицСчетИнф, Content.PersonalAccountInfo, "ЛицСчетИнфТип");
	КонецЕсли;
	УстановитьЗначениеXDTO(ТаблСНО, "СведЛицСчет", СведЛицСчет);
	
	Если ЗначениеЗаполнено(Items) Тогда
		
		НомерСтроки		 = 0;
		ВсеСтрокиБезНДС	 = Истина;
		
		Для Каждого Item Из Items Цикл
			
			НомерСтроки = НомерСтроки + 1;
			
			СведТов = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблСНО.СведТов", ИмяПакетаXDTO);
			
			УстановитьЗначениеXDTO(СведТов, "НомСтр", НомерСтроки);
			
			XML_Счет_ЗаполнитьСтрокуТабЧасти(ИмяПакетаXDTO, СведТов, Item, ВсеСтрокиБезНДС, ПараметрыФормированияФайла);
			
			ТаблСНО.СведТов.Добавить(СведТов);
			
		КонецЦикла;
		
		// Итоги
		ВсегоОпл	 = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблСНО.ВсегоОпл", ИмяПакетаXDTO);
		СумНалВсего	 = XML_ПолучитьПустойОбъектXDTO("СумНДСТип", ИмяПакетаXDTO);
		
		УстановитьЗначениеXDTO(ВсегоОпл, "СтТовБезНДСВсего"	, XML_ФорматЧисла(ProformaInvoiceTable.TotalWithVatExcluded, 2));
		УстановитьЗначениеXDTO(ВсегоОпл, "СтТовУчНалВсего"	, XML_ФорматЧисла(ProformaInvoiceTable.Total, 2));
		
		Если ВсеСтрокиБезНДС Тогда
			УстановитьЗначениеXDTO(СумНалВсего, "БезНДС", СтрокаБезНДС());
		Иначе
			УстановитьЗначениеXDTO(СумНалВсего, "СумНал", XML_ФорматЧисла(ProformaInvoiceTable.TotalVat, 2));
		КонецЕсли;
		УстановитьЗначениеXDTO(ВсегоОпл, "СумНалВсего", СумНалВсего);
		
		// Итого к оплате, с учетом дополнительных начислений, перерасчетов и оплат
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ProformaInvoiceTable, "GrandTotal") Тогда
			УстановитьЗначениеXDTO(ВсегоОпл, "Итого", XML_ФорматЧисла(ProformaInvoiceTable.GrandTotal, 2));
		КонецЕсли;
		
		УстановитьЗначениеXDTO(ТаблСНО, "ВсегоОпл", ВсегоОпл);
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Документ, "ТаблСНО", ТаблСНО);
	
КонецПроцедуры

Процедура XML_Счет_ЗаполнитьСтрокуТабЧасти(ИмяПакетаXDTO, СведТов, ProformaInvoiceItem, ВсеСтрокиБезНДС, ПараметрыФормированияФайла)
	
	УстановитьЗначениеXDTO(СведТов, "НаимТов"	, ProformaInvoiceItem.Product);
	УстановитьЗначениеXDTO(СведТов, "НаимЕдИзм"	, ProformaInvoiceItem.UnitName);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ProformaInvoiceItem, "Unit") Тогда
		УстановитьЗначениеXDTO(СведТов, "ОКЕИ_Тов", ProformaInvoiceItem.Unit);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "КолТов"	, XML_ФорматЧисла(ProformaInvoiceItem.Quantity	, ПараметрыФормированияФайла.ТочностьКоличества));
	УстановитьЗначениеXDTO(СведТов, "ЦенаТов"	, XML_ФорматЧисла(ProformaInvoiceItem.Price		, ПараметрыФормированияФайла.ТочностьЦены));
	
	УстановитьЗначениеXDTO(СведТов, "СтТовБезНДС"	, XML_ФорматЧисла(ProformaInvoiceItem.SubtotalWithVatExcluded, 2));
	УстановитьЗначениеXDTO(СведТов, "НалСт"			, НалСт_Из_TaxRate(ProformaInvoiceItem.TaxRate));
	УстановитьЗначениеXDTO(СведТов, "СтТовУчНал"	, XML_ФорматЧисла(ProformaInvoiceItem.Subtotal, 2));
	
	// Акциз
	СумАкциз = XML_ПолучитьПустойОбъектXDTO("СумАкцизТип", ИмяПакетаXDTO);
	
	Если НЕ ЗначениеЗаполнено(ProformaInvoiceItem.Excise) ИЛИ Число(ProformaInvoiceItem.Excise) = 0 Тогда
		УстановитьЗначениеXDTO(СумАкциз, "БезАкциз", "без акциза");
	Иначе
		УстановитьЗначениеXDTO(СумАкциз, "СумАкциз", XML_ФорматЧисла(Число(ProformaInvoiceItem.Excise), 2));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "Акциз", СумАкциз);
	
	// НДС
	СумНал = XML_ПолучитьПустойОбъектXDTO("СумНДСТип", ИмяПакетаXDTO);
	
	Если TaxRate_ЭтоЗначениеБезНДС(ProformaInvoiceItem.TaxRate) Тогда
		УстановитьЗначениеXDTO(СумНал, "БезНДС", СтрокаБезНДС());
	Иначе
		ВсеСтрокиБезНДС = Ложь;
		УстановитьЗначениеXDTO(СумНал, "СумНал", XML_ФорматЧисла(ProformaInvoiceItem.Vat, 2));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СведТов, "СумНал", СумНал);
	
	// Доп. сведения
	XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, СведТов.ДопСведТов, ProformaInvoiceItem.StructedAdditionalInfos);
	
КонецПроцедуры

// XML: функции заполнения Торг_172

Процедура XML_ОтветныйТитул_ЗаполнитьСведенияПокупателя_Торг_172(ИмяПакетаXDTO
	, ОбъектXDTO
	, ПротоКонтент
	, НомерДокумента
	, ДатаДокумента)
	
	Фабрика		 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ФорматДаты	 = "ДФ=dd.MM.yyyy";
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "НаимПервДок", "Товарная накладная");
	УстановитьЗначениеXDTO(ОбъектXDTO, "ОКУДПервДок", "0330212");
	УстановитьЗначениеXDTO(ОбъектXDTO, "НомФорм"	, "ТОРГ-12");
	УстановитьЗначениеXDTO(ОбъектXDTO, "НомТН"		, НомерДокумента);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ДатаТН"		, Дата_В_Формат(ДатаДокумента, ФорматДаты));
	
	Если ЗначениеЗаполнено(ПротоКонтент.ShipmentReceiptDate) Тогда
		
		// Сведения по получению груза
		ПолучилГруз = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ПолучилГруз");
		
		УстановитьЗначениеXDTO(ПолучилГруз, "ДатаПолуч", Дата_В_Формат(ПротоКонтент.ShipmentReceiptDate, ФорматДаты));
		
		Если ЗначениеЗаполнено(ПротоКонтент.Accepter.Surname) Тогда
			
			ГрузПринял = НовыйДочернийОбъектXDTO(Фабрика, ПолучилГруз, "ГрузПринял");
			XML_ЗаполнитьДолжЛицоТип(ИмяПакетаXDTO, ГрузПринял, ПротоКонтент.Accepter);
			
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ПротоКонтент.Receiver.Surname) Тогда
			
			ГрузПолучил = НовыйДочернийОбъектXDTO(Фабрика, ПолучилГруз, "ГрузПолучил");
			XML_ЗаполнитьДолжЛицоТип(ИмяПакетаXDTO, ГрузПолучил, ПротоКонтент.Receiver);
			
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "Attorney") Тогда
			
			Доверенность = НовыйДочернийОбъектXDTO(Фабрика, ПолучилГруз, "Доверенность");
			XML_ДоверенностьТип(ИмяПакетаXDTO, Доверенность, ПротоКонтент.Attorney);
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "AdditionalInfo") И ЗначениеЗаполнено(ПротоКонтент.AdditionalInfo) Тогда
		ИнфПол = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ИнфПол");
		УстановитьЗначениеXDTO(ИнфПол, "ТекстИнф", ПротоКонтент.AdditionalInfo);
	КонецЕсли;
	
КонецПроцедуры

// XML: функции заполнения Акт_172

Процедура XML_ОтветныйТитул_ЗаполнитьСведенияПокупателя_Акт_172(ИмяПакетаXDTO
	, ОбъектXDTO
	, ПротоКонтент
	, НомерДокумента
	, ДатаДокумента)
	
	Фабрика		 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ФорматДаты	 = "ДФ=dd.MM.yyyy";
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "НаимПервДок", "Акт о выполнении работ (оказании услуг)");
	
	// Сведения акта о выполнении работ (оказании услуг) - титул исполнителя
	СвАктИ = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "СвАктИ");
	УстановитьЗначениеXDTO(СвАктИ, "НомАкт"	, НомерДокумента);
	УстановитьЗначениеXDTO(СвАктИ, "ДатаАкт", Дата_В_Формат(ДатаДокумента, ФорматДаты));
	
	// Принял
	Принял = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "Принял");
	УстановитьЗначениеXDTO(Принял, "ДатаЗаказ", Дата_В_Формат(ПротоКонтент.SignatureDate, ФорматДаты));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "Complaints") Тогда
		
		// Претензии
		УстановитьЗначениеXDTO(Принял, "Претенз", ПротоКонтент.Complaints);
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПротоКонтент.Official.Surname) Тогда
		
		// Должностное лицо, представляющее заказчика работ (услуг)
		ПодписьЗаказ = НовыйДочернийОбъектXDTO(Фабрика, Принял, "ПодписьЗаказ");
		XML_ЗаполнитьДолжЛицоТип(ИмяПакетаXDTO, ПодписьЗаказ, ПротоКонтент.Official);
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "Attorney") Тогда
		
		// Сведения о доверенности подписывающего со стороны заказчика работ (услуг)
		ДоверенЗаказ = НовыйДочернийОбъектXDTO(Фабрика, Принял, "ДоверенЗаказ");
		XML_ДоверенностьТип(ИмяПакетаXDTO, ДоверенЗаказ, ПротоКонтент.Attorney);
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "AdditionalInfo") И ЗначениеЗаполнено(ПротоКонтент.AdditionalInfo) Тогда
		
		// Информационное поле
		ИнфПол = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ИнфПол");
		УстановитьЗначениеXDTO(ИнфПол, "ТекстИнф", ПротоКонтент.AdditionalInfo);
		
	КонецЕсли;
	
КонецПроцедуры

// XML: функции заполнения Торг2

Функция XML_Торг2_ДопПрефиксИмениФайла(ПротоКонтент)
	
	Результат = "";
	
	ПроверяемыеСтруктуры = Новый Массив;
	ПроверяемыеСтруктуры.Добавить("AccordingToSellerDocument");
	ПроверяемыеСтруктуры.Добавить("ActuallyAccepted");
	ПроверяемыеСтруктуры.Добавить("Breakage");
	ПроверяемыеСтруктуры.Добавить("Surplus");
	ПроверяемыеСтруктуры.Добавить("Shortfall");
	ПроверяемыеСтруктуры.Добавить("Reject");
	
	Для Каждого AcceptanceResult Из ПротоКонтент.EvidenceAcceptanceInfo.AcceptanceResults.AcceptanceResult Цикл
		
		Для Каждого Структура Из ПроверяемыеСтруктуры Цикл
			
			Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptanceResult, Структура)
				И ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptanceResult[Структура], "ItemIdentificationNumbers")
				И AcceptanceResult[Структура].ItemIdentificationNumbers.ItemIdentificationNumber.Количество() > 0 Тогда
				
				Результат = "MARK";
				Прервать;
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура XML_Торг2_ЗаполнитьСведенияОбОбстоятельствахПриемки(ИмяПакетаXDTO, СодФХЖ1, CircumstancesAcceptanceInfo, ПараметрыФормированияФайла)
	
	ФорматДаты		 = "ДФ=dd.MM.yyyy";
	ФорматДатаВремя	 = "ДФ='dd.MM.yyyy HH.mm.ss'";
	Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Продавец = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "Продавец");
	XML_Торг2_ЗаполнитьУчастникТип(ИмяПакетаXDTO, Продавец, CircumstancesAcceptanceInfo.Seller);
	
	Покупатель = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "Покупатель");
	XML_Торг2_ЗаполнитьУчастникТип(ИмяПакетаXDTO, Покупатель, CircumstancesAcceptanceInfo.Buyer);
	
	Если ЗначениеЗаполнено(CircumstancesAcceptanceInfo.Shipper.OrganizationDetails.OrgName) Тогда
		Грузоотправитель = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "Грузоотправитель");
		XML_Торг2_ЗаполнитьУчастникТип(ИмяПакетаXDTO, Грузоотправитель, CircumstancesAcceptanceInfo.Shipper);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(CircumstancesAcceptanceInfo.Consignee.OrganizationDetails.OrgName) Тогда
		Грузополучатель = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "Грузополучатель");
		XML_Торг2_ЗаполнитьУчастникТип(ИмяПакетаXDTO, Грузополучатель, CircumstancesAcceptanceInfo.Consignee);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(CircumstancesAcceptanceInfo.InsuranceCompany.OrganizationDetails.OrgName) Тогда
		СтрахКом = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "СтрахКом");
		XML_Торг2_ЗаполнитьУчастникТип(ИмяПакетаXDTO, СтрахКом, CircumstancesAcceptanceInfo.InsuranceCompany);
	КонецЕсли;
	
	// Дата и номер приказа (распоряжения) о назначении комиссии
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(CircumstancesAcceptanceInfo, "Order") И ЗначениеЗаполнено(CircumstancesAcceptanceInfo.Order.Date) Тогда
		Приказ = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "Приказ");
		УстановитьЗначениеXDTO(Приказ, "ДатаПрик", Дата_В_Формат(CircumstancesAcceptanceInfo.Order.Date, ФорматДаты));
		УстановитьЗначениеXDTO(Приказ, "НомПрик", CircumstancesAcceptanceInfo.Order.Number);
	КонецЕсли;
	
	// Сведения о событиях, связанных с осмотром груза (о результатах осмотра прибывшего груза)
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(CircumstancesAcceptanceInfo, "CargoInspectionInformation") Тогда
		СвОсмГруз = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "СвОсмГруз");
		XML_Торг2_ЗаполнитьСведенияОРезультатахОсмотра(ИмяПакетаXDTO, СвОсмГруз, CircumstancesAcceptanceInfo.CargoInspectionInformation);
	КонецЕсли;
	
	// Сведения о грузе по сопроводительным транспортным документам
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(CircumstancesAcceptanceInfo, "AccompanyingDocumentTable") Тогда
		
		Для Каждого Item Из CircumstancesAcceptanceInfo.AccompanyingDocumentTable.Item Цикл
			СвСопрДок = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "СвСопрДок");
			XML_Торг2_ЗаполнитьСведенияОГрузе(ИмяПакетаXDTO, СвСопрДок, Item, ПараметрыФормированияФайла);
		КонецЦикла;
		
	КонецЕсли;
	
	// Сведения о дате и времени событий, связанных с приемкой груза
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(CircumstancesAcceptanceInfo, "ReceptionInformation") Тогда
		
		ReceptionInformation = CircumstancesAcceptanceInfo.ReceptionInformation;
		
		ПрибГруз = ПолучитьЗначениеXDTO(ReceptionInformation, "CargoArrivalDateTime");
		ВыдачГруз = ПолучитьЗначениеXDTO(ReceptionInformation, "DeliveryPoint");
		ВскрытГруз = ПолучитьЗначениеXDTO(ReceptionInformation, "CargoOpeningDateTime");
		ДостГруз = ПолучитьЗначениеXDTO(ReceptionInformation, "DeliveryDateTime");
		
		Если ЗначениеЗаполнено(ПрибГруз)
			Или ЗначениеЗаполнено(ВыдачГруз)
			Или ЗначениеЗаполнено(ВскрытГруз)
			Или ЗначениеЗаполнено(ДостГруз) Тогда
			
			СвВремПрием = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "СвВремПрием");
			
			УстановитьЗначениеXDTO(СвВремПрием, "ПрибГруз", Дата_В_Формат(ПрибГруз, ФорматДатаВремя));
			УстановитьЗначениеXDTO(СвВремПрием, "ВыдачГруз", Дата_В_Формат(ВыдачГруз, ФорматДатаВремя));
			УстановитьЗначениеXDTO(СвВремПрием, "ВскрытГруз", Дата_В_Формат(ВскрытГруз, ФорматДатаВремя));
			УстановитьЗначениеXDTO(СвВремПрием, "ДостГруз", Дата_В_Формат(ДостГруз, ФорматДатаВремя));
			
		КонецЕсли;
		
	КонецЕсли;
	
	// Другие обстоятельства приемки ценностей
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(CircumstancesAcceptanceInfo, "OtherCircumstancesAcceptanceInfo")
		И ОбъектXDTOЗаполнен(CircumstancesAcceptanceInfo.OtherCircumstancesAcceptanceInfo) Тогда
		OtherCircumstancesAcceptanceInfo = CircumstancesAcceptanceInfo.OtherCircumstancesAcceptanceInfo;
		ДрОбстПрием = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "ДрОбстПрием");
		XML_Торг2_ЗаполнитьДругиеОбстоятельстваПриемки(ИмяПакетаXDTO, ДрОбстПрием, OtherCircumstancesAcceptanceInfo);
	КонецЕсли;
	
	// Сведения о лице, принявшем товар (получившем груз) (в том числе на ответственное хранение)
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(CircumstancesAcceptanceInfo, "PersonAcceptedCargoInfo") Тогда
		
		СвЛицПрин = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "СвЛицПрин");
		PersonAcceptedCargoInfo = CircumstancesAcceptanceInfo.PersonAcceptedCargoInfo;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(PersonAcceptedCargoInfo, "Employee")
			И ЗначениеЗаполнено(PersonAcceptedCargoInfo.Employee.Position) Тогда
			
			РабОргПок = НовыйДочернийОбъектXDTO(Фабрика, СвЛицПрин, "РабОргПок");
			XML_ЗаполнитьРабОргПок(ИмяПакетаXDTO, РабОргПок, PersonAcceptedCargoInfo.Employee);
			
		Иначе
			ИнЛицо = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "ИнЛицо");
			XML_ЗаполнитьИнЛицо(ИмяПакетаXDTO, ИнЛицо, PersonAcceptedCargoInfo.OtherIssuer);
		КонецЕсли;
		
	КонецЕсли;
	
	// Информационное поле события (факта хозяйственной жизни) 1
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(CircumstancesAcceptanceInfo, "AdditionalInfoId") Тогда
		ИнфПолФХЖ1 = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ1, "ИнфПолФХЖ1");
		XML_ЗаполнитьИнфПолТип(ИмяПакетаXDTO, ИнфПолФХЖ1, CircumstancesAcceptanceInfo.AdditionalInfoId);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СодФХЖ1, "ОбстИсп", ПолучитьЗначениеXDTO(CircumstancesAcceptanceInfo, "CircumFormat"));
	УстановитьЗначениеXDTO(СодФХЖ1, "ИдГосКон", ПолучитьЗначениеXDTO(CircumstancesAcceptanceInfo, "GovernmentContractInfo"));
	
КонецПроцедуры

Процедура XML_Торг2_ЗаполнитьСведенияОРезультатахОсмотра(ИмяПакетаXDTO, СвОсмГруз, CargoInspectionInformation)
	
	ФорматДаты	 = "ДФ=dd.MM.yyyy";
	Фабрика		 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	// Номер сертификата соответствия
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(CargoInspectionInformation, "CertificateConformityNumbers") Тогда
		Для Каждого CertificateConformityNumber Из CargoInspectionInformation.CertificateConformityNumbers.CertificateConformityNumber Цикл
			СвОсмГруз.СертСоотв.Добавить(CertificateConformityNumber);
		КонецЦикла;
	КонецЕсли;
	
	// Наименование, номер и дата сопроводительного документа (идентификатор сопроводительного документа)
	СопрДок = НовыйДочернийОбъектXDTO(Фабрика, СвОсмГруз, "СопрДок");
	XML_ЗаполнитьСопрДокТип(СопрДок, CargoInspectionInformation.AccompanyingDocumentInfo);
	
	// Информационное поле сведений о результатах осмотра
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(CargoInspectionInformation, "AdditionalInfoId")
		И ОбъектXDTOЗаполнен(CargoInspectionInformation.AdditionalInfoId) Тогда
		ИнфПолСвОсм = НовыйДочернийОбъектXDTO(Фабрика, СвОсмГруз, "ИнфПолСвОсм");
		XML_ЗаполнитьИнфПолТип(ИмяПакетаXDTO, ИнфПолСвОсм, CargoInspectionInformation.AdditionalInfoId);
	КонецЕсли;
	
	ДатаОсм = ПолучитьЗначениеXDTO(CargoInspectionInformation, "ReceptionDate");
	Если ЗначениеЗаполнено(ДатаОсм) Тогда
		УстановитьЗначениеXDTO(СвОсмГруз, "ДатаОсм", Дата_В_Формат(ДатаОсм, ФорматДаты));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СвОсмГруз, "МестоСост", ПолучитьЗначениеXDTO(CargoInspectionInformation, "ReceptionPlace"));
	УстановитьЗначениеXDTO(СвОсмГруз, "ВремяНач", ПолучитьЗначениеXDTO(CargoInspectionInformation, "ReceptionStartTime"));
	УстановитьЗначениеXDTO(СвОсмГруз, "ВремяОконч", ПолучитьЗначениеXDTO(CargoInspectionInformation, "ReceptionFinishTime"));
	
	ДатаОтпр = ПолучитьЗначениеXDTO(CargoInspectionInformation, "DeliveryDate");
	Если ЗначениеЗаполнено(ДатаОтпр) Тогда
		УстановитьЗначениеXDTO(СвОсмГруз, "ДатаОтпр", Дата_В_Формат(ДатаОтпр, ФорматДаты));
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_Торг2_ЗаполнитьСведенияОГрузе(ИмяПакетаXDTO, СвСопрДок, Item, ПараметрыФормированияФайла)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Item, "DocumentWeight") Тогда
		МассаДок = НовыйДочернийОбъектXDTO(Фабрика, СвСопрДок, "МассаДок");
		XML_ЗаполнитьМасГрузТип(МассаДок, Item.DocumentWeight, ПараметрыФормированияФайла);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Item, "FactWeight") Тогда
		МассаФакт = НовыйДочернийОбъектXDTO(Фабрика, СвСопрДок, "МассаФакт");
		XML_ЗаполнитьМасГрузТип(МассаФакт, Item.FactWeight, ПараметрыФормированияФайла);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Item, "AdditionalInfo") И ЗначениеЗаполнено(Item.AdditionalInfo.Id) Тогда
		ИнфПолСопрДок = НовыйДочернийОбъектXDTO(Фабрика, СвСопрДок, "ИнфПолСопрДок");
		XML_ЗаполнитьТекстИнфТип(ИнфПолСопрДок, Item.AdditionalInfo);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СвСопрДок, "ОтмПломб", ПолучитьЗначениеXDTO(Item, "SealingMark"));
	УстановитьЗначениеXDTO(СвСопрДок, "ВидУпак", ПолучитьЗначениеXDTO(Item, "PackingType"));
	УстановитьЗначениеXDTO(СвСопрДок, "КолМест", ПолучитьЗначениеXDTO(Item, "Quantity"));
	УстановитьЗначениеXDTO(СвСопрДок, "НаимГруз", Item.CargoName);
	УстановитьЗначениеXDTO(СвСопрДок, "ОКЕИ_Тов", ПолучитьЗначениеXDTO(Item, "Unit"));
	УстановитьЗначениеXDTO(СвСопрДок, "НаимЕдИзм", ПолучитьЗначениеXDTO(Item, "UnitName"));
	
КонецПроцедуры

Процедура XML_Торг2_ЗаполнитьДругиеОбстоятельстваПриемки(ИмяПакетаXDTO, ДрОбстПрием, OtherCircumstancesAcceptanceInfo)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(OtherCircumstancesAcceptanceInfo, "Carrier") Тогда
		Перевозчик = НовыйДочернийОбъектXDTO(Фабрика, ДрОбстПрием, "Перевозчик");
		XML_Торг2_ЗаполнитьУчастникТип(ИмяПакетаXDTO, Перевозчик, OtherCircumstancesAcceptanceInfo.Carrier);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(OtherCircumstancesAcceptanceInfo, "AccompanyingTransferDocumentInfo") Тогда
		СопрТрансДок = НовыйДочернийОбъектXDTO(Фабрика, ДрОбстПрием, "СопрТрансДок");
		XML_ЗаполнитьСопрДокТип(СопрТрансДок, OtherCircumstancesAcceptanceInfo.AccompanyingTransferDocumentInfo);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(ДрОбстПрием, "УсловХран", ПолучитьЗначениеXDTO(OtherCircumstancesAcceptanceInfo, "StorageConditions"));
	УстановитьЗначениеXDTO(ДрОбстПрием, "СостТара", ПолучитьЗначениеXDTO(OtherCircumstancesAcceptanceInfo, "CargoActualState"));
	УстановитьЗначениеXDTO(ДрОбстПрием, "ОпредНедост", ПолучитьЗначениеXDTO(OtherCircumstancesAcceptanceInfo, "MissingAssets"));
	УстановитьЗначениеXDTO(ДрОбстПрием, "ДопСв", ПолучитьЗначениеXDTO(OtherCircumstancesAcceptanceInfo, "AdditionalInfo"));
	
КонецПроцедуры

Процедура XML_Торг2_ЗаполнитьСведенияОФактеПриемки(ИмяПакетаXDTO, СодФХЖ2, EvidenceAcceptanceInfo, ПараметрыФормированияФайла)
	
	ФорматДаты	 = "ДФ=dd.MM.yyyy";
	Фабрика		 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	// Результаты приемки ценностей (груза)
	Для Каждого AcceptanceResult Из EvidenceAcceptanceInfo.AcceptanceResults.AcceptanceResult Цикл
		РезПрием = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ2, "РезПрием");
		XML_Торг2_ЗаполнитьРезультатыПриемкиЦенностей(ИмяПакетаXDTO, РезПрием, AcceptanceResult, ПараметрыФормированияФайла);
	КонецЦикла;
	
	// Прилагаемые документы
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(EvidenceAcceptanceInfo, "AttachedDocuments") Тогда
		
		Для Каждого AttachedDocument Из EvidenceAcceptanceInfo.AttachedDocuments.AttachedDocument Цикл
			
			ПрилДок = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ2, "ПрилДок");
			
			УстановитьЗначениеXDTO(ПрилДок, "ВидПрилДок", AttachedDocument.Type);
			УстановитьЗначениеXDTO(ПрилДок, "НомерПрилДок", AttachedDocument.Number);
			УстановитьЗначениеXDTO(ПрилДок, "ДатаПрилДок", Дата_В_Формат(AttachedDocument.Date, ФорматДаты));
			
		КонецЦикла;
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СодФХЖ2, "ВидОперации", ПолучитьЗначениеXDTO(EvidenceAcceptanceInfo, "OperationType"));
	
	// Информационное поле события (факта хозяйственной жизни) 2
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(EvidenceAcceptanceInfo, "AdditionalInfos") Тогда
		
		Для Каждого AdditionalInfo Из EvidenceAcceptanceInfo.AdditionalInfos.AdditionalInfo Цикл
			ИнфПолФХЖ2 = НовыйДочернийОбъектXDTO(Фабрика, СодФХЖ2, "ИнфПолФХЖ2");
			XML_ЗаполнитьТекстИнфТип(ИнфПолФХЖ2, AdditionalInfo);
		КонецЦикла;
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СодФХЖ2, "СодСоб", EvidenceAcceptanceInfo.EventSummary);
	УстановитьЗначениеXDTO(СодФХЖ2, "Заключение", ПолучитьЗначениеXDTO(EvidenceAcceptanceInfo, "Conclusion"));
	
КонецПроцедуры

Процедура XML_Торг2_ЗаполнитьРезультатыПриемкиЦенностей(ИмяПакетаXDTO, РезПрием, AcceptanceResult, ПараметрыФормированияФайла)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	// Ценности
	Ценности = НовыйДочернийОбъектXDTO(Фабрика, РезПрием, "Ценности");
	Assets = AcceptanceResult.Assets;
	
	УстановитьЗначениеXDTO(Ценности, "НомСтрСопрДок", ПолучитьЗначениеXDTO(Assets, "AccompanyingDocumentLineNumber"));
	УстановитьЗначениеXDTO(Ценности, "НаимТов", ПолучитьЗначениеXDTO(Assets, "Product"));
	УстановитьЗначениеXDTO(Ценности, "ХарактТов", ПолучитьЗначениеXDTO(Assets, "ItemCharact"));
	УстановитьЗначениеXDTO(Ценности, "СортТов", ПолучитьЗначениеXDTO(Assets, "ItemKind"));
	УстановитьЗначениеXDTO(Ценности, "АртикулТов", ПолучитьЗначениеXDTO(Assets, "ItemArticle"));
	УстановитьЗначениеXDTO(Ценности, "КодТов", ПолучитьЗначениеXDTO(Assets, "ItemVendorCode"));
	УстановитьЗначениеXDTO(Ценности, "Паспорт", ПолучитьЗначениеXDTO(Assets, "PassportNumber"));
	
	// Информация по документам поставщика
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptanceResult, "AccordingToSellerDocument")
		И ЗначениеЗаполнено(AcceptanceResult.AccordingToSellerDocument.Quantity) Тогда
		
		ПоДокум = НовыйДочернийОбъектXDTO(Фабрика, РезПрием, "ПоДокум");
		XML_ЗаполнитьПринТовТип(
			ИмяПакетаXDTO,
			ПоДокум,
			AcceptanceResult.AccordingToSellerDocument,
			ПараметрыФормированияФайла
		);
		
	КонецЕсли;
	
	// Фактически
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptanceResult, "ActuallyAccepted")
		И ЗначениеЗаполнено(AcceptanceResult.ActuallyAccepted.Quantity) Тогда
		
		ПоФакту = НовыйДочернийОбъектXDTO(Фабрика, РезПрием, "ПоФакту");
		XML_ЗаполнитьПринТовТип(ИмяПакетаXDTO, ПоФакту, AcceptanceResult.ActuallyAccepted, ПараметрыФормированияФайла);
		
	КонецЕсли;
	
	// Брак
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptanceResult, "Reject")
		И ЗначениеЗаполнено(AcceptanceResult.Reject.Quantity) Тогда
		
		Брак = НовыйДочернийОбъектXDTO(Фабрика, РезПрием, "Брак");
		XML_ЗаполнитьРасхТип(ИмяПакетаXDTO, Брак, AcceptanceResult.Reject, ПараметрыФормированияФайла);
		
	КонецЕсли;
	
	// Бой
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptanceResult, "Breakage")
		И ЗначениеЗаполнено(AcceptanceResult.Breakage.Quantity) Тогда
		
		Бой = НовыйДочернийОбъектXDTO(Фабрика, РезПрием, "Бой");
		XML_ЗаполнитьРасхТип(ИмяПакетаXDTO, Бой, AcceptanceResult.Breakage, ПараметрыФормированияФайла);
		
	КонецЕсли;
	
	// Недостача
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptanceResult, "Shortfall")
		И ЗначениеЗаполнено(AcceptanceResult.Shortfall.Quantity) Тогда
		
		Недостача = НовыйДочернийОбъектXDTO(Фабрика, РезПрием, "Недостача");
		XML_ЗаполнитьРасхТип(ИмяПакетаXDTO, Недостача, AcceptanceResult.Shortfall, ПараметрыФормированияФайла);
		
	КонецЕсли;
	
	// Излишки
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptanceResult, "Surplus")
		И ЗначениеЗаполнено(AcceptanceResult.Surplus.Quantity) Тогда
		
		Излишки = НовыйДочернийОбъектXDTO(Фабрика, РезПрием, "Излишки");
		XML_ЗаполнитьРасхТип(ИмяПакетаXDTO, Излишки, AcceptanceResult.Surplus, ПараметрыФормированияФайла);
		
	КонецЕсли;
	
	// Ценовые отклонения
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptanceResult, "PriceDeviations") Тогда
		
		PriceDeviations = AcceptanceResult.PriceDeviations;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(PriceDeviations, "PositiveDeviations")
			И ЗначениеЗаполнено(PriceDeviations.PositiveDeviations.Subtotal) Тогда
			
			ЦенОтклон = НовыйДочернийОбъектXDTO(Фабрика, РезПрием, "ЦенОтклон");
			ПолДельта = НовыйДочернийОбъектXDTO(Фабрика, ЦенОтклон, "ПолДельта");
			XML_ЗаполнитьДельтаТип(ИмяПакетаXDTO, ПолДельта, PriceDeviations.PositiveDeviations, ПараметрыФормированияФайла);
			
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(PriceDeviations, "NegativeDeviations")
			И ЗначениеЗаполнено(PriceDeviations.NegativeDeviations.Subtotal) Тогда
			
			ЦенОтклон = НовыйДочернийОбъектXDTO(Фабрика, РезПрием, "ЦенОтклон");
			ОтрицатДельта = НовыйДочернийОбъектXDTO(Фабрика, ЦенОтклон, "ОтрицатДельта");
			XML_ЗаполнитьДельтаТип(ИмяПакетаXDTO, ОтрицатДельта, PriceDeviations.NegativeDeviations, ПараметрыФормированияФайла);
			
		КонецЕсли;
		
	КонецЕсли;
	
	// Информационное поле сведений о результатах приемки ценностей (груза)
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptanceResult, "AdditionalInfos") Тогда
		
		Для Каждого AdditionalInfo Из AcceptanceResult.AdditionalInfos.AdditionalInfo Цикл
			ИнфПолРезПрием = НовыйДочернийОбъектXDTO(Фабрика, РезПрием, "ИнфПолРезПрием");
			XML_ЗаполнитьТекстИнфТип(ИнфПолРезПрием, AdditionalInfo);
		КонецЦикла;
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(РезПрием, "ОКЕИ_Тов", ПолучитьЗначениеXDTO(AcceptanceResult, "Unit"));
	УстановитьЗначениеXDTO(РезПрием, "РасхРабУслПрав", ПолучитьЗначениеXDTO(AcceptanceResult, "AcceptanceDiscrepancyInfo"));
	УстановитьЗначениеXDTO(РезПрием, "НаимЕдИзм", ПолучитьЗначениеXDTO(AcceptanceResult, "UnitName"));
	
КонецПроцедуры

Процедура XML_Торг2_ЗаполнитьПодписантов(ИмяПакетаXDTO, Документ, Signers)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого Signer Из Signers Цикл
		
		Подписант = НовыйДочернийОбъектXDTO(Фабрика, Документ, "Подписант");
		
		SignerDetails = Signer.SignerDetails; // см. ExtendedSignerDetailsTorg2
		
		Если SignerDetails.SignerType = "LegalEntity" Тогда
			
			ЮЛ = НовыйДочернийОбъектXDTO(Фабрика, Подписант, "ЮЛ");
			
			ФИО = НовыйДочернийОбъектXDTO(Фабрика, ЮЛ, "ФИО");
			
			УстановитьЗначениеXDTO(ФИО, "Фамилия", SignerDetails.LastName);
			УстановитьЗначениеXDTO(ФИО, "Имя", SignerDetails.FirstName);
			УстановитьЗначениеXDTO(ФИО, "Отчество", ПолучитьЗначениеXDTO(SignerDetails, "MiddleName"));
			
			УстановитьЗначениеXDTO(ЮЛ, "ИННЮЛ", SignerDetails.Inn);
			УстановитьЗначениеXDTO(ЮЛ, "НаимОрг", ПолучитьЗначениеXDTO(SignerDetails, "SignerOrganizationName"));
			УстановитьЗначениеXDTO(ЮЛ, "Должн", SignerDetails.Position);
			УстановитьЗначениеXDTO(ЮЛ, "ИныеСвед", ПолучитьЗначениеXDTO(SignerDetails, "SignerInfo"));
			
		ИначеЕсли SignerDetails.SignerType = "IndividualEntity" Тогда
			
			ИП = НовыйДочернийОбъектXDTO(Фабрика, Подписант, "ИП");
			
			ФИО = НовыйДочернийОбъектXDTO(Фабрика, ИП, "ФИО");
			
			УстановитьЗначениеXDTO(ФИО, "Фамилия", SignerDetails.LastName);
			УстановитьЗначениеXDTO(ФИО, "Имя", SignerDetails.FirstName);
			УстановитьЗначениеXDTO(ФИО, "Отчество", ПолучитьЗначениеXDTO(SignerDetails, "MiddleName"));
			
			УстановитьЗначениеXDTO(ИП, "ИННФЛ", SignerDetails.Inn);
			УстановитьЗначениеXDTO(ИП, "СвГосРегИП", ПолучитьЗначениеXDTO(SignerDetails, "RegistrationCertificate"));
			УстановитьЗначениеXDTO(ИП, "ИныеСвед", ПолучитьЗначениеXDTO(SignerDetails, "SignerInfo"));
			
		Иначе
			
			ФЛ = НовыйДочернийОбъектXDTO(Фабрика, Подписант, "ФЛ");
			
			ФИО = НовыйДочернийОбъектXDTO(Фабрика, ФЛ, "ФИО");
			
			УстановитьЗначениеXDTO(ФИО, "Фамилия", SignerDetails.LastName);
			УстановитьЗначениеXDTO(ФИО, "Имя", SignerDetails.FirstName);
			УстановитьЗначениеXDTO(ФИО, "Отчество", ПолучитьЗначениеXDTO(SignerDetails, "MiddleName"));
			
			УстановитьЗначениеXDTO(ФЛ, "ИННФЛ", ПолучитьЗначениеXDTO(SignerDetails, "Inn"));
			УстановитьЗначениеXDTO(ФЛ, "ИныеСвед", ПолучитьЗначениеXDTO(SignerDetails, "SignerInfo"));
			
		КонецЕсли;
		
		СтатусПодписанта = XML_СтатусПодписанта(SignerDetails.SignerStatus);
		ОбластьПолномочий = XML_ОбластьПолномочийПодписанта(SignerDetails.SignerPowers);
		
		УстановитьЗначениеXDTO(Подписант, "ОблПолн", ОбластьПолномочий);
		УстановитьЗначениеXDTO(Подписант, "Статус", СтатусПодписанта);
		УстановитьЗначениеXDTO(Подписант, "ОснПолн", SignerDetails.SignerPowersBase);
		УстановитьЗначениеXDTO(Подписант, "ОснПолнОрг", ПолучитьЗначениеXDTO(SignerDetails, "SignerOrgPowersBase"));
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_ОтветныйТитул_ЗаполнитьСведенияПоИтогамПриемки(ИмяПакетаXDTO, СвИтПрием, Torg2RecipientTitle)
	
	ФорматДаты	 = "ДФ=dd.MM.yyyy";
	Фабрика		 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Torg2RecipientTitle, "AdditionalInfoId") Тогда
		ИнфПол = НовыйДочернийОбъектXDTO(Фабрика, СвИтПрием, "ИнфПол");
		XML_ЗаполнитьИнфПолТип(ИмяПакетаXDTO, ИнфПол, Torg2RecipientTitle.AdditionalInfoId);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СвИтПрием, "ДатаСвИтПрием", Дата_В_Формат(Torg2RecipientTitle.CompilationInfoDate, ФорматДаты));
	УстановитьЗначениеXDTO(СвИтПрием, "СодСвИтПрием", Torg2RecipientTitle.TotalAcceptanceContent);
	
КонецПроцедуры

Процедура XML_Торг2_ЗаполнитьУчастникТип(ИмяПакетаXDTO, УчастникТип, ExtendedOrganizationInfo_Torg2)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ИдСв = НовыйДочернийОбъектXDTO(Фабрика, УчастникТип, "ИдСв");
	
	OrganizationDetails = ExtendedOrganizationInfo_Torg2.OrganizationDetails;
	OrgType = OrganizationDetails.OrgType;
	
	Если OrgType = "PhysicalEntity" Тогда
		
		// Сведения о физическом лице
		
		СвФЛ = НовыйДочернийОбъектXDTO(Фабрика, ИдСв, "СвФЛ");
		ФИО = НовыйДочернийОбъектXDTO(Фабрика, СвФЛ, "ФИО");
		СтруктураФИО = XML_ПротоСтруктураФИО(OrganizationDetails.OrgName);
		XML_ЗаполнитьФИОТип(ФИО, СтруктураФИО);
		
		УстановитьЗначениеXDTO(СвФЛ, "ИННФЛ", ПолучитьЗначениеXDTO(OrganizationDetails, "Inn"));
		УстановитьЗначениеXDTO(СвФЛ, "ИныеСвед", ПолучитьЗначениеXDTO(OrganizationDetails, "OrganizationOrPersonInfo"));
		
	ИначеЕсли OrgType = "IndividualEntity" Тогда
		
		// Сведения об индивидуальном предпринимателе
		
		СвИП = НовыйДочернийОбъектXDTO(Фабрика, ИдСв, "СвИП");
		ФИО = НовыйДочернийОбъектXDTO(Фабрика, СвИП, "ФИО");
		СтруктураФИО = XML_ПротоСтруктураФИО(OrganizationDetails.OrgName);
		XML_ЗаполнитьФИОТип(ФИО, СтруктураФИО);
		
		УстановитьЗначениеXDTO(СвИП, "ИННФЛ", OrganizationDetails.Inn);
		УстановитьЗначениеXDTO(СвИП, "СвГосРегИП", ПолучитьЗначениеXDTO(OrganizationDetails, "IndividualEntityRegistrationCertificate"));
		УстановитьЗначениеXDTO(СвИП, "ИныеСвед", ПолучитьЗначениеXDTO(OrganizationDetails, "OrganizationOrPersonInfo"));
		
	Иначе
		
		СвОрг = НовыйДочернийОбъектXDTO(Фабрика, ИдСв, "СвОрг");
		
		Если OrgType = "LegalEntity" Тогда
			
			// Сведения о юридическом лице, состоящем на учете в налоговых органах
			
			СвОргУч = НовыйДочернийОбъектXDTO(Фабрика, СвОрг, "СвОргУч");
			
			УстановитьЗначениеXDTO(СвОргУч, "ИННЮЛ", OrganizationDetails.Inn);
			УстановитьЗначениеXDTO(СвОргУч, "НаимОрг", OrganizationDetails.OrgName);
			УстановитьЗначениеXDTO(СвОргУч, "КПП", ПолучитьЗначениеXDTO(OrganizationDetails, "Kpp"));
			
		Иначе
			
			// Сведения об иностранном лице, не состоящем на учете в налоговых органах
			
			СвИнНеУч = НовыйДочернийОбъектXDTO(Фабрика, СвОрг, "СвИнНеУч");
			
			УстановитьЗначениеXDTO(СвИнНеУч, "НаимОрг", OrganizationDetails.OrgName);
			УстановитьЗначениеXDTO(СвИнНеУч, "Идентиф", ПолучитьЗначениеXDTO(OrganizationDetails, "LegalEntityId"));
			УстановитьЗначениеXDTO(СвИнНеУч, "ИныеСвед", ПолучитьЗначениеXDTO(OrganizationDetails, "OrganizationOrPersonInfo"));
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(OrganizationDetails, "Address") Тогда
		Адрес = НовыйДочернийОбъектXDTO(Фабрика, УчастникТип, "Адрес");
		XML_ЗаполнитьАдресТип(ИмяПакетаXDTO, Адрес, OrganizationDetails.Address);
	КонецЕсли;
	
	// Контактные данные
	Тлф = ПолучитьЗначениеXDTO(OrganizationDetails, "Phone");
	ЭлПочта = ПолучитьЗначениеXDTO(OrganizationDetails, "Email");
	Если ЗначениеЗаполнено(Тлф) Или ЗначениеЗаполнено(ЭлПочта) Тогда
		Контакт = НовыйДочернийОбъектXDTO(Фабрика, УчастникТип, "Контакт");
		УстановитьЗначениеXDTO(Контакт, "Тлф", Тлф);
		УстановитьЗначениеXDTO(Контакт, "ЭлПочта", ЭлПочта);
	КонецЕсли;
	
	// Банковские реквизиты
	НаимБанк = ПолучитьЗначениеXDTO(OrganizationDetails, "BankName");
	БИК = ПолучитьЗначениеXDTO(OrganizationDetails, "BankId");
	КорСчет = ПолучитьЗначениеXDTO(OrganizationDetails, "CorrespondentAccount");
	НомерСчета = ПолучитьЗначениеXDTO(OrganizationDetails, "BankAccountNumber");
	
	ЕстьСвБанк = ЗначениеЗаполнено(НаимБанк) Или ЗначениеЗаполнено(БИК) Или ЗначениеЗаполнено(КорСчет);
	
	Если ЕстьСвБанк Или ЗначениеЗаполнено(НомерСчета) Тогда
		
		БанкРекв = НовыйДочернийОбъектXDTO(Фабрика, УчастникТип, "БанкРекв");
		
		УстановитьЗначениеXDTO(БанкРекв, "НомерСчета", НомерСчета);
		
		Если ЕстьСвБанк Тогда
			
			СвБанк = НовыйДочернийОбъектXDTO(Фабрика, БанкРекв, "СвБанк");
			УстановитьЗначениеXDTO(СвБанк, "НаимБанк", НаимБанк);
			УстановитьЗначениеXDTO(СвБанк, "БИК", БИК);
			УстановитьЗначениеXDTO(СвБанк, "КорСчет", КорСчет);
			
		КонецЕсли;
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(УчастникТип, "ОКПО", ПолучитьЗначениеXDTO(OrganizationDetails, "Okpo"));
	УстановитьЗначениеXDTO(УчастникТип, "СтруктПодр", ПолучитьЗначениеXDTO(OrganizationDetails, "Department"));
	УстановитьЗначениеXDTO(УчастникТип, "ИнфДляУчаст", ПолучитьЗначениеXDTO(OrganizationDetails, "OrganizationAdditionalInfo"));
	
КонецПроцедуры

// XML: функции заполнения Акта сверки 405

Процедура XML_АктСверки_ЗаполнитьСведенияОДокументе(ИмяПакетаXDTO, Документ, Content)
	
	// Сведения документа, кроме сведений таблицы акта сверки взаимных расчетов
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	СвДокКрАкт = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СвДокКрАкт");
	
	// Отправитель
	СвОтпр = НовыйДочернийОбъектXDTO(Фабрика, СвДокКрАкт, "СвОтпр");
	XML_АктСверки_ЗаполнитьУчастникТип(ИмяПакетаXDTO, СвОтпр, Content.ReconciliationAct.AdditionalInfoReconciliationAct.Sender);
	
	// Получатель
	СвПол = НовыйДочернийОбъектXDTO(Фабрика, СвДокКрАкт, "СвПол");
	XML_АктСверки_ЗаполнитьУчастникТип(ИмяПакетаXDTO, СвПол, Content.ReconciliationAct.AdditionalInfoReconciliationAct.Recipient);
	
	УстановитьЗначениеXDTO(СвДокКрАкт, "КодОКВ", Content.ReconciliationAct.AdditionalInfoReconciliationAct.Currency);
	
КонецПроцедуры

Процедура XML_АктСверки_ЗаполнитьУчастникТип(ИмяПакетаXDTO, СвОтпр, Content)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ИдСв = НовыйДочернийОбъектXDTO(Фабрика, СвОтпр, "ИдСв");
	
	OrgType = Content.Type;
	
	Если OrgType = "PhysicalEntity" Тогда
		
		// Сведения о физическом лице
		
		СвФЛ = НовыйДочернийОбъектXDTO(Фабрика, ИдСв, "СвФЛ");
		ФИО = НовыйДочернийОбъектXDTO(Фабрика, СвФЛ, "ФИО");
		СтруктураФИО = XML_ПротоСтруктураФИО(Content.Name);
		XML_ЗаполнитьФИОТип(ФИО, СтруктураФИО);
		
		УстановитьЗначениеXDTO(СвФЛ, "ИННФЛ", ПолучитьЗначениеXDTO(Content, "Inn"));
		
	ИначеЕсли OrgType = "IndividualEntity" Тогда
		
		// Сведения об индивидуальном предпринимателе
		
		СвИП = НовыйДочернийОбъектXDTO(Фабрика, ИдСв, "СвИП");
		ФИО = НовыйДочернийОбъектXDTO(Фабрика, СвИП, "ФИО");
		СтруктураФИО = XML_ПротоСтруктураФИО(Content.Name);
		XML_ЗаполнитьФИОТип(ФИО, СтруктураФИО);
		
		УстановитьЗначениеXDTO(СвИП, "ИННФЛ", Content.Inn);
		УстановитьЗначениеXDTO(СвИП, "ОГРНИП", Content.Ogrn);
		
	ИначеЕсли OrgType = "LegalEntity" Тогда
		
		// Сведения о юридическом лице, состоящем на учете в налоговых органах
		
		СвЮЛУч = НовыйДочернийОбъектXDTO(Фабрика, ИдСв, "СвЮЛУч");
		
		УстановитьЗначениеXDTO(СвЮЛУч, "ИННЮЛ", Content.Inn);
		УстановитьЗначениеXDTO(СвЮЛУч, "НаимОрг", Content.Name);
		УстановитьЗначениеXDTO(СвЮЛУч, "КПП", ПолучитьЗначениеXDTO(Content, "Kpp"));
		УстановитьЗначениеXDTO(СвЮЛУч, "ОГРН", ПолучитьЗначениеXDTO(Content, "Ogrn"));
		
	ИначеЕсли OrgType = "ForeignEntity" Тогда
		
		// Сведения об иностранном лице, не состоящем на учете в налоговых органах
		
		СвИнНеУч = НовыйДочернийОбъектXDTO(Фабрика, ИдСв, "СвИнНеУч");
		
		УстановитьЗначениеXDTO(СвИнНеУч, "НаимОрг", Content.Name);
		УстановитьЗначениеXDTO(СвИнНеУч, "Идентиф", ПолучитьЗначениеXDTO(Content, "LegalEntityId"));
		
	Иначе
		
		ШаблонТекстаОшибки = "Неожиданный идентификатор типа участника сверки: %1";
		
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(
			ШаблонТекстаОшибки,
			OrgType
		);
		
		ВызватьИсключение ТекстОшибки;
		
	КонецЕсли;
	
	// Контактные данные
	Контакт = НовыйДочернийОбъектXDTO(Фабрика, СвОтпр, "Контакт");
	
	Тлф = ПолучитьЗначениеXDTO(Content, "Phone");
	ЭлПочта = ПолучитьЗначениеXDTO(Content, "Email");
	ИнКонт = ПолучитьЗначениеXDTO(Content, "AdditionalCont");
	
	УстановитьЗначениеXDTO(Контакт, "Тлф", Тлф);
	УстановитьЗначениеXDTO(Контакт, "ЭлПочта", ЭлПочта);
	УстановитьЗначениеXDTO(Контакт, "ИнКонт", ИнКонт);
	
КонецПроцедуры

Процедура XML_АктСверки_ЗаполнитьТаблицуВзаиморасчетов(ИмяПакетаXDTO, Документ, Content)
	
	// Сведения таблицы акта сверки взаимных расчетов
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ТаблАкт = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ТаблАкт");
	
	ReconciliationActTable = Content.ReconciliationAct.ReconciliationActTable;
	
	XML_АктСверки_ЗаполнитьСведенияОДоговорах(ИмяПакетаXDTO, ТаблАкт, ReconciliationActTable);
	
	УстановитьЗначениеXDTO(ТаблАкт, "СальдоНачДеб", ПолучитьЗначениеXDTO(ReconciliationActTable, "BalanceDebitFrom"));
	УстановитьЗначениеXDTO(ТаблАкт, "СальдоНачКр", ПолучитьЗначениеXDTO(ReconciliationActTable, "BalanceAmountFrom"));
	УстановитьЗначениеXDTO(ТаблАкт, "ОборотДеб", ПолучитьЗначениеXDTO(ReconciliationActTable, "TurnoverDebit"));
	УстановитьЗначениеXDTO(ТаблАкт, "ОборотКр", ПолучитьЗначениеXDTO(ReconciliationActTable, "TurnoverAmount"));
	УстановитьЗначениеXDTO(ТаблАкт, "СальдоКонДеб", ПолучитьЗначениеXDTO(ReconciliationActTable, "BalanceDebitEnd"));
	УстановитьЗначениеXDTO(ТаблАкт, "СальдоКонКр", ПолучитьЗначениеXDTO(ReconciliationActTable, "BalanceAmountEnd"));
	
КонецПроцедуры

Процедура XML_АктСверки_ЗаполнитьСведенияОДоговорах(ИмяПакетаXDTO, ТаблАкт, Content)
	
	// Сведения о договоре
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого Contract Из Content.ContractInfo Цикл
		
		ДогСв = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблАкт.ДогСв", ИмяПакетаXDTO);
		
		XML_АктСверки_ЗаполнитьСведенияОДокументах(ИмяПакетаXDTO, ДогСв, Contract);
		
		ДатаДог = ПолучитьЗначениеXDTO(Contract, "ContractDate");
		
		Если ЗначениеЗаполнено(ДатаДог) Тогда
			УстановитьЗначениеXDTO(ДогСв, "ДатаДог", Дата1С_в_ДатаXML(ДатаДог));
		КонецЕсли;
		
		УстановитьЗначениеXDTO(ДогСв, "ИдДог", ПолучитьЗначениеXDTO(Contract, "ContractId"));
		УстановитьЗначениеXDTO(ДогСв, "ОписТипДог", ПолучитьЗначениеXDTO(Contract, "ContractAdditionalInfo"));
		УстановитьЗначениеXDTO(ДогСв, "НомДог", ПолучитьЗначениеXDTO(Contract, "ContractNumber"));
		УстановитьЗначениеXDTO(ДогСв, "СальдоНачДеб", ПолучитьЗначениеXDTO(Contract, "BalanceDebitFrom"));
		УстановитьЗначениеXDTO(ДогСв, "СальдоНачКр", ПолучитьЗначениеXDTO(Contract, "BalanceAmountFrom"));
		УстановитьЗначениеXDTO(ДогСв, "ОборотДеб", ПолучитьЗначениеXDTO(Contract, "TurnoverDebit"));
		УстановитьЗначениеXDTO(ДогСв, "ОборотКр", ПолучитьЗначениеXDTO(Contract, "TurnoverAmount"));
		УстановитьЗначениеXDTO(ДогСв, "СальдоКонДеб", ПолучитьЗначениеXDTO(Contract, "BalanceDebitEnd"));
		УстановитьЗначениеXDTO(ДогСв, "СальдоКонКр", ПолучитьЗначениеXDTO(Contract, "BalanceAmountEnd"));
		
		ТаблАкт.ДогСв.Добавить(ДогСв);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_АктСверки_ЗаполнитьСведенияОДокументах(ИмяПакетаXDTO, ДогСв, Content)
	
	// Сведения о документах
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого Document Из Content.DocumentInfo Цикл
		
		ДокСв = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблАкт.ДогСв.ДокСв", ИмяПакетаXDTO);
		
		XML_АктСверки_ЗаполнитьСведенияОбОперациях(ИмяПакетаXDTO, ДокСв, Document);
		
		УстановитьЗначениеXDTO(ДокСв, "НаимДок", Document.DocumentName);
		УстановитьЗначениеXDTO(ДокСв, "НомДок", Document.DocumentNumber);
		УстановитьЗначениеXDTO(ДокСв, "ДатаДок", Дата1С_в_ДатаXML(Document.DocumentDate));
		УстановитьЗначениеXDTO(ДокСв, "ИдДок", ПолучитьЗначениеXDTO(Document, "DocumentId"));
		УстановитьЗначениеXDTO(ДокСв, "ДопИнф", ПолучитьЗначениеXDTO(Document, "AdditionalInfo"));
		
		ДогСв.ДокСв.Добавить(ДокСв);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_АктСверки_ЗаполнитьСведенияОбОперациях(ИмяПакетаXDTO, ДокСв, Content)
	
	// Сведения об операциях
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого Operation Из Content.OperationInfo Цикл
		
		СвОпер = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблАкт.ДогСв.ДокСв.СвОпер", ИмяПакетаXDTO);
		
		УстановитьЗначениеXDTO(СвОпер, "НомСтр", Operation.StringNumber);
		УстановитьЗначениеXDTO(СвОпер, "ДатаОпер", Дата1С_в_ДатаXML(Operation.OperationDate));
		УстановитьЗначениеXDTO(СвОпер, "НаимОпер", Operation.OperationName);
		УстановитьЗначениеXDTO(СвОпер, "СумДебет", ПолучитьЗначениеXDTO(Operation, "SumDebit"));
		УстановитьЗначениеXDTO(СвОпер, "СумКредит", ПолучитьЗначениеXDTO(Operation, "SumAmount"));
		
		ДокСв.СвОпер.Добавить(СвОпер);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_АктСверки_ЗаполнитьПодписанта(ИмяПакетаXDTO, Документ, Content)
	
	// Сведения о лице, подписавшем информацию отправителя в электронной форме
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Подписант = НовыйДочернийОбъектXDTO(Фабрика, Документ, "Подписант");
	
	SignerDetails = Content.ReconciliationAct.Signer;
	
	XML_АктСверки_ЗаполнитьСведенияОДоверенности(Фабрика, Подписант, SignerDetails);
	
	XML_АктСверки_ЗаполнитьСведенияОБумажнойДоверенности(Фабрика, Подписант, SignerDetails);
	
	XML_АктСверки_ЗаполнитьСведенияОПодписанте(Фабрика, Подписант, SignerDetails);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(SignerDetails, "SignerStatus")
		И ЗначениеЗаполнено(SignerDetails.SignerStatus) Тогда
		
		СтатусПодписанта = XML_АктСверки_СтатусПодписанта(SignerDetails.SignerStatus);
		УстановитьЗначениеXDTO(Подписант, "СтатПодп", СтатусПодписанта);
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(SignerDetails, "SignatureType")
		И ЗначениеЗаполнено(SignerDetails.SignatureType) Тогда
		
		ТипПодписи = XML_АктСверки_ТипПодписи(SignerDetails.SignatureType);
		УстановитьЗначениеXDTO(Подписант, "ТипПодпис", ТипПодписи);
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Подписант, "ИдСистХран", ПолучитьЗначениеXDTO(SignerDetails, "IdSystemStorage"));
	УстановитьЗначениеXDTO(Подписант, "Должн", ПолучитьЗначениеXDTO(SignerDetails, "JobTitle"));
	
КонецПроцедуры

Процедура XML_АктСверки_ЗаполнитьСведенияОДоверенности(Фабрика, Подписант, Content)
	
	// Сведения о доверенности, используемой для подтверждения полномочий в электронной форме
	
	DigitalAttorneyInfo = Content.DigitalAttorneyInfo;
	
	Если НЕ ОбъектXDTOЗаполнен(DigitalAttorneyInfo) Тогда
		Возврат;
	КонецЕсли;
	
	СвДовер = НовыйДочернийОбъектXDTO(Фабрика, Подписант, "СвДовер");
	
	ДатаНач = ПолучитьЗначениеXDTO(DigitalAttorneyInfo, "Date");
	ДатаВнРегДовер = ПолучитьЗначениеXDTO(DigitalAttorneyInfo, "InternalDate");
	
	УстановитьЗначениеXDTO(СвДовер, "НомДовер", ПолучитьЗначениеXDTO(DigitalAttorneyInfo, "Number"));
	УстановитьЗначениеXDTO(СвДовер, "ДатаНач", Дата1С_в_ДатаXML(ДатаНач));
	УстановитьЗначениеXDTO(СвДовер, "ВнНомДовер", ПолучитьЗначениеXDTO(DigitalAttorneyInfo, "InternalNumber"));
	УстановитьЗначениеXDTO(СвДовер, "ДатаВнРегДовер", Дата1С_в_ДатаXML(ДатаВнРегДовер));
	УстановитьЗначениеXDTO(СвДовер, "СведСистОтм", ПолучитьЗначениеXDTO(DigitalAttorneyInfo, "CancelAdditionalInfo"));
	
КонецПроцедуры

Процедура XML_АктСверки_ЗаполнитьСведенияОБумажнойДоверенности(Фабрика, Подписант, Content)
	
	// Сведения о доверенности, используемой для подтверждения полномочий в бумажном вид
	
	PaperAttorneyInfo = Content.PaperAttorneyInfo;
	
	Если НЕ ОбъектXDTOЗаполнен(PaperAttorneyInfo) Тогда
		Возврат;
	КонецЕсли;
	
	СвДоверБум = НовыйДочернийОбъектXDTO(Фабрика, Подписант, "СвДоверБум");
	
	ДатаДовер = ПолучитьЗначениеXDTO(PaperAttorneyInfo, "Date");
	
	УстановитьЗначениеXDTO(СвДоверБум, "ДатаДовер", Дата1С_в_ДатаXML(ДатаДовер));
	УстановитьЗначениеXDTO(СвДоверБум, "ВнНомДовер", ПолучитьЗначениеXDTO(PaperAttorneyInfo, "InternalNumber"));
	УстановитьЗначениеXDTO(СвДоверБум, "СвИдДовер", ПолучитьЗначениеXDTO(PaperAttorneyInfo, "IssuerAdditionalInfo"));
	
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, СвДоверБум, "ФИО");
	
	Official = PaperAttorneyInfo.Official;
	
	УстановитьЗначениеXDTO(ФИО, "Фамилия", Official.Surname);
	УстановитьЗначениеXDTO(ФИО, "Имя", Official.FirstName);
	УстановитьЗначениеXDTO(ФИО, "Отчество", ПолучитьЗначениеXDTO(Official, "Patronymic"));
	
КонецПроцедуры

Процедура XML_АктСверки_ЗаполнитьСведенияОПодписанте(Фабрика, Подписант, Content)
	
	// Фамилия, имя, отчество (при наличии)
	
	Official = Content.Official;
	
	Если НЕ ОбъектXDTOЗаполнен(Official) Тогда
		Возврат;
	КонецЕсли;
	
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, Подписант, "ФИО");
	
	УстановитьЗначениеXDTO(ФИО, "Фамилия", Official.Surname);
	УстановитьЗначениеXDTO(ФИО, "Имя", Official.FirstName);
	УстановитьЗначениеXDTO(ФИО, "Отчество", ПолучитьЗначениеXDTO(Official, "Patronymic"));
	
КонецПроцедуры

Процедура XML_АктСверки_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл, Документ, Content, КНД, ВерсияФормата, ПараметрыФормированияФайла)
	
	ИдентификаторОтправителя = ПараметрыФормированияФайла.ИдентификаторОтправителя;
	ИдентификаторПолучателя = ПараметрыФормированияФайла.ИдентификаторПолучателя;
	
	Если Не ЗначениеЗаполнено(ИдентификаторОтправителя) Или Не ЗначениеЗаполнено(ИдентификаторПолучателя) Тогда
		ИдентификаторПолучателя	 = Content.ReconciliationAct.AdditionalInfoReconciliationAct.Recipient.FnsParticipantId;
		ИдентификаторОтправителя = Content.ReconciliationAct.AdditionalInfoReconciliationAct.Sender.FnsParticipantId;
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(ИдентификаторОтправителя)
		ИЛИ НЕ ЗначениеЗаполнено(ИдентификаторПолучателя) Тогда
		
		ТекстОшибки = НСтр("ru = 'Не заполнены идентификаторы отправителя или получателя'");
		
		ВызватьИсключение ТекстОшибки;
		
	КонецЕсли;
	
	ВерсПрог = XML_ВерсияПрограммыФормированияФайла();
	ДопПрефикс = "";
	
	ИдФайл = XML_ИдФайл(
		ИмяПакетаXDTO,
		ДопПрефикс,
		ИдентификаторПолучателя,
		ИдентификаторОтправителя,
		ПараметрыФормированияФайла
	);
	
	УстановитьЗначениеXDTO(Файл, "ИдФайл", ИдФайл);
	УстановитьЗначениеXDTO(Файл, "ВерсФорм", ВерсияФормата);
	УстановитьЗначениеXDTO(Файл, "ВерсПрог", ВерсПрог);
	УстановитьЗначениеXDTO(Документ, "КНД", КНД);
	
	// Дата и время формирования файла
	ДатаФормирования = Дата1С_в_ДатаXML(ПараметрыФормированияФайла.ДатаФормирования);
	ВремяФормирования = Время1С_в_ВремяXML(ПараметрыФормированияФайла.ДатаФормирования);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ДатаИнфОтпр") Тогда
		
		УстановитьЗначениеXDTO(Документ, "ДатаИнфОтпр", ДатаФормирования);
		УстановитьЗначениеXDTO(Документ, "ВрИнфОтпр", ВремяФормирования);
		
	КонецЕсли;
	
	ДатаНачПер = Дата1С_в_ДатаXML(Content.ReconcilationDateFrom);
	ДатаОкПер = Дата1С_в_ДатаXML(Content.ReconcilationDateTo);
	
	УстановитьЗначениеXDTO(Документ, "ДатаНачПер", ДатаНачПер);
	УстановитьЗначениеXDTO(Документ, "ДатаОкПер", ДатаОкПер);
	УстановитьЗначениеXDTO(Документ, "НомерАкт", Content.ActNumber);
	
КонецПроцедуры

Процедура XML_АктСверки_ОтветныйТитул_ЗаполнитьДанныеФайлаОтправителя(ИмяПакетаXDTO, Документ, Content)
	
	// Идентификация файла обмена информации отправителя
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ИдИнфОтпр = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ИдИнфОтпр");
	
	IdentityInfoSender = Content.ReconciliationAct.IdentityInfoSender;
	
	УстановитьЗначениеXDTO(ИдИнфОтпр, "ИдФайлИнфОтпр", IdentityInfoSender.BaseIdFile);
	УстановитьЗначениеXDTO(ИдИнфОтпр, "ДатФайлИнфОтпр", Дата1С_в_ДатаXML(IdentityInfoSender.DateFileInfoSender));
	УстановитьЗначениеXDTO(ИдИнфОтпр, "ВрФайлИнфОтпр", IdentityInfoSender.TimeFileInfoSender);
	УстановитьЗначениеXDTO(ИдИнфОтпр, "ЭП", IdentityInfoSender.SignSender);
	
КонецПроцедуры

Процедура XML_АктСверки_ОтветныйТитул_ЗаполнитьТаблицуВзаиморасчетов(ИмяПакетаXDTO, Документ, Content)
	
	// Сведения таблицы акта сверки взаимных расчетов
	
	ReconciliationActTable = Content.ReconciliationAct.ReconciliationActTable;
	
	Если НЕ ОбъектXDTOЗаполнен(ReconciliationActTable) Тогда
		Возврат;
	КонецЕсли;
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ТаблАкт = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ТаблАкт");
	
	XML_АктСверки_ОтветныйТитул_ЗаполнитьСведенияОДоговорах(ИмяПакетаXDTO, ТаблАкт, ReconciliationActTable);
	
	УстановитьЗначениеXDTO(ТаблАкт, "СальдоНачДеб", ПолучитьЗначениеXDTO(ReconciliationActTable, "BalanceDebitFrom"));
	УстановитьЗначениеXDTO(ТаблАкт, "СальдоНачКр", ПолучитьЗначениеXDTO(ReconciliationActTable, "BalanceAmountFrom"));
	УстановитьЗначениеXDTO(ТаблАкт, "ОборотДеб", ПолучитьЗначениеXDTO(ReconciliationActTable, "TurnoverDebit"));
	УстановитьЗначениеXDTO(ТаблАкт, "ОборотКр", ПолучитьЗначениеXDTO(ReconciliationActTable, "TurnoverAmount"));
	УстановитьЗначениеXDTO(ТаблАкт, "СальдоКонДеб", ПолучитьЗначениеXDTO(ReconciliationActTable, "BalanceDebitEnd"));
	УстановитьЗначениеXDTO(ТаблАкт, "СальдоКонКр", ПолучитьЗначениеXDTO(ReconciliationActTable, "BalanceAmountEnd"));
	УстановитьЗначениеXDTO(ТаблАкт, "ИтогоРасхДеб", ПолучитьЗначениеXDTO(ReconciliationActTable, "TotalConsumptionDebit"));
	УстановитьЗначениеXDTO(ТаблАкт, "ИтогоРасхКр", ПолучитьЗначениеXDTO(ReconciliationActTable, "TotalConsumptionAmount"));
	УстановитьЗначениеXDTO(ТаблАкт, "ДопИнф", ПолучитьЗначениеXDTO(ReconciliationActTable, "AdditionalInfo"));
	
КонецПроцедуры

Процедура XML_АктСверки_ОтветныйТитул_ЗаполнитьСведенияОДоговорах(ИмяПакетаXDTO, ТаблАкт, Content)
	
	// Сведения о договоре
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого Contract Из Content.ContractInfo Цикл
		
		ДогСв = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблАкт.ДогСв", ИмяПакетаXDTO);
		
		XML_АктСверки_ОтветныйТитул_ЗаполнитьСведенияОДокументах(ИмяПакетаXDTO, ДогСв, Contract);
		
		ДатаДог = ПолучитьЗначениеXDTO(Contract, "ContractDate");
		
		УстановитьЗначениеXDTO(ДогСв, "ИдДог", ПолучитьЗначениеXDTO(Contract, "ContractId"));
		УстановитьЗначениеXDTO(ДогСв, "ОписТипДог", ПолучитьЗначениеXDTO(Contract, "ContractAdditionalInfo"));
		УстановитьЗначениеXDTO(ДогСв, "НомДог", ПолучитьЗначениеXDTO(Contract, "ContractNumber"));
		УстановитьЗначениеXDTO(ДогСв, "ДатаДог", Дата1С_в_ДатаXML(ДатаДог));
		УстановитьЗначениеXDTO(ДогСв, "СальдоНачДеб", ПолучитьЗначениеXDTO(Contract, "BalanceDebitFrom"));
		УстановитьЗначениеXDTO(ДогСв, "СальдоНачКр", ПолучитьЗначениеXDTO(Contract, "BalanceAmountFrom"));
		УстановитьЗначениеXDTO(ДогСв, "ОборотДеб", ПолучитьЗначениеXDTO(Contract, "TurnoverDebit"));
		УстановитьЗначениеXDTO(ДогСв, "ОборотКр", ПолучитьЗначениеXDTO(Contract, "TurnoverAmount"));
		УстановитьЗначениеXDTO(ДогСв, "СальдоКонДеб", ПолучитьЗначениеXDTO(Contract, "BalanceDebitEnd"));
		УстановитьЗначениеXDTO(ДогСв, "СальдоКонКр", ПолучитьЗначениеXDTO(Contract, "BalanceAmountEnd"));
		УстановитьЗначениеXDTO(ДогСв, "ИтогоРасхДеб", ПолучитьЗначениеXDTO(Contract, "TotalConsumptionDebit"));
		УстановитьЗначениеXDTO(ДогСв, "ИтогоРасхКр", ПолучитьЗначениеXDTO(Contract, "TotalConsumptionAmount"));
		УстановитьЗначениеXDTO(ДогСв, "ДопИнф", ПолучитьЗначениеXDTO(Contract, "AdditionalInfo"));
		
		ТаблАкт.ДогСв.Добавить(ДогСв);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_АктСверки_ОтветныйТитул_ЗаполнитьСведенияОДокументах(ИмяПакетаXDTO, ДогСв, Content)
	
	// Сведения о документах
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого Document Из Content.DocumentInfo Цикл
		
		ДокСв = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблАкт.ДогСв.ДокСв", ИмяПакетаXDTO);
		
		XML_АктСверки_ОтветныйТитул_ЗаполнитьСведенияОбОперациях(ИмяПакетаXDTO, ДокСв, Document);
		
		УстановитьЗначениеXDTO(ДокСв, "НаимДок", Document.DocumentName);
		УстановитьЗначениеXDTO(ДокСв, "НомДок", Document.DocumentNumber);
		УстановитьЗначениеXDTO(ДокСв, "ДатаДок", Дата1С_в_ДатаXML(Document.DocumentDate));
		УстановитьЗначениеXDTO(ДокСв, "ИтогоРасх", Document.TotalConsumption);
		УстановитьЗначениеXDTO(ДокСв, "ИдДок", ПолучитьЗначениеXDTO(Document, "DocumentId"));
		УстановитьЗначениеXDTO(ДокСв, "ДопИнф", ПолучитьЗначениеXDTO(Document, "AdditionalInfo"));
		
		ДогСв.ДокСв.Добавить(ДокСв);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_АктСверки_ОтветныйТитул_ЗаполнитьСведенияОбОперациях(ИмяПакетаXDTO, ДокСв, Content)
	
	// Сведения об операциях
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого Operation Из Content.OperationInfo Цикл
		
		СвОпер = XML_ПолучитьПустойОбъектXDTO("Файл.Документ.ТаблАкт.ДогСв.ДокСв.СвОпер", ИмяПакетаXDTO);
		
		УстановитьЗначениеXDTO(СвОпер, "НомСтр", Operation.StringNumber);
		УстановитьЗначениеXDTO(СвОпер, "ДатаОпер", Дата1С_в_ДатаXML(Operation.OperationDate));
		УстановитьЗначениеXDTO(СвОпер, "НаимОпер", Operation.OperationName);
		УстановитьЗначениеXDTO(СвОпер, "СумДебет", ПолучитьЗначениеXDTO(Operation, "SumDebit"));
		УстановитьЗначениеXDTO(СвОпер, "СумКредит", ПолучитьЗначениеXDTO(Operation, "SumAmount"));
		УстановитьЗначениеXDTO(СвОпер, "ДопИнф", ПолучитьЗначениеXDTO(Operation, "AdditionalInfo"));
		
		ДокСв.СвОпер.Добавить(СвОпер);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_АктСверки_ОтветныйТитул_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл, Документ, Content, КНД, ВерсияФормата, ПараметрыФормированияФайла)
	
	ИдентификаторОтправителя = ПараметрыФормированияФайла.ИдентификаторОтправителя;
	ИдентификаторПолучателя = ПараметрыФормированияФайла.ИдентификаторПолучателя;
	
	Если НЕ ЗначениеЗаполнено(ИдентификаторОтправителя)
		ИЛИ НЕ ЗначениеЗаполнено(ИдентификаторПолучателя) Тогда
		
		ТекстОшибки = НСтр("ru = 'Не заполнены идентификаторы отправителя или получателя'");
		
		ВызватьИсключение ТекстОшибки;
		
	КонецЕсли;
	
	ВерсПрог = XML_ВерсияПрограммыФормированияФайла();
	ДопПрефикс = "";
	
	ИдФайл = XML_ИдФайл(
		ИмяПакетаXDTO,
		ДопПрефикс,
		ИдентификаторПолучателя,
		ИдентификаторОтправителя,
		ПараметрыФормированияФайла
	);
	
	ПризнРазн = XML_АктСверки_ПризнакРазногласий(Content.SignDisagreement);
	
	УстановитьЗначениеXDTO(Файл, "ИдФайл", ИдФайл);
	УстановитьЗначениеXDTO(Файл, "ВерсФорм", ВерсияФормата);
	УстановитьЗначениеXDTO(Файл, "ВерсПрог", ВерсПрог);
	УстановитьЗначениеXDTO(Файл, "ПризнРазн", ПризнРазн);
	УстановитьЗначениеXDTO(Документ, "КНД", КНД);
	
	ДатаИнфПол = Дата1С_в_ДатаXML(Content.DateCreateFile);
	ВрИнфПол = Время1С_в_ВремяXML(Content.TimeCreateFile);
	
	УстановитьЗначениеXDTO(Документ, "ДатаИнфПол", ДатаИнфПол);
	УстановитьЗначениеXDTO(Документ, "ВрИнфПол", ВрИнфПол);
	
КонецПроцедуры

Функция XML_АктСверки_СтатусПодписанта(ИдентификаторСтатусаПодписанта)
	
	СтатусыПодписанта = Новый Соответствие;
	
	// 1 - лицо, имеющее полномочия на подписание документа без доверенности
	СтатусыПодписанта.Вставить(НРег("WithoutPowerOfAttorney"), 1);
	
	// 2 - лицо, имеющее полномочия на подписание документа на основании доверенности в электронной форме
	СтатусыПодписанта.Вставить(НРег("DigitalPowerOfAttorney"), 2);
	
	// 3 - лицо, имеющее полномочия на подписание документа на основании доверенности в бумажном виде
	СтатусыПодписанта.Вставить(НРег("PaperPowerOfAttorney"), 3);
	
	Результат = СтатусыПодписанта.Получить(НРег(ИдентификаторСтатусаПодписанта));
	
	Если НЕ ЗначениеЗаполнено(Результат) Тогда
		
		ШаблонТекстаОшибки = "Неожиданный статус подписанта: %1";
		
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(
			ШаблонТекстаОшибки,
			ИдентификаторСтатусаПодписанта
		);
		
		ВызватьИсключение ТекстОшибки;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция XML_АктСверки_ТипПодписи(ИдентификаторТипаПодписи)
	
	ТипыПодписи = Новый Соответствие;
	
	// 1 - усиленная квалифицированная электронная подпись
	ТипыПодписи.Вставить(НРег("EnhancedQualifiedSignature"), 1);
	
	// 2 - простая электронная подпись
	ТипыПодписи.Вставить(НРег("SimpleSignature"), 2);
	
	// 3 - усиленная неквалифицированная электронная подпись
	ТипыПодписи.Вставить(НРег("EnhancedUnqualifiedSignature"), 3);
	
	Результат = ТипыПодписи.Получить(НРег(ИдентификаторТипаПодписи));
	
	Если НЕ ЗначениеЗаполнено(Результат) Тогда
		
		ШаблонТекстаОшибки = "Неожиданный тип подписи: %1";
		
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(
			ШаблонТекстаОшибки,
			ИдентификаторТипаПодписи
		);
		
		ВызватьИсключение ТекстОшибки;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция XML_АктСверки_ПризнакРазногласий(ИдентификаторПризнака)
	
	ПризнакиРазногласий = Новый Соответствие;
	
	// 1 - нет разногласий между сверяемыми операциями отправителя и получателя
	ПризнакиРазногласий.Вставить(НРег("WithoutDisagreements"), 1);
	
	// 2 - есть разногласия между сверяемыми операциями отправителя и получателя
	ПризнакиРазногласий.Вставить(НРег("WithDisagreements"), 2);
	
	Результат = ПризнакиРазногласий.Получить(НРег(ИдентификаторПризнака));
	
	Если НЕ ЗначениеЗаполнено(Результат) Тогда
		
		ШаблонТекстаОшибки = "Неожиданный признак разногласий: %1";
		
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(
			ШаблонТекстаОшибки,
			ИдентификаторПризнака
		);
		
		ВызватьИсключение ТекстОшибки;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// XML: общие функции заполнения

Функция XML_ФабрикаXDTO(ИмяПакетаXDTO) Экспорт
	
	КлючКэш = "ФабрикаXDTO_" + ИмяПакетаXDTO;
	
	Результат = Кэш_Прочитать(КлючКэш);
	
	Если Результат = Неопределено Тогда
		
		ИмяМакета	 = ИмяПакетаXDTO + "_xsd";
		ТекстМакета	 = ПолучитьМакет(ИмяМакета).ПолучитьТекст();
		
		ЧтениеXML = Новый ЧтениеXML;
		ЧтениеXML.УстановитьСтроку(ТекстМакета);
		
		ПостроительDOM = Новый ПостроительDOM;
		ДокументDOM = ПостроительDOM.Прочитать(ЧтениеXML);
		
		ПостроительСхемXML = Новый ПостроительСхемXML;
		СхемаXML = ПостроительСхемXML.СоздатьСхемуXML(ДокументDOM);
		
		НаборСхемXML = Новый НаборСхемXML;
		НаборСхемXML.Добавить(СхемаXML);
		
		Результат = Новый ФабрикаXDTO(НаборСхемXML);
		
		Кэш_Поместить(КлючКэш, Результат);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция XML_ФорматЧисла(Число, Точность)
	
	Результат = Неопределено;
	
	Если Число <> Неопределено Тогда
		
		Если Точность = Неопределено Тогда
			Число			= Окр(Число, 11); 
			ФорматнаяСтрока	= "ЧРД=.; ЧН=; ЧГ=";
		Иначе
			ФорматнаяСтрока = "ЧДЦ=" + Точность + "; ЧРД=.; ЧН=; ЧГ=";
		КонецЕсли; 
		
		Результат = Формат(Число, ФорматнаяСтрока);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура XML_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO, Файл, Документ, Content, КНД, ВерсияФормата, ПараметрыФормированияФайла, ДопПрефикс = "")
	
	ИдентификаторОтправителя = ПараметрыФормированияФайла.ИдентификаторОтправителя;
	ИдентификаторПолучателя	 = ПараметрыФормированияФайла.ИдентификаторПолучателя;
	
	Если Не ЗначениеЗаполнено(ИдентификаторОтправителя) Или Не ЗначениеЗаполнено(ИдентификаторПолучателя) Тогда
		Если ИмяПакетаXDTO = "ON_NSCHFDOPPR" Тогда
			ИдентификаторПолучателя	 = ?(Content.Buyers.Количество() > 0, Content.Buyers[0].FnsParticipantId, "");
			ИдентификаторОтправителя = ?(Content.Sellers.Количество() > 0, Content.Sellers[0].FnsParticipantId, "");
		ИначеЕсли ИмяПакетаXDTO = "DP_PRIRASXPRIN" Тогда
			ИдентификаторПолучателя	 = Content.CircumstancesAcceptanceInfo.Buyer.OrganizationDetails.FnsParticipantId;
			ИдентификаторОтправителя = Content.CircumstancesAcceptanceInfo.Seller.OrganizationDetails.FnsParticipantId;
		Иначе
			ИдентификаторПолучателя	 = Content.Buyer.FnsParticipantId;
			ИдентификаторОтправителя = Content.Seller.FnsParticipantId;
		КонецЕсли;
	КонецЕсли;
	
	ВерсПрог	 = "Diadoc 1.0";
	ОператорЭДО	 = XML_ОператорЭДО();
	Фабрика		 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ИдФайл		 = XML_ИдФайл(ИмяПакетаXDTO, ДопПрефикс, ИдентификаторПолучателя, ИдентификаторОтправителя, ПараметрыФормированияФайла);
	
	УстановитьЗначениеXDTO(Файл, "ИдФайл", ИдФайл);
	УстановитьЗначениеXDTO(Файл, "ВерсФорм", ВерсияФормата);
	УстановитьЗначениеXDTO(Файл, "ВерсПрог", ВерсПрог);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "КНД") Тогда
		УстановитьЗначениеXDTO(Документ, "КНД", КНД);
	КонецЕсли;
	
	// Сведения об участниках электронного документооборота
	СвУчДокОбор = НовыйДочернийОбъектXDTO(Фабрика, Файл, "СвУчДокОбор");
	УстановитьЗначениеXDTO(СвУчДокОбор, "ИдОтпр", ИдентификаторОтправителя);
	УстановитьЗначениеXDTO(СвУчДокОбор, "ИдПол",  ИдентификаторПолучателя);
	
	// Сведения об операторе
	Если ИмяПакетаXDTO = "DP_PRIRASXPRIN" Тогда
		
		СвОЭДОтпр	 = НовыйДочернийОбъектXDTO(Фабрика, СвУчДокОбор, "СвОЭДОтпрСФ");
		УстановитьЗначениеXDTO(СвОЭДОтпр, "НаимОрг", ОператорЭДО.Имя);
		УстановитьЗначениеXDTO(СвОЭДОтпр, "ИННЮЛ",	ОператорЭДО.ИНН);
		УстановитьЗначениеXDTO(СвОЭДОтпр, "ИдЭДОСФ",	ОператорЭДО.ID);
		
	Иначе
		
		СвОЭДОтпр	 = НовыйДочернийОбъектXDTO(Фабрика, СвУчДокОбор, "СвОЭДОтпр");
		УстановитьЗначениеXDTO(СвОЭДОтпр, "НаимОрг", ОператорЭДО.Имя);
		УстановитьЗначениеXDTO(СвОЭДОтпр, "ИННЮЛ",	ОператорЭДО.ИНН);
		УстановитьЗначениеXDTO(СвОЭДОтпр, "ИдЭДО",	ОператорЭДО.ID);
		
	КонецЕсли;
	
	// Дата и время формирования файла
	ДатаФормирования	 = Дата_В_Формат(ПараметрыФормированияФайла.ДатаФормирования, "ДФ=dd.MM.yyyy");
	ВремяФормирования	 = Дата_В_Формат(ПараметрыФормированияФайла.ДатаФормирования, "ДФ=HH.mm.ss");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ДатаИнфИсп") Тогда
		
		УстановитьЗначениеXDTO(Документ, "ДатаИнфИсп", ДатаФормирования);
		УстановитьЗначениеXDTO(Документ, "ВремИнфИсп", ВремяФормирования);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ДатаИнфПр") Тогда
		
		УстановитьЗначениеXDTO(Документ, "ДатаИнфПр", ДатаФормирования);
		УстановитьЗначениеXDTO(Документ, "ВремИнфПр", ВремяФормирования);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ДатаДокПт") Тогда
		
		УстановитьЗначениеXDTO(Документ, "ДатаДокПт", ДатаФормирования);
		УстановитьЗначениеXDTO(Документ, "ВремДокПт", ВремяФормирования);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ДатаИнфПок") Тогда
		
		УстановитьЗначениеXDTO(Документ, "ДатаИнфПок", ДатаФормирования);
		УстановитьЗначениеXDTO(Документ, "ВремИнфПок", ВремяФормирования);
		
	КонецЕсли;
	
	// Наименование экономического субъекта
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "НаимЭконСубСост") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Content, "DocumentCreator") Тогда
			СоставительФайла = Content.DocumentCreator;
		Иначе
			СоставительФайла = Content.Creator;
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Документ, "НаимЭконСубСост", СоставительФайла);
		
	КонецЕсли;
	
	// Основание, по которому экономический субъект является составителем файла обмена
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "ОснДоверОргСост") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Content, "DocumentCreatorBase") Тогда
			ОснованиеСоставителя = Content.DocumentCreatorBase;
		Иначе
			ОснованиеСоставителя = Content.CreatorBase;
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Документ, "ОснДоверОргСост", ОснованиеСоставителя);
		
	КонецЕсли;
	
	// Информация о наличии согласованной структуры дополнительных информационных полей 
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Документ, "СоглСтрДопИнф") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Content, "ApprovedStructureAdditionalInfoFields") Тогда
			СтруктураДопПолей = Content.ApprovedStructureAdditionalInfoFields;
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Документ, "СоглСтрДопИнф", СтруктураДопПолей);
		
	КонецЕсли;
	
	Если ИмяПакетаXDTO = "ON_SCHFDOPPR" Или ИмяПакетаXDTO = "ON_NSCHFDOPPR" Тогда // УПД
		
		XML_УПД_ЗаполнитьСведенияОДокументе(Документ, Content);
		
	ИначеЕсли ИмяПакетаXDTO = "ON_KORSCHFDOPPR" Тогда // УКД
		
		XML_УКД_ЗаполнитьСведенияОДокументе(Документ, Content);
		
	ИначеЕсли ИмяПакетаXDTO = "ON_NKORSCHFDOPPR" Тогда // УКД
		
		XML_УКД_736_ЗаполнитьСведенияОДокументе(Документ, Content);
		
	ИначеЕсли ИмяПакетаXDTO = "ON_SCHET" Тогда // Счет
		
		УстановитьЗначениеXDTO(Документ, "ТипДок", "ФормализованныйСчет");
		УстановитьЗначениеXDTO(Документ, "НаимДок", "Формализованный счет");
		
	ИначеЕсли ИмяПакетаXDTO = "DP_PRIRASXPRIN" Тогда
		
		XML_Торг2_ЗаполнитьСведенияОДокументе(Фабрика, Документ, Content);
		
	КонецЕсли;
		
КонецПроцедуры

Процедура XML_УПД_ЗаполнитьСведенияОДокументе(Документ, Content)
	
	ФункцияУПД = XML_УПД_ПолучитьФункцию(Content.Function);
	УстановитьЗначениеXDTO(Документ, "Функция", ФункцияУПД);
	
	ФункцииУПД = Перечисление_ФункцииУПД();
	Если ФункцияУПД = ФункцииУПД.СЧФ Тогда
		Возврат;
	КонецЕсли;
	
	// Определено приказом ФНС
	ПоФактХЖ = "Документ об отгрузке товаров (выполнении работ), передаче имущественных прав (документ об оказании услуг)";
	
	Если ФункцияУПД = ФункцииУПД.СЧФДОП Тогда
		
		// Определено приказом ФНС
		НаимДокОпр = "Счет-фактура и документ об отгрузке товаров (выполнении работ), передаче имущественных прав (документ об оказании услуг)";

	Иначе // ДОП
		
		// Наименование явно указано в контенте
		НаимДокОпр = НаименованиеПервичногоДокументаИзПрото(Content);
		
		Если Не ЗначениеЗаполнено(НаимДокОпр) Тогда
			// Вариант по умолчанию, определенный приказом ФНС
			НаимДокОпр = "Документ об отгрузке товаров (выполнении работ), передаче имущественных прав (документ об оказании услуг)";
		КонецЕсли;
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Документ, "ПоФактХЖ"		, ПоФактХЖ);
	УстановитьЗначениеXDTO(Документ, "НаимДокОпр"	, НаимДокОпр);
	
КонецПроцедуры

Процедура XML_УПД_970_ЗаполнитьСведенияОДокументе(Документ, Content)
	
	ФункцияУПД = XML_УПД_ПолучитьФункцию(Content.Function);
	УстановитьЗначениеXDTO(Документ, "Функция", ФункцияУПД);
	
	ФункцииУПД = Перечисление_ФункцииУПД();
	Если ФункцияУПД = ФункцииУПД.СЧФ Тогда
		Возврат;
	КонецЕсли;
	
	// Определено приказом ФНС
	ПоФактХЖ = "Документ об отгрузке товаров (выполнении работ), передаче имущественных прав (документ об оказании услуг)";
	УстановитьЗначениеXDTO(Документ, "ПоФактХЖ", ПоФактХЖ);
	
	УстановитьЗначениеXDTO(Документ, "НаимДокОпр", Content.DocumentName);
	
КонецПроцедуры

Процедура XML_УКД_ЗаполнитьСведенияОДокументе(Документ, Content)
	
	ФункцияУПД = XML_УКД_ПолучитьФункцию(Content.Function);
	УстановитьЗначениеXDTO(Документ, "Функция", ФункцияУПД);
	
	Если ФункцияУПД = "КСЧФ" Тогда
		Возврат;
	КонецЕсли;
	
	// Определено приказом ФНС
	ПоФактХЖ = "Документ об изменении стоимости отгруженных товаров (выполненных работ, оказанных услуг), переданных имущественных прав";
	
	Если ФункцияУПД = "КСЧФДИС" Тогда
		
		// Определено приказом ФНС
		НаимДокОпр = "Корректировочный счет-фактура и документ об изменении стоимости отгруженных товаров (выполненных работ, оказанных услуг), переданных имущественных прав";

	Иначе // ДИС
		
		// Наименование явно указано в контенте
		НаимДокОпр = НаименованиеПервичногоДокументаИзПрото(Content);
		
		Если Не ЗначениеЗаполнено(НаимДокОпр) Тогда
			// Вариант по умолчанию, определенный приказом ФНС
			НаимДокОпр = "Документ об изменении стоимости отгруженных товаров (выполненных работ, оказанных услуг), переданных имущественных прав";
		КонецЕсли;
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Документ, "ПоФактХЖ"		, ПоФактХЖ);
	УстановитьЗначениеXDTO(Документ, "НаимДокОпр"	, НаимДокОпр);
	
КонецПроцедуры

Процедура XML_УКД_736_ЗаполнитьСведенияОДокументе(Документ, Content)
	
	ФункцияУПД = XML_УКД_ПолучитьФункцию(Content.Function);
	УстановитьЗначениеXDTO(Документ, "Функция", ФункцияУПД);
	
	Если ФункцияУПД = "КСЧФ" Тогда
		Возврат;
	КонецЕсли;
	
	// Определено приказом ФНС
	ПоФактХЖ = "Документ, подтверждающий согласие (факт уведомления) покупателя на изменение стоимости отгруженных товаров (выполненных работ, оказанных услуг), переданных имущественных прав";
	
	// Наименование явно указано в контенте
	НаимДокОпр = НаименованиеПервичногоДокументаИзПрото(Content);
	
	Если Не ЗначениеЗаполнено(НаимДокОпр) Тогда
		// Вариант по умолчанию, определенный приказом ФНС
		НаимДокОпр = "Документ, подтверждающий согласие (факт уведомления) покупателя на изменение стоимости отгруженных товаров (выполненных работ, оказанных услуг), переданных имущественных прав";
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Документ, "ПоФактХЖ"		, ПоФактХЖ);
	УстановитьЗначениеXDTO(Документ, "НаимДокОпр"	, НаимДокОпр);
	
КонецПроцедуры

Процедура XML_Торг2_ЗаполнитьСведенияОДокументе(Фабрика, Документ, Content)
	
	ПоФактХЖ = "Документ о приемке ценностей и (или) расхождениях, выявленных при их приемке";
	НаимДокОпр = НаименованиеПервичногоДокументаИзПрото(Content);
	
	УстановитьЗначениеXDTO(Документ, "ПоФактХЖ"		, ПоФактХЖ);
	УстановитьЗначениеXDTO(Документ, "НаимДокОпр"	, НаимДокОпр);
	
	// Обстоятельства составления документа
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Content, "CircumDraft") Тогда
		УстановитьЗначениеXDTO(Документ, "ОбстСостДок", Content.CircumDraft);
	КонецЕсли;
	
	// Назначение и подписанты дополнительных сведений
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Content, "AdditionalInfosPurpose") Тогда
		УстановитьЗначениеXDTO(Документ, "НазнДопСв", Content.AdditionalInfosPurpose);
	КонецЕсли;
	
	// Информация о формировании дополнительных сведений к документу
	Для Каждого DocumentAdditionalInfo Из Content.DocumentAdditionalInfos.DocumentAdditionalInfo Цикл
		Документ.ИнфДопСв.Добавить(DocumentAdditionalInfo);
	КонецЦикла;
	
	// Дата и номер документа о приемке и (или) расхождениях
	ИдентДокПР = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ИдентДокПР");
	УстановитьЗначениеXDTO(ИдентДокПР, "НомДокПР", Content.DocumentNumber);
	УстановитьЗначениеXDTO(ИдентДокПР, "ДатаДокПР", Дата_В_Формат(Content.DocumentDate, "ДФ=dd.MM.yyyy"));
	
	// Исправление документа о приемке и (или) расхождениях
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Content, "RevisionInfo")
		И ОбъектXDTOЗаполнен(Content.RevisionInfo) Тогда
		ИспрДокПР = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ИспрДокПР");
		УстановитьЗначениеXDTO(ИспрДокПР, "НомИспрДокПР", Content.RevisionInfo.RevisionNumber);
		УстановитьЗначениеXDTO(ИспрДокПР, "ДатаИспрДокПР", Дата_В_Формат(Content.RevisionInfo.RevisionDate, "ДФ=dd.MM.yyyy"));
	КонецЕсли;
	
КонецПроцедуры

Функция НаименованиеПервичногоДокументаИзПрото(Content)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Content, "DocumentName") Тогда // 820@, 423@ (Торг2)
		Результат = Content.DocumentName;
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Content, "Name") Тогда // 155@, 189@
		Результат = Content.Name;
	Иначе
		Результат = Неопределено;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура XML_ЗаполнитьСведенияОКонтрагенте(ИмяПакетаXDTO, ОбъектXDTO, ПротоКонтент)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	// Основные сведения
	УстановитьЗначениеXDTO(ОбъектXDTO, "ОКПО"		, ПротоКонтент.Okpo);
	УстановитьЗначениеXDTO(ОбъектXDTO, "СтруктПодр"	, ПротоКонтент.Department);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИнфДляУчаст", ПротоКонтент.AdditionalInfo);
	
	ИдСв = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ИдСв");
	
	Если ПротоКонтент.Type = "LegalEntity" Тогда
		
		XML_ЗаполнитьЮридическоеЛицоКонтрагента(ИмяПакетаXDTO, ИдСв, ПротоКонтент);
		
	ИначеЕсли ПротоКонтент.Type = "IndividualEntity" Тогда
		
		XML_ЗаполнитьИндивидуальногоПредпринимателяКонтрагента_820(ИмяПакетаXDTO, ИдСв, ПротоКонтент);
		
	ИначеЕсли ПротоКонтент.Type = "PhysicalEntity" Тогда
		
		XML_ЗаполнитьФизическоеЛицоКонтрагента(ИмяПакетаXDTO, ИдСв, ПротоКонтент);
		
	ИначеЕсли ПротоКонтент.Type = "ForeignEntity" Тогда
		
		XML_ЗаполнитьИностранноеЛицоКонтрагента(ИмяПакетаXDTO, ИдСв, ПротоКонтент);
		
	КонецЕсли;
	
	// Адрес
	Адрес = XML_ПолучитьПустойОбъектXDTO("АдресТип", ИмяПакетаXDTO);
	Если XML_ЗаполнитьАдрес(ИмяПакетаXDTO, Адрес, ПротоКонтент.Address) Тогда
		УстановитьЗначениеXDTO(ОбъектXDTO, "Адрес", Адрес);
	КонецЕсли;
	
	//++#1478 смарт: Лопато 29.09.2023 //Перенос доработок в новую обработку Диадок  
	//// Контакты: телефон и эл.почта
	//Если ЗначениеЗаполнено(ПротоКонтент.Phone) ИЛИ ЗначениеЗаполнено(ПротоКонтент.Email) Тогда
	//	
	//	Контакт = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "Контакт");
	//	УстановитьЗначениеXDTO(Контакт, "Тлф"		, ПротоКонтент.Phone);
	//	УстановитьЗначениеXDTO(Контакт, "ЭлПочта"	, ПротоКонтент.Email);
	//	
	//КонецЕсли;
	//
	//// Банковские реквизиты
	//Если БанковскиеРеквизитыКонтрагентаЗаполнены(ПротоКонтент) Тогда
	//	
	//	БанкРекв = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO	, "БанкРекв");
	//	СвБанк	 = НовыйДочернийОбъектXDTO(Фабрика, БанкРекв	, "СвБанк");
	//	
	//	Если ЗначениеЗаполнено(ПротоКонтент.BankAccountNumber) Тогда
	//		УстановитьЗначениеXDTO(БанкРекв, "НомерСчета", ПротоКонтент.BankAccountNumber);
	//	КонецЕсли;
	//	
	//	Если ЗначениеЗаполнено(ПротоКонтент.BankName) Тогда
	//		УстановитьЗначениеXDTO(СвБанк, "НаимБанк", ПротоКонтент.BankName);
	//	КонецЕсли;
	//	
	//	Если ЗначениеЗаполнено(ПротоКонтент.BankId) Тогда
	//		УстановитьЗначениеXDTO(СвБанк, "БИК", ПротоКонтент.BankId);
	//	КонецЕсли;
	//	
	//	Если ЗначениеЗаполнено(ПротоКонтент.CorrespondentAccount) Тогда
	//		УстановитьЗначениеXDTO(СвБанк, "КорСчет", ПротоКонтент.CorrespondentAccount);
	//	КонецЕсли;
	//	
	//КонецЕсли;
	//--#1478 смарт: Лопато 29.09.2023
	
КонецПроцедуры

Процедура XML_ЗаполнитьСведенияОКонтрагенте_970(ИмяПакетаXDTO, ОбъектXDTO, ПротоКонтент)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "ОКПО", ПротоКонтент.Okpo);
	УстановитьЗначениеXDTO(ОбъектXDTO, "КодОПФ", ПротоКонтент.Okopf);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ПолнНаимОПФ", ПротоКонтент.FullNameOPF);
	УстановитьЗначениеXDTO(ОбъектXDTO, "СтруктПодр", ПротоКонтент.Department);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИнфДляУчаст", ПротоКонтент.AdditionalInfo);

	XML_ЗаполнитьИдСв(ОбъектXDTO, ПротоКонтент, ИмяПакетаXDTO);
	
	XML_ЗаполнитьАдрес_970(ИмяПакетаXDTO, ОбъектXDTO, ПротоКонтент.Address);
	
	Если ПротоКонтент.Phone.Количество() > 0
		Или ПротоКонтент.Email.Количество() > 0
		Или ЗначениеЗаполнено(ПротоКонтент.OtherContactInfo) Тогда
		
		XML_ЗаполнитьКонтактТип(ОбъектXDTO, ПротоКонтент, ИмяПакетаXDTO);
		
	КонецЕсли;

	Если БанковскиеРеквизитыКонтрагентаЗаполнены(ПротоКонтент) Тогда
		XML_ЗаполнитьБанкРекв_970(ОбъектXDTO, ПротоКонтент, ИмяПакетаXDTO);
	КонецЕсли;
	
КонецПроцедуры

Функция БанковскиеРеквизитыКонтрагентаЗаполнены(ПротоКонтент)
	
	Результат = ЗначениеЗаполнено(ПротоКонтент.BankAccountNumber)
				Или ЗначениеЗаполнено(ПротоКонтент.BankName)
				Или ЗначениеЗаполнено(ПротоКонтент.BankId)
				Или ЗначениеЗаполнено(ПротоКонтент.CorrespondentAccount);
	Возврат Результат;
	
КонецФункции

Функция XML_ЗаполнитьАдрес(ИмяПакетаXDTO, ОбъектXDTO, Address)

	Результат = Истина;  // признак - был ли заполнен адрес
	
	Если ЗначениеЗаполнено(Address.AddressCode) Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "КодГАР", Address.AddressCode);
	
	ИначеЕсли Address.IsForeign = Ложь Тогда
		
		АдрРФ = XML_ПолучитьПустойОбъектXDTO("АдрРФТип", ИмяПакетаXDTO);
		
		УстановитьЗначениеXDTO(АдрРФ, "Индекс", 	Address.ZipCode);
		УстановитьЗначениеXDTO(АдрРФ, "КодРегион", 	Address.RegionCode);
		УстановитьЗначениеXDTO(АдрРФ, "Район", 		Address.Territory);
		УстановитьЗначениеXDTO(АдрРФ, "Город", 		Address.City);
		УстановитьЗначениеXDTO(АдрРФ, "НаселПункт", Address.Locality);
		УстановитьЗначениеXDTO(АдрРФ, "Улица", 		Address.Street);
		УстановитьЗначениеXDTO(АдрРФ, "Дом", 		Address.Building);
		УстановитьЗначениеXDTO(АдрРФ, "Корпус", 	Address.Block);
		УстановитьЗначениеXDTO(АдрРФ, "Кварт", 		Address.Apartment);
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "АдрРФ", АдрРФ);
		
	ИначеЕсли ЗначениеЗаполнено(Address.CountryCode) Или ЗначениеЗаполнено(Address.AddressText) Тогда
		
		АдрИнф = XML_ПолучитьПустойОбъектXDTO("АдрИнфТип", ИмяПакетаXDTO);
		
		УстановитьЗначениеXDTO(АдрИнф, "КодСтр",	Address.CountryCode);
		УстановитьЗначениеXDTO(АдрИнф, "АдрТекст",	Address.AddressText);
		
		Если ОбъектXDTO.Свойства().Получить("АдрИнф") <> Неопределено Тогда
			УстановитьЗначениеXDTO(ОбъектXDTO, "АдрИнф", АдрИнф);
		Иначе
			УстановитьЗначениеXDTO(ОбъектXDTO, "АдрИно", АдрИнф);
		КонецЕсли;
		
	Иначе
		Результат = Ложь;
	КонецЕсли;
	
	Возврат Результат;
		
КонецФункции

Процедура XML_ЗаполнитьАдрес_970(ИмяПакетаXDTO, ОбъектXDTO, ПротоКонтент)
	
	Адрес = Неопределено;

	ВидыАдресов = Перечисление_ВидыАдресов();
	
	Если ПротоКонтент.Type = ВидыАдресов.АдресРФ Тогда
		
		Адрес = XML_ЗаполнитьАдресРФ(ИмяПакетаXDTO, ПротоКонтент.AddressRF);	
		
	ИначеЕсли ПротоКонтент.Type = ВидыАдресов.АдресГАР Тогда
		
		Адрес = XML_ЗаполнитьАдресГАР(ИмяПакетаXDTO, ПротоКонтент.AddressSAR);
		
	ИначеЕсли ПротоКонтент.Type = ВидыАдресов.АдресСтрокой Тогда
		
		Адрес = XML_ЗаполнитьАдресСтрокой(ИмяПакетаXDTO, ПротоКонтент.AddressText);
		
	КонецЕсли;
	
	Если Адрес <> Неопределено Тогда
		
		УстановитьЗначениеXDTO(Адрес, "ГЛНМеста", ПротоКонтент.GLN);
		УстановитьЗначениеXDTO(ОбъектXDTO, "Адрес", Адрес);

	КонецЕсли;
	
КонецПроцедуры

Функция XML_ЗаполнитьАдресРФ(ИмяПакетаXDTO, ПротоКонтент)
	
	Перем АдресТип;
	
	Если ПротоКонтент <> Неопределено И ЗначениеЗаполнено(ПротоКонтент.Region) Тогда
		
		АдресТип = XML_ПолучитьПустойОбъектXDTO("АдресТип", ИмяПакетаXDTO);
		
		Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
		АдрРФ = НовыйДочернийОбъектXDTO(Фабрика, АдресТип, "АдрРФ");
		
		УстановитьЗначениеXDTO(АдрРФ, "Индекс", ПротоКонтент.ZipCode);
		УстановитьЗначениеXDTO(АдрРФ, "КодРегион", ПротоКонтент.RegionCode);
		УстановитьЗначениеXDTO(АдрРФ, "НаимРегион", АдаптацияНазванияРегиона(ПротоКонтент.Region));
		УстановитьЗначениеXDTO(АдрРФ, "Район", ПротоКонтент.Territory);
		УстановитьЗначениеXDTO(АдрРФ, "Город", ПротоКонтент.City);
		УстановитьЗначениеXDTO(АдрРФ, "НаселПункт", ПротоКонтент.Locality);
		УстановитьЗначениеXDTO(АдрРФ, "Улица", ПротоКонтент.Street);
		УстановитьЗначениеXDTO(АдрРФ, "Дом", ПротоКонтент.Building);
		УстановитьЗначениеXDTO(АдрРФ, "Корпус", ПротоКонтент.Block);
		УстановитьЗначениеXDTO(АдрРФ, "Кварт", ПротоКонтент.Apartment);
		УстановитьЗначениеXDTO(АдрРФ, "ИныеСвед", ПротоКонтент.AdditionalInfo);
		
	КонецЕсли;
	
	Возврат АдресТип;
	
КонецФункции

Функция XML_ЗаполнитьАдресГАР(ИмяПакетаXDTO, ПротоКонтент)
	
	Перем АдресТип;
	
	Если ПротоКонтент <> Неопределено И ЗначениеЗаполнено(ПротоКонтент.ID) Тогда
		
		АдресТип = XML_ПолучитьПустойОбъектXDTO("АдресТип", ИмяПакетаXDTO);
		
		Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
		АдрГАР = НовыйДочернийОбъектXDTO(Фабрика, АдресТип, "АдрГАР");
		
		УстановитьЗначениеXDTO(АдрГАР, "ИдНом", ПротоКонтент.ID);
		УстановитьЗначениеXDTO(АдрГАР, "Индекс", ПротоКонтент.ZipCode);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(АдрГАР, "КодРегион") Тогда
			УстановитьЗначениеXDTO(АдрГАР, "КодРегион", ПротоКонтент.RegionCode);
		Иначе
			УстановитьЗначениеXDTO(АдрГАР, "Регион", ПротоКонтент.RegionCode);
		КонецЕсли;
		
		УстановитьЗначениеXDTO(АдрГАР, "НаимРегион", АдаптацияНазванияРегиона(ПротоКонтент.Region));
		
		Если ЗначениеЗаполнено(ПротоКонтент.MunicipalDistrictCode) Тогда
			ВидНаимКодТип = XML_НовыйВидНаимКодТип(ИмяПакетаXDTO, ПротоКонтент.MunicipalDistrictCode, ПротоКонтент.MunicipalDistrict);
			УстановитьЗначениеXDTO(АдрГАР, "МуниципРайон", ВидНаимКодТип);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ПротоКонтент.SettlementCode) Тогда
			ВидНаимКодТип = XML_НовыйВидНаимКодТип(ИмяПакетаXDTO, ПротоКонтент.SettlementCode, ПротоКонтент.Settlement);
			УстановитьЗначениеXDTO(АдрГАР, "ГородСелПоселен", ВидНаимКодТип);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ПротоКонтент.LocalityType) Тогда
			ВидНаимТип = XML_НовыйВидНаимТип(ИмяПакетаXDTO, ПротоКонтент.LocalityType, ПротоКонтент.Locality);
			УстановитьЗначениеXDTO(АдрГАР, "НаселенПункт", ВидНаимТип);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ПротоКонтент.PlanningElementType) Тогда
			ТипНаимТип = XML_НовыйТипНаимТип(ИмяПакетаXDTO, ПротоКонтент.PlanningElementType, ПротоКонтент.PlanningElement);
			УстановитьЗначениеXDTO(АдрГАР, "ЭлПланСтруктур", ТипНаимТип);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ПротоКонтент.StreetElementType) Тогда
			ТипНаимТип = XML_НовыйТипНаимТип(ИмяПакетаXDTO, ПротоКонтент.StreetElementType, ПротоКонтент.StreetElement);
			УстановитьЗначениеXDTO(АдрГАР, "ЭлУлДорСети", ТипНаимТип);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ПротоКонтент.StreetElementType) Тогда
			ТипНаимТип = XML_НовыйТипНаимТип(ИмяПакетаXDTO, ПротоКонтент.StreetElementType, ПротоКонтент.StreetElement);
			УстановитьЗначениеXDTO(АдрГАР, "ЭлУлДорСети", ТипНаимТип);
		КонецЕсли;
		
		УстановитьЗначениеXDTO(АдрГАР, "ЗемелУчасток", ПротоКонтент.LandPlot);
		
		Для Каждого BuildingInfo Из ПротоКонтент.Buildings Цикл
			
			НовыйЭл = НовыйДочернийОбъектXDTO(Фабрика, АдрГАР, "Здание");
			
			УстановитьЗначениеXDTO(НовыйЭл, "Тип", BuildingInfo.Type);
			УстановитьЗначениеXDTO(НовыйЭл, "Номер", BuildingInfo.Number);
			
		КонецЦикла;
		
		Если ЗначениеЗаполнено(ПротоКонтент.HouseType) Тогда
			НомерТип = XML_НовыйНомерТип(ИмяПакетаXDTO, ПротоКонтент.HouseType, ПротоКонтент.House);
			УстановитьЗначениеXDTO(АдрГАР, "ПомещЗдания", НомерТип);
		КонецЕсли;
		
	Если ЗначениеЗаполнено(ПротоКонтент.ApartmentType) Тогда
			НомерТип = XML_НовыйНомерТип(ИмяПакетаXDTO, ПротоКонтент.ApartmentType, ПротоКонтент.Apartment);
			УстановитьЗначениеXDTO(АдрГАР, "ПомещКвартиры", НомерТип);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат АдресТип;
	
КонецФункции

Функция XML_ЗаполнитьАдресСтрокой(ИмяПакетаXDTO, ПротоКонтент)
	
	Перем АдресТип;
	
	Если ПротоКонтент <> Неопределено И ЗначениеЗаполнено(ПротоКонтент.AddressText) Тогда
		
		АдресТип = XML_ПолучитьПустойОбъектXDTO("АдресТип", ИмяПакетаXDTO);

		Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
        АдрИнф = НовыйДочернийОбъектXDTO(Фабрика, АдресТип, "АдрИнф");
		
		УстановитьЗначениеXDTO(АдрИнф, "КодСтр", ПротоКонтент.CountryCode);
		УстановитьЗначениеXDTO(АдрИнф, "НаимСтран", ПротоКонтент.Country);
		УстановитьЗначениеXDTO(АдрИнф, "АдрТекст", ПротоКонтент.AddressText);
		
	КонецЕсли;
	
	Возврат АдресТип;
	
КонецФункции

Функция XML_НовыйВидНаимКодТип(ИмяПакетаXDTO, ВидКод, Наим)
	
	Результат = XML_ПолучитьПустойОбъектXDTO("ВидНаимКодТип", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(Результат, "ВидКод", ВидКод);
	УстановитьЗначениеXDTO(Результат, "Наим", Наим);
	
	Возврат Результат;
	
КонецФункции

Функция XML_НовыйВидНаимТип(ИмяПакетаXDTO, Вид, Наим)
	
	Результат = XML_ПолучитьПустойОбъектXDTO("ВидНаимТип", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(Результат, "Вид", Вид);
	УстановитьЗначениеXDTO(Результат, "Наим", Наим);
	
	Возврат Результат;
	
КонецФункции

Функция XML_НовыйТипНаимТип(ИмяПакетаXDTO, Тип, Наим)
	
	Результат = XML_ПолучитьПустойОбъектXDTO("ТипНаимТип", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(Результат, "Тип", Тип);
	УстановитьЗначениеXDTO(Результат, "Наим", Наим);
	
	Возврат Результат;
	
КонецФункции

Функция XML_НовыйНомерТип(ИмяПакетаXDTO, Тип, Номер)
	
	Результат = XML_ПолучитьПустойОбъектXDTO("НомерТип", ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(Результат, "Тип", Тип);
	УстановитьЗначениеXDTO(Результат, "Номер", Номер);
	
	Возврат Результат;
	
КонецФункции

Процедура XML_ЗаполнитьАдресТип(ИмяПакетаXDTO, АдресТип, Address_Torg2)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Если ЗначениеЗаполнено(Address_Torg2.RussianAddress.Region) Тогда
		
		RussianAddress = Address_Torg2.RussianAddress;
		АдрРФ = НовыйДочернийОбъектXDTO(Фабрика, АдресТип, "АдрРФ");
		
		УстановитьЗначениеXDTO(АдрРФ, "Индекс", ПолучитьЗначениеXDTO(RussianAddress, "ZipCode"));
		УстановитьЗначениеXDTO(АдрРФ, "КодРегион", RussianAddress.Region);
		УстановитьЗначениеXDTO(АдрРФ, "Район", ПолучитьЗначениеXDTO(RussianAddress, "Territory"));
		УстановитьЗначениеXDTO(АдрРФ, "Город", ПолучитьЗначениеXDTO(RussianAddress, "City"));
		УстановитьЗначениеXDTO(АдрРФ, "НаселПункт", ПолучитьЗначениеXDTO(RussianAddress, "Locality"));
		УстановитьЗначениеXDTO(АдрРФ, "Улица", ПолучитьЗначениеXDTO(RussianAddress, "Street"));
		УстановитьЗначениеXDTO(АдрРФ, "Дом", ПолучитьЗначениеXDTO(RussianAddress, "Building"));
		УстановитьЗначениеXDTO(АдрРФ, "Корпус", ПолучитьЗначениеXDTO(RussianAddress, "Block"));
		УстановитьЗначениеXDTO(АдрРФ, "Кварт", ПолучитьЗначениеXDTO(RussianAddress, "Apartment"));
		
	ИначеЕсли ЗначениеЗаполнено(Address_Torg2.ForeignAddress.Address) Тогда
		
		ForeignAddress = Address_Torg2.ForeignAddress;
		АдрИнф = НовыйДочернийОбъектXDTO(Фабрика, АдресТип, "АдрИнф");
		
		УстановитьЗначениеXDTO(АдрИнф, "КодСтр", ForeignAddress.Country);
		УстановитьЗначениеXDTO(АдрИнф, "АдрТекст", ForeignAddress.Address);
		
	Иначе
		
		УстановитьЗначениеXDTO(АдресТип, "КодГАР", Address_Torg2.AddressCode);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, ОбъектXDTO, AdditionalInfos, ТипОбъектаКоллекции = "ТекстИнфТип")
	
	Если НЕ ЗначениеЗаполнено(AdditionalInfos) Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого AdditionalInfo Из AdditionalInfos Цикл
		ТекстИнф = XML_ПолучитьПустойОбъектXDTO(ТипОбъектаКоллекции, ИмяПакетаXDTO);
		Заполнить_ЗначенИдентиф(ТекстИнф, AdditionalInfo);
		ОбъектXDTO.Добавить(ТекстИнф);
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_ЗаполнитьПодписанта(ИмяПакетаXDTO, ОбъектXDTO, Content, ИмяСвойства)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого Signer Из СписокXDTOИзЭлемента(Content, ИмяСвойства) Цикл
		
		Если Не ЗначениеЗаполнено(Signer.FirstName) Тогда 
			Продолжить;
		КонецЕсли;
		
		Подписант = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "Подписант");
		
		Если Signer.IsSoleProprietor Тогда
			
			ИП = НовыйДочернийОбъектXDTO(Фабрика, Подписант, "ИП");
			XML_ЗаполнитьИндивидуальногоПредпринимателяПодписанта(ИмяПакетаXDTO, ИП, Signer);
			
		Иначе
			
			ЮЛ = НовыйДочернийОбъектXDTO(Фабрика, Подписант, "ЮЛ");
			XML_ЗаполнитьЮридическоеЛицоПодписанта(ИмяПакетаXDTO, ЮЛ, Signer);
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_ЗаполнитьРасширенныхПодписантов(ИмяПакетаXDTO, ОбъектXDTO, Content)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Для Каждого ExtendedSigner Из СписокXDTOИзЭлемента(Content, "Signers") Цикл
		
		SignerDetails		 = ExtendedSigner.SignerDetails;
		Статус				 = XML_СтатусПодписанта(SignerDetails.Status);
		ОбластьПолномочий	 = XML_ОбластьПолномочийПодписанта(SignerDetails.Powers);
		
		// Сведения о лице, подписавшем информацию покупателя в электронном виде
		Подписант = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "Подписант");
		XML_ЗаполнитьРасширенногоПодписанта(ИмяПакетаXDTO, Подписант, SignerDetails, Статус, ОбластьПолномочий);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_ЗаполнитьРасширенногоПодписанта(ИмяПакетаXDTO, ОбъектXDTO, SignerDetails, Статус, ОбластьПолномочий)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "Статус"		, Статус);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ОблПолн"	, ОбластьПолномочий);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ОснПолнОрг"	, SignerDetails.OrganizationPowersBase);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ОснПолн") Тогда
		УстановитьЗначениеXDTO(ОбъектXDTO, "ОснПолн", SignerDetails.PowersBase);
	Иначе
		УстановитьЗначениеXDTO(ОбъектXDTO, "ОснПолнПодп", SignerDetails.PowersBase);
	КонецЕсли;
	
	Если SignerDetails.SignerType = "PhysicalPerson" Тогда
		
		// Физическое лицо
		ФЛ = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ФЛ");
		XML_ЗаполнитьФизическоеЛицоПодписанта(ИмяПакетаXDTO, ФЛ, SignerDetails);
		
	ИначеЕсли SignerDetails.SignerType = "IndividualEntity" Тогда
		
		// Индивидуальный предприниматель
		ИП = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ИП");
		XML_ЗаполнитьИндивидуальногоПредпринимателяПодписанта(ИмяПакетаXDTO, ИП, SignerDetails);
		
	ИначеЕсли SignerDetails.SignerType = "LegalEntity" Тогда
		
		// Представитель юридического лица
		ЮЛ = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ЮЛ");
		XML_ЗаполнитьЮридическоеЛицоПодписанта(ИмяПакетаXDTO, ЮЛ, SignerDetails);
		
	КонецЕсли;
	
КонецПроцедуры

// Хранит соответствие значений перечисления SignerPowers и областей полномочий в схемах ФНС.
// см. https://diadoc-sdk.readthedocs.io/ru/latest/proto/utd/ExtendedSignerDetailsToPost.html
//
// Параметры:
//  ИдентификаторОбласти - Строка - Значение перечисления SignerPowers.
// 
// Возвращаемое значение:
//  Число - значение области полномочий в соответствии с форматом ФНС (ОблПолн).
//
Функция XML_ОбластьПолномочийПодписанта(ИдентификаторОбласти)
	
	// BSLLS:MagicNumber-off
	СоответствиеПолномочий = Новый Соответствие;
	
	// лицо, ответственное за подписание счетов-фактур
	СоответствиеПолномочий.Вставить("InvoiceSigner", 0);
	
	// лицо, совершившее сделку, операцию
	СоответствиеПолномочий.Вставить("PersonMadeOperation", 1);
	
	// лицо, совершившее сделку, операцию и ответственное за её оформление
	СоответствиеПолномочий.Вставить("MadeAndSignOperation", 2);
	
	// лицо, ответственное за оформление свершившегося события
	СоответствиеПолномочий.Вставить("PersonDocumentedOperation", 3);
	
	// лицо, совершившее сделку, операцию и ответственное за подписание счетов-фактур
	СоответствиеПолномочий.Вставить("MadeOperationAndSignedInvoice", 4);
	
	// лицо, совершившее сделку, операцию и ответственное за её оформление и за подписание счетов-фактур
	СоответствиеПолномочий.Вставить("MadeAndResponsibleForOperationAndSignedInvoice", 5);
	
	// лицо, ответственное за оформление свершившегося события и за подписание счетов-фактур
	СоответствиеПолномочий.Вставить("ResponsibleForOperationAndSignerForInvoice", 6);
	
	// председатель комиссии
	СоответствиеПолномочий.Вставить("ChairmanCommission", 7);
	
	// член комиссии
	СоответствиеПолномочий.Вставить("MemberCommission", 8);
	
	// лицо, в полномочия которого входит утверждение документа, оформляющего событие (факт хозяйственной жизни)
	СоответствиеПолномочий.Вставить("PersonApprovedDocument", 21);
	
	// лицо, в полномочия которого входит подтверждение оформленного события (факта хозяйственной жизни)
	СоответствиеПолномочий.Вставить("PersonConfirmedDocument", 22);
	
	// лицо, в полномочия которого входит согласование документа, оформляющего событие (факт хозяйственной жизни)
	СоответствиеПолномочий.Вставить("PersonAgreedOnDocument", 23);
	
	// лицо с иными полномочиями
	СоответствиеПолномочий.Вставить("PersonOtherPower", 29);
	// BSLLS:MagicNumber-on
	
	Результат = СоответствиеПолномочий.Получить(ИдентификаторОбласти);
	
	Возврат Результат;
	
КонецФункции

// Хранит соответствие значений перечисления SignerStatus и статусов подписанта в схемах ФНС.
// см. https://diadoc-sdk.readthedocs.io/ru/latest/proto/utd/ExtendedSignerDetailsToPost.html
//
// Параметры:
//  ИдентификаторСтатуса - Строка - Значение перечисления SignerStatus.
// 
// Возвращаемое значение:
//  Число - значение статуса подписанта в соответствии с форматом ФНС (Статус)
//
Функция XML_СтатусПодписанта(ИдентификаторСтатуса)
	
	СоответствиеСтатусов = Новый Соответствие;
	
	// BSLLS:MagicNumber-off
	// Работник организации продавца товаров (работ, услуг, имущественных прав)
	СоответствиеСтатусов.Вставить("SellerEmployee", 1);
	
	// Работник организации - составителя информации продавца
	СоответствиеСтатусов.Вставить("InformationCreatorEmployee", 2);
	
	// Работник иной уполномоченной организации
	СоответствиеСтатусов.Вставить("OtherOrganizationEmployee", 3);
	
	// Уполномоченное физическое лицо (в том числе индивидуальный предприниматель
	СоответствиеСтатусов.Вставить("AuthorizedPerson", 4);
	
	// Работник организации - покупателя
	СоответствиеСтатусов.Вставить("BuyerEmployee", 5);
	
	// Работник организации - составителя файла обмена информации покупателя, если составитель файла обмена информации покупателя не является покупателем
	СоответствиеСтатусов.Вставить("InformationCreatorBuyerEmployee", 6);
	// BSLLS:MagicNumber-on
	
	Результат = СоответствиеСтатусов.Получить(ИдентификаторСтатуса);
	
	Возврат Результат;
	
КонецФункции

Функция XML_ПолучитьПризнакНоменклатуры(ItemMark)
	
	СоответствиеПризнаков = Новый Соответствие;
	
	// BSLLS:MagicNumber-off
	СоответствиеПризнаков.Вставить("NotSpecified", Неопределено); // - не указано
	СоответствиеПризнаков.Вставить("Property", 1); // - имущество
	СоответствиеПризнаков.Вставить("Job", 2); // - работа
	СоответствиеПризнаков.Вставить("Service", 3); // - услуга
	СоответствиеПризнаков.Вставить("PropertyRights", 4); // - имущественные права
	СоответствиеПризнаков.Вставить("Other", 5); // - иное
	// BSLLS:MagicNumber-on
	
	Возврат СоответствиеПризнаков.Получить(ItemMark);
	
КонецФункции

Функция XML_ОператорЭДО()
	
	Результат = Новый Структура;
	
	Результат.Вставить("Имя",	"АО ""ПФ ""СКБ Контур""");
	Результат.Вставить("ИНН",	"6663003127");
	Результат.Вставить("ID",	"2BM");
	
	Возврат Результат;
	
КонецФункции

Функция XML_ВерсияПрограммыФормированияФайла()
	
	// Версия программы, с помощью которой сформирован файл
	
	Результат = "Diadoc 1.0";
	
	Возврат Результат;
	
КонецФункции

Функция XML_ИдФайл(ИмяПакетаXDTO, ДопПрефикс, ИдентификаторПолучателя, ИдентификаторОтправителя, ПараметрыФормированияФайла)
	
	Результат = ИмяПакетаXDTO + ДопПрефикс
	+ "_" + ИдентификаторПолучателя
	+ "_" + ИдентификаторОтправителя
	+ "_" + Формат(ПараметрыФормированияФайла.ДатаФормирования, "ДФ=yyyyMMdd")
	+ "_" + ПараметрыФормированияФайла.УникальныйИдентификатор;
	
	Возврат Результат;
	
КонецФункции

Процедура XML_ЗаполнитьФИОТип(ОбъектXDTO, Content)
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "Фамилия", Content.Surname);
	УстановитьЗначениеXDTO(ОбъектXDTO, "Имя"	, Content.Firstname);
	
	Если ЗначениеЗаполнено(Content.Patronymic) Тогда
		УстановитьЗначениеXDTO(ОбъектXDTO, "Отчество", Content.Patronymic);
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ЗаполнитьДолжЛицоТип(ИмяПакетаXDTO, ОбъектXDTO, Official)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ФИО");
	XML_ЗаполнитьФИОТип(ФИО, Official);
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "Должность", Official.JobTitle);
	
КонецПроцедуры

Процедура XML_ДоверенностьТип(ИмяПакетаXDTO, ОбъектXDTO, Attorney)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "ДатаДоверен", Дата_В_Формат(Attorney.Date, ФорматДаты));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Attorney, "Number") Тогда
		УстановитьЗначениеXDTO(ОбъектXDTO, "НомДоверен", Attorney.Number);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Attorney.Issuer.Firstname) Тогда
		
		// Доверенность выдана кем
		ВыданаКем = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ВыданаКем");
		
		ФИО = НовыйДочернийОбъектXDTO(Фабрика, ВыданаКем, "ФИО");
		XML_ЗаполнитьФИОТип(ФИО, Attorney.Issuer);
		
		УстановитьЗначениеXDTO(ВыданаКем, "ДолжнКем", Attorney.Issuer.JobTitle);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Attorney, "IssuerOrganizationName") Тогда
			УстановитьЗначениеXDTO(ВыданаКем, "НаимОргКем", Attorney.IssuerOrganizationName);
		КонецЕсли;
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Attorney, "IssuerAdditionalInfo") Тогда
			УстановитьЗначениеXDTO(ВыданаКем, "ДопСведКем", Attorney.IssuerAdditionalInfo);
		КонецЕсли;
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(Attorney.Recipient.Firstname) Тогда
		
		// Доверенность выдана кому
		ВыданаКому = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ВыданаКому");
		
		ФИО = НовыйДочернийОбъектXDTO(Фабрика, ВыданаКому, "ФИО");
		XML_ЗаполнитьФИОТип(ФИО, Attorney.Recipient);
		
		УстановитьЗначениеXDTO(ВыданаКому, "Должн", Attorney.Recipient.JobTitle);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Attorney, "RecipientAdditionalInfo") Тогда
			УстановитьЗначениеXDTO(ВыданаКому, "ДопСведКому", Attorney.RecipientAdditionalInfo);
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_УдалитьОбъявлениеПрефиксовПространстваИмен(ИмяПакетаXDTO, СтрокаXML)
	
	СтрокаXML = СтрЗаменить(СтрокаXML, "xmlns=""" + ИмяПакетаXDTO + """ xmlns:xs=""http://www.w3.org/2001/XMLSchema"" xmlns:xsi=""http://www.w3.org/2001/XMLSchema-instance""", "");
	СтрокаXML = СтрЗаменить(СтрокаXML, "xmlns=""" + ИмяПакетаXDTO + """ xmlns:xsd=""http://www.w3.org/2001/XMLSchema"" xmlns:xsi=""http://www.w3.org/2001/XMLSchema-instance""", "");
	
КонецПроцедуры

Процедура XML_ЗаполнитьРаботникаОрганизации(ИмяПакетаXDTO, ОбъектXDTO, Employee)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ФИО");
	XML_ЗаполнитьФИОТип(ФИО, Employee);
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "Должность"	, Employee.EmployeePosition);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИныеСвед"	, Employee.EmployeeInfo);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ОснПолн"	, Employee.EmployeeBase);
	
КонецПроцедуры

Процедура XML_ЗаполнитьПредставителяОрганизации(ИмяПакетаXDTO, ОбъектXDTO, OtherIssuer)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ФИО");
	XML_ЗаполнитьФИОТип(ФИО, OtherIssuer);
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "Должность"	, OtherIssuer.EmployeePosition);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИныеСвед"	, OtherIssuer.EmployeeInfo);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "НаимОргПер") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "НаимОргПер"		, OtherIssuer.OrganizationName);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ОснДоверОргПер"	, OtherIssuer.OrganizationBase);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ОснПолнПредПер"	, OtherIssuer.EmployeeBase);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "НаимОргПол") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "НаимОргПол"		, OtherIssuer.OrganizationName);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ОснДоверОргПол"	, OtherIssuer.OrganizationBase);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ОснПолнПредПол"	, OtherIssuer.EmployeeBase);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "НаимОргПрин") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "НаимОргПрин"	, OtherIssuer.OrganizationName);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ОснДоверОргПрин", OtherIssuer.OrganizationBase);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ОснПолнПредПрин", OtherIssuer.EmployeeBase);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ЗаполнитьЮридическоеЛицоКонтрагента(ИмяПакетаXDTO, ОбъектXDTO, ПротоКонтент)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "СвЮЛУч") Тогда
		
		// 155@, 820@, 970@
		СвЮЛУч = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "СвЮЛУч");
		
		УстановитьЗначениеXDTO(СвЮЛУч, "НаимОрг", ПротоКонтент.Name);
		
		Если ЗначениеЗаполнено(ПротоКонтент.Inn) Тогда
			УстановитьЗначениеXDTO(СвЮЛУч, "ИННЮЛ", ПротоКонтент.Inn);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(ПротоКонтент.Kpp) Тогда
			УстановитьЗначениеXDTO(СвЮЛУч, "КПП", ПротоКонтент.Kpp);
		КонецЕсли;
		
		// 820@
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвЮЛУч, "ДефИННЮЛ") Тогда
			Если ЭтоИстина(ПротоКонтент.HyphenInn)
				Или Не ЗначениеЗаполнено(ПротоКонтент.Inn) Тогда
				УстановитьЗначениеXDTO(СвЮЛУч, "ДефИННЮЛ", СтрокаДефис());
			КонецЕсли;
		КонецЕсли;
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "СвОрг") Тогда
		
		// 551@, 552@
		СвОрг = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "СвОрг");
		СвЮЛ = НовыйДочернийОбъектXDTO(Фабрика, СвОрг, "СвЮЛ");
		
		УстановитьЗначениеXDTO(СвЮЛ, "НаимОрг"	, ПротоКонтент.Name);
		УстановитьЗначениеXDTO(СвЮЛ, "ИННЮЛ"	, ПротоКонтент.Inn);
		УстановитьЗначениеXDTO(СвЮЛ, "КПП"		, ПротоКонтент.Kpp);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ЗаполнитьИндивидуальногоПредпринимателяКонтрагента_820(ИмяПакетаXDTO, ОбъектXDTO, ПротоКонтент)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	СвИП = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "СвИП");
	XML_ЗаполнитьОбщиеСведенияОбИндивидуальномПредпринимателе(ИмяПакетаXDTO, СвИП, ПротоКонтент);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвИП, "ДефИННФЛ") Тогда
		Если ЭтоИстина(ПротоКонтент.HyphenInn)
			Или Не ЗначениеЗаполнено(ПротоКонтент.Inn) Тогда
			УстановитьЗначениеXDTO(СвИП, "ДефИННФЛ", СтрокаДефис());
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры

// 970@
Процедура XML_ЗаполнитьСведенияОбИндивидуальномПредпринимателе(ИмяПакетаXDTO, СвИП, ПротоКонтент)
	
	XML_ЗаполнитьОбщиеСведенияОбИндивидуальномПредпринимателе(ИмяПакетаXDTO, СвИП, ПротоКонтент);
	
	Если ЗначениеЗаполнено(ПротоКонтент.IndividualEntityRegistrationDate) Тогда
		ДатаОГРНИП = Формат(ПротоКонтент.IndividualEntityRegistrationDate, "ДФ=dd.MM.yyyy");
		УстановитьЗначениеXDTO(СвИП, "ДатаОГРНИП", ДатаОГРНИП);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СвИП, "ОГРНИП", ПротоКонтент.IndividualEntityRegistrationNumber);
	
КонецПроцедуры

Процедура XML_ЗаполнитьОбщиеСведенияОбИндивидуальномПредпринимателе(ИмяПакетаXDTO, СвИП, ПротоКонтент)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(СвИП, "ИныеСвед", ПротоКонтент.OrganizationOrPersonInfo);
	УстановитьЗначениеXDTO(СвИП, "СвГосРегИП", ПротоКонтент.IndividualEntityRegistrationCertificate);
	
	Если ЗначениеЗаполнено(ПротоКонтент.Inn) Тогда
		УстановитьЗначениеXDTO(СвИП, "ИННФЛ", ПротоКонтент.Inn);
	КонецЕсли;
	
	СтруктураФИО = XML_ПротоСтруктураФИО(ПротоКонтент.Name);
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, СвИП, "ФИО");
	XML_ЗаполнитьФИОТип(ФИО, СтруктураФИО);
	
КонецПроцедуры

Процедура XML_ЗаполнитьФизическоеЛицоКонтрагента(ИмяПакетаXDTO, ОбъектXDTO, ПротоКонтент)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "СвФЛУчастФХЖ") Тогда
		ИмяСвойства = "СвФЛУчастФХЖ"; // 820@
	Иначе
		ИмяСвойства = "СвФЛ"; // 551@, 552@
	КонецЕсли;
	
	СвТип = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, ИмяСвойства);
	
	Если ЗначениеЗаполнено(ПротоКонтент.Inn) Тогда
		УстановитьЗначениеXDTO(СвТип, "ИННФЛ", ПротоКонтент.Inn);
	КонецЕсли;
	
	// 820@
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвТип, "ДефИННФЛ") Тогда
		Если ЭтоИстина(ПротоКонтент.HyphenInn)
			Или Не ЗначениеЗаполнено(ПротоКонтент.Inn) Тогда
			УстановитьЗначениеXDTO(СвТип, "ДефИННФЛ", СтрокаДефис());
		КонецЕсли;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СвТип, "ИныеСвед", ПротоКонтент.OrganizationOrPersonInfo);
	
	СтруктураФИО = XML_ПротоСтруктураФИО(ПротоКонтент.Name);
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, СвТип, "ФИО");
	XML_ЗаполнитьФИОТип(ФИО, СтруктураФИО);
	
КонецПроцедуры

// 970@
Процедура XML_ЗаполнитьСведенияОФизическомЛице(ИмяПакетаXDTO, СвФЛУч, ПротоКонтент)
	
	Если ЗначениеЗаполнено(ПротоКонтент.Inn) Тогда
		УстановитьЗначениеXDTO(СвФЛУч, "ИННФЛ", ПротоКонтент.Inn);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПротоКонтент.PhysicalEntityStatusID) Тогда
		УстановитьЗначениеXDTO(СвФЛУч, "ИдСтатЛ", ПротоКонтент.PhysicalEntityStatusID);
	КонецЕсли;
	
	УстановитьЗначениеXDTO(СвФЛУч, "ИныеСвед", ПротоКонтент.OrganizationOrPersonInfo);
	
	СтруктураФИО = XML_ПротоСтруктураФИО(ПротоКонтент.Name);
	
	ФИОЗаполнено = ЗначениеЗаполнено(СтруктураФИО.Surname)
		И ЗначениеЗаполнено(СтруктураФИО.Firstname);
		
	Если ФИОЗаполнено Тогда
		Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
		ФИО = НовыйДочернийОбъектXDTO(Фабрика, СвФЛУч, "ФИО");
		XML_ЗаполнитьФИОТип(ФИО, СтруктураФИО);
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ЗаполнитьИностранноеЛицоКонтрагента(ИмяПакетаXDTO, ОбъектXDTO, ПротоКонтент)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "СвИнНеУч") Тогда
		
		// 155@, 820@
		СвИнНеУч = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "СвИнНеУч");
		
		УстановитьЗначениеXDTO(СвИнНеУч, "НаимОрг"	, ПротоКонтент.Name);
		УстановитьЗначениеXDTO(СвИнНеУч, "ИныеСвед"	, ПротоКонтент.OrganizationOrPersonInfo);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвИнНеУч, "Идентиф") Тогда
			// 820@
			УстановитьЗначениеXDTO(СвИнНеУч, "Идентиф", ПротоКонтент.LegalEntityId);
		КонецЕсли;
		
	Иначе
		
		// 551@, 552@
		СвОрг = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO	, "СвОрг");
		ИнОрг = НовыйДочернийОбъектXDTO(Фабрика, СвОрг		, "ИнОрг");
		
		УстановитьЗначениеXDTO(ИнОрг, "НаимОрг"	, ПротоКонтент.Name);
		УстановитьЗначениеXDTO(ИнОрг, "Страна"	, ПротоКонтент.Country);
		УстановитьЗначениеXDTO(ИнОрг, "ИныеСвед", ПротоКонтент.OrganizationOrPersonInfo);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ЗаполнитьФизическоеЛицо(ИмяПакетаXDTO, ОбъектXDTO, OtherIssuer)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ФИО");
	XML_ЗаполнитьФИОТип(ФИО, OtherIssuer);
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИныеСвед", OtherIssuer.EmployeeInfo);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ОснДоверФЛ") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "ОснДоверФЛ", OtherIssuer.EmployeeBase);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ОснДоверФЛПер") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "ОснДоверФЛПер", OtherIssuer.EmployeeBase);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ЗаполнитьФизическоеЛицоПодписанта(ИмяПакетаXDTO, ОбъектXDTO, SignerDetails)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ФИО");
	XML_ЗаполнитьФИОТип(ФИО, SignerDetails);
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИННФЛ"		, SignerDetails.Inn);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИныеСвед"	, SignerDetails.SignerInfo);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ГосРегИПВыдДов") Тогда
		УстановитьЗначениеXDTO(ОбъектXDTO, "ГосРегИПВыдДов", SignerDetails.RegistrationCertificate);
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ЗаполнитьИндивидуальногоПредпринимателяПодписанта(ИмяПакетаXDTO, ОбъектXDTO, ПротоКонтент)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ФИО");
	XML_ЗаполнитьФИОТип(ФИО, ПротоКонтент);
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИННФЛ", ПротоКонтент.Inn);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "RegistrationCertificate") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "СвГосРегИП", ПротоКонтент.RegistrationCertificate);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "SoleProprietorRegistrationCertificate") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "СвГосРегИП", ПротоКонтент.SoleProprietorRegistrationCertificate);
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ИныеСвед") Тогда
		УстановитьЗначениеXDTO(ОбъектXDTO, "ИныеСвед", ПротоКонтент.SignerInfo);
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ЗаполнитьЮридическоеЛицоПодписанта(ИмяПакетаXDTO, ОбъектXDTO, ПротоКонтент)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ФИО");
	XML_ЗаполнитьФИОТип(ФИО, ПротоКонтент);
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИННЮЛ", ПротоКонтент.Inn);
	УстановитьЗначениеXDTO(ОбъектXDTO, "Должн", ПротоКонтент.JobTitle);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "НаимОрг") Тогда
		УстановитьЗначениеXDTO(ОбъектXDTO, "НаимОрг", ПротоКонтент.OrganizationName);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ИныеСвед") Тогда
		УстановитьЗначениеXDTO(ОбъектXDTO, "ИныеСвед", ПротоКонтент.SignerInfo);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ГосРегИПВыдДов") Тогда
		УстановитьЗначениеXDTO(ОбъектXDTO, "ГосРегИПВыдДов", ПротоКонтент.RegistrationCertificate);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ГосРегИППодп") Тогда
		УстановитьЗначениеXDTO(ОбъектXDTO, "ГосРегИППодп", ПротоКонтент.RegistrationCertificate);
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ДобавитьФайлыЭлектронныхПодписей(ИмяПакетаXDTO, ОбъектXDTO, ФайлыПодписей)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	МассивФайлов = ЗначениеВМассив(ФайлыПодписей);
	
	Для каждого ФайлПодписи Из МассивФайлов Цикл
		
		НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ЭП", Base64СтрокаИзДвоичныхДанных(ФайлПодписи));
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_ЗаполнитьИнфоПоляФХЖ(ИмяПакетаXDTO, ОбъектXDTO, AdditionalInfoId, ИмяИнфоПоля, ТипОбъектаКоллекции = Неопределено)
	
	Если НЕ ЗначениеЗаполнено(AdditionalInfoId.InfoFileId)
		И НЕ ЗначениеЗаполнено(AdditionalInfoId.StructedAdditionalInfos) Тогда
		Возврат;
	КонецЕсли;
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	// Информационное поле факта хозяйственной жизни
	ИнфПолФХЖ = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, ИмяИнфоПоля);
	УстановитьЗначениеXDTO(ИнфПолФХЖ, "ИдФайлИнфПол", AdditionalInfoId.InfoFileId);
	
	XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, ИнфПолФХЖ.ТекстИнф, AdditionalInfoId.StructedAdditionalInfos, ТипОбъектаКоллекции);
	
КонецПроцедуры

Процедура XML_ЗаполнитьИнфПолТип(ИмяПакетаXDTO, ИнфПолТип, AdditionalInfoId)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(ИнфПолТип, "ИдФайлИнфПол", ПолучитьЗначениеXDTO(AdditionalInfoId, "InfoFileId"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AdditionalInfoId, "AdditionalInfo") Тогда
		
		Для Каждого AdditionalInfo Из AdditionalInfoId.AdditionalInfo Цикл
			ТекстИнф = НовыйДочернийОбъектXDTO(Фабрика, ИнфПолТип, "ТекстИнф");
			XML_ЗаполнитьТекстИнфТип(ТекстИнф, AdditionalInfo);
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ЗаполнитьТекстИнфТип(ТекстИнфТип, AdditionalInfo)
	
	УстановитьЗначениеXDTO(ТекстИнфТип, "Идентиф", AdditionalInfo.Id);
	УстановитьЗначениеXDTO(ТекстИнфТип, "Значен", AdditionalInfo.Value);
	
КонецПроцедуры

Процедура XML_ЗаполнитьМасГрузТип(МасГрузТип, WeightCargo, ПараметрыФормированияФайла)
	
	ТочностьКоличества = ПараметрыФормированияФайла.ТочностьКоличества;
	
	УстановитьЗначениеXDTO(МасГрузТип, "МассаБр", XML_ФорматЧисла(WeightCargo.GrossWeight, ТочностьКоличества));
	УстановитьЗначениеXDTO(МасГрузТип, "МассаНет", XML_ФорматЧисла(WeightCargo.NetWeight, ТочностьКоличества));
	
КонецПроцедуры

Процедура XML_ЗаполнитьСопрДокТип(СопрДокТип, AccompanyingDocument)
	
	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	ДатаСопрДок = ПолучитьЗначениеXDTO(AccompanyingDocument, "Date");
	
	УстановитьЗначениеXDTO(СопрДокТип, "НаимСопрДок", AccompanyingDocument.Name);
	УстановитьЗначениеXDTO(СопрДокТип, "НомСопрДок", ПолучитьЗначениеXDTO(AccompanyingDocument, "Number"));
	УстановитьЗначениеXDTO(СопрДокТип, "ДопИдСопрДок", ПолучитьЗначениеXDTO(AccompanyingDocument, "AdditionalIdentifier"));
	
	Если ЗначениеЗаполнено(ДатаСопрДок) Тогда
		УстановитьЗначениеXDTO(СопрДокТип, "ДатаСопрДок", Дата_В_Формат(ДатаСопрДок, ФорматДаты));
	КонецЕсли;

КонецПроцедуры

Процедура XML_ЗаполнитьРабОргПок(ИмяПакетаXDTO, РабОргПок, Employee)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ФИО = НовыйДочернийОбъектXDTO(Фабрика, РабОргПок, "ФИО");
	
	УстановитьЗначениеXDTO(ФИО, "Фамилия", Employee.LastName);
	УстановитьЗначениеXDTO(ФИО, "Имя", Employee.FirstName);
	УстановитьЗначениеXDTO(ФИО, "Отчество", ПолучитьЗначениеXDTO(Employee, "MiddleName"));
	
	УстановитьЗначениеXDTO(РабОргПок, "Должность", Employee.Position);
	УстановитьЗначениеXDTO(РабОргПок, "ИныеСвед", ПолучитьЗначениеXDTO(Employee, "EmployeeInfo"));
	УстановитьЗначениеXDTO(РабОргПок, "ОснПолн", ПолучитьЗначениеXDTO(Employee, "EmployeeBase"));
	
КонецПроцедуры

Процедура XML_ЗаполнитьИнЛицо(ИмяПакетаXDTO, ИнЛицо, OtherIssuer)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Должность = ПолучитьЗначениеXDTO(OtherIssuer, "Position");
	Если ЗначениеЗаполнено(Должность) Тогда
		
		ПредОргПер = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ПредОргПер");
		
		ФИО = НовыйДочернийОбъектXDTO(Фабрика, ПредОргПер, "ФИО");
		
		УстановитьЗначениеXDTO(ФИО, "Фамилия", OtherIssuer.LastName);
		УстановитьЗначениеXDTO(ФИО, "Имя", OtherIssuer.FirstName);
		УстановитьЗначениеXDTO(ФИО, "Отчество", ПолучитьЗначениеXDTO(OtherIssuer, "MiddleName"));
		
		УстановитьЗначениеXDTO(ПредОргПер, "Должность", Должность);
		УстановитьЗначениеXDTO(ПредОргПер, "ИныеСвед", ПолучитьЗначениеXDTO(OtherIssuer, "EmployeeInfo"));
		УстановитьЗначениеXDTO(ПредОргПер, "ОснПолнПредПрин", ПолучитьЗначениеXDTO(OtherIssuer, "EmployeeBase"));
		УстановитьЗначениеXDTO(ПредОргПер, "НаимОргПрин", ПолучитьЗначениеXDTO(OtherIssuer, "OrganizationName"));
		УстановитьЗначениеXDTO(ПредОргПер, "ОснДоверОргПрин", ПолучитьЗначениеXDTO(OtherIssuer, "OrganizationBase"));
		
	Иначе
		
		ФЛПрин = НовыйДочернийОбъектXDTO(Фабрика, ИнЛицо, "ФЛПрин");
		
		ФИО = НовыйДочернийОбъектXDTO(Фабрика, ФЛПрин, "ФИО");
		
		УстановитьЗначениеXDTO(ФИО, "Фамилия", OtherIssuer.LastName);
		УстановитьЗначениеXDTO(ФИО, "Имя", OtherIssuer.FirstName);
		УстановитьЗначениеXDTO(ФИО, "Отчество", ПолучитьЗначениеXDTO(OtherIssuer, "MiddleName"));
		
		УстановитьЗначениеXDTO(ФЛПрин, "ИныеСвед", ПолучитьЗначениеXDTO(OtherIssuer, "EmployeeInfo"));
		УстановитьЗначениеXDTO(ФЛПрин, "ОснДоверФЛ", ПолучитьЗначениеXDTO(OtherIssuer, "EmployeeBase"));
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ЗаполнитьПринТовТип(ИмяПакетаXDTO, ПринТовТип, AcceptedProductData, ПараметрыФормированияФайла)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ТочностьКоличества = ПараметрыФормированияФайла.ТочностьКоличества;
	ТочностьЦены = ПараметрыФормированияФайла.ТочностьЦены;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptedProductData, "ItemIdentificationNumbers") Тогда
		
		Для Каждого ItemIdentificationNumber Из AcceptedProductData.ItemIdentificationNumbers.ItemIdentificationNumber Цикл
			НомСредИдентТов = НовыйДочернийОбъектXDTO(Фабрика, ПринТовТип, "НомСредИдентТов");
			XML_Торг2_ЗаполнитьНомСредИдентТов(ИмяПакетаXDTO, НомСредИдентТов, ItemIdentificationNumber);
		КонецЦикла;
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(ПринТовТип, "КолТовПрин", XML_ФорматЧисла(AcceptedProductData.Quantity, ТочностьКоличества));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptedProductData, "Price")
		И AcceptedProductData.Price <> Неопределено Тогда
		УстановитьЗначениеXDTO(ПринТовТип, "ЦенаПрин", XML_ФорматЧисла(AcceptedProductData.Price, ТочностьЦены));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptedProductData, "TaxRate")
		И AcceptedProductData.TaxRate <> Неопределено Тогда
		УстановитьЗначениеXDTO(ПринТовТип, "СтавНДСПрин", НалСт_Из_TaxRate(ПолучитьЗначениеXDTO(AcceptedProductData, "TaxRate")));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptedProductData, "Subtotal")
		И AcceptedProductData.Subtotal <> Неопределено Тогда
		УстановитьЗначениеXDTO(ПринТовТип, "СтоимТовНДСПрин", XML_ФорматЧисла(AcceptedProductData.Subtotal, ТочностьЦены));
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(AcceptedProductData, "Vat")
		И AcceptedProductData.Vat <> Неопределено Тогда
		УстановитьЗначениеXDTO(ПринТовТип, "СумНДСПрин", XML_ФорматЧисла(AcceptedProductData.Vat, ТочностьЦены));
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ЗаполнитьРасхТип(ИмяПакетаXDTO, РасхТип, DiscrepancyData, ПараметрыФормированияФайла)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ТочностьЦены = ПараметрыФормированияФайла.ТочностьЦены;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(DiscrepancyData, "ItemIdentificationNumbers") Тогда
		
		Для Каждого ItemIdentificationNumber Из DiscrepancyData.ItemIdentificationNumbers.ItemIdentificationNumber Цикл
			НомСредИдентТов = НовыйДочернийОбъектXDTO(Фабрика, РасхТип, "НомСредИдентТов");
			XML_Торг2_ЗаполнитьНомСредИдентТов(ИмяПакетаXDTO, НомСредИдентТов, ItemIdentificationNumber);
		КонецЦикла;
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(РасхТип, "КолТовРасх", DiscrepancyData.Quantity);
	УстановитьЗначениеXDTO(РасхТип, "СтоимТовНДСРасх", XML_ФорматЧисла(DiscrepancyData.Subtotal, ТочностьЦены));
	УстановитьЗначениеXDTO(РасхТип, "СумНДСРасх", XML_ФорматЧисла(DiscrepancyData.Vat, ТочностьЦены));
	УстановитьЗначениеXDTO(РасхТип, "ПоясненияРасх", ПолучитьЗначениеXDTO(DiscrepancyData, "Explanations"));
	
КонецПроцедуры

Процедура XML_ЗаполнитьДельтаТип(ИмяПакетаXDTO, ДельтаТип, Delta, ПараметрыФормированияФайла)
	
	ТочностьЦены = ПараметрыФормированияФайла.ТочностьЦены;
	
	УстановитьЗначениеXDTO(ДельтаТип, "ДельтаЦена", XML_ФорматЧисла(Delta.Price, ТочностьЦены));
	УстановитьЗначениеXDTO(ДельтаТип, "ДельтаСтоимНДС", XML_ФорматЧисла(Delta.Subtotal, ТочностьЦены));
	УстановитьЗначениеXDTO(ДельтаТип, "ДельтаСумНДС", XML_ФорматЧисла(Delta.Vat, ТочностьЦены));
	УстановитьЗначениеXDTO(ДельтаТип, "ПояснДельта", ПолучитьЗначениеXDTO(Delta, "Explanations"));
	
КонецПроцедуры

Процедура XML_Торг2_ЗаполнитьНомСредИдентТов(ИмяПакетаXDTO, НомСредИдентТов, ItemIdentificationNumber)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ItemIdentificationNumber, "Unit") Тогда
		Для Каждого Unit Из ItemIdentificationNumber.Unit Цикл
			НомСредИдентТов.КИЗ.Добавить(Unit);
		КонецЦикла;
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ItemIdentificationNumber, "PackageId") Тогда
		Для Каждого PackageId Из ItemIdentificationNumber.PackageId Цикл
			НомСредИдентТов.НомУпак.Добавить(PackageId);
		КонецЦикла;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(НомСредИдентТов, "ИдентТрансУпак", ПолучитьЗначениеXDTO(ItemIdentificationNumber, "TransPackageId"));
	
КонецПроцедуры

Процедура XML_ЗаполнитьКодСодержанияОперации(ИмяПакетаXDTO, ОбъектXDTO, ContentOperCode)
	
	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "КодИтога",		ContentOperCode.TotalCode);
	УстановитьЗначениеXDTO(ОбъектXDTO, "НаимДокРасх",	ContentOperCode.NameDiscrepDocument);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ВидДокРасх",	ContentOperCode.TypeDiscrepDocument);
	УстановитьЗначениеXDTO(ОбъектXDTO, "НомДокРасх",	ContentOperCode.NumberDiscrepDocument);
	УстановитьЗначениеXDTO(ОбъектXDTO, "ДатаДокРасх",	Дата_В_Формат(ContentOperCode.DateDiscrepDocument, ФорматДаты));
	УстановитьЗначениеXDTO(ОбъектXDTO, "ИдФайлДокРасх",	ContentOperCode.IdDiscrepDocument);
	
КонецПроцедуры

Процедура XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияФайла(ИмяПакетаXDTO
	, Файл
	, ВерсияФормата
	, ИдентификаторПолучателя
	, ИдентификаторОтправителя
	, ПараметрыФормированияФайла
	, ДопПрефикс = "")
	
	ВерсПрог	 = "Diadoc 1.0";
	ОператорЭДО	 = XML_ОператорЭДО();
	Фабрика		 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ИдФайл		 = XML_ИдФайл(ИмяПакетаXDTO, ДопПрефикс, ИдентификаторПолучателя, ИдентификаторОтправителя, ПараметрыФормированияФайла);
	
	УстановитьЗначениеXDTO(Файл, "ИдФайл"	, ИдФайл);
	УстановитьЗначениеXDTO(Файл, "ВерсПрог"	, ВерсПрог);
	УстановитьЗначениеXDTO(Файл, "ВерсФорм"	, ВерсияФормата);
	
	// Сведения об участниках электронного документооборота
	СвУчДокОбор = НовыйДочернийОбъектXDTO(Фабрика, Файл, "СвУчДокОбор");
	
	УстановитьЗначениеXDTO(СвУчДокОбор, "ИдОтпр", ИдентификаторОтправителя);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвУчДокОбор, "ИдПол") Тогда
		УстановитьЗначениеXDTO(СвУчДокОбор, "ИдПол", ИдентификаторПолучателя);
	Иначе
		УстановитьЗначениеXDTO(СвУчДокОбор, "ИдПок", ИдентификаторПолучателя);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвУчДокОбор, "СвОЭДОтпр") Тогда
		
		// Сведения об операторе
		СвОЭДОтпр = НовыйДочернийОбъектXDTO(Фабрика, СвУчДокОбор, "СвОЭДОтпр");
		УстановитьЗначениеXDTO(СвОЭДОтпр, "НаимОрг"	, ОператорЭДО.Имя);
		УстановитьЗначениеXDTO(СвОЭДОтпр, "ИННЮЛ"	, ОператорЭДО.ИНН);
		УстановитьЗначениеXDTO(СвОЭДОтпр, "ИдЭДО"	, ОператорЭДО.ID);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СвУчДокОбор, "СвОЭДОтпрСФ") Тогда
		
		// Сведения об операторе приказ №172 (устарел)
		// 423@ (Торг2)
		СвОЭДОтпрСФ = НовыйДочернийОбъектXDTO(Фабрика, СвУчДокОбор, "СвОЭДОтпрСФ");
		УстановитьЗначениеXDTO(СвОЭДОтпрСФ, "НаимОрг"	, ОператорЭДО.Имя);
		УстановитьЗначениеXDTO(СвОЭДОтпрСФ, "ИННЮЛ"		, ОператорЭДО.ИНН);
		УстановитьЗначениеXDTO(СвОЭДОтпрСФ, "ИдЭДОСФ"	, ОператорЭДО.ID);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ОтветныйТитул_ЗаполнитьОсновныеСведенияПокупателя(ОбъектXDTO, ПротоКонтент, КНД, ПараметрыФормированияФайла)
	
	УстановитьЗначениеXDTO(ОбъектXDTO, "КНД", КНД);
	
	ДатаФормирования	 = Дата_В_Формат(ПараметрыФормированияФайла.ДатаФормирования, "ДФ=dd.MM.yyyy");
	ВремяФормирования	 = Дата_В_Формат(ПараметрыФормированияФайла.ДатаФормирования, "ДФ=HH.mm.ss");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ДатаИнфПок") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "ДатаИнфПок", ДатаФормирования);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ВремИнфПок", ВремяФормирования);
	
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ДатаИнфЗак") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "ДатаИнфЗак", ДатаФормирования);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ВремИнфЗак", ВремяФормирования);
	
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ДатаДок") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "ДатаДок", ДатаФормирования);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ВремДок", ВремяФормирования);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ДатаИнфПол") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "ДатаИнфПол", ДатаФормирования);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ВрИнфПол", ВремяФормирования);
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "НаимЭконСубСост") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "DocumentCreator") Тогда
			СоставительФайла = ПротоКонтент.DocumentCreator;
		Иначе
			СоставительФайла = ПротоКонтент.Creator;
		КонецЕсли;
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "НаимЭконСубСост", СоставительФайла);
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ОснДоверОргСост") Тогда
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ПротоКонтент, "DocumentCreatorBase") Тогда
			ОснованиеСоставителя = ПротоКонтент.DocumentCreatorBase;
		Иначе
			ОснованиеСоставителя = ПротоКонтент.CreatorBase;
		КонецЕсли;
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "ОснДоверОргСост", ОснованиеСоставителя);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ОтветныйТитул_ЗаполнитьСведенияПродавца(ИмяПакетаXDTO
	, ОбъектXDTO
	, ИмяФайла
	, ДатаФайла
	, ВремяФайла
	, ФайлыПодписей = Неопределено)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ИдФайлИнфПр") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "ИдФайлИнфПр"	, ИмяФайла);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ДатаФайлИнфПр"	, ДатаФайла);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ВремФайлИнфПр"	, ВремяФайла);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ИдФайлИнфИсп") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "ИдФайлИнфИсп"	, ИмяФайла);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ДатаФайлИнфИсп"	, ДатаФайла);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ВремФайлИнфИсп"	, ВремяФайла);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ИдФайлТН") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "ИдФайлТН"	, ИмяФайла);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ДатаДокТН"	, ДатаФайла);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ВремДокТН"	, ВремяФайла);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ИдФайлАктИ") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "ИдФайлАктИ"	, ИмяФайла);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ДатаДокАктИ", ДатаФайла);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ВремДокАктИ", ВремяФайла);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ИдФайлИнфПок") Тогда
		
		УстановитьЗначениеXDTO(ОбъектXDTO, "ИдФайлИнфПок"	, ИмяФайла);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ДатаФайлИнфПок"	, ДатаФайла);
		УстановитьЗначениеXDTO(ОбъектXDTO, "ВремФайлИнфПок"	, ВремяФайла);
		
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ФайлыПодписей) Тогда
		XML_ДобавитьФайлыЭлектронныхПодписей(ИмяПакетаXDTO, ОбъектXDTO, ФайлыПодписей)
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ОтветныйТитул_УПД_ЗаполнитьИнфПокГосЗакКазн(ИмяПакетаXDTO, ИнфПок, ПротоКонтент)
	
	Если ЗначениеЗаполнено(ПротоКонтент.BuyerInfoCircumPublicProc.PersonalAccountBuyer) Тогда
		
		Фабрика			 = XML_ФабрикаXDTO(ИмяПакетаXDTO);
		ИнфПокГосЗакКазн = НовыйДочернийОбъектXDTO(Фабрика, ИнфПок,	"ИнфПокГосЗакКазн");
		
		ФорматДаты		= "ДФ=dd.MM.yyyy";
		КонтентГосЗак	= ПротоКонтент.BuyerInfoCircumPublicProc;
		
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "ИдКодЗак",		КонтентГосЗак.ProcCode);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "ЛицСчетПок",		КонтентГосЗак.PersonalAccountBuyer);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "НаимФинОргПок",	КонтентГосЗак.NameFinAuthority);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "НомРеестрЗапПок",	КонтентГосЗак.BuyerRegistryEntryNumber);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "УчНомБюдОбязПок",	КонтентГосЗак.BuyerLiabNumber);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "КодКазначПок",	КонтентГосЗак.BuyerTreasuryCode);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "НаимКазначПок",	КонтентГосЗак.SellerTreasuryName);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "ОКТМОПок",		КонтентГосЗак.OKTMOBuy);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "ОКТМОМесПост",	КонтентГосЗак.OKTMOPlaceDelivery);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "ДатаОплПред",		Дата_В_Формат(КонтентГосЗак.PayDeadLine, ФорматДаты));
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "УчНомДенОбяз",	КонтентГосЗак.NumberFundsLiab);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "ОчерПлат",		КонтентГосЗак.PriorityPayment);
		УстановитьЗначениеXDTO(ИнфПокГосЗакКазн, "ВидПлат",			КонтентГосЗак.TypePayment);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(КонтентГосЗак, "InfoFundsLiab") И ЗначениеЗаполнено(КонтентГосЗак.InfoFundsLiab) Тогда
			XML_ОтветныйТитул_УПД_820_ЗаполнитьИнфСведДенОбяз(ИмяПакетаXDTO, ИнфПокГосЗакКазн, КонтентГосЗак.InfoFundsLiab, "ИнфСведДенОбяз");
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Функция XML_ПротоСтруктураФИО(ФИО)
	
	Результат = Новый Структура;
	Результат.Вставить("Surname"	, "");
	Результат.Вставить("Firstname"	, "");
	Результат.Вставить("Patronymic"	, "");
	
	МассивФИО = РазложитьСтрокуВМассивСлов(ФИО, " ");
	Разделитель = "";
	
	Для Сч = 0 По МассивФИО.ВГраница() Цикл
		
		ЧастьФИО = МассивФИО[Сч];
		
		Если Сч = 0 Тогда
			
			Результат.Surname = ЧастьФИО;
			
		ИначеЕсли Сч = 1 Тогда
			
			Результат.Firstname = ЧастьФИО;
			
		Иначе
			
			Результат.Patronymic = Результат.Patronymic + Разделитель + ЧастьФИО;
			Разделитель = " ";
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция XML_СтрокаФИО(ОбъектXDTO)
	
	Результат = ОбъектXDTO.Фамилия + " " + ОбъектXDTO.Имя;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "Отчество") Тогда
		Результат = Результат + " " + ОбъектXDTO.Отчество;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции	

Функция ЭтоИстина(Значение)
	
	Если ТипЗнч(Значение) = Тип("Строка") Тогда
		Результат = (НРег(Значение) = "true");
	Иначе
		Результат = (Значение = Истина);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция СтрокаДефис()
	Возврат "-";
КонецФункции

Функция СтрокаБезНДС()
	Возврат "без НДС";
КонецФункции

Функция Дата1С_в_ДатаXML(ДатаСтрокой)
	
	ФорматДаты = "ДФ=dd.MM.yyyy";
	
	Результат = Дата_В_Формат(
		ДатаСтрокой,
		ФорматДаты
	);
	
	Возврат Результат;
	
КонецФункции

Функция Время1С_в_ВремяXML(ВремяСтрокой)
	
	ФорматВремени = "ДФ=HH:mm:ss";
	
	Результат = Дата_В_Формат(
		ВремяСтрокой,
		ФорматВремени
	);
	
	Возврат Результат;
	
КонецФункции

//} МЕТОДЫ ГЕНЕРАЦИИ XML-файлов ИЗ Proto


//**********************************************
//{		КЭШИРОВАНИЕ

Функция Кэш_Прочитать(ИмяЗначения)
	
	Если Кэш = Неопределено Тогда
		Кэш = Новый Соответствие;
	КонецЕсли;
	
	Возврат Кэш[ИмяЗначения];
	
КонецФункции

Процедура Кэш_Поместить(ИмяЗначения, Значение)
	
	Если Кэш = Неопределено Тогда
		Кэш = Новый Соответствие;
	КонецЕсли;
	
	Кэш.Вставить(ИмяЗначения, Значение);
	
КонецПроцедуры

//}		КЭШИРОВАНИЕ

//{ Общие методы

Процедура ДобавитьКолонкиВТаблицуЗначений(Таблица, Колонки, ОписаниеТипов = Неопределено)
	
	Если ТипЗнч(Колонки) = Тип("Массив") Тогда
		МассивИменКолонок = Колонки;
	Иначе
		МассивИменКолонок = РазложитьСтрокуВМассивСлов(Колонки, ",");
	КонецЕсли;
	
	Для Каждого ИмяКолонки Из МассивИменКолонок Цикл
		Если Таблица.Колонки.Найти(ИмяКолонки) = Неопределено Тогда
			Таблица.Колонки.Добавить(ИмяКолонки, ОписаниеТипов);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

// Сравнить две строки версий.
//
// Параметры
//  СтрокаВерсии1  - Строка - номер версии в формате РР.{П|ПП}.ЗЗ.СС
//  СтрокаВерсии2  - Строка - второй сравниваемый номер версии
//
// Возвращаемое значение:
//   Число   - больше 0, если СтрокаВерсии1 > СтрокаВерсии2; 0, если версии равны.
//
Функция СравнитьВерсии(Знач СтрокаВерсии1, Знач СтрокаВерсии2)
	
	Строка1 = ?(ПустаяСтрока(СтрокаВерсии1), "0.0.0.0", СтрокаВерсии1);
	Строка2 = ?(ПустаяСтрока(СтрокаВерсии2), "0.0.0.0", СтрокаВерсии2);
	
	// Для составных номеров версий берем только первую часть.
	Строка1 = РазложитьСтрокуВМассивСлов(Строка1, "/")[0];
	Строка2 = РазложитьСтрокуВМассивСлов(Строка2, "/")[0];
	
	Версия1 = РазложитьСтрокуВМассивСлов(Строка1, ".");
	Если Версия1.Количество() > 4 Тогда
		ВызватьИсключение СтрЗаменить(НСтр("ru = 'Неправильный формат параметра СтрокаВерсии1: %1'"), "%1", СтрокаВерсии1);
	Иначе
		Для Индекс = Версия1.Количество() По 4 Цикл
			Версия1.Добавить("0");
		КонецЦикла;
	КонецЕсли;
	Версия2 = РазложитьСтрокуВМассивСлов(Строка2, ".");
	Если Версия2.Количество() > 4 Тогда
		ВызватьИсключение СтрЗаменить(НСтр("ru = 'Неправильный формат параметра СтрокаВерсии2: %1'"), "%1", СтрокаВерсии2);
	Иначе
		Для Индекс = Версия2.Количество() По 4 Цикл
			Версия2.Добавить("0");
		КонецЦикла;
	КонецЕсли;
	
	Результат = 0;
	Для Разряд = 0 По 3 Цикл
		Результат = Число(Версия1[Разряд]) - Число(Версия2[Разряд]);
		Если Результат <> 0 Тогда
			Возврат Результат;
		КонецЕсли;
	КонецЦикла;
	Возврат Результат;
	
КонецФункции

// Сравнивает текущую версию приложения с указанной версией.
// 
// Параметры:
//  Версия - Строка - номер версии, с которой нужно сравнить текущую версию приложения.
//
// Возвращаемое значение:
//   Булево - Истина, если текущая версия приложения старше чем версия параметра.
//            Например, 8.2 старше чем 8.3.
//
Функция ПриложениеСтаршеВерсии(Версия) Экспорт
	
	КэшРезультатов = Кэш_Прочитать("КэшРезультатов_ПриложениеСтаршеВерсии");
	
	Если КэшРезультатов = Неопределено Тогда
		КэшРезультатов = Новый Соответствие;
		Кэш_Поместить("КэшРезультатов_ПриложениеСтаршеВерсии", КэшРезультатов);
	КонецЕсли;
	
	Результат = КэшРезультатов[Версия];
	
	Если Результат = Неопределено Тогда
		
		СисИнфо = Новый СистемнаяИнформация;
		ВерсияПриложения = СисИнфо.ВерсияПриложения;
		
		Результат = СравнитьВерсии(ВерсияПриложения, Версия) < 0;
		
		КэшРезультатов.Вставить(Версия, Результат);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Проверяет, можно ли для вычисления хеш-суммы использовать объект ХешированиеДанных.
// 
// Возвращаемое значение:
//  Булево - Истина, если версия платформы 8.3.1 или выше.
//
Функция ИспользоватьХешированиеДанных()
	
	Результат = Истина;
	
	МинимальнаяВерсия = "8.3.1";
	
	Если ПриложениеСтаршеВерсии(МинимальнаяВерсия) Тогда
		Результат = Ложь;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЗначениеВМассив(Значение)
	
	Если ТипЗнч(Значение) = Тип("Массив") Тогда
		
		Результат = Значение;
		
	Иначе
		
		Результат = Новый Массив;
		Результат.Добавить(Значение);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция Base64СтрокаИзДвоичныхДанных(ДвоичныеДанные)
	
	Результат = Base64Строка(ДвоичныеДанные);
	Результат = СтрЗаменить(Результат, Символы.ВК, "");
	Результат = СтрЗаменить(Результат, Символы.ПС, "");
	
	Возврат Результат;
	
КонецФункции

// Подставляет параметры в строку, максимальное число параметров - 5.
//
// Параметры:
//  ШаблонСтроки - Строка - шаблон строки с параметрами;
//  Параметр<n>	 - Строка - подставляемый параметр.
//
// Пример:
//  Общее_ПодставитьПараметрыВСтроку(НСтр("ru='%1 пошел в %2'"), "Вася", "Зоопарк") = "Вася пошел в Зоопарк".
// 
// Возвращаемое значение:
//  Строка - строка с подставленными параметрами.
//
Функция Общее_ПодставитьПараметрыВСтроку(ШаблонСтроки
	, Параметр1
	, Параметр2 = Неопределено
	, Параметр3 = Неопределено
	, Параметр4 = Неопределено
	, Параметр5 = Неопределено
	)
	
	Результат = СтрЗаменить(ШаблонСтроки, "%1", Параметр1);
	Результат = СтрЗаменить(Результат	, "%2", Параметр2);
	Результат = СтрЗаменить(Результат	, "%3", Параметр3);
	Результат = СтрЗаменить(Результат	, "%4", Параметр4);
	Результат = СтрЗаменить(Результат	, "%5", Параметр5);
	
	Возврат Результат;
	
КонецФункции

// Соединяет массив переданных строк в одну строку с указанным разделителем.
// По аналогии с глобальным методом СтрСоединить, добавленным в платформу версии 8.3.6.
// 
// Параметры:
//  Строки - Массив из Строка - коллекция строк, которые нужно соединить.
//  Разделитель - Строка - (Необязательный) Строка, которая будет вставлена между объединяемыми строками.
//                         Если параметр не задан, строки будут объединены слитно друг с другом.
//
// Возвращаемое значение:
//   Строка - строка, содержащая соединенные исходные строки с разделителем между ними.
//
Функция Общее_СоединитьСтроку(Строки, Разделитель = "")
	
	Результат		 = "";
	ТекРазделитель	 = "";
	
	Если ЗначениеЗаполнено(Строки) Тогда
		
		Для Каждого Строка Из Строки Цикл
			
			Если ЗначениеЗаполнено(Строка) Тогда
				Результат		 = Результат + ТекРазделитель + Строка;
				ТекРазделитель	 = Разделитель;
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Проверяет, заканчивается ли строка указанной подстрокой.
//
// Параметры:
//  Строка - Строка - Проверяемая строка
//  СтрокаПоиска - Строка - Искомое окончание строки
// 
// Возвращаемое значение:
//  Булево - Истина, если строка зкаканчивается указанной подстрокой
//
Функция СтрЗаканчиваетсяНаЛок(Знач Строка, Знач СтрокаПоиска)
	
	Результат = (Прав(Строка, СтрДлина(СтрокаПоиска)) = СтрокаПоиска);
	Возврат Результат;
	
КонецФункции

// Извлекает текст из файла с помощью объекта ЧтениеТекста
//
// Параметры:
//  ИмяФайла - Строка - полное имя файла
//  Кодировка - КодировкаТекста, Строка - кодировка текста читаемого текстового файла
// 
// Возвращаемое значение:
//  Строка - содержимое текстового файла.
//
Функция СтрокаИзФайла(ИмяФайла, Кодировка)
	
	ЧтениеТекста = Новый ЧтениеТекста(ИмяФайла, Кодировка);
	Результат = ЧтениеТекста.Прочитать();
	ЧтениеТекста.Закрыть();
	
	Возврат Результат;
	
КонецФункции

// Извлекает текст из XML-файла в кодировке, указанной в объявлении XML.
//
// Параметры:
//  ИмяФайлаXML - Строка - полное имя XML-файла
// 
// Возвращаемое значение:
//  Строка - содержимое XML-файла.
//  Неопределено - если файл не существует.
//
Функция СодержимоеФайлаXML(ИмяФайлаXML)
	
	ФайлXML = Новый Файл(ИмяФайлаXML);
	Если Не ФайлXML.Существует() Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	КодировкаПоУмолчанию = КодировкаПоУмолчанию();
	Результат = СтрокаИзФайла(ИмяФайлаXML, КодировкаПоУмолчанию);
	
	КодировкаФайла = КодировкаXML(Результат);
	Если КодировкаФайла <> КодировкаПоУмолчанию Тогда
		Результат = СтрокаИзФайла(ИмяФайлаXML, КодировкаФайла);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Извлекает текст из двоичных данных
//
// Параметры:
//  ДвоичныеДанные - ДвоичныеДанные - Двоичные данные, которые требуется преобразовать в строку
//  Кодировка - КодировкаТекста, Строка - кодировка, в которой требуется получить строку
// 
// Возвращаемое значение:
//  Строка - содержимое текста, переданного в двоичных данных.
//
Функция СтрокаИзДвоичныхДанных(ДвоичныеДанные, Кодировка)
	
	Если Не ПриложениеСтаршеВерсии("8.3.10") Тогда
		
		Результат = Вычислить("ПолучитьСтрокуИзДвоичныхДанных(ДвоичныеДанные, Кодировка)");
		
	Иначе
		
		ИмяФайла = ПолучитьИмяВременногоФайла("xml");
		ДвоичныеДанные.Записать(ИмяФайла);
		
		Результат = СтрокаИзФайла(ИмяФайла, Кодировка);
		
		Попытка
			
			УдалитьФайлы(ИмяФайла);
			
		Исключение
			
			Ошибка = ИнформацияОбОшибке();
			
			ЖурналРегистрации_ЗаписатьПредупреждение(
				"ЧтениеФайла",
				ПодробноеПредставлениеОшибки(Ошибка)
			);
			
		КонецПопытки;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Извлекает текст двоичных данных XML-файла в кодировке, указанной в объявлении XML.
//
// Параметры:
//  ДвоичныеДанныеXML - ДвоичныеДанные - Двоичные данные XML-файла, которые требуется преобразовать в строку
// 
// Возвращаемое значение:
//  Строка - текст из двоичных данных XML-файла.
//
Функция СодержимоеДвоичныхДанныхXML(ДвоичныеДанныеXML)
	
	КодировкаПоУмолчанию = КодировкаПоУмолчанию();
	
	Результат = СтрокаИзДвоичныхДанных(ДвоичныеДанныеXML, КодировкаПоУмолчанию);
	
	КодировкаФайла = КодировкаXML(Результат);
	
	Если КодировкаФайла <> КодировкаПоУмолчанию Тогда
		Результат = СтрокаИзДвоичныхДанных(ДвоичныеДанныеXML, КодировкаФайла);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает кодировку файла по объявлению xml: <?xml version="1.0" encoding="windows-1251"?>.
//
// Параметры:
//  СтрокаXML - Строка - строка с текстом XML-документа
// 
// Возвращаемое значение:
//  Строка - кодировка из объявления XML.
//
Функция КодировкаXML(СтрокаXML) Экспорт
	
	Результат = "";
	
	Тег = "encoding";
	ПозНачало = Найти(СтрокаXML, Тег);
	ПозНачало = ?(ПозНачало > 0, ПозНачало + СтрДлина(Тег), 0);
	
	Если ПозНачало > 0 Тогда
		
		Тег = "=";
		ПозДоп = Найти(Сред(СтрокаXML, ПозНачало), Тег);
		ПозНачало = ПозНачало + ПозДоп;
		
		Тег = Сред(СтрокаXML, ПозНачало, 1);
		ПозКонец = Найти(Сред(СтрокаXML, ПозНачало + 1), Тег);
		ПозКонец = ?(ПозКонец > 0, ПозКонец + 1, 0);
		
		Если ПозКонец > 0 Тогда
			
			Результат = Сред(СтрокаXML, ПозНачало, ПозКонец);
			Результат = НРег(Результат);
			Результат = СокрЛП(Результат);
			Результат = СтрЗаменить(Результат, Тег, "");
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоСтрокаXML(Строка)
	
	Результат = Ложь;
	
	Если ТипЗнч(Строка) = Тип("Строка") Тогда
		Результат = (Лев(Строка, 1) = "<");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция КодировкаПоУмолчанию()
	Возврат "windows-1251";
КонецФункции

Процедура ЖурналРегистрации_ЗаписатьПредупреждение(Знач ИмяСобытия, Знач Комментарий)
	
	ИдентификаторСобытия = ЖурналРегистрации_ИдентификаторСобытия(ИмяСобытия);
	
	ЗаписьЖурналаРегистрации(
		ИдентификаторСобытия,
		УровеньЖурналаРегистрации.Предупреждение,
		Неопределено,
		Неопределено,
		Комментарий
	);
	
КонецПроцедуры

Функция ЖурналРегистрации_ИдентификаторСобытия(ИмяСобытия)
	
	Подстроки = Новый Массив;
	Подстроки.Добавить(ЖурналРегистрации_ИдентификаторПродукта());
	Подстроки.Добавить(ЖурналРегистрации_ИмяМодуля());
	Подстроки.Добавить(ИмяСобытия);
	
	Разделитель = ".";
	
	Результат = Общее_СоединитьСтроку(Подстроки, Разделитель);
	
	Возврат Результат;
	
КонецФункции

Функция ЖурналРегистрации_ИдентификаторПродукта()
	Возврат "КонтурДиадок";
КонецФункции

Функция ЖурналРегистрации_ИмяМодуля()
	Возврат "МенеджерИнтеграции";
КонецФункции

Функция ПолучитьТекущуюДатуСеанса()
	
	Результат = ТекущаяДатаСеанса();
	
	Возврат Результат;
	
КонецФункции

Функция ТаблицаЗначений_2_МассивСтруктур(ТаблЗнач)
	
	Результат = Новый Массив;
	СтрокаКлючей = "";
	
	Для Каждого Колонка Из ТаблЗнач.Колонки Цикл
		СтрокаКлючей = СтрокаКлючей + ?(ПустаяСтрока(СтрокаКлючей), "", ",") + Колонка.Имя;
	КонецЦикла;
	
	Если ТипЗнч(ТаблЗнач) = Тип("ТаблицаЗначений") Тогда
		
		Для Каждого Стр Из ТаблЗнач Цикл
			
			ЭлементСписка = Новый Структура(СтрокаКлючей);
			ЗаполнитьЗначенияСвойств(ЭлементСписка, Стр);
			
			Для Каждого ЭлементСтруктуры Из ЭлементСписка Цикл
				Если ТипЗнч(ЭлементСтруктуры.Значение) = Тип("ТаблицаЗначений") Тогда
					ЭлементСписка.Вставить(ЭлементСтруктуры.Ключ, ТаблицаЗначений_2_МассивСтруктур(ЭлементСтруктуры.Значение));
				КонецЕсли;
			КонецЦикла;
			
			Результат.Добавить(ЭлементСписка);
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция МассивСтруктур_НайтиСтроки(МассивСтруктур, ПараметрыОтбора)
	
	Результат = Новый Массив;
	
	Если ЗначениеЗаполнено(МассивСтруктур) Тогда
		
		Для Каждого Структура Из МассивСтруктур Цикл
			
			СтруктураСоответствуетОтбору = Истина;
			
			Для Каждого ЭлементОтбора Из ПараметрыОтбора Цикл
				
				Если Структура[ЭлементОтбора.Ключ] <> ЭлементОтбора.Значение Тогда
					СтруктураСоответствуетОтбору = Ложь;
					Прервать;
				КонецЕсли;
				
			КонецЦикла;
			
			Если СтруктураСоответствуетОтбору Тогда
				Результат.Добавить(Структура);
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоИмяСвойстваСсылки(Имя)
	
	ИменаСсылок = Новый Массив;
	ИменаСсылок.Добавить("ВалютаСсылка");
	ИменаСсылок.Добавить("СтранаПроисхожденияСсылка");
	ИменаСсылок.Добавить("ЕдиницаИзмеренияСсылка");
	ИменаСсылок.Добавить("Ссылка");
	ИменаСсылок.Добавить("СсылкаНаЕИ");
	
	Результат = ИменаСсылок.Найти(Имя) <> Неопределено;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоОбъектXDTOСсылки(ОбъектXDTO, Ключ)
	
	Результат = Ложь;
	
	Если ТипЗнч(ОбъектXDTO) = Тип("ОбъектXDTO") Тогда
		
		СвойствоXDTO = ОбъектXDTO.Свойства().Получить(Ключ);
		Если СвойствоXDTO <> Неопределено Тогда
			
			Результат = ЭтоТипОбъектаXDTOСсылка(СвойствоXDTO.Тип);
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ЭтоТипОбъектаXDTOСсылка(Тип)
	
	Результат = Тип.Имя = "Ссылка1С";
	
	Возврат Результат;
	
КонецФункции

Функция Перечисление_ВидыЮрФизЛиц() Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("ЮридическоеЛицо", "LegalEntity");
	Результат.Вставить("ИндивидуальныйПредприниматель", "IndividualEntity");
	Результат.Вставить("ФизическоеЛицо", "PhysicalEntity");
	Результат.Вставить("ОрганИсполнительнойВласти", "AuthorityEntitry");
	Результат.Вставить("ИностраннаяОрганизация", "ForeignEntity");
	Результат.Вставить("ИностранныйГражданин", "ForeignPhysicalEntity");
	
	Возврат Результат;
	
КонецФункции

Функция Перечисление_ВидыАдресов() Экспорт
	
	Результат = Новый Структура;
	Результат.Вставить("АдресРФ", "AddressRF");
	Результат.Вставить("АдресГАР", "AddressSAR");
	Результат.Вставить("АдресСтрокой", "AddressText");
	
	Возврат Результат;
	
КонецФункции

Функция Перечисление_ТипыПодписей()
	
	Результат = Новый Структура;
	Результат.Вставить("Квалифицированная", "Qualified");
	Результат.Вставить("Неквалифицированная", "Unqualified");
	Результат.Вставить("Простая", "Simple");
	
	Возврат Результат;
	
КонецФункции

// Возвращает представление даты в указанном формате, дату можно передавать строкой.
//
// Важно! Для пустой даты метод Формат с пустой форматной строкой работает иначе чем преобразование к строке:
// 	Формат('00010101') = ""
// 	Строка('00010101') = "01.01.0001 0:00:00"
//
Функция Дата_В_Формат(ДатаСтрока, ФорматнаяСтрока = "", ПустоеЗначение = Неопределено)
	
	Если ПустаяСтрока(ДатаСтрока) Тогда
		Возврат ПустоеЗначение;
	КонецЕсли;
	
	Попытка
		Значение = Дата(ДатаСтрока);
	Исключение
		Значение = ДатаСтрока;
	КонецПопытки;
	
	Если НЕ ЗначениеЗаполнено(Значение) И ПустоеЗначение <> Неопределено Тогда
		Возврат ПустоеЗначение;
	КонецЕсли;
	
	Возврат Формат(Значение, ФорматнаяСтрока);
	
КонецФункции

Функция СтруктураЗаполнена(Структура)
	
	Результат = Ложь;
	
	Для Каждого КлючИЗначение Из Структура Цикл
		
		ТипЗначения = ТипЗнч(КлючИЗначение.Значение);
		
		Если ТипЗначения = Тип("Структура") Тогда
			Результат = СтруктураЗаполнена(КлючИЗначение.Значение);
		ИначеЕсли ТипЗначения = Тип("Булево") Тогда
			Результат = КлючИЗначение.Значение = Истина;
		Иначе
			Результат = ЗначениеЗаполнено(КлючИЗначение.Значение);
		КонецЕсли;
		
		Если Результат = Истина Тогда
			Прервать;
		КонецЕсли;
		
	КонецЦикла;	
	
	Возврат Результат;
	
КонецФункции

Функция ОбъектXDTOЗаполнен(ОбъектXDTO)
	
	Результат = Ложь;
	
	СвойстваОбъекта = ОбъектXDTO.Свойства();
	Для Каждого Свойство Из СвойстваОбъекта Цикл
		
		Если ТипЗнч(ОбъектXDTO[Свойство.Имя]) = Тип("ОбъектXDTO") Тогда
			
			Если ОбъектXDTOЗаполнен(ОбъектXDTO[Свойство.Имя]) Тогда
				Результат = Истина;
				Прервать;
			КонецЕсли;
			
		ИначеЕсли ОбъектXDTO.Установлено(Свойство.Имя) Тогда
			Результат = Истина;
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ПространствоИменПрото()
	Возврат "http://diadoc.kontur.ru/dll_types";
КонецФункции

// Преобразовывает произвольное значение в число.
//
// Параметры:
//  Значение - Произвольный - произвольное значение.
// 
// Возвращаемое значение:
//  Число - результат преобразования.
//
// Пример:
//   Цена = Привести_К_Числу("1 000,50"); // результат 1000.5
//
Функция Привести_К_Числу(Знач Значение) Экспорт
	
	Если ТипЗнч(Значение) = Тип("Число") Тогда // нечего преобразовывать
		Возврат Значение;
	КонецЕсли;
	
	Если ТипЗнч(Значение) = Тип("Строка") Тогда // удалим пробелы
		Значение = СтрЗаменить(Значение, " ", "");
	КонецЕсли;
	
	ОписаниеТипов = Новый ОписаниеТипов("Число");
	Результат = ОписаниеТипов.ПривестиЗначение(Значение);
	
	Возврат Результат;
	
КонецФункции

Функция ТипПрото_ИнформацияПродавца155()
	Возврат "UtdSellerContent";
КонецФункции

Функция ТипПрото_ИнформацияПокупателя155()
	Возврат "UtdBuyerContent";
КонецФункции

Функция ТипПрото_ИнформацияПродавца736()
	Возврат "UcdSellerContent";
КонецФункции

Функция ТипПрото_ИнформацияПокупателя736()
	Возврат "UcdBuyerContent";
КонецФункции

Функция ТипПрото_ИнформацияПродавца970()
	Возврат "Utd970SellerContent";
КонецФункции

Функция ТипПрото_ИнформацияПродавца820()
	Возврат "Utd820SellerContent";
КонецФункции

Функция ТипПрото_ИнформацияПокупателя820()
	Возврат "Utd820BuyerContent";
КонецФункции

Функция ТипПрото_ИнформацияПродавца552()
	Возврат "Act552SellerContent";
КонецФункции

Функция ТипПрото_ИнформацияПокупателя552()
	Возврат "Act552BuyerContent";
КонецФункции

Функция ТипПрото_ИнформацияПродавца551()
	Возврат "TovTorgSellerContent";
КонецФункции

Функция ТипПрото_ИнформацияПокупателя551()
	Возврат "TovTorgBuyerContent";
КонецФункции

Функция ТипПрото_ИнформацияПродавцаНакладная172()
	Возврат "Torg12SellerContent";
КонецФункции

Функция ТипПрото_ИнформацияПокупателяНакладная172()
	Возврат "Torg12BuyerContent";
КонецФункции

Функция ТипПрото_ИнформацияПродавцаАкт172()
	Возврат "AcceptanceCertificateSellerContent";
КонецФункции

Функция ТипПрото_ИнформацияПокупателяАкт172()
	Возврат "AcceptanceCertificateBuyerContent";
КонецФункции

Функция ТипПрото_СчетФактура93()
	Возврат "InvoiceContent";
КонецФункции

Функция ТипПрото_КорректировочныйСчетФактура93()
	Возврат "InvoiceCorrectionContent";
КонецФункции

Функция ТипПрото_ИнформацияПродавцаСчетНаОплату()
	Возврат "ProformaInvoiceSellerContent";
КонецФункции

Функция ТипПрото_ИнформацияОтправителя423()
	Возврат "Torg2SenderTitle";
КонецФункции

Функция ТипПрото_ИнформацияПолучателя423()
	Возврат "Torg2RecipientTitle";
КонецФункции

Функция ТипПрото_ИнформацияОтправителя405()
	Возврат "ReconciliationActSenderTitle";
КонецФункции

Функция ТипПрото_ИнформацияПолучателя405()
	Возврат "ReconciliationActRecipientTitle";
КонецФункции

Функция ТипНоменклатурыService()
	Возврат "Service";	
КонецФункции

Функция ТипНоменклатурыProperty()
	Возврат "Property";	
КонецФункции

//} Общие методы

//{ Алгоритмы валидации контента исходящих документов

// РАБОТА С КОЛЛЕКЦИЯМИ ОШИБОК - ДОБАВЛЕНИЕ, ИНТЕРПРЕТАЦИЯ

// Основной метод для добавления ошибок валидации.
Функция ДобавитьОшибкуВалидации(СписокОшибок = Неопределено, ТекстОшибки, СсылкаДляИсправления = Неопределено) Экспорт
	
	Возврат ДобавитьОшибкуВалидацииСОформлением(СписокОшибок, ТекстОшибки, СсылкаДляИсправления);
	
КонецФункции

// времянка для выделения в коде мест, где требуется оформление ошибки (<h3>, <b>)
Функция ДобавитьОшибкуВалидацииСОформлением(СписокОшибок = Неопределено, ТекстОшибки, СсылкаДляИсправления = Неопределено, Обрамление = "")
	
	Если СписокОшибок = Неопределено Тогда
		СписокОшибок = Новый Массив;
	КонецЕсли;
	
    Если ПустаяСтрока(ТекстОшибки) Тогда
        Возврат СписокОшибок;
	КонецЕсли;
	
	НоваяОшибка = НовыйОшибкаВалидации(ТекстОшибки, Обрамление, СсылкаДляИсправления);
	СписокОшибок.Добавить(НоваяОшибка);
	
	Возврат СписокОшибок;
	
КонецФункции

// Добавляет группу ошибок с общим заголовком
Функция ДобавитьГруппуОшибокВалидации(СписокОшибок, СписокДобавляемыхОшибок, ШаблонЗаголовка, СсылкаДляИсправления = Неопределено) Экспорт
	
	Если Не ЗначениеЗаполнено(СписокДобавляемыхОшибок) Тогда
		Возврат СписокОшибок;
	КонецЕсли;
	
	Обрамление = "h3";  // выделим заголовок группы
	ДобавитьОшибкуВалидацииСОформлением(СписокОшибок, ШаблонЗаголовка, СсылкаДляИсправления, Обрамление);
	ДополнитьМассивОшибокВалидации(СписокОшибок, СписокДобавляемыхОшибок);
	
	Возврат СписокОшибок;
	
КонецФункции

Процедура ДополнитьМассивОшибокВалидации(ДополняемыйМассив, ДобавляемыеОшибки) Экспорт
	
	Для каждого Ошибка Из ДобавляемыеОшибки Цикл
		ДополняемыйМассив.Добавить(Ошибка);
	КонецЦикла;
		
КонецПроцедуры

Функция ОформитьОшибкиВHTML(СписокОшибок, ЗаголовокОшибки = "") Экспорт
	
	Результат = "";
	
	// заглушка для возможности переключения между структурированным и неструктурированным поведением
	Если ТипЗнч(СписокОшибок) = Тип("Строка") Тогда
		
		Ошибка = СписокОшибок;
		
	Иначе
	
		Ошибка = ПеревестиСтруктурированныеОшибкиВТекст(СписокОшибок);
		
	КонецЕсли;

	Если ЗначениеЗаполнено(Ошибка) Тогда

		ТелоHTML = ?(ЗначениеЗаполнено(ЗаголовокОшибки),
					"<h2>" + ЗаголовокОшибки + "</h2>" + Ошибка,
					Ошибка);
					
		Результат = 
		"<HTML><head>
		|<META content=""text/html; charset=utf-8"" http-equiv=Content-Type>
		|<style type=""text/css"">
		| h3 {
		|  margin-top: 0.5em; /* Отступ сверху */
		|  margin-bottom: 0em; /* Отступ снизу */
		| }
		| p {
		|  margin-top: 0.2em; /* Отступ сверху */
		|  margin-bottom: 0em; /* Отступ снизу */
		| }
		|</style>
		|</head>
		|<body>" + ТелоHTML + "</body></HTML>";
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Конструктор для ошибки валидации
//
// Параметры:
//  ТекстОшибки			 - Строка	 - текст ошибки
//  Обрамление			 - Строка	 - оформление ошибки в HTML, обрамляющие теги. Например, "b" или "i,h3"
//  СсылкаДляИсправления - СправочникСсылка
//						 - ДокументСсылка
//						 - Строка	 - Ссылка на элемент данных или закодированная строка, указывающая на место возникновения ошибки
// 
// Возвращаемое значение:
//   - Структура
//
Функция НовыйОшибкаВалидации(ТекстОшибки, Обрамление = "", СсылкаДляИсправления = Неопределено)
	
	Результат = Новый Структура;
	
	Результат.Вставить("ТекстОшибки",			ТекстОшибки);
	Результат.Вставить("Обрамление",			Обрамление);
	Результат.Вставить("СсылкаДляИсправления",	СсылкаДляИсправления);
	
	Возврат Результат;

КонецФункции	

Функция ПеревестиСтруктурированныеОшибкиВТекст(СписокОшибок)
	
	Результат = "";
	
    Для каждого СтрокаОшибки Из СписокОшибок Цикл
	
		МассивОбрамляющихТегов = РазложитьСтрокуВМассивСлов(СтрокаОшибки.Обрамление, ",");
		
		Если МассивОбрамляющихТегов.Найти("p") = Неопределено Тогда
			МассивОбрамляющихТегов.Вставить(0, "p");  // каждый тег обязательно будет отдельным параграфом
		КонецЕсли;	
		
		Если ЗначениеЗаполнено(СтрокаОшибки.СсылкаДляИсправления) Тогда
			ПредставлениеОшибки = ПодставитьГиперссылкуВОшибкуВалидации(СтрокаОшибки.ТекстОшибки, СтрокаОшибки.СсылкаДляИсправления);
		Иначе
			ПредставлениеОшибки = СтрокаОшибки.ТекстОшибки;
		КонецЕсли;	
	
		Результат = Результат 
				+ ОбрамляющиеТегиСтрокой(МассивОбрамляющихТегов, Ложь)		// <p><h3>
				+ ПредставлениеОшибки										// Ошибка в ИНН
				+ ОбрамляющиеТегиСтрокой(МассивОбрамляющихТегов, Истина);	// </h3></p>
	
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция ОбрамляющиеТегиСтрокой(МассивОбрамляющихТегов, ОбратныйПорядок)
	
	Результат = "";
	
	ВГраница = МассивОбрамляющихТегов.ВГраница();
	
	Для Сч = 0 По ВГраница Цикл
		
		Если НЕ ОбратныйПорядок Тогда
			Результат = Результат + "<" + МассивОбрамляющихТегов[ВГраница - Сч] + ">";
		Иначе
			Результат = Результат + "</" + МассивОбрамляющихТегов[Сч] + ">";  // закрывающий тег
		КонецЕсли;	
		
	КонецЦикла;	
	
	Возврат Результат;
	
КонецФункции

// Текст: Строка такого вида:
// "Ошибки в данных о <поставщике>: откройте его"
// На выходе получаем:
// "Ошибки в данных о <a href="СправочникСсылка.Контрагенты:faefaef-aegaeg-sahy43-334ff">поставщике</a>: откройте его"
// или
// "Ошибки в <a href="НастройкаПараметровПодписания">параметрах подписанта</a>"
// Ссылка - либо ссылка на элемент справочника/документа, либо Строка. 
Функция ПодставитьГиперссылкуВОшибкуВалидации(Текст, Ссылка)
	
	ПозицияОткрывающейСкобки = Найти(Текст, "<");
	ПозицияЗакрывающейСкобки = Найти(Текст, ">");
	
	Если (ПозицияОткрывающейСкобки = 0 И ПозицияЗакрывающейСкобки = 0)
		Или ПозицияОткрывающейСкобки > ПозицияЗакрывающейСкобки Тогда
		ВызватьИсключение "Некорректный параметр метода: ПодставитьГиперссылкуВОшибкуВалидации";
	КонецЕсли;
	
	Если ТипЗнч(Ссылка) = Тип("Строка") Тогда
		СодержимоеСсылки = Ссылка;
	ИначеЕсли Справочники.ТипВсеСсылки().СодержитТип(ТипЗнч(Ссылка))
		Или	Документы.ТипВсеСсылки().СодержитТип(ТипЗнч(Ссылка)) Тогда
		СодержимоеСсылки = ВРЕГ("" + Ссылка.Метаданные().ПолноеИмя() + ":" + Ссылка.УникальныйИдентификатор());
	Иначе
		ВызватьИсключение "Некорректный параметр метода: ПодставитьГиперссылкуВОшибкуВалидации";
	КонецЕсли;
	
	Результат = Лев(Текст, ПозицияОткрывающейСкобки - 1)
				+ "<a href="
				+ СодержимоеСсылки
				+ ">" + Сред(Текст, ПозицияОткрывающейСкобки + 1, ПозицияЗакрывающейСкобки - ПозицияОткрывающейСкобки - 1)
				+ "</a>"
				+ Сред(Текст, ПозицияЗакрывающейСкобки + 1);
	
	Возврат Результат;
	
КонецФункции	



// ПРОВЕРКА КОНТЕНТОВ КОНКРЕТНЫХ ТИПОВ ДОКУМЕНТОВ

Функция ВалидацияXmlTorg12Content(XmlTorg12Content) Экспорт
	
	СписокОшибок = Новый Массив;
	
	ПроверитьФорматДаты(СписокОшибок, XmlTorg12Content.Date, "Дата документа", Истина);
	
	ПроверитьФорматПоля(СписокОшибок, XmlTorg12Content.Number, "Номер документа", Ложь, 0, 256, Ложь, "м");
	
	Если СтруктураЗаполнена(XmlTorg12Content.Seller) Тогда 
		ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляТОРГ12(XmlTorg12Content.Seller, 1);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <поставщике>", XmlTorg12Content.Seller.Ссылка);
	Иначе
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют данные о поставщике");
	КонецЕсли;		
	
	Если СтруктураЗаполнена(XmlTorg12Content.Shipper) Тогда 
		ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляТОРГ12(XmlTorg12Content.Shipper, 2);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <грузоотправителе>", XmlTorg12Content.Shipper.Ссылка);
	КонецЕсли;		
	
	Если СтруктураЗаполнена(XmlTorg12Content.Buyer) Тогда 
		ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляТОРГ12(XmlTorg12Content.Buyer, 1);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <покупателе>", XmlTorg12Content.Buyer.Ссылка);
	КонецЕсли;		
	
	Если СтруктураЗаполнена(XmlTorg12Content.Consignee) Тогда 
		ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляТОРГ12(XmlTorg12Content.Consignee, 2);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <грузополучателе>", XmlTorg12Content.Consignee.Ссылка);
	КонецЕсли;		
	
	// основание 
	Если ЗначениеЗаполнено( XmlTorg12Content.GroundDate) Тогда 
		ПроверитьФорматДаты(СписокОшибок, XmlTorg12Content.GroundDate, "Дата договора");
	КонецЕсли;
	
	Если XmlTorg12Content.Items.Количество() = 0 Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "отсутствует табличная часть документа");
	Иначе 
		ц = 0;
		
		КодыЕдиницДляПроверки = Новый Соответствие;
		
		Для каждого Item Из XmlTorg12Content.Items Цикл 
			
			ОшибкиВСтроке = Новый Массив;
			ц = ц + 1;
			
			Если ЗначениеЗаполнено(Item.UnitCode) Тогда
				КодыЕдиницДляПроверки.Вставить(Item.СсылкаНаЕИ, сокрЛП(Item.UnitCode));
			КонецЕсли;
	
			Если НЕ ЗначениеЗаполнено(Item.Product) Тогда
				ДобавитьОшибкуВалидации(ОшибкиВСтроке, "не указано наименование товара");
			КонецЕсли;	  
			
			Если НЕ ЗначениеЗаполнено(Item.UnitName) Тогда
				ДобавитьОшибкуВалидации(ОшибкиВСтроке, "не указано наименование единицы измерения товара");
			КонецЕсли;	
			
			ПроверитьФорматПоля(ОшибкиВСтроке, Item.UnitCode, "Код единицы измерения", Ложь, 3, 4, Истина, "м");
			
			Если НЕ ЗначениеЗаполнено(Item.Quantity) Тогда
				ДобавитьОшибкуВалидации(ОшибкиВСтроке, "не указано количество товара");
			ИначеЕсли Item.Quantity < 0 Тогда
				ДобавитьОшибкуВалидации(ОшибкиВСтроке, "количество не может быть отрицательным");
			КонецЕсли;
			
			Если НЕ ЗначениеЗаполнено(Item.Total) Тогда
				ДобавитьОшибкуВалидации(ОшибкиВСтроке, "не указана общая сумма по строке");
			КонецЕсли;
			
			Если TaxRate_ЭтоЗначениеИсчНалАг(Item.TaxRate) Тогда
				ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Для данного формата документа недопустимо использовать ставку НДС ""НДС исчисляется налоговым агентом""");
 			ИначеЕсли Item.TaxRate = "##" Тогда 
				ДобавитьОшибкуВалидации(ОшибкиВСтроке, "указана недопустимая ставка налога");
			КонецЕсли;	  
			
			ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиВСтроке, "Ошибки в строке " + ц + " табличной части");
			
		КонецЦикла;
		
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ПроверитьЕдиницыИзмерения(КодыЕдиницДляПроверки),	"Ошибки в классификаторе единиц измерения");
	
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции	

Функция ВалидацияXmlAcceptanceCertificateContent(xmlAcceptanceCertificateContent) Экспорт
	
	СписокОшибок = Новый Массив;
	
	ПроверитьФорматДаты(СписокОшибок, xmlAcceptanceCertificateContent.Date, "Дата документа", Истина);
	ПроверитьФорматПоля(СписокОшибок, xmlAcceptanceCertificateContent.Number, "Номер документа", Ложь, 0, 256, Ложь, "м");
	ПроверитьФорматПоля(СписокОшибок, xmlAcceptanceCertificateContent.Title, "Заголовок документа", Истина, 0, 2000, Ложь, "м");
	
	Если Не СтруктураЗаполнена(xmlAcceptanceCertificateContent.Seller)  Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют данные о продавце");
	Иначе
		ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляТОРГ12(xmlAcceptanceCertificateContent.Seller, 1);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <поставщике>", xmlAcceptanceCertificateContent.Seller.Ссылка);
	КонецЕсли;
	
	// валидируем строки
	номерСтроки = 0;
	
	Если xmlAcceptanceCertificateContent.Items.Количество() = 0 Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "отсутствует табличная часть документа");
	Иначе
		
		Для Каждого Строка Из xmlAcceptanceCertificateContent.Items Цикл
			
			НомерСтроки = НомерСтроки + 1;
			СписокОшибокСтроки = Новый Массив;
			
			ПроверитьФорматПоля(СписокОшибокСтроки, Строка.UnitCode, "Код единицы измерения", Ложь, 3, 4, Истина, "м");
			
			Если НЕ ЗначениеЗаполнено(Строка.Total) Тогда
				ДобавитьОшибкуВалидации(СписокОшибокСтроки, "Не указана общая сумма по строке");
			КонецЕсли;
			
			ДобавитьГруппуОшибокВалидации(СписокОшибок, СписокОшибокСтроки, "Ошибки в строке " + НомерСтроки + " табличной части");
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияInvoiceContent(InvoiceContent) Экспорт
	
	СписокОшибок = Новый Массив;
	
	Если InvoiceContent.Signer.IsSoleProprietor = Истина Тогда
		ПроверитьСвидетельствоОРегистрацииИП(СписокОшибок, InvoiceContent.Signer.SoleProprietorRegistrationCertificate);
	КонецЕсли;
	
	Если InvoiceContent.items.Количество() = 0 Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствует табличная часть документа");
	КонецЕсли;
	
	// проверяем сведения об участниках сделки
	Если СтруктураЗаполнена(InvoiceContent.seller) Тогда
		ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляТОРГ12(InvoiceContent.Seller, 1, Истина);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <продавце>", InvoiceContent.seller.Ссылка);
	Иначе 
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют данные о продавце");
	КонецЕсли;	
	
	Если СтруктураЗаполнена(InvoiceContent.Shipper) Тогда 
		Если InvoiceContent.Shipper.SameAsSellerOrBuyer = Ложь Тогда
			ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляТОРГ12(InvoiceContent.Shipper, 2, Истина);
			ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <грузоотправителе>", InvoiceContent.Shipper.Ссылка);
		КонецЕсли;	
	КонецЕсли;	
	
	Если СтруктураЗаполнена(InvoiceContent.Buyer) Тогда
		ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляТОРГ12(InvoiceContent.Buyer, 1, Истина);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <покупателе>", InvoiceContent.Buyer.Ссылка);
	Иначе 
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют данные о покупателе");
	КонецЕсли;	
	
	Если СтруктураЗаполнена(InvoiceContent.Consignee) Тогда 
		ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляТОРГ12(InvoiceContent.Consignee, 2, Истина);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <грузополучателе>", InvoiceContent.Consignee.Ссылка);
	КонецЕсли;
	
	ПроверитьФорматПоля(СписокОшибок, InvoiceContent.Number, "Номер счета-фактуры", Истина, 0, 256, Ложь, "м");
	ПроверитьФорматДаты(СписокОшибок, InvoiceContent.date, "Дата счета-фактуры", Истина);
	
	Если ЗначениеЗаполнено(InvoiceContent.InvoiceRevisionDate) ИЛИ ЗначениеЗаполнено(InvoiceContent.InvoiceRevisionNumber) Тогда 
		ПроверитьФорматПоля(СписокОшибок, InvoiceContent.InvoiceRevisionNumber, "Номер исправления счета-фактуры", Ложь, 0, 3, Истина, "м");
		ПроверитьФорматДаты(СписокОшибок, InvoiceContent.InvoiceRevisionDate, "Дата исправления счета-фактуры");
	КонецЕсли;
	
	Если InvoiceContent.PaymentDocuments.Количество() > 10 Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Количество платежно-расчетных документов не может быть более 10");
	КонецЕсли;
		
	ПроверитьФорматПоля(СписокОшибок, InvoiceContent.Currency, "Код валюты счета-фактуры", Истина, 3, 3, Истина, "м");
	
	Для каждого стр Из InvoiceContent.PaymentDocuments Цикл 
		ПроверитьФорматПоля(СписокОшибок, стр.Number, "Номер платежно-расчетного документа", Ложь, 0, 30, Ложь, "м");
		ПроверитьФорматДаты(СписокОшибок, стр.Date, "Дата платежно-расчетного документа");
	КонецЦикла;
	
	// валидируем ТЧ
	ц = 0;
	
	Для Каждого item Из InvoiceContent.Items Цикл 
		ц = ц + 1;
		ОшибкиВСтроке = ПроверитьОшибкиВСтрокеСФ(item);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиВСтроке, "Ошибки в строке " + ц + " табличной части");
	КонецЦикла;	
	
	ДополнитьМассивОшибокВалидации(СписокОшибок, ПроверитьКлассификаторыСФ(InvoiceContent));
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияInvoiceCorrectionContent(InvoiceCorrectionContent) Экспорт
	
	СписокОшибок = Новый Массив;
	
	Если InvoiceCorrectionContent.Signer.IsSoleProprietor = Истина Тогда
		ПроверитьСвидетельствоОРегистрацииИП(СписокОшибок, InvoiceCorrectionContent.Signer.SoleProprietorRegistrationCertificate);
	КонецЕсли;
	
	Если InvoiceCorrectionContent.items.Количество() = 0 Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Нет строк, по которым происходит корректировка суммовых или количественных показателей");
	КонецЕсли;

	Если InvoiceCorrectionContent.TotalsDec.Total = 0
		И InvoiceCorrectionContent.TotalsInc.Total = 0 Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Нет увеличения или уменьшения суммы всего по документу");
	КонецЕсли;
	
	//проверяем сведения об участниках сделки
	Если СтруктураЗаполнена(InvoiceCorrectionContent.seller) Тогда
		ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляТОРГ12(InvoiceCorrectionContent.Seller, 1, Истина);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <продавце>", InvoiceCorrectionContent.seller.Ссылка);
	Иначе
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют данные о продавце");
	КонецЕсли;
	
	Если СтруктураЗаполнена(InvoiceCorrectionContent.Buyer) Тогда 
		ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляТОРГ12(InvoiceCorrectionContent.Buyer, 1, Истина);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <покупателе>", InvoiceCorrectionContent.Buyer.Ссылка);
	Иначе 
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют данные о покупателе");
	КонецЕсли;
	
	Если InvoiceCorrectionContent.OriginalInvoices.Количество() = 0 Тогда
	
		ДобавитьОшибкуВалидации(СписокОшибок, "Не указан исправляемый счет-фактура");
	
	Иначе
	
		OriginalInvoice = InvoiceCorrectionContent.OriginalInvoices[0];
		
		ПроверитьФорматПоля(СписокОшибок, OriginalInvoice.Number, "Номер счета-фактуры", Истина, 0, 256, Ложь, "м");
		ПроверитьФорматДаты(СписокОшибок, OriginalInvoice.Date, "Дата счета-фактуры", Истина);
	
		Если ЗначениеЗаполнено(OriginalInvoice.InvoiceRevisionNumber)
			ИЛИ ЗначениеЗаполнено(OriginalInvoice.InvoiceRevisionDate) Тогда 
			ПроверитьФорматПоля(СписокОшибок, OriginalInvoice.InvoiceRevisionNumber, "Номер исправления счета-фактуры", Истина, 0, 3, Истина, "м");
			ПроверитьФорматДаты(СписокОшибок, OriginalInvoice.InvoiceRevisionDate, "Дата исправления счета-фактуры", Истина);
		КонецЕсли;
	
	КонецЕсли;
	ПроверитьФорматПоля(СписокОшибок, InvoiceCorrectionContent.InvoiceCorrectionNumber, "Номер корректировочного счета-фактуры", Истина, 0, 256, Ложь, "м");
	ПроверитьФорматДаты(СписокОшибок, InvoiceCorrectionContent.InvoiceCorrectionDate, "Дата корректировочного счета-фактуры", Истина);
	
	Если ЗначениеЗаполнено(InvoiceCorrectionContent.InvoiceCorrectionRevisionNumber)
		ИЛИ ЗначениеЗаполнено(InvoiceCorrectionContent.InvoiceCorrectionRevisionDate) Тогда
		ПроверитьФорматПоля(СписокОшибок, InvoiceCorrectionContent.InvoiceCorrectionRevisionNumber, "Номер исправления корректировочного счета-фактуры", Истина, 0, 3, Ложь, "м");
		ПроверитьФорматДаты(СписокОшибок, InvoiceCorrectionContent.InvoiceCorrectionRevisionDate, "Дата исправления корректировочного счета-фактуры", Истина);
	КонецЕсли;
	
	ПроверитьФорматПоля(СписокОшибок, InvoiceCorrectionContent.Currency, "Код валюты счета-фактуры", Истина, 3, 3, Истина, "м");
	
	ц = 0;
	Для Каждого item Из InvoiceCorrectionContent.items Цикл
		ц = ц + 1;
		ОшибкиВСтроке = ПроверитьОшибкиВСтрокеКСФ(Item, Ложь);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиВСтроке, "Ошибки в строке " + ц + " табличной части");
	КонецЦикла;
	
	ДополнитьМассивОшибокВалидации(СписокОшибок, ПроверитьКлассификаторыКСФ(InvoiceCorrectionContent, InvoiceCorrectionContent.items));
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияUTD820(Content) Экспорт
	
	ФорматПриказа = "820";
	ФункцияУПД = XML_УПД_ПолучитьФункцию(Content.Function);
	
	СписокОшибок = Новый Массив;
	
	// Дата и номер документа
	ПроверитьФорматПоля(СписокОшибок, Content.DocumentNumber, "Номер счета-фактуры"	, Истина, 1, 1000, Ложь, "м");
	ПроверитьФорматДаты(СписокОшибок, Content.DocumentDate	, "Дата счета-фактуры"	, Истина);
	
	// Дата и номер исправления
	Если ЗначениеЗаполнено(Content.RevisionNumber) Тогда
		ПроверитьФорматПоля(СписокОшибок, Content.RevisionNumber, "Номер исправления счета-фактуры", Ложь, 1, 3, Истина, "м");
		ПроверитьФорматДаты(СписокОшибок, Content.RevisionDate	, "Дата исправления счета-фактуры");
	КонецЕсли;
	
	// Наименование первичного документа, определенное организацией
	Если ЗначениеЗаполнено(Content.DocumentName) Тогда
		ПроверитьФорматПоля(СписокОшибок, Content.DocumentName, "Наименование первичного документа", Ложь, 1, 255, Ложь, "м");
	КонецЕсли;
	
	ПроверитьФорматПоля(СписокОшибок, Content.DocumentCreator, "Наименование экономического субъекта - составителя файла обмена счета-фактуры (информации продавца)", Истина, 1, 1000, , "с");
	
	// Идентификатор государственного контракта
	Если ЗначениеЗаполнено(Content.GovernmentContractInfo) Тогда
		ПроверитьФорматПоля(СписокОшибок, Content.GovernmentContractInfo, "Идентификатор государственного контракта", Ложь, 20, 25, Ложь, "м");
	КонецЕсли;
	
	// Код валюты
	ПроверитьФорматПоля(СписокОшибок, Content.Currency, "Код валюты счета-фактуры", Истина, 3, 3, Истина, "м");
	
	МаксДлинаАдреса = 1000;
	
	// Продавцы
	Если Не ЗначениеЗаполнено(Content.Sellers) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют сведения о продавцах");
	КонецЕсли;
	
	Для Каждого Элемент Из Content.Sellers Цикл
		
		ИнформацияОбОрганизации = Элемент;
		ОшибкиКонтрагента = ВалидацияUTD820_УчастникТип(ИнформацияОбОрганизации, ФункцияУПД);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в сведениях о <продавце>", ИнформацияОбОрганизации.Ссылка);
		
	КонецЦикла;
	
	// Покупатели
	Если Не ЗначениеЗаполнено(Content.Buyers) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют сведения о покупателях");
	КонецЕсли;
	
	Для Каждого Элемент Из Content.Buyers Цикл
		
		ИнформацияОбОрганизации = Элемент;
		ОшибкиКонтрагента = ВалидацияUTD820_УчастникТип(ИнформацияОбОрганизации, ФункцияУПД);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в сведениях о <покупателе>", ИнформацияОбОрганизации.Ссылка);
		
	КонецЦикла;
	
	// Грузоотправители
	Для Каждого Элемент Из Content.Shippers Цикл
		
		Если Не СтруктураЗаполнена(Элемент)
			Или Элемент.SameAsSeller Тогда
			Продолжить;
		КонецЕсли;
		
		ИнформацияОбОрганизации = Элемент.OrganizationInfo; // TODO: уточнить имя свойства, возможно это OrganizationDetails
		ОшибкиКонтрагента = ВалидацияUTD820_УчастникТип(ИнформацияОбОрганизации, ФункцияУПД);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в сведениях о <грузоотправителе>", ИнформацияОбОрганизации.Ссылка);
		
	КонецЦикла;
	
	// Грузополучатели
	Для Каждого Элемент Из Content.Consignees Цикл
		
		Если Не СтруктураЗаполнена(Элемент) Тогда
			Продолжить;
		КонецЕсли;
		
		ИнформацияОбОрганизации = Элемент;
		ОшибкиКонтрагента = ВалидацияUTD820_УчастникТип(ИнформацияОбОрганизации, ФункцияУПД);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в сведениях о <грузополучателе>", ИнформацияОбОрганизации.Ссылка);
		
	КонецЦикла;
	
	// Фактор
	Если СтруктураЗаполнена(Content.FactorInfo) Тогда
		
		ИнформацияОбОрганизации = Content.FactorInfo;
		ОшибкиКонтрагента = ВалидацияUTD820_УчастникТип(ИнформацияОбОрганизации, ФункцияУПД);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в сведениях о <факторе>", ИнформацияОбОрганизации.Ссылка);
		
	КонецЕсли;
	
	// Перевозчик
	Если СтруктураЗаполнена(Content.TransferInfo.Carrier) Тогда
		
		ИнформацияОбОрганизации = Content.TransferInfo.Carrier;
		ОшибкиКонтрагента = ВалидацияUTD820_УчастникТип(ИнформацияОбОрганизации, ФункцияУПД);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в сведениях о <перевозчике>", ИнформацияОбОрганизации.Ссылка);
		
	КонецЕсли;
	
	// Платежно-расчетные документы
	Для Каждого СведенияПРД Из Content.PaymentDocuments Цикл 
		ПроверитьФорматПоля(СписокОшибок, СведенияПРД.Number, "Номер платежно-расчетного документа", Истина, 1, 30, Ложь, "м");
		ПроверитьФорматДаты(СписокОшибок, СведенияПРД.Date, "Дата платежно-расчетного документа", Истина);
	КонецЦикла;
	
	// Основания передачи
	ПроверитьОшибкиОснованияПередачи(СписокОшибок, Content, "820");
	
	// Табличная часть
	ТабличнаяЧасть = Content.Table.Items;
	
	Если Не ЗначениеЗаполнено(ТабличнаяЧасть) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствует табличная часть документа");
	КонецЕсли;
	
	НомерСтроки = 0;
	
	МассивОшибокГТД = Новый Массив;
	
	ВДокументеЕстьПрослеживаемость	 = Ложь;
	ВДокументеЕстьМаркировка		 = Ложь;
	
	Для Каждого СтрокаТЧ Из ТабличнаяЧасть Цикл
		
		НомерСтроки = НомерСтроки + 1;
		
		ОшибкиВСтроке = Новый Массив;
		
		// Товар
		Если ЗначениеЗаполнено(СтрокаТЧ.Product) Тогда
			
			ПроверитьФорматПоля(ОшибкиВСтроке, СтрокаТЧ.Product, "Наименование товара", Истина, 0, 1000, Ложь, "с");
			
		ИначеЕсли Не ЗначениеЗаполнено(СтрокаТЧ.ItemVendorCode) И Не ЗначениеЗаполнено(СтрокаТЧ.ItemCharact)
			И Не ЗначениеЗаполнено(СтрокаТЧ.ItemArticle) Тогда
			
			ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Наименование товара не заполнено");
			
		КонецЕсли;
		
		// Код ЕИ
		ПроверитьФорматПоля(ОшибкиВСтроке, СтрокаТЧ.Unit, "Код единицы измерения", Ложь, 3, 4, Истина, "м");
		
		// Ставка НДС
		ВалидацияUTD820_ПроверитьСтавкуНДС(СтрокаТЧ.TaxRate, ОшибкиВСтроке);
		
		// Прослеживаемость и маркировка
		Если НЕ ВДокументеЕстьПрослеживаемость И ЗначениеЗаполнено(СтрокаТЧ.ItemTracingInfos) Тогда
			ВДокументеЕстьПрослеживаемость = Истина;
		КонецЕсли;
		
		Если НЕ ВДокументеЕстьМаркировка И ЗначениеЗаполнено(СтрокаТЧ.ItemIdentificationNumbers) Тогда
			ВДокументеЕстьМаркировка = Истина;
		КонецЕсли;
		
		Если ЗначениеЗаполнено(СтрокаТЧ.CustomDeclarations) Тогда
			
			Для Каждого CustomsDeclaration Из СтрокаТЧ.CustomDeclarations Цикл 
				
				ИнформацияОСтроке = "Строка №" + НомерСтроки + ": ";
				
				Если ЗначениеЗаполнено(CustomsDeclaration.DeclarationNumber)
					И Не ЗначениеЗаполнено(CustomsDeclaration.CountryCode)
					И Не ЭтоИстина(CustomsDeclaration.HyphenCountry) Тогда
					
					МассивОшибокГТД.Добавить(ИнформацияОСтроке + "Для номенклатуры """ + СтрокаТЧ.Product + """ не заполнена страна происхождения товара");
					
				ИначеЕсли Не ЗначениеЗаполнено(CustomsDeclaration.DeclarationNumber)
					И ЗначениеЗаполнено(CustomsDeclaration.CountryCode)
					И Не СтранаЧленТС(CustomsDeclaration.CountryCode) 			// код страны без указания код таможенной декларации допустимо только для стран ЕАЭС
					И НЕ ЗначениеЗаполнено(СтрокаТЧ.ItemTracingInfos) Тогда 	// для прослеживаемых товаров номер ГТД не проверяется
					
					МассивОшибокГТД.Добавить(ТекстОшибкиНеЗаполненГТД(СтрокаТЧ, ИнформацияОСтроке));

				КонецЕсли;
				
				Если (СтрДлина(СокрЛП(CustomsDeclaration.DeclarationNumber)) > 29) Тогда
					
					МассивОшибокГТД.Добавить(ИнформацияОСтроке + "Длина номера ГТД превышает максимальное ограничение в 29 символов");
					
				КонецЕсли;

			КонецЦикла;
			
		КонецЕсли;
		
		// Код ТН ВЭД
		ПроверитьФорматПоля(ОшибкиВСтроке, СтрокаТЧ.ItemTypeCode, "Код ТН ВЭД", Ложь, 10, 10, Истина, "м");
		
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиВСтроке, "Ошибки в строке " + НомерСтроки + " табличной части");
		
	КонецЦикла;
	
	// Прослеживаемость и маркировка
	Если ВДокументеЕстьПрослеживаемость И ВДокументеЕстьМаркировка Тогда
		
		ТекстОшибкиПрослеживаемостьИМаркировкаВместе = 
		"В документе указаны сведения по маркировке и прослеживаемости товаров.
		|Оформите операции отдельными документами.";
		
		ОшибкиПрослеживаемостьИМаркировка = Новый Массив;
		ДобавитьОшибкуВалидации(ОшибкиПрослеживаемостьИМаркировка, ТекстОшибкиПрослеживаемостьИМаркировкаВместе);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиПрослеживаемостьИМаркировка, "Ошибки заполнения данных прослеживаемости и маркировки:");
		
	КонецЕсли;	
	
	Если МассивОшибокГТД.Количество() > 0 Тогда
		
		ОшибкиВСтроке = Новый Массив;

		ТекстИнструкции	= ПодставитьГиперссылкуВОшибкуВалидации("<Как заполнить?>", "ИНСТРУКЦИЯКАКЗАПОЛНИТЬГТД");
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, ТекстИнструкции);

		Для Каждого ОшибкаГТД Из МассивОшибокГТД Цикл
			
			Если ТипЗнч(ОшибкаГТД) = Тип("Массив") Тогда
				ДополнитьМассивОшибокВалидации(ОшибкиВСтроке, ОшибкаГТД);
			Иначе
				ДобавитьОшибкуВалидации(ОшибкиВСтроке, ОшибкаГТД);
			КонецЕсли;
			
		КонецЦикла;
		
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиВСтроке, "Ошибки в заполнении номеров ГТД");
		
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

// Проверяет сведения об организации на соответствие требованиям приказа 820@
//
// Параметры:
//  OrganizationInfo - Структура - см. тип ExtendedOrganizationInfo970 в схеме Proto.xsd
// 
// Возвращаемое значение:
//  Массив из НовыйОшибкаВалидации.
//
Функция ВалидацияUTD820_УчастникТип(OrganizationInfo, ФункцияУПД)
	
	СписокОшибок = Новый Массив;
	
	// BSLLS:MagicNumber-off
	
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Name, "Наименование контрагента", Истина, 0, 1000, Ложь);
	
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Okpo, "ОКПО контрагента", Ложь, 1, 10, Ложь);
	
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Department, "Структурное подразделение", Ложь, 1, 500, Ложь);
	
	ПроверитьФорматПоля(СписокОшибок,
		OrganizationInfo.AdditionalInfo,
		"Информация для участника документооборота",
		Ложь,
		1,
		255,
		Ложь
	);
	
	// BSLLS:MagicNumber-on
	
	ВидыЮрФизЛиц = Перечисление_ВидыЮрФизЛиц();
	
	ВидКонтрагента = OrganizationInfo.Type;
	
	Если ВидКонтрагента = ВидыЮрФизЛиц.ЮридическоеЛицо Тогда
		
		Ошибки = ВалидацияUTD820_УчастникТип_СвЮлУч(OrganizationInfo);
		ДополнитьМассивОшибокВалидации(СписокОшибок, Ошибки);
		
	ИначеЕсли ВидКонтрагента = ВидыЮрФизЛиц.ИндивидуальныйПредприниматель Тогда
		
		Ошибки = ВалидацияUTD820_УчастникТип_СвИП(OrganizationInfo);
		ДополнитьМассивОшибокВалидации(СписокОшибок, Ошибки);
		
	ИначеЕсли ВидКонтрагента = ВидыЮрФизЛиц.ФизическоеЛицо Тогда
		
		Ошибки = ВалидацияUTD820_УчастникТип_СвФЛУчастФХЖ(OrganizationInfo);
		ДополнитьМассивОшибокВалидации(СписокОшибок, Ошибки);
		
	ИначеЕсли ВидКонтрагента = ВидыЮрФизЛиц.ИностраннаяОрганизация
		Или ВидКонтрагента = ВидыЮрФизЛиц.ИностранныйГражданин Тогда
		
		Ошибки = ВалидацияUTD820_УчастникТип_СвИнНеУч(OrganizationInfo);
		ДополнитьМассивОшибокВалидации(СписокОшибок, Ошибки);
		
	ИначеЕсли ЗначениеЗаполнено(ВидКонтрагента) Тогда
		
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(
			"Неправильно указан тип контрагента (%1)",
			ВидКонтрагента
		);
		ДобавитьОшибкуВалидации(СписокОшибок, ТекстОшибки);
		
	Иначе
		
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(
			"Не указан тип контрагента",
			ВидКонтрагента
		);
		ДобавитьОшибкуВалидации(СписокОшибок, ТекстОшибки);
		
	КонецЕсли;
	
	Ошибки = ВалидацияUTD820_УчастникТип_Адрес(OrganizationInfo.Address, ФункцияУПД);
	ДополнитьМассивОшибокВалидации(СписокОшибок, Ошибки);
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияUTD820_УчастникТип_Адрес(Address, ФункцияУПД)
	
	СписокОшибок = Новый Массив;
	
	АдресОбязателен = (ФункцияУПД <> Перечисление_ФункцииУПД().ДОП);
	МаксДлинаАдреса = 1000;
	Ошибки = ПроверитьСтруктуруАдреса(Address, МаксДлинаАдреса, АдресОбязателен);
	
	Если ЗначениеЗаполнено(Ошибки) Тогда 
		ДобавитьОшибкуВалидацииСОформлением(СписокОшибок, "Ошибки в адресе:", , "b");
		ДополнитьМассивОшибокВалидации(СписокОшибок, Ошибки);
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияUTD820_УчастникТип_СвЮлУч(OrganizationInfo)
	
	СписокОшибок = Новый Массив;
	
	HyphenInn = ПолучитьЗначениеXDTO(OrganizationInfo, "HyphenInn");
	Если ЭтоИстина(HyphenInn) Тогда
		Возврат СписокОшибок;
	КонецЕсли;
	
	Ошибки = ПроверитьИННКонтрагента(OrganizationInfo.Inn, "ЮрЛицо");
	ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
	
	Если ЗначениеЗаполнено(OrganizationInfo.Kpp) Тогда
		Ошибки = ПроверитьКППКонтрагента(OrganizationInfo.Kpp);
		ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияUTD820_УчастникТип_СвИП(OrganizationInfo)
	
	СписокОшибок = Новый Массив;
	
	// BSLLS:MagicNumber-off
	
	ЧастиФИО = РазложитьСтрокуВМассивСлов(OrganizationInfo.Name, " ");
	Если ЧастиФИО.Количество() < 2 Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "не указано имя индивидуального предпринимателя");
	КонецЕсли;
	
	HyphenInn = ПолучитьЗначениеXDTO(OrganizationInfo, "HyphenInn");
	Если Не ЭтоИстина(HyphenInn) Тогда
		Ошибки = ПроверитьИННКонтрагента(OrganizationInfo.Inn, "ФизЛицо");
		ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(OrganizationInfo.Kpp) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Для физ. лиц КПП не указывается");
	КонецЕсли;
	
	ПроверитьФорматПоля(СписокОшибок,
		OrganizationInfo.IndividualEntityRegistrationCertificate,
		"Свидетельство о гос. регистрации ИП",
		Ложь,
		1,
		100,
		Ложь
	);
	
	ПроверитьФорматПоля(СписокОшибок,
		OrganizationInfo.OrganizationOrPersonInfo,
		"иные сведения",
		Ложь,
		1,
		255,
		Ложь
	);
	
	// BSLLS:MagicNumber-on
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияUTD820_УчастникТип_СвИнНеУч(OrganizationInfo)
	
	СписокОшибок = Новый Массив;
	
	Если ЗначениеЗаполнено(OrganizationInfo.Inn) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Для иностранной организации (иностранного гражданина) ИНН не указывается");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(OrganizationInfo.Kpp) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Для иностранной организации (иностранного гражданина) КПП не указывается");
	КонецЕсли;
	
	// BSLLS:MagicNumber-off
	
	ПроверитьФорматПоля(
		СписокОшибок,
		OrganizationInfo.LegalEntityId,
		"идентификатор иностранной организации",
		Ложь,
		1,
		255,
		Ложь
	);
	
	ПроверитьФорматПоля(
		СписокОшибок,
		OrganizationInfo.OrganizationOrPersonInfo,
		"иные сведения для однозначной идентификации иностранной организации",
		Ложь,
		1,
		255,
		Ложь
	);
	
	// BSLLS:MagicNumber-on
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияUTD820_УчастникТип_СвФЛУчастФХЖ(OrganizationInfo)
	
	СписокОшибок = Новый Массив;
	
	ЧастиФИО = РазложитьСтрокуВМассивСлов(OrganizationInfo.Name, " ");
	
	// BSLLS:MagicNumber-off
	
	Если ЧастиФИО.Количество() < 2 Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "не указано имя физического лица");
	КонецЕсли;
	
	HyphenInn = ПолучитьЗначениеXDTO(OrganizationInfo, "HyphenInn");
	Если Не ЭтоИстина(HyphenInn) И ЗначениеЗаполнено(OrganizationInfo.Inn) Тогда
		Ошибки = ПроверитьИННКонтрагента(OrganizationInfo.Inn, "ФизЛицо");
		ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(OrganizationInfo.Kpp) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Для физ. лиц КПП не указывается");
	КонецЕсли;
	
	ПроверитьФорматПоля(СписокОшибок,
		OrganizationInfo.OrganizationOrPersonInfo,
		"иные сведения",
		Ложь,
		1,
		255,
		Ложь
	);
	
	// BSLLS:MagicNumber-on
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияUTD970(Content) Экспорт
	
	ФункцияУПД = XML_УПД_ПолучитьФункцию(Content.Function);

	СписокОшибок = Новый Массив;
	
	// Дата и номер документа
	ПроверитьФорматПоля(СписокОшибок, Content.DocumentNumber, "Номер счета-фактуры"	, Истина, 1, 1000, Ложь, "м");
	ПроверитьФорматДаты(СписокОшибок, Content.DocumentDate	, "Дата счета-фактуры"	, Истина);
	
	// Дата и номер исправления
	Если ЗначениеЗаполнено(Content.RevisionNumber) Тогда
		ПроверитьФорматПоля(СписокОшибок, Content.RevisionNumber, "Номер исправления счета-фактуры", Ложь, 1, 3, Истина, "м");
		ПроверитьФорматДаты(СписокОшибок, Content.RevisionDate	, "Дата исправления счета-фактуры");
	КонецЕсли;
	
	// Наименование первичного документа, определенное организацией
	Если ЗначениеЗаполнено(Content.DocumentName) Тогда
		ПроверитьФорматПоля(СписокОшибок, Content.DocumentName, "Наименование первичного документа", Ложь, 1, 255, Ложь, "м");
	КонецЕсли;
	
	ПроверитьФорматПоля(СписокОшибок, Content.DocumentCreator, "Наименование экономического субъекта - составителя файла обмена счета-фактуры (информации продавца)", Ложь, 1, 1000, , "с");
	
	// Идентификатор государственного контракта
	Если ЗначениеЗаполнено(Content.GovernmentContractInfo) Тогда
		ПроверитьФорматПоля(СписокОшибок, Content.GovernmentContractInfo, "Идентификатор государственного контракта", Ложь, 20, 25, Ложь, "м");
	КонецЕсли;
	
	// Валюта документа
	ВалидацияUTD970_ПроверитьВалюту(СписокОшибок, Content.Currency);
	
	// Продавцы
	Если Не ЗначениеЗаполнено(Content.Sellers) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют сведения о продавцах");
	КонецЕсли;
	
	Для Каждого Элемент Из Content.Sellers Цикл
		
		ИнформацияОбОрганизации = Элемент;
		
		ОшибкиКонтрагента = ВалидацияUTD970_УчастникТип(ИнформацияОбОрганизации);
		
		ОшибкиАдреса = УПД970_ПроверитьСтруктуруАдреса(ИнформацияОбОрганизации.Address, Content.Function);
		УПД970_ДополнитьСписокОшибкамиПроверкиЮридическогоАдреса(ОшибкиКонтрагента, ОшибкиАдреса);
		
		ОрганизацияСсылка = ПолучитьЗначениеXDTO(ИнформацияОбОрганизации.СлужебнаяИнформация, "Ссылка");
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в сведениях о <продавце>", ОрганизацияСсылка);
		
	КонецЦикла;
	
	// Покупатели
	Если Не ЗначениеЗаполнено(Content.Buyers) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют сведения о покупателях");
	КонецЕсли;
	
	Для Каждого Элемент Из Content.Buyers Цикл
		
		ИнформацияОбОрганизации = Элемент;
		
		ОшибкиКонтрагента = ВалидацияUTD970_УчастникТип(ИнформацияОбОрганизации);
		
		ОшибкиАдреса = УПД970_ПроверитьСтруктуруАдреса(ИнформацияОбОрганизации.Address, Content.Function);
		УПД970_ДополнитьСписокОшибкамиПроверкиЮридическогоАдреса(ОшибкиКонтрагента, ОшибкиАдреса);
		
		ОрганизацияСсылка = ПолучитьЗначениеXDTO(ИнформацияОбОрганизации.СлужебнаяИнформация, "Ссылка");
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в сведениях о <покупателе>", ОрганизацияСсылка);
		
	КонецЦикла;
	
	// Грузоотправители
	Для Каждого Элемент Из Content.Shippers Цикл
		
		Если Не СтруктураЗаполнена(Элемент)
			Или Элемент.SameAsSeller = Истина Тогда
			Продолжить;
		КонецЕсли;
		
		ИнформацияОбОрганизации = Элемент.OrganizationInfo;
		
		ОшибкиКонтрагента = ВалидацияUTD970_УчастникТип(ИнформацияОбОрганизации);
		
		ОшибкиАдреса = УПД970_ПроверитьСтруктуруАдреса(ИнформацияОбОрганизации.Address, Content.Function);
		УПД970_ДополнитьСписокОшибкамиПроверкиФактическогоАдреса(ОшибкиКонтрагента, ОшибкиАдреса);
		
		ОрганизацияСсылка = ПолучитьЗначениеXDTO(ИнформацияОбОрганизации.СлужебнаяИнформация, "Ссылка");
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в сведениях о <грузоотправителе>", ОрганизацияСсылка);
		
	КонецЦикла;
	
	// Грузополучатели
	Для Каждого Элемент Из Content.Consignees Цикл
		
		Если Не СтруктураЗаполнена(Элемент) Тогда
			Продолжить;
		КонецЕсли;
		
		ИнформацияОбОрганизации = Элемент;
		
		ОшибкиКонтрагента = ВалидацияUTD970_УчастникТип(ИнформацияОбОрганизации);
		
		ОшибкиАдреса = УПД970_ПроверитьСтруктуруАдреса(ИнформацияОбОрганизации.Address, Content.Function);
		УПД970_ДополнитьСписокОшибкамиПроверкиФактическогоАдреса(ОшибкиКонтрагента, ОшибкиАдреса);
		
		ОрганизацияСсылка = ПолучитьЗначениеXDTO(ИнформацияОбОрганизации.СлужебнаяИнформация, "Ссылка");
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в сведениях о <грузополучателе>", ОрганизацияСсылка);
		
	КонецЦикла;
	
	// Фактор
	Если Content.FactorInfo <> Неопределено И СтруктураЗаполнена(Content.FactorInfo) Тогда
		
		ИнформацияОбОрганизации = Content.FactorInfo;
		
		ОшибкиКонтрагента = ВалидацияUTD970_УчастникТип(ИнформацияОбОрганизации);
		
		ОшибкиАдреса = УПД970_ПроверитьСтруктуруАдреса(ИнформацияОбОрганизации.Address, Content.Function);
		УПД970_ДополнитьСписокОшибкамиПроверкиЮридическогоАдреса(ОшибкиКонтрагента, ОшибкиАдреса);
		
		ОрганизацияСсылка = ПолучитьЗначениеXDTO(ИнформацияОбОрганизации.СлужебнаяИнформация, "Ссылка");
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в сведениях о <факторе>", ОрганизацияСсылка);
		
	КонецЕсли;
	
	// Платежно-расчетные документы
	Для Каждого СведенияПРД Из Content.PaymentDocuments Цикл 
		ПроверитьФорматПоля(СписокОшибок, СведенияПРД.Number, "Номер платежно-расчетного документа", Ложь, 1, 30, Ложь, "м");
		Если ЗначениеЗаполнено(СведенияПРД.Date) Тогда
			ПроверитьФорматДаты(СписокОшибок, СведенияПРД.Date, "Дата платежно-расчетного документа", Ложь);
		КонецЕсли;
	КонецЦикла;
	
	// Основания передачи
	ВалидацияUTD970_ПроверитьЗаполнениеОснованияПередачи(СписокОшибок, Content);
	
	// Табличная часть
	ТабличнаяЧасть = Content.Table.Items;
	
	Если Не ЗначениеЗаполнено(ТабличнаяЧасть) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствует табличная часть документа");
	КонецЕсли;
	
	НомерСтроки = 0;
	
	МассивОшибокГТД = Новый Массив;
	
	Для Каждого СтрокаТЧ Из ТабличнаяЧасть Цикл
		
		НомерСтроки = НомерСтроки + 1;
		
		ОшибкиВСтроке = ВалидацияUTD970_ПроверитьДанныеВСтрокеТаблицы(СтрокаТЧ, ФункцияУПД, Content.CircumFormatInvoice);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиВСтроке, "Ошибки в строке " + НомерСтроки + " табличной части");

		ВалидацияUTD970_ПроверитьГТДвСтрокеТаблицы(СтрокаТЧ, НомерСтроки, МассивОшибокГТД);

	КонецЦикла;
	
	Валидация_ДополнитьСписокОшибкамиГТД(СписокОшибок, МассивОшибокГТД);
	
	// Документ отгрузки
	ВалидацияUTD970_ПроверитьЗаполнениеДокументаОтгрузки(СписокОшибок, Content);
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияUTD970_ПроверитьДанныеВСтрокеТаблицы(СтрокаТЧ, ФункцияУПД, CircumFormatInvoice)
	
	ФункцииУПД = Перечисление_ФункцииУПД();
	
	ОшибкиВСтроке = Новый Массив;
	
	// Товар
	Если ЗначениеЗаполнено(СтрокаТЧ.Product) Тогда
		
		ПроверитьФорматПоля(ОшибкиВСтроке, СтрокаТЧ.Product, "Наименование товара", Истина, 0, 2000, Ложь, "с");
		
	ИначеЕсли ФункцияУПД <> ФункцииУПД.ДОП Тогда
		
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Наименование товара не заполнено");
		
	ИначеЕсли НЕ ЗначениеЗаполнено(СтрокаТЧ.ItemVendorCode)
		И НЕ ЗначениеЗаполнено(СтрокаТЧ.ItemCharact)
		И НЕ ЗначениеЗаполнено(СтрокаТЧ.ItemArticle) Тогда
		
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Должно быть заполнено одно из полей наименование товара | характеристика | артикул | код товара");
		
	КонецЕсли;
	
	// Код ЕИ
	ПроверитьФорматПоля(ОшибкиВСтроке, СтрокаТЧ.Unit, "Код единицы измерения", Ложь, 3, 4, Истина, "м");

	Если ЗначениеЗаполнено(СтрокаТЧ.Unit) Тогда
		Валидация_ПроверитьЕдиницуИзмеренияПоОКЕИ(ОшибкиВСтроке, СтрокаТЧ.Unit);
	КонецЕсли;
	
	ЭтоСФнаАванс = (ФункцияУПД = ФункцииУПД.СЧФ И CircumFormatInvoice = "2");
	
	// Количество
	Если НЕ ЗначениеЗаполнено(СтрокаТЧ.Quantity) Тогда
		
		КоличествоОбязательно = Не ЭтоСФнаАванс 
								И ФункцияУПД <> ФункцииУПД.ДОП
								И ЗначениеЗаполнено(СтрокаТЧ.Unit);

		Если КоличествоОбязательно Тогда
			ДобавитьОшибкуВалидации(
				ОшибкиВСтроке,
				"Не заполнено количество. В соответствии с форматом ФНС количество должно быть больше ноля. " + 
				"Заполните в документе 1С количество");
		КонецЕсли;
			
	КонецЕсли;
	
	// Цена
	Если НЕ ЗначениеЗаполнено(СтрокаТЧ.Price) Тогда
		
		ЦенаОбязательна = Не ЭтоСФнаАванс
						  И ФункцияУПД <> ФункцииУПД.ДОП
						  И (ЗначениеЗаполнено(СтрокаТЧ.Unit) Или ЗначениеЗаполнено(СтрокаТЧ.Quantity));
		
		Если ЦенаОбязательна Тогда
			ДобавитьОшибкуВалидации(
				ОшибкиВСтроке,
				"Не заполнена цена. В соответствии с форматом ФНС цена должна быть больше ноля. " +
				"Заполните в документе 1С цену");
		КонецЕсли;
	
	КонецЕсли;
	
	// Ставка НДС
	Валидация_ПроверитьСтавкуНДС(СтрокаТЧ.TaxRate, ОшибкиВСтроке);
	
	// Прослеживаемость и маркировка
	Если XML_УПД_970_ИспользуетсяФормат5_03() Тогда
		ВалидацияUTD970_ПрослеживаемостьИМаркировка5_03(СтрокаТЧ, ОшибкиВСтроке);
	Иначе
		ВалидацияUTD970_ПрослеживаемостьИМаркировка5_02(СтрокаТЧ, ОшибкиВСтроке);
	КонецЕсли;
	
	// Код ТН ВЭД
	ПроверитьФорматПоля(ОшибкиВСтроке, СтрокаТЧ.ItemTypeCode, "Код ТН ВЭД", Ложь, 10, 10, Истина, "м");
	
	Возврат ОшибкиВСтроке;
	
КонецФункции

Процедура ВалидацияUTD970_ПрослеживаемостьИМаркировка5_02(Знач СтрокаТЧ, ОшибкиВСтроке)
	
	МинимальнаяДлина = 27;
	МаксимальнаяДлина = 29;
	
	Если ЗначениеЗаполнено(СтрокаТЧ.ItemTracingInfos) Тогда
		Для Каждого ItemTracing Из СтрокаТЧ.ItemTracingInfos Цикл
			ПроверитьФорматПоля(
				ОшибкиВСтроке,
				ItemTracing.RegNumberUnit,
				"Регистрационный номер партии товаров",
				Истина,
				МинимальнаяДлина,
				МаксимальнаяДлина,
				Ложь,
				"м");
		КонецЦикла;
	КонецЕсли;
	
	ВалидацияUTD970_ПартияМаркируемойПродукции(СтрокаТЧ, ОшибкиВСтроке);
	
КонецПроцедуры

Процедура ВалидацияUTD970_ПрослеживаемостьИМаркировка5_03(Знач СтрокаТЧ, ОшибкиВСтроке)
	
	МинимальнаяДлина = 25;
	МаксимальнаяДлина = 29;
	
	Если ЗначениеЗаполнено(СтрокаТЧ.ItemTracingInfos) Тогда
		Для Каждого ItemTracing Из СтрокаТЧ.ItemTracingInfos Цикл
			ПроверитьФорматПоля(
				ОшибкиВСтроке,
				ItemTracing.RegNumberUnit,
				"Регистрационный номер партии товаров",
				Истина,
				МинимальнаяДлина,
				МаксимальнаяДлина,
				Ложь,
				"м");
		КонецЦикла;
	КонецЕсли;
	
	ВалидацияUTD970_ПартияМаркируемойПродукции(СтрокаТЧ, ОшибкиВСтроке);
	
КонецПроцедуры

Процедура ВалидацияUTD970_ПартияМаркируемойПродукции(СтрокаТЧ, ОшибкиВСтроке)
	
	МинимальнаяДлина = 0;
	МаксимальнаяДлина = 55;
	
	Если ЗначениеЗаполнено(СтрокаТЧ.ItemIdentificationNumbers) Тогда
		Для Каждого ItemIdentificationNumber Из СтрокаТЧ.ItemIdentificationNumbers Цикл
			ПроверитьФорматПоля(
				ОшибкиВСтроке,
				ItemIdentificationNumber.BatchCode,
				"Партия маркируемых товаров",
				Ложь,
				МинимальнаяДлина,
				МаксимальнаяДлина,
				Ложь,
				"м");
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры

Процедура ВалидацияUTD820_ПроверитьСтавкуНДС(TaxRate, ОшибкиВСтроке)
	
	// Для 820@ проверки ставки точно такие же, как в 970@
	Валидация_ПроверитьСтавкуНДС(TaxRate, ОшибкиВСтроке);
	
КонецПроцедуры

Процедура ВалидацияUTD970_ПроверитьГТДвСтрокеТаблицы(СтрокаТЧ, НомерСтроки, МассивОшибокГТД)
	
	Если ЗначениеЗаполнено(СтрокаТЧ.CustomDeclarations) Тогда
		
		Для Каждого CustomsDeclaration Из СтрокаТЧ.CustomDeclarations Цикл
			
			ИнформацияОСтроке = "Строка №" + НомерСтроки + ": ";
			
			Если НЕ ЗначениеЗаполнено(CustomsDeclaration.DeclarationNumber)
				И ЗначениеЗаполнено(CustomsDeclaration.CountryCode)
				И НЕ СтранаЧленТС(CustomsDeclaration.CountryCode) // код страны без указания код таможенной декларации допустимо только для стран ЕАЭС
				И НЕ ЗначениеЗаполнено(СтрокаТЧ.ItemTracingInfos) Тогда // для прослеживаемых товаров номер ГТД не проверяется
				
				МассивОшибокГТД.Добавить(ТекстОшибкиНеЗаполненГТД(СтрокаТЧ, ИнформацияОСтроке));
				
			КонецЕсли;
			
			Если (СтрДлина(СокрЛП(CustomsDeclaration.DeclarationNumber)) > 29) Тогда
				
				МассивОшибокГТД.Добавить(ИнформацияОСтроке + "Длина номера ГТД превышает максимальное ограничение в 29 символов");
				
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ВалидацияUTD970_ПроверитьЗаполнениеОснованияПередачи(СписокОшибок, Content)
	
	ФункцияУПД = XML_УПД_ПолучитьФункцию(Content.Function);
	ФункцииУПД = Перечисление_ФункцииУПД();
	
	ЭтоУПДСЧФ = (ФункцияУПД = ФункцииУПД.СЧФ);
	
	TransferInfo = Content.TransferInfo;
	
	Если TransferInfo.WithoutTransferBase <> Истина
		И TransferInfo.TransferBases.Количество() = 0
		И НЕ ЭтоУПДСЧФ Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Не заполнены основания передачи");
		Возврат;
	КонецЕсли;
	
	ДокументОснованиеОтсутствует = "Без документа-основания";
		
	Для Каждого TransferBase Из TransferInfo.TransferBases Цикл
		
		ОшибкиОснования = Новый Массив;
		
		Если TransferBase.Name = ДокументОснованиеОтсутствует Тогда
			Продолжить;
		КонецЕсли;
		
		Если Не ЗначениеЗаполнено(TransferBase.Date) 
			Или Не ЗначениеЗаполнено(TransferBase.Number) Тогда

			ДобавитьОшибкуВалидации(ОшибкиОснования, "Заполните дату и номер основания/договора");
			
		КонецЕсли;
		
		Если ОшибкиОснования.Количество() > 0 Тогда
			ТекстИнструкции	= ПодставитьГиперссылкуВОшибкуВалидации("<Как заполнить?>", "ИНСТРУКЦИЯКАКЗАПОЛНИТЬОСНОВАНИЕ");
			ДобавитьОшибкуВалидации(ОшибкиОснования, ТекстИнструкции);
		КонецЕсли;
		
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиОснования, "Ошибки в основании. <Заполните дату и номер>", СвойствоСлужебнойИнформации(TransferBase, "ОснованиеСсылка"));
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ВалидацияUTD970_ПроверитьЗаполнениеДокументаОтгрузки(СписокОшибок, Content)
	
	ФункцияУПД = XML_УПД_ПолучитьФункцию(Content.Function);
	ФункцииУПД = Перечисление_ФункцииУПД();
	
	ЭтоУПДСЧФ = (ФункцияУПД = ФункцииУПД.СЧФ);
	ЭтоУПДСЧФДОП = (ФункцияУПД = ФункцииУПД.СЧФДОП);
	
	ДокументыОтгрузки = Content.DocumentShipments;
	
	ОшибкиДокументаОтгрузки = Новый Массив;
	
	Если ЭтоУПДСЧФ И Content.CircumFormatInvoice <> "2" Тогда
		
		Если ДокументыОтгрузки.Количество() = 0 Тогда
			
			ДобавитьОшибкуВалидации(ОшибкиДокументаОтгрузки, "Заполните документ отгрузки. Это поле обязательно для функции СЧФ");	
			
		КонецЕсли;
		
		Для ИндексСтроки = 0 По ДокументыОтгрузки.Количество() - 1 Цикл
			
			НомерСтроки = ИндексСтроки + 1;
			
			ДокументОтгрузки = ДокументыОтгрузки[ИндексСтроки];
			
			Если Не ЗначениеЗаполнено(ДокументОтгрузки.Name) Тогда
				
				ДобавитьОшибкуВалидации(ОшибкиДокументаОтгрузки, "Не заполнены данные документа отгрузки в строке " + НомерСтроки + ". Не заполнено поле Name");
				
			КонецЕсли;
			
			Если Не ЗначениеЗаполнено(ДокументОтгрузки.Date) Тогда
				
				ДобавитьОшибкуВалидации(ОшибкиДокументаОтгрузки, "Не заполнены данные документа отгрузки в строке " + НомерСтроки + ". Не заполнено поле Date");
				
			КонецЕсли;
			
			Если Не ЗначениеЗаполнено(ДокументОтгрузки.Number) Тогда
				
				ДобавитьОшибкуВалидации(ОшибкиДокументаОтгрузки, "Не заполнены данные документа отгрузки в строке " + НомерСтроки + ". Не заполнено поле Number");
				
			КонецЕсли;
			
		КонецЦикла;
		
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиДокументаОтгрузки, "Ошибки в данных о документе отгрузки");
		
		
	ИначеЕсли ЭтоУПДСЧФДОП Тогда
		
		Если ДокументыОтгрузки.Количество() = 0 Тогда
			
			ДобавитьОшибкуВалидации(ОшибкиДокументаОтгрузки, "Заполните документ отгрузки. Это поле обязательно для функции СЧФДОП");	
			
			
		ИначеЕсли ДокументыОтгрузки.Количество() > 1 Тогда
			
			ДобавитьОшибкуВалидации(ОшибкиДокументаОтгрузки, "Для функции СЧФДОП предусмотрено заполнение только одного документа отгрузки");
			
		Иначе
			
			ДокументОтгрузки = ДокументыОтгрузки[0];
			
			Если Content.DocumentName <> ДокументОтгрузки.Name Тогда
				
				ДобавитьОшибкуВалидации(ОшибкиДокументаОтгрузки, "Реквизиты должны совпадать с реквизитами УПД. Ошибка заполнения реквизита Name");
				
			КонецЕсли;
			
			Если Content.DocumentDate <> ДокументОтгрузки.Date Тогда
				
				ДобавитьОшибкуВалидации(ОшибкиДокументаОтгрузки, "Реквизиты должны совпадать с реквизитами УПД. Ошибка заполнения реквизита Date");
				
			КонецЕсли;
			
			Если Content.DocumentNumber <> ДокументОтгрузки.Number Тогда
				
				ДобавитьОшибкуВалидации(ОшибкиДокументаОтгрузки, "Реквизиты должны совпадать с реквизитами УПД. Ошибка заполнения реквизита Number");
				
			КонецЕсли;
			
		КонецЕсли;
		
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиДокументаОтгрузки, "Ошибки в данных о документе отгрузки");
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ВалидацияUTD970_ПроверитьВалюту(СписокОшибок, ВалютаКод)
	
	ОшибкиВалютаДокумента = Новый Массив;
	
	ПроверитьФорматПоля(ОшибкиВалютаДокумента, ВалютаКод, "Код валюты счета-фактуры", Истина, 3, 3, Истина, "м");
	
	Если ОшибкиВалютаДокумента.Количество() = 0 Тогда
		ПроверитьКодВалютыПоКлассификатору(ВалютаКод, ОшибкиВалютаДокумента);
	КонецЕсли;
	
	ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиВалютаДокумента, "Ошибки в заполнении валюты");
	
КонецПроцедуры

Процедура ВалидацияUTD970_Предупреждения(Знач Content, СписокПредупреждений) Экспорт
	
	ВалидацияUTD970_ПроверитьИнфПолФХЖ1(Content, СписокПредупреждений);
	
КонецПроцедуры

Процедура ВалидацияUTD970_ПроверитьИнфПолФХЖ1(Content, СписокПредупреждений)
	
	ИнфПолФХЖ1 = Content.AdditionalInfoId.StructedAdditionalInfos;
	
	ВидыТоваровДокумента = Content.СлужебнаяИнформация.ContentsDocumentProductsType;
	
	Если ВидыТоваровДокумента.ContainsAlcoholeProducts Тогда
		
		ОшибкиАлкогольнаяПродукция = ВалидацияUTD970_ПроверитьСоставИнфПолФХЖ1АлкогольнойПродукции(ИнфПолФХЖ1);
		
		Если ОшибкиАлкогольнаяПродукция.Количество() > 0 Тогда
			
			ВалидацияUTD970_ДобавитьШапкуПредупрежденияВалидацияИнфПолФХЖ1(СписокПредупреждений);
			
			Для Каждого Элемент Из ОшибкиАлкогольнаяПродукция Цикл
				СписокПредупреждений.Добавить(Элемент);
			КонецЦикла;
			
			ВалидацияUTD970_ДобавитьПодвалПредупрежденияВалидацияИнфПолФХЖ1(СписокПредупреждений);
			
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Функция ВалидацияUTD970_ПроверитьСоставИнфПолФХЖ1АлкогольнойПродукции(ИнфПолФХЖ1)
	
	Результат = Новый Массив;
	
	Продавец_ФиасИД = УПД970_СвойствоПродавец_ФиасИД(ИнфПолФХЖ1);
	Покупатель_ФиасИД = УПД970_СвойствоПокупатель_ФиасИД(ИнфПолФХЖ1);
	НомерПеревозДок = УПД970_СвойствоНомерПеревозДок(ИнфПолФХЖ1);
	ДатаПеревозДок = УПД970_СвойствоДатаПеревозДок(ИнфПолФХЖ1);
	РегНомер = УПД970_СвойствоРегНомер(ИнфПолФХЖ1);
	НомерПеревозЭлДок = УПД970_СвойствоНомерПеревозЭлДок(ИнфПолФХЖ1);
	
	Если Продавец_ФиасИД.Количество() > 0 Тогда
		ВалидацияUTD970_ПроверитьЭлементИнфПолФХЖ1АлкогольнойПродукции(Продавец_ФиасИД, Результат);
	КонецЕсли;
	
	Если Покупатель_ФиасИД.Количество() > 0 Тогда
		ВалидацияUTD970_ПроверитьЭлементИнфПолФХЖ1АлкогольнойПродукции(Покупатель_ФиасИД, Результат);
	КонецЕсли;
	
	ЕстьДанныеЭлектронногоТранспортногоДокумента = (НомерПеревозЭлДок.Количество() > 0);
	ЕстьДанныеБумажногоТранспортногоДокумента = (НомерПеревозДок.Количество() > 0)
		И (ДатаПеревозДок.Количество() > 0)
		И (РегНомер.Количество() > 0);
	
	Если ЕстьДанныеБумажногоТранспортногоДокумента Тогда
		
		ВалидацияUTD970_ПроверитьЭлементИнфПолФХЖ1АлкогольнойПродукции(НомерПеревозДок, Результат);
		ВалидацияUTD970_ПроверитьЭлементИнфПолФХЖ1АлкогольнойПродукции(ДатаПеревозДок, Результат);
		ВалидацияUTD970_ПроверитьЭлементИнфПолФХЖ1АлкогольнойПродукции(РегНомер, Результат);
		
	ИначеЕсли ЕстьДанныеЭлектронногоТранспортногоДокумента Тогда
		
		ВалидацияUTD970_ПроверитьЭлементИнфПолФХЖ1АлкогольнойПродукции(НомерПеревозЭлДок, Результат);
		
	Иначе
		
		ТекстПредупреждения = "НомерПеревозЭлДок или НомерПеревозДок, ДатаПеревозДок, РегНомер";
		
		ВалидацияUTD970_ДобавитьПредупреждениеВалидацияИнфПолФХЖ1(Результат, ТекстПредупреждения);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура ВалидацияUTD970_ПроверитьЭлементИнфПолФХЖ1АлкогольнойПродукции(Свойство, СписокОшибок)
	
	КлючСвойства = Свойство[0].Key;
	ЗначениеСвойства = Свойство[0].Value;
	
	Если ЗначениеСвойства = "-" Тогда
		ВалидацияUTD970_ДобавитьПредупреждениеВалидацияИнфПолФХЖ1(СписокОшибок, КлючСвойства);
	КонецЕсли;
	
КонецПроцедуры

Функция УПД970_СвойствоПродавец_ФиасИД(ИнфПолФХЖ1)
	
	ПараметрыОтбора = Новый Структура;
	ПараметрыОтбора.Вставить("Key", "Продавец_ФиасИД");
	
	Результат = МассивСтруктур_НайтиСтроки(ИнфПолФХЖ1, ПараметрыОтбора);
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_СвойствоПокупатель_ФиасИД(ИнфПолФХЖ1)
	
	ПараметрыОтбора = Новый Структура;
	ПараметрыОтбора.Вставить("Key", "Покупатель_ФиасИД");
	
	Результат = МассивСтруктур_НайтиСтроки(ИнфПолФХЖ1, ПараметрыОтбора);
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_СвойствоНомерПеревозДок(ИнфПолФХЖ1)
	
	ПараметрыОтбора = Новый Структура;
	ПараметрыОтбора.Вставить("Key", "НомерПеревозДок");
	
	Результат = МассивСтруктур_НайтиСтроки(ИнфПолФХЖ1, ПараметрыОтбора);
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_СвойствоДатаПеревозДок(ИнфПолФХЖ1)
	
	ПараметрыОтбора = Новый Структура;
	ПараметрыОтбора.Вставить("Key", "ДатаПеревозДок");
	
	Результат = МассивСтруктур_НайтиСтроки(ИнфПолФХЖ1, ПараметрыОтбора);
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_СвойствоРегНомер(ИнфПолФХЖ1)
	
	ПараметрыОтбора = Новый Структура;
	ПараметрыОтбора.Вставить("Key", "РегНомер");
	
	Результат = МассивСтруктур_НайтиСтроки(ИнфПолФХЖ1, ПараметрыОтбора);
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_СвойствоНомерПеревозЭлДок(ИнфПолФХЖ1)
	
	ПараметрыОтбора = Новый Структура;
	ПараметрыОтбора.Вставить("Key", "НомерПеревозЭлДок");
	
	Результат = МассивСтруктур_НайтиСтроки(ИнфПолФХЖ1, ПараметрыОтбора);
	
	Возврат Результат;
	
КонецФункции

Процедура ВалидацияUTD970_ДобавитьПредупреждениеВалидацияИнфПолФХЖ1(СписокПредупреждений, ТекстПредупреждения)
	
	ПутьКПолю = "Документ / СвСчФакт / ИнфПолФХЖ1";
	
	ОписаниеПредупреждения = НовоеОписаниеПредупреждения(
		ВалидацияUTD970_ГруппаПредупрежденийИнфПолФХЖ1АлкогольнойПродукции(),
		ТекстПредупреждения,
		ПутьКПолю
	);
	
	СписокПредупреждений.Добавить(ОписаниеПредупреждения);
	
КонецПроцедуры

Процедура ВалидацияUTD970_ДобавитьШапкуПредупрежденияВалидацияИнфПолФХЖ1(СписокПредупреждений)
	
	ПутьКПолю = "Документ / СвСчФакт / ИнфПолФХЖ1";
	ТекстПредупреждения =
	"В документе содержится товарная группа
	|""Пиво, напитки, изготавливаемые на основе пива, слабоалкогольные напитки"".
	|При необходимости, заполните в доп. сведениях:";
	
	ОписаниеПредупреждения = НовоеОписаниеПредупреждения(
		ВалидацияUTD970_ГруппаПредупрежденийИнфПолФХЖ1АлкогольнойПродукции(),
		ТекстПредупреждения,
		ПутьКПолю
	);
	
	СписокПредупреждений.Добавить(ОписаниеПредупреждения);
	
КонецПроцедуры

Процедура ВалидацияUTD970_ДобавитьПодвалПредупрежденияВалидацияИнфПолФХЖ1(СписокПредупреждений)
	
	ПутьКПолю = "Документ / СвСчФакт / ИнфПолФХЖ1";
	ТекстПредупреждения = "Чтобы заполнить, перейдите в пакете по кнопке ""Заполнить дополнительные сведения""";
	
	ОписаниеПредупреждения = НовоеОписаниеПредупреждения(
		ВалидацияUTD970_ГруппаПредупрежденийИнфПолФХЖ1АлкогольнойПродукции(),
		ТекстПредупреждения,
		ПутьКПолю
	);
	
	СписокПредупреждений.Добавить(ОписаниеПредупреждения);
	
КонецПроцедуры

Функция ВалидацияUTD970_ГруппаПредупрежденийИнфПолФХЖ1АлкогольнойПродукции()
	
	Возврат "ИнфПолФХЖ1АлкогольнойПродукции";
	
КонецФункции

Функция ВалидацияUTD(UTDContent) Экспорт
	
	списокОшибок = Новый Массив;
	
	ПроверитьОшибкиВШапкеУПД_УКД(СписокОшибок, UTDContent);
	
	МаксДлинаАдреса = 255;
	
	Если  СтруктураЗаполнена(UTDContent.Shipper.OrganizationInfo)
		И Не UTDContent.Shipper.SameAsSeller Тогда
		ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляУПД(UTDContent.Shipper.OrganizationInfo, 2, UTDContent.Function, МаксДлинаАдреса);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <грузоотправителе>", UTDContent.Shipper.OrganizationInfo.Ссылка);
	КонецЕсли;
	
	Если  СтруктураЗаполнена(UTDContent.Consignee) Тогда 
		ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляУПД(UTDContent.Consignee, 2, UTDContent.Function, МаксДлинаАдреса);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <грузополучателе>", UTDContent.Consignee.Ссылка);
	КонецЕсли;
	
	Для каждого стр Из UTDContent.PaymentDocuments Цикл 
		ПроверитьФорматПоля(СписокОшибок, стр.Number, "Номер платежно-расчетного документа", Ложь, 0, 30, Ложь, "м");
		ПроверитьФорматДаты(СписокОшибок, стр.Date, "Дата платежно-расчетного документа");
	КонецЦикла;
		
	ПроверитьОшибкиОснованияПередачи(СписокОшибок, UTDContent);
	
	// валидируем ТЧ
	
	Если UTDContent.InvoiceTable.Items.Количество() = 0 Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствует табличная часть документа");
	КонецЕсли;
	
	ц = 0;
	Для каждого item Из UTDContent.InvoiceTable.Items Цикл 
		ц = ц + 1;
		ОшибкиВСтроке = ПроверитьОшибкиВСтрокеУПД(item);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиВСтроке, "Ошибки в строке " + ц + " табличной части");
	КонецЦикла;	
	
	ДополнитьМассивОшибокВалидации(СписокОшибок, ПроверитьКлассификаторыУПД(UTDContent));
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияUCD(UCDContent) Экспорт
	
	СписокОшибок = Новый Массив;
	
	Если UCDContent.InvoiceCorrectionTable.Items.Количество() = 0 Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Нет строк, по которым происходит корректировка суммовых или количественных показателей");
	КонецЕсли;
	
	ПроверитьОшибкиВШапкеУПД_УКД(СписокОшибок, UCDContent);
	
	//валидируем ТЧ
	ц = 0;
	Для Каждого item Из UCDContent.InvoiceCorrectionTable.Items Цикл 
		ц = ц + 1;
		ОшибкиВСтроке = ПроверитьОшибкиВСтрокеКСФ(Item);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиВСтроке, "Ошибки в строке " + ц + " табличной части");
	КонецЦикла;
	
	ДополнитьМассивОшибокВалидации(СписокОшибок, ПроверитьКлассификаторыУКД(UCDContent, UCDContent.InvoiceCorrectionTable.Items));
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ПроверитьРеквизитыКорректируемогоСчетаФактуры(РеквизитыСчФ)
	
	Ошибки = Новый Массив;
	
	ПроверитьФорматПоля(
		Ошибки,
		РеквизитыСчФ.InvoiceDate,
		"ДатаСчФ",
		Истина
	);
	
	ПроверитьФорматПоля(
		Ошибки,
		РеквизитыСчФ.InvoiceNumber,
		"НомерСчФ",
		Истина,
		1,
		1000
	);
	
	Возврат Ошибки;
	
КонецФункции

Функция ВалидацияUCD736(UCDContent) Экспорт
	
	СписокОшибок = Новый Массив;
	
	Если Не ЗначениеЗаполнено(UCDContent.Invoices) Тогда
		
		ДобавитьОшибкуВалидации(
			СписокОшибок,
			"Не заполнен элемент Файл/Документ/СвКСчФ/СчФ"
		);
		
	КонецЕсли;
	
	Индекс = 0;
	
	Для Каждого РеквизитыСчФ Из UCDContent.Invoices Цикл
		
		ОшибкиСчФ = ПроверитьРеквизитыКорректируемогоСчетаФактуры(РеквизитыСчФ);
		
		ЗаголовокГруппыОшибок = "Файл/Документ/СвКСчФ/СчФ[" + XMLСтрока(Индекс) + "]";
		
		ДобавитьГруппуОшибокВалидации(
			СписокОшибок,
			ОшибкиСчФ,
			ЗаголовокГруппыОшибок
		);
		
		Индекс = Индекс + 1;
		
	КонецЦикла;
	
	Если UCDContent.InvoiceCorrectionTable.Items.Количество() = 0 Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Нет строк, по которым происходит корректировка суммовых или количественных показателей");
	КонецЕсли;
	
	ФорматПриказа = "736";
	
	ПроверитьОшибкиВШапкеУПД_УКД(СписокОшибок, UCDContent, ФорматПриказа);
	
	// валидируем ТЧ
	ВсеСтрокиНДС_ИсчНалАг = Истина;
	ц = 0;
	
	ЕстьСтрокиБезИзмененияСуммыНалога = Ложь;
	ОшибкиПоСтрокамТЧ = Новый Массив;
	
	Для Каждого item Из UCDContent.InvoiceCorrectionTable.Items Цикл 
		
		ц = ц + 1;
		
		ОшибкиВСтроке = Новый Массив;
		ПроверитьФорматПоля(ОшибкиВСтроке, Item.Product, "Наименование товара", Истина, 0, 1000, Ложь, "с");
		
		Если Item.AmountsDec.Total = 0 И Item.AmountsInc.Total = 0 Тогда
			ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Нет изменения стоимости товаров (работ, услуг), имущественных прав с налогом - всего");
		КонецЕсли;
		
		// Должно присутствовать хотя бы одно из полей @НаимТов, ДопСведТов/@ХарактерТов, ДопСведТов/@АртикулТов, ДопСведТов/@КодТов"
		Если Не ЗначениеЗаполнено(item.Product)
			И Не ЗначениеЗаполнено(item.ItemCharact)
			И Не ЗначениеЗаполнено(item.ItemArticle)
			И Не ЗначениеЗаполнено(item.ItemVendorCode) Тогда
			
			ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Должно быть заполнено хотя бы одно из полей: Наименование товара, характеристика, артикул, код");
			
		КонецЕсли;
		
		// @КолТовДо должен быть больше 0"
		КоличествоДоИзменения = item.OriginalValues.Quantity;
		Если КоличествоДоИзменения <> Неопределено
			И КоличествоДоИзменения <= 0 Тогда
			ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Количество товара до изменения должно быть больше 0");
		КонецЕсли;
		
		// СумНДС должна быть >= 0"
		СуммаНалогаДоИзменения = item.OriginalValues.Vat;
		Если ЗначениеЗаполнено(СуммаНалогаДоИзменения)
			И СуммаНалогаДоИзменения < 0 Тогда
			ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Сумма налога до изменения должна быть больше или равной 0");
		КонецЕсли;
		
		ВалидацияUCD736_ПроверитьСтавкиНДС(item, ОшибкиВСтроке);
		
		Если Не TaxRate_ЭтоЗначениеИсчНалАг(item.OriginalValues.TaxRate) Тогда
			
			ВсеСтрокиНДС_ИсчНалАг = Ложь;
			
			// СумНалПосле должен присутствовать при @НалСтДо != 'НДС исчисляется налоговым агентом'
			// СумНДС должна быть >= 0"
			СуммаНалогаПослеИзменения = item.CorrectedValues.Vat;
			Если ЗначениеЗаполнено(СуммаНалогаПослеИзменения)
				И СуммаНалогаПослеИзменения < 0 Тогда
				ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Сумма налога после изменения должна быть больше или равной 0");
			КонецЕсли;
			
			// СумНалРазн должен присутствовать при @НалСтДо != 'НДС исчисляется налоговым агентом'"
			Если НЕ ЗначениеЗаполнено(item.AmountsInc.Vat) 
				И НЕ ЗначениеЗаполнено(item.AmountsDec.Vat) 
				И ПроверятьСуммыНалогов(item.OriginalValues.TaxRate) Тогда 
				ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Сумма увеличения или уменьшения налога должна быть заполнена");
				ЕстьСтрокиБезИзмененияСуммыНалога = Истина;
			КонецЕсли;
			
		КонецЕсли;
		
		// ДопСведТов/@НаимЕдИзмДо (ДопСведТов/@НаимЕдИзмДо=<value-of select=""ДопСведТов/@НаимЕдИзмДо""/>) должно быть заполнено при заполненном @ОКЕИ_ТовДо"
		Если ЗначениеЗаполнено(item.OriginalValues.UnitCode)
			И Не ЗначениеЗаполнено(item.OriginalValues.UnitName) Тогда
			ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Не заполнено наименование единицы измерения до изменения");
		КонецЕсли;
		
		// Код ТН ВЭД
		ПроверитьФорматПоля(ОшибкиВСтроке, item.ItemTypeCode, "Код вида товара (ТН ВЭД)", Ложь, 10, 10, Истина, "м");

		ДобавитьГруппуОшибокВалидации(ОшибкиПоСтрокамТЧ, ОшибкиВСтроке, "Ошибки в строке " + ц + " табличной части");
		
	КонецЦикла;
	
	Если ЕстьСтрокиБезИзмененияСуммыНалога Тогда
		ДобавитьОшибкуДолжныБытьТолькоСтрокиСКорректировкойНалога(СписокОшибок);
	КонецЕсли;
	
	ДополнитьМассивОшибокВалидации(СписокОшибок, ОшибкиПоСтрокамТЧ);
	
	Если ВсеСтрокиНДС_ИсчНалАг Тогда
		ДополнитьМассивОшибокВалидации(СписокОшибок, ПроверитьИтоговыеСуммыУвеличенияУменьшенияУКД736(UCDContent.InvoiceCorrectionTable));
	КонецЕсли;
	
	ДополнитьМассивОшибокВалидации(СписокОшибок, ПроверитьКлассификаторыУКД(UCDContent, UCDContent.InvoiceCorrectionTable.Items));
	
	Возврат СписокОшибок;
	
КонецФункции

Процедура ВалидацияUCD736_ПроверитьСтавкиНДС(Строка736, ОшибкиВСтроке)
	
	СтавкаДоИзменения = Строка736.OriginalValues.TaxRate;
	
	// BSLLS:IfElseIfEndsWithElse-off
	Если Не ЗначениеЗаполнено(СтавкаДоИзменения) Тогда
		
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Ставка НДС до изменения не указана");
		
	ИначеЕсли Не TaxRate_ЭтоВалидноеЗначение(СтавкаДоИзменения) Тогда
		
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Указана недопустимая ставка налога до изменения (" + СтавкаДоИзменения + ")");
		
	КонецЕсли;
	// BSLLS:IfElseIfEndsWithElse-on
	
	СтавкаПослеИзменения = Строка736.CorrectedValues.TaxRate;
	
	// BSLLS:IfElseIfEndsWithElse-off
	Если Не ЗначениеЗаполнено(СтавкаПослеИзменения) Тогда
		
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Ставка НДС после изменения не указана");
		
	ИначеЕсли Не TaxRate_ЭтоВалидноеЗначение(СтавкаПослеИзменения) Тогда
		
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Указана недопустимая ставка налога после изменения (" + СтавкаПослеИзменения + ")");
		
	КонецЕсли;
	// BSLLS:IfElseIfEndsWithElse-on
	
КонецПроцедуры

Функция ПроверятьСуммыНалогов(Ставка) 
	Результат = Истина;
	
	НеПроверяемыеСтавкиНалогов = Новый Массив;
	НеПроверяемыеСтавкиНалогов.Добавить(TaxRate_БезНДС());
	НеПроверяемыеСтавкиНалогов.Добавить(TaxRate_0());
	
	ПодходящаяСтавка = НеПроверяемыеСтавкиНалогов.Найти(Ставка);
	
	Если Не ПодходящаяСтавка = Неопределено Тогда
		Результат = Ложь;
	КонецЕсли;
	
	Возврат Результат;
КонецФункции

Процедура ДобавитьОшибкуДолжныБытьТолькоСтрокиСКорректировкойНалога(Ошибки)
	
	ЧастиТекста = Новый Массив;
	ЧастиТекста.Добавить("В УКД 736@ формата в табличной части должны быть только те позиции,");
	ЧастиТекста.Добавить("по которым произошли изменения в сумме НДС.");
	ЧастиТекста.Добавить("<Подробнее>.");
	РазделительТекста = Символы.НПП;
	
	ТекстОшибки = Общее_СоединитьСтроку(ЧастиТекста, РазделительТекста);
	
	Гиперссылка = "https://support.kontur.ru/pages/viewpage.action?pageId=83865722";
	
	ДобавитьОшибкуВалидации(Ошибки, ТекстОшибки, Гиперссылка);
	
КонецПроцедуры

Функция ПроверитьИтоговыеСуммыУвеличенияУменьшенияУКД736(InvoiceCorrectionTable)
	
	Результат = Новый Массив;
	
	Если СтруктураЗаполнена(InvoiceCorrectionTable.TotalsInc) Тогда
		ДобавитьГруппуОшибокВалидации(Результат, ПроверитьInvoiceTotalsDiff(InvoiceCorrectionTable.TotalsInc), "Ошибки в заполнении итоговых сумм увеличения");
	Иначе
		ДобавитьГруппуОшибокВалидации(Результат, ПроверитьInvoiceTotalsDiff(InvoiceCorrectionTable.TotalsDec), "Ошибки в заполнении итоговых сумм уменьшения");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПроверитьInvoiceTotalsDiff(InvoiceTotalsDiff)
	
	Результат = Новый Массив;
	
	// @СумНал должен быть заполнен в случаях, когда все // СведТов/@НалСтДо = 'НДС исчисляется налоговым агентом'
	// СумНДС должна быть >= 0"
	СуммаНалога = InvoiceTotalsDiff.Vat;
	ЕстьБезНДС = ЭтоИстина(InvoiceTotalsDiff.WithoutVat);
	ЕстьСумНДС = (СуммаНалога <> Неопределено);
	
	// Должно быть что-то одно и только одно: либо @СумНДС, либо @БезНДС
	Если ЕстьБезНДС И ЕстьСумНДС 
		Или Не ЕстьБезНДС И Не ЕстьСумНДС Тогда
		
		ШаблонОшибки = "Должна быть указана итоговая сумма налога или значение ""%1""";
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки, СтрокаБезНДС());
		ДобавитьОшибкуВалидации(Результат, ТекстОшибки);
		
	ИначеЕсли ЕстьСумНДС Тогда
		
		Если СуммаНалога < 0 Тогда
			ДобавитьОшибкуВалидации(Результат, "Не указана итоговая сумма налога");
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ВалидацияАктСверки405ИнформацияОтправителя(Content) Экспорт
	
	СписокОшибок = Новый Массив;
	
	Sender = Content.ReconciliationAct.AdditionalInfoReconciliationAct.Sender;
	Ошибки = ВалидацияАктСверки405_Участник(Sender,
		Content.СлужебнаяИнформация.Организация,
		"об отправителе",
		"организации");
	
	ДополнитьМассивОшибокВалидации(СписокОшибок, Ошибки);

	Recipient = Content.ReconciliationAct.AdditionalInfoReconciliationAct.Recipient;
	Ошибки = ВалидацияАктСверки405_Участник(Recipient,
		Content.СлужебнаяИнформация.Контрагент,
		"о получателе",
		"контрагента");
	
	ДополнитьМассивОшибокВалидации(СписокОшибок, Ошибки);

	СписокОшибокДокумента = Новый Массив;

	Currency = Content.ReconciliationAct.AdditionalInfoReconciliationAct.Currency;
	
	ПроверитьФорматПоля(СписокОшибокДокумента, Currency,
		"Код валюты", Истина, 3, 3, Истина);
	
	Если СписокОшибокДокумента.Количество() > 0 Тогда
	
		ДобавитьГруппуОшибокВалидации(СписокОшибок, СписокОшибокДокумента,
			"В <связанном документе 1С> не указана валюта.
			|Перейдите в документ 1С и заполните валюту",
			Content.СлужебнаяИнформация.Ссылка);
			
	Иначе
		
		Если Не ВалидацияАктСверки405_ПроверитьВалютуПоКлассификатору(Currency) Тогда
			ДобавитьОшибкуВалидации(СписокОшибокДокумента, "Перейдите в документ 1С и выберите валюту из классификатора");
			
			ШаблонОшибки = "В <связанном документе 1С> валюта с кодом %1 не найдена в классификаторе валют";
			СтрокаОшибка = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки, Currency);
			
			ДобавитьГруппуОшибокВалидации(СписокОшибок, СписокОшибокДокумента,
				СтрокаОшибка,
				Content.СлужебнаяИнформация.Ссылка);
			
		КонецЕсли;
		
	КонецЕсли;
	
	СписокОшибокДокумента = Новый Массив;

	ПроверитьФорматДаты(СписокОшибокДокумента, Content.ReconcilationDateFrom,
		"Дата начала периода взаимных расчетов (ДатаНачПер)", Истина);
	
	Если СписокОшибокДокумента.Количество() > 0 Тогда

		ДобавитьГруппуОшибокВалидации(СписокОшибок, СписокОшибокДокумента,
			"В <связанном документе 1С> не указана дата начала периода взаимных расчетов.
			|Перейдите в документ 1С и заполните дату начала  периода взаимных расчетов",
			Content.СлужебнаяИнформация.Ссылка);
	
	КонецЕсли;
	
	СписокОшибокДокумента = Новый Массив;

	ПроверитьФорматДаты(СписокОшибокДокумента, Content.ReconcilationDateTo,
		"Дата окончания периода взаимных расчетов (ДатаОкПер)", Истина);
	
	Если СписокОшибокДокумента.Количество() > 0 Тогда

		ДобавитьГруппуОшибокВалидации(СписокОшибок, СписокОшибокДокумента,
			"В <связанном документе 1С> не указана дата окончания периода взаимных расчетов.
			|Перейдите в документ 1С и заполните дату окончания периода взаимных расчетов",
			Content.СлужебнаяИнформация.Ссылка);
	
	КонецЕсли;
	
	СписокОшибокДокумента = Новый Массив;

	Ошибки = ВалидацияАктСверки405_ДокументыПоДоговору(Content.ReconciliationAct.ReconciliationActTable.ContractInfo);
	ДополнитьМассивОшибокВалидации(СписокОшибокДокумента, Ошибки);

	ДобавитьГруппуОшибокВалидации(СписокОшибок, СписокОшибокДокумента,
		"Ошибки в связанном документе. Перейдите в <документ 1С> и заполните",
		Content.СлужебнаяИнформация.Ссылка);

	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияАктСверки405ИнформацияПолучателя(Content) Экспорт
	
	СписокОшибок = Новый Массив;
	
	Если ЗначениеЗаполнено(Content.СлужебнаяИнформация.Ссылка) Тогда
		СписокОшибокДокумента = Новый Массив;

		Ошибки = ВалидацияАктСверки405_ДокументыПоДоговору(Content.ReconciliationAct.ReconciliationActTable.ContractInfo);
		ДополнитьМассивОшибокВалидации(СписокОшибокДокумента, Ошибки);

		ДобавитьГруппуОшибокВалидации(СписокОшибок, СписокОшибокДокумента,
			"Ошибки в связанном документе. Перейдите в <документ 1С> и заполните",
			Content.СлужебнаяИнформация.Ссылка);
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2(Content) Экспорт 
	
	СписокОшибок = Новый Массив;
	
	// Дата и номер документа о приемке и (или) расхождениях
	ПроверитьФорматПоля(СписокОшибок, Content.DocumentNumber, "Номер документа о приемке и (или) расхождениях", Истина, 1, 255);
	ПроверитьФорматДаты(СписокОшибок, Content.DocumentDate, "Дата составления (выписки) документа о приемке и (или) расхождениях", Истина);
	
	// Исправление документа о приемке и (или) расхождениях
	Если СтруктураЗаполнена(Content.RevisionInfo) Тогда
		ПроверитьФорматПоля(СписокОшибок, Content.RevisionInfo.RevisionNumber, "Номер исправления документа", Истина, 1, 3, Истина);
		ПроверитьФорматДаты(СписокОшибок, Content.RevisionInfo.RevisionDate, "Дата исправления документа", Истина);
	КонецЕсли;
	
	// Наименование документа, определенное организацией (согласованное сторонами сделки)
	ПроверитьФорматПоля(СписокОшибок, Content.DocumentName, "Наименование документа, определенное организацией (согласованное сторонами сделки)", Истина, 1, 255, , "с");
	
	// Сведения об обстоятельствах приемки
	Ошибки = ВалидацияТорг2_СведенияОбОбстоятельствахПриемки(Content.CircumstancesAcceptanceInfo);
	ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в сведениях об обстоятельствах приемки");
	
	// Сведения о факте приемки и (или) о расхождениях
	Ошибки = ВалидацияТорг2_СведенияОФактеПриемки(Content.EvidenceAcceptanceInfo);
	ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в сведениях о факте приемки и (или) о расхождениях");
	
	ПроверитьФорматПоля(СписокОшибок, Content.SenderFnsParticipantId, "Идентификатор участника ЭДО (отправителя файла обмена информации покупателя)", , 4, 46);
	ПроверитьФорматПоля(СписокОшибок, Content.RecipientFnsParticipantId, "Идентификатор участника ЭДО (получателя файла обмена информации покупателя)", , 4, 46);
	ПроверитьФорматПоля(СписокОшибок, Content.DocumentCreator, "Наименование экономического субъекта - составителя файла обмена информации покупателя", Истина, 1, 1000, , "с");
	ПроверитьФорматПоля(СписокОшибок, Content.DocumentCreatorBase, "Основание, по которому экономический субъект является составителем файла обмена информации покупателя", , 1, 120, , "с");
	ПроверитьФорматПоля(СписокОшибок, Content.AdditionalInfosPurpose, "Назначение и подписанты дополнительных сведений", , 1, 2000, , "с");
	ПроверитьФорматПоля(СписокОшибок, Content.IsInformed, " Признак ознакомления с правилами приемки и предупреждением об ответственности", , 1, 1);
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_СведенияОбОбстоятельствахПриемки(CircumstancesAcceptanceInfo)
	
	СписокОшибок = Новый Массив;
	
	// Продавец (поставщик, исполнитель)
	Ошибки = ВалидацияТорг2_Участник(CircumstancesAcceptanceInfo.Seller);
	ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в сведениях о <продавце (поставщике, исполнителе)>");
	
	// Покупатель (заказчик)
	Ошибки = ВалидацияТорг2_Участник(CircumstancesAcceptanceInfo.Buyer);
	ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в сведениях о <покупателе (заказчике)>");
	
	// Грузоотправитель (отправитель)
	Если СтруктураЗаполнена(CircumstancesAcceptanceInfo.Shipper) Тогда
		Ошибки = ВалидацияТорг2_Участник(CircumstancesAcceptanceInfo.Shipper);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в сведениях о <грузоотправителе (отправителе)>");
	КонецЕсли;
	
	// Грузополучатель (получатель)
	Если СтруктураЗаполнена(CircumstancesAcceptanceInfo.Consignee) Тогда
		Ошибки = ВалидацияТорг2_Участник(CircumstancesAcceptanceInfo.Consignee);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в сведениях о <грузополучателе (получателе)>");
	КонецЕсли;
	
	// Страховая компания
	Если СтруктураЗаполнена(CircumstancesAcceptanceInfo.InsuranceCompany) Тогда
		Ошибки = ВалидацияТорг2_Участник(CircumstancesAcceptanceInfo.InsuranceCompany);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в сведениях о <страховой компании>");
	КонецЕсли;
	
	// Дата и номер приказа (распоряжения) о назначении комиссии
	Если СтруктураЗаполнена(CircumstancesAcceptanceInfo.Order) Тогда
		Order = CircumstancesAcceptanceInfo.Order;
		ПроверитьФорматПоля(СписокОшибок, Order.Number, "Номер приказа (распоряжения)", Истина, 1, 255);
		ПроверитьФорматДаты(СписокОшибок, Order.Date, "Дата приказа (распоряжения)", Истина);
	КонецЕсли;
	
	// Сведения о событиях, связанных с осмотром груза
	Если СтруктураЗаполнена(CircumstancesAcceptanceInfo.CargoInspectionInformation) Тогда
		Ошибки = ВалидацияТорг2_СведенияОРезультатахОсмотра(CircumstancesAcceptanceInfo.CargoInspectionInformation);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в сведениях о событиях, связанных с осмотром груза");
	КонецЕсли;
	
	// Сведения о дате и времени событий, связанных с приемкой груза
	Если СтруктураЗаполнена(CircumstancesAcceptanceInfo.ReceptionInformation) Тогда
		Ошибки = ВалидацияТорг2_СведенияОДатеИВремениПриемки(CircumstancesAcceptanceInfo.ReceptionInformation);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в сведениях о дате и времени событий, связанных с приемкой груза");
	КонецЕсли;
	
	// Сведения о грузе по сопроводительным транспортным документам
	Если СтруктураЗаполнена(CircumstancesAcceptanceInfo.AccompanyingDocumentTable) Тогда
		Ошибки = ВалидацияТорг2_СведенияОГрузе(CircumstancesAcceptanceInfo.AccompanyingDocumentTable);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в сведениях о грузе по сопроводительным транспортным документам");
	КонецЕсли;
	
	// Сведения о лице, принявшем товар (получившем груз) (в том числе на ответственное хранение)
	Если СтруктураЗаполнена(CircumstancesAcceptanceInfo.PersonAcceptedCargoInfo) Тогда
		Ошибки = ВалидацияТорг2_СведенияОЛицеПринявшемТовар(CircumstancesAcceptanceInfo.PersonAcceptedCargoInfo);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в сведениях о лице, принявшем товар (получившем груз) (в том числе на ответственное хранение)");
	КонецЕсли;
	
	// Другие обстоятельства приемки ценностей
	Если СтруктураЗаполнена(CircumstancesAcceptanceInfo.OtherCircumstancesAcceptanceInfo) Тогда
		Ошибки = ВалидацияТорг2_ДругиеОбстоятельстваПриемки(CircumstancesAcceptanceInfo.OtherCircumstancesAcceptanceInfo);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в других обстоятельства приемки ценностей");
	КонецЕсли;
	
	// Обозначение (код) обстоятельств формирования (использования) документа
	Если ЗначениеЗаполнено(CircumstancesAcceptanceInfo.CircumFormat) Тогда
		ПроверитьФорматПоля(СписокОшибок, CircumstancesAcceptanceInfo.CircumFormat, "Обозначение (код) обстоятельств формирования (использования) документа", , , 4);
	КонецЕсли;
	
	// Идентификатор государственного контракта
	Если ЗначениеЗаполнено(CircumstancesAcceptanceInfo.GovernmentContractInfo) Тогда
		ПроверитьФорматПоля(СписокОшибок, CircumstancesAcceptanceInfo.GovernmentContractInfo, "Идентификатор государственного контракта", , 1, 255);
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_СведенияОРезультатахОсмотра(CargoInspectionInformation)
	
	СписокОшибок = Новый Массив;
	
	Для Каждого CertificateConformityNumber Из CargoInspectionInformation.CertificateConformityNumbers Цикл
		ПроверитьФорматПоля(СписокОшибок, CertificateConformityNumber, "Номер сертификата соответствия", , 1, 50);
	КонецЦикла;
	
	Ошибки = ВалидацияТорг2_СопроводительныйДокумент(CargoInspectionInformation.AccompanyingDocumentInfo);
	ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в сопроводительном документе");
	
	Если ЗначениеЗаполнено(CargoInspectionInformation.ReceptionDate) Тогда
		ПроверитьФорматДаты(СписокОшибок, CargoInspectionInformation.ReceptionDate, "Дата осмотра прибывшего груза");
	КонецЕсли;
	
	ПроверитьФорматПоля(СписокОшибок, CargoInspectionInformation.ReceptionPlace, "Место составления документа о приемке и (или) расхождениях", , 1, 1000);
	ПроверитьФорматПоля(СписокОшибок, CargoInspectionInformation.ReceptionStartTime, "Время начала приемки", , 8, 8);
	ПроверитьФорматПоля(СписокОшибок, CargoInspectionInformation.ReceptionFinishTime, "Время окончания приемки", , 8, 8);
	
	Если ЗначениеЗаполнено(CargoInspectionInformation.DeliveryDate) Тогда
		ПроверитьФорматДаты(СписокОшибок, CargoInspectionInformation.DeliveryDate, "Дата отправки груза со станции (пристани, порта)");
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_СведенияОДатеИВремениПриемки(ReceptionInformation)
	
	СписокОшибок = Новый Массив;
	
	Если ЗначениеЗаполнено(ReceptionInformation.CargoArrivalDateTime) Тогда
		ПроверитьФорматДаты(СписокОшибок, ReceptionInformation.CargoArrivalDateTime, "Прибытие на место назначения (станция, пристань, порт)");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ReceptionInformation.DeliveryPoint) Тогда
		ПроверитьФорматДаты(СписокОшибок, ReceptionInformation.DeliveryPoint, "Выдача груза транспортной организацией");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ReceptionInformation.CargoOpeningDateTime) Тогда
		ПроверитьФорматДаты(СписокОшибок, ReceptionInformation.CargoOpeningDateTime, "Вскрытие вагона (других транспортных средств)");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ReceptionInformation.DeliveryDateTime) Тогда
		ПроверитьФорматДаты(СписокОшибок, ReceptionInformation.DeliveryDateTime, "Доставка на склад получателя");
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_СведенияОГрузе(AccompanyingDocumentTable)
	
	СписокОшибок = Новый Массив;
	
	Для Каждого Item Из AccompanyingDocumentTable.Item Цикл
		
		ПроверитьФорматПоля(СписокОшибок, Item.SealingMark, "Отметка об опломбировании", , 1, 20, , "ж");
		ПроверитьФорматПоля(СписокОшибок, Item.PackingType, "Вид упаковки", , 1, 100);
		ПроверитьФорматПоля(СписокОшибок, Item.CargoName, "Наименование груза (товара)", Истина, 1, 1000, , "с");
		ПроверитьФорматПоля(СписокОшибок, Item.Unit, "Единицы измерения товара (код)", , 3, 4, Истина, "ж");
		ПроверитьФорматПоля(СписокОшибок, Item.UnitName, "Наименование единицы измерения товара", Item.Unit = "0000", 1, 255, , "с");
		
	КонецЦикла;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_СведенияОЛицеПринявшемТовар(PersonAcceptedCargoInfo)
	
	СписокОшибок = Новый Массив;
	
	Если СтруктураЗаполнена(PersonAcceptedCargoInfo.Employee) Тогда
		
		Employee = PersonAcceptedCargoInfo.Employee;
		
		ПроверитьФорматПоля(СписокОшибок, Employee.Position, "Должность", Истина, 1, 128, , "ж");
		ПроверитьФорматПоля(СписокОшибок, Employee.EmployeeInfo, "Иные сведения, идентифицирующие физическое лицо", , 1, 255, , , "множ");
		ПроверитьФорматПоля(СписокОшибок, Employee.EmployeeBase, "Основание полномочий представителя", , 1, 120, , "с");
		ПроверитьФорматПоля(СписокОшибок, Employee.LastName, "Фамилия", Истина, 1, 60, , "ж");
		ПроверитьФорматПоля(СписокОшибок, Employee.FirstName, "Имя", Истина, 1, 60, , "с");
		ПроверитьФорматПоля(СписокОшибок, Employee.MiddleName, "Отчество", , 1, 60, , "с");
		
	Иначе
		
		OtherIssuer = PersonAcceptedCargoInfo.OtherIssuer;
		
		ПроверитьФорматПоля(СписокОшибок, OtherIssuer.Position, "Должность", Истина, 1, 128, , "ж");
		ПроверитьФорматПоля(СписокОшибок, OtherIssuer.EmployeeInfo, "Иные сведения, идентифицирующие физическое лицо", , 1, 255, , , "множ");
		ПроверитьФорматПоля(СписокОшибок, OtherIssuer.EmployeeBase, "Основание полномочий представителя", , 1, 120, , "с");
		ПроверитьФорматПоля(СписокОшибок, OtherIssuer.OrganizationName, "Наименование организации, которой доверена передача", , 1, 128, , "с");
		ПроверитьФорматПоля(СписокОшибок, OtherIssuer.OrganizationBase, "Основание, по которому организации доверена передача", , 1, 120, , "с");
		ПроверитьФорматПоля(СписокОшибок, OtherIssuer.LastName, "Фамилия", Истина, 1, 60, , "ж");
		ПроверитьФорматПоля(СписокОшибок, OtherIssuer.FirstName, "Имя", Истина, 1, 60, , "с");
		ПроверитьФорматПоля(СписокОшибок, OtherIssuer.MiddleName, "Отчество", , 1, 60, , "с");
		
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_ДругиеОбстоятельстваПриемки(OtherCircumstancesAcceptanceInfo)
	
	СписокОшибок = Новый Массив;
	
	Если СтруктураЗаполнена(OtherCircumstancesAcceptanceInfo.Carrier) Тогда
		Ошибки = ВалидацияТорг2_Участник(OtherCircumstancesAcceptanceInfo.Carrier);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в сведениях о <перевозчике ценностей (груза), представитель которого передает груз>");
	КонецЕсли;
	
	Если СтруктураЗаполнена(OtherCircumstancesAcceptanceInfo.AccompanyingTransferDocumentInfo) Тогда
		Ошибки = ВалидацияТорг2_СопроводительныйДокумент(OtherCircumstancesAcceptanceInfo.AccompanyingTransferDocumentInfo);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в сопроводительном документе");
	КонецЕсли;
	
	ПроверитьФорматПоля(СписокОшибок, OtherCircumstancesAcceptanceInfo.StorageConditions, "Условия хранения ценностей на складе получателя", , 1, 1000);
	ПроверитьФорматПоля(СписокОшибок, OtherCircumstancesAcceptanceInfo.CargoActualState, "Состояние тары и упаковки в момент осмотра ценностей", , 1, 1000);
	ПроверитьФорматПоля(СписокОшибок, OtherCircumstancesAcceptanceInfo.MissingAssets, "Тип определения недостающих ценностей", , 1, 200);
	ПроверитьФорматПоля(СписокОшибок, OtherCircumstancesAcceptanceInfo.AdditionalInfo, "Дополнительные сведения", , 1, 1000);
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_Участник(ExtendedOrganizationInfo_Torg2)
	
	СписокОшибок = Новый Массив;
	
	OrganizationDetails = ExtendedOrganizationInfo_Torg2.OrganizationDetails;
	
	Если OrganizationDetails.OrgType = "4" Тогда
		
		// Сведения о физическом лице
		
		Ошибки = ПроверитьФИО(OrganizationDetails.OrgName);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в ФИО физического лица");
		
		Если ЗначениеЗаполнено(OrganizationDetails.Inn) Тогда
			Ошибки = ПроверитьИННКонтрагента(OrganizationDetails.Inn, "ФизЛицо");
			ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(OrganizationDetails.OrganizationOrPersonInfo) Тогда
			ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.OrganizationOrPersonInfo, "Иные сведения, идентифицирующие физическое лицо", , 1, 255, , , "множ");
		КонецЕсли;
		
	ИначеЕсли OrganizationDetails.OrgType = "2" Тогда
		
		// Сведения об индивидуальном предпринимателе
		
		Ошибки = ПроверитьФИО(OrganizationDetails.OrgName);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в ФИО индивидуального предпринимателя");
		
		Ошибки = ПроверитьИННКонтрагента(OrganizationDetails.Inn, "ФизЛицо");
		ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
		
		Если ЗначениеЗаполнено(OrganizationDetails.IndividualEntityRegistrationCertificate) Тогда
			ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.IndividualEntityRegistrationCertificate, "Реквизиты свидетельства о государственной регистрации ИП", , 1, 100, , , "множ");
		КонецЕсли;
		
		Если ЗначениеЗаполнено(OrganizationDetails.OrganizationOrPersonInfo) Тогда
			ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.OrganizationOrPersonInfo, "Иные сведения, идентифицирующие физическое лицо", , 1, 255, , , "множ");
		КонецЕсли;
		
	Иначе
		
		ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.OrgName, "Наименование организации", Истина, 1, 1000, , "с");
		
		Если OrganizationDetails.OrgType = "1" Тогда
			
			// Сведения о юридическом лице, состоящем на учете в налоговых органах
			
			Ошибки = ПроверитьИННКонтрагента(OrganizationDetails.Inn, "ЮрЛицо");
			ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
			
			Если ЗначениеЗаполнено(OrganizationDetails.Kpp) Тогда
				Ошибки = ПроверитьКППКонтрагента(OrganizationDetails.Kpp);
				ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
			КонецЕсли;
			
		Иначе
			
			// Сведения об иностранном лице, не состоящем на учете в налоговых органах
			
			Если ЗначениеЗаполнено(OrganizationDetails.LegalEntityId) Тогда
				ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.LegalEntityId, "Идентификатор юридического лица", , 1, 255);
			КонецЕсли;
			
			Если ЗначениеЗаполнено(OrganizationDetails.OrganizationOrPersonInfo) Тогда
				ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.OrganizationOrPersonInfo, "Иные сведения, идентифицирующие организацию", , 1, 255, , , "множ");
			КонецЕсли;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Ошибки = ВалидацияТорг2_Адрес(OrganizationDetails.Address);
	ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в адресе");
	
	ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.Okpo, "Код в общероссийском классификаторе предприятий и организаций", , 1, 10);
	ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.Phone, "Номер контактного телефона/факс", , 1, 255);
	ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.Email, "Адрес электронной почты", , 1, 255);
	ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.CorrespondentAccount, "Корреспондентский счёт", , 1, 20);
	ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.BankAccountNumber, "Номер банковского счета", , 1, 20);
	ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.BankName, "Название банка", , 1, 1000, , "с");
	ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.BankId, "БИК", , 1, 9);
	ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.Department, "Структурное подразделение", , 1, 1000, , "с");
	ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.OrganizationAdditionalInfo, "Информация для участника", , 1, 255, , "ж");
	ПроверитьФорматПоля(СписокОшибок, OrganizationDetails.Country, "Страна", , 1, 255, , "ж");
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_Адрес(Address_Torg2)
	
	СписокОшибок = Новый Массив;
	
	Если Не СтруктураЗаполнена(Address_Torg2) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют сведения об адресе");
	КонецЕсли;
	
	Если СтруктураЗаполнена(Address_Torg2.RussianAddress) Тогда
		
		RussianAddress = Address_Torg2.RussianAddress;
		
		ПроверитьФорматПоля(СписокОшибок, RussianAddress.ZipCode, "Индекс", , , 6, Истина);
		ПроверитьФорматПоля(СписокОшибок, RussianAddress.Region, "Регион", Истина, 1, 50);
		ПроверитьФорматПоля(СписокОшибок, RussianAddress.Territory, "Район", , 1, 50);
		ПроверитьФорматПоля(СписокОшибок, RussianAddress.City, "Город", , 1, 50);
		ПроверитьФорматПоля(СписокОшибок, RussianAddress.Locality, "Населенный пункт", , 1, 50);
		ПроверитьФорматПоля(СписокОшибок, RussianAddress.Street, "Улица", , 1, 50, , "ж");
		ПроверитьФорматПоля(СписокОшибок, RussianAddress.Building, "Дом", , 1, 20);
		ПроверитьФорматПоля(СписокОшибок, RussianAddress.Block, "Корпус", , 1, 20);
		ПроверитьФорматПоля(СписокОшибок, RussianAddress.Apartment, "Квартира", , 1, 20, , "ж");
		
	ИначеЕсли СтруктураЗаполнена(Address_Torg2.ForeignAddress) Тогда
		
		ForeignAddress = Address_Torg2.ForeignAddress;
		
		ПроверитьФорматПоля(СписокОшибок, ForeignAddress.Country, "Страна (код)", Истина, 1, 60, , "ж");
		ПроверитьФорматПоля(СписокОшибок, ForeignAddress.Address, "Текст адреса", Истина, 1, 255);
		
	ИначеЕсли ЗначениеЗаполнено(Address_Torg2.AddressCode) Тогда
		ПроверитьФорматПоля(СписокОшибок, Address_Torg2.AddressCode, "Уникальный номер адреса объекта адресации в государственном адресном реестре", Истина, 1, 36);
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_СопроводительныйДокумент(AccompanyingDocument)
	
	СписокОшибок = Новый Массив;
	
	ПроверитьФорматПоля(СписокОшибок, AccompanyingDocument.Name, "Наименование сопроводительного документа", Истина, 1, 200, , "с");
	ПроверитьФорматПоля(СписокОшибок, AccompanyingDocument.Number, "Номер сопроводительного документа", , 1, 100);
	ПроверитьФорматДаты(СписокОшибок, AccompanyingDocument.Date, "Дата сопроводительного документа");
	ПроверитьФорматПоля(СписокОшибок, AccompanyingDocument.AdditionalIdentifier, "Дополнительный идентификатор сопроводительного документа", , 1, 255);
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_СведенияОФактеПриемки(EvidenceAcceptanceInfo)
	
	СписокОшибок = Новый Массив;
	
	Ошибки = ВалидацияТорг2_РезультатыПриемки(EvidenceAcceptanceInfo.AcceptanceResults);
	ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в результатах приемки ценностей (груза)");
	
	Если СтруктураЗаполнена(EvidenceAcceptanceInfo.AttachedDocuments) Тогда
		Ошибки = ВалидацияТорг2_ПрилагаемыеДокументы(EvidenceAcceptanceInfo.AttachedDocuments);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в прилагаемых документах");
	КонецЕсли;
	
	ПроверитьФорматПоля(СписокОшибок, EvidenceAcceptanceInfo.OperationType, "Вид операции", , 1, 255);
	ПроверитьФорматПоля(СписокОшибок, EvidenceAcceptanceInfo.EventSummary, "Краткое описание события", Истина, 1, 2000, , "с");
	ПроверитьФорматПоля(СписокОшибок, EvidenceAcceptanceInfo.Conclusion, "Заключение комиссии", , 1, 2000);
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_РезультатыПриемки(AcceptanceResults)
	
	СписокОшибок = Новый Массив;
	
	Для Каждого AcceptanceResult Из AcceptanceResults.AcceptanceResult Цикл
		
		Assets = AcceptanceResult.Assets;
		
		ПроверитьФорматПоля(СписокОшибок, Assets.Product, "Наименование товара (ценности)", , 1, 1000, , "с");
		ПроверитьФорматПоля(СписокОшибок, Assets.ItemCharact, "Характеристика товара (ценности)", , 1, 1000, , "ж");
		ПроверитьФорматПоля(СписокОшибок, Assets.ItemKind, "Сорт товара (ценности)", , 1, 50);
		ПроверитьФорматПоля(СписокОшибок, Assets.ItemArticle, "Артикул товара (ценности)", , 1, 50);
		ПроверитьФорматПоля(СписокОшибок, Assets.ItemVendorCode, "Код товара (ценности)", , 1, 100);
		ПроверитьФорматПоля(СписокОшибок, Assets.PassportNumber, "Номер паспорта", , 1, 100);
		
		Если СтруктураЗаполнена(AcceptanceResult.AccordingToSellerDocument) Тогда
			Ошибки = ВалидацияТорг2_ДанныеОПринимаемомТоваре(AcceptanceResult.AccordingToSellerDocument);
			ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в информация по документам поставщика");
		КонецЕсли;
		
		Если СтруктураЗаполнена(AcceptanceResult.ActuallyAccepted) Тогда
			Ошибки = ВалидацияТорг2_ДанныеОПринимаемомТоваре(AcceptanceResult.ActuallyAccepted);
			ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в фактической информации");
		КонецЕсли;
		
		Если СтруктураЗаполнена(AcceptanceResult.Breakage) Тогда
			Ошибки = ВалидацияТорг2_ДанныеОРасхождениях(AcceptanceResult.Breakage);
			ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в данных о бое");
		КонецЕсли;
		
		Если СтруктураЗаполнена(AcceptanceResult.Surplus) Тогда
			Ошибки = ВалидацияТорг2_ДанныеОРасхождениях(AcceptanceResult.Surplus);
			ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в данных об излишках");
		КонецЕсли;
		
		Если СтруктураЗаполнена(AcceptanceResult.Shortfall) Тогда
			Ошибки = ВалидацияТорг2_ДанныеОРасхождениях(AcceptanceResult.Shortfall);
			ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в данных о недостаче");
		КонецЕсли;
		
		Если СтруктураЗаполнена(AcceptanceResult.Reject) Тогда
			Ошибки = ВалидацияТорг2_ДанныеОРасхождениях(AcceptanceResult.Reject);
			ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в данных о браке");
		КонецЕсли;
		
		ПроверитьФорматПоля(СписокОшибок, AcceptanceResult.Unit, "Единица измерения товара (код)", , 3, 4, Истина, "ж");
		ПроверитьФорматПоля(СписокОшибок, AcceptanceResult.AcceptanceDiscrepancyInfo, "Информация о расхождениях (претензиях), выявленных при приемке результатов выполненных работ (при документировании факта оказания услуг) или при получении имущественных прав", , 1, 2000, , "ж");
		ПроверитьФорматПоля(СписокОшибок, AcceptanceResult.UnitName, "Наименование единицы измерения товара", AcceptanceResult.Unit = "0000", 1, 255, , "с");
		
	КонецЦикла;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_ПрилагаемыеДокументы(AttachedDocuments)
	
	СписокОшибок = Новый Массив;
	
	Для Каждого AttachedDocument Из AttachedDocuments.AttachedDocument Цикл
		ПроверитьФорматПоля(СписокОшибок, AttachedDocument.Type, "Вид документа", Истина, 1, 500);
		ПроверитьФорматПоля(СписокОшибок, AttachedDocument.Number, "Номер", Истина, 1, 40);
		ПроверитьФорматДаты(СписокОшибок, AttachedDocument.Date, "Дата");
	КонецЦикла;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_ДанныеОПринимаемомТоваре(AcceptedProductData)
	
	СписокОшибок = Новый Массив;
	
	Если СтруктураЗаполнена(AcceptedProductData.ItemIdentificationNumbers) Тогда
		Ошибки = ВалидацияТорг2_НомераСредствИдентификацииТоваров(AcceptedProductData.ItemIdentificationNumbers);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в номерах средств идентификации товаров");
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(AcceptedProductData.Quantity) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Не указано количество (масса)");
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_ДанныеОРасхождениях(DiscrepancyData)
	
	СписокОшибок = Новый Массив;
	
	Если СтруктураЗаполнена(DiscrepancyData.ItemIdentificationNumbers) Тогда
		Ошибки = ВалидацияТорг2_НомераСредствИдентификацииТоваров(DiscrepancyData.ItemIdentificationNumbers);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, Ошибки, "Ошибки в номерах средств идентификации товаров");
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(DiscrepancyData.Quantity) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Не указано количество (масса)");
	КонецЕсли;
	
	ПроверитьФорматПоля(СписокОшибок, DiscrepancyData.Explanations, "Пояснения к выявленному расхождению (причины отказа в приемке)", , 1, 255);
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияТорг2_НомераСредствИдентификацииТоваров(ItemIdentificationNumbers)
	
	СписокОшибок = Новый Массив;
	
	Для Каждого ItemIdentificationNumber Из ItemIdentificationNumbers.ItemIdentificationNumber Цикл
		
		Для Каждого Unit Из ItemIdentificationNumber.Unit Цикл
			ПроверитьФорматПоля(СписокОшибок, Unit, "Контрольный идентификационный знак", , 1, 255);
		КонецЦикла;
		
		Для Каждого PackageId Из ItemIdentificationNumber.PackageId Цикл
			ПроверитьФорматПоля(СписокОшибок, PackageId, "Уникальный идентификатор вторичной (потребительской)/третичной (заводской, транспортной) упаковки", , 1, 255);
		КонецЦикла;
		
		ПроверитьФорматПоля(СписокОшибок, ItemIdentificationNumber.TransPackageId, "Уникальный идентификатор транспортной упаковки", , 1, 255);
		
	КонецЦикла;
	
	Возврат СписокОшибок;
	
КонецФункции

// ЧАСТНЫЕ ПРОВЕРКИ - ИНН, АДРЕС, ...

Функция ПроверитьФИО(ФИО)
	
	СписокОшибок = Новый Массив;
	
	СтруктураФИО = XML_ПротоСтруктураФИО(ФИО);
	ПроверитьФорматПоля(СписокОшибок, СтруктураФИО.Surname, "Фамилия", Истина, 1, 60, , "ж");
	ПроверитьФорматПоля(СписокОшибок, СтруктураФИО.Firstname, "Имя", Истина, 1, 60, , "с");
	Если ЗначениеЗаполнено(СтруктураФИО.Patronymic) Тогда
		ПроверитьФорматПоля(СписокОшибок, СтруктураФИО.Patronymic, "Отчество", Истина, 1, 60, , "с");
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Процедура ПроверитьСвидетельствоОРегистрацииИП(СписокОшибок, Свидетельство)
	
	Если НЕ ЗначениеЗаполнено(Свидетельство) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, 
			"Не указаны данные о государственной регистрации продавца-индивидуального предпринимателя
			|(номер и дата свидетельства о госрегистрации)");
	Иначе
		Если СтрДлина(Свидетельство) > 128 Тогда
			ДобавитьОшибкуВалидации(СписокОшибок, "Реквизиты свидетельства о регистрации должны иметь длину менее 128 символов");
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры	

Функция ПроверитьСтруктуруКонтрагентаДляТОРГ12(OrganizationInfo, ТипСтруктуры, ПроверкаДляСФ = Ложь)
	// ТипСтруктуры =1 : юридический адрес. Проверяем Seller и Buyer.
	// ТипСтруктуры =2 : фактический адрес. Проверяем Shipper и Consignee.
	
	списокОшибок = Новый Массив;
	
	// проверяем наименование
	Если НЕ ЗначениеЗаполнено(OrganizationInfo.Name) Тогда 
		ДобавитьОшибкуВалидации(СписокОшибок, "не указано название организации");
	ИначеЕсли (OrganizationInfo.IsSoleProprietor = Истина) И (найти(OrganizationInfo.Name, " ") = 0) Тогда 
		ДобавитьОшибкуВалидации(СписокОшибок, "не указано имя индивидуального предпринимателя");
	Иначе 
		ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Name, "Наименование контрагента", Ложь, 0, 1000, Ложь, "с");
	КонецЕсли; 
	
	Если Не (ПроверкаДляСФ И (ТипСтруктуры = 2)) Тогда 
		// проверяем ИНН
		Если ЗначениеЗаполнено(OrganizationInfo.Inn) Тогда 
			ТекстОшибки = "";
			ИНН = OrganizationInfo.Inn;
			Если OrganizationInfo.IsSoleProprietor = Ложь Тогда 
				ТекстОшибки = ПроверитьИННКонтрагента(ИНН, "ЮрЛицо");
			Иначе
				// ФЛ
				ТекстОшибки = ПроверитьИННКонтрагента(ИНН, "ФизЛицо");
			КонецЕсли;
			
			ДобавитьОшибкуВалидации(СписокОшибок, ТекстОшибки);
			
		ИначеЕсли ТипСтруктуры = 1 Тогда 
			ДобавитьОшибкуВалидации(СписокОшибок, "ИНН контрагента не указан");
			
		КонецЕсли;	
		
		// проверяем КПП
		Если ЗначениеЗаполнено(OrganizationInfo.Kpp) Тогда 
			ТекстОшибки = ПроверитьКППКонтрагента(OrganizationInfo.Kpp);
			ДобавитьОшибкуВалидации(СписокОшибок, ТекстОшибки);
		ИначеЕсли OrganizationInfo.IsSoleProprietor = Ложь
			И ПроверкаДляСФ Тогда
			ДобавитьОшибкуВалидации(СписокОшибок, "КПП контрагента не указан");
		КонецЕсли;
	КонецЕсли;
	
	ОшибкиАдреса = ПроверитьСтруктуруАдреса(OrganizationInfo.Address);

	Если ЗначениеЗаполнено(ОшибкиАдреса) Тогда 
		ДобавитьОшибкуВалидацииСОформлением(СписокОшибок, "Ошибки в " + ?(ТипСтруктуры = 1, "юридическом", "фактическом") + " адресе:", , "b");
		ДополнитьМассивОшибокВалидации(СписокОшибок, ОшибкиАдреса);
	КонецЕсли; 
	
	Если ПроверкаДляСФ = Ложь Тогда
		// проверяем ОКОПФ
		ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Okopf, "ОКОПФ контрагента", Ложь, 2, 5, Истина, "м");
		
		// проверяем ОКПО
		Если ЗначениеЗаполнено(OrganizationInfo.Okpo) Тогда 
			ОКПО = OrganizationInfo.Okpo;
			Если стрДлина(ОКПО) <> 8 и стрДлина(ОКПО) <> 10 Тогда 
				ДобавитьОшибкуВалидации(СписокОшибок, "ОКПО контрагента (" + ОКПО + ") должен иметь длину 8 или 10 цифр");
			ИначеЕсли СтрокаСодержитТолькоЦифры(ОКПО) = Ложь Тогда
				ДобавитьОшибкуВалидации(СписокОшибок, "ОКПО контрагента (" + ОКПО + ") должен состоять из цифр");
			КонецЕсли;
		КонецЕсли;
		
		// проверяем Телефон
		Если ЗначениеЗаполнено(OrganizationInfo.Phone) Тогда 
			ОшибкаТелефона = ОшибкаФорматаПоля(OrganizationInfo.Phone, "Номер телефона контрагента", Ложь, 0, 20, Ложь, "м");
			Если ЗначениеЗаполнено(ОшибкаТелефона) Тогда
				ДобавитьОшибкуВалидации(СписокОшибок,  ОшибкаТелефона);
				ДобавитьОшибкуВалидации(СписокОшибок, "Для передачи через ЭДО части строки поставьте разделитель «;»");
			КонецЕсли;
		КонецЕсли;
		
		// проверяем факс
		Если ЗначениеЗаполнено(OrganizationInfo.Fax) Тогда 
			ОшибкиФакса = ОшибкаФорматаПоля(OrganizationInfo.Fax, "Номер факса контрагента", Ложь, 0, 20, Ложь, "м");
			Если ЗначениеЗаполнено(ОшибкиФакса) Тогда 
				ДополнитьМассивОшибокВалидации(СписокОшибок,  ОшибкиФакса);
				ДобавитьОшибкуВалидации(СписокОшибок, "Для передачи через ЭДО части строки поставьте разделитель «;»");
			КонецЕсли;
		КонецЕсли;
		
		// проверяем БИК Банка 
		ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.BankId, "БИК банка контрагента", Ложь, 9, 9, Истина, "м");
		
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции	

Функция КонтрольнаяСуммаИННЮЛСовпадает(ИНН)
	КС_Диадок = умножитьВектора(ИНН, "2:4:10:3:5:9:4:6:8:");
	Возврат ((КС_Диадок % 11) % 10) = Число(Сред(ИНН, 10, 1));
КонецФункции

Функция КонтрольнаяСуммаИННФЛСовпадает(ИНН)
	
	КС1_Диадок = УмножитьВектора(ИНН, "7:2:4:10:3:5:9:4:6:8:");
	КС2_Диадок = УмножитьВектора(ИНН, "3:7:2:4:10:3:5:9:4:6:8:");
	
	Возврат (((КС1_Диадок % 11) % 10) = Число(Сред(ИНН, 11, 1))) И (((КС2_Диадок % 11) % 10) = Число(Сред(ИНН, 12, 1)));
	
КонецФункции

Функция ПроверитьСтруктуруАдреса(Address, МаксДлинаАдреса = 255, ПроверятьЗаполненостьАдреса = Истина) Экспорт

	Результат = Новый Массив;
	
	Если НЕ СтруктураЗаполнена(Address) Тогда
		
		Если ПроверятьЗаполненостьАдреса Тогда
			ДобавитьОшибкуВалидации(Результат, "отсутствуют сведения об адресе");
		КонецЕсли;
		
	ИначеЕсли НЕ Address.IsForeign Тогда
		
		// проверяем российский адрес
		
		ПроверитьФорматПоля(Результат, Address.ZipCode, "Индекс", Ложь, 6, 6, Истина, "м");
		
		RegionCode = Address.RegionCode;
		
		Если НЕ ЗначениеЗаполнено(RegionCode) Тогда 
			ДобавитьОшибкуВалидации(Результат, "В адресе не указан регион по классификатору");
		ИначеЕсли RegionCode = "##" Тогда 
			ДобавитьОшибкуВалидации(Результат, "Наименование региона указано не по классификатору");
		ИначеЕсли НЕ СтрДлина(RegionCode) = 2 Тогда 
			ДобавитьОшибкуВалидации(Результат, "Код региона должен иметь длину 2 символа");
		ИначеЕсли НЕ СтрокаСодержитТолькоЦифры(RegionCode) Тогда 
			ДобавитьОшибкуВалидации(Результат, "Код региона должен состоять из цифр");
		КонецЕсли;
		
		ПроверитьФорматПоля(Результат, Address.Territory	, "наименование района",			 Ложь, 0, 50, Ложь, "с");
		ПроверитьФорматПоля(Результат, Address.City			, "наименование города",			 Ложь, 0, 50, Ложь, "с");
		ПроверитьФорматПоля(Результат, Address.Locality		, "наименование населенного пункта", Ложь, 0, 50, Ложь, "с");
		ПроверитьФорматПоля(Результат, Address.Street		, "наименование улицы",				 Ложь, 0, 50, Ложь, "с");
		
		ПроверитьФорматПоля(Результат, Address.Building		, "номер дома", 					 Ложь, 0, 20, Ложь, "м");
		ПроверитьФорматПоля(Результат, Address.Block		, "номер корпуса", 					 Ложь, 0, 20, Ложь, "м");
		ПроверитьФорматПоля(Результат, Address.Apartment	, "номер квартиры", 				 Ложь, 0, 20, Ложь, "м");
		
	Иначе
		
		// проверяем адрес в свободной форме
		
		Country 	= Address.CountryCode;
		AddressName = Address.AddressText;
		
		Если ЗначениеЗаполнено(Country) Тогда
			
			ПроверитьФорматПоля(Результат, Country, "Код страны", Ложь, 3, 3, Истина, "м");
			
			Если ЗначениеЗаполнено(AddressName) Тогда
				ПроверитьФорматПоля(Результат, AddressName, "строковое представление адреса", Ложь, 0, МаксДлинаАдреса, Ложь, "с");
			Иначе
				ДобавитьОшибкуВалидации(Результат, "Не указан адрес внутри страны");
			КонецЕсли;
			
		Иначе 
			ДобавитьОшибкуВалидации(Результат, "В адресе не заполнена страна");
		КонецЕсли;
		
	КонецЕсли;
				
	Возврат Результат;
	
КонецФункции	

Функция УПД970_ПроверитьСтруктуруАдреса(Address, ContentFunction) 

	Результат = УПД970_ОбщаяПроверкаЗаполненияАдреса(Address, ContentFunction);
	
	Если Результат.Количество() = 0 Тогда
		
		ВидАдреса = Перечисление_ВидыАдресов();
		
		Если Address.Type = ВидАдреса.АдресРФ Тогда
			Результат = Валидация_ПроверитьСтруктуруАдресаРФ(Address.AddressRF);
		ИначеЕсли Address.Type = ВидАдреса.АдресГАР Тогда
			Результат = Валидация_ПроверитьСтруктуруАдресаГАР(Address.AddressSAR);
		ИначеЕсли Address.Type = ВидАдреса.АдресСтрокой Тогда
			Результат = Валидация_ПроверитьСтруктуруАдресаСтрокой(Address.AddressText);
		КонецЕсли;
		
	КонецЕсли;
	
	ПроверитьФорматПоля(Результат, Address.GLN, "GLN места", Ложь, 13, 13, Ложь, "м");
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_ОбщаяПроверкаЗаполненияАдреса(AddressInfo, ContentFunction)
	
	Перем Address;
	
	Результат = Новый Массив;
	
	Если ЗначениеЗаполнено(AddressInfo) Тогда
		
		ВидАдреса = Перечисление_ВидыАдресов();
		
		Если AddressInfo.Type = ВидАдреса.АдресРФ Тогда
			Address = AddressInfo.AddressRF;
		ИначеЕсли AddressInfo.Type = ВидАдреса.АдресГАР Тогда
			Address = AddressInfo.AddressSAR;
		ИначеЕсли AddressInfo.Type = ВидАдреса.АдресСтрокой Тогда
			Address = AddressInfo.AddressText;
		КонецЕсли;
		
	КонецЕсли;
	
	Если Address = Неопределено
		Или Не СтруктураЗаполнена(Address) Тогда
		
		ФункцияУПД = XML_УПД_ПолучитьФункцию(ContentFunction);
		ФункцииУПД = Перечисление_ФункцииУПД();
		
		Если ФункцияУПД <> ФункцииУПД.ДОП Тогда
			ДобавитьОшибкуВалидации(Результат, "отсутствуют сведения об адресе");
		КонецЕсли;
		
	Иначе
		ПроверитьФорматПоля(Результат, AddressInfo.Type, "вид адреса", Истина, 0, 11, Ложь, "м");
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_СсылкаНаОрганизацию(СведенияОКонтрагенте)
	
	Перем Результат;
	
	СлужебнаяИнформация = СведенияОКонтрагенте.СлужебнаяИнформация;
	
	Если Тип(СлужебнаяИнформация) = Тип("Структура") Тогда
		СлужебнаяИнформация.Свойство("Ссылка", Результат);
	ИначеЕсли Тип(СлужебнаяИнформация) = Тип("ОбъектXDTO") Тогда
		Результат = ПолучитьЗначениеXDTO(СлужебнаяИнформация, "Ссылка");
	КонецЕсли;

	Возврат Результат;
КонецФункции

Функция ПроверитьИННКонтрагента(ИНН, ВидКонтрагента, ВидУчастникаРодительныйПадеж = "контрагента")
	
	ТекстОшибки = "";
	КонтрольнаяСуммаСовпадает = Ложь;
	
	Если ВидКонтрагента = "ЮрЛицо" Тогда
		
		Если СтрДлина(ИНН) <> 10 Тогда 
			
			ШаблонОшибки = "ИНН %1 (%2) должен иметь длину 10 цифр";
			
			ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
				ВидУчастникаРодительныйПадеж,
				ИНН);
			
		Иначе	
			КонтрольнаяСуммаСовпадает = КонтрольнаяСуммаИННЮЛСовпадает(ИНН);
		КонецЕсли;
		
		
	ИначеЕсли ВидКонтрагента = "ФизЛицо" Тогда
		
		Если СтрДлина(ИНН) <> 12 Тогда
			
			ШаблонОшибки = "ИНН %1-индивидуального предпринимателя (%2) должен иметь длину 12 цифр";
			
			ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
				ВидУчастникаРодительныйПадеж,
				ИНН);
			
		Иначе	
			КонтрольнаяСуммаСовпадает = КонтрольнаяСуммаИННФЛСовпадает(ИНН);
		КонецЕсли;
		
	Иначе
		
		ВызватьИсключение "Некорректное использование метода: ПроверитьИННКонтрагента";
		
	КонецЕсли;	
	
	// Общие проверки для ИП и ЮЛ
	Если Не ЗначениеЗаполнено(ТекстОшибки) Тогда
		
		Если Не СтрокаСодержитТолькоЦифры(ИНН) Тогда
			
			ШаблонОшибки = "ИНН %1 (%2) должен состоять из цифр";
			
			ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
				ВидУчастникаРодительныйПадеж,
				ИНН);
			
		ИначеЕсли Лев(ИНН, 2) = "00" Тогда
			
			ШаблонОшибки = "ИНН %1 (%2) не должен начинаться с двух нулей";
			
			ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
				ВидУчастникаРодительныйПадеж,
				ИНН);
			
		ИначеЕсли Не КонтрольнаяСуммаСовпадает Тогда
			
			ШаблонОшибки = "ИНН %1 (%2) неверный (не совпадает контрольная сумма)";
			
			ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
				ВидУчастникаРодительныйПадеж,
				ИНН);
			
		КонецЕсли;	
			
	КонецЕсли;	
	
	Возврат ТекстОшибки;
	
КонецФункции	

Функция ПроверитьКППКонтрагента(КПП, ВидУчастникаРодительныйПадеж = "контрагента")
	
	ТекстОшибки = "";
	
	Если СтрДлина(КПП) <> 9 Тогда
		
		ШаблонОшибки = "КПП %1 (%2) должен иметь длину 9 цифр";
		
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
			ВидУчастникаРодительныйПадеж,
			КПП);
		
	ИначеЕсли НЕ СтрокаСодержитТолькоЦифры(КПП) Тогда
		
		ШаблонОшибки = "КПП %1 (%2) должен состоять из цифр";
		
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
			ВидУчастникаРодительныйПадеж,
			КПП);
		
	ИначеЕсли Лев(КПП, 2) = "00" Тогда
		
		ШаблонОшибки = "КПП %1 (%2) не должен начинаться с двух нулей";
		
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
			ВидУчастникаРодительныйПадеж,
			КПП);
		
	КонецЕсли;
	
	Возврат ТекстОшибки;
	
КонецФункции	

Функция СтрокаЧтоМенять(НеправильноеНаименование, НеправильныйКод)
	
	Если НеправильноеНаименование И НЕ НеправильныйКод Тогда
		Возврат "наименования";
	ИначеЕсли НЕ НеправильноеНаименование И НеправильныйКод Тогда
		Возврат "коды";
	ИначеЕсли НеправильноеНаименование И НеправильныйКод Тогда
		Возврат "наименования и коды";
	Иначе 
		Возврат "";
	КонецЕсли;	
	
КонецФункции

Функция ПроверитьОшибкиВСтрокеСФ(item)
	
	СписокОшибокСтроки = Новый Массив;
	
	ПроверитьФорматПоля(СписокОшибокСтроки, Item.Product, "Наименование товара", Истина, 0, 1000, Ложь, "с");
	
	Если TaxRate_ЭтоЗначениеИсчНалАг(item.TaxRate) Тогда
		ДобавитьОшибкуВалидации(СписокОшибокСтроки, "Для данного формата документа недопустимо использовать ставку НДС ""НДС исчисляется налоговым агентом""");
	Иначе
		ПроверитьОшибкиВ_UnitCode_TaxRate(СписокОшибокСтроки, item.UnitCode, item.TaxRate);
	КонецЕсли;
				
	Если ЗначениеЗаполнено(Item.Quantity) и (Item.Quantity < 0) Тогда
		ДобавитьОшибкуВалидации(СписокОшибокСтроки, "Количество не может быть отрицательным");
	КонецЕсли;	
	
    Если НЕ ЗначениеЗаполнено(Item.Total) Тогда
		ДобавитьОшибкуВалидации(СписокОшибокСтроки, "Не указана общая сумма по строке");
	КонецЕсли;
	
	Если 	 ЗначениеЗаполнено(Item.CustomsDeclarationNumbers)
		И НЕ ЗначениеЗаполнено(Item.CountriesOfOrigin) Тогда
		
		ДобавитьОшибкуВалидации(СписокОшибокСтроки, "Для номенклатуры " + """" + Item.Product + """" + " не заполнена страна происхождения товара");
		
	ИначеЕсли НЕ ЗначениеЗаполнено(Item.CustomsDeclarationNumbers)
			И ЗначениеЗаполнено(Item.CountriesOfOrigin)
			И НЕ СокрЛП(Item.CountriesOfOrigin) = "643"
			И НЕ СокрЛП(Item.CountriesOfOrigin) = "981" Тогда
			
		ДополнитьМассивОшибокВалидации(СписокОшибокСтроки, ТекстОшибкиНеЗаполненГТД(Item));
			
	КонецЕсли;
				
	Если СтрДлина(СокрЛП(Item.CustomsDeclarationNumbers)) > 29 Тогда 
		ДобавитьОшибкуВалидации(СписокОшибокСтроки, "Длина номера ГТД превышает максимальное ограничение в 29 символов");
	КонецЕсли;		
	
	Возврат СписокОшибокСтроки;
	
КонецФункции 

Функция ПроверитьКлассификаторыСФ(InvoiceContent)
	
	Результат = Новый Массив;
	
	КодыСтранДляПроверки = Новый Соответствие;
	КодыЕдиницДляПроверки = Новый Соответствие;
	
	Для каждого item Из InvoiceContent.Items Цикл
		
		ЕдиницаИзмеренияСсылка = СвойствоСлужебнойИнформации(Item, "ЕдиницаИзмерения");
		Если ЗначениеЗаполнено(Item.UnitCode) Тогда
			КодыЕдиницДляПроверки.Вставить(ЕдиницаИзмеренияСсылка, СокрЛП(Item.UnitCode));
		КонецЕсли;
		
		СтранаПроисхожденияСсылка = СвойствоСлужебнойИнформации(Item, "СтранаПроисхождения");
		
		Если ЗначениеЗаполнено(Item.CountriesOfOrigin) Тогда 
			КодыСтранДляПроверки.Вставить(СтранаПроисхожденияСсылка, Item.CountriesOfOrigin);
		КонецЕсли;
		
	КонецЦикла;
	
	ДобавитьГруппуОшибокВалидации(Результат, ПроверитьВалюту(InvoiceContent.ВалютаСсылка, InvoiceContent.Currency),	"Ошибки в классификаторе валют");
	ДобавитьГруппуОшибокВалидации(Результат, ПроверитьСтраны(КодыСтранДляПроверки),									"Ошибки в классификаторе стран");
	ДобавитьГруппуОшибокВалидации(Результат, ПроверитьЕдиницыИзмерения(КодыЕдиницДляПроверки),						"Ошибки в классификаторе единиц измерения");
	
	Возврат Результат;
	
КонецФункции

Процедура ПроверитьОшибкиВ_UnitCode_TaxRate(СписокОшибок, UnitCode, TaxRate)
	
	ПроверитьФорматПоля(СписокОшибок, UnitCode, "Код единицы измерения", Ложь, 3, 4, Истина, "м");
	
	Если TaxRate = "##"	Тогда 
		ДобавитьОшибкуВалидации(СписокОшибок, "Указана недопустимая ставка налога");
	ИначеЕсли Не ЗначениеЗаполнено(TaxRate) Тогда 
		ДобавитьОшибкуВалидации(СписокОшибок, "Ставка НДС не указана");
	КонецЕсли;
	
КонецПроцедуры

Функция КодЕдиницыИзмеренияСоответствуетКлассификатору(ЕдиницаИзмеренияСсылка, КодЕдиницы) Экспорт
	
	// Предыстория и аналитика: DD1C-84
	
	// Замена функции ПроверитьЕИ в основном модуле.
	Результат = Ложь;
	
	Если Не ЗначениеЗаполнено(ЕдиницаИзмеренияСсылка) Тогда
		Результат = Истина;
		Возврат Результат;
	КонецЕсли;
	
	ПодходящиеЕИ = ПодходящиеЕдиницыИзмерения(ЕдиницаИзмеренияСсылка.Наименование);
	
	Для Каждого СтрокаОКЕИ Из ПодходящиеЕИ Цикл
		Если СтрокаОКЕИ.Код = КодЕдиницы Тогда
			Результат = Истина;
			Прервать;
		КонецЕсли;
	КонецЦикла;	
	
	Возврат Результат;
	
КонецФункции	

Функция ПроверитьЕдиницыИзмерения(СоответствиеЕдиниц)
	
	Результат = Новый Массив;
	
	ОшибкаЕИНеНайдена = Ложь;
	ОшибкаЕИНеТотКод = Ложь;
	ГиперссылкаОКЕИ = "<a href=""Открыть_ОКЕИ"" title=""Общероссийский классификатор единиц измерения"">ОКЕИ</a>";
	
	ТаблицаЕИ = Соответствие_В_СортированнуюТЗ(СоответствиеЕдиниц, "Значение");
	
	Для каждого СтрокаТЗ Из ТаблицаЕИ Цикл
	
		ЕИ = СтрокаТЗ.Ключ;
		ЕИПредставление = Строка(ЕИ);
		КодЕИ = СтрокаТЗ.Значение;
		
		Если Не ЗначениеЗаполнено(ЕИ) Тогда
			Продолжить;
		КонецЕсли;
		
		ПодходящиеЕИ = ПодходящиеЕдиницыИзмерения(ЕИ.Наименование);
		
		ФлагВсеХорошо = Ложь;
		
		Для Каждого СтрокаОКЕИ Из ПодходящиеЕИ Цикл
			Если СтрокаОКЕИ.Код = КодЕИ Тогда
				ФлагВсеХорошо = Истина;  // ЕИ найдена, код совпал с классификатором, все хорошо
				Прервать;
			КонецЕсли;
		КонецЦикла;
		
		Если ФлагВсеХорошо Тогда
			Продолжить;
		КонецЕсли;	
		
		Если ПодходящиеЕИ.Количество() = 0 Тогда
			
			ОшибкаЕИНеНайдена = Истина;
			ДобавитьОшибкуВалидации(Результат,
					"Единица измерения «<" + ЕИПредставление + ">» не найдена в " + ГиперссылкаОКЕИ, ЕИ);
			
		ИначеЕсли ПодходящиеЕИ.Количество() = 1 Тогда
			
			ОшибкаЕИНеТотКод = Истина;
			ДобавитьОшибкуВалидации(Результат,
				"Код единицы измерения «<" + ЕИПредставление + ">» - " + КодЕИ
				+ " не соответствует коду по " + ГиперссылкаОКЕИ + " - " + ПодходящиеЕИ[0].код, ЕИ);
			
		Иначе 
			
			ОшибкаЕИНеТотКод = Истина;
			ДобавитьОшибкуВалидации(Результат, 
				"Код единицы измерения «<" + ЕИПредставление + ">» - " + КодЕИ
				+ " не соответствует коду по " + ГиперссылкаОКЕИ, ЕИ);
				
			Для Каждого СтрокаОКЕИ Из ПодходящиеЕИ Цикл
				
				ДобавитьОшибкуВалидации(Результат, 
					"Единице измерения «" + СтрокаОКЕИ.НаименованиеПолное + "», ("
					+ СтрокаОКЕИ.НаименованиеКраткое + ") соответствует код ОКЕИ " + СтрокаОКЕИ.код);
					
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЦикла;
		
	Если ЗначениеЗаполнено(Результат) Тогда

		ДобавитьОшибкуВалидации(Результат, 
			"Перейдите в <справочник единиц измерения> и приведите "
			+ строкаЧтоМенять(ОшибкаЕИНеНайдена, ОшибкаЕИНеТотКод)
			+ " единиц в соответствие с классификатором", "СПРАВОЧНИКЕДИНИЦ");

	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции	

Функция ПроверитьЕдиницыИзмеренияПоОКЕИ(СоответствиеЕдиниц)
	
	Результат = Новый Массив;
	
	ОКЕИ = ПолучитьОКЕИ();
	
	ГиперссылкаОКЕИ	 = "<a href=""Открыть_ОКЕИ"" title=""Общероссийский классификатор единиц измерения"">ОКЕИ</a>";
	ШаблонОшибки	 = "Единица измерения <%1> не найдена в %2";
	
	ТаблицаЕИ = Соответствие_В_СортированнуюТЗ(СоответствиеЕдиниц, "Значение");
	
	Для каждого СтрокаТЗ Из ТаблицаЕИ Цикл
		
		ЕИ = СтрокаТЗ.Ключ;
		ЕИПредставление = Строка(ЕИ);
		КодЕИ = СтрокаТЗ.Значение;
		
		Если Не ЗначениеЗаполнено(ЕИ) Тогда
			Продолжить;
		КонецЕсли;
		
		Если ОКЕИ.Найти(КодЕИ, "Код") = Неопределено Тогда
			ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки, ЕИПредставление, ГиперссылкаОКЕИ);
			ДобавитьОшибкуВалидации(Результат, ТекстОшибки, ЕИ);
		КонецЕсли;
		
	КонецЦикла;
	
	Если ЗначениеЗаполнено(Результат) Тогда
		ТекстОшибки = "Перейдите в <справочник единиц измерения> и приведите коды единиц в соответствие с классификатором";
		ДобавитьОшибкуВалидации(Результат, ТекстОшибки, "СПРАВОЧНИКЕДИНИЦ");
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ПроверитьСтраны(СоответствиеКодов)
	
	Результат = Новый Массив;
	
	ОшибкаСтраныНеНайдена = Ложь;
	ОшибкаСтраныНеТотКод = Ложь;
	
	ГиперссылкаОКСМ = "<a href=""Открыть_ОКСМ"" title=""Общероссийский классификатор стран мира"">ОКСМ</a>";
		
	СортированноеСоответствие = Соответствие_В_СортированнуюТЗ(СоответствиеКодов, "Значение");
	
	Для каждого КлючИЗначение Из СортированноеСоответствие Цикл
	
		Страна = КлючИЗначение.Ключ;  // здесь ссылка на элемент справочника
		СтранаПредставление = Строка(Страна);
		КодСтраны = КлючИЗначение.Значение;
		
		Если Не ЗначениеЗаполнено(Страна) Тогда
			Продолжить;
		КонецЕсли;
		
		ПодходящиеСтраны = ПодходящиеСтраны(СтранаПредставление);
		
		ФлагВсеХорошо = Ложь;
		Для Каждого СтрокаОКСМ Из ПодходящиеСтраны Цикл
			Если СтрокаОКСМ.Код = КодСтраны Тогда
				ФлагВсеХорошо = Истина;
				Прервать;  // код по ОКСМ верный, и эту страну надо будет пропустить
			КонецЕсли;
		КонецЦикла;
		
		Если ФлагВсеХорошо Тогда
			Продолжить;
		КонецЕсли;	
		
		// что-то плохо, коды не совпадают.
		Если ПодходящиеСтраны.Количество() = 0 Тогда
			
			ОшибкаСтраныНеНайдена = Истина;
			ДобавитьОшибкуВалидации(Результат, 
				"Страна происхождения «<" + СтранаПредставление + ">» не найдена в " + ГиперссылкаОКСМ, Страна);
			
		ИначеЕсли ПодходящиеСтраны.Количество() = 1 Тогда
			
			ОшибкаСтраныНеТотКод = Истина;
			ДобавитьОшибкуВалидации(Результат, 
				"Код страны происхождения «<" + СтранаПредставление + ">» - " + КодСтраны
				+ " не соответствует коду по " + ГиперссылкаОКСМ + " - " + ПодходящиеСтраны[0].Код, Страна);
			
		Иначе 
			
			ОшибкаСтраныНеТотКод = Истина;
			
			ДобавитьОшибкуВалидации(Результат, 
				"Код страны происхождения «<" + СтранаПредставление + ">» - " + КодСтраны
				+ " не соответствует коду по " + ГиперссылкаОКСМ, Страна);
			
			Для каждого СтрокаОКСМ Из ПодходящиеСтраны Цикл 
				ДобавитьОшибкуВалидации(Результат, 
					"Стране «" + СтрокаОКСМ.НаименованиеКраткое + "» соответствует код ОКСМ " + СтрокаОКСМ.Код);
			КонецЦикла;
			
		КонецЕсли;
		
	КонецЦикла;
	
	Если ЗначениеЗаполнено(Результат) Тогда
		ДобавитьОшибкуВалидации(Результат, 
			"Перейдите в <справочник стран мира> и приведите " + СтрокаЧтоМенять(ОшибкаСтраныНеНайдена, ОшибкаСтраныНетотКод) + " стран в соответствие с классификатором", "СПРАВОЧНИКСТРАН");
	КонецЕсли;
		
	Возврат Результат;
	
КонецФункции	

Функция ПроверитьВалюту(валюта, ВалютаКод)
	
	Результат = Новый Массив;
	
	Если Валюта = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	ОшибкаВалютыНеНайдена = Ложь;
	ОшибкаВалютыНеТотКод = Ложь;
	
	НаименованиеВалюты = Новый Массив;
	НаименованиеВалюты.Добавить(Валюта.Наименование);
	НаименованиеВалюты.Добавить(Валюта.НаименованиеПолное);
	ПодходящаяВалюта = ПодходящаяВалюта(НаименованиеВалюты);
	
	Для Каждого СтрокаОКВ Из ПодходящаяВалюта Цикл
		Если СтрокаОКВ.Код = ВалютаКод Тогда 
			Возврат Результат;
		КонецЕсли;
	КонецЦикла;
	
	ТекстГиперссылкиОКВ = "<a href=""Открыть_ОКВ"" title=""Общероссийский классификатор валют"">ОКВ</a>";
	
	Если ПодходящаяВалюта.Количество() = 0 Тогда
		
		ОшибкаВалютыНеНайдена = Истина;
		ДобавитьОшибкуВалидации(Результат, "Валюта «<" + Строка(Валюта) + ">» не найдена в " + ТекстГиперссылкиОКВ, валюта);
		
	ИначеЕсли ПодходящаяВалюта.Количество() = 1 Тогда
		
		ОшибкаВалютыНеТотКод = Истина;
		ДобавитьОшибкуВалидации(Результат, "Код валюты «<" + Строка(Валюта) + ">» - " + ВалютаКод + " не соответствует коду по " + ТекстГиперссылкиОКВ + " - " + ПодходящаяВалюта[0].Код, валюта);
		
	Иначе
		
		ОшибкаВалютыНеТотКод = Истина;
		ДобавитьОшибкуВалидации(Результат, "Код валюты «<" + Строка(Валюта) + ">» - " + ВалютаКод + " не соответствует коду по " + ТекстГиперссылкиОКВ, валюта);
		
		Для Каждого СтрокаОКВ Из ПодходящаяВалюта Цикл 
			ДобавитьОшибкуВалидации(Результат, "Валюте «" + СтрокаОКВ.Наименование + "», (" + СтрокаОКВ.КодСимвольный + ") соответствует код ОКВ " + СтрокаОКВ.Код);
		КонецЦикла;	
		
	КонецЕсли;
	
	ДобавитьОшибкуВалидации(Результат, 
		"Перейдите в <справочник валют> и приведите " + СтрокаЧтоМенять(ОшибкаВалютыНеНайдена, ОшибкаВалютыНеТотКод) + " валют в соответствие с классификатором", "СПРАВОЧНИКВАЛЮТ");
	
	Возврат Результат;
	
КонецФункции		

// Используется в алгоритмах, где на вход подается Соответствие, но для обработки его надо осортировать.
Функция Соответствие_В_СортированнуюТЗ(Соответствие, ПолеСортировки = "Ключ")
	
	Результат = Новый ТаблицаЗначений;
	Результат.Колонки.Добавить("Ключ");
	Результат.Колонки.Добавить("Значение");
	
	Для каждого КлючИЗначение Из Соответствие Цикл
		
		НовСтрока = Результат.Добавить();
		НовСтрока.Ключ		= КлючИЗначение.Ключ;
		НовСтрока.Значение	= КлючИЗначение.Значение;
	
	КонецЦикла;
	
	Результат.Сортировать(ПолеСортировки);
	
	Возврат Результат;
	
КонецФункции	

Функция КлассификаторВалютПолучитьНаименованиеПоКоду(Код) Экспорт
	Если СокрЛП(Строка(Код)) = "643" Тогда
		Возврат "RUB";
	ИначеЕсли СокрЛП(Строка(Код)) = "978" Тогда
		Возврат "EUR";
	Иначе
		Возврат "";
	КонецЕсли;
КонецФункции

Функция ТекстОшибкиНеЗаполненГТД(Item, НачалоОшибки = "")

	Результат = Новый Массив;
	
	НомерГТДСсылка = СвойствоСлужебнойИнформации(Item, "НомерГТД");
	Если ЗначениеЗаполнено(НомерГТДСсылка) Тогда
		ДобавитьОшибкуВалидации(Результат, НачалоОшибки + "Для номенклатуры """ + Item.Product + """" + " не заполнен <регистрационный номер ГТД>", НомерГТДСсылка);
	Иначе
		ДобавитьОшибкуВалидации(Результат, НачалоОшибки + "Для номенклатуры " + """" + Item.Product + """" + " не указан номер ГТД");
	КонецЕсли;
	
	Возврат Результат;		

КонецФункции

Функция ПроверитьОшибкиВСтрокеКСФ(Item, ЭтоУКД = Истина)
	
	СписокОшибокСтроки = Новый Массив;
	ПроверитьФорматПоля(СписокОшибокСтроки, Item.Product, "Наименование товара", Истина, 0, 1000, Ложь, "с");
	
	Если Item.AmountsDec.Total = 0 И Item.AmountsInc.Total = 0 Тогда
		ДобавитьОшибкуВалидации(СписокОшибокСтроки, "Нет изменения стоимости товаров (работ, услуг), имущественных прав с налогом - всего");
	КонецЕсли;
	
	ПроверитьОшибкиВ_UnitCode_TaxRate(СписокОшибокСтроки, Item.OriginalValues.UnitCode, Item.OriginalValues.TaxRate);
	ПроверитьОшибкиВ_UnitCode_TaxRate(СписокОшибокСтроки, Item.CorrectedValues.UnitCode, Item.CorrectedValues.TaxRate);
	
	Если НЕ ЭтоУКД Тогда
		
		Если TaxRate_ЭтоЗначениеИсчНалАг(Item.OriginalValues.TaxRate)
			Или TaxRate_ЭтоЗначениеИсчНалАг(Item.CorrectedValues.TaxRate) Тогда
			ДобавитьОшибкуВалидации(СписокОшибокСтроки, "Для данного формата документа недопустимо использовать ставку НДС ""НДС исчисляется налоговым агентом""");
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат СписокОшибокСтроки;
	
КонецФункции
	
Функция ПроверитьКлассификаторыКСФ(InvoiceCorrectionContent, Items)
	
	Результат = Новый Массив;
	
	КодыЕдиницДляПроверки = КодыЕдиницИзмеренияДляПроверкиКСФ_УКД(Items);
	
	ДобавитьГруппуОшибокВалидации(Результат, ПроверитьВалюту(InvoiceCorrectionContent.ВалютаСсылка, InvoiceCorrectionContent.Currency),	"Ошибки в классификаторе валют");
	ДобавитьГруппуОшибокВалидации(Результат, ПроверитьЕдиницыИзмерения(КодыЕдиницДляПроверки),											"Ошибки в классификаторе единиц измерения");
	
	Возврат Результат;
	
КонецФункции

Функция ПроверитьКлассификаторыУКД(InvoiceCorrectionContent, Items)
	
	Результат = Новый Массив;
	
	КодыЕдиницДляПроверки = КодыЕдиницИзмеренияДляПроверкиКСФ_УКД(Items);
	
	ДобавитьГруппуОшибокВалидации(Результат, ВалидацияUCD736_ПроверитьВалюту(InvoiceCorrectionContent.ВалютаСсылка, InvoiceCorrectionContent.Currency),	"Ошибки в классификаторе валют");
	ДобавитьГруппуОшибокВалидации(Результат, ПроверитьЕдиницыИзмеренияПоОКЕИ(КодыЕдиницДляПроверки),									"Ошибки в классификаторе единиц измерения");
	
	Возврат Результат;
	
КонецФункции

Функция КодыЕдиницИзмеренияДляПроверкиКСФ_УКД(Items)
	
	Результат = Новый Соответствие;
	
	Для каждого item Из Items Цикл
		
		ЕдиницаИзмеренияДоИзмененияСсылка = СвойствоСлужебнойИнформации(Item, "ЕдиницаИзмеренияДоИзменения");
		Если ЗначениеЗаполнено(Item.OriginalValues.UnitCode) И Item.OriginalValues.UnitCode <> "0000" Тогда 
			Результат.Вставить(ЕдиницаИзмеренияДоИзмененияСсылка, СокрЛП(Item.OriginalValues.UnitCode));
		КонецЕсли;
		
		ЕдиницаИзмеренияСсылка = СвойствоСлужебнойИнформации(Item, "ЕдиницаИзмерения");
		Если ЗначениеЗаполнено(Item.CorrectedValues.UnitCode) И Item.CorrectedValues.UnitCode <> "0000" Тогда 
			Результат.Вставить(ЕдиницаИзмеренияСсылка, СокрЛП(Item.CorrectedValues.UnitCode));
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура ПроверитьОшибкиВШапкеУПД_УКД(СписокОшибок, Content, ФорматПриказа = "")
	
	ПроверитьФорматПоля(СписокОшибок, Content.Number, "Номер счета-фактуры", Истина, 0, 1000, Ложь, "м");
	ПроверитьФорматДаты(СписокОшибок, Content.Date, "Дата счета-фактуры", Истина);
	
	Если ЗначениеЗаполнено(Content.RevisionDate) или  значениеЗаполнено(Content.RevisionNumber) Тогда 
		ПроверитьФорматПоля(СписокОшибок, Content.RevisionNumber, "Номер исправления счета-фактуры", Ложь, 1, 3, Истина, "м");
		ПроверитьФорматДаты(СписокОшибок, Content.RevisionDate, "Дата исправления счета-фактуры");
	КонецЕсли;
	
	ПроверитьФорматПоля(СписокОшибок, Content.Currency, "Код валюты счета-фактуры", Истина, 3, 3, Истина, "м");
	
	МаксДлинаАдреса = 255;
	
	// проверяем сведения об участниках сделки
	Если  СтруктураЗаполнена(Content.Seller) Тогда
		ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляУПД(Content.Seller, 1, Content.Function, МаксДлинаАдреса, ФорматПриказа);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <продавце>", Content.Seller.Ссылка);
	Иначе 
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют данные о продавце");
	КонецЕсли;

	Если  СтруктураЗаполнена(Content.Buyer) Тогда
		ОшибкиКонтрагента = ПроверитьСтруктуруКонтрагентаДляУПД(Content.Buyer, 1, Content.Function, МаксДлинаАдреса, ФорматПриказа);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиКонтрагента, "Ошибки в данных о <покупателе>", Content.Buyer.Ссылка);
	Иначе 
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют данные о покупателе");
	КонецЕсли;
	
КонецПроцедуры

// Проверяет сведения об организации на соответствие требованиям приказа 970@
//
// Параметры:
//  OrganizationInfo - Структура - см. тип ExtendedOrganizationInfo970 в схеме Proto.xsd
// 
// Возвращаемое значение:
//  Массив из НовыйОшибкаВалидации.
//
Функция ВалидацияUTD970_УчастникТип(OrganizationInfo)
	
	СписокОшибок = Новый Массив;
	
	// BSLLS:MagicNumber-off
	
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Name, "Наименование контрагента", Истина, 0, 1000, Ложь);
	
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Okpo, "ОКПО контрагента", Ложь, 1, 10, Ложь);
	
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Okopf, "ОКОПФ контрагента", Ложь, 2, 5, Ложь);
	
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Department, "Структурное подразделение", Ложь, 1, 500, Ложь);
	
	ПроверитьФорматПоля(СписокОшибок,
		OrganizationInfo.FullNameOPF,
		"Полное наименование ОПФ",
		Ложь,
		4,
		255,
		Ложь
	);
	
	ПроверитьФорматПоля(СписокОшибок,
		OrganizationInfo.AdditionalInfo,
		"Информация для участника документооборота",
		Ложь,
		1,
		255,
		Ложь
	);
	
	ВидыЮрФизЛиц = Перечисление_ВидыЮрФизЛиц();
	
	ВидКонтрагента = OrganizationInfo.Type;
	
	Если ВидКонтрагента = ВидыЮрФизЛиц.ЮридическоеЛицо Тогда
		
		Ошибки = ПроверитьИННКонтрагента(OrganizationInfo.Inn, "ЮрЛицо");
		ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
		
		Если ЗначениеЗаполнено(OrganizationInfo.Kpp) Тогда
			Ошибки = ПроверитьКППКонтрагента(OrganizationInfo.Kpp);
			ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
		КонецЕсли;
		
	ИначеЕсли ВидКонтрагента = ВидыЮрФизЛиц.ИндивидуальныйПредприниматель Тогда
		
		Ошибки = ПроверитьИННКонтрагента(OrganizationInfo.Inn, "ФизЛицо");
		ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
		
		Если ЗначениеЗаполнено(OrganizationInfo.Kpp) Тогда
			ДобавитьОшибкуВалидации(СписокОшибок, "Для физ. лиц КПП не указывается");
		КонецЕсли;
		
		ПроверитьФорматПоля(СписокОшибок,
			OrganizationInfo.IndividualEntityRegistrationCertificate,
			"Свидетельство о гос. регистрации ИП",
			Ложь,
			1,
			100,
			Ложь
		);
		
		ПроверитьФорматПоля(СписокОшибок, 
			OrganizationInfo.IndividualEntityRegistrationNumber,
			"ОГРНИП",
			Ложь,
			15,
			15,
			Истина
		);
		
		ПроверитьФорматПоля(СписокОшибок,
			OrganizationInfo.OrganizationOrPersonInfo,
			"иные сведения",
			Ложь,
			1,
			255,
			Ложь
		);
		
		ЧастиФИО = РазложитьСтрокуВМассивСлов(OrganizationInfo.Name, " ");
		Если ЧастиФИО.Количество() < 2 Тогда
			ДобавитьОшибкуВалидации(СписокОшибок, "не указано имя индивидуального предпринимателя");
		КонецЕсли;
		
	ИначеЕсли ВидКонтрагента = ВидыЮрФизЛиц.ФизическоеЛицо Тогда
		
		Если НЕ ЗначениеЗаполнено(OrganizationInfo.OrganizationOrPersonInfo)
			И НЕ ЗначениеЗаполнено(OrganizationInfo.Name) Тогда
			
			Ошибки = ПроверитьИННКонтрагента(OrganizationInfo.Inn, "ФизЛицо");
			ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
			
		КонецЕсли;
		
		Если ЗначениеЗаполнено(OrganizationInfo.Kpp) Тогда
			ДобавитьОшибкуВалидации(СписокОшибок, "Для физ. лиц КПП не указывается");
		КонецЕсли;
		
		Если ЗначениеЗаполнено(OrganizationInfo.PhysicalEntityStatusID) Тогда
			
			Если OrganizationInfo.PhysicalEntityStatusID <> "0" И OrganizationInfo.PhysicalEntityStatusID <> "1" Тогда
				
				ДобавитьОшибкуВалидации(
					СписокОшибок,
					"указано не валидное значение в статусе контрагента (допустимо ""0"" или ""1"")"
				);
				
			КонецЕсли;
			
		КонецЕсли;
		
		ПроверитьФорматПоля(СписокОшибок,
			OrganizationInfo.OrganizationOrPersonInfo,
			"иные сведения",
			Ложь,
			1,
			255,
			Ложь
		);
		
		ЧастиФИО = РазложитьСтрокуВМассивСлов(OrganizationInfo.Name, " ");
		
		Если ЧастиФИО.Количество() < 2 Тогда
			ДобавитьОшибкуВалидации(СписокОшибок, "не указано имя физического лица");
		КонецЕсли;
		
	ИначеЕсли ВидКонтрагента = ВидыЮрФизЛиц.ИностраннаяОрганизация
		Или ВидКонтрагента = ВидыЮрФизЛиц.ИностранныйГражданин Тогда
		
		ПроверитьФорматПоля(
			СписокОшибок,
			OrganizationInfo.CountryCode,
			"код страны",
			Истина,
			3,
			3,
			Истина
		);
		
		ПроверитьФорматПоля(
			СписокОшибок,
			OrganizationInfo.Country,
			"наименование страны",
			Истина,
			1,
			255,
			Ложь
		);
		
		Если Не ЗначениеЗаполнено(OrganizationInfo.LegalEntityId) И 
			Не ЗначениеЗаполнено(OrganizationInfo.OrganizationOrPersonInfo) Тогда
			
			ДобавитьОшибкуВалидации(СписокОшибок, "Должно быть заполнено хотя бы одно поле для идентификации
			|иностранной организации (иностранного гражданина): налоговый номер или регистрационный номер");
			
		КонецЕсли;
		
		ПроверитьФорматПоля(
			СписокОшибок,
			OrganizationInfo.LegalEntityId,
			"идентификатор иностранной организации",
			Ложь,
			1,
			255,
			Ложь
		);
		
		ПроверитьФорматПоля(
			СписокОшибок,
			OrganizationInfo.OrganizationOrPersonInfo,
			"иные сведения для однозначной идентификации иностранной организации",
			Ложь,
			1,
			255,
			Ложь
		);
				
	Иначе
		
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(
			"Неправильно указан тип контрагента (%1)",
			ВидКонтрагента
		);
		ДобавитьОшибкуВалидации(СписокОшибок, ТекстОшибки);
		
	КонецЕсли;
	
	// BSLLS:MagicNumber-on
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ПроверитьСтруктуруКонтрагентаДляУПД(OrganizationInfo, ТипСтруктуры, ContentFunction, МаксДлинаАдреса, ФорматПриказа = "")
	// ТипСтруктуры =1 : юридический адрес. Проверяем Seller и Buyer.
	// ТипСтруктуры =2 : фактический адрес. Проверяем Shipper и Consignee.
	
	списокОшибок = Новый Массив;
	
	// проверяем наименование
	Если Не ЗначениеЗаполнено(OrganizationInfo.Name) Тогда 
		ДобавитьОшибкуВалидации(СписокОшибок, "не указано название организации");
	ИначеЕсли OrganizationInfo.Type = "IndividualEntity"  и (найти(OrganizationInfo.Name, " ") = 0) Тогда 
		ДобавитьОшибкуВалидации(СписокОшибок, "не указано имя индивидуального предпринимателя");
	Иначе 
		ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Name, "Наименование контрагента", Ложь, 0, 1000, Ложь, "с");
	КонецЕсли; 
	
	// проверяем ИНН
	Если ЗначениеЗаполнено(OrganizationInfo.Inn) Тогда
		
		ТекстОшибки = "";
		ИНН = OrganizationInfo.Inn;
		
		Если OrganizationInfo.Type = "LegalEntity" Тогда 
			ТекстОшибки = ПроверитьИННКонтрагента(ИНН, "ЮрЛицо");
		ИначеЕсли OrganizationInfo.Type = "IndividualEntity" Тогда
			ТекстОшибки = ПроверитьИННКонтрагента(ИНН, "ФизЛицо");
		КонецЕсли;
		
		ДобавитьОшибкуВалидации(СписокОшибок, ТекстОшибки);
		
	ИначеЕсли OrganizationInfo.Type = "LegalEntity" Тогда
		
		HyphenInn = ПолучитьЗначениеXDTO(OrganizationInfo, "HyphenInn");
		
		Если Не ЭтоИстина(HyphenInn) Тогда
			ДобавитьОшибкуВалидации(СписокОшибок, "ИНН контрагента не указан");
		КонецЕсли;
		
	КонецЕсли;
	
	// проверяем КПП
	Если ЗначениеЗаполнено(OrganizationInfo.Kpp) Тогда 
		ТекстОшибки = ПроверитьКППКонтрагента(OrganizationInfo.Kpp);
		ДобавитьОшибкуВалидации(СписокОшибок, ТекстОшибки);
	КонецЕсли;
	
	ПроверятьЗаполненостьАдреса = ПроверятьЗаполненностьАдресаУПД(ФорматПриказа, ContentFunction);
	
	ОшибкиАдреса = ПроверитьСтруктуруАдреса(OrganizationInfo.Address, МаксДлинаАдреса, ПроверятьЗаполненостьАдреса);
	
	Если ЗначениеЗаполнено(ОшибкиАдреса) Тогда 
		ДобавитьОшибкуВалидацииСОформлением(СписокОшибок, "Ошибки в " + ?(ТипСтруктуры = 1, "юридическом", "фактическом") + " адресе:", , "b");
		ДополнитьМассивОшибокВалидации(СписокОшибок, ОшибкиАдреса);
	КонецЕсли; 
	
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Okopf, "ОКОПФ контрагента", Ложь, 2, 5, Истина, "м");
	
	Возврат СписокОшибок;
	
КонецФункции	

Процедура УПД970_ДополнитьСписокОшибкамиПроверкиЮридическогоАдреса(СписокОшибок, ОшибкиАдреса)
	
	Если ЗначениеЗаполнено(ОшибкиАдреса) Тогда 
		ДобавитьОшибкуВалидацииСОформлением(СписокОшибок, "Ошибки в юридическом адресе:", , "b");
		ДополнитьМассивОшибокВалидации(СписокОшибок, ОшибкиАдреса);
	КонецЕсли; 
	
КонецПроцедуры

Процедура УПД970_ДополнитьСписокОшибкамиПроверкиФактическогоАдреса(СписокОшибок, ОшибкиАдреса)
	
	Если ЗначениеЗаполнено(ОшибкиАдреса) Тогда 
		ДобавитьОшибкуВалидацииСОформлением(СписокОшибок, "Ошибки в фактическом адресе:", , "b");
		ДополнитьМассивОшибокВалидации(СписокОшибок, ОшибкиАдреса);
	КонецЕсли; 
	
КонецПроцедуры

Процедура ПроверитьОшибкиОснованияПередачи(СписокОшибок, Content, ФорматПриказа = "155")
	
	ФункцияУПД = XML_УПД_ПолучитьФункцию(Content.Function);
	ФункцииУПД = Перечисление_ФункцииУПД();
	
	ЭтоУПДСЧФ_820 = ФорматПриказа = "820"
					И ФункцияУПД = ФункцииУПД.СЧФ;
	
	Если Content.TransferInfo.TransferBases.Количество() = 0
		И НЕ ЭтоУПДСЧФ_820 Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Не заполнены основания передачи");
		Возврат;
	КонецЕсли;
	
	Если ФорматПриказа = "155" Тогда
		ДокументОснованиеОтсутствует = "Отсутствует";
		ТекстФорматаПриказа	= ПодставитьГиперссылкуВОшибкуВалидации(" согласно формату <155 приказа>", "https://normativ.kontur.ru/document?moduleId=1&documentId=339569&rangeId=242736");
	Иначе // @820
		ДокументОснованиеОтсутствует = "Без документа-основания";
		ТекстФорматаПриказа = ПодставитьГиперссылкуВОшибкуВалидации(" согласно формату <820 приказа>", "https://normativ.kontur.ru/document?moduleId=1&documentId=328588&rangeId=242759");
	КонецЕсли;
		
	Для Каждого TransferBase Из Content.TransferInfo.TransferBases Цикл
		
		ОшибкиОснования = Новый Массив;
		
		Если TransferBase.DocumentName = ДокументОснованиеОтсутствует Тогда
			Продолжить;
		КонецЕсли;
		
		ТекстНезаполненаДата	= "При наличии основания дата обязательна" + ТекстФорматаПриказа + ".";
		ТекстНезаполненНомер	= " Номер заполняется в рекомендательном порядке.";
		
		ТекстГруппыОшибок = "";
		
		Если Не ЗначениеЗаполнено(TransferBase.DocumentDate) 
			И Не ЗначениеЗаполнено(TransferBase.DocumentNumber) Тогда
			
			ДобавитьОшибкуВалидации(ОшибкиОснования, "" + ТекстНезаполненаДата + ТекстНезаполненНомер);
			ТекстГруппыОшибок = "Заполните дату и номер.";
			
		ИначеЕсли Не ЗначениеЗаполнено(TransferBase.DocumentDate) Тогда
			
			ДобавитьОшибкуВалидации(ОшибкиОснования, ТекстНезаполненаДата);
			ТекстГруппыОшибок = "Заполните дату.";
			
		КонецЕсли;
		
		Если ОшибкиОснования.Количество() > 0 Тогда
			ТекстИнструкции	= ПодставитьГиперссылкуВОшибкуВалидации("<Как заполнить?>", "ИНСТРУКЦИЯКАКЗАПОЛНИТЬОСНОВАНИЕ");
			ДобавитьОшибкуВалидации(ОшибкиОснования, ТекстИнструкции);
		КонецЕсли;
		
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиОснования, "Ошибки в основании. <" + ТекстГруппыОшибок + ">", СвойствоСлужебнойИнформации(TransferBase, "ОснованиеСсылка"));
		
	КонецЦикла;
	
КонецПроцедуры

Функция ПроверитьОшибкиВСтрокеУПД(item) 

	СписокОшибокСтроки = Новый Массив;
	
	ПроверитьФорматПоля(СписокОшибокСтроки, Item.Product, "Наименование товара", Истина, 0, 1000, Ложь, "с");
	
	ПроверитьОшибкиВ_UnitCode_TaxRate(СписокОшибокСтроки, item.UnitCode, item.TaxRate);
	
	Если item.CustomDeclarations <> Неопределено Тогда
		
		Для Каждого CustomsDeclaration Из item.CustomDeclarations Цикл 
	
			Если ЗначениеЗаполнено(CustomsDeclaration.DeclarationNumber)
				И Не ЗначениеЗаполнено(CustomsDeclaration.CountryCode) Тогда
				
				ДобавитьОшибкуВалидации(СписокОшибокСтроки, "Для номенклатуры " + """" + Item.Product + """" + " не заполнена страна происхождения товара");
				
			ИначеЕсли Не ЗначениеЗаполнено(CustomsDeclaration.DeclarationNumber)
					И ЗначениеЗаполнено(CustomsDeclaration.CountryCode)
					И (СокрЛП(CustomsDeclaration.CountryCode) <> "643"
                    И СокрЛП(CustomsDeclaration.CountryCode) <> "981") Тогда
					
					// 981 - это ЕАЭС, которого нет в ОКСМ.
					// http://www.consultant.ru/document/cons_doc_LAW_197242/7556e2e94ff8990c8aa5f2cf32bb0e715f031cf5/
					// https://forum.infostart.ru/forum81/topic167091/
					
				ДополнитьМассивОшибокВалидации(СписокОшибокСтроки, ТекстОшибкиНеЗаполненГТД(Item));
					
			КонецЕсли;
						
			Если (стрДлина(сокрЛП(CustomsDeclaration.DeclarationNumber)) > 29) Тогда
				ДобавитьОшибкуВалидации(СписокОшибокСтроки, "Длина номера ГТД превышает максимальное ограничение в 29 символов");
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат СписокОшибокСтроки;
	
КонецФункции 

Функция ПроверитьКлассификаторыУПД(InvoiceContent)			
	
	Результат = Новый Массив;
	
	КодыСтранДляПроверки = Новый Соответствие;
	КодыЕдиницДляПроверки = Новый Соответствие;

	Для Каждого item Из InvoiceContent.InvoiceTable.Items Цикл
		
		ЕдиницаИзмеренияСсылка = СвойствоСлужебнойИнформации(Item, "ЕдиницаИзмерения");
		
		Если ЗначениеЗаполнено(Item.UnitCode) И Item.UnitCode <> "0000" Тогда
			КодыЕдиницДляПроверки.Вставить(ЕдиницаИзмеренияСсылка, СокрЛП(Item.UnitCode));
		КонецЕсли;
		
		Если item.CustomDeclarations <> Неопределено Тогда
			Для Каждого CustomsDeclaration Из item.CustomDeclarations Цикл 
				
				СтранаПроисхожденияСсылка = СвойствоСлужебнойИнформации(item, "СтранаПроисхождения");
				
				Если НЕ ЗначениеЗаполнено(CustomsDeclaration.DeclarationNumber) ИЛИ СтранаЧленТС(CustomsDeclaration.CountryCode) Тогда 
					Продолжить;  // можно объединить с условием ниже
				КонецЕсли;
				
				Если ЗначениеЗаполнено(CustomsDeclaration.CountryCode) Тогда 
					КодыСтранДляПроверки.Вставить(СтранаПроисхожденияСсылка, CustomsDeclaration.CountryCode);
				КонецЕсли;
				
			КонецЦикла;
		КонецЕсли;
		
	КонецЦикла;
	
	ДобавитьГруппуОшибокВалидации(Результат, ПроверитьВалюту(InvoiceContent.ВалютаСсылка, InvoiceContent.Currency),	"Ошибки в классификаторе валют");
	ДобавитьГруппуОшибокВалидации(Результат, ПроверитьСтраны(КодыСтранДляПроверки),									"Ошибки в классификаторе стран");
	ДобавитьГруппуОшибокВалидации(Результат, ПроверитьЕдиницыИзмеренияПоОКЕИ(КодыЕдиницДляПроверки),				"Ошибки в классификаторе единиц измерения");
	
	Возврат Результат;
	
КонецФункции

Функция СтранаЧленТС(Знач КодСтраны) Экспорт
	
	КодСтраны = СокрЛП(КодСтраны);
	
	КодыСтранТС = Новый Массив;
	КодыСтранТС.Добавить("643"); // Россия
	КодыСтранТС.Добавить("051"); // Армения
	КодыСтранТС.Добавить("112"); // Беларусь
	КодыСтранТС.Добавить("398"); // Казахстан
	КодыСтранТС.Добавить("417"); // Киргизия
	КодыСтранТС.Добавить("981"); // ЕАЭС
	
	Возврат КодыСтранТС.Найти(КодСтраны) <> Неопределено;
	
КонецФункции

// обертка для совместимости ОФ-УФ
Функция СвойствоСлужебнойИнформации(ВходящиеПараметры, ИмяСвойства)
	
	Результат = Неопределено;
	
	СтруктураСлужебнаяИнформация = Новый Структура("СлужебнаяИнформация");
	ЗаполнитьЗначенияСвойств(СтруктураСлужебнаяИнформация, ВходящиеПараметры);
  	СлужебнаяИнформация = СтруктураСлужебнаяИнформация.СлужебнаяИнформация;
	
	Если ТипЗнч(СлужебнаяИнформация) <> Тип("Структура") Тогда
		Возврат Результат;  // может встретиться в Про, либо СлужебнаяИнформация не была добавлена в ПМ
	КонецЕсли;	
	
	Если ИмяСвойства = "ЕдиницаИзмерения" Тогда
		
		Если СлужебнаяИнформация.Свойство("ЕдиницаИзмерения") Тогда
			Результат = СлужебнаяИнформация.ЕдиницаИзмерения;
		ИначеЕсли СлужебнаяИнформация.Свойство("Упаковка") Тогда
			Результат = СлужебнаяИнформация.Упаковка;
		КонецЕсли;	
		
	ИначеЕсли ИмяСвойства = "ЕдиницаИзмеренияДоИзменения" Тогда
		
		Если СлужебнаяИнформация.Свойство("ЕдиницаИзмеренияДоИзменения") Тогда
			Результат = СлужебнаяИнформация.ЕдиницаИзмеренияДоИзменения;
		ИначеЕсли СлужебнаяИнформация.Свойство("УпаковкаДоИзменения") Тогда
			Результат = СлужебнаяИнформация.УпаковкаДоИзменения;
		КонецЕсли;	
		
	ИначеЕсли ИмяСвойства = "НомерГТД" Тогда
		
		Если СлужебнаяИнформация.Свойство("НомерГТД") Тогда
			Результат = СлужебнаяИнформация.НомерГТД;
		КонецЕсли;
		
	ИначеЕсли ИмяСвойства = "СтранаПроисхождения" Тогда
		
		Если СлужебнаяИнформация.Свойство("СтранаПроисхождения") Тогда
			Результат = СлужебнаяИнформация.СтранаПроисхождения;
		КонецЕсли;
		
	ИначеЕсли ИмяСвойства = "ОснованиеСсылка" Тогда
		
		Если СлужебнаяИнформация.Свойство("ОснованиеСсылка") Тогда
			Результат = СлужебнаяИнформация.ОснованиеСсылка;
		КонецЕсли;
		
	КонецЕсли;	
	
	Возврат Результат;
	
КонецФункции

// ПРОВЕРКИ ФОРМАТОВ ПОЛЕЙ - ДЛИНА, ДИАПАЗОН ДАТ, ОБЯЗАТЕЛЬНОСТЬ, ...

Процедура ПроверитьФорматДаты(СписокОшибок, Значение, Представление, ОбязательноеЗаполнение = Ложь)
	
	ТекстОшибки = ОшибкаФорматаДаты(Значение, Представление, ОбязательноеЗаполнение);
	ДобавитьОшибкуВалидации(СписокОшибок, ТекстОшибки);

КонецПроцедуры

Функция ОшибкаФорматаДаты(Знач Значение, Представление, ОбязательноеЗаполнение = Ложь)
	
	Результат = "";
	
	Если ТипЗнч(Значение) <> Тип("Дата") Тогда
		ОписаниеТипа = Новый ОписаниеТипов("Дата");
		Значение = ОписаниеТипа.ПривестиЗначение(Значение);
	КонецЕсли;
	
	Если ОбязательноеЗаполнение И Не ЗначениеЗаполнено(Значение) Тогда 
		
		Результат = "не заполнена";
		
	ИначеЕсли Значение < '19000101' ИЛИ Значение >= '21000101' Тогда 
		
		Результат = "(" + Формат(Значение, "ДФ=dd.MM.yyyy") + ") должна находиться в интервале 01.01.1900 - 31.12.2099";
		
	КонецЕсли;
	
	Если Результат <> "" Тогда
		Результат = Представление + " " + Результат;
	КонецЕсли;
	
	Возврат Результат; 
	
КонецФункции

Процедура ПроверитьФорматПоля(СписокОшибок, Значение, Представление, Обязательность = Ложь, МинимальнаяДлина = 0, МаксимальнаяДлина = 0, ТолькоЦифры = Ложь, Род = "м", Число = "ед")
	
	ТекстОшибки = ОшибкаФорматаПоля(Значение, Представление, Обязательность, МинимальнаяДлина, МаксимальнаяДлина, ТолькоЦифры, Род, Число);
	ДобавитьОшибкуВалидации(СписокОшибок, ТекстОшибки);
	
КонецПроцедуры

Функция ОшибкаФорматаПоля(Знач Значение, Представление, Обязательность = Ложь, МинимальнаяДлина = 0, МаксимальнаяДлина = 0, ТолькоЦифры = Ложь, Род = "м", Число = "ед")
	
	Результат = "";
	КоличествоСимволовОграниченияВидимости = 147;
	
	Значение = СокрЛП(Значение);
	
	Если НЕ ЗначениеЗаполнено(Значение) Тогда 
		
		Если Обязательность Тогда
			Результат = "не " + ГлаголВалидации_Заполнен(Род, Число);
		КонецЕсли;	
		
	ИначеЕсли МинимальнаяДлина = МаксимальнаяДлина И МинимальнаяДлина > 0 И СтрДлина(Значение) <> МинимальнаяДлина Тогда
		// частный случай - фиксированая длина поля
		
		Результат = "(" + Значение + ") " + ГлаголВалидации_Должен(Род, Число) + " состоять из " + МинимальнаяДлина;
		
		Если ТолькоЦифры Тогда
			Результат = Результат + " цифр";
		Иначе
			Результат = Результат + " символов";
		КонецЕсли;
		
	ИначеЕсли СтрДлина(Значение) < МинимальнаяДлина Тогда
		
		Результат = "(" + Значение + ") не " + ГлаголВалидации_Должен(Род, Число) + " быть короче " + МинимальнаяДлина + " символов";
		
	ИначеЕсли СтрДлина(Значение) > МаксимальнаяДлина И МаксимальнаяДлина > 0 Тогда  // по умолчанию длину не ограничиваем
		
		ВидимоеЗначение = Лев(Значение, КоличествоСимволовОграниченияВидимости) + "...";
		
		Результат =
			"(" + ВидимоеЗначение
			+ ") не " + ГлаголВалидации_Должен(Род, Число) + " быть длиннее " + МаксимальнаяДлина + " символов";
		
		// тут можно явно указывать требуемую по формату длину: от и до.
		
	ИначеЕсли ТолькоЦифры И НЕ СтрокаСодержитТолькоЦифры(Значение) Тогда
		
		Результат = ГлаголВалидации_Должен(Род, Число) + " состоять из цифр";
		
	КонецЕсли;
	
	Если Результат <> "" Тогда
		Результат = Представление + " " + Результат;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ГлаголВалидации_Заполнен(Род, Число)
	
	Если Число = "ед" Тогда
		
		Если Род = "ж" Тогда
			Результат = "заполнена";
		ИначеЕсли Род = "м" Тогда
			Результат = "заполнен";
		ИначеЕсли Род = "с" Тогда
			Результат = "заполнено";
		КонецЕсли;
		
	Иначе
		Результат = "заполнены";
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ГлаголВалидации_Должен(Род, Число)
	
	Если Число = "ед" Тогда
		
		Если Род = "ж" Тогда
			Результат = "должна";
		ИначеЕсли Род = "м" Тогда
			Результат = "должен";
		ИначеЕсли Род = "с" Тогда
			Результат = "должно";
		КонецЕсли;
		
	Иначе
		Результат = "должны";
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

//Проверяет есть ли в строке символы отличные от "0123456789"
// Возвращаемое значение:
//	Булево
Функция СтрокаСодержитТолькоЦифры(СтрокаПроверки)
	
	Для й = 1 По СтрДлина(СтрокаПроверки) Цикл
		
		ПроверяемыйСимвол = Сред(СтрокаПроверки, й, 1);
		
		Если ПроверяемыйСимвол < "0" ИЛИ ПроверяемыйСимвол > "9" Тогда
			Возврат Ложь;
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Истина;
	
КонецФункции

Функция УмножитьВектора(ИНН, Множители)
	Результат = 0;
	н = 0;
	Пока найти(Множители, ":") > 0 Цикл
		н = н + 1;
		Множитель = число(Лев(Множители, Найти(Множители, ":") - 1));
		Множители = прав(Множители, СтрДлина(Множители) - найти(Множители, ":"));
		
		Результат = Результат + Множитель * число(Сред(ИНН, н, 1));
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции


//} Алгоритмы валидации контента исходящих документов

//#Область ПубличныеКонтракты

// Контракт структуры НоменклатураЭлектронногоДокумента.
// 
// Возвращаемое значение:
//  Структура:
//    * Идентификатор - Строка - идентификатор номенклатуры документа
//    * Тип - Строка - принимает значения "Имущество", "Работа", "Услуга", "Право", "Иное" или ""
//    * Код - Строка - код номенклатуры
//    * Артикул - Строка - артикул номенклатуры
//    * Наименование - Строка - наименование номенклатуры
//    * Характеристика - Строка - наименование характеристики
//    * Сорт - Строка - сорт номенклатуры
//    * СтавкаНДС - Строка - представление ставки НДС
//    * ЕдиницаИзмерения - Строка - наименование единицы измерения
//    * ЕдиницаИзмеренияКод - Строка - код единицы измерения
//
// @ПубличныйПрограммныйИнтерфейс
//
Функция Контракт_НоменклатураЭлектронногоДокумента()
	
	ПустаяСтрока = "";
	
	Результат = Новый Структура;
	Результат.Вставить("Идентификатор", ПустаяСтрока);
	Результат.Вставить("Тип", ТипНоменклатурыНеУказан());
	Результат.Вставить("Код", ПустаяСтрока);
	Результат.Вставить("Артикул", ПустаяСтрока);
	Результат.Вставить("Сорт", ПустаяСтрока);
	Результат.Вставить("Наименование", ПустаяСтрока);
	Результат.Вставить("Характеристика", ПустаяСтрока);
	Результат.Вставить("СтавкаНДС", ПустаяСтрока);
	Результат.Вставить("ЕдиницаИзмерения", ПустаяСтрока);
	Результат.Вставить("ЕдиницаИзмеренияКод", ПустаяСтрока);
	Результат.Вставить("GTIN", ПустаяСтрока);
	
	Возврат Результат;
	
КонецФункции

// Возвращает заполненный ОбъектXDTO со сведениями о подписанте для документов в формате 970
//
// Параметры:
//  Полномочия				 - Структура - сведения о полномочиях подписанта (см. Ядро.Подписант_ОписаниеПолномочий)
//  Сертификат				 - Структура - сведения о сертификате (см. Ядро.ВнешниеСервисы_Контракт_ОписаниеСертификата)
//  КонтрактМЧД				 - Структура - сведения о МЧД (см. Ядро.Контракт_МЧД)
//  ПараметрыФормирования	 - Структура - доп. параметры для заполнения сведений о подписанте (см. Новый_ПараметрыФормированияSigner970)
// 
// Возвращаемое значение:
//  ОбъектXDTO - протоструктура с описанием сведений о подписанте (см. в макете "Proto_xsd" описание типа "Signer970")
//
Функция УПД970_ЗаполненныйSigner(Полномочия, Сертификат, КонтрактМЧД, ПараметрыФормирования) Экспорт
	
	Результат = НовыйProtoОбъектXDTO("Signer970");
	
	Если Полномочия = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	Результат.Surname = Полномочия.Surname;
	Результат.FirstName = Полномочия.FirstName;
	
	ЕстьСвойствоОтчество = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Полномочия, "Patronymic");
	Если ЕстьСвойствоОтчество Тогда
		Результат.Patronymic = СокрЛП(Полномочия.Patronymic);
	КонецЕсли;
	
	ЕстьСвойствоДолжность = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Полномочия, "JobTitle");
	Если ЕстьСвойствоДолжность Тогда
		Результат.JobTitle = СокрЛП(Полномочия.JobTitle);
	КонецЕсли;
	
	ЭтоКЭП = Ложь;
	
	ЕстьСвойствоКЭП = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Сертификат, "ЭтоКЭП");
	Если ЕстьСвойствоКЭП Тогда
		ЭтоКЭП = Сертификат.ЭтоКЭП;
	КонецЕсли;
	
	Если ЭтоКЭП Тогда
		Результат.SignType = "Qualified";
	КонецЕсли;
	
	СпосПодтПолном = XML_СпособПодтвержденияПолномочийПодписанта(Сертификат, КонтрактМЧД, ПараметрыФормирования);
	Результат.ConfirmCredentials = СпосПодтПолном;
	
	ЕстьСвойствоДопСведений = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Полномочия, "SignerInfo");
	Если ЕстьСвойствоДопСведений Тогда
		Результат.AdditionalInfo = СокрЛП(Полномочия.SignerInfo);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает структуру параметров для заполнения прото со сведениями о подписанте в методе Новый_SignerContent970
//
// Параметры:
//  ПередаватьМЧДФайлом	 - Булево - признак передачи МЧД файлом
//  ИННОрганизации		 - Строка - ИНН организации (владельца ящика)
// 
// Возвращаемое значение:
//  Структура - структура доп. параметров для заполнения сведений о подписанте
//
Функция УПД970_Новый_ПараметрыФормированияSigner(ПередаватьМЧДФайлом, ИННОрганизации) Экспорт
			
	Результат = Новый Структура;
	
	Результат.Вставить("ПередаватьМЧДФайлом", ПередаватьМЧДФайлом);
	Результат.Вставить("ИННОрганизации", ИННОрганизации);
	
	Возврат Результат;		
		
КонецФункции

Функция XML_СпособПодтвержденияПолномочийПодписанта(Сертификат, КонтрактМЧД, ПараметрыФормирования)
	
	Результат = Неопределено;
	
	ЕстьСвойствоТип = ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Сертификат, "Тип");
	Если ЕстьСвойствоТип Тогда
		ТипСертификата = Сертификат.Тип;
	Иначе
		ТипСертификата = "ФЛ";
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Сертификат, "ИНН") Тогда
		ИННСертификата = Сертификат.ИНН;
	Иначе
		ИННСертификата = "";
	КонецЕсли;
	
	Если (ТипСертификата = "ЮЛ" Или ТипСертификата = "ИП")
		И ИННСертификата = ПараметрыФормирования.ИННОрганизации Тогда
		
		Результат = 1;
		
	ИначеЕсли ТипСертификата = "ФЛ"
		И ИННСертификата = ПараметрыФормирования.ИННОрганизации Тогда
		
		Результат = 6;
		
	ИначеЕсли ИННСертификата <> ПараметрыФормирования.ИННОрганизации Тогда
		
		Если КонтрактМЧД <> Неопределено
			И ПараметрыФормирования.ПередаватьМЧДФайлом Тогда
			
			Результат = 2;
			
		ИначеЕсли КонтрактМЧД <> Неопределено
			И НЕ ПараметрыФормирования.ПередаватьМЧДФайлом Тогда
			
			Результат = 4;
			
		ИначеЕсли КонтрактМЧД = Неопределено Тогда
			
			Результат = 6;
			
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Заполняет колонки строки табличной части по описанию номенклатуры электронного документа
//
// Параметры:
//  СтрокаЭД - СтрокаТаблицыЗначений - см. Новый_ТабличнаяЧасть.
//  НоменклатураЭД - Структура - см. Контракт_НоменклатураЭлектронногоДокумента
//
Процедура ПередаточныеДокументы_ЗаполнитьСтрокуТабличнойЧастиПоНоменклатуреДокумента(СтрокаЭД, НоменклатураЭД)
	
	СтрокаЭД.НоменклатураДокумента = НоменклатураЭД;
	
	СтрокаЭД.ИдентификаторНоменклатуры = НоменклатураЭД.Идентификатор;
	
	СтрокаЭД.Наименование = НоменклатураЭД.Наименование;
	СтрокаЭД.Характеристика = НоменклатураЭД.Характеристика;
	СтрокаЭД.Код = НоменклатураЭД.Код;
	СтрокаЭД.Артикул = НоменклатураЭД.Артикул;
	СтрокаЭД.Сорт = НоменклатураЭД.Сорт;
	
	СтрокаЭД.НаименованиеЕдИзм = НоменклатураЭД.ЕдиницаИзмерения;
	СтрокаЭД.КодЕдИзм = НоменклатураЭД.ЕдиницаИзмеренияКод;
	СтрокаЭД.СтавкаНДС = НоменклатураЭД.СтавкаНДС;
	
КонецПроцедуры

// Заполняет колонки строки табличной части по описанию номенклатуры электронного документа
//
// Параметры:
//  СтрокаЭД - СтрокаТаблицыЗначений - см. Новый_ТабличнаяЧастьКорректировки.
//  НоменклатураЭД_До - Структура - см. Контракт_НоменклатураЭлектронногоДокумента
//  НоменклатураЭД_После - Структура - см. Контракт_НоменклатураЭлектронногоДокумента
//
Процедура КорректировочныеДокументы_ЗаполнитьСтрокуТабличнойЧастиПоНоменклатуреДокумента(СтрокаЭД, НоменклатураЭД_До, НоменклатураЭД_После)
	
	СтрокаЭД.НоменклатураДокумента = НоменклатураЭД_После;
	
	СтрокаЭД.ИдентификаторНоменклатуры = НоменклатураЭД_После.Идентификатор;
	
	СтрокаЭД.Наименование = НоменклатураЭД_После.Наименование;
	СтрокаЭД.Характеристика = НоменклатураЭД_После.Характеристика;
	СтрокаЭД.Код = НоменклатураЭД_После.Код;
	СтрокаЭД.Артикул = НоменклатураЭД_После.Артикул;
	СтрокаЭД.Сорт = НоменклатураЭД_После.Сорт;
	
	СтрокаЭД.НаименованиеЕдИзм_До = НоменклатураЭД_До.ЕдиницаИзмерения;
	СтрокаЭД.НаименованиеЕдИзм = НоменклатураЭД_После.ЕдиницаИзмерения;
	
	СтрокаЭД.КодЕдИзм_До = НоменклатураЭД_До.ЕдиницаИзмеренияКод;
	СтрокаЭД.КодЕдИзм = НоменклатураЭД_После.ЕдиницаИзмеренияКод;
	
	СтрокаЭД.СтавкаНДС_До = НоменклатураЭД_До.СтавкаНДС;
	СтрокаЭД.СтавкаНДС = НоменклатураЭД_После.СтавкаНДС;
	
КонецПроцедуры

// Заполняет колонки строки табличной части по описанию номенклатуры электронного документа
//
// Параметры:
//  СтрокаЭД - СтрокаТаблицыЗначений - см. Новый_ТабличнаяЧастьАктаОРасхождениях
//  НоменклатураЭД - Структура - см. Контракт_НоменклатураЭлектронногоДокумента
//
Процедура АктыОРасхождениях_ЗаполнитьСтрокуТабличнойЧастиПоНоменклатуреДокумента(СтрокаТЧ, НоменклатураДокумента)
	
	СтрокаТЧ.ИдентификаторНоменклатуры = НоменклатураДокумента.Идентификатор;
	СтрокаТЧ.НоменклатураДокумента = НоменклатураДокумента;
	
КонецПроцедуры

// Формирует описание номенклатуры по строке табличной части документов в формате 970@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.ТаблСчФакт.СведТов
//  Идентификатор - Строка - Идентификатор номенклатуры, полученный из дополнительной информации строки ТЧ.
// 
// Возвращаемое значение:
//  Структура - см. Контракт_НоменклатураЭлектронногоДокумента.
//
Функция НоменклатураЭлектронногоДокумента970(СтрокаXDTO, Идентификатор)
	
	Перем Код, Артикул, Характеристика, Сорт, ПризнакНоменклатуры, GTIN;
	
	Наименование = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимТов");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ДопСведТов") Тогда
		
		ДопСведТов = СтрокаXDTO.ДопСведТов;
		
		Код = ПолучитьЗначениеXDTO(ДопСведТов, "КодТов");
		Артикул = ПолучитьЗначениеXDTO(ДопСведТов, "АртикулТов");
		Характеристика = ПолучитьЗначениеXDTO(ДопСведТов, "ХарактерТов");
		Сорт = ПолучитьЗначениеXDTO(ДопСведТов, "СортТов");
		ПризнакНоменклатуры = ПолучитьЗначениеXDTO(ДопСведТов, "ПрТовРаб");
		GTIN = ПолучитьЗначениеXDTO(ДопСведТов, "ГТИН");
		
	КонецЕсли;
	
	Результат = Новый_НоменклатураЭлектронногоДокумента(
		Идентификатор,
		Наименование,
		Артикул,
		Код,
		Характеристика
	);
	
	Результат.Сорт = Сорт;
	Результат.ЕдиницаИзмерения = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимЕдИзм");
	Результат.ЕдиницаИзмеренияКод = ПолучитьЗначениеXDTO(СтрокаXDTO, "ОКЕИ_Тов");
	Результат.СтавкаНДС = ПолучитьЗначениеXDTO(СтрокаXDTO, "НалСт");
	Результат.Тип = ОпределитьТипНоменклатурыЛок(ПризнакНоменклатуры);
	Результат.GTIN = GTIN;
	
	НормализоватьПоляНоменклатурыЭлектронногоДокумента(Результат);
	
	Возврат _ФиксированныеДанные(Результат);
	
КонецФункции

// Формирует описание номенклатуры по строке табличной части документов в формате 820@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.ТаблСчФакт.СведТов
//  Идентификатор - Строка - Идентификатор номенклатуры, полученный из дополнительной информации строки ТЧ.
// 
// Возвращаемое значение:
//  Структура - см. Контракт_НоменклатураЭлектронногоДокумента.
//
Функция НоменклатураЭлектронногоДокумента820(СтрокаXDTO, Идентификатор)
	
	Перем Код, Артикул, Характеристика, Сорт, ЕдиницаНаименование, ПризнакНоменклатуры;
	
	Наименование = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимТов");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ДопСведТов") Тогда
		
		ДопСведТов = СтрокаXDTO.ДопСведТов;
		
		Код = ПолучитьЗначениеXDTO(ДопСведТов, "КодТов");
		Артикул = ПолучитьЗначениеXDTO(ДопСведТов, "АртикулТов");
		Характеристика = ПолучитьЗначениеXDTO(ДопСведТов, "ХарактерТов");
		Сорт = ПолучитьЗначениеXDTO(ДопСведТов, "СортТов");
		ЕдиницаНаименование = ПолучитьЗначениеXDTO(ДопСведТов, "НаимЕдИзм");
		ПризнакНоменклатуры = ПолучитьЗначениеXDTO(ДопСведТов, "ПрТовРаб");
		
	КонецЕсли;
	
	Результат = Новый_НоменклатураЭлектронногоДокумента(
		Идентификатор,
		Наименование,
		Артикул,
		Код,
		Характеристика
	);
	
	Результат.Сорт = Сорт;
	Результат.ЕдиницаИзмерения = ЕдиницаНаименование;
	Результат.ЕдиницаИзмеренияКод = ПолучитьЗначениеXDTO(СтрокаXDTO, "ОКЕИ_Тов");
	Результат.СтавкаНДС = ПолучитьЗначениеXDTO(СтрокаXDTO, "НалСт");
	Результат.Тип = ОпределитьТипНоменклатурыЛок(ПризнакНоменклатуры);
	
	НормализоватьПоляНоменклатурыЭлектронногоДокумента(Результат);
	
	Возврат _ФиксированныеДанные(Результат);
	
КонецФункции

// Формирует описание номенклатуры по строке табличной части документов в формате 736@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.ТаблКСчФ.СведТов
//  Идентификатор - Строка - Идентификатор номенклатуры, полученный из дополнительной информации строки ТЧ.
// 
// Возвращаемое значение:
//  Структура - см. Контракт_НоменклатураЭлектронногоДокумента.
//
Функция НоменклатураЭлектронногоДокумента736_До(СтрокаXDTO, Идентификатор)
	
	Результат = НоменклатураЭлектронногоДокумента736_Служебный(
		СтрокаXDTO,
		Идентификатор,
		"До"
	);
	
	Возврат _ФиксированныеДанные(Результат);
	
КонецФункции

// Формирует описание номенклатуры по строке табличной части документов в формате 736@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.ТаблКСчФ.СведТов
//  Идентификатор - Строка - Идентификатор номенклатуры, полученный из дополнительной информации строки ТЧ.
// 
// Возвращаемое значение:
//  Структура - см. Контракт_НоменклатураЭлектронногоДокумента.
//
Функция НоменклатураЭлектронногоДокумента736_После(СтрокаXDTO, Идентификатор)
	
	Результат = НоменклатураЭлектронногоДокумента736_Служебный(
		СтрокаXDTO,
		Идентификатор,
		"После"
	);
	
	Возврат _ФиксированныеДанные(Результат);
	
КонецФункции

Функция НоменклатураЭлектронногоДокумента736_Служебный(СтрокаXDTO, Идентификатор, Постфикс)
	
	Перем Код, Артикул, Характеристика, Сорт, ЕдиницаНаименование;
	
	Наименование = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимТов");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ДопСведТов") Тогда
		
		ДопСведТов = СтрокаXDTO.ДопСведТов;
		
		Код = ПолучитьЗначениеXDTO(ДопСведТов, "КодТов");
		Артикул = ПолучитьЗначениеXDTO(ДопСведТов, "АртикулТов");
		Характеристика = ПолучитьЗначениеXDTO(ДопСведТов, "ХарактерТов");
		Сорт = ПолучитьЗначениеXDTO(ДопСведТов, "СортТов");
		ЕдиницаНаименование = ПолучитьЗначениеXDTO(ДопСведТов, "НаимЕдИзм" + Постфикс);
		
	КонецЕсли;
	
	Результат = Новый_НоменклатураЭлектронногоДокумента(
		Идентификатор,
		Наименование,
		Артикул,
		Код,
		Характеристика
	);
	
	Результат.Сорт = Сорт;
	Результат.ЕдиницаИзмерения = ЕдиницаНаименование;
	Результат.ЕдиницаИзмеренияКод = ПолучитьЗначениеXDTO(СтрокаXDTO, "ОКЕИ_Тов" + Постфикс);
	Результат.СтавкаНДС = ПолучитьЗначениеXDTO(СтрокаXDTO, "НалСт" + Постфикс);
	Результат.Тип = ТипНоменклатурыНеУказан();
	
	НормализоватьПоляНоменклатурыЭлектронногоДокумента(Результат);
	
	Возврат Результат;
	
КонецФункции

// Формирует описание номенклатуры по строке табличной части акта о расхождениях в формате 423@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.СодФХЖ2.РезПрием
//  Идентификатор - Строка - Идентификатор номенклатуры, полученный из дополнительной информации строки ТЧ.
// 
// Возвращаемое значение:
//  Структура - см. Контракт_НоменклатураЭлектронногоДокумента.
//
Функция НоменклатураЭлектронногоДокумента423(СтрокаXDTO, Идентификатор)
	
	Перем Сорт, СтавкаНДС;
	
	Ценности = СтрокаXDTO.Ценности;
	
	Код = ПолучитьЗначениеXDTO(Ценности, "КодТов");
	Артикул = ПолучитьЗначениеXDTO(Ценности, "АртикулТов");
	Наименование = ПолучитьЗначениеXDTO(Ценности, "НаимТов");
	Характеристика = ПолучитьЗначениеXDTO(Ценности, "ХарактТов");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ПоФакту") Тогда
		ПоФакту = СтрокаXDTO.ПоФакту;
		СтавкаНДС = ПолучитьЗначениеXDTO(ПоФакту, "СтавНДСПрин");
	КонецЕсли;
	
	Результат = Новый_НоменклатураЭлектронногоДокумента(
		Идентификатор,
		Наименование,
		Артикул,
		Код,
		Характеристика
	);
	
	Результат.Сорт = Сорт;
	Результат.ЕдиницаИзмерения = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимЕдИзм");
	Результат.ЕдиницаИзмеренияКод = ПолучитьЗначениеXDTO(СтрокаXDTO, "ОКЕИ_Тов");
	Результат.СтавкаНДС = СтавкаНДС;
	Результат.Тип = ТипНоменклатурыНеУказан();
	
	НормализоватьПоляНоменклатурыЭлектронногоДокумента(Результат);
	
	Возврат _ФиксированныеДанные(Результат);
	
КонецФункции

// Формирует описание номенклатуры по строке табличной части документов в формате 155@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.ТаблСчФакт.СведТов
//  Идентификатор - Строка - Идентификатор номенклатуры, полученный из дополнительной информации строки ТЧ.
// 
// Возвращаемое значение:
//  Структура - см. Контракт_НоменклатураЭлектронногоДокумента.
//
Функция НоменклатураЭлектронногоДокумента155(СтрокаXDTO, Идентификатор)
	
	Перем Код, Артикул, Характеристика, Сорт, ЕдиницаНаименование, ПризнакНоменклатуры;
	
	Наименование = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимТов");
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СтрокаXDTO, "ДопСведТов") Тогда
		
		ДопСведТов = СтрокаXDTO.ДопСведТов;
		
		Код = ПолучитьЗначениеXDTO(ДопСведТов, "КодТов");
		ЕдиницаНаименование = ПолучитьЗначениеXDTO(ДопСведТов, "НаимЕдИзм");
		ПризнакНоменклатуры = ПолучитьЗначениеXDTO(ДопСведТов, "ПрТовРаб");
		
	КонецЕсли;
	
	Результат = Новый_НоменклатураЭлектронногоДокумента(
		Идентификатор,
		Наименование,
		Артикул,
		Код,
		Характеристика
	);
	
	Результат.Сорт = Сорт;
	Результат.ЕдиницаИзмерения = ЕдиницаНаименование;
	Результат.ЕдиницаИзмеренияКод = ПолучитьЗначениеXDTO(СтрокаXDTO, "ОКЕИ_Тов");
	Результат.СтавкаНДС = ПолучитьЗначениеXDTO(СтрокаXDTO, "НалСт");
	Результат.Тип = ОпределитьТипНоменклатурыЛок(ПризнакНоменклатуры);
	
	НормализоватьПоляНоменклатурыЭлектронногоДокумента(Результат);
	
	Возврат _ФиксированныеДанные(Результат);
	
КонецФункции

// Формирует описание номенклатуры по строке табличной части счетов на оплату в формате Диадока.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.ТаблСНО.СведТов
//  Идентификатор - Строка - Идентификатор номенклатуры, полученный из дополнительной информации строки ТЧ.
// 
// Возвращаемое значение:
//  Структура - см. Контракт_НоменклатураЭлектронногоДокумента.
//
Функция НоменклатураЭлектронногоСчетаДД(СтрокаXDTO, Идентификатор)
	
	Перем Код, Артикул, Характеристика, Сорт;
	
	Наименование = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимТов");
	
	Результат = Новый_НоменклатураЭлектронногоДокумента(
		Идентификатор,
		Наименование,
		Артикул,
		Код,
		Характеристика
	);
	
	Результат.Сорт = Сорт;
	Результат.ЕдиницаИзмерения = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимЕдИзм");
	Результат.ЕдиницаИзмеренияКод = ПолучитьЗначениеXDTO(СтрокаXDTO, "ОКЕИ_Тов");
	Результат.СтавкаНДС = ПолучитьЗначениеXDTO(СтрокаXDTO, "НалСт");
	Результат.Тип = ТипНоменклатурыНеУказан();
	
	НормализоватьПоляНоменклатурыЭлектронногоДокумента(Результат);
	
	Возврат _ФиксированныеДанные(Результат);
	
КонецФункции

// Формирует описание номенклатуры по строке табличной части накладной в формате 551@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.СвДокПТПрКроме.СодФХЖ2.СвТов
//  Идентификатор - Строка - Идентификатор номенклатуры, полученный из дополнительной информации строки ТЧ.
// 
// Возвращаемое значение:
//  Структура - см. Контракт_НоменклатураЭлектронногоДокумента.
//
Функция НоменклатураЭлектроннойНакладной551(СтрокаXDTO, Идентификатор)
	
	Наименование = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимТов");
	Код = ПолучитьЗначениеXDTO(СтрокаXDTO, "КодТов");
	Артикул = ПолучитьЗначениеXDTO(СтрокаXDTO, "АртикулТов");
	Характеристика = ПолучитьЗначениеXDTO(СтрокаXDTO, "ХарактерТов");
	
	Результат = Новый_НоменклатураЭлектронногоДокумента(
		Идентификатор,
		Наименование,
		Артикул,
		Код,
		Характеристика
	);
	
	Результат.Сорт = ПолучитьЗначениеXDTO(СтрокаXDTO, "СортТов");
	Результат.ЕдиницаИзмерения = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимЕдИзм");
	Результат.ЕдиницаИзмеренияКод = ПолучитьЗначениеXDTO(СтрокаXDTO, "ОКЕИ_Тов");
	Результат.СтавкаНДС = ПолучитьЗначениеXDTO(СтрокаXDTO, "НалСт");
	Результат.Тип = ТипНоменклатурыИмущество();
	
	НормализоватьПоляНоменклатурыЭлектронногоДокумента(Результат);
	
	Возврат _ФиксированныеДанные(Результат);
	
КонецФункции

// Формирует описание номенклатуры по строке табличной части накладной в формате 172@
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.СвТНО.ТН.Таблица.СвТов
//  Идентификатор - Строка - Идентификатор номенклатуры, полученный из дополнительной информации строки ТЧ.
// 
// Возвращаемое значение:
//  Структура - см. Контракт_НоменклатураЭлектронногоДокумента.
//
Функция НоменклатураЭлектроннойНакладной172(СтрокаXDTO, Идентификатор)
	
	Наименование = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимТов");
	Код = ПолучитьЗначениеXDTO(СтрокаXDTO, "КодТов");
	Артикул = ПолучитьЗначениеXDTO(СтрокаXDTO, "АртикулТов");
	Характеристика = ПолучитьЗначениеXDTO(СтрокаXDTO, "ХарактерТов");
	
	Результат = Новый_НоменклатураЭлектронногоДокумента(
		Идентификатор,
		Наименование,
		Артикул,
		Код,
		Характеристика
	);
	
	Результат.Сорт = ПолучитьЗначениеXDTO(СтрокаXDTO, "СортТов");
	Результат.ЕдиницаИзмерения = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимЕдИзм");
	Результат.ЕдиницаИзмеренияКод = ПолучитьЗначениеXDTO(СтрокаXDTO, "ОКЕИ_Тов");
	Результат.СтавкаНДС = ПолучитьЗначениеXDTO(СтрокаXDTO, "СтавкаНДС");
	Результат.Тип = ТипНоменклатурыИмущество();
	
	НормализоватьПоляНоменклатурыЭлектронногоДокумента(Результат);
	
	Возврат _ФиксированныеДанные(Результат);
	
КонецФункции

// Формирует описание номенклатуры по строке табличной части акта в формате 552@
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.СвДокПРУ.СодФХЖ1.ОписРабот.Работа
//  Идентификатор - Строка - Идентификатор номенклатуры, полученный из дополнительной информации строки ТЧ.
// 
// Возвращаемое значение:
//  Структура - см. Контракт_НоменклатураЭлектронногоДокумента.
//
Функция НоменклатураЭлектронногоАкта552(СтрокаXDTO, Идентификатор)
	
	Перем Код, Артикул, Сорт;
	
	Наименование = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимРабот");
	Характеристика = ПолучитьЗначениеXDTO(СтрокаXDTO, "Описание");
	
	Если Не ЗначениеЗаполнено(Наименование) Тогда
		// Описание работ является обязательным, если не заполнен тэг НаимРабот
		Наименование = ПолучитьЗначениеXDTO(СтрокаXDTO, "Описание");
	КонецЕсли;
	
	Результат = Новый_НоменклатураЭлектронногоДокумента(
		Идентификатор,
		Наименование,
		Артикул,
		Код,
		Характеристика
	);
	
	Результат.Сорт = Сорт;
	Результат.ЕдиницаИзмерения = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимЕдИзм");
	Результат.ЕдиницаИзмеренияКод = ПолучитьЗначениеXDTO(СтрокаXDTO, "ОКЕИ");
	Результат.СтавкаНДС = ПолучитьЗначениеXDTO(СтрокаXDTO, "НалСт");
	Результат.Тип = ТипНоменклатурыРабота();
	
	НормализоватьПоляНоменклатурыЭлектронногоДокумента(Результат);
	
	Возврат _ФиксированныеДанные(Результат);
	
КонецФункции

// Формирует описание номенклатуры по строке табличной части акта в формате 552@
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.СвАктИ.ОписРабот.Работа
//  Идентификатор - Строка - Идентификатор номенклатуры, полученный из дополнительной информации строки ТЧ.
// 
// Возвращаемое значение:
//  Структура - см. Контракт_НоменклатураЭлектронногоДокумента.
//
Функция НоменклатураЭлектронногоАкта172(СтрокаXDTO, Идентификатор)
	
	Перем Код, Артикул, Характеристика, Сорт, СтавкаНДС;
	
	Наименование = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимРабот");
	Если Не ЗначениеЗаполнено(Наименование) Тогда
		Наименование = ПолучитьЗначениеXDTO(СтрокаXDTO, "Описание");
	КонецЕсли;
	
	Результат = Новый_НоменклатураЭлектронногоДокумента(
		Идентификатор,
		Наименование,
		Артикул,
		Код,
		Характеристика
	);
	
	Результат.Сорт = Сорт;
	Результат.ЕдиницаИзмерения = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимЕдИзм");
	Результат.ЕдиницаИзмеренияКод = ПолучитьЗначениеXDTO(СтрокаXDTO, "ОКЕИ");
	Результат.СтавкаНДС = СтавкаНДС;
	Результат.Тип = ТипНоменклатурыРабота();
	
	НормализоватьПоляНоменклатурыЭлектронногоДокумента(Результат);
	
	Возврат _ФиксированныеДанные(Результат);
	
КонецФункции

Функция ТипНоменклатурыИмущество()
	Возврат "Имущество";
КонецФункции

Функция ТипНоменклатурыРабота()
	Возврат "Работа";
КонецФункции

Функция ТипНоменклатурыУслуга()
	Возврат "Услуга";
КонецФункции

Функция ТипНоменклатурыИмущественныеПрава()
	Возврат "ИмущественныеПрава";
КонецФункции

Функция ТипНоменклатурыИное()
	Возврат "Иное";
КонецФункции

Функция ТипНоменклатурыНеУказан()
	Возврат "";
КонецФункции

//#КонецОбласти ПубличныеКонтракты


// Рекурсивно преобразует коллекции в фиксированные коллекции
//
// Параметры:
//  Данные - Произвольный - данные для преобразования
// 
// Возвращаемое значение:
//  ФиксированныйМассив, ФиксированнаяСтруктура, ФиксированноеСоответствие
//
Функция _ФиксированныеДанные(Данные)
	
	// BSLLS:CyclomaticComplexity-off
	// BSLLS:CognitiveComplexity-off
	
	Если ТипЗнч(Данные) = Тип("Массив") Тогда
		
		Массив = Новый Массив;
		
		Для Каждого Значение Из Данные Цикл
			Если ТипЗнч(Значение) = Тип("Структура")
				ИЛИ ТипЗнч(Значение) = Тип("Соответствие")
				ИЛИ ТипЗнч(Значение) = Тип("Массив") Тогда
				Массив.Добавить(_ФиксированныеДанные(Значение));
			Иначе
				Массив.Добавить(Значение);
			КонецЕсли;
		КонецЦикла;
		
		Результат = Новый ФиксированныйМассив(Массив);
		
	ИначеЕсли ТипЗнч(Данные) = Тип("Структура")
		ИЛИ ТипЗнч(Данные) = Тип("Соответствие") Тогда
		
		Если ТипЗнч(Данные) = Тип("Структура") Тогда
			Коллекция = Новый Структура;
		Иначе
			Коллекция = Новый Соответствие;
		КонецЕсли;
		
		Для Каждого КлючИЗначение Из Данные Цикл
			
			Значение = КлючИЗначение.Значение;
			Если ТипЗнч(Значение) = Тип("Структура")
				ИЛИ ТипЗнч(Значение) = Тип("Соответствие")
				ИЛИ ТипЗнч(Значение) = Тип("Массив") Тогда
				Коллекция.Вставить(КлючИЗначение.Ключ, _ФиксированныеДанные(Значение));
			Иначе
				Коллекция.Вставить(КлючИЗначение.Ключ, Значение);
			КонецЕсли;
			
		КонецЦикла;
		
		Если ТипЗнч(Данные) = Тип("Структура") Тогда
			Результат = Новый ФиксированнаяСтруктура(Коллекция);
		Иначе
			Результат = Новый ФиксированноеСоответствие(Коллекция);
		КонецЕсли;
		
	Иначе
		Результат = Данные;
	КонецЕсли;
	
	Возврат Результат;
	
	// BSLLS:CyclomaticComplexity-on
	// BSLLS:CognitiveComplexity-on
	
КонецФункции // _ФиксированныеДанные

// КОНВЕРТАЦИЯ КОНТЕНТА 155 В 820

// Возвращает прото-контент титула продавца в формате 820, заполненный по данным контента в 155 формате
//
// Параметры:
//  Контент155	 - Структура - заполненная протоструктура ответного титула в 155 формате
//								(см. НовыйProtoОбъектXDTO("UtdSellerContent"))
//
// Возвращаемое значение:
//  ОбъектXDTO - заполненная протоструктура ответного титула в 155 формате
//							(см. НовыйProtoОбъектXDTO("UtdSellerContent"))
//
Функция УПД155_Конвертировать_в820(Знач Контент155) Экспорт
	
	Контент155вXDTO = НовыйProtoОбъектXDTO("UtdSellerContent");
	ЗаполнитьПротоКонтентXDTOПоСтруктуре(Контент155вXDTO, Контент155);
	
	Контент820 = ПолучитьUtd820SellerContentИзUtdSellerContent(Контент155вXDTO);
	
	Если Не ЗначениеЗаполнено(Контент155.Consignee.Type) Тогда
		Контент820.Consignees.Очистить();
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Контент155.Shipper.SameAsSeller)
		И Не ЗначениеЗаполнено(Контент155.Shipper.OrganizationInfo.Type) Тогда
		Контент820.Shippers.Очистить();
	КонецЕсли;
	
	InvoiceTableItems155 = Контент155.InvoiceTable.Items;
	Для ИндексСтроки = 0 По InvoiceTableItems155.ВГраница() Цикл
		
		Item155 = InvoiceTableItems155[ИндексСтроки];
		Item820 = Контент820.Table.Items[ИндексСтроки];
		
		Для Каждого ItemIdentificationNumber155 Из Item155.СлужебнаяИнформация.ItemIdentificationNumbers Цикл
			ItemIdentificationNumber820 = НовыйProtoОбъектXDTO("ItemIdentificationNumber");
			ЗаполнитьПротоКонтентXDTOПоСтруктуре(ItemIdentificationNumber820, ItemIdentificationNumber155);
			Item820.ItemIdentificationNumbers.Добавить(ItemIdentificationNumber820);
		КонецЦикла;
		
		Для Каждого ItemTracingInfo155 Из Item155.СлужебнаяИнформация.ItemTracingInfos Цикл
			ItemTracingInfo820 = НовыйProtoОбъектXDTO("ItemTracingInfo");
			ЗаполнитьПротоКонтентXDTOПоСтруктуре(ItemTracingInfo820, ItemTracingInfo155);
			Item820.ItemTracingInfos.Добавить(ItemTracingInfo820);
		КонецЦикла;
		
	КонецЦикла;
	
	Возврат Контент820;
	
КонецФункции

// КОНВЕРТАЦИЯ КОНТЕНТА 820 В 155

// Возвращает прото-контент титула продавца в формате 155, заполненный по данным контента в 820 формате
//
// Параметры:
//  Контент820	 - Структура - заполненная протоструктура ответного титула в 820 формате
//							(см. НовыйProtoОбъектXDTO("Utd820SellerContent"))
//
// Возвращаемое значение:
//  Структура - заполненная протоструктура ответного титула в 155 формате
//							(см. НовыйProtoОбъектXDTO("UtdSellerContent"))
//
Функция УПД820_Конвертировать_в155(Знач Контент820) Экспорт
	
	Контент155 = ОбъектXDTOВСтруктуру(НовыйProtoОбъектXDTO("UtdSellerContent"));
	Контент820 = ОбъектXDTOВСтруктуру(Контент820);
	
	ЗаполнитьЗначенияСвойств(Контент155, Контент820, , "TransferInfo, PaymentDocuments");
	
	Контент155.Name = Контент820.DocumentName;
	Контент155.Date = Контент820.DocumentDate;
	Контент155.Number = Контент820.DocumentNumber;
	
	Контент155.Creator = Контент820.DocumentCreator;
	Контент155.CreatorBase = Контент820.DocumentCreatorBase;
	
	Если Контент820.Sellers.Количество() > 0 Тогда
		ЗаполнитьЗначенияСвойств(Контент155.Seller, Контент820.Sellers[0]);
	КонецЕсли;
	
	Если Контент820.Buyers.Количество() > 0 Тогда
		ЗаполнитьЗначенияСвойств(Контент155.Buyer, Контент820.Buyers[0]);
	КонецЕсли;
	
	Если Контент820.Consignees.Количество() > 0 Тогда
		ЗаполнитьЗначенияСвойств(Контент155.Consignee, Контент820.Consignees[0]);
	КонецЕсли;
	
	Если Контент820.Shippers.Количество() > 0 Тогда
		
		Shipper820 = Контент820.Shippers[0];
		
		Контент155.Shipper.SameAsSeller = Shipper820.SameAsSeller;
		
		ЗаполнитьЗначенияСвойств(Контент155.Shipper.OrganizationInfo, Shipper820.OrganizationInfo);
		
	КонецЕсли;
	
	Для Каждого PaymentDocument820 Из Контент820.PaymentDocuments Цикл
		PaymentDocument155 = ОбъектXDTOВСтруктуру(НовыйProtoОбъектXDTO("PaymentDocument"));
		ЗаполнитьЗначенияСвойств(PaymentDocument155, PaymentDocument820);
		Контент155.PaymentDocuments.Добавить(PaymentDocument155);
	КонецЦикла;
	
	УПД820_КонвертироватьTransferInfo_в155(Контент155.TransferInfo, Контент820.TransferInfo);
	
	УПД820_КонвертироватьСведенияОТоварах_в155(Контент155, Контент820);
	
	Контент155.Вставить("ОшибкиВДанных", Контент820.ОшибкиВДанных);
	
	Возврат Контент155;
	
КонецФункции

// Заполняет в прото-источник данные, которые были "потеряны" при конвертации 820-155-820
//
// Параметры:
//  Приемник - ОбъектXDTO - контент документа в 820, полученный из 155
//  Источник - ОбъектXDTO - контент документа в 820 до конвертации в 155
//
Процедура УПД820_ВосстановитьДанныеПослеКонвертации_из155(Приемник, Источник) Экспорт

	Для Каждого DocumentShipment Из Источник.DocumentShipments Цикл
		НовыйDocumentShipment = НовыйЭлементСпискаXDTO(Приемник.DocumentShipments);
		ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(НовыйDocumentShipment, DocumentShipment);
	КонецЦикла;
	
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(
		Приемник.SellerInfoCircumPublicProc,
		Источник.SellerInfoCircumPublicProc);
		
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(
		Приемник.MainAssignMonetaryClaim,
		Источник.MainAssignMonetaryClaim);
		
	УстановитьЗначениеXDTO(
		Приемник,
		"ApprovedStructureAdditionalInfoFields",
		Источник.ApprovedStructureAdditionalInfoFields);
		
	УстановитьЗначениеXDTO(Приемник, "CircumFormatInvoice", Источник.CircumFormatInvoice);
	
	УстановитьЗначениеXDTO(Приемник, "HyphenRevisionDate", Источник.HyphenRevisionDate);
	УстановитьЗначениеXDTO(Приемник, "HyphenRevisionNumber", Источник.HyphenRevisionNumber);
	
	Если Источник.TransferInfo <> Неопределено
		И Приемник.TransferInfo <> Неопределено Тогда
		
		УстановитьЗначениеXDTO(Приемник.TransferInfo, "TransferStartDate", Источник.TransferInfo.TransferStartDate);
		УстановитьЗначениеXDTO(Приемник.TransferInfo, "TransferEndDate", Источник.TransferInfo.TransferEndDate);
		
	КонецЕсли;

	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(
		Приемник.FactorInfo,
		Источник.FactorInfo);
		
	Если Источник.FactorInfo <> Неопределено
		И Приемник.FactorInfo <> Неопределено Тогда
		
		ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(
			Приемник.FactorInfo.Address,
			Источник.FactorInfo.Address);
			
	КонецЕсли;

КонецПроцедуры

Процедура УПД820_КонвертироватьTransferInfo_в155(TransferInfo155, TransferInfo820)
	
	ЗаполнитьЗначенияСвойств(TransferInfo155, TransferInfo820, , "TransferBases, Carrier");
	
	Для каждого TransferBase820 Из TransferInfo820.TransferBases Цикл
		
		TransferBase155 = ОбъектXDTOВСтруктуру(НовыйProtoОбъектXDTO("TransferBase"));
		
		ЗаполнитьЗначенияСвойств(TransferBase155, TransferBase820);
		
		Если TransferBase820.DocumentName = "Без документа-основания" Тогда
			TransferBase155.DocumentName = "Отсутствует";
		КонецЕсли;
		
		TransferInfo155.TransferBases.Добавить(TransferBase155);
		
	КонецЦикла;
	
	ЗаполнитьЗначенияСвойств(TransferInfo155.Carrier, TransferInfo820.Carrier);
	
КонецПроцедуры

Процедура УПД820_КонвертироватьСведенияОТоварах_в155(Контент155, Контент820)
	
	InvoiceTable155 = Контент155.InvoiceTable;
	
	ЗаполнитьЗначенияСвойств(InvoiceTable155, Контент820.Table, , "Items");
	
	Для Каждого Item820 Из Контент820.Table.Items Цикл
		
		Item155 = ОбъектXDTOВСтруктуру(НовыйProtoОбъектXDTO("ExtendedInvoiceItem"));
		
		ЗаполнитьЗначенияСвойств(Item155, Item820, , "СлужебнаяИнформация, CustomDeclarations");
		
		Item155.UnitCode = Item820.Unit;
		Item155.VendorCode = Item820.ItemVendorCode;
		Item155.ToRelease = Item820.ItemToRelease;
		
		Для Каждого CustomDeclaration820 Из Item820.CustomDeclarations Цикл
			CustomDeclaration155 = ОбъектXDTOВСтруктуру(НовыйProtoОбъектXDTO("CustomDeclaration"));
			ЗаполнитьЗначенияСвойств(CustomDeclaration155, CustomDeclaration820);
			Item155.CustomDeclarations.Добавить(CustomDeclaration155);
		КонецЦикла;
		
		Если Item820.WithoutVat = "true" Тогда
			Item155.TaxRate = "без НДС";
		КонецЕсли;
		
		ЗаполнитьЗначенияСвойств(Item155.СлужебнаяИнформация, Item820.СлужебнаяИнформация);
		
		Item155.СлужебнаяИнформация.Вставить("ItemTracingInfos", Item820.ItemTracingInfos);
		Item155.СлужебнаяИнформация.Вставить("ItemIdentificationNumbers", Item820.ItemIdentificationNumbers);
		
		Item820.СлужебнаяИнформация.Свойство("ЕдиницаИзмерения", Item155.ЕдиницаИзмеренияСсылка);
		
		InvoiceTable155.Items.Добавить(Item155);
		
	КонецЦикла;
	
КонецПроцедуры

// КОНВЕРТАЦИЯ КОНТЕНТА 820 В 970

// Возвращает прото-контент титула продавца в формате 970, заполненный по данным контента в 820 формате
//
// Параметры:
//  Контент820	 - Структура - заполненная протоструктура ответного титула в 820 формате
//							(см. НовыйProtoОбъектXDTO("Utd820SellerContent"))
//  ОшибкиКонвертации - Массив - ошибки конвертации
//		* Структура - см. НовоеОписаниеОшибкиКонвертации()
//
// Возвращаемое значение:
//  ОбъектXDTO - заполненная протоструктура ответного титула в 970 формате
//							(см. НовыйProtoОбъектXDTO("Utd970SellerContent"))
//
Функция УПД820_Конвертировать_в970(Знач Контент820, ОшибкиКонвертации) Экспорт
	
	Контент970 = НовыйProtoОбъектXDTO("Utd970SellerContent");

	ИсключаемыеСвойства = Новый Массив;
	ИсключаемыеСвойства.Добавить("CircumFormatInvoice");
	ИсключаемыеСвойства.Добавить("DocumentCreatorBase");
	ИсключаемыеСвойства.Добавить("Function");
	ИсключаемыеСвойства.Добавить("DocumentName");
	
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Контент970, Контент820, ИсключаемыеСвойства);
	
	Если ЗначениеЗаполнено(Контент820.DocumentCreatorBase) Тогда
		УПД820_ДобавитьОшибкуКонвертацииСведенийОрганизацииСоставителяФайла(ОшибкиКонвертации);
	КонецЕсли;
	
	ФункцияДокумента = УПД820_КонвертироватьЗначениеФункцииДокумента_в970(Контент820);
	УстановитьЗначениеXDTO(Контент970, "Function", ФункцияДокумента);
	
	ЗначениеСпецОбстоятельств = УПД820_КонвертироватьСпецОбстоятельствФормированияДокумента_в970(Контент820);
	УстановитьЗначениеXDTO(Контент970, "CircumFormatInvoice", ЗначениеСпецОбстоятельств);
	
	НаименованиеВалюты = НаименованиеВалюты(Контент970.Currency, Истина);
	УстановитьЗначениеXDTO(Контент970, "CurrencyName", НаименованиеВалюты);

	MainAssignMonetaryClaim = УПД820_КонвертироватьОснованиеУступкиДенТребования_в970(Контент820);
	УстановитьЗначениеXDTO(Контент970, "MainAssignMonetaryClaim", MainAssignMonetaryClaim);
	
	SellerInfoCircumPublicProc = УПД820_КонвертироватьСведенияОЗакупкахДляГосНужд_в970(Контент820);
	УстановитьЗначениеXDTO(Контент970, "SellerInfoCircumPublicProc", SellerInfoCircumPublicProc);
	
	AdditionalInfoId = УПД820_КонвертироватьИнформационныеПоляФХЖ_в970(Контент820.AdditionalInfoId);
	УстановитьЗначениеXDTO(Контент970, "AdditionalInfoId", AdditionalInfoId);
	
	УПД820_КонвертироватьСведенияОбУчастникахЭДО_в970(Контент820, Контент970);
	
	УПД820_КонвертироватьСведенияОТоварах_в970(Контент970, Контент820);
	
	TransferInfo = УПД820_КонвертироватьСведенияОПередачеТоваров_в970(Контент820.TransferInfo, ОшибкиКонвертации);
	УстановитьЗначениеXDTO(Контент970, "TransferInfo", TransferInfo);
	
	Для Каждого PaymentDocument820 Из Контент820.PaymentDocuments Цикл
		PaymentDocument = УПД820_КонвертироватьСведенияОДокументеОплаты_в970(PaymentDocument820);
		Контент970.PaymentDocuments.Добавить(PaymentDocument);
	КонецЦикла;
	
	УПД820_КонвертироватьСведенияОДокументахОтгрузки_в970(Контент820, Контент970, ОшибкиКонвертации);
	
	НаименованиеДокумента = УПД820_КонвертироватьНаименованиеДокумента_в970(Контент820, Контент970);
	УстановитьЗначениеXDTO(Контент970, "DocumentName", НаименованиеДокумента);
	
	Контент970.СлужебнаяИнформация = НовыйProtoОбъектXDTO("СлужебнаяИнформация");
	Для Каждого ОписаниеОшибки Из Контент820.ОшибкиВДанных Цикл
		НовоеОписаниеОшибки = НовыйProtoОбъектXDTO("ОшибкиВДанныхТип");
		ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(НовоеОписаниеОшибки, ОписаниеОшибки);
		Контент970.СлужебнаяИнформация.ОшибкиВДанных.Добавить(НовоеОписаниеОшибки);
	КонецЦикла;
	
	ЕстьМаркировка = XML_УПД_970_ПризнакНаличияТоваровПодлежащихМаркировке(Контент970);
	ЕстьПрослежка = XML_УПД_970_ПризнакНаличияТоваровПодлежащихПрослеживаемости(Контент970);
	
	ContentsDocumentProductsType = Контент970.СлужебнаяИнформация.ContentsDocumentProductsType;
	ContentsDocumentProductsType.ContainsMarkedProducts = ЕстьМаркировка;
	ContentsDocumentProductsType.ContainsTraceableProducts = ЕстьПрослежка;
	
	Возврат Контент970;
	
КонецФункции

// Заполняет в прото-источник данные, которые были "потеряны" при конвертации 970-820*-970
//
// Параметры:
//  Приемник - ОбъектXDTO - контент документа в 970, полученный из 820
//  Источник - ОбъектXDTO - контент документа в 970 до конвертации в 820
//
Процедура УПД970_ВосстановитьДанныеПослеКонвертации_из820(Приемник, Источник) Экспорт

	ПризнакиТоваровВПриемнике = Приемник.СлужебнаяИнформация.ContentsDocumentProductsType;
	ПризнакиТоваровВИсточнике = Источник.СлужебнаяИнформация.ContentsDocumentProductsType;

	ПризнакиТоваровВПриемнике.ContainsAlcoholeProducts = ПризнакиТоваровВИсточнике.ContainsAlcoholeProducts;
	ПризнакиТоваровВПриемнике.ContainsPetroleumProducts = ПризнакиТоваровВИсточнике.ContainsPetroleumProducts;
	ПризнакиТоваровВПриемнике.ContainsTobaccoProducts = ПризнакиТоваровВИсточнике.ContainsTobaccoProducts;

КонецПроцедуры

Функция УПД820_КонвертироватьСпецОбстоятельствФормированияДокумента_в970(Контент820)
	
	Перем Результат;
	
	ФункцияДокумента = XML_УПД_ПолучитьФункцию(Контент820.Function);
	
	Если ФункцияДокумента = Перечисление_ФункцииУПД().СЧФ Тогда
		
		Результат = УПД820_КонвертироватьОбстоятельстваФормированияСчетФактуры_в970(Контент820);
		
	Иначе
		
		Значение = Контент820.CircumFormatInvoice;
		
		Если Значение = "4" Тогда
			Результат = "00005";
		ИначеЕсли Значение = "5" Тогда
			Результат = "00006";
		ИначеЕсли Значение = "6" Тогда
			Результат = "00007";
		ИначеЕсли Значение = "7" Тогда
			Результат = "00008";
		ИначеЕсли Значение = "8" Тогда
			Результат = "00009";
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьЗначениеФункцииДокумента_в970(Контент820)
	
	Результат = XML_УПД_ПолучитьФункцию(Контент820.Function);
	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьОснованиеУступкиДенТребования_в970(Контент820)
	
	Результат = НовыйProtoОбъектXDTO("MainAssignMonetaryClaim");

	Если Контент820.MainAssignMonetaryClaim <> Неопределено Тогда
		
		УстановитьЗначениеXDTO(Результат, "Name", Контент820.MainAssignMonetaryClaim.DocumentName);
		УстановитьЗначениеXDTO(Результат, "Number", Контент820.MainAssignMonetaryClaim.DocumentNumber);
		УстановитьЗначениеXDTO(Результат, "Date", Контент820.MainAssignMonetaryClaim.DocumentDate);
		УстановитьЗначениеXDTO(Результат, "ID", Контент820.MainAssignMonetaryClaim.BaseDocumentId);
		УстановитьЗначениеXDTO(Результат, "AdditionalInfo", Контент820.MainAssignMonetaryClaim.DocumentInfo);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьСведенияОЗакупкахДляГосНужд_в970(Контент820)
	
	Перем Результат;
	
	Результат = НовыйProtoОбъектXDTO("SellerInfoCircumPublicProc");
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, Контент820.SellerInfoCircumPublicProc);
	
	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьСведенияУчастникаЭДО_в970(УчастникЭДО820)
	
	Перем Результат;
	
	Если НЕ УПД820_СведенияОБУчастникеЭДОЗаполнены(УчастникЭДО820) Тогда
		Возврат Результат;
	КонецЕсли;
	
	Результат = НовыйProtoОбъектXDTO("ExtendedOrganizationInfo970");
	
	ИсключаемыеСвойства = Новый Массив;
	ИсключаемыеСвойства.Добавить("Phone");
	ИсключаемыеСвойства.Добавить("Email");

	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, УчастникЭДО820, ИсключаемыеСвойства);
	
	Если ЗначениеЗаполнено(УчастникЭДО820.Phone) Тогда
		Результат.Phone.Добавить(УчастникЭДО820.Phone);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(УчастникЭДО820.Email) Тогда
		Результат.Email.Добавить(УчастникЭДО820.Email);
	КонецЕсли;
	
	КодСтраны = КодСтраны(УчастникЭДО820.Country);
	УстановитьЗначениеXDTO(Результат, "CountryCode", КодСтраны);

	Address820 = УчастникЭДО820.Address;
	Если Address820 <> Неопределено Тогда
		
		Address = УПД820_КонвертироватьАдресУчастникаЭДО_в970(Address820);
		УстановитьЗначениеXDTO(Результат, "Address", Address);
		
	КонецЕсли;
	
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат.СлужебнаяИнформация, УчастникЭДО820);
	
	Возврат Результат;
	
КонецФункции

Функция УПД820_СведенияОБУчастникеЭДОЗаполнены(УчастникЭДО820)

	Результат = УчастникЭДО820 <> Неопределено
		И (ЗначениеЗаполнено(УчастникЭДО820.Type)
			Или ЗначениеЗаполнено(УчастникЭДО820.Ссылка)
			Или ЗначениеЗаполнено(УчастникЭДО820.Name)
			Или ЗначениеЗаполнено(УчастникЭДО820.Inn)
			Или ЗначениеЗаполнено(УчастникЭДО820.BoxId)
			Или ЗначениеЗаполнено(УчастникЭДО820.FnsParticipantId));
	
	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьАдресУчастникаЭДО_в970(Address820)
	
	Перем Результат;
	
	Результат = НовыйProtoОбъектXDTO("AddressInfo970");

	ВидыАдресов = Перечисление_ВидыАдресов();

	Если ЗначениеЗаполнено(Address820.RegionCode) Тогда
		
		Результат.Type = ВидыАдресов.АдресРФ;
		Результат.AddressRF = НовыйProtoОбъектXDTO("AddressRFInfo");
		
		ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат.AddressRF, Address820);
		
		НаименованиеРегиона = НаименованиеРегиона(Address820.RegionCode);
		УстановитьЗначениеXDTO(Результат.AddressRF, "Region", НаименованиеРегиона);
		
	ИначеЕсли ЗначениеЗаполнено(Address820.AddressText) Тогда
		
		Результат.Type = ВидыАдресов.АдресСтрокой;
		Результат.AddressText = НовыйProtoОбъектXDTO("AddressTextInfo");
		
		НаименованиеСтраны = НаименованиеСтраны(Address820.CountryCode);
		
		УстановитьЗначениеXDTO(Результат.AddressText, "Country", НаименованиеСтраны);
		УстановитьЗначениеXDTO(Результат.AddressText, "CountryCode", Address820.CountryCode);
		УстановитьЗначениеXDTO(Результат.AddressText, "AddressText", Address820.AddressText);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьСведенияГрузоотправителя_в970(Грузоотправитель820)
	
	Перем Результат;
	
	Если Грузоотправитель820.SameAsSeller <> Истина
		И Не УПД820_СведенияОБУчастникеЭДОЗаполнены(Грузоотправитель820.OrganizationInfo) Тогда
		Возврат Результат;
	КонецЕсли;
	
	Результат = НовыйProtoОбъектXDTO("Shipper970");
	
	УстановитьЗначениеXDTO(Результат, "SameAsSeller", Грузоотправитель820.SameAsSeller);
	
	Если Грузоотправитель820.SameAsSeller <> Истина Тогда
		
		Shipper = УПД820_КонвертироватьСведенияУчастникаЭДО_в970(Грузоотправитель820.OrganizationInfo);
		УстановитьЗначениеXDTO(Результат, "OrganizationInfo", Shipper);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьAdditionalInfoItem_в970(AdditionalInfoItem820)
	
	Результат = НовыйProtoОбъектXDTO("AdditionalInfoItem");
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, AdditionalInfoItem820);
	
	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьСведенияОПередачеТоваров_в970(TransferInfo820, ОшибкиКонвертации)
	
	Перем Результат;
	
	Если TransferInfo820 = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	Результат = НовыйProtoОбъектXDTO("TransferInfo970");
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, TransferInfo820);
	
	Если Не ЗначениеЗаполнено(Результат.TransferDate) Тогда
		ДатаОтгрузки = УПД820_ДатаОтгрузки(TransferInfo820);
		УстановитьЗначениеXDTO(Результат, "TransferDate", ДатаОтгрузки);
	КонецЕсли;
	
	Employee = УПД820_КонвертироватьСведенияОСотруднике_в970(TransferInfo820.Employee, ОшибкиКонвертации);
	УстановитьЗначениеXDTO(Результат, "Employee", Employee);

	OtherIssuer = УПД820_КонвертироватьСведенияОРаботникеИнойОрганизации_в970(
		TransferInfo820.OtherIssuer,
		ОшибкиКонвертации);
	УстановитьЗначениеXDTO(Результат, "OtherIssuer", OtherIssuer);

	AdditionalInfoId = УПД820_КонвертироватьИнформационныеПоляФХЖ_в970(TransferInfo820.AdditionalInfoId);
	УстановитьЗначениеXDTO(Результат, "AdditionalInfoId", AdditionalInfoId);
	
	Для Каждого TransferBase820 Из TransferInfo820.TransferBases Цикл
		
		Если TransferBase820.DocumentName = БезДокументаОснования() Тогда
			Результат.WithoutTransferBase = Истина;
			Прервать;
		Иначе
			TransferBase = УПД820_КонвертироватьРеквизитыОснованияОтгрузки_в970(TransferBase820);
			Результат.TransferBases.Добавить(TransferBase);
		КонецЕсли;
			
	КонецЦикла;

	УПД820_КонвертироватьСведенияОТранспортныхНакладных_в970(TransferInfo820, Результат);
	
	ЕстьСведенияОПеревозчике = (TransferInfo820.Carrier <> Неопределено
		И ЗначениеЗаполнено(TransferInfo820.Carrier.Type)
		И ЗначениеЗаполнено(TransferInfo820.Carrier.Name));
		
	Если ЕстьСведенияОПеревозчике Тогда
		УПД820_ДобавитьОшибкуКонвертацииСведенийОПеревозчике(ОшибкиКонвертации);
	КонецЕсли;

	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьСведенияОСотруднике_в970(Employee820, ОшибкиКонвертации)

	Перем Результат;
	
	Если Employee820 <> Неопределено Тогда
		
		Результат = НовыйProtoОбъектXDTO("Employee970");
		ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, Employee820);
		
	КонецЕсли;

	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьСведенияОРаботникеИнойОрганизации_в970(OtherIssuer820, ОшибкиКонвертации)

	Перем Результат;
	
	Если OtherIssuer820 <> Неопределено Тогда
		
		Результат = НовыйProtoОбъектXDTO("OtherIssuer970");
		
		ИсключаемыеСвойства = Новый Массив;
		ИсключаемыеСвойства.Добавить("EmployeeBase");
		ИсключаемыеСвойства.Добавить("OrganizationBase");

		ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, OtherIssuer820, ИсключаемыеСвойства);
		
		Если ЗначениеЗаполнено(OtherIssuer820.EmployeeBase) Тогда
			УПД820_ДобавитьОшибкуКонвертацииОснованияПолномочийПредставителяИнойОрганизации(ОшибкиКонвертации);
		КонецЕсли;
		
		Если ЗначениеЗаполнено(OtherIssuer820.OrganizationBase) Тогда
			Если ЗначениеЗаполнено(OtherIssuer820.EmployeePosition) Тогда
				УПД820_ДобавитьОшибкуКонвертацииОснованияПолномочийИнойОрганизации(ОшибкиКонвертации);
			Иначе
				УПД820_ДобавитьОшибкуКонвертацииОснованияПолномочийФизЛица(ОшибкиКонвертации);
			КонецЕсли;
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьИнформационныеПоляФХЖ_в970(AdditionalInfoId820)
	
	Перем Результат;
	
	Если AdditionalInfoId820 = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	Результат = НовыйProtoОбъектXDTO("AdditionalInfoId");
	
	УстановитьЗначениеXDTO(Результат, "InfoFileId", AdditionalInfoId820.InfoFileId);
	
	Для Каждого AdditionalInfoItem820 Из AdditionalInfoId820.StructedAdditionalInfos Цикл
		AdditionalInfoItem = УПД820_КонвертироватьAdditionalInfoItem_в970(AdditionalInfoItem820);
		Результат.StructedAdditionalInfos.Добавить(AdditionalInfoItem);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьРеквизитыОснованияОтгрузки_в970(TransferBase820)
	
	НомерДокумента = TransferBase820.DocumentNumber;
	Если Не ЗначениеЗаполнено(НомерДокумента) Тогда
		НомерДокумента = "Без номера";
	КонецЕсли;
	
	Результат = НовыйProtoОбъектXDTO("TransferBase970");
	
	УстановитьЗначениеXDTO(Результат, "Name", TransferBase820.DocumentName);
	УстановитьЗначениеXDTO(Результат, "Number", НомерДокумента);
	УстановитьЗначениеXDTO(Результат, "Date", TransferBase820.DocumentDate);
	УстановитьЗначениеXDTO(Результат, "ID", TransferBase820.BaseDocumentId);
	УстановитьЗначениеXDTO(Результат, "AdditionalInfo", TransferBase820.DocumentInfo);
	
	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьСведенияОДокументеОплаты_в970(PaymentDocument820)
	
	Результат = НовыйProtoОбъектXDTO("PaymentDocumentInfo");
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, PaymentDocument820);
	
	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьСведенияОДокументеОтгрузки_в970(DocumentShipment820)
	
	Результат = НовыйProtoОбъектXDTO("DocumentShipment970");
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, DocumentShipment820);

	Возврат Результат;
	
КонецФункции

Процедура УПД820_КонвертироватьСведенияОТранспортныхНакладных_в970(TransferInfo820, TransferInfo970)
	
	Если Не ЗначениеЗаполнено(TransferInfo820.Waybills) Тогда
		Возврат;
	КонецЕсли;
	
	КлючПоля = "ТранНакл820";
	ШаблонЗначения = "%1 от %2";
	
	Для Каждого СведенияОНакладной Из TransferInfo820.Waybills Цикл
		
		ЗначениеПоля = Общее_ПодставитьПараметрыВСтроку(
			ШаблонЗначения,
			СведенияОНакладной.DocumentNumber,
			Дата1С_в_ДатаXML(СведенияОНакладной.DocumentDate));
			
		AdditionalInfoItem = НовыйProtoОбъектXDTO("AdditionalInfoItem");
		УстановитьЗначениеXDTO(AdditionalInfoItem, "Key", КлючПоля);
		УстановитьЗначениеXDTO(AdditionalInfoItem, "Value", ЗначениеПоля);
		
		TransferInfo970.AdditionalInfoId.StructedAdditionalInfos.Добавить(AdditionalInfoItem);
		
	КонецЦикла;
	
КонецПроцедуры

Процедура УПД820_КонвертироватьСведенияОбУчастникахЭДО_в970(Контент820, Контент970)

	Для Каждого Seller820 Из Контент820.Sellers Цикл
		Seller = УПД820_КонвертироватьСведенияУчастникаЭДО_в970(Seller820);
		Если Seller <> Неопределено Тогда
			Контент970.Sellers.Добавить(Seller);
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Buyer820 Из Контент820.Buyers Цикл
		Buyer = УПД820_КонвертироватьСведенияУчастникаЭДО_в970(Buyer820);
		Если Buyer <> Неопределено Тогда
			Контент970.Buyers.Добавить(Buyer);
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Consignee820 Из Контент820.Consignees Цикл
		Consignee = УПД820_КонвертироватьСведенияУчастникаЭДО_в970(Consignee820);
		Если Consignee <> Неопределено Тогда
			Контент970.Consignees.Добавить(Consignee);
		КонецЕсли;
	КонецЦикла;
	
	Для Каждого Shipper820 Из Контент820.Shippers Цикл
		Shipper = УПД820_КонвертироватьСведенияГрузоотправителя_в970(Shipper820);
		Если Shipper <> Неопределено Тогда
			Контент970.Shippers.Добавить(Shipper);
		КонецЕсли;
	КонецЦикла;
	
	Если Контент820.FactorInfo <> Неопределено
		И ЗначениеЗаполнено(Контент820.FactorInfo.Type) Тогда
		FactorInfo = УПД820_КонвертироватьСведенияУчастникаЭДО_в970(Контент820.FactorInfo);
		УстановитьЗначениеXDTO(Контент970, "FactorInfo", FactorInfo);
	КонецЕсли;

КонецПроцедуры

Процедура УПД820_КонвертироватьСведенияОДокументахОтгрузки_в970(Контент820, Контент970, ОшибкиКонвертации)
	
	Если Контент970.Function = Перечисление_ФункцииУПД().СЧФДОП Тогда
		
		ДокументОтгрузки820 = Неопределено;
		Если Контент820.DocumentShipments.Количество() = 1 Тогда
			ДокументОтгрузки820 = Контент820.DocumentShipments[0];
		КонецЕсли;
		
		ДанныеДокументаОтгрузкиРазличаются = (ДокументОтгрузки820 = Неопределено
			Или ДокументОтгрузки820.Name <> ИмяДокументаОтгрузкиУПД()
			Или ДокументОтгрузки820.Number <> Контент970.DocumentNumber
			Или ДокументОтгрузки820.Date <> Контент970.DocumentDate
			Или Контент820.DocumentName <> ИмяДокументаОтгрузкиУПД());
			
		Если ДанныеДокументаОтгрузкиРазличаются Тогда
			
			DocumentShipment = НовыйЭлементСпискаXDTO(Контент970.DocumentShipments);
			DocumentShipment.Name = ИмяДокументаОтгрузкиУПД();
			DocumentShipment.Number = Контент970.DocumentNumber;
			DocumentShipment.Date = Контент970.DocumentDate;
			
			Контент970.DocumentName = ИмяДокументаОтгрузкиУПД();
			
		Иначе
			
			DocumentShipment = НовыйЭлементСпискаXDTO(Контент970.DocumentShipments);
			ЗаполнитьЗначенияСвойств(DocumentShipment, Контент820.DocumentShipments[0]);
			
		КонецЕсли;

	Иначе
		
		Для Каждого DocumentShipment820 Из Контент820.DocumentShipments Цикл
			DocumentShipment = УПД820_КонвертироватьСведенияОДокументеОтгрузки_в970(DocumentShipment820);
			Контент970.DocumentShipments.Добавить(DocumentShipment);
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

Функция УПД820_ДатаОтгрузки(TransferInfo820)

	Контент820 = TransferInfo820.Владелец();
	
	Результат = Контент820.DocumentDate;
	
	Для Каждого ДокументОтгрузки Из Контент820.DocumentShipments Цикл
		Если ЗначениеЗаполнено(ДокументОтгрузки.Date)
			И ЗначениеЗаполнено(Результат) Тогда
			Результат = Мин(ДокументОтгрузки.Date, Результат);
		КонецЕсли;
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Процедура УПД820_ДобавитьОшибкуКонвертацииСведенийОПеревозчике(ОшибкиКонвертации)
	
	ТекстОшибки = "Поле отсутствует в формате ФНС 970";
	ПутьКПолю = "Документ / СвПродПер / СвПер / ТранГруз / Перевозчик"; 
	
	ОписаниеОшибки = НовоеОписаниеПредупреждения(
		ГруппаОшибокКонвертацииПотеряДанных(),
		ТекстОшибки,
		ПутьКПолю);
		
	ОшибкиКонвертации.Добавить(ОписаниеОшибки);
	
КонецПроцедуры

Процедура УПД820_ДобавитьОшибкуКонвертацииСведенийОрганизацииСоставителяФайла(ОшибкиКонвертации)
	
	ТекстОшибки = "Изменился тип значения поля в формате ФНС 970";
	ПутьКПолю = "Документ / ОснДоверОргСост"; 
	
	ОписаниеОшибки = НовоеОписаниеПредупреждения(
		ГруппаОшибокКонвертацииПотеряДанных(),
		ТекстОшибки,
		ПутьКПолю);
		
	ОшибкиКонвертации.Добавить(ОписаниеОшибки);
	
КонецПроцедуры

Процедура УПД820_ДобавитьОшибкуКонвертацииСведенийОПредставителеИнойОрганизации(ОшибкиКонвертации)

	ТекстОшибки = "Изменился тип значения поля в формате ФНС 970";
	ПутьКПолю = "Документ / СвПродПер / СвПер / СвЛицПер / ИнЛицо / ПредОргПер / ОснПолнПредПер"; 
	
	ОписаниеОшибки = НовоеОписаниеПредупреждения(
		ГруппаОшибокКонвертацииПотеряДанных(),
		ТекстОшибки,
		ПутьКПолю);
		
	ОшибкиКонвертации.Добавить(ОписаниеОшибки);

КонецПроцедуры

Процедура УПД820_ДобавитьОшибкуКонвертацииОснованияПолномочийПредставителяИнойОрганизации(ОшибкиКонвертации)

	ТекстОшибки = "Изменился тип значения поля в формате ФНС 970";
	ПутьКПолю = "Документ / СвПродПер / СвПер / СвЛицПер / ИнЛицо / ПредОргПер / ОснПолнПредПер"; 
	
	ОписаниеОшибки = НовоеОписаниеПредупреждения(
		ГруппаОшибокКонвертацииПотеряДанных(),
		ТекстОшибки,
		ПутьКПолю);
		
	ОшибкиКонвертации.Добавить(ОписаниеОшибки);

КонецПроцедуры

Процедура УПД820_ДобавитьОшибкуКонвертацииОснованияПолномочийИнойОрганизации(ОшибкиКонвертации)

	ТекстОшибки = "Изменился тип значения поля в формате ФНС 970";
	ПутьКПолю = "Документ / СвПродПер / СвПер / СвЛицПер / ИнЛицо / ПредОргПер / ОснДоверОргПер"; 
	
	ОписаниеОшибки = НовоеОписаниеПредупреждения(
		ГруппаОшибокКонвертацииПотеряДанных(),
		ТекстОшибки,
		ПутьКПолю);
		
	ОшибкиКонвертации.Добавить(ОписаниеОшибки);

КонецПроцедуры

Процедура УПД820_ДобавитьОшибкуКонвертацииОснованияПолномочийФизЛица(ОшибкиКонвертации)

	ТекстОшибки = "Изменился тип значения поля в формате ФНС 970";
	ПутьКПолю = "Документ / СвПродПер / СвПер / СвЛицПер / ИнЛицо / ФЛПер / ОснДоверФЛ"; 
	
	ОписаниеОшибки = НовоеОписаниеПредупреждения(
		ГруппаОшибокКонвертацииПотеряДанных(),
		ТекстОшибки,
		ПутьКПолю);
		
	ОшибкиКонвертации.Добавить(ОписаниеОшибки);

КонецПроцедуры

Функция НовоеОписаниеПредупреждения(Группа, Текст, Дополнение)

	Результат = Новый Структура;
	Результат.Вставить("Группа", Группа);
	Результат.Вставить("Текст", Текст);
	Результат.Вставить("Дополнение", Дополнение);
	
	Возврат Результат;
	
КонецФункции

Функция ГруппаОшибокКонвертацииПредупреждение()
	Возврат "Предупреждение";
КонецФункции

Функция ГруппаОшибокКонвертацииПотеряДанных()
	Возврат "ПотеряДанных";	
КонецФункции

// КОНВЕРТАЦИЯ КОНТЕНТА 970 В 820

// Возвращает прото-контент титула продавца в формате 820, заполненный по данным контента в 970 формате
//
// Параметры:
//  Контент970	 - Структура - заполненная протоструктура ответного титула в 970 формате
//							(см. НовыйProtoОбъектXDTO("Utd970SellerContent"))
//  ОшибкиКонвертации - Массив - ошибки конвертации
//		* Структура - см. НовоеОписаниеОшибкиКонвертации()
//
// Возвращаемое значение:
//  ОбъектXDTO - заполненная протоструктура ответного титула в 820 формате
//							(см. НовыйProtoОбъектXDTO("Utd820SellerContent"))
//
Функция УПД970_Конвертировать_в820(Знач Контент970, ОшибкиКонвертации) Экспорт

	Контент820 = НовыйProtoОбъектXDTO("Utd820SellerContent");
	
	ИсключаемыеСвойства = Новый Массив;
	ИсключаемыеСвойства.Добавить("Function");
	ИсключаемыеСвойства.Добавить("CircumFormatInvoice");
	ИсключаемыеСвойства.Добавить("DocumentCreatorBase");
	
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Контент820, Контент970, ИсключаемыеСвойства);
	
	ФункцияДокумента = УПД970_КонвертироватьЗначениеФункцииДокумента_в820(Контент970);
	УстановитьЗначениеXDTO(Контент820, "Function", ФункцияДокумента);

	ЗначениеСпецОбстоятельств = УПД970_КонвертироватьСпецОбстоятельствФормированияДокумента_в820(Контент970);
	УстановитьЗначениеXDTO(Контент820, "CircumFormatInvoice", ЗначениеСпецОбстоятельств);
	
	DocumentCreatorBase = УПД970_КонвертироватьСведенияОСоставителеФайла_в820(Контент970);
	УстановитьЗначениеXDTO(Контент820, "DocumentCreatorBase", DocumentCreatorBase);
	
	MainAssignMonetaryClaim = УПД970_КонвертироватьОснованиеУступкиДенТребования_в820(Контент970);
	УстановитьЗначениеXDTO(Контент820, "MainAssignMonetaryClaim", MainAssignMonetaryClaim);
	
	SellerInfoCircumPublicProc = УПД970_КонвертироватьСведенияОЗакупкахДляГосНужд_в820(Контент970);
	УстановитьЗначениеXDTO(Контент820, "SellerInfoCircumPublicProc", SellerInfoCircumPublicProc);
	
	AdditionalInfoId = УПД970_КонвертироватьИнформационныеПоляФХЖ_в820(Контент970.AdditionalInfoId);
	УстановитьЗначениеXDTO(Контент820, "AdditionalInfoId", AdditionalInfoId);
	
	УПД970_КонвертироватьСведенияОбУчастникахЭДО_в820(Контент970, Контент820);
	
	Если Контент970.FactorInfo <> Неопределено
		И ЗначениеЗаполнено(Контент970.FactorInfo.Type) Тогда
		FactorInfo = УПД970_КонвертироватьСведенияУчастникаЭДО_в820(Контент970.FactorInfo);
		УстановитьЗначениеXDTO(Контент820, "FactorInfo", FactorInfo);
	КонецЕсли;
	
	Table = УПД970_КонвертироватьСведенияОТоварах_в820(Контент970.Table);
	УстановитьЗначениеXDTO(Контент820, "Table", Table);
	
	TransferInfo = УПД970_КонвертироватьСведенияОПередачеТоваров_в820(Контент970.TransferInfo);
	УстановитьЗначениеXDTO(Контент820, "TransferInfo", TransferInfo);
	
	Для Каждого PaymentDocument970 Из Контент970.PaymentDocuments Цикл
		PaymentDocument = УПД970_КонвертироватьСведенияОДокументеОплаты_в820(PaymentDocument970);
		Контент820.PaymentDocuments.Добавить(PaymentDocument);
	КонецЦикла;
	
	Для Каждого DocumentShipment970 Из Контент970.DocumentShipments Цикл
		DocumentShipment = УПД970_КонвертироватьСведенияОДокументеОтгрузки_в820(DocumentShipment970);
		Контент820.DocumentShipments.Добавить(DocumentShipment);
	КонецЦикла;
	
	Если Контент970.СлужебнаяИнформация <> Неопределено Тогда
		Для Каждого ОписаниеОшибки Из Контент970.СлужебнаяИнформация.ОшибкиВДанных Цикл
			НовоеОписаниеОшибки = НовыйProtoОбъектXDTO("ОшибкиВДанныхТип");
			ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(НовоеОписаниеОшибки, ОписаниеОшибки);
			Контент820.ОшибкиВДанных.Добавить(НовоеОписаниеОшибки);
		КонецЦикла;
	КонецЕсли;

	Возврат Контент820;
	
КонецФункции

Функция УПД970_КонвертироватьЗначениеФункцииДокумента_в820(Контент970)
	
	ФункцииУПД = Перечисление_ФункцииУПД();
	
	СоответствиеФункций = Новый Соответствие;
	
	СоответствиеФункций.Вставить(ФункцииУПД.СЧФДОП, "InvoiceAndBasic");
	СоответствиеФункций.Вставить(ФункцииУПД.СЧФ, "Invoice");
	СоответствиеФункций.Вставить(ФункцииУПД.ДОП, "Basic");
	
	Результат = СоответствиеФункций.Получить(Контент970.Function);
	
	Если Результат = Неопределено Тогда
		Результат = Контент970.Function;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьСведенияОСоставителеФайла_в820(Контент970)
	
	Перем Результат;
	
	Если Контент970.DocumentCreatorBase <> Неопределено
		И ЗначениеЗаполнено(Контент970.DocumentCreatorBase.Name) Тогда
		Результат = УПД970_ПолучитьСведенияОДокументеИзОбъектаXDTOСтрокой(Контент970.DocumentCreatorBase);
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьОснованиеУступкиДенТребования_в820(Контент970)
	
	Результат = НовыйProtoОбъектXDTO("TransferBase820");

	Если Контент970.MainAssignMonetaryClaim <> Неопределено Тогда
		
		УстановитьЗначениеXDTO(Результат, "DocumentName", Контент970.MainAssignMonetaryClaim.Name);
		УстановитьЗначениеXDTO(Результат, "DocumentNumber", Контент970.MainAssignMonetaryClaim.Number);
		УстановитьЗначениеXDTO(Результат, "DocumentDate", Контент970.MainAssignMonetaryClaim.Date);
		УстановитьЗначениеXDTO(Результат, "BaseDocumentId", Контент970.MainAssignMonetaryClaim.ID);
		УстановитьЗначениеXDTO(Результат, "DocumentInfo", Контент970.MainAssignMonetaryClaim.AdditionalInfo);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьСведенияОЗакупкахДляГосНужд_в820(Контент970)
	
	Перем Результат;
	
	Результат = НовыйProtoОбъектXDTO("SellerInfoCircumPublicProc");
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, Контент970.SellerInfoCircumPublicProc);
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьСведенияУчастникаЭДО_в820(УчастникЭДО970)
	
	Перем Результат;
	
	ВидыЮрФизЛиц = Перечисление_ВидыЮрФизЛиц();
	
	Если УчастникЭДО970.Type = ВидыЮрФизЛиц.ИностранныйГражданин Тогда
		Возврат Результат;
	КонецЕсли;
	
	Результат = НовыйProtoОбъектXDTO("ExtendedOrganizationInfo820");
	
	ИсключаемыеСвойства = Новый Массив;
	ИсключаемыеСвойства.Добавить("Phone");
	ИсключаемыеСвойства.Добавить("Email");
	
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, УчастникЭДО970, ИсключаемыеСвойства);
	
	Phone = ПолучитьЗначенияСпискаXDTOСтрокой(УчастникЭДО970.Phone);
	УстановитьЗначениеXDTO(Результат, "Phone", Phone);
	
	Email = ПолучитьЗначенияСпискаXDTOСтрокой(УчастникЭДО970.Email);
	УстановитьЗначениеXDTO(Результат, "Email", Email);
	
	Address970 = УчастникЭДО970.Address;
	Если Address970 <> Неопределено
		И ЗначениеЗаполнено(Address970.Type) Тогда
		
		Address = УПД970_КонвертироватьАдресУчастникаЭДО_в820(Address970, УчастникЭДО970.Type);
		УстановитьЗначениеXDTO(Результат, "Address", Address);
		
	КонецЕсли;
	
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, УчастникЭДО970.СлужебнаяИнформация);
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьСведенияГрузоотправителя_в820(Грузоотправитель970)
	
	Результат = НовыйProtoОбъектXDTO("Shipper820");
	
	УстановитьЗначениеXDTO(Результат, "SameAsSeller", Грузоотправитель970.SameAsSeller);
	
	Если Грузоотправитель970.OrganizationInfo <> Неопределено
		И Грузоотправитель970.SameAsSeller <> Истина Тогда
		
		Shipper820 = УПД970_КонвертироватьСведенияУчастникаЭДО_в820(Грузоотправитель970.OrganizationInfo);
		УстановитьЗначениеXDTO(Результат, "OrganizationInfo", Shipper820);

	КонецЕсли;
	
	Если Результат.SameAsSeller <> Истина
		И Результат.OrganizationInfo = Неопределено Тогда
		Результат = Неопределено;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьАдресУчастникаЭДО_в820(Address970, ВидЮрФизЛица)
	
	Перем Результат;
	
	Результат = НовыйProtoОбъектXDTO("AddressInfo");
	Результат.IsForeign = Истина;
	
	ВидыАдресов = Перечисление_ВидыАдресов();
	
	Если Address970.Type = ВидыАдресов.АдресРФ Тогда
		
		ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, Address970.AddressRF);
		Результат.IsForeign = Ложь;
		
	ИначеЕсли Address970.Type = ВидыАдресов.АдресСтрокой Тогда
		
		ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, Address970.AddressText);
		
	ИначеЕсли Address970.Type = ВидыАдресов.АдресГАР Тогда
		
		АдресСтрокой = ПредставлениеАдрГАР(Address970.AddressSAR);
		
		УстановитьЗначениеXDTO(Результат, "CountryCode", "643");
		УстановитьЗначениеXDTO(Результат, "AddressText", АдресСтрокой);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьСведенияОТоварах_в820(Table970)
	
	Перем Результат;
	
	Если Table970 = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	Результат = НовыйProtoОбъектXDTO("InvoiceTable820");
	
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, Table970);
	
	Для Каждого Item970 Из Table970.Items Цикл
		
		Item820 = НовыйProtoОбъектXDTO("Item820");
		Результат.Items.Добавить(Item820);
		
		ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Item820, Item970);
		
		Для Каждого CustomDeclaration970 Из Item970.CustomDeclarations Цикл
			НоваяСтрока = НовыйProtoОбъектXDTO("CustomDeclaration820");
			ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(НоваяСтрока, CustomDeclaration970);
			Item820.CustomDeclarations.Добавить(НоваяСтрока);
		КонецЦикла;
		
		Для Каждого ItemTracingInfo970 Из Item970.ItemTracingInfos Цикл
			НоваяСтрока = НовыйProtoОбъектXDTO("ItemTracingInfo");
			ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(НоваяСтрока, ItemTracingInfo970);
			Item820.ItemTracingInfos.Добавить(НоваяСтрока);
		КонецЦикла;
		
		КоличествоОСУ = 0;
		Для Каждого ItemIdentificationNumber970 Из Item970.ItemIdentificationNumbers Цикл
			
			НоваяСтрока = НовыйProtoОбъектXDTO("ItemIdentificationNumber");
			ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(НоваяСтрока, ItemIdentificationNumber970);
			
			Для Каждого Unit Из ItemIdentificationNumber970.Units Цикл
				НоваяСтрока.Units.Добавить(Unit);
			КонецЦикла;
			
			Для Каждого PackageId Из ItemIdentificationNumber970.PackageIds Цикл
				НоваяСтрока.PackageIds.Добавить(PackageId);
			КонецЦикла;
			
			Если НоваяСтрока.Units.Количество() > 0
				Или НоваяСтрока.PackageIds.Количество() > 0
				Или ЗначениеЗаполнено(НоваяСтрока.TransPackageId) Тогда
				
				Item820.ItemIdentificationNumbers.Добавить(НоваяСтрока);
				
			КонецЕсли;
			
			Если ЗначениеЗаполнено(ItemIdentificationNumber970.MarkedItemsQuantity) Тогда
				КоличествоОСУ = КоличествоОСУ + ItemIdentificationNumber970.MarkedItemsQuantity;
			КонецЕсли;
			
		КонецЦикла;
		
		// Код ОСУ
		Если ЗначениеЗаполнено(Item970.GTIN) И КоличествоОСУ > 0 Тогда
			КодОСУ = НовыйКодОСУ(Item970.GTIN, КоличествоОСУ);
			НоваяСтрока = НовыйProtoОбъектXDTO("ItemIdentificationNumber");
			НоваяСтрока.PackageIds.Добавить(КодОСУ);
			Item820.ItemIdentificationNumbers.Добавить(НоваяСтрока);
		КонецЕсли;
		
		Для Каждого AdditionalInfoItem970 Из Item970.StructedAdditionalInfos Цикл
			AdditionalInfoItem820 = УПД970_КонвертироватьAdditionalInfoItem_в820(AdditionalInfoItem970);
			Item820.StructedAdditionalInfos.Добавить(AdditionalInfoItem820);
		КонецЦикла;
		
		ItemСлужебнаяИнформация = НовыйProtoОбъектXDTO("ItemСлужебнаяИнформация");
		ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(ItemСлужебнаяИнформация, Item970.СлужебнаяИнформация);
		УстановитьЗначениеXDTO(Item820, "СлужебнаяИнформация", ItemСлужебнаяИнформация);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьAdditionalInfoItem_в820(AdditionalInfoItem970)
	
	Результат = НовыйProtoОбъектXDTO("AdditionalInfoItem");
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, AdditionalInfoItem970);
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьСведенияОПередачеТоваров_в820(TransferInfo970)
	
	Перем Результат;
	
	Если TransferInfo970 = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	Результат = НовыйProtoОбъектXDTO("TransferInfo820");
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, TransferInfo970);
	
	Employee = УПД970_КонвертироватьСведенияОСотруднике_в820(TransferInfo970.Employee);
	УстановитьЗначениеXDTO(Результат, "Employee", Employee);

	AdditionalInfoId = УПД970_КонвертироватьИнформационныеПоляФХЖ_в820(TransferInfo970.AdditionalInfoId);
	УстановитьЗначениеXDTO(Результат, "AdditionalInfoId", AdditionalInfoId);
	
	Если TransferInfo970.WithoutTransferBase = Истина Тогда
		НоваяСтрока = НовыйЭлементСпискаXDTO(Результат.TransferBases);
		НоваяСтрока.DocumentName = БезДокументаОснования();
	Иначе
		
		Для Каждого TransferBase970 Из TransferInfo970.TransferBases Цикл
		
			TransferBase = УПД970_КонвертироватьРеквизитыОснованияОтгрузки_в820(TransferBase970);
			Результат.TransferBases.Добавить(TransferBase);
			
		КонецЦикла;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьИнформационныеПоляФХЖ_в820(AdditionalInfoId970)
	
	Перем Результат;
	
	Если AdditionalInfoId970 = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	Результат = НовыйProtoОбъектXDTO("AdditionalInfoId");
	
	УстановитьЗначениеXDTO(Результат, "InfoFileId", AdditionalInfoId970.InfoFileId);
	
	Для Каждого AdditionalInfoItem970 Из AdditionalInfoId970.StructedAdditionalInfos Цикл
		AdditionalInfoItem = УПД970_КонвертироватьAdditionalInfoItem_в820(AdditionalInfoItem970);
		Результат.StructedAdditionalInfos.Добавить(AdditionalInfoItem);
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьРеквизитыОснованияОтгрузки_в820(TransferBase970)
	
	Результат = НовыйProtoОбъектXDTO("TransferBase820");
	
	УстановитьЗначениеXDTO(Результат, "DocumentName", TransferBase970.Name);
	УстановитьЗначениеXDTO(Результат, "DocumentNumber", TransferBase970.Number);
	УстановитьЗначениеXDTO(Результат, "DocumentDate", TransferBase970.Date);
	УстановитьЗначениеXDTO(Результат, "BaseDocumentId", TransferBase970.ID);
	УстановитьЗначениеXDTO(Результат, "DocumentInfo", TransferBase970.AdditionalInfo);
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьСведенияОСотруднике_в820(Employee970)

	Перем Результат;
	
	Если Employee970 = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	Результат = НовыйProtoОбъектXDTO("Employee");
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, Employee970);
	
	Результат.EmployeeBase = "Должностные обязанности";
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьСведенияОДокументеОплаты_в820(PaymentDocument970)
	
	Результат = НовыйProtoОбъектXDTO("PaymentDocumentInfo");
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, PaymentDocument970);
	
	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьСведенияОДокументеОтгрузки_в820(DocumentShipment970)
	
	Результат = НовыйProtoОбъектXDTO("DocumentShipment");
	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Результат, DocumentShipment970);

	Возврат Результат;
	
КонецФункции

Функция УПД970_ПолучитьСведенияОДокументеИзОбъектаXDTOСтрокой(СведенияОДокументе)
	
	Результат = СформироватьПредставлениеДокумента(
		СведенияОДокументе.Name,
		СведенияОДокументе.Number,
		СведенияОДокументе.Date);

	Возврат Результат;
	
КонецФункции

Функция УПД970_КонвертироватьСпецОбстоятельствФормированияДокумента_в820(Контент970)
	
	Перем Результат;
	
	Значение = Контент970.CircumFormatInvoice;
	ФункцияДокумента = Контент970.Function;

	ФункцииУПД = Перечисление_ФункцииУПД();
	
	Если ФункцияДокумента = ФункцииУПД.СЧФ Тогда
		
		Результат = "";
		
		Если Значение = "2"
			Или Значение = "3" Тогда
			Результат = Значение;
		КонецЕсли;
		
	Иначе
		
		Если Значение = "00005" Тогда
			Результат = "4";
		ИначеЕсли Значение = "00006" Тогда
			Результат = "5";
		ИначеЕсли Значение = "00007" Тогда
			Результат = "6";
		ИначеЕсли Значение = "00008" Тогда
			Результат = "7";
		ИначеЕсли Значение = "00009" Тогда
			Результат = "8";
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура УПД970_КонвертироватьСведенияОбУчастникахЭДО_в820(Контент970, Контент820)

	Для Каждого Seller970 Из Контент970.Sellers Цикл
		
		Seller = УПД970_КонвертироватьСведенияУчастникаЭДО_в820(Seller970);
		Если Seller <> Неопределено Тогда
			Контент820.Sellers.Добавить(Seller);
		КонецЕсли;
		
	КонецЦикла;
	
	Для Каждого Buyer970 Из Контент970.Buyers Цикл
		
		Buyer = УПД970_КонвертироватьСведенияУчастникаЭДО_в820(Buyer970);
		Если Buyer <> Неопределено Тогда
			Контент820.Buyers.Добавить(Buyer);
		КонецЕсли;
		
	КонецЦикла;
	
	Контент820.Consignees.Очистить();
	
	Для Каждого Consignee970 Из Контент970.Consignees Цикл
		
		Consignee = УПД970_КонвертироватьСведенияУчастникаЭДО_в820(Consignee970);
		Если Consignee <> Неопределено Тогда
			Контент820.Consignees.Добавить(Consignee);
		КонецЕсли;
		
	КонецЦикла;
	
	Если Контент820.Consignees.Количество() = 0 Тогда
		Контент820.Consignees.Добавить(НовыйProtoОбъектXDTO("ExtendedOrganizationInfo820"));
	КонецЕсли;
	
	Контент820.Shippers.Очистить();
	
	Для Каждого Shipper970 Из Контент970.Shippers Цикл
		Shipper = УПД970_КонвертироватьСведенияГрузоотправителя_в820(Shipper970);
		Если Shipper <> Неопределено Тогда
			Контент820.Shippers.Добавить(Shipper);
		КонецЕсли;
	КонецЦикла;
	
	Если Контент820.Shippers.Количество() = 0 Тогда
		Shipper = НовыйProtoОбъектXDTO("Shipper820");
		Shipper.OrganizationInfo = НовыйProtoОбъектXDTO("ExtendedOrganizationInfo820");
		Контент820.Shippers.Добавить(Shipper);
	КонецЕсли;
	
	Если Контент970.FactorInfo <> Неопределено
		И ЗначениеЗаполнено(Контент970.FactorInfo.Type) Тогда
		
		FactorInfo = УПД970_КонвертироватьСведенияУчастникаЭДО_в820(Контент970.FactorInfo);
		УстановитьЗначениеXDTO(Контент820, "FactorInfo", FactorInfo);
		
	КонецЕсли;

КонецПроцедуры

// МЕТОДЫ РАБОТЫ С XDTO

Процедура ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Приемник, Источник, Знач ИсключаемыеСвойства = Неопределено)
	
	Если Источник = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого СвойствоПриемника Из Приемник.Свойства() Цикл
		
		ИмяСвойства = СвойствоПриемника.Имя;
		
		Если ИсключаемыеСвойства <> Неопределено
			И ИсключаемыеСвойства.Найти(ИмяСвойства) <> Неопределено Тогда
			Продолжить;
		КонецЕсли;
		
		ЗначениеИсточника = ЗначениеСвойстваОбъектаXDTO(Источник, ИмяСвойства);
		Если Не ЭтоОбъектXDTO(ЗначениеИсточника)
			И Не ЭтоСписокXDTO(ЗначениеИсточника) Тогда
			
			УстановитьЗначениеXDTO(Приемник, ИмяСвойства, ЗначениеИсточника);
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

// Возвращает значения списка XDTO строкой, разделенные запятой
//
// Параметры:
//  Список	 - СписокXDTO - список, содержащий строковые значения
// 
// Возвращаемое значение:
//  Строка - значения списка строкой
//
Функция ПолучитьЗначенияСпискаXDTOСтрокой(Список)
	
	МассивЗначений = Новый Массив;
	
	Для Каждого ЗначениеСписка Из Список Цикл
		МассивЗначений.Добавить(ЗначениеСписка);
	КонецЦикла;
	
	Результат = Общее_СоединитьСтроку(МассивЗначений, ", ");
	
	Возврат Результат;

КонецФункции

Функция ЭтоОбъектXDTO(ПроверяемоеЗначение)

	Результат = (ТипЗнч(ПроверяемоеЗначение) = Тип("ОбъектXDTO"));
	Возврат Результат;
	
КонецФункции

Функция ЭтоСписокXDTO(ПроверяемоеЗначение)

	Результат = (ТипЗнч(ПроверяемоеЗначение) = Тип("СписокXDTO"));
	Возврат Результат;
	
КонецФункции

Функция ЗначениеСвойстваОбъектаXDTO(ОбъектXDTO, ИмяСвойства)
	
	Перем Результат;
	
	Свойство = ОбъектXDTO.Свойства().Получить(ИмяСвойства);
	Если Свойство <> Неопределено Тогда
		
		Если Свойство.ВерхняяГраница = -1 Тогда
			Результат = ОбъектXDTO.ПолучитьСписок(Свойство.Имя);
		Иначе
			Результат = ОбъектXDTO.Получить(Свойство.Имя);
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// СЛУЖЕБНЫЕ МЕТОДЫ

Функция ЭтоКодОСУ(ПроверяемоеЗначение)

	Результат = (Лев(ПроверяемоеЗначение, 2) = "02"
			  И Сред(ПроверяемоеЗначение, 17, 2) = "37");
			
	Возврат Результат;
	
КонецФункции

Функция РазложитьКодОСУ(КодОСУ)
	
	GTIN = Сред(КодОСУ, 3, 14);
	КолвоСтрокой = Сред(КодОСУ, 19);
	
	Количество = XMLЗначение(Тип("Число"), КолвоСтрокой);
	
	Результат = Новый Структура;
	Результат.Вставить("GTIN", GTIN);
	Результат.Вставить("Количество", Количество);
	
	Возврат Результат;
	
КонецФункции

Функция НовыйКодОСУ(GTIN, Количество)
	
	ЧастиКодаОСУ = Новый Массив;
	ЧастиКодаОСУ.Добавить("02");
	ЧастиКодаОСУ.Добавить(GTINКаноническогоВида(GTIN));
	ЧастиКодаОСУ.Добавить("37");
	ЧастиКодаОСУ.Добавить(XMLСтрока(Количество));
	
	Результат = Общее_СоединитьСтроку(ЧастиКодаОСУ);
	
	Возврат Результат;
		
КонецФункции

Функция БезДокументаОснования()
	Возврат "Без документа-основания";
КонецФункции

Функция ПредставлениеАдрГАР(AddressInfo) Экспорт
	
	Регион = НаименованиеРегиона(AddressInfo.RegionCode);

	ЧастиАдреса = Новый Массив;
	
	ЧастиАдреса.Добавить(AddressInfo.ZipCode);
	ЧастиАдреса.Добавить(Регион);
	
	ВидМуниципальногоРайона = НаименованиеВидаМуниципальногоРайонаПоКоду(AddressInfo.MunicipalDistrictCode);
	МуниципальныйРайон = СоединитьЭлементыАдресногоОбъекта(ВидМуниципальногоРайона, AddressInfo.MunicipalDistrict);
	ЧастиАдреса.Добавить(МуниципальныйРайон);
	
	ВидПоселения = НаименованиеВидаПоселенияПоКоду(AddressInfo.SettlementCode);
	ГородПоселение = СоединитьЭлементыАдресногоОбъекта(ВидПоселения, AddressInfo.Settlement);
	ЧастиАдреса.Добавить(ГородПоселение);
	
	НаселенныйПункт = СоединитьЭлементыАдресногоОбъекта(AddressInfo.LocalityType, AddressInfo.Locality);
	ЧастиАдреса.Добавить(НаселенныйПункт);
	
	ЭлементПланировочнойСтруктуры = СоединитьЭлементыАдресногоОбъекта(AddressInfo.PlanningElementType, AddressInfo.PlanningElement);
	ЧастиАдреса.Добавить(ЭлементПланировочнойСтруктуры);
	
	ЭлементУличнойДорожнойСети = СоединитьЭлементыАдресногоОбъекта(AddressInfo.StreetElementType, AddressInfo.StreetElement);
	ЧастиАдреса.Добавить(ЭлементУличнойДорожнойСети);
	
	ЗемельныйУчасток = AddressInfo.LandPlot;
	ЧастиАдреса.Добавить(ЗемельныйУчасток);
	
	Для Каждого ЗданиеСооружение Из AddressInfo.Buildings Цикл
		ЭлЗданиеСооружение = СоединитьЭлементыАдресногоОбъекта(ЗданиеСооружение.Type, ЗданиеСооружение.Number);
		ЧастиАдреса.Добавить(ЭлЗданиеСооружение);
	КонецЦикла;
	
	ПомещениеВЗдании = СоединитьЭлементыАдресногоОбъекта(AddressInfo.HouseType, AddressInfo.House);
	ЧастиАдреса.Добавить(ПомещениеВЗдании);
	
	ПомещениеВКвартире = СоединитьЭлементыАдресногоОбъекта(AddressInfo.ApartmentType, AddressInfo.Apartment);
	ЧастиАдреса.Добавить(ПомещениеВКвартире);

	Разделитель = ", ";
	Результат = Общее_СоединитьСтроку(ЧастиАдреса, Разделитель);
	
	Возврат Результат;
	
КонецФункции

Функция НаименованиеВидаМуниципальногоРайонаПоКоду(Код)
	
	Если Код = "1" Тогда
		Результат = "муниципальный район";	
	ИначеЕсли Код = "2" Тогда
		Результат = "городской округ";
	ИначеЕсли Код = "3" Тогда
		Результат = "внутригородская территория города федерального значения";
	ИначеЕсли Код = "4" Тогда
		Результат = "муниципальный округ";
	ИначеЕсли Код = "5" Тогда
		Результат = "федеральная территория";
	Иначе
		Результат = "";
	КонецЕсли;
	
	Возврат Результат;	
	
КонецФункции

Функция НаименованиеВидаПоселенияПоКоду(Код)
	
	Если Код = "1" Тогда
		Результат = "городское поселение";	
	ИначеЕсли Код = "2" Тогда
		Результат = "сельское поселение";
	ИначеЕсли Код = "3" Тогда
		Результат = "межселенная территория в составе муниципального района";
	ИначеЕсли Код = "4" Тогда
		Результат = "внутригородской район городского округа";
	Иначе
		Результат = "";
	КонецЕсли;
	
	Возврат Результат;	
	
КонецФункции

Функция СоединитьЭлементыАдресногоОбъекта(Элемент1, Элемент2)
	
	ЭлементыОбъекта = Новый Массив;
	ЭлементыОбъекта.Добавить(Элемент1);
	ЭлементыОбъекта.Добавить(Элемент2);
	
	Результат = Общее_СоединитьСтроку(ЭлементыОбъекта, " ");
	
	Возврат Результат;
	
КонецФункции

Функция СформироватьПредставлениеДокумента(Наименование, Номер, Дата)

	Результат = Общее_ПодставитьПараметрыВСтроку("%1 №%2 от %3",
		Наименование,
		Номер,
		Формат(Дата, "ДФ=dd.MM.yyyy"));

	Возврат Результат;
	
КонецФункции

Функция ИмяДокументаОтгрузкиУПД()
	Возврат "Универсальный передаточный документ";
КонецФункции

Функция АдаптацияНазванияРегиона(Название)
	
	Если (Название = "Кемеровская область - Кузбасс обл")
		Или (Название = "Кемеровская область - Кузбасс область")
		Или (Название = "Кемеровская область")
		Или (Название = ИсторическоеНаименованиеКемеровскойОбласти()) Тогда
		
		Возврат ДействующиеНаименованиеКемеровскойОбласти();
		
	КонецЕсли;
	
	Название = СокрЛП(Название);
	Если СтрЗаканчиваетсяНаЛок(Название, ".") Тогда
		Название = Лев(Название, СтрДлина(Название) - 1);
	КонецЕсли;
	
	Возврат Название;
	
КонецФункции

Функция ДействующиеНаименованиеКемеровскойОбласти()
	
	Возврат "Кемеровская область - Кузбасс";
	
КонецФункции

Функция ИсторическоеНаименованиеКемеровскойОбласти()
	
	Возврат "Кемеровская обл";
	
КонецФункции

// *************************************************************************
// СТАВКИ НДС в proto.xsd (TaxRate)

// Преобразует XML-представление ставки НДС в значение прото
//
// Параметры:
//  НалСт - Строка - значение поля НалСт из XML
// 
// Возвращаемое значение:
//  Строка - значение ставки в терминах proto.xsd (см. Перечисление_TaxRate())
//
Функция TaxRate_Из_НалСт(Знач НалСт)
	
	Маппинг = Новый Соответствие;
	Маппинг.Вставить(НалСт_0(), TaxRate_0());
	Маппинг.Вставить(НалСт_5(), TaxRate_5());
	Маппинг.Вставить(НалСт_7(), TaxRate_7());
	Маппинг.Вставить(НалСт_9_09(), TaxRate_9_09());
	Маппинг.Вставить(НалСт_10(), TaxRate_10());
	Маппинг.Вставить(НалСт_16_67(), TaxRate_16_67());
	Маппинг.Вставить(НалСт_18(), TaxRate_18());
	Маппинг.Вставить(НалСт_20(), TaxRate_20());
	Маппинг.Вставить(НалСт_22(), TaxRate_22());
	Маппинг.Вставить(НалСт_5_105(), TaxRate_5_105());
	Маппинг.Вставить(НалСт_7_107(), TaxRate_7_107());
	Маппинг.Вставить(НалСт_10_110(), TaxRate_10_110());
	Маппинг.Вставить(НалСт_18_118(), TaxRate_18_118());
	Маппинг.Вставить(НалСт_20_120(), TaxRate_20_120());
	Маппинг.Вставить(НалСт_22_122(), TaxRate_22_122());
	Маппинг.Вставить(НалСт_БезНДС(), TaxRate_БезНДС());
	Маппинг.Вставить(НалСт_НДСИсчисляетсяНалоговымАгентом(), TaxRate_ИсчНалАг());
	
	Результат = Маппинг.Получить(НалСт);
	
	Если Не ЗначениеЗаполнено(Результат) Тогда
		
		Ошибка = Общее_ПодставитьПараметрыВСтроку(
			"InvalidValue: Преобразование НалСт в TaxRate не выполнено (%1)",
			НалСт
		);
		
		ВызватьИсключение Ошибка;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Определяет значение TaxRate по сумме без налога и сумме налога
//
// Параметры:
//  СуммаБезНДС - Число - сумма без налога
//  СуммаНДС - Число - сумма налога
// 
// Возвращаемое значение:
//  Строка - значение TaxRate
//
Функция TaxRate_Рассчитать(СуммаБезНДС, СуммаНДС)
	
	Если НЕ ЗначениеЗаполнено(СуммаБезНДС) ИЛИ НЕ ЗначениеЗаполнено(СуммаНДС) Тогда
		Возврат TaxRate_БезНДС();
	КонецЕсли;
	
	РасчетнаяСтавка = 100 * СуммаНДС / СуммаБезНДС;
	
	Если РасчетнаяСтавка < 5 Тогда
		Возврат TaxRate_БезНДС();
	ИначеЕсли РасчетнаяСтавка < 14 Тогда
		Возврат TaxRate_10();
	ИначеЕсли РасчетнаяСтавка < 21 Тогда
		Возврат TaxRate_20();
	Иначе
		Возврат TaxRate_22();
	КонецЕсли;
	
КонецФункции

// Проверяет, входит ли указанное значение в список допустимых значений типа "TaxRate"
//
// Параметры:
//  TaxRate - Произвольный - Проверяемое значение
// 
// Возвращаемое значение:
//  Булево - Истина, если это строка из допустимого перечня ставок.
//
Функция TaxRate_ЭтоВалидноеЗначение(Знач TaxRate)
	
	Если ТипЗнч(TaxRate) <> Тип("Строка") Тогда
		Возврат Ложь;
	КонецЕсли;
	
	ДопустимыеЗначения = Новый Массив;
	ДопустимыеЗначения.Добавить(ВРег(TaxRate_0()));
	ДопустимыеЗначения.Добавить(ВРег(TaxRate_5()));
	ДопустимыеЗначения.Добавить(ВРег(TaxRate_7()));
	ДопустимыеЗначения.Добавить(ВРег(TaxRate_10()));
	ДопустимыеЗначения.Добавить(ВРег(TaxRate_20()));
	ДопустимыеЗначения.Добавить(ВРег(TaxRate_22()));
	ДопустимыеЗначения.Добавить(ВРег(TaxRate_5_105()));
	ДопустимыеЗначения.Добавить(ВРег(TaxRate_7_107()));
	ДопустимыеЗначения.Добавить(ВРег(TaxRate_10_110()));
	ДопустимыеЗначения.Добавить(ВРег(TaxRate_20_120()));
	ДопустимыеЗначения.Добавить(ВРег(TaxRate_22_122()));
	ДопустимыеЗначения.Добавить(ВРег(TaxRate_БезНДС()));
	ДопустимыеЗначения.Добавить(ВРег(TaxRate_ИсчНалАг()));
	
	Результат = (ДопустимыеЗначения.Найти(ВРег(TaxRate)) <> Неопределено);
	
	Возврат Результат;
	
КонецФункции

// Допустимые значения типа "TaxRate" в proto.xsd
// 
// Возвращаемое значение:
//  ФиксированнаяСтруктура.
//
Функция Перечисление_TaxRate()
	
	ЗначенияСтавок = Новый Структура;
	ЗначенияСтавок.Добавить(TaxRate_0(), TaxRate_0());
	ЗначенияСтавок.Добавить(TaxRate_5(), TaxRate_5());
	ЗначенияСтавок.Добавить(TaxRate_7(), TaxRate_7());
	ЗначенияСтавок.Добавить(TaxRate_10(), TaxRate_10());
	ЗначенияСтавок.Добавить(TaxRate_18(), TaxRate_18());
	ЗначенияСтавок.Добавить(TaxRate_20(), TaxRate_20());
	ЗначенияСтавок.Добавить(TaxRate_22(), TaxRate_22());
	ЗначенияСтавок.Добавить(TaxRate_5_105(), TaxRate_5_105());
	ЗначенияСтавок.Добавить(TaxRate_7_107(), TaxRate_7_107());
	ЗначенияСтавок.Добавить(TaxRate_10_110(), TaxRate_10_110());
	ЗначенияСтавок.Добавить(TaxRate_18_118(), TaxRate_18_118());
	ЗначенияСтавок.Добавить(TaxRate_20_120(), TaxRate_20_120());
	ЗначенияСтавок.Добавить(TaxRate_22_122(), TaxRate_22_122());
	ЗначенияСтавок.Добавить(TaxRate_БезНДС(), TaxRate_БезНДС());
	ЗначенияСтавок.Добавить(TaxRate_ИсчНалАг(), TaxRate_ИсчНалАг());
	
	Результат = Новый ФиксированнаяСтруктура(ЗначенияСтавок);
	
	Возврат Результат;
	
КонецФункции

// Проверяет, что указанный параметр соответствует значению "ИсчНалАг".
//
// Параметры:
//  TaxRate - Произвольный - проверяемое значение.
// 
// Возвращаемое значение:
//  Булево - Истина, если это строка "ИсчНалАг" (без учета регистра)
//
Функция TaxRate_ЭтоЗначениеИсчНалАг(Знач TaxRate)
	
	Результат = (ВРег(TaxRate) = ВРег(TaxRate_ИсчНалАг()));
	Возврат Результат;
	
КонецФункции

Функция TaxRate_ЭтоЗначениеБезНДС(Знач TaxRate)
	
	Результат = (ВРег(TaxRate) = ВРег(TaxRate_БезНДС()));
	Возврат Результат;
	
КонецФункции

Функция TaxRate_0()
	Возврат "0";
КонецФункции

Функция TaxRate_5()
	Возврат "5";
КонецФункции

Функция TaxRate_7()
	Возврат "7";
КонецФункции

Функция TaxRate_10()
	Возврат "10";
КонецФункции

Функция TaxRate_18()
	Возврат "18";
КонецФункции

Функция TaxRate_20()
	Возврат "20";
КонецФункции

Функция TaxRate_5_105()
	Возврат "5/105";
КонецФункции

Функция TaxRate_7_107()
	Возврат "7/107";
КонецФункции

Функция TaxRate_10_110()
	Возврат "10/110";
КонецФункции

Функция TaxRate_18_118()
	Возврат "18/118";
КонецФункции

Функция TaxRate_20_120()
	Возврат "20/120";
КонецФункции

Функция TaxRate_БезНДС()
	Возврат "без НДС";
КонецФункции

Функция TaxRate_ИсчНалАг()
	Возврат "ИсчНалАг";
КонецФункции

// *************************************************************************
// СТАВКИ НДС в XML (НалСт)

// Преобразует значение ставки НДС из прото в представление для XML
//
// Параметры:
//  TaxRate - Строка - значение ставки в терминах proto.xsd (см. Перечисление_TaxRate())
// 
// Возвращаемое значение:
//  Строка - представление ставки в XML-файле
//
Функция НалСт_Из_TaxRate(Знач TaxRate)
	
	Маппинг = Новый Соответствие;
	Маппинг.Вставить(TaxRate_0(), НалСт_0());
	Маппинг.Вставить(TaxRate_5(), НалСт_5());
	Маппинг.Вставить(TaxRate_7(), НалСт_7());
	Маппинг.Вставить(TaxRate_9_09(), НалСт_9_09());
	Маппинг.Вставить(TaxRate_10(), НалСт_10());
	Маппинг.Вставить(TaxRate_16_67(), НалСт_16_67());
	Маппинг.Вставить(TaxRate_18(), НалСт_18());
	Маппинг.Вставить(TaxRate_20(), НалСт_20());
	Маппинг.Вставить(TaxRate_22(), НалСт_22());
	Маппинг.Вставить(TaxRate_5_105(), НалСт_5_105());
	Маппинг.Вставить(TaxRate_7_107(), НалСт_7_107());
	Маппинг.Вставить(TaxRate_10_110(), НалСт_10_110());
	Маппинг.Вставить(TaxRate_18_118(), НалСт_18_118());
	Маппинг.Вставить(TaxRate_20_120(), НалСт_20_120());
	Маппинг.Вставить(TaxRate_22_122(), НалСт_22_122());
	Маппинг.Вставить(TaxRate_БезНДС(), НалСт_БезНДС());
	Маппинг.Вставить(TaxRate_ИсчНалАг(), НалСт_НДСИсчисляетсяНалоговымАгентом());
	
	Результат = Маппинг.Получить(TaxRate);
	
	Если Не ЗначениеЗаполнено(Результат) Тогда
		// исключение не генерим, т.к. полагаемся на валидацию документа перед отправкой
		Результат = TaxRate;
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция НалСт_0()
	Возврат "0%";
КонецФункции

Функция НалСт_5()
	Возврат "5%";
КонецФункции

Функция НалСт_7()
	Возврат "7%";
КонецФункции

Функция НалСт_10()
	Возврат "10%";
КонецФункции

Функция НалСт_18()
	Возврат "18%";
КонецФункции

Функция НалСт_20()
	Возврат "20%";
КонецФункции

Функция НалСт_5_105()
	Возврат "5/105";
КонецФункции

Функция НалСт_7_107()
	Возврат "7/107";
КонецФункции

Функция НалСт_10_110()
	Возврат "10/110";
КонецФункции

Функция НалСт_18_118()
	Возврат "18/118";
КонецФункции

Функция НалСт_20_120()
	Возврат "20/120";
КонецФункции

Функция НалСт_БезНДС()
	Возврат "без НДС";
КонецФункции

Функция НалСт_НДСИсчисляетсяНалоговымАгентом()
	Возврат "НДС исчисляется налоговым агентом";
КонецФункции

Функция XML_ЭтоТитулПродавцаНакладнаяРБ01(ОбъектXDTO)
	
	Результат = Ложь;
	
	ЭтоТитулПокупателяРБ = НЕ ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ИдФайл")
		И ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "DeliveryNoteType")
		И ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "FunctionCode");
	
	Если ЭтоТитулПокупателяРБ Тогда
		
		Результат = (ОбъектXDTO.DeliveryNoteType = "701")
			И (ОбъектXDTO.FunctionCode = "9");
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция XML_ЭтоТитулПокупателяНакладнаяРБ01(ОбъектXDTO)
	
	Результат = Ложь;
	
	ЭтоТитулПокупателяРБ = НЕ ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "ИдФайл")
		И ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "DeliveryNoteType")
		И ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОбъектXDTO, "FunctionCode");
	
	Если ЭтоТитулПокупателяРБ Тогда
		
		Результат = (ОбъектXDTO.DeliveryNoteType = "701")
			И (ОбъектXDTO.FunctionCode = "11");
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура Заполнить_ProformaInvoice29SellerContent(Контент, Параметры)
	// BSLLS:Typo-off
	
	Документ = Параметры.Документ;
	
	СодСч = Документ.СодСч;
	
	УстановитьЗначениеXDTO(Контент, "DocumentDate", ДатаXML_в_Дата1С(СодСч.ДатаДок));
	УстановитьЗначениеXDTO(Контент, "DocumentNumber", СодСч.НомерДок);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодСч, "ДенИзм") Тогда
		
		ДенИзм = СодСч.ДенИзм;
		
		КурсВал = ПолучитьЗначениеXDTO(ДенИзм, "КурсВал");
		ОпрУЕ = ПолучитьЗначениеXDTO(ДенИзм, "ОпрУЕ");
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ДенИзм, "КодОКВ") Тогда
			УстановитьЗначениеXDTO(Контент, "Currency", ДенИзм.КодОКВ);
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Контент, "CurrencyRate", КурсВал);
		УстановитьЗначениеXDTO(Контент, "ConventionalUnitDefinition", ОпрУЕ);
		
		УЕ = ПолучитьЗначениеXDTO(ДенИзм, "УЕ");
		Если Нрег(УЕ) = "у.е." Тогда
			УстановитьЗначениеXDTO(Контент, "IsConventionalUnit", "true");
		Иначе
			УстановитьЗначениеXDTO(Контент, "IsConventionalUnit", "false");
		КонецЕсли;
		
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "TotalItems", ПолучитьЗначениеXDTO(СодСч, "ВсегоНаим"));
	
	Для Каждого Основание Из СписокXDTOИзЭлемента(СодСч, "Основание") Цикл
		ProformaInvoiceBase = НовыйЭлементСпискаXDTO(Контент.ProformaInvoiceBases);
		Заполнить_ProformaInvoice29Base(ProformaInvoiceBase, Основание);
	КонецЦикла;
	
	Для Каждого ВидОбяз Из СписокXDTOИзЭлемента(СодСч, "ВидОбяз") Цикл
		CommitmentType = НовыйЭлементСпискаXDTO(Контент.CommitmentTypes);
		
		УстановитьЗначениеXDTO(CommitmentType, "CommitmentTypeCode", ПолучитьЗначениеXDTO(ВидОбяз, "КодВидОбяз"));
		УстановитьЗначениеXDTO(CommitmentType, "CommitmentTypeName", ПолучитьЗначениеXDTO(ВидОбяз, "НаимВидОбязм"));
	КонецЦикла;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодСч, "ДопСв") Тогда
		Заполнить_ProformaInvoice29AdditionalInfo(Контент.ProformaInvoiceAdditionalInfo, СодСч.ДопСв);
	КонецЕсли;
	
	Для Каждого ИнфПол Из СписокXDTOИзЭлемента(Документ, "ИнфПол") Цикл
		AdditionalInfoId = НовыйЭлементСпискаXDTO(Контент.AdditionalInfoId);
		
		Заполнить_AdditionalInfoId(AdditionalInfoId, ИнфПол);
	КонецЦикла;
	
	Заполнить_ExtendedOrganizationInfo29(Контент.Seller, СодСч.СвПрод);
	Заполнить_ExtendedOrganizationInfo29(Контент.Buyer, СодСч.СвПокуп);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодСч, "СвПлат") Тогда
		Заполнить_ExtendedOrganizationInfo29(Контент.Payer, СодСч.СвПлат);
	КонецЕсли;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодСч, "ПолучДнСр") Тогда
		Заполнить_ExtendedOrganizationInfo29(Контент.MoneyRecipient, СодСч.ПолучДнСр);
	КонецЕсли;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодСч, "ГрузОт") Тогда
		Заполнить_ExtendedOrganizationInfo29(Контент.Shipper, СодСч.ГрузОт);
	КонецЕсли;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(СодСч, "ГрузПолуч") Тогда
		Заполнить_ExtendedOrganizationInfo29(Контент.Consignee, СодСч.ГрузПолуч);
	КонецЕсли;
	
	Заполнить_ProformaInvoice29Table(Контент.Table, СодСч);
	
	Для Каждого Подписант Из СписокXDTOИзЭлемента(Документ, "Подписант") Цикл
		Signer = НовыйЭлементСпискаXDTO(Контент.Signers);
		Заполнить_Signer970(Signer, Подписант);
	КонецЦикла;
	
	// BSLLS:Typo-on
КонецПроцедуры

Процедура Заполнить_ProformaInvoice29Base(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ОснИное") Тогда
		УстановитьЗначениеXDTO(Контент, "OtherBase", Параметры.ОснИное);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ОснРеквДок") Тогда
		ОснРеквДок = Параметры.ОснРеквДок;
		ProformaInvoiceBaseDocument = Контент.ProformaInvoiceBaseDocument;
		
		УстановитьЗначениеXDTO(ProformaInvoiceBaseDocument, "DocumentName", ОснРеквДок.РеквНаимДок);
		УстановитьЗначениеXDTO(ProformaInvoiceBaseDocument, "DocumentNumber", ОснРеквДок.РеквНомерДок);
		УстановитьЗначениеXDTO(ProformaInvoiceBaseDocument, "DocumentDate", ДатаXML_в_Дата1С(ОснРеквДок.РеквДатаДок));
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОснРеквДок, "РеквИдФайлДок") Тогда
			УстановитьЗначениеXDTO(ProformaInvoiceBaseDocument, "FileId", ОснРеквДок.РеквИдФайлДок);
		КонецЕсли;
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОснРеквДок, "РеквИдДок") Тогда
			УстановитьЗначениеXDTO(ProformaInvoiceBaseDocument, "Id", ОснРеквДок.РеквИдДок);
		КонецЕсли;
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОснРеквДок, "РИдСистХранД") Тогда
			УстановитьЗначениеXDTO(ProformaInvoiceBaseDocument, "SystemId", ОснРеквДок.РИдСистХранД);
		КонецЕсли;
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОснРеквДок, "РеквУРЛСистДок") Тогда
			УстановитьЗначениеXDTO(ProformaInvoiceBaseDocument, "SystemUrl", ОснРеквДок.РеквУРЛСистДок);
		КонецЕсли;
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ОснРеквДок, "РеквДопСведДок") Тогда
			УстановитьЗначениеXDTO(ProformaInvoiceBaseDocument, "AdditionalInfo", ОснРеквДок.РеквДопСведДок);
		КонецЕсли;
		
		Заполнить_ProformaInvoice29IdentificationDetails(ProformaInvoiceBaseDocument, ОснРеквДок);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_ProformaInvoice29IdentificationDetails(Контент, Параметры)
	
	Для Каждого РеквИдРекСост Из СписокXDTOИзЭлемента(Параметры, "РеквИдРекСост") Цикл
		IdentificationDetail = НовыйЭлементСпискаXDTO(Контент.IdentificationDetails);
		
		// BSLLS:IfElseIfEndsWithElse-off
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(РеквИдРекСост, "ИННЮЛ") Тогда
			УстановитьЗначениеXDTO(IdentificationDetail, "Inn", РеквИдРекСост.ИННЮЛ);
		ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(РеквИдРекСост, "ИННФЛ") Тогда
			УстановитьЗначениеXDTO(IdentificationDetail, "Inn", РеквИдРекСост.ИННФЛ);
		КонецЕсли;
		// BSLLS:IfElseIfEndsWithElse-on
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(РеквИдРекСост, "НаимОИВ") Тогда
			УстановитьЗначениеXDTO(IdentificationDetail, "ShortNameExecutiveAuthority", РеквИдРекСост.НаимОИВ);
		КонецЕсли;
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(РеквИдРекСост, "ДаннИно") Тогда
			ДаннИно = РеквИдРекСост.ДаннИно;
			
			Заполнить_ProformaInvoice29IdentificationDetails_ИностраннаяОрганизация(IdentificationDetail, ДаннИно);
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_ProformaInvoice29IdentificationDetails_ИностраннаяОрганизация(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Country", Параметры.КодСтр);
	УстановитьЗначениеXDTO(Контент, "OrgName", Параметры.Наим);
	
	// BSLLS:IfElseIfEndsWithElse-off
	Если ВРег(Параметры.ИдСтат) = "ИГ" Тогда
		УстановитьЗначениеXDTO(Контент, "StatusId", "PhysicalPerson");
	ИначеЕсли ВРег(Параметры.ИдСтат) = "ИО" Тогда
		УстановитьЗначениеXDTO(Контент, "StatusId", "LegalEntity");
	КонецЕсли;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Идентиф") Тогда
		УстановитьЗначениеXDTO(Контент, "LegalEntityId", Параметры.Идентиф);
	КонецЕсли;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ИныеСвед") Тогда
		УстановитьЗначениеXDTO(Контент, "OrganizationOrPersonInfo", Параметры.ИныеСвед);
	КонецЕсли;
	// BSLLS:IfElseIfEndsWithElse-on
	
КонецПроцедуры

Процедура Заполнить_ProformaInvoice29AdditionalInfo(Контент, Параметры)
	// BSLLS:Typo-off
	
	УстановитьЗначениеXDTO(Контент, "PaymentPurpose", Параметры.НазнПл);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ОгранОпДат") Тогда
		УстановитьЗначениеXDTO(Контент, "DateRestriction", ДатаXML_в_Дата1С(Параметры.ОгранОпДат));
	КонецЕсли;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ОгранОпДатВр") Тогда
		УстановитьЗначениеXDTO(Контент, "DateAndTimeRestriction", СокрЛП(Параметры.ОгранОпДатВр));
	КонецЕсли;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДатаНачТов") Тогда
		УстановитьЗначениеXDTO(Контент, "StartShipmentDate", ДатаXML_в_Дата1С(Параметры.ДатаНачТов));
	КонецЕсли;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "УникИнф") Тогда
		УстановитьЗначениеXDTO(Контент, "UniquePaymentInfo", Параметры.УникИнф);
	КонецЕсли;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "СпособОпл") Тогда
		УстановитьЗначениеXDTO(Контент, "PaymentMethod", Параметры.СпособОпл);
	КонецЕсли;
	
	Для Каждого УслОпл Из СписокXDTOИзЭлемента(Параметры, "УслОпл") Цикл
		Контент.PaymentTerms.Добавить(УслОпл);
	КонецЦикла;
	Для Каждого УслОказПост Из СписокXDTOИзЭлемента(Параметры, "УслОказПост") Цикл
		Контент.ShipmentTerms.Добавить(УслОказПост);
	КонецЦикла;
	
	Для Каждого Элемент Из СписокXDTOИзЭлемента(Параметры, "ИнфПол") Цикл
		AdditionalInfoId = НовыйЭлементСпискаXDTO(Контент.AdditionalInfoId);
		
		Заполнить_AdditionalInfoId(AdditionalInfoId, Элемент);
	КонецЦикла;
	
	// BSLLS:Typo-on
КонецПроцедуры

Процедура Заполнить_ExtendedOrganizationInfo29(Контент, Параметры)
	
	// BSLLS:IfElseIfEndsWithElse-off
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвЮЛУч") Тогда
		
		Заполнить_ExtendedOrganizationInfo29_ЮридическоеЛицо(Контент, Параметры.ИдСв.СвЮЛУч);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвИП") Тогда
		
		Заполнить_ExtendedOrganizationInfo29_ИП(Контент, Параметры.ИдСв.СвИП);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвФЛУч") Тогда
		
		Заполнить_ExtendedOrganizationInfo29_ФизическоеЛицо(Контент, Параметры.ИдСв.СвФЛУч);
		
	ИначеЕсли ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.ИдСв, "СвИнНеУч") Тогда
		
		Заполнить_ExtendedOrganizationInfo29_ИностранноеЛицо(Контент, Параметры.ИдСв.СвИнНеУч);
		
	КонецЕсли;
	// BSLLS:IfElseIfEndsWithElse-on
	
	УстановитьЗначениеXDTO(Контент, "Okpo", ПолучитьЗначениеXDTO(Параметры, "ОКПО"));
	УстановитьЗначениеXDTO(Контент, "Okopf", ПолучитьЗначениеXDTO(Параметры, "КодОПФ"));
	УстановитьЗначениеXDTO(Контент, "FullNameOPF", ПолучитьЗначениеXDTO(Параметры, "ПолнНаимОПФ"));
	УстановитьЗначениеXDTO(Контент, "Department", ПолучитьЗначениеXDTO(Параметры, "СтруктПодр"));
	УстановитьЗначениеXDTO(Контент, "AdditionalInfo", ПолучитьЗначениеXDTO(Параметры, "ИнфДляУчаст"));
	УстановитьЗначениеXDTO(Контент, "ShortOrgName", ПолучитьЗначениеXDTO(Параметры, "СокрНаим"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "БанкРекв") Тогда
		Заполнить_BankInfo29(Контент, Параметры.БанкРекв);
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Контакт") Тогда
		
		Для Каждого НомерТелефона Из СписокXDTOИзЭлемента(Параметры.Контакт, "Тлф") Цикл
			Контент.Phone.Добавить(НомерТелефона);
		КонецЦикла;
		
		Для Каждого ЭлПочта Из СписокXDTOИзЭлемента(Параметры.Контакт, "ЭлПочта") Цикл
			Контент.Email.Добавить(ЭлПочта);
		КонецЦикла;
		
		УстановитьЗначениеXDTO(Контент, "OtherContactInfo", ПолучитьЗначениеXDTO(Параметры.Контакт, "ИнКонт"));
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "Адрес") Тогда
		Заполнить_AddressInfo970(Контент.Address, Параметры.Адрес);
	КонецЕсли;
	
КонецПроцедуры

Процедура Заполнить_ExtendedOrganizationInfo29_ЮридическоеЛицо(Контент, Параметры)
	
	ВидыЮрФизЛиц = Перечисление_ВидыЮрФизЛиц();
	
	УстановитьЗначениеXDTO(Контент, "Type", ВидыЮрФизЛиц.ЮридическоеЛицо);
	
	УстановитьЗначениеXDTO(Контент, "Name", Параметры.НаимОрг);
	УстановитьЗначениеXDTO(Контент, "Inn", Параметры.ИННЮЛ);
	УстановитьЗначениеXDTO(Контент, "Kpp", ПолучитьЗначениеXDTO(Параметры, "КПП"));
	УстановитьЗначениеXDTO(Контент, "Ogrn", ПолучитьЗначениеXDTO(Параметры, "ОГРН"));
	
КонецПроцедуры

Процедура Заполнить_ExtendedOrganizationInfo29_ИП(Контент, Параметры)
	
	ВидыЮрФизЛиц = Перечисление_ВидыЮрФизЛиц();
	
	УстановитьЗначениеXDTO(Контент, "Type", ВидыЮрФизЛиц.ИндивидуальныйПредприниматель);
	
	УстановитьЗначениеXDTO(Контент, "Name", XML_СтрокаФИО(Параметры.ФИО));
	УстановитьЗначениеXDTO(Контент, "Inn", Параметры.ИННФЛ);
	
	УстановитьЗначениеXDTO(
		Контент,
		"IndividualEntityRegistrationCertificate",
		ПолучитьЗначениеXDTO(Параметры, "СвГосРегИП")
	);
	УстановитьЗначениеXDTO(
		Контент,
		"IndividualEntityRegistrationDate",
		ДатаXML_в_Дата1С(ПолучитьЗначениеXDTO(Параметры, "ДатаОГРНИП"))
	);
	УстановитьЗначениеXDTO(Контент, "IndividualEntityRegistrationNumber", ПолучитьЗначениеXDTO(Параметры, "ОГРНИП"));
	УстановитьЗначениеXDTO(Контент, "OrganizationOrPersonInfo", ПолучитьЗначениеXDTO(Параметры, "ИныеСвед"));
	
КонецПроцедуры

Процедура Заполнить_ExtendedOrganizationInfo29_ФизическоеЛицо(Контент, Параметры)
	
	ВидыЮрФизЛиц = Перечисление_ВидыЮрФизЛиц();
	
	УстановитьЗначениеXDTO(Контент, "Type", ВидыЮрФизЛиц.ФизическоеЛицо);
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ФИО") Тогда
		УстановитьЗначениеXDTO(Контент, "Name", XML_СтрокаФИО(Параметры.ФИО));
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Inn", ПолучитьЗначениеXDTO(Параметры, "ИННФЛ"));
	УстановитьЗначениеXDTO(Контент, "OrganizationOrPersonInfo", ПолучитьЗначениеXDTO(Параметры, "ИныеСвед"));
	УстановитьЗначениеXDTO(Контент, "PhysicalEntityStatusID", ПолучитьЗначениеXDTO(Параметры, "ИдСтатЛ"));
	УстановитьЗначениеXDTO(Контент, "Snils", ПолучитьЗначениеXDTO(Параметры, "СНИЛС"));
	
КонецПроцедуры

Процедура Заполнить_ExtendedOrganizationInfo29_ИностранноеЛицо(Контент, Параметры)
	
	ВидыЮрФизЛиц = Перечисление_ВидыЮрФизЛиц();
	
	Если Параметры.ИдСтат = "ИО" Тогда
		ВидЮрФизЛица = ВидыЮрФизЛиц.ИностраннаяОрганизация;
	Иначе
		ВидЮрФизЛица = ВидыЮрФизЛиц.ИностранныйГражданин;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "Type", ВидЮрФизЛица);
	
	УстановитьЗначениеXDTO(Контент, "Name", Параметры.Наим);
	УстановитьЗначениеXDTO(Контент, "OrganizationOrPersonInfo", ПолучитьЗначениеXDTO(Параметры, "ИныеСвед"));
	УстановитьЗначениеXDTO(Контент, "LegalEntityId", ПолучитьЗначениеXDTO(Параметры, "Идентиф"));
	
	УстановитьЗначениеXDTO(Контент, "Country", Параметры.НаимСтран);
	УстановитьЗначениеXDTO(Контент, "CountryCode", ПолучитьЗначениеXDTO(Параметры, "КодСтр"));
	
КонецПроцедуры

Процедура Заполнить_BankInfo29(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "BankAccountNumber", ПолучитьЗначениеXDTO(Параметры, "НомерСчета"));
	УстановитьЗначениеXDTO(Контент, "BankName", ПолучитьЗначениеXDTO(Параметры, "НаимБанк"));
	УстановитьЗначениеXDTO(Контент, "BankId", ПолучитьЗначениеXDTO(Параметры, "БИК"));
	УстановитьЗначениеXDTO(Контент, "CorrespondentAccount", ПолучитьЗначениеXDTO(Параметры, "КорСчет"));
	УстановитьЗначениеXDTO(Контент, "BankInn", ПолучитьЗначениеXDTO(Параметры, "ИННЮЛ"));
	УстановитьЗначениеXDTO(Контент, "BankKpp", ПолучитьЗначениеXDTO(Параметры, "КПП"));
	УстановитьЗначениеXDTO(Контент, "SwiftCode", ПолучитьЗначениеXDTO(Параметры, "SWIFTКод"));
	
КонецПроцедуры

Процедура Заполнить_ProformaInvoice29Table(Контент, Параметры)
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ВсегоОпл") Тогда
		ВсегоОпл = Параметры.ВсегоОпл;
		
		УстановитьЗначениеXDTO(Контент, "TotalWithVatExcluded", ВсегоОпл.СтТовБезНДСВсего);
		УстановитьЗначениеXDTO(Контент, "Total", ВсегоОпл.СтТовУчНалВсего);
		
		Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(ВсегоОпл.СумНалВсего, "СумНал") Тогда
			УстановитьЗначениеXDTO(Контент, "Vat", ВсегоОпл.СумНалВсего.СумНал);
		Иначе
			УстановитьЗначениеXDTO(Контент, "WithoutVat", "true");
		КонецЕсли;
		
		УстановитьЗначениеXDTO(Контент, "TotalDiscountSum", ПолучитьЗначениеXDTO(ВсегоОпл, "СумСкиВсего"));
	КонецЕсли;
	
	Для Каждого СведТовЦенПок Из СписокXDTOИзЭлемента(Параметры, "СведТовЦенПок") Цикл
		Заполнить_ProformaInvoice29Item(НовыйЭлементСпискаXDTO(Контент.Items), СведТовЦенПок);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_ProformaInvoice29Item(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "Product", ПолучитьЗначениеXDTO(Параметры, "НаимТов"));
	УстановитьЗначениеXDTO(Контент, "Unit", ПолучитьЗначениеXDTO(Параметры, "ОКЕИ_Тов"));
	УстановитьЗначениеXDTO(Контент, "UnitName", ПолучитьЗначениеXDTO(Параметры, "НаимЕдИзм"));
	УстановитьЗначениеXDTO(Контент, "Quantity", ПолучитьЗначениеXDTO(Параметры, "КолТов"));
	УстановитьЗначениеXDTO(Контент, "Price", ПолучитьЗначениеXDTO(Параметры, "ЦенаТов"));
	УстановитьЗначениеXDTO(Контент, "PriceWithoutDiscount", ПолучитьЗначениеXDTO(Параметры, "ЦенаТовБезСки"));
	УстановитьЗначениеXDTO(Контент, "OtherTaxRate", ПолучитьЗначениеXDTO(Параметры, "НалСтИн"));
	УстановитьЗначениеXDTO(Контент, "DiscountSum", ПолучитьЗначениеXDTO(Параметры, "СумСки"));
	УстановитьЗначениеXDTO(Контент, "DiscountPercent", ПолучитьЗначениеXDTO(Параметры, "ПроцентСки"));
	УстановитьЗначениеXDTO(Контент, "SubtotalWithVatExcluded", ПолучитьЗначениеXDTO(Параметры, "СтТовБезНДС"));
	УстановитьЗначениеXDTO(Контент, "SubtotalWithoutVatAndDiscount", ПолучитьЗначениеXDTO(Параметры, "СтТовБезНДССки"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "НалСт") Тогда
		УстановитьЗначениеXDTO(Контент, "TaxRate", TaxRate_Из_НалСт(Параметры.НалСт));
	КонецЕсли;
	
	БезНДС = Истина;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СумНал, "СумНал") Тогда
		УстановитьЗначениеXDTO(Контент, "Vat", Параметры.СумНал.СумНал);
		БезНДС = Ложь;
	КонецЕсли;
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры.СумНалБезСки, "СумНал") Тогда
		УстановитьЗначениеXDTO(Контент, "VatWithoutDiscount", Параметры.СумНалБезСки.СумНал);
		БезНДС = Ложь;
	КонецЕсли;
	
	УстановитьЗначениеXDTO(Контент, "WithoutVat", ?(БезНДС, "true", "false"));
	
	УстановитьЗначениеXDTO(Контент, "Subtotal", ПолучитьЗначениеXDTO(Параметры, "СтТовУчНал"));
	УстановитьЗначениеXDTO(Контент, "SubtotalWithoutDiscount", ПолучитьЗначениеXDTO(Параметры, "СтТовУчНалБезСки"));
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Параметры, "ДопСведТов") Тогда
		
		ДопСведТов = Параметры.ДопСведТов;
		
		ItemMark = ItemMark(ПолучитьЗначениеXDTO(ДопСведТов, "ПрТовРаб"));
		
		УстановитьЗначениеXDTO(Контент, "ItemMark", ItemMark);
		УстановитьЗначениеXDTO(Контент, "AdditionalProperty", ПолучитьЗначениеXDTO(ДопСведТов, "ДопПризн"));
		УстановитьЗначениеXDTO(Контент, "ItemVendorCode", ПолучитьЗначениеXDTO(ДопСведТов, "КодТов"));
		УстановитьЗначениеXDTO(Контент, "Country", ПолучитьЗначениеXDTO(ДопСведТов, "КодПроисх"));
		УстановитьЗначениеXDTO(Контент, "ItemCharact", ПолучитьЗначениеXDTO(ДопСведТов, "ХарактерТов"));
		УстановитьЗначениеXDTO(Контент, "ItemArticle", ПолучитьЗначениеXDTO(ДопСведТов, "АртикулТов"));
		УстановитьЗначениеXDTO(Контент, "ItemKind", ПолучитьЗначениеXDTO(ДопСведТов, "СортТов"));
		УстановитьЗначениеXDTO(Контент, "ItemSeries", ПолучитьЗначениеXDTO(ДопСведТов, "СерияТов"));
		УстановитьЗначениеXDTO(Контент, "CatalogCode", ПолучитьЗначениеXDTO(ДопСведТов, "КодКат"));
		УстановитьЗначениеXDTO(Контент, "ItemTypeCode", ПолучитьЗначениеXDTO(ДопСведТов, "КодВидТов"));
		
		Для Каждого СвГосСист Из СписокXDTOИзЭлемента(ДопСведТов, "СвГосСист") Цикл
			StateSystemItemInfo = НовыйЭлементСпискаXDTO(Контент.StateSystemItemInfos);
			Заполнить_StateSystemItemInfo(StateSystemItemInfo, СвГосСист);
		КонецЦикла;
		
	КонецЕсли;
	
	Для Каждого ИнфПол Из СписокXDTOИзЭлемента(Параметры, "ИнфПол") Цикл
		AdditionalInfoId = НовыйЭлементСпискаXDTO(Контент.AdditionalInfoId);
		
		Заполнить_AdditionalInfoId(AdditionalInfoId, ИнфПол);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_StateSystemItemInfo(Контент, Параметры)
	
	УстановитьЗначениеXDTO(Контент, "StateSystemName", Параметры.НаимГосСист);
	УстановитьЗначениеXDTO(Контент, "AccountingUnit", ПолучитьЗначениеXDTO(Параметры, "УчетЕд"));
	УстановитьЗначениеXDTO(Контент, "OtherInfo", ПолучитьЗначениеXDTO(Параметры, "ИнаяИнф"));
	
	Для Каждого ИдНомУчетЕд Из СписокXDTOИзЭлемента(Параметры, "ИдНомУчетЕд") Цикл
		Контент.StateSystemId.Добавить(ИдНомУчетЕд);
	КонецЦикла;
	
КонецПроцедуры

Процедура Заполнить_TorgBlrSellerContent(Контент, Параметры)
	
	Возврат;
	
КонецПроцедуры

Процедура Заполнить_TorgBlrBuyerContent(Контент, Параметры)
	
	Возврат;
	
КонецПроцедуры

Функция ТабличнаяЧасть_ProformaInvoice29(ОбъектXDTO)
	
	Результат	 = Новый_ТабличнаяЧасть();
	СписокXDTO	 = СписокXDTOИзЭлемента(ОбъектXDTO.Документ.СодСч, "СведТовЦенПок");
	
	Для Каждого СтрокаXDTO Из СписокXDTO Цикл
		
		ИнфоПоля = ПрочитатьТекстИнфТипФормализованногоСчета(СтрокаXDTO);
		ИдентификаторНоменклатуры = ПрочитатьИдентификаторНоменклатуры(ИнфоПоля);
		НоменклатураЭД = НоменклатураЭлектронногоСчета29(СтрокаXDTO, ИдентификаторНоменклатуры);
		
		СтрокаТЧ = Результат.Добавить();
		
		ЭДО_ЗаполнитьДопИнформациюСтрокиТЧ(СтрокаТЧ, ИнфоПоля);
		ПередаточныеДокументы_ЗаполнитьСтрокуТабличнойЧастиПоНоменклатуреДокумента(
			СтрокаТЧ,
			НоменклатураЭД
		);
		
		СтрокаТЧ.Наименование = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимТов", "");
		СтрокаТЧ.Количество			 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO, "КолТов"		, 0));
		СтрокаТЧ.Цена				 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO, "ЦенаТов"		, 0));
		СтрокаТЧ.СуммаБезНДС		 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO, "СтТовБезНДС"	, 0));
		СтрокаТЧ.СтавкаНДС			 = ПолучитьЗначениеXDTO(СтрокаXDTO, "НалСт", "");
		СтрокаТЧ.СуммаНДС 			 = Число(ПолучитьЗначениеXDTO(СтрокаXDTO.СумНал, "СумНал", 0));
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Определяет тип номенклатуры электронного документа по значению атрибута ПрТовРаб
//
// Параметры:
//  ПрТовРаб - Строка - значение атрибута ПрТовРаб (см. приказы 970@, 820@, 155@)
// 
// Возвращаемое значение:
//  Строка - см. описание поля "Тип" в Контракт_НоменклатураЭлектронногоДокумента
//
Функция ОпределитьТипНоменклатурыЛок(ПрТовРаб)
	
	Если ПрТовРаб = "1" Тогда
		Результат = ТипНоменклатурыИмущество();
	ИначеЕсли ПрТовРаб = "2" Тогда
		Результат = ТипНоменклатурыРабота();
	ИначеЕсли ПрТовРаб = "3" Тогда
		Результат = ТипНоменклатурыУслуга();
	ИначеЕсли ПрТовРаб = "4" Тогда
		Результат = ТипНоменклатурыИмущественныеПрава();
	ИначеЕсли ПрТовРаб = "5" Тогда
		Результат = ТипНоменклатурыИное();
	Иначе
		Результат = ТипНоменклатурыНеУказан();
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Возвращает счет в формате ФНС29
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO - титул продавца в терминах прото - структуры Диадока,
//                                            см. функцию ОбъектXDTO_В_ProtoОбъектXDTO();
//  ПараметрыФормированияФайла - Структура  - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - счет в формате ФНС29
//
Функция XML_ПолучитьXML_Счет_ФНС29(ПротоКонтент, ПараметрыФормированияФайла) Экспорт
	
	ИмяПакетаXDTO = "ON_CHETOP_1_998_01_05_01_03";
	Фабрика	= XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Файл = XML_ПолучитьПустойОбъектXDTO("Файл", ИмяПакетаXDTO);
	
	ИдФайла = XML_ФНС29_СформироватьИдФайла(ПротоКонтент, ПараметрыФормированияФайла);

	УстановитьЗначениеXDTO(Файл, "ИдФайл", ИдФайла);
	УстановитьЗначениеXDTO(Файл, "ВерсФорм", "5.01");
	УстановитьЗначениеXDTO(Файл, "ВерсПрог", "Diadoc 1.0");
	
	ДатаФормирования = Дата1С_в_ДатаXML(ПараметрыФормированияФайла.ДатаФормирования);
	ВремяФормирования = Дата_В_Формат(ПараметрыФормированияФайла.ДатаФормирования, "ДФ=HH:mm:ss");
	
	Документ = НовыйДочернийОбъектXDTO(Фабрика, Файл, "Документ");
	
	УстановитьЗначениеXDTO(Документ, "КНД", "1110379");
	УстановитьЗначениеXDTO(Документ, "ДатаИнф", ДатаФормирования);
	УстановитьЗначениеXDTO(Документ, "ВрИнф", ВремяФормирования);
	
	ФункцияСчета = "0";
	УстановитьЗначениеXDTO(Документ, "Функция", ФункцияСчета);
	
	XML_ФНС29_ЗаполнитьИнфПол(Документ, ПротоКонтент.AdditionalInfoId, ИмяПакетаXDTO);
	
	XML_ФНС29_ЗаполнитьСодСч(Документ, ПротоКонтент, ИмяПакетаXDTO, ПараметрыФормированияФайла);
	
	XML_УПД_970_ЗаполнитьПодписантов(ИмяПакетаXDTO, Документ, ПротоКонтент);

	Возврат Файл;
	
КонецФункции

// Возвращает счет-оферту в формате ФНС29
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO - титул продавца в терминах прото - структуры Диадока,
//                                            см. функцию ОбъектXDTO_В_ProtoОбъектXDTO();
//  ПараметрыФормированияФайла - Структура  - см. функцию XML_ПараметрыФормированияФайла();
// 
// Возвращаемое значение:
//   - ОбъектXDTO - счет в формате ФНС29
//
Функция XML_ПолучитьXML_СчетОферта_ФНС29(ПротоКонтент, ПараметрыФормированияФайла) Экспорт
	
	Файл = XML_ПолучитьXML_Счет_ФНС29(ПротоКонтент, ПараметрыФормированияФайла);

	ФункцияСчетаОферты = "1";
	УстановитьЗначениеXDTO(Файл.Документ, "Функция", ФункцияСчетаОферты);

	Возврат Файл;
	
КонецФункции

// Возвращает ответный титул покупателя белорусской накладной 133 формат
//
// Параметры:
//  ПротоКонтент               - ОбъектXDTO     - ответный титул в терминах прото структуры Диадока;
//  ФайлТитулаПродавца         - ДвоичныеДанные - файл УПД в формате 820@;
//  ФайлыПодписейПродавца      - Массив         - содержит все файлы подписей продавца;
//                             - ДвоичныеДанные - если файл подписи продавца всего один;
//  ПараметрыФормированияФайла - Структура      - см. функцию XML_ПараметрыФормированияФайла();
//
// Возвращаемое значение:
//   - ОбъектXDTO - ответный титул УПД в формате 970@
//
Функция XML_ОтветныйТитул_TORGBLR_133(
		ПротоКонтент,
		ФайлТитулаПродавца,
		ФайлыПодписейПродавца,
		ПараметрыФормированияФайла) Экспорт
	
	ИмяПакетаXDTO = "TORGBLRPOL";
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	ТитулПродавца = XML_В_ОбъектXDTO(ФайлТитулаПродавца);
	
	Результат = XML_ПолучитьПустойОбъектXDTO("DeliveryNote", ИмяПакетаXDTO);
	
	Результат.DeliveryNoteDate = ТитулПродавца.DeliveryNoteDate;
	Результат.DeliveryNoteID = ТитулПродавца.DeliveryNoteID;
	Результат.DeliveryNoteType = ТитулПродавца.DeliveryNoteType;
	Результат.FunctionCode = 11;
	Результат.CreationDateTime = Формат(ТекущаяДата(), "ДФ=yyyyMMddHHmmss");
	Результат.DocumentID = СтрЗаменить(XMLСтрока(Новый УникальныйИдентификатор), "-", "");
	
	Результат.ReferenceDocument = НовыйДочернийОбъектXDTO(Фабрика, Результат, "ReferenceDocument");
	Результат.ReferenceDocument.Date = ТитулПродавца.CreationDateTime;
	Результат.ReferenceDocument.ID = ТитулПродавца.DocumentID;
	
	Результат.Receiver = НовыйДочернийОбъектXDTO(Фабрика, Результат, "Receiver");
	ЗаполнитьЗначенияСвойств(Результат.Receiver, ТитулПродавца.Receiver);
	
	Результат.Shipper = НовыйДочернийОбъектXDTO(Фабрика, Результат, "Shipper");
	ЗаполнитьЗначенияСвойств(Результат.Shipper, ТитулПродавца.Shipper);
	
	Результат.ShipTo = НовыйДочернийОбъектXDTO(Фабрика, Результат, "ShipTo");
	
	Для Каждого Signer Из СписокXDTOИзЭлемента(ПротоКонтент, "Signers") Цикл
		
		Результат.ShipTo.Contact = Signer.FirstName
		+ " " + Signer.Surname
		+ " " + Signer.Patronymic
		+ ", " + Signer.JobTitle;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Возвращает универсальное сообщение в формате UserContract
//
// Параметры:
//  ДанныеСообщения - Структура - см. функцию Ядро.Контракт_ДанныеУниверсальногоСообщения()
// 
// Возвращаемое значение:
//  ОбъектXDTO - универсальное сообщение по схеме UniversalMessage_xsd
//
Функция XML_ПолучитьXML_УниверсальноеСообщение(ДанныеСообщения) Экспорт
	
	ИмяПакетаXDTO = "UniversalMessage";
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Результат = XML_ПолучитьПустойОбъектXDTO(ИмяПакетаXDTO, ИмяПакетаXDTO);
		
	UniversalMessageInfos = НовыйДочернийОбъектXDTO(Фабрика, Результат, "UniversalMessageInfos");
	UniversalMessageInfo = НовыйДочернийОбъектXDTO(Фабрика, UniversalMessageInfos, "UniversalMessageInfo");
	
	УстановитьЗначениеXDTO(UniversalMessageInfo, "StatusCode", ДанныеСообщения.Код);
	
	Если ЗначениеЗаполнено(ДанныеСообщения.ТекстСообщения) Тогда
		УстановитьЗначениеXDTO(UniversalMessageInfo, "PlainText", ДанныеСообщения.ТекстСообщения);
	КонецЕсли;
	
	Отправитель = ДанныеСообщения.Отправитель;
	
	Sender = НовыйДочернийОбъектXDTO(Фабрика, Результат, "Sender");
	УстановитьЗначениеXDTO(Sender, "UseDefaultPersonInfo", Отправитель.ДанныеПоУмолчанию);
	УстановитьЗначениеXDTO(Sender, "Position", Отправитель.Должность);
	
	Fio = НовыйДочернийОбъектXDTO(Фабрика, Sender, "Fio");
	
	УстановитьЗначениеXDTO(Fio, "LastName", Отправитель.Фамилия);
	УстановитьЗначениеXDTO(Fio, "FirstName", Отправитель.Имя);
	
	Если ЗначениеЗаполнено(Отправитель.Отчество) Тогда
		УстановитьЗначениеXDTO(Fio, "MiddleName", Отправитель.Отчество);
	КонецЕсли;
		
	Возврат Результат;
	
КонецФункции

Функция XML_УПД_970_ЭтоСпособПодтвержденияПолномочийЭлектроннойДоверенностью(ConfirmCredentials)

	Возврат (ConfirmCredentials = "3");
	
КонецФункции

Функция XML_УПД_970_ЭтоСпособПодтвержденияПолномочийДоверенностьюНаБумаге(ConfirmCredentials)
	
	Возврат (ConfirmCredentials = "5");
	
КонецФункции

// XML функции заполнения счета ФНС29

Функция XML_ФНС29_СформироватьИдФайла(ПротоКонтент, ПараметрыФормированияФайла)

	ПрефиксИдФайла = "ON_CHETOP";
	ПредставлениеДатыФормирования = Формат(ПараметрыФормированияФайла.ДатаФормирования, "ДФ=yyyyMMdd");
	
	ИдентификаторПолучателя = "";
	Если ПротоКонтент.Buyer <> Неопределено Тогда
		ИдентификаторПолучателя = ПротоКонтент.Buyer.FnsParticipantId;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ИдентификаторПолучателя) Тогда
		ПараметрыФормированияФайла.Свойство("ИдентификаторПолучателя", ИдентификаторПолучателя);
	КонецЕсли;

	ИдентификаторОтправителя = "";
	Если ПротоКонтент.Seller <> Неопределено Тогда
		ИдентификаторОтправителя = ПротоКонтент.Seller.FnsParticipantId;
	КонецЕсли;

	Если Не ЗначениеЗаполнено(ИдентификаторОтправителя) Тогда
		ПараметрыФормированияФайла.Свойство("ИдентификаторОтправителя", ИдентификаторОтправителя);
	КонецЕсли;

	ЧастиИдФайл = Новый Массив;
	ЧастиИдФайл.Добавить(ПрефиксИдФайла);
	ЧастиИдФайл.Добавить(ИдентификаторПолучателя);
	ЧастиИдФайл.Добавить(ИдентификаторОтправителя);
	ЧастиИдФайл.Добавить(ПредставлениеДатыФормирования);
	ЧастиИдФайл.Добавить(ПараметрыФормированияФайла.УникальныйИдентификатор);
	
	Разделитель = "_";
	Результат = Общее_СоединитьСтроку(ЧастиИдФайл, Разделитель);

	Возврат Результат;
	
КонецФункции

Процедура XML_ФНС29_ЗаполнитьСодСч(Документ, ПротоКонтент, ИмяПакетаXDTO, ПараметрыФормированияФайла)
	
	Фабрика	= XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	СодСч = НовыйДочернийОбъектXDTO(Фабрика, Документ, "СодСч");

	ДатаДокумента = Дата1С_в_ДатаXML(ПротоКонтент.DocumentDate);
	
	УстановитьЗначениеXDTO(СодСч, "НомерДок", ПротоКонтент.DocumentNumber);
	УстановитьЗначениеXDTO(СодСч, "ДатаДок", ДатаДокумента);
	
	ДенИзм = НовыйДочернийОбъектXDTO(Фабрика, СодСч, "ДенИзм");
	XML_ФНС29_ЗаполнитьДенИзм(ДенИзм, ПротоКонтент);
	
	Если ПротоКонтент.Table <> Неопределено Тогда
		ВсегоСтрок = ПротоКонтент.Table.Items.Количество();
		УстановитьЗначениеXDTO(СодСч, "ВсегоНаим", XML_ФорматЧисла(ВсегоСтрок, 0));
	КонецЕсли;
	
	Если ПротоКонтент.Seller <> Неопределено
		И ЗначениеЗаполнено(ПротоКонтент.Seller.Type) Тогда
		СвПрод = НовыйДочернийОбъектXDTO(Фабрика, СодСч, "СвПрод");
		XML_ФНС29_ЗаполнитьУчастникТип(СвПрод, ПротоКонтент.Seller, ИмяПакетаXDTO);
	КонецЕсли;
	
	Если ПротоКонтент.Buyer <> Неопределено
		И ЗначениеЗаполнено(ПротоКонтент.Buyer.Type) Тогда
		СвПокуп = НовыйДочернийОбъектXDTO(Фабрика, СодСч, "СвПокуп");
		XML_ФНС29_ЗаполнитьУчастникТип(СвПокуп, ПротоКонтент.Buyer, ИмяПакетаXDTO);
	КонецЕсли;
	
	Если ПротоКонтент.MoneyRecipient <> Неопределено
		И ЗначениеЗаполнено(ПротоКонтент.MoneyRecipient.Type) Тогда
		ПолучДнСр = НовыйДочернийОбъектXDTO(Фабрика, СодСч, "ПолучДнСр");
		XML_ФНС29_ЗаполнитьУчастникТип(ПолучДнСр, ПротоКонтент.MoneyRecipient, ИмяПакетаXDTO);
	КонецЕсли;
	
	Если ПротоКонтент.Payer <> Неопределено
		И ЗначениеЗаполнено(ПротоКонтент.Payer.Type) Тогда
		СвПлат = НовыйДочернийОбъектXDTO(Фабрика, СодСч, "СвПлат");
		XML_ФНС29_ЗаполнитьУчастникТип(СвПлат, ПротоКонтент.Payer, ИмяПакетаXDTO);
	КонецЕсли;
	
	Если ПротоКонтент.Shipper <> Неопределено
		И ЗначениеЗаполнено(ПротоКонтент.Shipper.Type) Тогда
		ГрузОт = НовыйДочернийОбъектXDTO(Фабрика, СодСч, "ГрузОт");
		XML_ФНС29_ЗаполнитьУчастникТип(ГрузОт, ПротоКонтент.Shipper, ИмяПакетаXDTO);
	КонецЕсли;
	
	Если ПротоКонтент.Consignee <> Неопределено
		И ЗначениеЗаполнено(ПротоКонтент.Consignee.Type) Тогда
		ГрузПолуч = НовыйДочернийОбъектXDTO(Фабрика, СодСч, "ГрузПолуч");
		XML_ФНС29_ЗаполнитьУчастникТип(ГрузПолуч, ПротоКонтент.Consignee, ИмяПакетаXDTO);
	КонецЕсли;
	
	Для Каждого ProformaInvoiceBase Из ПротоКонтент.ProformaInvoiceBases Цикл
		Основание = НовыйДочернийОбъектXDTO(Фабрика, СодСч, "Основание");
		XML_ФНС29_ЗаполнитьОснование(Основание, ProformaInvoiceBase, Фабрика);
	КонецЦикла;
	
	Для Каждого CommitmentTypes Из ПротоКонтент.CommitmentTypes Цикл
		ВидОбяз = НовыйДочернийОбъектXDTO(Фабрика, СодСч, "ВидОбяз");
		УстановитьЗначениеXDTO(ВидОбяз, "КодВидОбяз", CommitmentTypes.CommitmentTypeCode);
		// BSLLS:Typo-off
		УстановитьЗначениеXDTO(ВидОбяз, "НаимВидОбязм", CommitmentTypes.CommitmentTypeName);
		// BSLLS:Spelling:on
	КонецЦикла;
	
	Если ТипЗнч(ПротоКонтент.ProformaInvoiceAdditionalInfo) = Тип("ОбъектXDTO")
		И ОбъектXDTOЗаполнен(ПротоКонтент.ProformaInvoiceAdditionalInfo) Тогда
		ДопСв = НовыйДочернийОбъектXDTO(Фабрика, СодСч, "ДопСв");
		XML_ФНС29_ЗаполнитьДопСв(ДопСв, ПротоКонтент.ProformaInvoiceAdditionalInfo, ИмяПакетаXDTO);
	КонецЕсли;
	
	XML_ФНС29_ЗаполнитьВсегоОпл(СодСч, ПротоКонтент.Table, Фабрика);
	XML_ФНС29_ЗаполнитьСведТовЦенПок(СодСч, ПротоКонтент.Table, ИмяПакетаXDTO, ПараметрыФормированияФайла);
	
КонецПроцедуры

Процедура XML_ФНС29_ЗаполнитьУчастникТип(УчастникТип, СведенияОбУчастнике, ИмяПакетаXDTO)
	
	Если СведенияОбУчастнике = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	УстановитьЗначениеXDTO(УчастникТип, "ОКПО", СведенияОбУчастнике.Okpo);
	УстановитьЗначениеXDTO(УчастникТип, "КодОПФ", СведенияОбУчастнике.Okopf);
	УстановитьЗначениеXDTO(УчастникТип, "ПолнНаимОПФ", СведенияОбУчастнике.FullNameOPF);
	УстановитьЗначениеXDTO(УчастникТип, "СтруктПодр", СведенияОбУчастнике.Department);
	УстановитьЗначениеXDTO(УчастникТип, "ИнфДляУчаст", СведенияОбУчастнике.AdditionalInfo);
	
	УстановитьЗначениеXDTO(УчастникТип, "СокрНаим", СведенияОбУчастнике.ShortOrgName);
	
	XML_ЗаполнитьИдСв(УчастникТип, СведенияОбУчастнике, ИмяПакетаXDTO);
	
	Если СведенияОбУчастнике.Address <> Неопределено Тогда
		XML_ЗаполнитьАдрес_970(ИмяПакетаXDTO, УчастникТип, СведенияОбУчастнике.Address);
	КонецЕсли;
	
	Если СведенияОбУчастнике.Phone.Количество() > 0
		Или СведенияОбУчастнике.Email.Количество() > 0
		Или ЗначениеЗаполнено(СведенияОбУчастнике.OtherContactInfo) Тогда
		
		XML_ЗаполнитьКонтактТип(УчастникТип, СведенияОбУчастнике, ИмяПакетаXDTO);
		
	КонецЕсли;

	XML_ФНС29_ЗаполнитьБанкРекв(УчастникТип, СведенияОбУчастнике, Фабрика);
	
КонецПроцедуры

Процедура XML_ФНС29_ЗаполнитьДенИзм(ДенИзм, ПротоКонтент)
	
	// BSLLS:IfElseIfEndsWithElse-off
	Если ПротоКонтент.IsConventionalUnit Тогда
		
		УстановитьЗначениеXDTO(ДенИзм, "УЕ", "у.е.");
		УстановитьЗначениеXDTO(ДенИзм, "ОпрУЕ", ПротоКонтент.ConventionalUnitDefinition);
		
	ИначеЕсли ЗначениеЗаполнено(ПротоКонтент.Currency) Тогда
		
		УстановитьЗначениеXDTO(ДенИзм, "КодОКВ", ПротоКонтент.Currency);
		УстановитьЗначениеXDTO(ДенИзм, "КурсВал", ПротоКонтент.CurrencyRate);
		
		НаименованиеВалюты = НаименованиеВалюты(ПротоКонтент.Currency, Истина);
		Если ЗначениеЗаполнено(НаименованиеВалюты) Тогда
			УстановитьЗначениеXDTO(ДенИзм, "НаимОКВ", НаименованиеВалюты);
		КонецЕсли;
		
	КонецЕсли;
	// BSLLS:IfElseIfEndsWithElse-on
	
КонецПроцедуры

Процедура XML_ФНС29_ЗаполнитьИнфПол(Документ, ДанныеИнфоПоля, ИмяПакетаXDTO)
	
	Для Каждого ДанныеПоля Из ДанныеИнфоПоля Цикл
		
		ИдИнфоПоля = ДанныеПоля.InfoFileId;
		ЗначенияИнфоПоля = ДанныеПоля.StructedAdditionalInfos;
		
		Если ЗначениеЗаполнено(ИдИнфоПоля)
			ИЛИ ЗначениеЗаполнено(ЗначенияИнфоПоля) Тогда
			
			Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
			ИнфПол = НовыйДочернийОбъектXDTO(Фабрика, Документ, "ИнфПол");
			
			УстановитьЗначениеXDTO(ИнфПол, "ИдФайлИнфПол", ИдИнфоПоля);
			
			XML_ЗаполнитьТекстовуюИнформацию(ИмяПакетаXDTO, ИнфПол.ТекстИнф, ЗначенияИнфоПоля);
			
		КонецЕсли;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_ФНС29_ЗаполнитьБанкРекв(УчастникТип, СведенияОбУчастнике, Фабрика)
	
	Если Не ЗначениеЗаполнено(СведенияОбУчастнике.BankId)
		Или Не ЗначениеЗаполнено(СведенияОбУчастнике.BankAccountNumber)
		Или Не ЗначениеЗаполнено(СведенияОбУчастнике.CorrespondentAccount) Тогда
		Возврат;
	КонецЕсли;
	
	БанкРекв = НовыйДочернийОбъектXDTO(Фабрика, УчастникТип, "БанкРекв");
	
	УстановитьЗначениеXDTO(БанкРекв, "НомерСчета", СведенияОбУчастнике.BankAccountNumber);
	УстановитьЗначениеXDTO(БанкРекв, "НаимБанк", СведенияОбУчастнике.BankName);
	УстановитьЗначениеXDTO(БанкРекв, "ИННЮЛ", СведенияОбУчастнике.BankInn);
	УстановитьЗначениеXDTO(БанкРекв, "КПП", СведенияОбУчастнике.BankKpp);
	УстановитьЗначениеXDTO(БанкРекв, "БИК", СведенияОбУчастнике.BankId);
	УстановитьЗначениеXDTO(БанкРекв, "КорСчет", СведенияОбУчастнике.CorrespondentAccount);
	УстановитьЗначениеXDTO(БанкРекв, "SWIFTКод", СведенияОбУчастнике.SwiftCode);
	
КонецПроцедуры

Процедура XML_ФНС29_ЗаполнитьОснование(Основание, СведенияОбОсновании, Фабрика)
	
	Если ЗначениеЗаполнено(СведенияОбОсновании.OtherBase) Тогда
		УстановитьЗначениеXDTO(Основание, "ОснИное", СведенияОбОсновании.OtherBase);
	Иначе
		ОснРеквДок = НовыйДочернийОбъектXDTO(Фабрика, Основание, "ОснРеквДок");
		XML_ФНС29_ЗаполнитьРеквДокТип(ОснРеквДок, СведенияОбОсновании.ProformaInvoiceBaseDocument, Фабрика);
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ФНС29_ЗаполнитьРеквДокТип(РеквДокТип, СведенияОДокументе, Фабрика)
	
	УстановитьЗначениеXDTO(РеквДокТип, "РеквНаимДок", СведенияОДокументе.DocumentName);
	УстановитьЗначениеXDTO(РеквДокТип, "РеквНомерДок", СведенияОДокументе.DocumentNumber);
	УстановитьЗначениеXDTO(РеквДокТип, "РеквДатаДок", Дата1С_в_ДатаXML(СведенияОДокументе.DocumentDate));
	
	УстановитьЗначениеXDTO(РеквДокТип, "РеквИдФайлДок", СведенияОДокументе.FileId);
	УстановитьЗначениеXDTO(РеквДокТип, "РеквИдДок", СведенияОДокументе.Id);
	УстановитьЗначениеXDTO(РеквДокТип, "РИдСистХранД", СведенияОДокументе.SystemId);
	УстановитьЗначениеXDTO(РеквДокТип, "РеквУРЛСистДок", СведенияОДокументе.SystemUrl);
	УстановитьЗначениеXDTO(РеквДокТип, "РеквДопСведДок", СведенияОДокументе.AdditionalInfo);
	
	Для Каждого IdentificationDetail Из СведенияОДокументе.IdentificationDetails Цикл
		
		РеквИдРекСост = НовыйДочернийОбъектXDTO(Фабрика, РеквДокТип, "РеквИдРекСост");
		
		Если ЭтоИННЮрЛица(IdentificationDetail.Inn) Тогда
			УстановитьЗначениеXDTO(РеквИдРекСост, "ИННЮЛ", IdentificationDetail.Inn);
		Иначе
			УстановитьЗначениеXDTO(РеквИдРекСост, "ИННФЛ", IdentificationDetail.Inn);
		КонецЕсли;
		
		УстановитьЗначениеXDTO(РеквИдРекСост, "НаимОИВ", IdentificationDetail.ShortNameExecutiveAuthority);
		
		НаименованиеСтраны = НаименованиеСтраны(IdentificationDetail.Country);
		ВсеОбязательныеПоляДаннИноЗаполнены = ЗначениеЗаполнено(IdentificationDetail.Country)
			И ЗначениеЗаполнено(НаименованиеСтраны)
			И ЗначениеЗаполнено(IdentificationDetail.OrgName)
			И ЗначениеЗаполнено(IdentificationDetail.StatusId)
			И (ЗначениеЗаполнено(IdentificationDetail.LegalEntityId)
				ИЛИ ЗначениеЗаполнено(IdentificationDetail.OrganizationOrPersonInfo));
				
		Если ВсеОбязательныеПоляДаннИноЗаполнены Тогда
		
			ДаннИно = НовыйДочернийОбъектXDTO(Фабрика, РеквИдРекСост, "ДаннИно");
			
			НаименованиеСтраны = НаименованиеСтраны(IdentificationDetail.Country);
			
			УстановитьЗначениеXDTO(ДаннИно, "Наим", IdentificationDetail.OrgName);
			УстановитьЗначениеXDTO(ДаннИно, "КодСтр", IdentificationDetail.Country);
			УстановитьЗначениеXDTO(ДаннИно, "НаимСтран", НаименованиеСтраны);
			
			УстановитьЗначениеXDTO(ДаннИно, "Идентиф", IdentificationDetail.LegalEntityId);
			УстановитьЗначениеXDTO(ДаннИно, "ИныеСвед", IdentificationDetail.OrganizationOrPersonInfo);
			
			Если IdentificationDetail.StatusId = ТипУчастника_ФизическоеЛицо() Тогда
				ИдСтат = XML_ИностранныйГражданин();
			Иначе
				ИдСтат = XML_ИностраннаяОрганизация();
			КонецЕсли;
			
			УстановитьЗначениеXDTO(ДаннИно, "ИдСтат", ИдСтат);
			
		КонецЕсли;
		
	КонецЦикла;

КонецПроцедуры

Процедура XML_ФНС29_ЗаполнитьДопСв(ДопСв, ДопСведенияСчета, ИмяПакетаXDTO)
	
	УстановитьЗначениеXDTO(ДопСв, "НазнПл", ДопСведенияСчета.PaymentPurpose);
	УстановитьЗначениеXDTO(ДопСв, "ДатаНачТов", Дата1С_в_ДатаXML(ДопСведенияСчета.StartShipmentDate));
	УстановитьЗначениеXDTO(ДопСв, "УникИнф", ДопСведенияСчета.UniquePaymentInfo);
	УстановитьЗначениеXDTO(ДопСв, "СпособОпл", ДопСведенияСчета.PaymentMethod);
	
	// BSLLS:IfElseIfEndsWithElse-off
	Если ЗначениеЗаполнено(ДопСведенияСчета.DateRestriction) Тогда
		УстановитьЗначениеXDTO(ДопСв, "ОгранОпДат", Дата1С_в_ДатаXML(ДопСведенияСчета.DateRestriction));
	ИначеЕсли ЗначениеЗаполнено(ДопСведенияСчета.DateAndTimeRestriction) Тогда
		УстановитьЗначениеXDTO(ДопСв, "ОгранОпДатВр", XMLСтрока(ДопСведенияСчета.DateAndTimeRestriction));
	КонецЕсли;
	// BSLLS:IfElseIfEndsWithElse-on
	
	Для Каждого PaymentTerm Из ДопСведенияСчета.PaymentTerms Цикл
		ДопСв.УслОпл.Добавить(PaymentTerm);
	КонецЦикла;
	
	Для Каждого ShipmentTerm Из ДопСведенияСчета.ShipmentTerms Цикл
		ДопСв.УслОказПост.Добавить(ShipmentTerm);
	КонецЦикла;
	
	XML_ФНС29_ЗаполнитьИнфПол(ДопСв, ДопСведенияСчета.AdditionalInfoId, ИмяПакетаXDTO);
	
КонецПроцедуры

Процедура XML_ФНС29_ЗаполнитьВсегоОпл(СодСч, ТабличныеДанные, Фабрика)
	
	Если ТабличныеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	ВсегоОпл = НовыйДочернийОбъектXDTO(Фабрика, СодСч, "ВсегоОпл");
	
	УстановитьЗначениеXDTO(ВсегоОпл, "СтТовБезНДСВсего", ТабличныеДанные.TotalWithVatExcluded);
	УстановитьЗначениеXDTO(ВсегоОпл, "СтТовУчНалВсего", ТабличныеДанные.Total);
	УстановитьЗначениеXDTO(ВсегоОпл, "СумСкиВсего", ТабличныеДанные.TotalDiscountSum);

	СумНалВсего = НовыйДочернийОбъектXDTO(Фабрика, ВсегоОпл, "СумНалВсего");
	
	БезНДС = (ТабличныеДанные.WithoutVat = "true");
	XML_ЗаполнитьСумНал(СумНалВсего, ТабличныеДанные.Vat, БезНДС);
	
КонецПроцедуры

Процедура XML_ФНС29_ЗаполнитьСведТовЦенПок(СодСч, ТабличныеДанные, ИмяПакетаXDTO, ПараметрыФормированияФайла)

	Если ТабличныеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Фабрика	= XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ТочностьЦены = ПараметрыФормированияФайла.ТочностьЦены;
	
	НомерСтроки = 1;
	Для Каждого Item Из ТабличныеДанные.Items Цикл
		
		СведТовЦенПок = НовыйДочернийОбъектXDTO(Фабрика, СодСч, "СведТовЦенПок");
		
		УстановитьЗначениеXDTO(СведТовЦенПок, "НомСтр", НомерСтроки);
		УстановитьЗначениеXDTO(СведТовЦенПок, "НаимТов", Item.Product);
		
		Если ЗначениеЗаполнено(Item.Quantity) Тогда
			Количество = Окр(Item.Quantity, ПараметрыФормированияФайла.ТочностьКоличества);
			УстановитьЗначениеXDTO(СведТовЦенПок, "КолТов", Количество);
		КонецЕсли;
		
		XML_ФНС29_ЗаполнитьЦену(СведТовЦенПок, "ЦенаТов", Item.Price, ТочностьЦены);
		XML_ФНС29_ЗаполнитьЦену(СведТовЦенПок, "ЦенаТовБезСки", Item.PriceWithoutDiscount, ТочностьЦены);
		
		УстановитьЗначениеXDTO(СведТовЦенПок, "ОКЕИТов", Item.Unit);
		УстановитьЗначениеXDTO(СведТовЦенПок, "НаимЕдИзм", Item.UnitName);
		
		УстановитьЗначениеXDTO(СведТовЦенПок, "СтТовБезНДС", Item.SubtotalWithVatExcluded);
		УстановитьЗначениеXDTO(СведТовЦенПок, "СтТовБезНДССки", Item.SubtotalWithoutVatAndDiscount);
		УстановитьЗначениеXDTO(СведТовЦенПок, "СтТовУчНал", Item.Subtotal);
		УстановитьЗначениеXDTO(СведТовЦенПок, "СтТовУчНалБезСки", Item.SubtotalWithoutDiscount);

		БезНДС = TaxRate_ЭтоЗначениеБезНДС(Item.TaxRate);
		
		Если ЗначениеЗаполнено(Item.OtherTaxRate) Тогда
			УстановитьЗначениеXDTO(СведТовЦенПок, "НалСтИн", Item.OtherTaxRate);
		Иначе
			УстановитьЗначениеXDTO(СведТовЦенПок, "НалСт", НалСт_Из_TaxRate(Item.TaxRate));
		КонецЕсли;
		
		Если ЗначениеЗаполнено(Item.DiscountSum) Тогда
			УстановитьЗначениеXDTO(СведТовЦенПок, "СумСки", Item.DiscountSum);
		Иначе
			УстановитьЗначениеXDTO(СведТовЦенПок, "ПроцентСки", Item.DiscountPercent);
		КонецЕсли;

		СумНал = НовыйДочернийОбъектXDTO(Фабрика, СведТовЦенПок, "СумНал");
		XML_ЗаполнитьСумНал(СумНал, Item.Vat, БезНДС);
		
		СуммаНДСБезСкидки = Привести_К_Числу(Item.VatWithoutDiscount);
		СумНалБезСки = НовыйДочернийОбъектXDTO(Фабрика, СведТовЦенПок, "СумНалБезСки");
		XML_ЗаполнитьСумНал(СумНалБезСки, СуммаНДСБезСкидки, БезНДС);
		
		ДопСведТов = НовыйДочернийОбъектXDTO(Фабрика, СведТовЦенПок, "ДопСведТов");
		XML_ФНС29_ЗаполнитьДопСведТов(ДопСведТов, Item, Фабрика);
		
		XML_ФНС29_ЗаполнитьИнфПол(СведТовЦенПок, Item.AdditionalInfoId, ИмяПакетаXDTO);
		
		НомерСтроки = НомерСтроки + 1;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_ФНС29_ЗаполнитьЦену(СведТовЦенПок, ИмяСвойства, Цена, Точность)
	
	Если ЗначениеЗаполнено(Цена) Тогда
		
		Если Точность = Неопределено Тогда
			
			ТочностьПоУмолчанию = XML_ПараметрыФормированияФайла().ТочностьЦены;
			
			ЦенаПоУмолчанию = Окр(Цена, ТочностьПоУмолчанию);
			
			Если Число(ЦенаПоУмолчанию) = Цена Тогда
				УстановитьЗначениеXDTO(СведТовЦенПок, ИмяСвойства, ЦенаПоУмолчанию);
			Иначе
				УстановитьЗначениеXDTO(СведТовЦенПок, ИмяСвойства, Окр(Цена, Точность));
			КонецЕсли;
			
		Иначе
			УстановитьЗначениеXDTO(СведТовЦенПок, ИмяСвойства, Окр(Цена, Точность));
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ФНС29_ЗаполнитьДопСведТов(ДопСведТов, Item, Фабрика)
	
	ПрТовРаб = XML_ПолучитьПризнакНоменклатуры(Item.ItemMark);
	УстановитьЗначениеXDTO(ДопСведТов, "ПрТовРаб", ПрТовРаб);
	
	УстановитьЗначениеXDTO(ДопСведТов, "ДопПризн", Item.AdditionalProperty);
	УстановитьЗначениеXDTO(ДопСведТов, "КодТов", Item.ItemVendorCode);
	УстановитьЗначениеXDTO(ДопСведТов, "КодПроисх", Item.Country);
	УстановитьЗначениеXDTO(ДопСведТов, "ХарактерТов", Item.ItemCharact);
	УстановитьЗначениеXDTO(ДопСведТов, "АртикулТов", Item.ItemArticle);
	УстановитьЗначениеXDTO(ДопСведТов, "СортТов", Item.ItemKind);
	УстановитьЗначениеXDTO(ДопСведТов, "СерияТов", Item.ItemSeries);
	УстановитьЗначениеXDTO(ДопСведТов, "КодКат", Item.CatalogCode);
	УстановитьЗначениеXDTO(ДопСведТов, "КодВидТов", Item.ItemTypeCode);
	
	Если ЗначениеЗаполнено(Item.Country) Тогда
		СтранаПроисхождения = НаименованиеСтраны(Item.Country);
		УстановитьЗначениеXDTO(ДопСведТов, "КрНаимСтрПр", СтранаПроисхождения);
	КонецЕсли;
	
	Для Каждого StateSystemItemInfo Из Item.StateSystemItemInfos Цикл
		
		СвГосСист = НовыйДочернийОбъектXDTO(Фабрика, ДопСведТов, "СвГосСист");
		
		УстановитьЗначениеXDTO(СвГосСист, "НаимГосСист", StateSystemItemInfo.StateSystemName);
		УстановитьЗначениеXDTO(СвГосСист, "УчетЕд", StateSystemItemInfo.AccountingUnit);
		УстановитьЗначениеXDTO(СвГосСист, "ИнаяИнф", StateSystemItemInfo.OtherInfo);
		
		Для Каждого StateSystemId Из StateSystemItemInfo.StateSystemId Цикл
			СвГосСист.ИдНомУчетЕд.Добавить(StateSystemId);
		КонецЦикла;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура XML_ЗаполнитьИдСв(ОбъектXDTO, ПротоКонтент, ИмяПакетаXDTO)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	ИдСв = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "ИдСв");
	
	ВидыЮрФизЛиц = Перечисление_ВидыЮрФизЛиц();
	
	// BSLLS:IfElseIfEndsWithElse-off
	Если ПротоКонтент.Type = ВидыЮрФизЛиц.ЮридическоеЛицо Тогда
		
		XML_ЗаполнитьЮридическоеЛицоКонтрагента(ИмяПакетаXDTO, ИдСв, ПротоКонтент);
		
	ИначеЕсли ПротоКонтент.Type = ВидыЮрФизЛиц.ИндивидуальныйПредприниматель Тогда
		
		СвИП = НовыйДочернийОбъектXDTO(Фабрика, ИдСв, "СвИП");
		XML_ЗаполнитьСведенияОбИндивидуальномПредпринимателе(ИмяПакетаXDTO, СвИП, ПротоКонтент);
		
	ИначеЕсли ПротоКонтент.Type = ВидыЮрФизЛиц.ФизическоеЛицо Тогда
		
		СвФЛУч = НовыйДочернийОбъектXDTO(Фабрика, ИдСв, "СвФЛУч");
		XML_ЗаполнитьСведенияОФизическомЛице(ИмяПакетаXDTO, СвФЛУч, ПротоКонтент);
		
	ИначеЕсли ПротоКонтент.Type = ВидыЮрФизЛиц.ИностраннаяОрганизация
		Или ПротоКонтент.Type = ВидыЮрФизЛиц.ИностранныйГражданин Тогда
		
		СвИнНеУчТип = НовыйДочернийОбъектXDTO(Фабрика, ИдСв, "СвИнНеУч");
		
		XML_УПД_970_ЗаполнитьСведенияОИностраннойОрганизации(СвИнНеУчТип, ПротоКонтент);
		
	КонецЕсли;
	// BSLLS:IfElseIfEndsWithElse-on
	
КонецПроцедуры

Процедура XML_ЗаполнитьКонтактТип(ОбъектXDTO, ПротоКонтент, ИмяПакетаXDTO)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	Контакт = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "Контакт");
	
	Для Каждого НомерТелефона Из ПротоКонтент.Phone Цикл
		Контакт.Тлф.Добавить(НомерТелефона);	
	КонецЦикла;
	
	Для Каждого АдресЭлПочты Из ПротоКонтент.Email Цикл
		Контакт.ЭлПочта.Добавить(АдресЭлПочты);
	КонецЦикла;
	
	УстановитьЗначениеXDTO(Контакт, "ИнКонт", ПротоКонтент.OtherContactInfo);
	
КонецПроцедуры

Процедура XML_ЗаполнитьБанкРекв_970(ОбъектXDTO, ПротоКонтент, ИмяПакетаXDTO)
	
	Фабрика = XML_ФабрикаXDTO(ИмяПакетаXDTO);
	
	БанкРекв = НовыйДочернийОбъектXDTO(Фабрика, ОбъектXDTO, "БанкРекв");
	СвБанк = НовыйДочернийОбъектXDTO(Фабрика, БанкРекв, "СвБанк");
	
	Если ЗначениеЗаполнено(ПротоКонтент.BankAccountNumber) Тогда
		УстановитьЗначениеXDTO(БанкРекв, "НомерСчета", ПротоКонтент.BankAccountNumber);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПротоКонтент.BankName) Тогда
		УстановитьЗначениеXDTO(СвБанк, "НаимБанк", ПротоКонтент.BankName);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПротоКонтент.BankId) Тогда
		УстановитьЗначениеXDTO(СвБанк, "БИК", ПротоКонтент.BankId);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(ПротоКонтент.CorrespondentAccount) Тогда
		УстановитьЗначениеXDTO(СвБанк, "КорСчет", ПротоКонтент.CorrespondentAccount);
	КонецЕсли;
	
КонецПроцедуры

Процедура XML_ЗаполнитьСумНал(СумНал, СуммаНДС, БезНДС = Ложь)
	
	Если БезНДС Тогда
		УстановитьЗначениеXDTO(СумНал, "БезНДС", СтрокаБезНДС());
	Иначе
		УстановитьЗначениеXDTO(СумНал, "СумНал", Привести_К_Числу(СуммаНДС));
	КонецЕсли;

КонецПроцедуры

Функция ЭтоИННЮрЛица(Значение)
	
	ДлинаИННЮрЛица = 10;
	Результат = СтрДлина(СокрЛП(Значение)) = ДлинаИННЮрЛица;
	Возврат Результат;
	
КонецФункции

Функция XML_ИностранныйГражданин()
	Возврат "ИГ";
КонецФункции

Функция XML_ИностраннаяОрганизация()
	Возврат "ИО";
КонецФункции

Функция ТипУчастника_ФизическоеЛицо()
	Возврат "PhysicalPerson";
КонецФункции

Функция ТипУчастника_ЮридическоеЛицо()
	Возврат "LegalEntity";
КонецФункции

Функция ТипПрото_ИнформацияПродавцаСчетНаОплату29()
	Возврат "ProformaInvoice29SellerContent";
КонецФункции

Функция ТипПрото_ИнформацияПродавцаНакладнаяРБ01()
	Возврат "TorgBlrSellerContent";
КонецФункции

Функция ТипПрото_ИнформацияПокупателяНакладнаяРБ01()
	Возврат "TorgBlrBuyerContent";
КонецФункции

Функция УПД_НаименованиеДокументаПоФактХЖ()
	Возврат "Документ об отгрузке товаров (выполнении работ), передаче имущественных прав (документ об оказании услуг)";
КонецФункции

Функция ВалидацияUCD736_ПроверитьВалюту(ВалютаСсылка, ВалютаКод)
	
	Результат = Новый Массив;
	
	ПроверитьФорматПоля(Результат, ВалютаКод, "Код валюты счета-фактуры", Истина, 3, 3, Истина, "м");
	
	НаименованиеВалюты = НаименованиеВалюты(ВалютаКод, Истина);
	Если НЕ ЗначениеЗаполнено(НаименованиеВалюты) Тогда
		
		ШаблонОшибкиКодНеНайден = НСтр("ru='В классификаторе валют не найден код ""<%1>"". Выберите валюту из классификатора'");
		ТекстОшибкиКодНеНайден = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибкиКодНеНайден, ВалютаКод);
		
		ДобавитьОшибкуВалидации(Результат, ТекстОшибкиКодНеНайден, ВалютаСсылка);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ВалидацияАктСверки405_ПроверитьВалютуПоКлассификатору(ВалютаКод)
	
	ЕстьВалютаВКлассификаторе = Истина;
		
	НаименованиеВалюты = НаименованиеВалюты(ВалютаКод, Истина);
	Если НЕ ЗначениеЗаполнено(НаименованиеВалюты) Тогда
		
		ЕстьВалютаВКлассификаторе = Ложь;
		
	КонецЕсли;
	
	Возврат ЕстьВалютаВКлассификаторе;
	
КонецФункции

Функция ВалидацияАктСверки405_ДокументыПоДоговору(ContractInfo)
	
	СписокОшибок = Новый Массив;
	
	Для Каждого ДокументыПоДоговору Из ContractInfo Цикл
		
		Для Каждого ДокументПоДоговору Из ДокументыПоДоговору.DocumentInfo Цикл
			
			ПроверитьФорматПоля(СписокОшибок, ДокументПоДоговору.DocumentNumber,
				"Номер документа (НомДок)", Истина, 1, 255);
			ПроверитьФорматДаты(СписокОшибок, ДокументПоДоговору.DocumentDate,
				"Дата документа (ДатаДок)", Истина);
			
			Ошибки = ВалидацияАктСверки405_ОперацииПоДокументу(ДокументПоДоговору);
			ДополнитьМассивОшибокВалидации(СписокОшибок, Ошибки);
			
		КонецЦикла;
		
	КонецЦикла;

	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияАктСверки405_ОперацииПоДокументу(ДокументПоДоговору)
	
	СписокОшибок = Новый Массив;
	
	Для Каждого ОперацияПоДокументу Из ДокументПоДоговору.OperationInfo Цикл
		
		ПроверитьФорматДаты(СписокОшибок, ОперацияПоДокументу.OperationDate,
			"Дата операции (ДатаОпер)", Истина);
		
		ПроверитьФорматПоля(СписокОшибок, ОперацияПоДокументу.OperationName,
			"Наименование операции (НаимОпер)", Истина, 1, 255);
		
		Если ОперацияПоДокументу.SumAmount = Неопределено
			И ОперацияПоДокументу.SumDebit = Неопределено Тогда
			
			ДобавитьОшибкуВалидации(СписокОшибок,
				"не заполнено одно из полей: с суммой дебета (СумДебет) или суммой кредита (СумКредит)");
			
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияАктСверки405_Участник(ExtendedOrganizationInfo, СсылкаУчастник,
	ПредставлениеУчастника, ВидУчастникаРодительныйПадеж)
	
	СписокОшибок = Новый Массив;

	ТипУчастника = ExtendedOrganizationInfo.Type;
	
	ВидыЮрФизЛиц = Перечисление_ВидыЮрФизЛиц();
	
	Если ТипУчастника = ВидыЮрФизЛиц.ИндивидуальныйПредприниматель Тогда
		
		// Сведения об индивидуальном предпринимателе (СвИП)
		
		Ошибки = ВалидацияАктСверки405_Участник_ФЛ(ExtendedOrganizationInfo,
			СсылкаУчастник,
			ПредставлениеУчастника,
			ВидУчастникаРодительныйПадеж);
		
		ДополнитьМассивОшибокВалидации(СписокОшибок, Ошибки);
		
		ОшибкаОГРН = ОшибкаФорматаПоля(ExtendedOrganizationInfo.Ogrn,
			"ОГРНИП", Истина, 15, 15, Истина);
		
		Если ЗначениеЗаполнено(ОшибкаОГРН) Тогда
			
			ШаблонОшибки = "Ошибки в сведениях <%1>.
			|%2.
			|Перейдите в карточку %3 и заполните ОГРН";
			
			СтрокаОшибка = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
				ПредставлениеУчастника,
				ОшибкаОГРН,
				ВидУчастникаРодительныйПадеж);
			
			ДобавитьОшибкуВалидации(СписокОшибок, СтрокаОшибка, СсылкаУчастник);

		КонецЕсли;
		
	ИначеЕсли ТипУчастника = ВидыЮрФизЛиц.ФизическоеЛицо Тогда
		
		// Сведения о физическом лице СвФЛ
		
		Ошибки = ВалидацияАктСверки405_Участник_ФЛ(ExtendedOrganizationInfo,
			СсылкаУчастник,
			ПредставлениеУчастника,
			ВидУчастникаРодительныйПадеж);
		
		ДополнитьМассивОшибокВалидации(СписокОшибок, Ошибки);
		
	ИначеЕсли ТипУчастника = ВидыЮрФизЛиц.ЮридическоеЛицо Тогда
		
		// Сведения о юридическом лице, состоящем на учете в налоговых органах СвЮЛУч
		
		Ошибки = ВалидацияАктСверки405_Участник_СвЮЛУч(ExtendedOrganizationInfo,
			СсылкаУчастник,
			ПредставлениеУчастника,
			ВидУчастникаРодительныйПадеж);
		
		ДополнитьМассивОшибокВалидации(СписокОшибок, Ошибки);
		
	ИначеЕсли ТипУчастника = ВидыЮрФизЛиц.ИностраннаяОрганизация Тогда
		
		// Сведения об иностранном лице, не состоящем на учете в налоговых органах СвИнНеУч
		
		Ошибки = ВалидацияАктСверки405_Участник_СвИнНеУч(ExtendedOrganizationInfo,
			СсылкаУчастник,
			ПредставлениеУчастника,
			ВидУчастникаРодительныйПадеж);
		
		ДополнитьМассивОшибокВалидации(СписокОшибок, Ошибки);
		
	Иначе
		
		ШаблонТекстаОшибки = "Неожиданный идентификатор типа участника сверки: %1";
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(ШаблонТекстаОшибки, ТипУчастника);
		
		ДобавитьОшибкуВалидации(СписокОшибок, ТекстОшибки);
		
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияАктСверки405_Участник_СвИнНеУч(ExtendedOrganizationInfo, СсылкаУчастник,
	ПредставлениеУчастника, ВидУчастникаРодительныйПадеж)

	СписокОшибок = Новый Массив;
	
	ОшибкаИдентификатора = ОшибкаФорматаПоля(ExtendedOrganizationInfo.LegalEntityId,
		"Налоговый номер иностранной компании (Идентиф)", Истина, 1, 255, , , "Код");
	
	Если ЗначениеЗаполнено(ОшибкаИдентификатора) Тогда
		
		ШаблонОшибки = "Ошибки в сведениях <%1>.
		|Для иностранной компании не заполнен налоговый номер.
		|%2
		|Перейдите в карточку %3 и заполните налоговый номер";
		
		СтрокаОшибка = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
			ПредставлениеУчастника,
			ОшибкаИдентификатора,
			ВидУчастникаРодительныйПадеж);
		
		ДобавитьОшибкуВалидации(СписокОшибок, СтрокаОшибка, СсылкаУчастник);

	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияАктСверки405_Участник_СвЮЛУч(ExtendedOrganizationInfo, СсылкаУчастник,
	ПредставлениеУчастника, ВидУчастникаРодительныйПадеж)
	
	СписокОшибок = Новый Массив;
	
	СтрокаОшибка = ПроверитьИННКонтрагента(ExtendedOrganizationInfo.Inn, "ЮрЛицо", ВидУчастникаРодительныйПадеж);
	
	Если ЗначениеЗаполнено(СтрокаОшибка) Тогда
		
		ШаблонОшибки = "Ошибки в сведениях <%1>.
		|%2.
		|Перейдите в карточку %3 и заполните ИНН";
		
		СтрокаОшибка = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
			ПредставлениеУчастника,
			СтрокаОшибка,
			ВидУчастникаРодительныйПадеж);
		
		ДобавитьОшибкуВалидации(СписокОшибок, СтрокаОшибка, СсылкаУчастник);
		
	КонецЕсли;
	
	СтрокаОшибка = ПроверитьКППКонтрагента(ExtendedOrganizationInfo.Kpp, ВидУчастникаРодительныйПадеж);
	
	Если ЗначениеЗаполнено(СтрокаОшибка) Тогда
		
		ШаблонОшибки = "Ошибки в сведениях <%1>.
		|%2.
		|Перейдите в карточку %3 и заполните КПП";
		
		СтрокаОшибка = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
			ПредставлениеУчастника,
			СтрокаОшибка,
			ВидУчастникаРодительныйПадеж);
		
		ДобавитьОшибкуВалидации(СписокОшибок, СтрокаОшибка, СсылкаУчастник);
		
	КонецЕсли;
	
	ОшибкаОГРН = ОшибкаФорматаПоля(ExtendedOrganizationInfo.Ogrn,
	"ОГРН", Истина, 13, 13, Истина);
	
	Если ЗначениеЗаполнено(ОшибкаОГРН) Тогда
		
		ШаблонОшибки = "Ошибки в сведениях <%1>.
		|%2.
		|Перейдите в карточку %3 и заполните ОГРН";
		
		СтрокаОшибка = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
			ПредставлениеУчастника,
			ОшибкаОГРН,
			ВидУчастникаРодительныйПадеж);

		ДобавитьОшибкуВалидации(СписокОшибок, СтрокаОшибка, СсылкаУчастник);

	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияАктСверки405_Участник_ФЛ(ExtendedOrganizationInfo, СсылкаУчастник,
	ПредставлениеУчастника, ВидУчастникаРодительныйПадеж)
	
	СписокОшибок = Новый Массив;
	
	СтрокаОшибка = ПроверитьИННКонтрагента(ExtendedOrganizationInfo.Inn, "ФизЛицо", ВидУчастникаРодительныйПадеж);
	
	Если ЗначениеЗаполнено(СтрокаОшибка) Тогда
		
		ШаблонОшибки = "Ошибки в сведениях <%1>.
		|%2.
		|Перейдите в карточку %3 и заполните ИНН";
		
		СтрокаОшибка = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
			ПредставлениеУчастника,
			СтрокаОшибка,
			ВидУчастникаРодительныйПадеж);
		
		ДобавитьОшибкуВалидации(СписокОшибок, СтрокаОшибка, СсылкаУчастник);
		
	КонецЕсли;
	
	Если ExtendedOrganizationInfo.Свойство("Name") = Неопределено Тогда
		
		ШаблонОшибки = "Ошибки в сведениях <%1>.
		|Не указано имя/фамилия.
		|Перейдите в карточку %2 и заполните";
		
		СтрокаОшибка = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
			ПредставлениеУчастника,
			ВидУчастникаРодительныйПадеж);
		
		ДобавитьОшибкуВалидации(СписокОшибок, СтрокаОшибка, СсылкаУчастник);
		
	Иначе
		
		Ошибки = ВалидацияАктСверки405_Участник_ФИО(ExtendedOrganizationInfo,
			СсылкаУчастник,
			ПредставлениеУчастника,
			ВидУчастникаРодительныйПадеж);
		
		ДополнитьМассивОшибокВалидации(СписокОшибок, Ошибки);
		
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияАктСверки405_Участник_ФИО(ExtendedOrganizationInfo, СсылкаУчастник,
	ПредставлениеУчастника, ВидУчастникаРодительныйПадеж)
	
	СписокОшибок = Новый Массив;
	
	ПротоСтруктураФИО = XML_ПротоСтруктураФИО(ExtendedOrganizationInfo.Name);
	
	Если Не ЗначениеЗаполнено(ПротоСтруктураФИО.Surname) Тогда
		
		ШаблонОшибки = "Ошибки в сведениях <%1>.
		|Не указана фамилия.
		|Перейдите в карточку %2 и заполните фамилию";
		
		СтрокаОшибка = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
			ПредставлениеУчастника,
			ВидУчастникаРодительныйПадеж);
		
		ДобавитьОшибкуВалидации(СписокОшибок, СтрокаОшибка, СсылкаУчастник);
		
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(ПротоСтруктураФИО.Firstname) Тогда
		
		ШаблонОшибки = "Ошибки в сведениях <%1>.
		|Не указано имя.
		|Перейдите в карточку %2 и заполните имя";
		
		СтрокаОшибка = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки,
			ПредставлениеУчастника,
			ВидУчастникаРодительныйПадеж);
		
		ДобавитьОшибкуВалидации(СписокОшибок, СтрокаОшибка, СсылкаУчастник);
		
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияСчетаФНС29(Content) Экспорт
	
	СписокОшибок = Новый Массив;
	
	// BSLLS:MagicNumber-off
	ПроверитьФорматПоля(СписокОшибок, Content.DocumentNumber, "Номер счета", Истина, 1, 255, Ложь, "м");
	ПроверитьФорматДаты(СписокОшибок, Content.DocumentDate, "Дата счета", Истина);
	// BSLLS:MagicNumber-on
	
	ВалидацияСчетаФНС29_ПроверитьДополнительныеСведения(Content.ProformaInvoiceAdditionalInfo, СписокОшибок);
	
	ВалидацияСчетаФНС29_ПроверитьВалюту(Content.Currency, Content.IsConventionalUnit, СписокОшибок);
	
	ВалидацияСчетаФНС29_ПроверитьСведенияОПродавце(Content.Seller, СписокОшибок);
	ВалидацияСчетаФНС29_ПроверитьСведенияОПокупателе(Content.Buyer, СписокОшибок);
	
	ВалидацияСчетаФНС29_ПроверитьСведенияОГрузоотправителе(Content.Shipper, СписокОшибок);
	ВалидацияСчетаФНС29_ПроверитьСведенияОГрузополучателе(Content.Consignee, СписокОшибок);
	ВалидацияСчетаФНС29_ПроверитьСведенияОПлательщике(Content.Payer, СписокОшибок);
	ВалидацияСчетаФНС29_ПроверитьСведенияОПолучателеДенежныхСредств(Content.MoneyRecipient, СписокОшибок);
	
	ВалидацияСчетаФНС29_ПроверитьТабличныеДанныеДокумента(Content.Table, СписокОшибок);
	
	ВалидацияСчетаФНС29_ПроверитьРеквизитыОснованийВыставленияСчета(Content.ProformaInvoiceBases, СписокОшибок);
	
	Возврат СписокОшибок;
	
КонецФункции

Процедура ВалидацияСчетаФНС29_ПроверитьДополнительныеСведения(ProformaInvoiceAdditionalInfo, СписокОшибок)
	
	Если ProformaInvoiceAdditionalInfo = Неопределено
		Или Не СтруктураЗаполнена(ProformaInvoiceAdditionalInfo) Тогда
		Возврат;
	КонецЕсли;
	
	// BSLLS:MagicNumber-off
	НазначениеПлатежа = ProformaInvoiceAdditionalInfo.PaymentPurpose;
	ПроверитьФорматПоля(СписокОшибок, НазначениеПлатежа, "Назначение платежа", Истина, 1, 210, Ложь, "с");
	// BSLLS:MagicNumber-on
	
	ШаблонТекстаОшибки = "[%1] Дополнительное условие";
	
	ОшибкиВалидацииДопСведений = Новый Массив;

	НомерСтроки = 1;
	Для Каждого PaymentTerm Из ProformaInvoiceAdditionalInfo.PaymentTerms Цикл
		
		ПредставлениеПоля = Общее_ПодставитьПараметрыВСтроку(ШаблонТекстаОшибки, НомерСтроки);
		
		// BSLLS:MagicNumber-off
		ПроверитьФорматПоля(ОшибкиВалидацииДопСведений, PaymentTerm, ПредставлениеПоля, Ложь, 1, 1000, Ложь, "с");
		// BSLLS:MagicNumber-on
		
		НомерСтроки = НомерСтроки + 1;
		
	КонецЦикла;
	
	ДобавитьГруппуОшибокВалидации(
		СписокОшибок,
		ОшибкиВалидацииДопСведений,
		"Ошибки в дополнительных условиях по документу");
	
КонецПроцедуры

Процедура ВалидацияСчетаФНС29_ПроверитьВалюту(КодВалюты, ЭтоУсловныеЕдиницы, СписокОшибок)
	
	ОшибкиВалидацииВалюты = Новый Массив;
	
	Если Не ЭтоУсловныеЕдиницы Тогда
		
		// BSLLS:MagicNumber-off
		ПроверитьФорматПоля(ОшибкиВалидацииВалюты, КодВалюты, "Код валюты счета", Истина, 3, 3, Истина, "м");
		// BSLLS:MagicNumber-on
		
		Если ОшибкиВалидацииВалюты.Количество() = 0 Тогда
			ПроверитьКодВалютыПоКлассификатору(КодВалюты, ОшибкиВалидацииВалюты);
		КонецЕсли;
		
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиВалидацииВалюты, "Ошибки в заполнении валюты");
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ВалидацияСчетаФНС29_ПроверитьСведенияОПродавце(Seller, СписокОшибок)
	
	Если ЗначениеЗаполнено(Seller)
		И СтруктураЗаполнена(Seller) Тогда
			
		ОшибкиКонтрагента = ВалидацияСчетаФНС29_ПроверитьСведенияОбУчастнике(Seller);
		
		ОшибкиАдреса = ВалидацияСчетаФНС29_ПроверитьСтруктуруАдреса(Seller.Address);
		Если ЗначениеЗаполнено(ОшибкиАдреса) Тогда 
			ДобавитьОшибкуВалидацииСОформлением(ОшибкиКонтрагента, "Ошибки в юридическом адресе:", , "b");
			ДополнитьМассивОшибокВалидации(ОшибкиКонтрагента, ОшибкиАдреса);
		КонецЕсли;
		
		ОшибкиБанковскогоСчета = ВалидацияСчетаФНС29_ПроверитьСведенияОБанковскомСчете(Seller);
		ДополнитьМассивОшибокВалидации(ОшибкиКонтрагента, ОшибкиБанковскогоСчета);
		
		ОрганизацияСсылка = ПолучитьЗначениеXDTO(Seller.СлужебнаяИнформация, "Ссылка");
		
		ДобавитьГруппуОшибокВалидации(
			СписокОшибок,
			ОшибкиКонтрагента,
			"Ошибки в сведениях о <продавце>",
			ОрганизацияСсылка);
		
	Иначе
		
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют сведения о продавце");
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ВалидацияСчетаФНС29_ПроверитьСведенияОПокупателе(Buyer, СписокОшибок)
	
	Если ЗначениеЗаполнено(Buyer)
		И СтруктураЗаполнена(Buyer) Тогда
		
		ОшибкиКонтрагента = ВалидацияСчетаФНС29_ПроверитьСведенияОбУчастнике(Buyer);
		
		ОшибкиАдреса = ВалидацияСчетаФНС29_ПроверитьСтруктуруАдреса(Buyer.Address);
		Если ЗначениеЗаполнено(ОшибкиАдреса) Тогда 
			ДобавитьОшибкуВалидацииСОформлением(ОшибкиКонтрагента, "Ошибки в юридическом адресе:", , "b");
			ДополнитьМассивОшибокВалидации(ОшибкиКонтрагента, ОшибкиАдреса);
		КонецЕсли; 
		
		Если ВалидацияСчетаФНС29_ЕстьСведенияОБанковскомСчете(Buyer) Тогда
			ОшибкиБанковскогоСчета = ВалидацияСчетаФНС29_ПроверитьСведенияОБанковскомСчете(Buyer);
			ДополнитьМассивОшибокВалидации(ОшибкиКонтрагента, ОшибкиБанковскогоСчета);
		КонецЕсли;
		
		ОрганизацияСсылка = ПолучитьЗначениеXDTO(Buyer.СлужебнаяИнформация, "Ссылка");
		
		ДобавитьГруппуОшибокВалидации(
			СписокОшибок,
			ОшибкиКонтрагента,
			"Ошибки в сведениях о <покупателе>",
			ОрганизацияСсылка);
	
	Иначе
		
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствуют сведения о покупателе");
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ВалидацияСчетаФНС29_ПроверитьСведенияОГрузоотправителе(Shipper, СписокОшибок)
	
	Если ЗначениеЗаполнено(Shipper)
		И СтруктураЗаполнена(Shipper) Тогда
		
		ОшибкиКонтрагента = ВалидацияСчетаФНС29_ПроверитьСведенияОбУчастнике(Shipper);
		
		ОшибкиАдреса = ВалидацияСчетаФНС29_ПроверитьСтруктуруАдреса(Shipper.Address);
		Если ЗначениеЗаполнено(ОшибкиАдреса) Тогда 
			ДобавитьОшибкуВалидацииСОформлением(ОшибкиКонтрагента, "Ошибки в фактическом адресе:", , "b");
			ДополнитьМассивОшибокВалидации(ОшибкиКонтрагента, ОшибкиАдреса);
		КонецЕсли; 
		
		Если ВалидацияСчетаФНС29_ЕстьСведенияОБанковскомСчете(Shipper) Тогда
			ОшибкиБанковскогоСчета = ВалидацияСчетаФНС29_ПроверитьСведенияОБанковскомСчете(Shipper);
			ДополнитьМассивОшибокВалидации(ОшибкиКонтрагента, ОшибкиБанковскогоСчета);
		КонецЕсли;
		
		ОрганизацияСсылка = ПолучитьЗначениеXDTO(Shipper.СлужебнаяИнформация, "Ссылка");
		
		ДобавитьГруппуОшибокВалидации(
			СписокОшибок,
			ОшибкиКонтрагента,
			"Ошибки в сведениях о <грузоотправителе>",
			ОрганизацияСсылка);
		
	КонецЕсли;

КонецПроцедуры

Процедура ВалидацияСчетаФНС29_ПроверитьСведенияОГрузополучателе(Consignee, СписокОшибок)
	
	Если ЗначениеЗаполнено(Consignee)
		И СтруктураЗаполнена(Consignee) Тогда
		
		ОшибкиКонтрагента = ВалидацияСчетаФНС29_ПроверитьСведенияОбУчастнике(Consignee);
		
		ОшибкиАдреса = ВалидацияСчетаФНС29_ПроверитьСтруктуруАдреса(Consignee.Address);
		Если ЗначениеЗаполнено(ОшибкиАдреса) Тогда 
			ДобавитьОшибкуВалидацииСОформлением(ОшибкиКонтрагента, "Ошибки в фактическом адресе:", , "b");
			ДополнитьМассивОшибокВалидации(ОшибкиКонтрагента, ОшибкиАдреса);
		КонецЕсли; 
		
		Если ВалидацияСчетаФНС29_ЕстьСведенияОБанковскомСчете(Consignee) Тогда
			ОшибкиБанковскогоСчета = ВалидацияСчетаФНС29_ПроверитьСведенияОБанковскомСчете(Consignee);
			ДополнитьМассивОшибокВалидации(ОшибкиКонтрагента, ОшибкиБанковскогоСчета);
		КонецЕсли;
		
		ОрганизацияСсылка = ПолучитьЗначениеXDTO(Consignee.СлужебнаяИнформация, "Ссылка");
		
		ДобавитьГруппуОшибокВалидации(
			СписокОшибок,
			ОшибкиКонтрагента,
			"Ошибки в сведениях о <грузополучателе>",
			ОрганизацияСсылка);
		
	КонецЕсли;

КонецПроцедуры

Процедура ВалидацияСчетаФНС29_ПроверитьСведенияОПлательщике(Payer, СписокОшибок)

	Если ЗначениеЗаполнено(Payer)
		И СтруктураЗаполнена(Payer) Тогда
		
		ОшибкиКонтрагента = ВалидацияСчетаФНС29_ПроверитьСведенияОбУчастнике(Payer);
		
		ОшибкиАдреса = ВалидацияСчетаФНС29_ПроверитьСтруктуруАдреса(Payer.Address);
		Если ЗначениеЗаполнено(ОшибкиАдреса) Тогда 
			ДобавитьОшибкуВалидацииСОформлением(ОшибкиКонтрагента, "Ошибки в юридическом адресе:", , "b");
			ДополнитьМассивОшибокВалидации(ОшибкиКонтрагента, ОшибкиАдреса);
		КонецЕсли; 
		
		Если ВалидацияСчетаФНС29_ЕстьСведенияОБанковскомСчете(Payer) Тогда
			ОшибкиБанковскогоСчета = ВалидацияСчетаФНС29_ПроверитьСведенияОБанковскомСчете(Payer);
			ДополнитьМассивОшибокВалидации(ОшибкиКонтрагента, ОшибкиБанковскогоСчета);
		КонецЕсли;
		
		ОрганизацияСсылка = ПолучитьЗначениеXDTO(Payer.СлужебнаяИнформация, "Ссылка");
		
		ДобавитьГруппуОшибокВалидации(
			СписокОшибок,
			ОшибкиКонтрагента,
			"Ошибки в сведениях о <плательщике>",
			ОрганизацияСсылка);
		
	КонецЕсли;

КонецПроцедуры

Процедура ВалидацияСчетаФНС29_ПроверитьСведенияОПолучателеДенежныхСредств(MoneyRecipient, СписокОшибок)
	
	Если ЗначениеЗаполнено(MoneyRecipient)
		И СтруктураЗаполнена(MoneyRecipient) Тогда
		
		ОшибкиКонтрагента = ВалидацияСчетаФНС29_ПроверитьСведенияОбУчастнике(MoneyRecipient);
		
		ОшибкиАдреса = ВалидацияСчетаФНС29_ПроверитьСтруктуруАдреса(MoneyRecipient.Address);
		Если ЗначениеЗаполнено(ОшибкиАдреса) Тогда 
			ДобавитьОшибкуВалидацииСОформлением(ОшибкиКонтрагента, "Ошибки в юридическом адресе:", , "b");
			ДополнитьМассивОшибокВалидации(ОшибкиКонтрагента, ОшибкиАдреса);
		КонецЕсли; 
		
		ОшибкиБанковскогоСчета = ВалидацияСчетаФНС29_ПроверитьСведенияОБанковскомСчете(MoneyRecipient);
		ДополнитьМассивОшибокВалидации(ОшибкиКонтрагента, ОшибкиБанковскогоСчета);
		
		ОрганизацияСсылка = ПолучитьЗначениеXDTO(MoneyRecipient.СлужебнаяИнформация, "Ссылка");
		
		ДобавитьГруппуОшибокВалидации(
			СписокОшибок,
			ОшибкиКонтрагента,
			"Ошибки в сведениях о <получателе денежных средств>",
			ОрганизацияСсылка);
		
	КонецЕсли;

КонецПроцедуры

Процедура ВалидацияСчетаФНС29_ПроверитьТабличныеДанныеДокумента(Table, СписокОшибок)
	
	Если Не ЗначениеЗаполнено(Table)
		Или Не ЗначениеЗаполнено(Table.Items) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Отсутствует табличная часть документа");
	КонецЕсли;
	
	НомерСтроки = 1;
	
	ВсеСтрокиСНулевойСуммойНДС = Истина;
	
	Для Каждого СтрокаТЧ Из Table.Items Цикл
		
		ОшибкиВСтроке = ВалидацияСчетаФНС29_ПроверитьДанныеВСтрокеТаблицы(СтрокаТЧ);
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиВСтроке, "Ошибки в строке " + НомерСтроки + " табличной части");
		
		ВСтрокеНулеваяСуммаНДС = Не TaxRate_ЭтоЗначениеБезНДС(СтрокаТЧ.TaxRate)
			И (TaxRate_ЭтоЗначение_0(СтрокаТЧ.TaxRate)
				Или TaxRate_ЭтоЗначениеИсчНалАг(СтрокаТЧ.TaxRate));
			
		Если Не ВСтрокеНулеваяСуммаНДС Тогда
			ВсеСтрокиСНулевойСуммойНДС = Ложь;
		КонецЕсли;
			
		НомерСтроки = НомерСтроки + 1;
		
	КонецЦикла;
	
	ОшибкиИтоговыхЗначений = Новый Массив;
	
	Если Не ЗначениеЗаполнено(Table.TotalWithVatExcluded) Тогда
		ДобавитьОшибкуВалидации(ОшибкиИтоговыхЗначений, "Не заполнена итоговая стоимость товаров без НДС");
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Table.Total) Тогда
		ДобавитьОшибкуВалидации(ОшибкиИтоговыхЗначений, "Не заполнена итоговая стоимость товаров с учетом НДС");
	КонецЕсли;
	
	ДолжнаБытьИтоговаяСуммаНДС = Не ВсеСтрокиСНулевойСуммойНДС И Не Table.WithoutVat;
	
	Если ДолжнаБытьИтоговаяСуммаНДС И Не ЗначениеЗаполнено(Table.Vat) Тогда
		ДобавитьОшибкуВалидации(ОшибкиИтоговыхЗначений, "Не заполнена итоговая сумма НДС");
	КонецЕсли;
	
	ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиИтоговыхЗначений, "Ошибки в итоговых значениях по таблице товаров");
	
КонецПроцедуры

Процедура ВалидацияСчетаФНС29_ПроверитьРеквизитыОснованийВыставленияСчета(ProformaInvoiceBases, СписокОшибок)

	Для Каждого ОснованиеВыставленияСчета Из ProformaInvoiceBases Цикл
		
		Если Не СтруктураЗаполнена(ОснованиеВыставленияСчета.ProformaInvoiceBaseDocument) Тогда
			Продолжить;
		КонецЕсли;
		
		РеквизитыДокумента = ОснованиеВыставленияСчета.ProformaInvoiceBaseDocument;
		
		НаименованиеОснования = РеквизитыДокумента.DocumentName;
		НомерОснования = РеквизитыДокумента.DocumentNumber;
		ДатаОснования = РеквизитыДокумента.DocumentDate;
		
		ОшибкиОснования = Новый Массив;
		
		Если Не ЗначениеЗаполнено(ДатаОснования)
			Или Не ЗначениеЗаполнено(НомерОснования) Тогда
			
			ДобавитьОшибкуВалидации(ОшибкиОснования, "Заполните дату и номер основания/договора");
			
		КонецЕсли;
		
		// BSLLS:MagicNumber-off
		ПроверитьФорматПоля(ОшибкиОснования, НаименованиеОснования, "Наименование основания", Истина, 1, 255, Ложь, "с");
		// BSLLS:MagicNumber-on
		
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиОснования, "Ошибки в основании");
			
	КонецЦикла;
	
КонецПроцедуры

Функция ВалидацияСчетаФНС29_ПроверитьСведенияОбУчастнике(OrganizationInfo)
	
	СписокОшибок = Новый Массив;
	
	// BSLLS:MagicNumber-off
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Name, "Наименование контрагента", Истина, 0, 1000, Ложь);
	
	ПредставлениеПоля = "Краткое наименование контрагента";
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.ShortOrgName, ПредставлениеПоля, Ложь, 1, 510, Ложь);
	
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Okpo, "ОКПО контрагента", Ложь, 1, 10, Ложь);
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Okopf, "ОКОПФ контрагента", Ложь, 2, 5, Ложь);
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Department, "Структурное подразделение", Ложь, 1, 500, Ложь);
	
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.FullNameOPF, "Полное наименование ОПФ", Ложь, 4, 255, Ложь);
	
	ПредставлениеПоля = "Информация для участника документооборота";
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.AdditionalInfo, ПредставлениеПоля, Ложь, 1, 255, Ложь);
	// BSLLS:MagicNumber-on

	ВидыЮрФизЛиц = Перечисление_ВидыЮрФизЛиц();
	
	ВидКонтрагента = OrganizationInfo.Type;
	
	Если ВидКонтрагента = ВидыЮрФизЛиц.ЮридическоеЛицо Тогда
		
		ВалидацияСчетаФНС29_ПроверитьСведенияОбУчастникеКакЮрЛицо(OrganizationInfo, СписокОшибок);
		
	ИначеЕсли ВидКонтрагента = ВидыЮрФизЛиц.ИндивидуальныйПредприниматель Тогда
		
		ВалидацияСчетаФНС29_ПроверитьСведенияОбУчастникеКакИП(OrganizationInfo, СписокОшибок);
		
	ИначеЕсли ВидКонтрагента = ВидыЮрФизЛиц.ФизическоеЛицо Тогда
		
		ВалидацияСчетаФНС29_ПроверитьСведенияОбУчастникеКакФизЛице(OrganizationInfo, СписокОшибок);
		
	ИначеЕсли ВидКонтрагента = ВидыЮрФизЛиц.ИностраннаяОрганизация
		Или ВидКонтрагента = ВидыЮрФизЛиц.ИностранныйГражданин Тогда
		
		ВалидацияСчетаФНС29_ПроверитьСведенияОбИностранномУчастнике(OrganizationInfo, СписокОшибок);
			
	Иначе
		
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(
			"Неправильно указан тип контрагента (%1)",
			ВидКонтрагента);
			
		ДобавитьОшибкуВалидации(СписокОшибок, ТекстОшибки);
		
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Процедура ВалидацияСчетаФНС29_ПроверитьСведенияОбУчастникеКакЮрЛицо(OrganizationInfo, СписокОшибок)
	
	Ошибки = ПроверитьИННКонтрагента(OrganizationInfo.Inn, "ЮрЛицо");
	ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
	
	Если ЗначениеЗаполнено(OrganizationInfo.Kpp) Тогда
		Ошибки = ПроверитьКППКонтрагента(OrganizationInfo.Kpp);
		ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
	КонецЕсли;
	
КонецПроцедуры

Процедура ВалидацияСчетаФНС29_ПроверитьСведенияОбУчастникеКакИП(OrganizationInfo, СписокОшибок)
	
	Ошибки = ПроверитьИННКонтрагента(OrganizationInfo.Inn, "ФизЛицо");
	ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
	
	Если ЗначениеЗаполнено(OrganizationInfo.Kpp) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Для физ. лиц КПП не указывается");
	КонецЕсли;
	
	// BSLLS:MagicNumber-off
	Свидетельство =  OrganizationInfo.IndividualEntityRegistrationCertificate;
	ПроверитьФорматПоля(СписокОшибок, Свидетельство, "Свидетельство о гос. регистрации ИП", Ложь, 1, 100, Ложь);
	
	ОГРНИП = OrganizationInfo.IndividualEntityRegistrationNumber;
	ПроверитьФорматПоля(СписокОшибок, ОГРНИП, "ОГРНИП", Ложь, 15, 15, Истина);
	
	ИныеСведения = OrganizationInfo.OrganizationOrPersonInfo;
	ПроверитьФорматПоля(СписокОшибок, ИныеСведения, "иные сведения", Ложь, 1, 255, Ложь);
	// BSLLS:MagicNumber-on
	
	ЧастиФИО = РазложитьСтрокуВМассивСлов(OrganizationInfo.Name, " ");
	
	МинимальноеЧислоЭлементовФИО = 2;
	Если ЧастиФИО.Количество() < МинимальноеЧислоЭлементовФИО Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "не указано имя индивидуального предпринимателя");
	КонецЕсли;
	
КонецПроцедуры

Процедура ВалидацияСчетаФНС29_ПроверитьСведенияОбУчастникеКакФизЛице(OrganizationInfo, СписокОшибок)
	
	ИдРозничногоПокупателя = "0";
	ИдИногоЛица = "1";

	ЭтоРозничныйПокупатель = (OrganizationInfo.PhysicalEntityStatusID = ИдРозничногоПокупателя);
	ОбязательныеРеквизитыЗаполнены = (ЗначениеЗаполнено(OrganizationInfo.Inn)
		Или ЗначениеЗаполнено(OrganizationInfo.Name)
		Или ЗначениеЗаполнено(OrganizationInfo.OrganizationOrPersonInfo));
	
	Если Не ЭтоРозничныйПокупатель
		И Не ОбязательныеРеквизитыЗаполнены Тогда
		
		ДобавитьОшибкуВалидации(
			СписокОшибок,
			"Должно быть заполнено хотя бы одно поле для идентификации
			|физического лица: ФИО, ИНН или иные сведения");
		
	КонецЕсли;
	
	ИдСтатусаФизЛицаВалидный = (ЗначениеЗаполнено(OrganizationInfo.PhysicalEntityStatusID)
		И (OrganizationInfo.PhysicalEntityStatusID = ИдРозничногоПокупателя
		Или OrganizationInfo.PhysicalEntityStatusID = ИдИногоЛица));
		
	Если Не ИдСтатусаФизЛицаВалидный Тогда
		ДобавитьОшибкуВалидации(
			СписокОшибок,
			"указано не валидное значение в статусе контрагента (допустимо ""0"" или ""1"")");
	КонецЕсли;
	
	Если ЗначениеЗаполнено(OrganizationInfo.Inn) Тогда
		Ошибки = ПроверитьИННКонтрагента(OrganizationInfo.Inn, "ФизЛицо");
		ДобавитьОшибкуВалидации(СписокОшибок, Ошибки);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(OrganizationInfo.Kpp) Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "Для физ. лиц КПП не указывается");
	КонецЕсли;
	
	// BSLLS:MagicNumber-off
	ИныеСведения = OrganizationInfo.OrganizationOrPersonInfo;
	ПроверитьФорматПоля(СписокОшибок, ИныеСведения, "иные сведения", Ложь, 1, 255, Ложь);
	// BSLLS:MagicNumber-on
	
	ЧастиФИО = РазложитьСтрокуВМассивСлов(OrganizationInfo.Name, " ");
	
	МинимальноеЧислоЭлементовФИО = 2;
	Если ЧастиФИО.Количество() < МинимальноеЧислоЭлементовФИО Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, "не указано имя физического лица");
	КонецЕсли;
	
КонецПроцедуры

Процедура ВалидацияСчетаФНС29_ПроверитьСведенияОбИностранномУчастнике(OrganizationInfo, СписокОшибок)
	
	ОдноОбязательноеПолеЗаполнено = ЗначениеЗаполнено(OrganizationInfo.LegalEntityId)
		Или ЗначениеЗаполнено(OrganizationInfo.OrganizationOrPersonInfo);
		
	Если Не ОдноОбязательноеПолеЗаполнено Тогда
		
		ТекстОшибки = "Должно быть заполнено хотя бы одно поле для идентификации
		|иностранной организации (иностранного гражданина): налоговый номер или регистрационный номер";
		
		ДобавитьОшибкуВалидации(СписокОшибок, ТекстОшибки);
		
	КонецЕсли;
	
	// BSLLS:MagicNumber-off
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.CountryCode, "код страны", Истина, 3, 3, Истина);
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.Country, "наименование страны", Истина, 1, 255, Ложь);
	
	ИдентификаторОрганизации = OrganizationInfo.LegalEntityId;
	ПредставлениеПоля = "идентификатор иностранной организации";
	ПроверитьФорматПоля(СписокОшибок, ИдентификаторОрганизации, ПредставлениеПоля, Ложь, 1, 255, Ложь);
	
	ИныеСведения = OrganizationInfo.OrganizationOrPersonInfo;
	ПредставлениеПоля = "иные сведения для однозначной идентификации иностранной организации";
	ПроверитьФорматПоля(СписокОшибок, ИныеСведения, ПредставлениеПоля, Ложь, 1, 255, Ложь);
	// BSLLS:MagicNumber-on
	
КонецПроцедуры

Функция ВалидацияСчетаФНС29_ПроверитьСведенияОБанковскомСчете(OrganizationInfo)
	
	СписокОшибок = Новый Массив;
	
	НомерСчета = OrganizationInfo.BankAccountNumber;
	КоррСчет = OrganizationInfo.CorrespondentAccount;
	
	// BSLLS:MagicNumber-off
	ПроверитьФорматПоля(СписокОшибок, НомерСчета, "Номер банковского счета", Истина, 1, 20, Истина);
	ПроверитьФорматПоля(СписокОшибок, КоррСчет, "Корреспондентский счёт", Истина, 1, 20, Истина);
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.BankName, "Наименование банка", Ложь, 1, 1000, Ложь, "с");
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.BankId, "БИК", Истина, 9, 9, Истина);
	ПроверитьФорматПоля(СписокОшибок, OrganizationInfo.SwiftCode, "SWIFT-Код", Ложь, 8, 11, Ложь);
	// BSLLS:MagicNumber-on

	Если ЗначениеЗаполнено(OrganizationInfo.BankInn) Тогда
		ОшибкиИНН = ПроверитьИННКонтрагента(OrganizationInfo.BankInn, "ЮрЛицо");
		ДобавитьОшибкуВалидации(СписокОшибок, ОшибкиИНН);
	КонецЕсли;
	
	Если ЗначениеЗаполнено(OrganizationInfo.BankKpp) Тогда
		ОшибкиКПП = ПроверитьКППКонтрагента(OrganizationInfo.BankKpp, "ЮрЛицо");
		ДобавитьОшибкуВалидации(СписокОшибок, ОшибкиКПП);
	КонецЕсли;
	
	Возврат СписокОшибок;
	
КонецФункции

Функция ВалидацияСчетаФНС29_ПроверитьСтруктуруАдреса(Address)
	
	Результат = ВалидацияСчетаФНС29_ОбщаяПроверкаЗаполненияАдреса(Address);
	
	Если Результат.Количество() = 0 Тогда
		
		ВидАдреса = Перечисление_ВидыАдресов();
		
		// BSLLS:IfElseIfEndsWithElse-off
		Если Address.Type = ВидАдреса.АдресРФ Тогда
			Результат = Валидация_ПроверитьСтруктуруАдресаРФ(Address.AddressRF);
		ИначеЕсли Address.Type = ВидАдреса.АдресГАР Тогда
			Результат = Валидация_ПроверитьСтруктуруАдресаГАР(Address.AddressSAR);
		ИначеЕсли Address.Type = ВидАдреса.АдресСтрокой Тогда
			Результат = Валидация_ПроверитьСтруктуруАдресаСтрокой(Address.AddressText);
		КонецЕсли;
		// BSLLS:IfElseIfEndsWithElse-on
		
	КонецЕсли;
	
	Если ЕстьРеквизитИлиСвойствоОбъектаЛокальная(Address, "GLN") Тогда
		// BSLLS:MagicNumber-off
		ПроверитьФорматПоля(Результат, Address.GLN, "GLN места", Ложь, 13, 13, Ложь, "м");
		// BSLLS:MagicNumber-on
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ВалидацияСчетаФНС29_ОбщаяПроверкаЗаполненияАдреса(AddressInfo)
	
	Перем Address;
	
	Результат = Новый Массив;
	
	Если ЗначениеЗаполнено(AddressInfo) Тогда
		
		ВидАдреса = Перечисление_ВидыАдресов();
		
		// BSLLS:IfElseIfEndsWithElse-off
		Если AddressInfo.Type = ВидАдреса.АдресРФ Тогда
			Address = AddressInfo.AddressRF;
		ИначеЕсли AddressInfo.Type = ВидАдреса.АдресГАР Тогда
			Address = AddressInfo.AddressSAR;
		ИначеЕсли AddressInfo.Type = ВидАдреса.АдресСтрокой Тогда
			Address = AddressInfo.AddressText;
		КонецЕсли;
		// BSLLS:IfElseIfEndsWithElse-on
		
	КонецЕсли;
	
	Если Address = Неопределено
		Или Не СтруктураЗаполнена(Address) Тогда
		ДобавитьОшибкуВалидации(Результат, "отсутствуют сведения об адресе");
	Иначе
		// BSLLS:MagicNumber-off
		ПроверитьФорматПоля(Результат, AddressInfo.Type, "вид адреса", Истина, 0, 11, Ложь, "м");
		// BSLLS:MagicNumber-on
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция ВалидацияСчетаФНС29_ЕстьСведенияОБанковскомСчете(OrganizationInfo)

	Результат = ЗначениеЗаполнено(OrganizationInfo.BankId)
		Или ЗначениеЗаполнено(OrganizationInfo.BankAccountNumber)
		Или ЗначениеЗаполнено(OrganizationInfo.CorrespondentAccount)
		Или ЗначениеЗаполнено(OrganizationInfo.BankName)
		Или ЗначениеЗаполнено(OrganizationInfo.SwiftCode)
		Или ЗначениеЗаполнено(OrganizationInfo.BankInn)
		Или ЗначениеЗаполнено(OrganizationInfo.BankKpp);
		
	Возврат Результат;
	
КонецФункции

Функция ВалидацияСчетаФНС29_ПроверитьДанныеВСтрокеТаблицы(СтрокаТЧ)
	
	ОшибкиВСтроке = Новый Массив;
	
	ОдноОбязательноеПолеЗаполнено = ЗначениеЗаполнено(СтрокаТЧ.Product)
		Или ЗначениеЗаполнено(СтрокаТЧ.ItemVendorCode)
		Или ЗначениеЗаполнено(СтрокаТЧ.ItemCharact)
		Или ЗначениеЗаполнено(СтрокаТЧ.ItemArticle);
	
	Если НЕ ОдноОбязательноеПолеЗаполнено Тогда
		
		ДобавитьОшибкуВалидации(
			ОшибкиВСтроке,
			"Должно быть заполнено одно из полей наименование товара | характеристика | артикул | код товара");
		
	КонецЕсли;
	
	// BSLLS:MagicNumber-off
	ПроверитьФорматПоля(ОшибкиВСтроке, СтрокаТЧ.Product, "Наименование товара", Ложь, 1, 2000, Ложь, "с");
	ПроверитьФорматПоля(ОшибкиВСтроке, СтрокаТЧ.ItemVendorCode, "Код товара", Ложь, 1, 100, Ложь, "м");
	ПроверитьФорматПоля(ОшибкиВСтроке, СтрокаТЧ.ItemCharact, "Характеристика товара", Ложь, 1, 1000, Ложь, "ж");
	ПроверитьФорматПоля(ОшибкиВСтроке, СтрокаТЧ.ItemArticle, "Артикул товара", Ложь, 1, 50, Ложь, "м");
	
	ПроверитьФорматПоля(ОшибкиВСтроке, СтрокаТЧ.Country, "Код страны происхождения товара", Ложь, 3, 3, Истина, "м");
	// BSLLS:MagicNumber-on
	
	Если ЗначениеЗаполнено(СтрокаТЧ.Country) Тогда
		ПроверитьКодСтраныПоКлассификатору(СтрокаТЧ.Country, ОшибкиВСтроке);
	КонецЕсли;
	
	// BSLLS:MagicNumber-off
	ПроверитьФорматПоля(ОшибкиВСтроке, СтрокаТЧ.Unit, "Код единицы измерения", Ложь, 3, 4, Истина, "м");
	// BSLLS:MagicNumber-on
	
	Если ЗначениеЗаполнено(СтрокаТЧ.Unit) Тогда
		
		Если СокрЛП(СтрокаТЧ.Unit) = "0000" Тогда
			
			// BSLLS:MagicNumber-off
			ПроверитьФорматПоля(ОшибкиВСтроке, СтрокаТЧ.UnitName, "Наименование единицы измерения", Истина, 1, 255, Ложь, "с");
			// BSLLS:MagicNumber-on
			
		Иначе
			
			Валидация_ПроверитьЕдиницуИзмеренияПоОКЕИ(ОшибкиВСтроке, СтрокаТЧ.Unit);
			
		КонецЕсли;
		
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(СтрокаТЧ.Quantity)
		И ЗначениеЗаполнено(СтрокаТЧ.Unit) Тогда
		
		ТекстОшибки = Новый Массив;
		ТекстОшибки.Добавить("Не заполнено количество.");
		ТекстОшибки.Добавить("В соответствии с форматом ФНС количество должно быть больше ноля.");
		ТекстОшибки.Добавить("Заполните в документе 1С количество");
		
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, Общее_СоединитьСтроку(ТекстОшибки, " "));
		
	КонецЕсли;
	
	Валидация_ПроверитьСтавкуНДС(СтрокаТЧ.TaxRate, ОшибкиВСтроке);
	
	Если Не ЗначениеЗаполнено(СтрокаТЧ.SubtotalWithVatExcluded) Тогда
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Не заполнена стоимость товара без учета НДС");
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(СтрокаТЧ.Subtotal) Тогда
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Не заполнена стоимость товара с учетом НДС");
	КонецЕсли;
	
	ДолжнаБытьСуммаНДС = Не TaxRate_ЭтоЗначениеИсчНалАг(СтрокаТЧ.TaxRate)
		И Не TaxRate_ЭтоЗначение_0(СтрокаТЧ.TaxRate)
		И Не TaxRate_ЭтоЗначениеБезНДС(СтрокаТЧ.TaxRate);
		
	Если ДолжнаБытьСуммаНДС И Не ЗначениеЗаполнено(СтрокаТЧ.Vat) Тогда
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Не заполнена сумма НДС");
	КонецЕсли;
	
	Если ДолжнаБытьСуммаНДС И Не ЗначениеЗаполнено(СтрокаТЧ.VatWithoutDiscount) Тогда
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Не заполнена сумма НДС без учета скидки");
	КонецЕсли;
	
	// BSLLS:MagicNumber-off
	ПроверитьФорматПоля(ОшибкиВСтроке, СтрокаТЧ.ItemTypeCode, "Код ТН ВЭД", Ложь, 10, 10, Истина, "м");
	// BSLLS:MagicNumber-on
	
	ВалидацияСчетаФНС29_ПроверитьСведенияПоУчетуТовараВГосударственнойИнформационнойСистеме(
		СтрокаТЧ.StateSystemItemInfos,
		ОшибкиВСтроке);
	
	Возврат ОшибкиВСтроке;
	
КонецФункции

Процедура ВалидацияСчетаФНС29_ПроверитьСведенияПоУчетуТовараВГосударственнойИнформационнойСистеме(
	StateSystemItemInfos,
	ОшибкиВСтрокеТЧ)
	
	ШаблонТекстаОшибки = "Ошибки в строке %1 сведений о товаре, подлежащем учету в гос. инф. системе: "
		+ "наименование государственной информационной системы";
	
	НомерСтроки = 1;
	Для Каждого StateSystemItemInfo Из StateSystemItemInfos Цикл
		
		НаименованиеГосСистемы = StateSystemItemInfo.StateSystemName;
		
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(ШаблонТекстаОшибки, НомерСтроки);
		
		// BSLLS:MagicNumber-off
		ПроверитьФорматПоля(ОшибкиВСтрокеТЧ, НаименованиеГосСистемы, ТекстОшибки, Истина, 1, 255, Ложь, "с");
		// BSLLS:MagicNumber-on
		
		НомерСтроки = НомерСтроки + 1;
		
	КонецЦикла;
	
КонецПроцедуры

Процедура ПроверитьКодВалютыПоКлассификатору(КодВалюты, ОшибкиВалидации)
	
	НаименованиеВалюты = НаименованиеВалюты(КодВалюты, Истина);
	Если НЕ ЗначениеЗаполнено(НаименованиеВалюты) Тогда
		
		ШаблонОшибки = НСтр("ru='В классификаторе валют не найден код ""%1"". Выберите валюту из классификатора'");
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки, КодВалюты);
		
		ДобавитьОшибкуВалидации(ОшибкиВалидации, ТекстОшибки);
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ПроверитьКодСтраныПоКлассификатору(КодСтраны, ОшибкиВалидации)

	НаименованиеСтраны = НаименованиеСтраны(КодСтраны);
	Если Не ЗначениеЗаполнено(НаименованиеСтраны) Тогда
		
		ШаблонОшибки = НСтр("ru='В классификаторе стран не найден код ""%1"". Выберите страну из классификатора'");
		ТекстОшибки = Общее_ПодставитьПараметрыВСтроку(ШаблонОшибки, КодСтраны);
		
		ДобавитьОшибкуВалидации(ОшибкиВалидации, ТекстОшибки);
		
	КонецЕсли;
	
КонецПроцедуры

Функция Валидация_ПроверитьСтруктуруАдресаРФ(Address)
	
	Результат = Новый Массив;
	
	Если НЕ ЗначениеЗаполнено(Address) Тогда
		Возврат Результат;
	КонецЕсли;
	
	// BSLLS:MagicNumber-off
	ПроверитьФорматПоля(Результат, Address.ZipCode, "Индекс", Ложь, 6, 6, Истина, "м");
	
	RegionCode = Address.RegionCode;
	
	Если НЕ ЗначениеЗаполнено(RegionCode) Тогда
		ДобавитьОшибкуВалидации(Результат, "В адресе не указан код региона по классификатору");
	ИначеЕсли RegionCode = "##" Тогда
		ДобавитьОшибкуВалидации(Результат, "Код региона указан не по классификатору");
	ИначеЕсли НЕ СтрДлина(RegionCode) = 2 Тогда
		ДобавитьОшибкуВалидации(Результат, "Код региона должен иметь длину 2 символа");
	ИначеЕсли НЕ СтрокаСодержитТолькоЦифры(RegionCode) Тогда
		ДобавитьОшибкуВалидации(Результат, "Код региона должен состоять из цифр");
	КонецЕсли;
	
	ПроверитьФорматПоля(Результат, Address.Region, "наименование региона", Истина, 0, 51, Ложь, "с");
	
	ПроверитьФорматПоля(Результат, Address.Territory, "наименование района", Ложь, 0, 255, Ложь, "с");
	ПроверитьФорматПоля(Результат, Address.City, "наименование города", Ложь, 0, 255, Ложь, "с");
	ПроверитьФорматПоля(Результат, Address.Locality, "наименование населенного пункта", Ложь, 0, 255, Ложь, "с");
	ПроверитьФорматПоля(Результат, Address.Street, "наименование улицы", Ложь, 0, 255, Ложь, "с");
	
	ПроверитьФорматПоля(Результат, Address.Building, "номер дома", Ложь, 0, 20, Ложь, "м");
	ПроверитьФорматПоля(Результат, Address.Block, "номер корпуса", Ложь, 0, 20, Ложь, "м");
	ПроверитьФорматПоля(Результат, Address.Apartment, "номер квартиры", Ложь, 0, 50, Ложь, "м");
	// BSLLS:MagicNumber-on
	
	Возврат Результат;
	
КонецФункции

Функция Валидация_ПроверитьСтруктуруАдресаГАР(Address) 

	Результат = Новый Массив;
	
	Если Не ЗначениеЗаполнено(Address) Тогда
		Возврат Результат;
	КонецЕсли;
	
	// BSLLS:MagicNumber-off
	ПроверитьФорматПоля(Результат, Address.ID, "Уникальный номер адреса", Истина, 36, 36, Ложь, "м");
	
	ПроверитьФорматПоля(Результат, Address.ZipCode, "Индекс", Ложь, 6, 6, Истина, "м");
	
	RegionCode = Address.RegionCode;
	
	Если НЕ ЗначениеЗаполнено(RegionCode) Тогда 
		ДобавитьОшибкуВалидации(Результат, "В адресе не указан код региона по классификатору");
	ИначеЕсли RegionCode = "##" Тогда 
		ДобавитьОшибкуВалидации(Результат, "Код региона указан не по классификатору");
	ИначеЕсли НЕ СтрДлина(RegionCode) = 2 Тогда 
		ДобавитьОшибкуВалидации(Результат, "Код региона должен иметь длину 2 символа");
	ИначеЕсли НЕ СтрокаСодержитТолькоЦифры(RegionCode) Тогда 
		ДобавитьОшибкуВалидации(Результат, "Код региона должен состоять из цифр");
	КонецЕсли;
	
	ПроверитьФорматПоля(Результат, Address.Region, "наименование региона", Истина, 0, 51, Ложь, "с");
	
	Если RegionCode <> "99" Тогда
		
		Если Не ЗначениеЗаполнено(Address.MunicipalDistrict)
			И Не ЗначениеЗаполнено(Address.MunicipalDistrictCode) Тогда
			ДобавитьОшибкуВалидации(Результат, "Не указан муниципальный район");
		Иначе
			
			ПредставлениеПоля = "наименование муниципального района";
			ПроверитьФорматПоля(Результат, Address.MunicipalDistrict, ПредставлениеПоля, Истина, 0, 255, Ложь, "м");
			
			ПредставлениеПоля = "код вида муниципального района";
			ПроверитьФорматПоля(Результат, Address.MunicipalDistrictCode, ПредставлениеПоля, Истина, 0, 1, Ложь, "м");
			
		КонецЕсли;
		
	КонецЕсли;
	
	ПредставлениеПоля = "городское поселение/сельское поселение";
	ПроверитьФорматПоля(Результат, Address.Settlement, ПредставлениеПоля, Ложь, 0, 255, Ложь, "с");
	
	ПредставлениеПоля = "код городского поселения/сельского поселения";
	ПроверитьФорматПоля(Результат, Address.SettlementCode, ПредставлениеПоля, Ложь, 1, 1, Ложь, "с");
	
	ПроверитьФорматПоля(Результат, Address.Locality, "населенный пункт", Ложь, 0, 255, Ложь, "с");
	ПроверитьФорматПоля(Результат, Address.LocalityType, "тип населенного пункта", Ложь, 0, 50, Ложь, "с");
	
	ПредставлениеПоля = "элемент планировочной структуры";
	ПроверитьФорматПоля(Результат, Address.PlanningElement, ПредставлениеПоля, Ложь, 0, 255, Ложь, "с");
	
	ПредставлениеПоля = "тип элемента планировочной структуры";
	ПроверитьФорматПоля(Результат, Address.PlanningElementType, ПредставлениеПоля, Ложь, 0, 50, Ложь, "с");
	
	ПредставлениеПоля = "элемент уличной дорожной сети";
	ПроверитьФорматПоля(Результат, Address.StreetElement, ПредставлениеПоля, Ложь, 0, 255, Ложь, "с");
	
	ПредставлениеПоля = "тип элемента уличной дорожной сети";
	ПроверитьФорматПоля(Результат, Address.StreetElementType, ПредставлениеПоля, Ложь, 0, 50, Ложь, "с");
	
	ПроверитьФорматПоля(Результат, Address.LandPlot, "земельный участок", Ложь, 1, 50, Ложь, "с");
	
	// BSLLS:UseLessForEach-off
	Если ЗначениеЗаполнено(Address.Buildings) Тогда
		Для Каждого BuildingInfo Из Address.Buildings Цикл
			ПроверитьФорматПоля(Результат, Address.House, "номер здания/сооружения", Ложь, 0, 255, Ложь, "с");
			ПроверитьФорматПоля(Результат, Address.HouseType, "тип здания/сооружения", Ложь, 0, 50, Ложь, "с");
		КонецЦикла;
	КонецЕсли;
	// BSLLS:UseLessForEach-on
	
	ПредставлениеПоля = "номер помещения в пределах здание/сооружение";
	ПроверитьФорматПоля(Результат, Address.House, ПредставлениеПоля, Ложь, 0, 255, Ложь, "с");
	
	ПредставлениеПоля = "тип помещения в пределах здание/сооружения";
	ПроверитьФорматПоля(Результат, Address.HouseType, ПредставлениеПоля, Ложь, 0, 50, Ложь, "с");
	
	ПредставлениеПоля = "номер помещения в пределах здание/сооружение";
	ПроверитьФорматПоля(Результат, Address.Apartment, ПредставлениеПоля, Ложь, 0, 255, Ложь, "с");
	
	ПредставлениеПоля = "тип помещения в пределах здание/сооружения";
	ПроверитьФорматПоля(Результат, Address.ApartmentType, ПредставлениеПоля, Ложь, 0, 50, Ложь, "с");
	// BSLLS:MagicNumber-on
	
	Возврат Результат;
	
КонецФункции

Функция Валидация_ПроверитьСтруктуруАдресаСтрокой(Address) 
	
	Результат = Новый Массив;
	
	Если Не ЗначениеЗаполнено(Address) Тогда
		Возврат Результат;
	КонецЕсли;

	// BSLLS:MagicNumber-off
	ПроверитьФорматПоля(Результат, Address.Country, "Наименование страны", Истина, 0, 255, Ложь, "с");
	ПроверитьФорматПоля(Результат, Address.CountryCode, "Код страны", Ложь, 3, 3, Истина, "м");
	ПроверитьФорматПоля(Результат, Address.AddressText, "строковое представление адреса", Истина, 0, 1000, Ложь, "с");
	// BSLLS:MagicNumber-on
	
	Возврат Результат;
	
КонецФункции	

Процедура Валидация_ДополнитьСписокОшибкамиГТД(СписокОшибок, МассивОшибокГТД)
	
	Если МассивОшибокГТД.Количество() > 0 Тогда
		
		ОшибкиВСтроке = Новый Массив;
		
		ТекстИнструкции	= ПодставитьГиперссылкуВОшибкуВалидации("<Как заполнить?>", "ИНСТРУКЦИЯКАКЗАПОЛНИТЬГТД");
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, ТекстИнструкции);
		
		Для Каждого ОшибкаГТД Из МассивОшибокГТД Цикл
			
			Если ТипЗнч(ОшибкаГТД) = Тип("Массив") Тогда
				ДополнитьМассивОшибокВалидации(ОшибкиВСтроке, ОшибкаГТД);
			Иначе
				ДобавитьОшибкуВалидации(ОшибкиВСтроке, ОшибкаГТД);
			КонецЕсли;
			
		КонецЦикла;
		
		ДобавитьГруппуОшибокВалидации(СписокОшибок, ОшибкиВСтроке, "Ошибки в заполнении номеров ГТД");
		
	КонецЕсли;
	
КонецПроцедуры

Процедура Валидация_ПроверитьСтавкуНДС(TaxRate, ОшибкиВСтроке)
	
	// BSLLS:IfElseIfEndsWithElse-off
	Если Не ЗначениеЗаполнено(TaxRate) Тогда
		
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Ставка НДС не указана");
		
	ИначеЕсли Не TaxRate_ЭтоВалидноеЗначение(TaxRate) Тогда
		
		ДобавитьОшибкуВалидации(ОшибкиВСтроке, "Указана недопустимая ставка налога (" + TaxRate + ")");
		
	КонецЕсли;
	// BSLLS:IfElseIfEndsWithElse-on
	
КонецПроцедуры

Процедура Валидация_ПроверитьЕдиницуИзмеренияПоОКЕИ(СписокОшибок, КодЕИ)
	
	Если СокрЛП(КодЕИ) = "0000" Тогда
		Возврат;
	КонецЕсли;
	
	ТекстОшибки = "Неверно заполнена ед. измерения. Выберите ед. измерения из классификатора";
	
	ОКЕИ = ПолучитьОКЕИ();
	
	Если ОКЕИ.Найти(КодЕИ, "Код") = Неопределено Тогда
		ДобавитьОшибкуВалидации(СписокОшибок, ТекстОшибки);
	КонецЕсли;
	
КонецПроцедуры

Функция ПроверятьЗаполненностьАдресаУПД(ФорматПриказа, ContentFunction)
	
	Результат = Истина;
	
	Если ФорматПриказа = "820"
		Или ФорматПриказа = "970" Тогда
		
		ФункцияУПД = XML_УПД_ПолучитьФункцию(ContentFunction);
		Если ФункцияУПД = Перечисление_ФункцииУПД().ДОП Тогда
			Результат = Ложь;
		КонецЕсли;
	
	ИначеЕсли ФорматПриказа = "736" Тогда
		
		ФункцияУКД = XML_УКД_ПолучитьФункцию(ContentFunction);
		Если ФункцияУКД = Перечисление_ФункцииУКД().ДИС Тогда
			Результат = Ложь;
		КонецЕсли;
	
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

// Формирует описание номенклатуры по строке табличной части счетов на оплату в формате 29@.
//
// Параметры:
//  СтрокаXDTO - ОбъектXDTO - элемент коллекции Документ.ТаблСНО.СведТов
//  Идентификатор - Строка - Идентификатор номенклатуры, полученный из дополнительной информации строки ТЧ.
// 
// Возвращаемое значение:
//  Структура - см. Контракт_НоменклатураЭлектронногоДокумента.
//
Функция НоменклатураЭлектронногоСчета29(СтрокаXDTO, Идентификатор)
	
	Наименование = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимТов");
	КодТовара = ПолучитьЗначениеXDTO(СтрокаXDTO.ДопСведТов, "КодТов", "");
	Артикул = ПолучитьЗначениеXDTO(СтрокаXDTO.ДопСведТов, "АртикулТов", "");
	Характеристика = ПолучитьЗначениеXDTO(СтрокаXDTO.ДопСведТов, "ХарактерТов", "");
	Сорт = ПолучитьЗначениеXDTO(СтрокаXDTO.ДопСведТов, "СортТов", "");
	
	Результат = Новый_НоменклатураЭлектронногоДокумента(
		Идентификатор,
		Наименование,
		Артикул,
		КодТовара,
		Характеристика
	);
	
	Результат.Сорт = Сорт;
	Результат.ЕдиницаИзмерения = ПолучитьЗначениеXDTO(СтрокаXDTO, "НаимЕдИзм");
	Результат.ЕдиницаИзмеренияКод = ПолучитьЗначениеXDTO(СтрокаXDTO, "ОКЕИ_Тов");
	Результат.СтавкаНДС = ПолучитьЗначениеXDTO(СтрокаXDTO, "НалСт");
	Результат.Тип = ТипНоменклатурыНеУказан();
	
	НормализоватьПоляНоменклатурыЭлектронногоДокумента(Результат);
	
	Возврат _ФиксированныеДанные(Результат);
	
КонецФункции

Функция УПД820_КонвертироватьОбстоятельстваФормированияСчетФактуры_в970(Контент820)

	Результат = Контент820.CircumFormatInvoice;
	
	Если Не ЗначениеЗаполнено(Результат) Тогда
		
		ЕстьСведенияОДокументеОтгрузки = (Контент820.DocumentShipments.Количество() > 0);
		ВТаблицеТолькоУслуги = УПД820_ВТаблицеТолькоУслуги(Контент820.Table);
		
		Если Не ЕстьСведенияОДокументеОтгрузки И ВТаблицеТолькоУслуги Тогда
			Результат = "2";
		КонецЕсли;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Функция УПД820_ВТаблицеТолькоУслуги(Table820)

	Результат = Ложь;
	
	Если Не УПД820_ТаблицаКонтентаЗаполнена(Table820) Тогда
		Возврат Результат;
	КонецЕсли;
		
	Результат = Истина;
	
	ТипыНоменклатуры = Перечисление_ItemMarks();
	
	Для Каждого Item Из Table820.Items Цикл
		
		Если Item.ItemMark <> ТипыНоменклатуры.Услуга Тогда
			Результат = Ложь;
			Прервать;
		КонецЕсли;
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

Функция УПД820_ТаблицаКонтентаЗаполнена(Table820)

	Результат = Ложь;
	
	Результат = (Table820 <> Неопределено
		И Table820.Items.Количество() > 0);

	Возврат Результат;
	
КонецФункции

Функция УПД820_КонвертироватьНаименованиеДокумента_в970(Контент820, Контент970)
	
	ФункцияДокумента = XML_УПД_ПолучитьФункцию(Контент820.Function);
	
	ФункцииУПД = Перечисление_ФункцииУПД();
	
	Если ФункцияДокумента = ФункцииУПД.СЧФДОП Тогда
		
		Результат = Контент970.DocumentShipments[0].Name;
		
	ИначеЕсли ФункцияДокумента = ФункцииУПД.ДОП Тогда
		
		Результат = Контент820.DocumentName;
		Если Не ЗначениеЗаполнено(Результат) Тогда
			Результат = УПД_НаименованиеДокументаПоФактХЖ();
		КонецЕсли;
		
	Иначе
		
		Результат = Неопределено;
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции

Процедура УПД820_КонвертироватьСведенияОТоварах_в970(Контент970, Контент820)
	
	Table820 = Контент820.Table;
	
	Если Table820 = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	ВидыНоменклатуры = Перечисление_ItemMarks();
	
	Table970 = НовыйProtoОбъектXDTO("InvoiceTable970");
	УстановитьЗначениеXDTO(Контент970, "Table", Table970);

	ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Table970, Table820);
	
	ЭтоСчетФактураНаАванс = (Контент970.Function = Перечисление_ФункцииУПД().СЧФ
		И Контент970.CircumFormatInvoice = "2");
	
	Для Каждого Item820 Из Table820.Items Цикл
		
		Item970 = НовыйProtoОбъектXDTO("Item970");
		Table970.Items.Добавить(Item970);
		
		ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(Item970, Item820);
		
		ЭтоУслугаИлиРабота = (Item970.ItemMark = ВидыНоменклатуры.Услуга
			Или Item970.ItemMark = ВидыНоменклатуры.Работа);
		
		Если Не ЗначениеЗаполнено(Item970.Quantity) Тогда
			
			КоличествоОбязательно = Не ЭтоСчетФактураНаАванс
				И Контент820.Function <> "Basic"
				И ЭтоУслугаИлиРабота
				И ЗначениеЗаполнено(Item820.Unit);

			Если КоличествоОбязательно Тогда
				Item970.Quantity = 1;
			КонецЕсли;
			
		КонецЕсли;
		
		Если Не ЗначениеЗаполнено(Item970.Price) Тогда
			
			ЦенаОбязательна = Не ЭтоСчетФактураНаАванс
				И Контент820.Function <> "Basic"
				И ЭтоУслугаИлиРабота
				И ЗначениеЗаполнено(Item820.Unit)
				И ЗначениеЗаполнено(Item820.Quantity);

			Если ЦенаОбязательна Тогда
				
				СуммаСНДС = Item820.Subtotal;
				Если Не ЗначениеЗаполнено(СуммаСНДС) Тогда
					СуммаСНДС = 0;
				КонецЕсли;
				
				СуммаНДС = Item820.Vat;
				Если Не ЗначениеЗаполнено(СуммаНДС) Тогда
					СуммаНДС = 0;
				КонецЕсли;
				
				Item970.Price = СуммаСНДС - СуммаНДС;
					
			КонецЕсли;
				
		КонецЕсли;
		
		Для Каждого CustomDeclaration820 Из Item820.CustomDeclarations Цикл
			CustomDeclaration = НовыйProtoОбъектXDTO("CustomDeclaration");
			ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(CustomDeclaration, CustomDeclaration820);
			Item970.CustomDeclarations.Добавить(CustomDeclaration);
		КонецЦикла;
		
		ИтогоКоличествоПрослеж = 0;
		
		Для Каждого ItemTracingInfo820 Из Item820.ItemTracingInfos Цикл
			
			ItemTracingInfo = НовыйProtoОбъектXDTO("ItemTracingInfo970");
			Item970.ItemTracingInfos.Добавить(ItemTracingInfo);
			
			ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(ItemTracingInfo, ItemTracingInfo820);
			
			ИтогоКоличествоПрослеж = ИтогоКоличествоПрослеж + ItemTracingInfo.Quantity;
			
		КонецЦикла;
		
		Если ИтогоКоличествоПрослеж > 0
			И ЗначениеЗаполнено(Item970.SubtotalWithVatExcluded) Тогда
			
			ЦенаПрослеж = Item970.SubtotalWithVatExcluded / ИтогоКоличествоПрослеж;
			
			Для Каждого ItemTracingInfo Из Item970.ItemTracingInfos Цикл
				ItemTracingInfo.SumWithoutVat = Окр(ЦенаПрослеж * ItemTracingInfo.Quantity, 2);
			КонецЦикла;
			
		КонецЕсли;

		Для Каждого ItemIdentificationNumber820 Из Item820.ItemIdentificationNumbers Цикл
			
			НоваяСтрока = НовыйProtoОбъектXDTO("ItemIdentificationNumber970");
			ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(НоваяСтрока, ItemIdentificationNumber820);
			
			Для Каждого Unit Из ItemIdentificationNumber820.Units Цикл
				НоваяСтрока.Units.Добавить(Unit);
			КонецЦикла;
			
			КолвоОСУ = 0;
			ГТИН = Неопределено;
			Для Каждого PackageId Из ItemIdentificationNumber820.PackageIds Цикл
				
				Если ЭтоКодОСУ(PackageId) Тогда
					
					ДанныеОСУ = РазложитьКодОСУ(PackageId);
					КолвоОСУ = КолвоОСУ + ДанныеОСУ.Количество;
					
					Если Не ЗначениеЗаполнено(ГТИН) Тогда
						ГТИН = ДанныеОСУ.GTIN;
					КонецЕсли;
					
				Иначе
					НоваяСтрока.PackageIds.Добавить(PackageId);
				КонецЕсли;
				
			КонецЦикла;
			
			УстановитьЗначениеXDTO(Item970, "GTIN", ГТИН);
			УстановитьЗначениеXDTO(НоваяСтрока, "MarkedItemsQuantity", КолвоОСУ);
			
			Item970.ItemIdentificationNumbers.Добавить(НоваяСтрока);
			
		КонецЦикла;
		
		Для Каждого AdditionalInfoItem820 Из Item820.StructedAdditionalInfos Цикл
			AdditionalInfoItem820 = УПД820_КонвертироватьAdditionalInfoItem_в970(AdditionalInfoItem820);
			Item970.StructedAdditionalInfos.Добавить(AdditionalInfoItem820);
		КонецЦикла;
		
		ItemСлужебнаяИнформация = НовыйProtoОбъектXDTO("ItemСлужебнаяИнформация");
		ЗаполнитьЗначенияПростыхТиповОдноименныхПолейОбъектовXDTO(ItemСлужебнаяИнформация, Item820.СлужебнаяИнформация);
		УстановитьЗначениеXDTO(Item970, "СлужебнаяИнформация", ItemСлужебнаяИнформация);
		
	КонецЦикла;
	
КонецПроцедуры

Функция GTINКаноническогоВида(GTIN)
	
	Если НЕ ЗначениеЗаполнено(GTIN) Тогда
		Возврат GTIN;
	КонецЕсли;
	
	КаноничнаяДлинаGTIN = 14;
	
	Результат = Прав("000000" + GTIN, КаноничнаяДлинаGTIN);
	
	Возврат Результат;
	
КонецФункции

// Находит первое вхождение искомой строки как подстроки в исходной строке.
// Сравнение выполняется с учетом регистра.
//
// Параметры:
//  Строка - Строка - Исходная строка
//  ПодстрокаПоиска - Строка - Искомая строка
// 
// Возвращаемое значение:
//  Число - Позиция первого знака найденной подстроки.
//          Нумерация символов в строке начинается с 1.
//          Если строка не содержит указанной подстроки, то возвращается 0.
//
Функция СтрНайтиЛок(Строка, ПодстрокаПоиска)
	
	// BSLLS:DeprecatedFind-off
	Результат = Найти(Строка, ПодстрокаПоиска);
	// BSLLS:DeprecatedFind-on
	
	Возврат Результат;
	
КонецФункции

Функция TaxRate_ЭтоЗначение_0(Знач TaxRate)
	
	Результат = (TaxRate = TaxRate_0());
	Возврат Результат;
	
КонецФункции

Функция TaxRate_9_09()
	Возврат "9.09";
КонецФункции

Функция TaxRate_16_67()
	Возврат "16.67";
КонецФункции

Функция TaxRate_22()
	Возврат "22";
КонецФункции

Функция TaxRate_22_122()
	Возврат "22/122";
КонецФункции

Функция НалСт_9_09()
	Возврат "9.09%";
КонецФункции

Функция НалСт_16_67()
	Возврат "16.67%";
КонецФункции

Функция НалСт_22()
	Возврат "22%";
КонецФункции

Функция НалСт_22_122()
	Возврат "22/122";
КонецФункции

// МЕТОДЫ РАБОТЫ С УС

// Возвращает заполненный контракт УС из XML
//
// Параметры:
//  КонтентУС - Строка, ДвоичныеДанные - Данные УС в формате XML
//  КонтрактУС - Структура - Контракт УС см. Ядро.Контракт_ДанныеУниверсальногоСообщенияРасширенный()
// 
// Возвращаемое значение:
//  Структура - Заполненный контракт УС см. Ядро.Контракт_ДанныеУниверсальногоСообщенияРасширенный()
//
Функция УС_ДанныеУниверсальногоСообщенияXML_ФНС(КонтентУС, КонтрактУС) Экспорт
	// BSLLS:Typo-off
	
	ContentXDTO	= XML_В_ОбъектXDTO(КонтентУС);
	
	ДокументУС = ПолучитьЗначениеXDTO(ContentXDTO, "Документ");
	СвСобытДок = ПолучитьЗначениеXDTO(ДокументУС, "СвСобытДок");
	
	КонтрактУС.Код = ПолучитьЗначениеXDTO(СвСобытДок, "СтатусУС");
	КонтрактУС.ТекстСообщения = ПолучитьЗначениеXDTO(СвСобытДок, "ТекстУС");
	
	ОтпрУС = ПолучитьЗначениеXDTO(ДокументУС, "ОтпрУС");
	СвЛицОтпрУС = ПолучитьЗначениеXDTO(ОтпрУС, "СвЛицОтпрУС");
	КонтрактУС.Отправитель = УС_ДанныеУниверсальногоСообщенияXML_Участник(СвЛицОтпрУС, КонтрактУС.Отправитель);
	
	КонтрактУС.ИдентификаторОтправителя = ПолучитьЗначениеXDTO(ОтпрУС, "ИдОтпрУС");
	
	ПолучУС = ПолучитьЗначениеXDTO(ДокументУС, "ПолучУС");
	СвЛицПолучУС = ПолучитьЗначениеXDTO(ПолучУС, "СвЛицПолучУС");
	КонтрактУС.Получатель = УС_ДанныеУниверсальногоСообщенияXML_Участник(СвЛицПолучУС, КонтрактУС.Получатель);

	КонтрактУС.ИдентификаторПолучателя = ПолучитьЗначениеXDTO(ПолучУС, "ИдПолучУС");
	
	Возврат КонтрактУС;

	// BSLLS:Typo-on
КонецФункции

Функция УС_ДанныеУниверсальногоСообщенияXML_Участник(КонтентУС_Участник, КонтрактУС_Участник)
	// BSLLS:Typo-off

	ФИО = ПолучитьЗначениеXDTO(КонтентУС_Участник, "ФИО");
	КонтрактУС_Участник.Фамилия = ПолучитьЗначениеXDTO(ФИО, "Фамилия");
	КонтрактУС_Участник.Имя = ПолучитьЗначениеXDTO(ФИО, "Имя");
	КонтрактУС_Участник.Отчество = ПолучитьЗначениеXDTO(ФИО, "Отчество", "");
	
	КонтрактУС_Участник.Должность = ПолучитьЗначениеXDTO(КонтрактУС_Участник, "Должн", "");
	
	Возврат КонтрактУС_Участник;

	// BSLLS:Typo-on
КонецФункции
