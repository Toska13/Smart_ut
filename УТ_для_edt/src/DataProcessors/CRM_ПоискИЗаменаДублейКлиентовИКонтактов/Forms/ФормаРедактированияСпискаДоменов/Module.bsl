
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	СписокДоменов.ЗагрузитьЗначения(Параметры.МассивДоменов);
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ДобавитьДомен(Команда)
	Домен = СокрЛП(Домен);
	Если СтрДлина(Домен)>0 Тогда
		Если Лев(Домен, 1)<> "@" Тогда
			Домен = "@"+Домен;
		КонецЕсли;
		Если СписокДоменов.НайтиПоЗначению(Домен) = Неопределено Тогда
			СписокДоменов.Добавить(Домен);
		КонецЕсли;
		Домен = "";
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура КомандаЗакрыть(Команда)
	Закрыть();
КонецПроцедуры

&НаКлиенте
Процедура КомандаГотово(Команда)
	Закрыть(СписокДоменов.ВыгрузитьЗначения());
КонецПроцедуры

#КонецОбласти