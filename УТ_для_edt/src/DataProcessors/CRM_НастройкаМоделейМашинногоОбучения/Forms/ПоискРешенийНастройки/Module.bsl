
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	НастройкиПоискаРешений = CRM_МоделиМашинногоОбученияКлиентСервер.НастройкиПоискаРешений();
	ЗаполнитьЗначенияСвойств(НастройкиПоискаРешений, Параметры);
	ЗаполнитьЗначенияСвойств(ЭтотОбъект, НастройкиПоискаРешений);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ЗаписатьНастройки(Команда)
	
	НастройкиПоискаРешений = CRM_МоделиМашинногоОбученияКлиентСервер.НастройкиПоискаРешений();
	ЗаполнитьЗначенияСвойств(НастройкиПоискаРешений, ЭтотОбъект);
	Закрыть(НастройкиПоискаРешений);
	
КонецПроцедуры

#КонецОбласти

