
#Область ПрограммныйИнтерфейс

// Возвращает параметры контроля заполненности реквизитов.
// 
// Возвращаемое значение:
//  Структура - параметры контроля заполненности реквизитов.
//
Функция ПараметрыКонтроляЗаполненностиРеквизитов() Экспорт
	Возврат Константы.CRM_ПараметрыКонтроляЗаполненностиРеквизитовОбъектов.Получить().Получить();
КонецФункции // ПараметрыКонтроля()

// Возвращает проверяемые реквизиты интереса для указанного состояния интереса.
//
// Параметры:
//	СостояниеИнтереса - СправочникСсылка.CRM_СостоянияИнтересов - Состояние интереса.
// 
// Возвращаемое значение:
//  ФиксированныйМассив - проверяемые реквизиты интереса для указанного состояния интереса.
//
Функция ПроверяемыеРеквизитыИнтересаВСостоянии(СостояниеИнтереса) Экспорт
	
	ПроверяемыеРеквизитыИнтереса = СостояниеИнтереса.ПроверяемыеРеквизитыИнтереса.Получить();
	Если ПроверяемыеРеквизитыИнтереса = Неопределено Тогда
		ПроверяемыеРеквизитыИнтереса = Новый ФиксированныйМассив(Новый Массив);
	КонецЕсли;
	
	Если ТипЗнч(ПроверяемыеРеквизитыИнтереса) = Тип("Массив") Тогда
		Возврат Новый ФиксированныйМассив(ПроверяемыеРеквизитыИнтереса);
	Иначе
		Возврат ПроверяемыеРеквизитыИнтереса;
	КонецЕсли;
	
КонецФункции

// Возвращает блокируемые реквизиты интереса для указанного состояния интереса.
//
// Параметры:
//	СостояниеИнтереса - СправочникСсылка.CRM_СостоянияИнтересов - Состояние интереса.
// 
// Возвращаемое значение:
//  ФиксированныйМассив - проверяемые реквизиты интереса для указанного состояния интереса.
//
Функция БлокируемыеРеквизитыИнтересаВСостоянии(СостояниеИнтереса) Экспорт
	
	БлокируемыеРеквизитыИнтереса = СостояниеИнтереса.БлокируемыеРеквизитыИнтереса.Получить();
	Если БлокируемыеРеквизитыИнтереса = Неопределено Тогда
		БлокируемыеРеквизитыИнтереса = Новый ФиксированныйМассив(Новый Массив);
	КонецЕсли;
	
	Если ТипЗнч(БлокируемыеРеквизитыИнтереса) = Тип("Массив") Тогда
		Возврат Новый ФиксированныйМассив(БлокируемыеРеквизитыИнтереса);
	Иначе
		Возврат БлокируемыеРеквизитыИнтереса;
	КонецЕсли;
	
КонецФункции

#КонецОбласти

