
////////////////////////////////////////////////////////////////////////////////
// Классификаторы экспортные методы
//  
////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

// Обработчик для события формы ПриЧтенииНаСервере.
//
// Параметры:
//  Форма				 - ФормаКлиентскогоПриложения - Управляемая форма.
//  ОбъектСсылка		 - Ссылка - ссылка на объект формы.
//  ОбластьДобавления	 - ЭлементФормы - область добавления.
//
Процедура ФормаОбъектаПриЧтенииНаСервере(Форма, ОбъектСсылка, Знач ОбластьДобавления = Неопределено) Экспорт
	CRM_КлассификаторыВызовСервера.ФормаОбъектаПриЧтенииНаСервере(Форма, ОбъектСсылка, ОбластьДобавления);
КонецПроцедуры

// Обработчик для события формы ПослеЗаписиНаСервере.
//
// Параметры:
//  Форма - ФормаКлиентскогоПриложения - Управляемая форма.
//  Объект - ДанныеФормыКоллекция - данные объекта хранимые в форме.
//
Процедура ФормаОбъектаПослеЗаписиНаСервере(Форма, Объект) Экспорт
	CRM_КлассификаторыВызовСервера.ФормаОбъектаПослеЗаписиНаСервере(Форма, Объект);
КонецПроцедуры

// Изменяет классификатор.
//
// Параметры:
//  НаборЗаписей		 - РегистрСведенийНаборЗаписей - набор записей регистра сведений.
//  ОбъектСсылка		 - Ссылка - ссылка на объект.
//  СтрокаКлассификатора - Строка - строка классификатора.
//
Процедура ИзменитьКлассификатор(НаборЗаписей, ОбъектСсылка, СтрокаКлассификатора) Экспорт
	CRM_КлассификаторыВызовСервера.ИзменитьКлассификатор(НаборЗаписей, ОбъектСсылка, СтрокаКлассификатора);
КонецПроцедуры // ИзменитьКлассификатор()

// Получает таблицу состава классификаторов объекта.
//
// Параметры:
//  ОбъектСсылка - Ссылка - Ссылка на объект.
//  ИмяТаблицы	 - Строка - Имя таблицы.
// 
// Возвращаемое значение:
//  ТаблицаЗначений - Таблица состава классификаторов объекта.
//
Функция ТаблицаСоставаКлассификаторовОбъекта(ОбъектСсылка, ИмяТаблицы = Неопределено) Экспорт
	Возврат CRM_КлассификаторыВызовСервера.ТаблицаСоставаКлассификаторовОбъекта(ОбъектСсылка, ИмяТаблицы);
КонецФункции // ТаблицаСоставаКлассификаторовОбъекта()

// Возвращает таблицу классификаторов.
//
// Параметры:
//  ИмяТаблицы	 - Строка - Имя таблица классификаторов.
// 
// Возвращаемое значение:
//  ТаблицаЗначений - Таблица состава классификаторов.
//
Функция ТаблицаКлассификаторов(ИмяТаблицы) Экспорт
	Возврат CRM_КлассификаторыВызовСервера.ТаблицаКлассификаторов(ИмяТаблицы);
КонецФункции // ТаблицаКлассификаторов()

// Возвращает структуру строки таблицы классификаторов по значению.
//
// Параметры:
//  Значение - Ссылка - ссылка на значение классификатора.
// 
// Возвращаемое значение:
//  Структура - структура строки таблицы классификаторов.
//
Функция СтруктураСтрокиТаблицыКлассификаторовПоЗначению(Значение) Экспорт
	Возврат CRM_КлассификаторыВызовСервера.СтруктураСтрокиТаблицыКлассификаторовПоЗначению(Значение);
КонецФункции

#Область ФормированиеРеквизитовИЭлементовФормы

// Формирует теги классификаторов.
//
// Параметры:
//  Форма	 - ФормаКлиентскогоПриложения - управляемая форма.
//
Процедура СформироватьТегиКлассификаторов(Форма) Экспорт
	CRM_КлассификаторыВызовСервера.СформироватьТегиКлассификаторов(Форма);
КонецПроцедуры

// Проверяет налицие реквизита формы.
//
// Параметры:
//  Форма		 - ФормаКлиентскогоПриложения - управляемая форма.
//  ИмяРеквизита - Строка - имя реквизита.
// 
// Возвращаемое значение:
//  Булево - Истина, если существует указанный реквизит формы.
//
Функция РеквизитФормыСуществует(Форма, ИмяРеквизита) Экспорт
	Возврат CRM_КлассификаторыВызовСервера.РеквизитФормыСуществует(Форма, ИмяРеквизита);
КонецФункции // РеквизитФормыСуществует()

// Включает отбор по классификатору ВСКД.
//
// Параметры:
//  КомпоновщикНастроек	- КомпоновщикНастроек - Компоновщик настроек.
//
Процедура ВключитьОтборПоКлассификаторуВСКД(КомпоновщикНастроек) Экспорт
	CRM_КлассификаторыВызовСервера.ВключитьОтборПоКлассификаторуВСКД(КомпоновщикНастроек);
КонецПроцедуры

#КонецОбласти

#Область АналитикаКлассификаторов

// Функция - Получить значение используемой аналитики классификатора
//
// Параметры:
//  Классификатор - ПланВидовХарактеристикСсылка.CRM_Классификаторы - Классификатор.
// 
// Возвращаемое значение:
//  СправочникСсылка.НаправленияДеятельности, СправочникСсылка.СтруктураПредприятия - аналитика.
//
Функция ПолучитьЗначениеИспользуемойАналитикиКлассификатора(Классификатор) Экспорт
	Возврат CRM_КлассификаторыВызовСервера.ПолучитьЗначениеИспользуемойАналитикиКлассификатора(Классификатор);
КонецФункции // ПолучитьЗначениеИспользуемойАналитикиКлассификатора()

// Получает используемую аналитику классификатора.
//
// Параметры:
//  Пользователь			 - СправочникСсылка.Пользователи - Пользователь.
//  Классификатор			 - ПланВидовХарактеристикСсылка.CRM_Классификаторы - Классификатор.
//  ИспользуемаяАналитика	 - ПланВидовХарактеристикСсылка.CRM_АналитикаКлассификаторов - Используемая аналитика.
// 
// Возвращаемое значение:
//  СправочникСсылка.НаправленияДеятельности, СправочникСсылка.СтруктураПредприятия - аналитика.
//
Функция ПолучитьИспользуемуюАналитикуКлассификатора(Знач Пользователь, Знач Классификатор, ИспользуемаяАналитика) Экспорт
	Возврат CRM_КлассификаторыВызовСервера.ПолучитьИспользуемуюАналитикуКлассификатора(Пользователь, Классификатор, ИспользуемаяАналитика);
КонецФункции // ПолучитьИспользуемуюАналитикуКлассификатора()

// Возвращает значения используемых аналитик классификаторов.
// 
// Возвращаемое значение:
//  ПланВидовХарактеристик.CRM_АналитикаКлассификаторов - значения используемых аналитик классификаторов.
//
Функция ЗначенияИспользуемыхАналитикКлассификаторов() Экспорт
	Возврат CRM_КлассификаторыВызовСервера.ЗначенияИспользуемыхАналитикКлассификаторов();
КонецФункции // ЗначенияИспользуемыхАналитикКлассификаторов()

// Обновляет значения используемых аналитик.
//
Процедура ОбновитьЗначенияИспользуемыхАналитик() Экспорт
	CRM_КлассификаторыВызовСервера.ОбновитьЗначенияИспользуемыхАналитик();
КонецПроцедуры

// Выполняется при добавлении обработчиков установки параметров сеанса.
//
// Параметры:
//  Обработчики	 - Структура - обработчики.
//
Процедура ПриДобавленииОбработчиковУстановкиПараметровСеанса(Обработчики) Экспорт
	CRM_КлассификаторыВызовСервера.ПриДобавленииОбработчиковУстановкиПараметровСеанса(Обработчики);
КонецПроцедуры

// Устанавливает параметры сеанса.
//
// Параметры:
//  ИмяПараметра			 - Строка - Имя параметра.
//  УстановленныеПараметры	 - Структура - установленные параметры.
//
Процедура УстановкаПараметровСеанса(Знач ИмяПараметра, УстановленныеПараметры) Экспорт
	CRM_КлассификаторыВызовСервера.УстановкаПараметровСеанса(ИмяПараметра, УстановленныеПараметры);
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Функция НайтиРегламентноеЗадание(Знач УникальныйНомерЗадания) Экспорт
	Возврат CRM_КлассификаторыВызовСервера.НайтиРегламентноеЗадание(УникальныйНомерЗадания);
КонецФункции

Функция СформироватьТекстЗапросаПринадлежностиКлассификатора(Классификатор, Знач ТипЗначения = Неопределено) Экспорт
	Возврат CRM_КлассификаторыВызовСервера.СформироватьТекстЗапросаПринадлежностиКлассификатора(Классификатор, ТипЗначения);
КонецФункции

Функция ПолучитьАдресаСхемыКомпоновкиДанныхВоВременномХранилище(ЗначениеСсылка, ИмяШаблонаСКД, АдресСКД, АдресНастроекСКД, УникальныйИдентификатор) Экспорт
	Возврат CRM_КлассификаторыВызовСервера.ПолучитьАдресаСхемыКомпоновкиДанныхВоВременномХранилище(ЗначениеСсылка, ИмяШаблонаСКД, АдресСКД, АдресНастроекСКД, УникальныйИдентификатор);
КонецФункции 

Функция ПолучитьXML(Значение) Экспорт
	Возврат CRM_КлассификаторыВызовСервера.ПолучитьXML(Значение);
КонецФункции

Функция ПолучитьОписаниеИСхемуКомпоновкиДанныхПоИмениМакета(Ссылка, ИмяШаблона) Экспорт
	Возврат CRM_КлассификаторыВызовСервера.ПолучитьОписаниеИСхемуКомпоновкиДанныхПоИмениМакета(Ссылка, ИмяШаблона);
КонецФункции

Функция ПрименитьИзмененияКСхемеКомпоновкиДанных(ЗначениеСсылка, ИмяШаблонаСКД, АдресСКД, АдресНастроекСКД, УникальныйИдентификатор) Экспорт
	Возврат CRM_КлассификаторыВызовСервера.ПрименитьИзмененияКСхемеКомпоновкиДанных(ЗначениеСсылка, ИмяШаблонаСКД, АдресСКД, АдресНастроекСКД, УникальныйИдентификатор);
КонецФункции

Процедура ЗаписьЖурнала(ПараметрыЖурнала, УровеньЖурнала = Неопределено, ИмяСобытия = "", 
		Знач КомментарийСПараметрами = "", ИнформацияОбОшибке = Неопределено, 
		Параметр1 = Неопределено, Параметр2 = Неопределено, Параметр3 = Неопределено, Параметр4 = Неопределено) Экспорт
	CRM_КлассификаторыВызовСервера.ЗаписьЖурнала(ПараметрыЖурнала, УровеньЖурнала, ИмяСобытия, КомментарийСПараметрами,
		ИнформацияОбОшибке, Параметр1, Параметр2, Параметр3, Параметр4);
КонецПроцедуры

Процедура ПометитьНаУдалениеЗначенияКлассификаторов(Знач Источник) Экспорт
	CRM_КлассификаторыВызовСервера.ПометитьНаУдалениеЗначенияКлассификаторов(Источник);
КонецПроцедуры

#Область ПроцедурыФормированияИОчистки

Процедура ВыполнитьПериодическоеФормирование(Классификатор = Неопределено) Экспорт
	CRM_КлассификаторыВызовСервера.ВыполнитьПериодическоеФормирование(Классификатор);
КонецПроцедуры

Процедура Сформировать(ЗначениеСсылка, ХранитьИсториюИзменения = Ложь) Экспорт
	CRM_КлассификаторыВызовСервера.Сформировать(ЗначениеСсылка, ХранитьИсториюИзменения);
КонецПроцедуры

Процедура Очистить(ЗначениеСсылка) Экспорт
	CRM_КлассификаторыВызовСервера.Очистить(ЗначениеСсылка);
КонецПроцедуры

#КонецОбласти

#Область КлассификаторыПереопределяемый

Функция ИмяОсновногоЭлементаКлассификатора(Классификатор, ВидОтображения = Неопределено) Экспорт
	Возврат CRM_КлассификаторыВызовСервера.ИмяОсновногоЭлементаКлассификатора(Классификатор, ВидОтображения);
КонецФункции // ИмяОсновногоЭлементаКлассификатора()

Процедура ФормаОпросаПриЧтенииНаСервере(Форма, ОбъектОпроса, РеспондентСсылка, ВариантОпроса, Знач ОбластьДобавления = Неопределено) Экспорт
	CRM_КлассификаторыВызовСервера.ФормаОпросаПриЧтенииНаСервере(Форма, ОбъектОпроса, РеспондентСсылка, ВариантОпроса, ОбластьДобавления);
КонецПроцедуры

#Область ВариантыСписковКлассификаторов

Процедура ЗаполнитьИменаСписковУправленияКлассификацией(СписокПриемник, ОчиститьПриемник = Ложь) Экспорт
	CRM_КлассификаторыВызовСервера.ЗаполнитьИменаСписковУправленияКлассификацией(СписокПриемник, ОчиститьПриемник);
КонецПроцедуры

Процедура ИнициализироватьВариантыСписка(Форма, ПараметрыСписка) Экспорт
	CRM_КлассификаторыВызовСервера.ИнициализироватьВариантыСписка(Форма, ПараметрыСписка);
КонецПроцедуры

Процедура ИницализироватьНаборДанныхСписка(Форма, Знач ИдентификаторВарианта, ПараметрыСписка) Экспорт
	CRM_КлассификаторыВызовСервера.ИницализироватьНаборДанныхСписка(Форма, ИдентификаторВарианта, ПараметрыСписка);
КонецПроцедуры

#КонецОбласти

#Область ПроцедурыОбработчикиСобытийФормИМодулейОбъектовСправочниковКлассификаторов

Процедура ПриСозданииНаСервере(Форма) Экспорт
	CRM_КлассификаторыВызовСервера.ПриСозданииНаСервере(Форма);
КонецПроцедуры

Процедура ПередЗаписьюНаСервере(Форма, ТекущийОбъект) Экспорт
	CRM_КлассификаторыВызовСервера.ПередЗаписьюНаСервере(Форма, ТекущийОбъект);
КонецПроцедуры

#КонецОбласти

#Область ПодборПоКлассифкации

Процедура ОбработкаВыбораТаблицы(Форма, ИмяТаблицы, ВыбранноеЗначение, СтандартнаяОбработка) Экспорт
	CRM_КлассификаторыВызовСервера.ОбработкаВыбораТаблицы(Форма, ИмяТаблицы, ВыбранноеЗначение, СтандартнаяОбработка);
КонецПроцедуры // ОбработкаВыбора()

#КонецОбласти

Процедура ДобавитьЗакладкуКлассификацияВТиповуюФорму(Форма) Экспорт
	CRM_КлассификаторыВызовСервера.ДобавитьЗакладкуКлассификацияВТиповуюФорму(Форма);
КонецПроцедуры

#КонецОбласти

#КонецОбласти

