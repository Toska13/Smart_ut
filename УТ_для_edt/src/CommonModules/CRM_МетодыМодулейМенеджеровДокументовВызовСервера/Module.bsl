////////////////////////////////////////////////////////////////////////////////
// CRM методы модулей менеджеров документов
// 
// В данный модуль вынесены методы подсистемы CRM, вызываемые из модулей типовых объектов. 
// Выносить можно только те методы, которые не вызывают стандартные методы типового модуля или обработчики форм. 
// Т.е. вызывают только те методы, что тоже вынесены из типового или не содержат таких вызовов. 
////////////////////////////////////////////////////////////////////////////////

#Область ПрограммныйИнтерфейс

// Возвращает данные печати коммерческого предложения.
//
// Параметры:
//	СтруктураДокументыИВариант - Структура - Структура, содержащая документы и вариант.
//	МассивИменМакетов		   - Массив - Массив имен макетов печати.
// 
// Возвращаемое значение:
//	Структура - структура данных печати коммерческого предложения.
//	
Функция КП_ПолучитьДанныеПечати(знач СтруктураДокументыИВариант, знач МассивИменМакетов) Экспорт
	
	Возврат CRM_МетодыМодулейМенеджеровДокументов.КП_ПолучитьДанныеПечати(СтруктураДокументыИВариант, МассивИменМакетов);
	
КонецФункции

// Возвращает данные печати интереса.
//
// Параметры:
//	СтруктураДокументыИВариант - Структура - Структура, содержащая документы и вариант.
//	МассивИменМакетов		   - Массив - Массив имен макетов печати.
// 
// Возвращаемое значение:
//	Структура - структура данных печати интереса.
//	
Функция Интерес_ПолучитьДанныеПечати(знач СтруктураДокументыИВариант, знач МассивИменМакетов) Экспорт
	
	Возврат CRM_МетодыМодулейМенеджеровДокументов.Интерес_ПолучитьДанныеПечати(СтруктураДокументыИВариант, МассивИменМакетов);
	
КонецФункции


#КонецОбласти
