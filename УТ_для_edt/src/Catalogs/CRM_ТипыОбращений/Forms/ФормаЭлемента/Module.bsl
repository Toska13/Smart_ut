
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Для Сч = 0 По 15 Цикл
		ЭтотОбъект["Картинка" + Сч] = Сч;
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовФормы

&НаКлиенте
Процедура НастроитьКартинку(Элемент, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	Объект.ИндексКартинки = ЭтотОбъект[Элемент.Имя];
	Элементы.ВыбратьКартинку.Скрыть();
	
КонецПроцедуры

#КонецОбласти
