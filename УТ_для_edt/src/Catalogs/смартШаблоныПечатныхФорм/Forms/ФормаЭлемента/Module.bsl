
&НаКлиенте
Процедура ВыделитьВсе(Команда)
	
	Для Каждого СтрМодуль Из ЭтаФорма.Объект.Модули Цикл
		СтрМодуль.Используется = Истина;	
	КонецЦикла;	
	
КонецПроцедуры

&НаКлиенте
Процедура СнятьВсе(Команда)

	Для Каждого СтрМодуль Из ЭтаФорма.Объект.Модули Цикл
		СтрМодуль.Используется = Ложь;	
	КонецЦикла;	

КонецПроцедуры
