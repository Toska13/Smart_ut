#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

// Процедура - Заполняет реквизит УспешностьЗавершения.
//
Процедура ЗаполнитьРеквизитУспешностьЗавершения() Экспорт
	ВариантУспешно = Справочники.CRM_ВариантыЗавершенияБизнесПроцесса.Успешно.ПолучитьОбъект();
	ВариантУспешно.УспешностьЗавершения = Истина;
	ВариантУспешно.Записать();
	ВариантНеудачно = Справочники.CRM_ВариантыЗавершенияБизнесПроцесса.Неудачно.ПолучитьОбъект();
	ВариантНеудачно.УспешностьЗавершения = Ложь;
	ВариантНеудачно.Записать();
КонецПроцедуры

#КонецОбласти

#КонецЕсли