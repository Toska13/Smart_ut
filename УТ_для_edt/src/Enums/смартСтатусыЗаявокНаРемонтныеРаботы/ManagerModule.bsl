
//++ #1940 смарт: Мосолов 18.09.2024 //Изменения статусов в Заявках на ремонт Представительствами. 
Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
	
	ТекущийПользователь = ПараметрыСеанса.АвторизованныйПользователь;
	СписокПользователейСПравами = Справочники.смартПредопределенныеЗначения.ПользователиДляОповещенияОЗаявкахНаРемонтныеРаботы.Значения.Выгрузить().ВыгрузитьКолонку("Значение");
	
	Если НЕ (РольДоступна("ПолныеПрава") ИЛИ СписокПользователейСПравами.Найти(ТекущийПользователь) <> Неопределено) Тогда
		
		СтандартнаяОбработка = Ложь;
		
		МассивИсключаемыхЗначений = Новый Массив;
		МассивИсключаемыхЗначений.Добавить(Перечисления.смартСтатусыЗаявокНаРемонтныеРаботы.НоваяЗаявка);
		
		ОбщегоНазначенияУТВызовСервера.ДоступныеДляВыбораЗначенияПеречисления(
			"смартСтатусыЗаявокНаРемонтныеРаботы",
			ДанныеВыбора,
			Параметры,
			МассивИсключаемыхЗначений);  
		
	КонецЕсли;
	
КонецПроцедуры //-- #1940 смарт: Мосолов 18.09.2024  

