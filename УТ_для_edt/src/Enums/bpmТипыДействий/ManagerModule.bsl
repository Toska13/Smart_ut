#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
	Если Параметры.Свойство("Событие") Тогда
		Если Параметры.Событие = Перечисления.bpmСобытияЭтапа.ПриПринятииКИсполнению Тогда
			СтандартнаяОбработка = Ложь;
			СписокДляВыбора = Новый СписокЗначений;
			СписокДляВыбора.Добавить(Перечисления.bpmТипыДействий.Оповещение);
			СписокДляВыбора.Добавить(Перечисления.bpmТипыДействий.ОповещениеСМС);
			СписокДляВыбора.Добавить(Перечисления.bpmТипыДействий.ПроверкаУсловия);
			ДанныеВыбора = СписокДляВыбора;
		ИначеЕсли Параметры.Событие = Перечисления.bpmСобытияЭтапа.ПриСозданииЗадачи Тогда
			СтандартнаяОбработка = Ложь;
			СписокДляВыбора = Новый СписокЗначений;
			СписокДляВыбора.Добавить(Перечисления.bpmТипыДействий.Оповещение);
			СписокДляВыбора.Добавить(Перечисления.bpmТипыДействий.ОповещениеСМС);
			ДанныеВыбора = СписокДляВыбора;
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры

#КонецОбласти

#КонецЕсли