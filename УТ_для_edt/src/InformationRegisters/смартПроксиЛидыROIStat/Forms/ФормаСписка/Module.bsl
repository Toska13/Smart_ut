
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда // Возврат при получении формы для анализа.
		Возврат;
	КонецЕсли;
	
	УстановитьФильтрНеИспользовать();
	
КонецПроцедуры

&НаКлиенте
Процедура СписокВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	Если Поле.Имя = "НеИспользовать" Тогда
		
		СтандартнаяОбработка = Ложь;
	
		СписокВыборНаСервере(Элемент.ТекущиеДанные.ИдентификаторROIStat);
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура НеПоказыватьНеиспользуемыеПриИзменении(Элемент)
	
	УстановитьФильтрНеИспользовать();
	
КонецПроцедуры

#КонецОбласти

#Область ПрочиеПроцедуры

Процедура УстановитьФильтрНеИспользовать()

	Если НеПоказыватьНеиспользуемые Тогда
		ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(Список, "НеИспользовать", Ложь, ВидСравненияКомпоновкиДанных.Равно, , Истина);
	Иначе	
		ОбщегоНазначенияКлиентСервер.УстановитьЭлементОтбораДинамическогоСписка(Список, "НеИспользовать", , , , Ложь);
	КонецЕсли;
	
КонецПроцедуры	

&НаСервере
Процедура СписокВыборНаСервере(ИдентификаторROIStat)
	
	НаборЗаписей = РегистрыСведений.смартПроксиЛидыROIStat.СоздатьНаборЗаписей();
	НаборЗаписей.Отбор.ИдентификаторROIStat.Установить(ИдентификаторROIStat);
	НаборЗаписей.Прочитать();
	
	НаборЗаписей[0].НеИспользовать = НЕ НаборЗаписей[0].НеИспользовать;
	НаборЗаписей.Записать(Истина);
	
	Элементы.Список.Обновить();
	
КонецПроцедуры

#КонецОбласти



