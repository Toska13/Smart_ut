
#Область ПеренесеноСДоработками

//++ #1152 смарт: Лопато 18.10.2024 //Партнеры. Не контролируется уникальность КИ при изменении.  
&НаСервере
&ИзменениеИКонтроль("ПриСозданииНаСервере")
Процедура смарт_ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	Если Параметры.Свойство("ПараметрыНового") Тогда
		Если Параметры.ПараметрыНового.Свойство("ЭтоКонтактныйКлиент") И Параметры.ПараметрыНового.ЭтоКонтактныйКлиент = Истина Тогда
			Заголовок						= НСтр("ru='Регистрация новой связи с юридическим лицом';en='Logging of new communication with the legal entity'");
			Элементы.ГруппаСтраниц.ТекущаяСтраница	= Элементы.СтраницаКонтактногоКлиента;
			ЭтоКонтактныйКлиент						= Параметры.ПараметрыНового.ЭтоКонтактныйКлиент;
			КонтактныйКлиент						= Параметры.ПараметрыНового.КонтактныйКлиент;
			Состояние								= Перечисления.CRM_Состояние.Работает;
			ЗаполнитьПоФизЛицу();
		Иначе
			Элементы.ГруппаСтраниц.ТекущаяСтраница = Элементы.СтраницаКонтактногоЛица;
			Наименование			= Параметры.ПараметрыНового.Наименование;
			ВидEmailКонтактногоЛица	= Справочники.ВидыКонтактнойИнформации.EmailКонтактногоЛица;
			EmailКонтактногоЛица	= Параметры.ПараметрыНового.АдресПочты;
		КонецЕсли;
	КонецЕсли;
	// +Софтфон
	Если Параметры.Свойство("Основание") И ТипЗнч(Параметры.Основание) = Тип("Структура") Тогда
		Если Параметры.Основание.Свойство("Интерес") Тогда
			сфпИнтерес = Параметры.Основание.Интерес;
		КонецЕсли;	
		Клиент = ?(Параметры.Основание.Свойство("Владелец"),Параметры.Основание.Владелец, ПредопределенноеЗначение("Справочник.Партнеры.ПустаяСсылка"));
		Если Параметры.Основание.Свойство("НомерТелефона") Тогда
			Телефон = сфпСофтФонПроСервер.сфпУбратьИзНомераТелефонаВсеПрефиксы(Параметры.Основание.НомерТелефона);		
			СтруктураНомера = сфпСофтФонПроСервер.сфпЗаполнитьСтруктуруПолейПоПредставлениюТелефон(Телефон);
			Если Найти(СтруктураНомера.КодГорода, "9") = 1 Тогда
				ВидТелефонаКонтактногоЛица = ПредопределенноеЗначение("Справочник.ВидыКонтактнойИнформации.МобильныйТелефонКонтактногоЛица");
			Иначе	
				ВидТелефонаКонтактногоЛица = ПредопределенноеЗначение("Справочник.ВидыКонтактнойИнформации.ТелефонКонтактногоЛица");
			КонецЕсли;			
			Если ЗначениеЗаполнено(СтруктураНомера.КодГорода) Тогда
				КодГородаКонтактногоЛица = СтруктураНомера.КодГорода;
			КонецЕсли;	
			Если ЗначениеЗаполнено(СтруктураНомера.КодСтраны) Тогда
				КодСтраныКонтактногоЛица = СтруктураНомера.КодСтраны;
			КонецЕсли;				
			ТелефонКонтактногоЛица = СтруктураНомера.НомерТелефона;
		КонецЕсли;					
	КонецЕсли;		
	// +Софтфон

 	#Вставка
	//++ #1152 смарт: Лопато 18.10.2024 //Партнеры. Не контролируется уникальность КИ при изменении.  
	ЭтаФорма.Элементы.ТелефонКонтактногоЛица.УстановитьДействие("ПриИзменении", "смартПриИзмененииТелефона"); 
	//-- #1152 смарт: Лопато 18.10.2024
	#КонецВставки
	
КонецПроцедуры

//++ #1152 смарт: Лопато 18.10.2024 //Партнеры. Не контролируется уникальность КИ при изменении.  
&НаКлиенте
&ИзменениеИКонтроль("EmailКонтактногоЛицаПриИзменении")
Процедура смарт_EmailКонтактногоЛицаПриИзменении(Элемент)

	РезультатПроверки = CRM_ОбщегоНазначенияКлиентСервер.АнализАдресаЭП(СокрЛП(Элемент.ТекстРедактирования));
	Если РезультатПроверки.КодОшибки<>0 Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(РезультатПроверки.Сообщение, , Элемент.Имя);
	КонецЕсли;

	#Вставка
	//++ #1152 смарт: Лопато 18.10.2024 //Партнеры. Не контролируется уникальность КИ при изменении.  
	смартОбщегоНазначенияКлиент.ПроверитьПочтуПриИзменении(
		Элемент, ПредопределенноеЗначение("Справочник.КонтактныеЛицаПартнеров.ПустаяСсылка"), ЭтаФорма);
	//-- #1152 смарт: Лопато 18.10.2024
	#КонецВставки
	
КонецПроцедуры

#КонецОбласти

#Область ДоработкиСмарт

//++ #1152 смарт: Лопато 18.10.2024 //Партнеры. Не контролируется уникальность КИ при изменении.  
&НаКлиенте
Процедура смартПриИзмененииТелефона(Элемент) // можно вводить только цифры
		
	Строка = СокрЛП(Элемент.ТекстРедактирования);
	
	СтрокаТелефон = СокрЛП(ЭтотОбъект["КодСтраныКонтактногоЛица"])
				  + СокрЛП(ЭтотОбъект["КодГородаКонтактногоЛица"])
				  + СокрЛП(ЭтотОбъект["ТелефонКонтактногоЛица"]);
	
	СтруктураПроверки = Новый Структура;
	
	СтруктураПроверки.Вставить("Значение", 		"");
	СтруктураПроверки.Вставить("Представление", СтрокаТелефон);
	СтруктураПроверки.Вставить("Владелец", 		Неопределено);
	СтруктураПроверки.Вставить("Ссылка", ПредопределенноеЗначение("Справочник.КонтактныеЛицаПартнеров.ПустаяСсылка"));
	
	ИтогПроверки = смартОбщегоНазначения.ПроверитьТелефон(СтруктураПроверки); 
	
	Если ИтогПроверки.КодОшибки <> 0 Тогда

		ОчиститьСообщения();

		Если ИтогПроверки.КодОшибки = 12 И ИтогПроверки.Свойство("МассивДублей") Тогда
			
			ОбщегоНазначенияКлиент.СообщитьПользователю(ИтогПроверки.СообщениеОшибки, , Элемент.Имя);
			
			Для Каждого ЭлементДубль Из ИтогПроверки.МассивДублей Цикл
				ОбщегоНазначенияКлиент.СообщитьПользователю(ЭлементДубль.ТекстСообщения, ЭлементДубль.Ключ);
			КонецЦикла;
			
		Иначе	

			ОбщегоНазначенияКлиент.СообщитьПользователю(ИтогПроверки.СообщениеОшибки, , Элемент.Имя);
			
		КонецЕсли;

		ЭтотОбъект["КодСтраныКонтактногоЛица"] = "";
		ЭтотОбъект["КодГородаКонтактногоЛица"] = "";
		ЭтотОбъект["ТелефонКонтактногоЛица"] = "";
		
	КонецЕсли;
	
КонецПроцедуры //-- #1152 смарт: Лопато 18.10.2024

#КонецОбласти
