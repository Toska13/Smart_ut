
&НаСервере
&ИзменениеИКонтроль("ПриСозданииНаСервере")
Процедура смарт_ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;

	УстановитьТекстЗапросаСписка();

	ОбщегоНазначенияКлиентСервер.УстановитьПараметрДинамическогоСписка(
	Список, "НачалоТекущегоДня", НачалоДня(ТекущаяДатаСеанса()), Истина);

	УстановитьВидимость();

	// СтандартныеПодсистемы.ПодключаемыеКоманды
	ПодключаемыеКоманды.ПриСозданииНаСервере(ЭтотОбъект);
	// Конец СтандартныеПодсистемы.ПодключаемыеКоманды

	// ИнтеграцияС1СДокументооборотом
	ИнтеграцияС1СДокументооборот.ПриСозданииНаСервере(ЭтаФорма, Элементы.ГруппаГлобальныеКоманды);
	// Конец ИнтеграцияС1СДокументооборотом

	СобытияФорм.ПриСозданииНаСервере(ЭтаФорма, Отказ, СтандартнаяОбработка);

	СтандартныеПодсистемыСервер.УстановитьУсловноеОформлениеПоляДата(ЭтотОбъект, "Список.Дата", Элементы.Дата.Имя);
	
	#Вставка
	//++ #6996 смарт: Тимофеева 20.07.2026 / Создавать заказ на внутреннее потребление из заказа на перемещение
	смартФормированиеИнтерфейса.СздПоле(ЭтаФорма, "ЗаказНаВнутреннееПотребление", "Список", "Заказ на внутреннее потребление", 1, "Список.ЗаказНаВнутреннееПотребление");
	смартФормированиеИнтерфейса.СздПоле(ЭтаФорма, "смартПричинаВозврата", "Список", "Причина возврата", 1, "Список.смартПричинаВозврата");
	ДополнитьТекстЗапросаСписка();
	//-- #6996 смарт: Тимофеева 20.07.2026
	#КонецВставки
КонецПроцедуры

&НаСервере
&ИзменениеИКонтроль("УстановитьТекстЗапросаСписка")
Процедура смарт_УстановитьТекстЗапросаСписка()

	СвойстваСписка = ОбщегоНазначения.СтруктураСвойствДинамическогоСписка();
	ЗаполнитьЗначенияСвойств(СвойстваСписка, Список);

	Если ПравоДоступа("Чтение", Метаданные.РегистрыСведений.СостоянияВнутреннихЗаказов) Тогда

		СвойстваСписка.ТекстЗапроса =
		"ВЫБРАТЬ
		|	ДокументЗаказНаПеремещение.Ссылка,
		|	ДокументЗаказНаПеремещение.ПометкаУдаления,
		|	ДокументЗаказНаПеремещение.Номер,
		|	ДокументЗаказНаПеремещение.Дата,
		|	ДокументЗаказНаПеремещение.Проведен,
		|	ДокументЗаказНаПеремещение.ДлительностьПеремещения,
		|	ДокументЗаказНаПеремещение.ЖелаемаяДатаПоступления,
		|	ДокументЗаказНаПеремещение.Комментарий,
		|	ДокументЗаказНаПеремещение.МаксимальныйКодСтроки,
		|	ДокументЗаказНаПеремещение.Организация,
		|	ДокументЗаказНаПеремещение.ОрганизацияПолучатель,
		|	ДокументЗаказНаПеремещение.Ответственный,
		|	ДокументЗаказНаПеремещение.Подразделение,
		|	ДокументЗаказНаПеремещение.Сделка,
		|	ДокументЗаказНаПеремещение.СкладОтправитель,
		|	ДокументЗаказНаПеремещение.СкладПолучатель,
		|	ДокументЗаказНаПеремещение.Статус,
		|	ДокументЗаказНаПеремещение.ХозяйственнаяОперация,
		|	ДокументЗаказНаПеремещение.Назначение,
		|	ДокументЗаказНаПеремещение.ДокументОснование,
		|	ДокументЗаказНаПеремещение.СостояниеЗаполненияМногооборотнойТары,
		|	ДокументЗаказНаПеремещение.МоментВремени,
		#Вставка
		//++ #6996 смарт: Тимофеева 20.07.2026 / Создавать заказ на внутреннее потребление из заказа на перемещение
		|	ЗНАЧЕНИЕ(Документ.ЗаказНаВнутреннееПотребление.ПустаяСсылка) КАК ЗаказНаВнутреннееПотребление,
		|	ЗНАЧЕНИЕ(Перечисление.смартПричиныВозврата.ПустаяСсылка) КАК смартПричинаВозврата,
		//-- #6996 смарт: Тимофеева 20.07.2026
		#КонецВставки
		|	ВЫБОР
		|		КОГДА НЕ ДокументЗаказНаПеремещение.Проведен
		|			ТОГДА ЗНАЧЕНИЕ(Перечисление.СостоянияВнутреннихЗаказов.ПустаяСсылка)
		|		ИНАЧЕ ЕСТЬNULL(СостоянияВнутреннихЗаказов.Состояние, ЗНАЧЕНИЕ(Перечисление.СостоянияВнутреннихЗаказов.Закрыт))
		|	КОНЕЦ КАК Состояние,
		|	ЕСТЬNULL(СостоянияВнутреннихЗаказов.ЕстьРасхожденияОрдерНакладная, ЛОЖЬ) КАК ЕстьРасхожденияОрдерНакладная
		|ИЗ
		|	Документ.ЗаказНаПеремещение КАК ДокументЗаказНаПеремещение
		|		ЛЕВОЕ СОЕДИНЕНИЕ РегистрСведений.СостоянияВнутреннихЗаказов КАК СостоянияВнутреннихЗаказов
		|		ПО (СостоянияВнутреннихЗаказов.Заказ = ДокументЗаказНаПеремещение.Ссылка)";

	Иначе

		СвойстваСписка.ТекстЗапроса =
		"ВЫБРАТЬ
		|	ДокументЗаказНаПеремещение.Ссылка,
		|	ДокументЗаказНаПеремещение.ПометкаУдаления,
		|	ДокументЗаказНаПеремещение.Номер,
		|	ДокументЗаказНаПеремещение.Дата,
		|	ДокументЗаказНаПеремещение.Проведен,
		|	ДокументЗаказНаПеремещение.ДлительностьПеремещения,
		|	ДокументЗаказНаПеремещение.ЖелаемаяДатаПоступления,
		|	ДокументЗаказНаПеремещение.Комментарий,
		|	ДокументЗаказНаПеремещение.МаксимальныйКодСтроки,
		|	ДокументЗаказНаПеремещение.Организация,
		|	ДокументЗаказНаПеремещение.ОрганизацияПолучатель,
		|	ДокументЗаказНаПеремещение.Ответственный,
		|	ДокументЗаказНаПеремещение.Подразделение,
		|	ДокументЗаказНаПеремещение.Сделка,
		|	ДокументЗаказНаПеремещение.СкладОтправитель,
		|	ДокументЗаказНаПеремещение.СкладПолучатель,
		|	ДокументЗаказНаПеремещение.Статус,
		|	ДокументЗаказНаПеремещение.ХозяйственнаяОперация,
		|	ДокументЗаказНаПеремещение.Назначение,
		|	ДокументЗаказНаПеремещение.ДокументОснование,
		|	ДокументЗаказНаПеремещение.СостояниеЗаполненияМногооборотнойТары,
		|	ДокументЗаказНаПеремещение.МоментВремени
		#Вставка
		//++ #6996 смарт: Тимофеева 20.07.2026 / Создавать заказ на внутреннее потребление из заказа на перемещение
		|	,ЗНАЧЕНИЕ(Документ.ЗаказНаВнутреннееПотребление.ПустаяСсылка) КАК ЗаказНаВнутреннееПотребление,
		|	ЗНАЧЕНИЕ(Перечисление.смартПричиныВозврата.ПустаяСсылка) КАК смартПричинаВозврата,
		//-- #6996 смарт: Тимофеева 20.07.2026
		#КонецВставки
		|ИЗ
		|	Документ.ЗаказНаПеремещение КАК ДокументЗаказНаПеремещение";

	КонецЕсли;

	ОбщегоНазначения.УстановитьСвойстваДинамическогоСписка(Элементы.Список, СвойстваСписка);	
	 	
КонецПроцедуры  

Процедура ДополнитьТекстЗапросаСписка()

	СвойстваСписка = ОбщегоНазначения.СтруктураСвойствДинамическогоСписка();
	ЗаполнитьЗначенияСвойств(СвойстваСписка, Список);

	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	ЗаказНаВнутреннееПотребление.ДокументОснование КАК ДокументОснование
	|ИЗ
	|	Документ.ЗаказНаВнутреннееПотребление КАК ЗаказНаВнутреннееПотребление
	|ГДЕ
	|	ЗаказНаВнутреннееПотребление.Проведен
	|	И ЗаказНаВнутреннееПотребление.ДокументОснование ССЫЛКА Документ.ЗаказНаПеремещение
	|
	|СГРУППИРОВАТЬ ПО
	|	ЗаказНаВнутреннееПотребление.ДокументОснование
	|
	|ИМЕЮЩИЕ
	|	КОЛИЧЕСТВО(РАЗЛИЧНЫЕ ЗаказНаВнутреннееПотребление.Ссылка) > 1"; 
	РезультатЗапроса = Запрос.Выполнить();	
	Если РезультатЗапроса.Пустой() Тогда
		СвойстваСписка.ТекстЗапроса = СвойстваСписка.ТекстЗапроса + " 
		|  		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ЗаказНаВнутреннееПотребление КАК ЗаказНаВнутреннееПотребление
		|  		ПО (ДокументЗаказНаПеремещение.Ссылка = ЗаказНаВнутреннееПотребление.ДокументОснование)
		|          И ЗаказНаВнутреннееПотребление.Проведен
		|";
		СвойстваСписка.ТекстЗапроса = СтрЗаменить(СвойстваСписка.ТекстЗапроса, 
										"ЗНАЧЕНИЕ(Документ.ЗаказНаВнутреннееПотребление.ПустаяСсылка)",
										"ЕСТЬNULL(ЗаказНаВнутреннееПотребление.Ссылка, ЗНАЧЕНИЕ(Документ.ЗаказНаВнутреннееПотребление.ПустаяСсылка))");
		СвойстваСписка.ТекстЗапроса = СтрЗаменить(СвойстваСписка.ТекстЗапроса, 
										"ЗНАЧЕНИЕ(Перечисление.смартПричиныВозврата.ПустаяСсылка)",
										"ЕСТЬNULL(ЗаказНаВнутреннееПотребление.смартПричинаВозврата, ЗНАЧЕНИЕ(Перечисление.смартПричиныВозврата.ПустаяСсылка))");
	Иначе
		СвойстваСписка.ТекстЗапроса = СвойстваСписка.ТекстЗапроса + " 
		|		ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|			МАКСИМУМ(ЗаказНаВнутреннееПотребление.Ссылка) КАК ЗаказНаВнутреннееПотребление,
		|			ЗаказНаВнутреннееПотребление.ДокументОснование КАК ЗаказНаПеремещение
		|		ИЗ
		|			Документ.ЗаказНаВнутреннееПотребление КАК ЗаказНаВнутреннееПотребление
		|				ВНУТРЕННЕЕ СОЕДИНЕНИЕ (ВЫБРАТЬ
		|					ЗаказНаВнутреннееПотребление.ДокументОснование КАК ЗаказНаПеремещение,
		|					МАКСИМУМ(ЗаказНаВнутреннееПотребление.Дата) КАК Дата
		|				ИЗ
		|					Документ.ЗаказНаВнутреннееПотребление КАК ЗаказНаВнутреннееПотребление
		|				ГДЕ
		|					ЗаказНаВнутреннееПотребление.ДокументОснование ССЫЛКА Документ.ЗаказНаПеремещение
		|				    И ЗаказНаВнутреннееПотребление.Проведен
		|				СГРУППИРОВАТЬ ПО
		|					ЗаказНаВнутреннееПотребление.ДокументОснование) КАК ВложенныйЗапрос
		|				ПО ЗаказНаВнутреннееПотребление.Дата = ВложенныйЗапрос.Дата
		|		ГДЕ
		|			ЗаказНаВнутреннееПотребление.ДокументОснование ССЫЛКА Документ.ЗаказНаПеремещение
		|		    И ЗаказНаВнутреннееПотребление.Проведен
		|		СГРУППИРОВАТЬ ПО
		|			ЗаказНаВнутреннееПотребление.ДокументОснование) КАК ВложенныйЗапрос
		|		ПО (ДокументЗаказНаПеремещение.Ссылка = ВложенныйЗапрос.ЗаказНаПеремещение)
		|";
		СвойстваСписка.ТекстЗапроса = СтрЗаменить(СвойстваСписка.ТекстЗапроса, 
										"ЗНАЧЕНИЕ(Документ.ЗаказНаВнутреннееПотребление.ПустаяСсылка)",
										"ЕСТЬNULL(ВложенныйЗапрос.ЗаказНаВнутреннееПотребление, ЗНАЧЕНИЕ(Документ.ЗаказНаВнутреннееПотребление.ПустаяСсылка))");
		СвойстваСписка.ТекстЗапроса = СтрЗаменить(СвойстваСписка.ТекстЗапроса, 
										"ЗНАЧЕНИЕ(Перечисление.смартПричиныВозврата.ПустаяСсылка)",
										"ЕСТЬNULL(ВложенныйЗапрос.ЗаказНаВнутреннееПотребление.смартПричинаВозврата, ЗНАЧЕНИЕ(Перечисление.смартПричиныВозврата.ПустаяСсылка))");
	КонецЕсли;		
	
	ОбщегоНазначения.УстановитьСвойстваДинамическогоСписка(Элементы.Список, СвойстваСписка);	
	 	
КонецПроцедуры

