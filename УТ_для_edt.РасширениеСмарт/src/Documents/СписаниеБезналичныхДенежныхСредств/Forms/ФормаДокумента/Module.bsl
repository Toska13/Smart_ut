
#Область ПеренесеноСДоработками

&НаСервере
&После("ДатаПриИзмененииСервер")
Процедура смарт_ДатаПриИзмененииСерверПосле()
	//++#1308 смарт: Лопато 08.02.2023 //Новый вид налогового платежа «Единый налоговый платеж» из УТ 11.5.10.93 - Не переносить в новую конфигурацию!  
	УстановитьПараметрыВыбораТипаНалогов();
	//--#1308 смарт: Лопато 08.02.2023 - из УТ 11.5.10.93 - Не переносить в новую конфигурацию!
КонецПроцедуры 

&НаСервере
&После("ХозяйственнаяОперацияПриИзмененииСервер")
Процедура смарт_ХозяйственнаяОперацияПриИзмененииСерверПосле()
	//++#1308 смарт: Лопато 08.02.2023 //Новый вид налогового платежа «Единый налоговый платеж» из УТ 11.5.10.93 - Не переносить в новую конфигурацию!  
	УстановитьПараметрыВыбораТипаНалогов();
	//--#1308 смарт: Лопато 08.02.2023 - из УТ 11.5.10.93 - Не переносить в новую конфигурацию!
КонецПроцедуры 

&НаСервере
&После("ПриЧтенииСозданииНаСервере")
Процедура смарт_ПриЧтенииСозданииНаСервереПосле()
	//++#1308 смарт: Лопато 08.02.2023 //Новый вид налогового платежа «Единый налоговый платеж» из УТ 11.5.10.93 - Не переносить в новую конфигурацию!  
	ПроверитьКорректностьЗаполненияПлатежаВБюджет();
	УстановитьПараметрыВыбораТипаНалогов();
	//--#1308 смарт: Лопато 08.02.2023 - из УТ 11.5.10.93 - Не переносить в новую конфигурацию!
КонецПроцедуры 

&НаСервере
&После("ТипНалогаПриИзмененииНаСервере")
Процедура смарт_ТипНалогаПриИзмененииНаСервереПосле()
	//++#1308 смарт: Лопато 08.02.2023 //Новый вид налогового платежа «Единый налоговый платеж» из УТ 11.5.10.93 - Не переносить в новую конфигурацию!  
	ПроверитьКорректностьЗаполненияПлатежаВБюджет();
	//--#1308 смарт: Лопато 08.02.2023 - из УТ 11.5.10.93 - Не переносить в новую конфигурацию!
КонецПроцедуры 

&НаСервере
&После("КонтрагентПриИзмененииСервер")
Процедура смарт_КонтрагентПриИзмененииСерверПосле()
	//++#1308 смарт: Лопато 08.02.2023 //Новый вид налогового платежа «Единый налоговый платеж» из УТ 11.5.10.93 - Не переносить в новую конфигурацию!  
	ПроверитьКорректностьЗаполненияПлатежаВБюджет();
	//--#1308 смарт: Лопато 08.02.2023 - из УТ 11.5.10.93 - Не переносить в новую конфигурацию!
КонецПроцедуры 

&НаСервере
&После("БанковскийСчетКонтрагентаПриИзмененииНаСервере")
Процедура смарт_БанковскийСчетКонтрагентаПриИзмененииНаСервереПосле()
	//++#1308 смарт: Лопато 08.02.2023 //Новый вид налогового платежа «Единый налоговый платеж» из УТ 11.5.10.93 - Не переносить в новую конфигурацию!  
	ПроверитьКорректностьЗаполненияПлатежаВБюджет();
	//--#1308 смарт: Лопато 08.02.2023 - из УТ 11.5.10.93 - Не переносить в новую конфигурацию!
КонецПроцедуры 

&НаКлиенте
Процедура смарт_ДатаПроведенияБанкомПриИзмененииПосле(Элемент)
	//++#1308 смарт: Лопато 08.02.2023 //Новый вид налогового платежа «Единый налоговый платеж» из УТ 11.5.10.93 - Не переносить в новую конфигурацию!  
	УстановитьПараметрыВыбораТипаНалогов();
	//--#1308 смарт: Лопато 08.02.2023 - из УТ 11.5.10.93 - Не переносить в новую конфигурацию!
КонецПроцедуры 

&НаСервере
&После("БанковскийСчетПриИзмененииСервер")
Процедура смарт_БанковскийСчетПриИзмененииСервер(СтруктураРеквизитов, ПересчитыватьСуммы)
	//++#1308 смарт: Лопато 08.02.2023 //Новый вид налогового платежа «Единый налоговый платеж» из УТ 11.5.10.93 - Не переносить в новую конфигурацию!  
	Если ЗначениеЗаполнено(Объект.БанковскийСчет) Тогда
		УстановитьПараметрыВыбораТипаНалогов();
	КонецЕсли;
	
	ПроверитьКорректностьЗаполненияПлатежаВБюджет();
	//--#1308 смарт: Лопато 08.02.2023 - из УТ 11.5.10.93 - Не переносить в новую конфигурацию!

КонецПроцедуры 

//++ #Б-962 смарт: Лопато 23.09.2024 //Рефакторинг кода 1С. База УТ СМАРТ. Документы. СписаниеБезналичныхДенежныхСредств  
&НаСервере
&ИзменениеИКонтроль("ЗаполнитьСписокКодовВидаДохода")
Процедура смарт_ЗаполнитьСписокКодовВидаДохода()

	Элементы.КодВидаДохода.СписокВыбора.Очистить();

	#Удаление //#Б-962 смарт: Лопато 23.09.2024 //Рефакторинг кода 1С. База УТ СМАРТ. Документы. СписаниеБезналичныхДенежныхСредств
	Для каждого ЭлементСписка Из ПлатежиВБюджетКлиентСервер.КодыВидовДохода(
	#КонецУдаления
	
	#Вставка //#Б-962 смарт: Лопато 23.09.2024 //Рефакторинг кода 1С. База УТ СМАРТ. Документы. СписаниеБезналичныхДенежныхСредств
	//++#1332 смарт: Лопато 27.03.2023 //Изменения кодов для перечисления по исполнительным листам
	ТекущаяДата = ?(ЗначениеЗаполнено(Объект.Дата), Объект.Дата, ТекущаяДатаСеанса());
	//++ #1571 смарт: Мосолов 11.12.2023 //Рефакторинг. Вызов переадресован на ПлатежиВБюджетКлиентСервер.смарт_КодыВидовДохода_Расширенная
	Для Каждого ЭлементСписка Из ПлатежиВБюджетКлиентСервер.смарт_КодыВидовДохода_Расширенная(ТекущаяДата,
	//--#1571 смарт: Мосолов 11.12.2023	
	//--#1332 смарт: Лопато 27.03.2023	
	#КонецВставки
	
		Объект.ХозяйственнаяОперация = Перечисления.ХозяйственныеОперации.ВыплатаЗарплатыПоЗарплатномуПроекту
		Или Объект.ХозяйственнаяОперация = Перечисления.ХозяйственныеОперации.ВыплатаЗарплатыНаЛицевыеСчета) Цикл
		Элементы.КодВидаДохода.СписокВыбора.Добавить(ЭлементСписка.Значение, ЭлементСписка.Представление);
	КонецЦикла;

КонецПроцедуры

#КонецОбласти

#Область ДоработкиСмарт

//++#1308 смарт: Лопато 08.02.2023 //Новый вид налогового платежа «Единый налоговый платеж» из УТ 11.5.10.93 - Не переносить в новую конфигурацию!  
&НаСервере
Процедура УстановитьПараметрыВыбораТипаНалогов()
	
	ТекущаяДата = ?(ЗначениеЗаполнено(Объект.Дата), Объект.Дата, ТекущаяДатаСеанса());
	Если НЕ ПлатежиВБюджет.ИспользуетсяЕдиныйНалоговыйПлатеж(Объект.Организация, ТекущаяДата) Тогда
		Возврат;
	КонецЕсли;
	
	ДоступныеТипыНалогов = Новый Массив;
	ДоступныеТипыНалогов.Добавить(Перечисления.ТипыНалогов.ПрочиеНалогиИСборы);
	ОбщегоНазначенияКлиентСервер.ДополнитьМассив(ДоступныеТипыНалогов, ПлатежиВБюджет.ТипыНалоговУплачиваемыеОтдельно());
	
	ПараметрыВыбораНалогов = Новый Массив;
	ПараметрыВыбораНалогов.Добавить(Новый ПараметрВыбора("Отбор.Ссылка", ДоступныеТипыНалогов));
	Элементы.ТипНалога.ПараметрыВыбора = Новый ФиксированныйМассив(ПараметрыВыбораНалогов);
	
КонецПроцедуры //--#1308 смарт: Лопато 08.02.2023 - из УТ 11.5.10.93 - Не переносить в новую конфигурацию!

//++#1308 смарт: Лопато 08.02.2023 //Новый вид налогового платежа «Единый налоговый платеж» из УТ 11.5.10.93 - Не переносить в новую конфигурацию!  
&НаСервере
Процедура ПроверитьКорректностьЗаполненияПлатежаВБюджет()
	
	ТекущаяДата = ?(ЗначениеЗаполнено(Объект.Дата), Объект.Дата, ТекущаяДатаСеанса());
	ПлатежиВБюджет.ПроверитьКорректностьЗаполненияПлатежаВБюджет(ЭтотОбъект, ТекущаяДата);
	
КонецПроцедуры //--#1308 смарт: Лопато 08.02.2023 - из УТ 11.5.10.93 - Не переносить в новую конфигурацию!

#КонецОбласти