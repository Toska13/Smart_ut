
//++#1542 смарт: Лопато 03.11.2023 //Рефакторинг кода 1С. Расширение CRM. Перевести в расширение Смарт.  
//&Вместо("ПередЗаписью")
//Процедура extCRM_ПередЗаписью(Отказ, Замещение)
//	
//	Если Не ДополнительныеСвойства.Свойство("extCRM_СлужебнаяЗапись") Тогда
//		Запрос = Новый Запрос("ВЫБРАТЬ
//		|	CRM_Напоминания.Пользователь КАК Пользователь,
//		|	CRM_Напоминания.Предмет КАК Предмет,
//		|	CRM_Напоминания.ДатаНачала КАК ДатаНачала,
//		|	CRM_Напоминания.ДатаОповещения КАК ДатаОповещения
//		|ИЗ
//		|	РегистрСведений.CRM_Напоминания КАК CRM_Напоминания
//		|ГДЕ
//		|	CRM_Напоминания.Пользователь = &Пользователь
//		|	И CRM_Напоминания.Предмет = &Предмет");
//		
//		Запрос.УстановитьПараметр("Пользователь", ЭтотОбъект.Отбор.Пользователь.Значение);
//		Запрос.УстановитьПараметр("Предмет", ЭтотОбъект.Отбор.Предмет.Значение);
//		
//		Выборка = Запрос.Выполнить().Выбрать();
//		Пока Выборка.Следующий() Цикл
//			НаборЗ = РегистрыСведений.CRM_Напоминания.СоздатьНаборЗаписей();
//			НаборЗ.Отбор.Пользователь.Установить(Выборка.Пользователь);
//			НаборЗ.Отбор.Предмет.Установить(Выборка.Предмет);
//			НаборЗ.Отбор.ДатаНачала.Установить(Выборка.ДатаНачала);
//			НаборЗ.Отбор.ДатаОповещения.Установить(Выборка.ДатаОповещения);
//			НаборЗ.Прочитать();
//			Если НаборЗ.Количество() Тогда
//				НаборЗ.Очистить();
//				НаборЗ.ДополнительныеСвойства.Вставить("extCRM_СлужебнаяЗапись");
//				НаборЗ.Записать();
//			КонецЕсли;
//		КонецЦикла;
//	КонецЕсли;
//	// Вставить содержимое обработчика.
//	ПродолжитьВызов(Отказ, Замещение);
//КонецПроцедуры
//--#1542 смарт: Лопато 03.11.2023