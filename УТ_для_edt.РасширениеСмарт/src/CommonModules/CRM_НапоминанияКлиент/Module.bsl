
//++#1542 смарт: Лопато 03.11.2023 //Рефакторинг кода 1С. Расширение CRM. Перевести в расширение Смарт.  
&ИзменениеИКонтроль("ПриНачалеРаботыСистемы")
Процедура extCRM_ПриНачалеРаботыСистемы()

	Если Не CRM_НапоминанияСервер.ИспользуютсяНапоминания() Тогда
		Возврат;
	КонецЕсли;
	
	#Удаление
	ВывестиАктивныеНапоминания();
	#КонецУдаления

	ПериодОбновления = CRM_НапоминанияСервер.ПолучитьПериодОбновленияНапоминаний();
	ПодключитьОбработчикОжидания("CRM_ВывестиАктивныеНапоминания", ПериодОбновления);
	#Удаление
	ПодключитьОбработчикОповещенияВходящиеПисьма(Истина);
	#КонецУдаления
	#Вставка
	ПодключитьОбработчикОповещенияВходящиеПисьма(Ложь);
	#КонецВставки

КонецПроцедуры //--#1542 смарт: Лопато 03.11.2023

//++#1542 смарт: Лопато 03.11.2023 //Рефакторинг кода 1С. Расширение CRM. Перевести в расширение Смарт.  
&Вместо("ВывестиАктивныеНапоминания")
Процедура extCRM_ВывестиАктивныеНапоминания(НеОткрыватьДляРабочегоСтола)

	МассивОкон = ПолучитьОкна();
	Для каждого Окно из МассивОкон Цикл
		//Если Окно.НачальнаяСтраница Или Окно.Основное Тогда
		Если Окно.Основное Тогда
			Продолжить;
		КонецЕсли;
		ПродолжитьВызов(НеОткрыватьДляРабочегоСтола);
		Возврат;
	КонецЦикла;
	
КонецПроцедуры //--#1542 смарт: Лопато 03.11.2023

//++#1542 смарт: Лопато 03.11.2023 //Рефакторинг кода 1С. Расширение CRM. Перевести в расширение Смарт.  
&Вместо("ВывестиАктивныеНапоминанияОВходящихПисьмах")
Процедура extCRM_ВывестиАктивныеНапоминанияОВходящихПисьмах()

	МассивОкон = ПолучитьОкна();
	Для каждого Окно из МассивОкон Цикл
		//Если Окно.НачальнаяСтраница Или Окно.Основное Тогда
		Если Окно.Основное Тогда
			Продолжить;
		КонецЕсли;
		ПродолжитьВызов();
		Возврат;
	КонецЦикла;

КонецПроцедуры //--#1542 смарт: Лопато 03.11.2023