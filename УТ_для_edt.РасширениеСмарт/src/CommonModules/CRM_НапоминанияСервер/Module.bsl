
//++#1542 смарт: Лопато 03.11.2023 //Рефакторинг кода 1С. Расширение CRM. Перевести в расширение Смарт.  
&ИзменениеИКонтроль("ИспользуютсяНапоминания")
Функция extCRM_ИспользуютсяНапоминания()

	#Удаление
	ИспользоватьНапоминания = ПолучитьФункциональнуюОпцию("CRM_ИспользоватьНапоминанияПользователя");
	ИспользоватьCRM = CRM_ОбщегоНазначенияПовтИсп.ИспользоватьCRM();
	
	Возврат ИспользоватьНапоминания И ИспользоватьCRM;
	#КонецУдаления

	#Вставка
	Возврат ПолучитьФункциональнуюОпцию("CRM_ИспользоватьНапоминанияПользователя");
	#КонецВставки
	
КонецФункции //--#1542 смарт: Лопато 03.11.2023

//++#1542 смарт: Лопато 03.11.2023 //Рефакторинг кода 1С. Расширение CRM. Перевести в расширение Смарт.  
&Вместо("ВычислитьСрокИсполнения")
Функция extCRM_ВычислитьСрокИсполнения(ДанныеСтроки)

	extCRM_СтруктураНапоминания = Новый Структура("Важность, ДатаНачала, Предмет, Содержание, Пользователь, ДатаОповещения, Счетчик, ВидОповещения");
	ЗаполнитьЗначенияСвойств(extCRM_СтруктураНапоминания, ДанныеСтроки);
	extCRM_СтруктураНапоминания.ДатаОповещения = extCRM_СтруктураНапоминания.ДатаНачала;
	
	extCRM_Результат = ПродолжитьВызов(extCRM_СтруктураНапоминания);

	extCRM_СтрокаСоздано = НСтр("ru='Создано ';en='Created '");
	
	Возврат СтрЗаменить(extCRM_Результат, extCRM_СтрокаСоздано, "");

КонецФункции //--#1542 смарт: Лопато 03.11.2023

//++#1542 смарт: Лопато 03.11.2023 //Рефакторинг кода 1С. Расширение CRM. Перевести в расширение Смарт.  
//Смарт {[+](фрагмент ДОБАВЛЕН), Подкин Алексей Иванович 21.09.2021 14:32:49
Функция СмартСдвинутьДатуОповещения(Параметры) Экспорт
	
	Если Параметры.Свойство("Напоминания") и Параметры.Напоминания.Количество()>0 И ЗначениеЗаполнено(Параметры.ТекДата) Тогда
		ТекДата=Параметры.ТекДата;
		ЗначениеОтложенногоВремени=Параметры.ЗначениеОтложенногоВремени;
		
		Выполнено=0;
		Всего=Параметры.Напоминания.Количество();
		БылиИзмененыОповещенияОВходящихПисьмах = Ложь;
		Для Каждого Строка Из Параметры.Напоминания Цикл
			СтруктураСтроки = Новый Структура();
			СтруктураСтроки.Вставить("Пользователь",	Строка.Пользователь);
			СтруктураСтроки.Вставить("Предмет",		Строка.Предмет);
			Если ТипЗнч(Строка.Предмет) = Тип("СправочникСсылка.УчетныеЗаписиЭлектроннойПочты") И ЗначениеЗаполнено(Строка.ВидОповещения) Тогда
				СтруктураСтроки.Вставить("Содержание",		Строка.Содержание);
			Иначе	
				СтруктураСтроки.Вставить("ДатаНачала",		Строка.ДатаНачала);
				СтруктураСтроки.Вставить("ДатаОповещения",	Строка.ДатаОповещения);
			КонецЕсли;
			
			//Смарт {[*](фрагмент ИЗМЕНЕН), Подкин Алексей Иванович 21.09.2021 11:31:36 добавил передачу текущей даты сеанса,
			//изменил вызов функци КнопкаОтложитьНажатиеНаСервере на свою.
			СтруктураНапоминания = extCRM_КнопкаОтложитьНажатиеНаСервере(СтруктураСтроки, ТекДата,ЗначениеОтложенногоВремени);
			//Смарт }Подкин Алексей Иванович 21.09.2021 11:31:36
			
			Если НЕ СтруктураНапоминания.Выбран Тогда
				ОбщегоНазначения.СообщитьПользователю(НСтр("ru='Напоминание удалено или модифицировано!';en='The reminder is deleted or modified!'"));
				Продолжить;
			КонецЕсли;
			Если СтруктураНапоминания.ЭтоНапоминаниеОВходящихПисьмах Тогда
				БылиИзмененыОповещенияОВходящихПисьмах = Истина;
			КонецЕсли; 
			
			Выполнено=Выполнено+1;
			Процент = Окр(Выполнено/Всего*100,0);
			Если НЕ (Выполнено) % 10 Тогда
				ДлительныеОперации.СообщитьПрогресс(Окр(Процент,0));
			КонецЕсли;
			
		КонецЦикла;
		
	КонецЕсли; 
	
	Возврат Новый Структура("Статус,БылиИзмененыОповещенияОВходящихПисьмах",Истина,БылиИзмененыОповещенияОВходящихПисьмах) ;
	
КонецФункции //--#1542 смарт: Лопато 03.11.2023

//++#1542 смарт: Лопато 03.11.2023 //Рефакторинг кода 1С. Расширение CRM. Перевести в расширение Смарт.  
//Смарт {[+](фрагмент ДОБАВЛЕН), Подкин Алексей Иванович 21.09.2021 14:32:57
Функция extCRM_КнопкаОтложитьНажатиеНаСервере(ВыделеннаяСтрока,ТекДата,ЗначениеОтложенногоВремени)
	НоваяДатаНапоминания = ТекДата;//CRM_ОбщегоНазначенияСервер.ПолучитьТекущуюДатуСеанса();
	РегистрСведенийМенеджерЗаписи = РегистрыСведений.CRM_Напоминания.СоздатьМенеджерЗаписи();
	ЗаполнитьЗначенияСвойств(РегистрСведенийМенеджерЗаписи, ВыделеннаяСтрока);
	РегистрСведенийМенеджерЗаписи.Прочитать();
	СтруктураНапоминания = Новый Структура();
	СтруктураНапоминания.Вставить("Выбран",			РегистрСведенийМенеджерЗаписи.Выбран());
	Если РегистрСведенийМенеджерЗаписи.Выбран() Тогда
		
		СтруктураНапоминания.Вставить("ЭтоНапоминаниеОВходящихПисьмах", РегистрСведенийМенеджерЗаписи.ВидОповещения = Справочники.CRM_ВидыОповещений.ОповещатьОНовыхВходящихПисьмах);
		
		РегистрСведенийМенеджерЗаписи.ДатаОповещения = НоваяДатаНапоминания + ЗначениеОтложенногоВремени;//+ ПараметрСмещение;
		РегистрСведенийМенеджерЗаписи.Счетчик = РегистрСведенийМенеджерЗаписи.Счетчик + 1;
		РегистрСведенийМенеджерЗаписи.Записать(Истина);
		
	ИначеЕсли ТипЗнч(ВыделеннаяСтрока.Предмет) = Тип("СправочникСсылка.УчетныеЗаписиЭлектроннойПочты") Тогда
			
		МассивКлючейНапоминаний = CRM_НапоминанияСервер.ПолучитьКлючиНапоминанийОВходящихЭлектронныхПисьмахУчетнойЗаписиПользователя(ВыделеннаяСтрока.Предмет, ВыделеннаяСтрока.Пользователь);
		
		Для каждого КлючНапоминанияУчетнойЗаписи Из МассивКлючейНапоминаний Цикл
			
			РегистрСведенийМенеджерЗаписи = РегистрыСведений.CRM_Напоминания.СоздатьМенеджерЗаписи();
			ЗаполнитьЗначенияСвойств(РегистрСведенийМенеджерЗаписи, КлючНапоминанияУчетнойЗаписи);
			РегистрСведенийМенеджерЗаписи.Прочитать();
			СтруктураНапоминания.Вставить("Выбран",	РегистрСведенийМенеджерЗаписи.Выбран());
			Если РегистрСведенийМенеджерЗаписи.Выбран() Тогда
			
				Если РегистрСведенийМенеджерЗаписи.ВидОповещения = Справочники.CRM_ВидыОповещений.ОповещатьОНовыхВходящихПисьмах Тогда
							
					РегистрСведенийМенеджерЗаписи.ДатаОповещения = НоваяДатаНапоминания + ЗначениеОтложенногоВремени;// + ПараметрСмещение;
					РегистрСведенийМенеджерЗаписи.Счетчик = РегистрСведенийМенеджерЗаписи.Счетчик + 1;
					РегистрСведенийМенеджерЗаписи.Записать(Истина);
							
				КонецЕсли; 
			
			КонецЕсли;	
			
		КонецЦикла; 
		
		СтруктураНапоминания.Вставить("ЭтоНапоминаниеОВходящихПисьмах", Истина);
		
	КонецЕсли;
	Возврат СтруктураНапоминания;

КонецФункции //--#1542 смарт: Лопато 03.11.2023