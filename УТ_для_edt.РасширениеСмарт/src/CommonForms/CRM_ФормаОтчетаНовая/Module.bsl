
&НаКлиенте
&Перед("Сформировать")
//++ #1585 смарт: Фролов 11.01.2024 //Доработка отчета Воронка продаж
Процедура смарт_Сформировать()
	Попытка
		Если Заголовок="Расшифровка воронки продаж" Тогда
			Для каждого СтрНастройки Из Отчет.КомпоновщикНастроек.Настройки.Отбор.Элементы Цикл
				Если СтрНастройки.ЛевоеЗначение=Новый ПолеКомпоновкиДанных("ЭтапОтбор") ТОгда
					Заголовок=Заголовок+", этап: "+СтрНастройки.ПравоеЗначение;
				КонецЕсли;	
			КонецЦикла;
		КонецЕсли;
	Исключение
	КонецПопытки;
КонецПроцедуры //-- #1585 смарт: Фролов 11.01.2024
