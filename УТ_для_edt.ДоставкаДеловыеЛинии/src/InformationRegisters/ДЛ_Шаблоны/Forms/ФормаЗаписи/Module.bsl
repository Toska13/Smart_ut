

&НаСервере
Процедура ПриЧтенииНаСервере(ТекущийОбъект)
	
	СтруктураДанных = ТекущийОбъект.Данные.Получить();
	
	Для Каждого ДанныеСтруктуры Из СтруктураДанных Цикл
		
		ЗначениеРеквизитов.ДобавитьСтроку(ДанныеСтруктуры.Ключ + ":  " + ДанныеСтруктуры.Значение);	
		
	КонецЦикла;
	
КонецПроцедуры
